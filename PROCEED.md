# 진행 이력

kWise 프로젝트의 세션별 작업 기록. **각 세션 종료 시 클로드코드가 직접 추가한다.**

---

## 현재 상태

| 항목 | 값 |
|---|---|
| 환경 | Windows 11 (안드로이드 proot 환경에서 이관) |
| 최근 세션 | 11차 (2026-08-07) — 감도 명칭·범위 표시 + 설치 밀도 라벨 + ESS 단가·차익거래 |
| 다음 작업 | **7세션 케이스 스터디** (보완 없이 바로 시작한다) |
| 테스트 상태 | pytest 620 passed / ruff pass / mypy pass (strict, 78 files) |
| 기상 사전 취득 | **완료** — 전국 135격자 × 2023~2025, 405파일 40.4 MB, 실패 0 |
| 적용 범위 | **대한민국 전용** (요구사항서 1.4) |
| 요금표 | 8종별 (일반용 갑Ⅰ·갑Ⅱ·을 / 산업용 갑Ⅰ·갑Ⅱ·을 / 교육용 갑·을), schema 0.2 |
| 블로커 | — |

---

## 이름·목적 변경 (2026-08-06)

프로젝트명을 `peakcut` 에서 **`kWise`** 로 바꾸고 목적을 재정의했다.
태양광·피크컷에 치우친 이름과 범위에서 벗어나, 전력 비용 진단과 개선안 비교로 넓혔다.

| | 이전 | 현재 |
|---|---|---|
| 이름 | peakcut | kWise |
| 목적 | 태양광 도입 시 요금 절감 산출 | 전력 비용 구조 진단 + 개선 수단 비교 |
| 태양광 위치 | 주인공 | 개선 수단 중 하나 |
| 첫 산출물 | PV 피크 저감량 | 투자 없이 가능한 절감액 |
| UI | 업로드 → PV 입력 → 결과 | 1 진단 → 2 수단 선택 → 3 조합 비교 |
| 모듈 | io/quality/tariff/pv/analysis/report/ui | io/quality/tariff/pv/**diagnose**/**measures**/**compare**/report/ui |

근거는 샘플 데이터다. 설비 투자 없이 선택요금 전환만으로 상당한 절감이 산출됐다.
태양광부터 묻는 구조는 이 가치를 가린다.

---

## 이관 메모

안드로이드(Termux proot Debian, ARM64) 환경에서 Windows 11 로 옮겼다.
기존 환경이 자주 중단되어 개발을 지속하기 어려웠다.

**이전 환경에서 3세션까지 완료했다** (pytest 162 통과). 저장소를 통째로 옮겼다면
아래 「세션 기록」에 이전 기록이 남아 있고 1세션부터 다시 할 필요가 없다.
저장소 없이 새로 시작하는 경우, 아래 「결정 사항」에 1~3세션에서 확정한 내용이
모두 담겨 있으므로 같은 결론에 다시 도달할 수 있다.

**환경 차이 요약**

| 항목 | 이전 (안드로이드) | 현재 (Windows) |
|---|---|---|
| 가상환경 | 공용 `~/venv` (용량 문제) | 프로젝트 내 `.venv` |
| 패키지 관리 | `uv` | 표준 `venv` + `pip` |
| 인코딩 | UTF-8 기본 | **cp949 기본 — 명시 필수** |
| 한글 폰트 | `mpl_korean` shim | 맑은 고딕 내장 |
| 경로 | `/sdcard/projects/peakcut` | `C:\work\kwise` |
| Streamlit | 헤드리스, 수동 실행 | 브라우저 자동 실행 |
| RAM | 8 GB 제약 | 여유 있음 (순차 처리 구조는 유지) |

---

## 다음 세션 — **7세션 케이스 스터디**

10차 보완이 끝났다. 감도·기상·적용범위가 모두 정리되었으므로 이제 케이스를 돌려도
결과 해석이 흔들리지 않는다.

**케이스를 돌리기 전에 읽을 것**

- **감도표는 범위로 읽는다** (평탄형 / 기준 / 첨예형). 상한이 어느 시나리오에서
  나오는지는 부하 형태가 정한다 — 이름이 좋고 나쁨을 뜻하지 않는다 (11차 기록)
- 기상은 `data\weather\` 에 전국 3개년치가 **모두** 들어 있다. 케이스가
  2023~2025 안이면 네트워크 없이 돌아간다. 폴백하면 결과에 문구가 붙는다
- 케이스 정의에 지역을 넣을 때 `"서울특별시/강남구"` 키를 쓴다
- ESS 단가 키는 `ess_unit_cost_won_per_kw` 다 (kWh 아님). 총액은
  `ess_total_investment_won`
- ESS 결론은 **판정의 강도를 구분해서** 쓴다 — "경제성 없음" 은 강하게,
  "가능성 있음" 은 "견적을 받아볼 가치가 있다" 로 약하게

---

## 미해결 항목

세션을 넘어 남아 있는 것. **해결된 항목은 지운다** — 경위는 아래 「세션 기록」에 있다.

### 참고단가를 확보해야 풀리는 것

- [ ] **태양광 설치 단가 참고값** — ESS 는 에너지경제연구원 LCOS 연구를 인용할 수
      있지만 태양광은 확보하지 못했다. 근거 없는 기본값을 만들지 않기로 하고
      단가 미입력 시 사유를 반환하도록 했다 (7.5). 공개 자료(신재생에너지
      보급통계·공공조달 단가 등)를 확보하면 `PvCostReference` 와
      `data\pv_cost_reference.json` 을 ESS 와 같은 방식으로 채우면 된다.
      **구조는 열어 두었다** (`src\kwise\measures\pv_cost.py`)

### 청구서를 받아야 풀리는 것 (한 묶음)

자사 사옥이나 현장 사무소의 한전 청구서 한 벌이면 아래가 동시에 풀린다.
**7세션 케이스 스터디의 신뢰도가 여기 걸려 있다.**

- [ ] **요금 엔진 검증** — 확보하면 `data\tariff_kr_20260601.json` 의 `verified` 를
      true 로 바꾼다. **일요일 경부하 계량을 가장 먼저 대조한다**
      (3세션 수기 케이스에서 월 6.2% 차이가 났다)
- [ ] **요금적용전력 직전 이력** — 샘플에 없어 첫 11개월이 과소 산출된다.
      `prior_peaks=` 로 주입 가능하다. 청구서와 함께 받는다
- [ ] **실측 역률** — 약관 제42조는 30분 누적 계량을 요구하는데 우리 데이터는
      15분 유효전력뿐이다. 기본값 92% 는 같은 조항의 무효전력계 미설치 간주값이라
      근거가 있고 그 값에서 조정액이 0 이지만, 청구서의 역률 항목을 `power_factor_pct=`
      로 넣으면 바로 재계산된다
- [ ] **엑셀 요금표 옮김 오차 대조** — 농사용(을) 고압 여름철 단가가 엑셀 66.6원 /
      요금표 원본 68.6원으로 다르다. 범위 밖 종별이라 넣지 않았지만
      **같은 표에 다른 오차가 더 있을 수 있다.** 일반용(을)부터 대조한다

### 약관 조문 확인이 필요한 것

- [ ] **갑 종별 기본요금 기준** — 약관 제68조 ②의 기준은 갑/을 구분이 아니라
      **최대수요전력계 설치 여부**다. 현재 구현(갑=계약전력, 을=요금적용전력)은
      관행적 대응이다. 특히 **일반용(갑)Ⅱ 는 시간대별 구분계량기 설치 고객**이라
      최대수요계를 함께 갖추었을 수 있고, 그렇다면 갑Ⅱ 기본요금은 요금적용전력
      기준이어야 한다. **제38조(계기의 설치) 설치기준을 확인해 확정한다.**
      확인 전까지 갑 종별은 계약전력 기준으로 계산하고, 계약전력이 없으면 금액을
      만들지 않고 멈춘다

### 실측 데이터를 얻어야 풀리는 것

- [ ] **Open-Meteo 일사량의 라벨 규약** — 라벨이 구간 시작이라는 전제로 대표
      순시각을 라벨 + 30분으로 두었다. 실측 발전 데이터를 얻으면 발전 곡선이
      통째로 30분 밀렸는지 대조한다. `WeatherLabel` 로 바꿔 끼울 수 있다

### 해결된 항목 (경위는 세션 기록 참조)

| 항목 | 해결 |
|---|---|
| 계약전력 하한 규정 | 6세션 — 30%, 교육용(을) 15% 특례. `contract_floor_ratio` |
| 선택요금 전환의 전압구분 넘나듦 | 보완 세션 — `switchable_selections()` 로 현행 전압에 가둠 |
| 야간 진상역률 하한 | 8차 — 약관 제43조 ② 2호 나목에 명시. 지상이면 100% 간주 |

---

## 결정 사항

세션 중 내린 판단 중 나중에 다시 들출 만한 것.

### 9차 — DR 감축량·지역·태양광 입력 (2026-08-07)

| 결정 | 이유 |
|---|---|
| **등록 권장값과 연간 감축량을 분리** | 경제성DR 은 하루 전 입찰이라 매일 다른 양을 입찰한다. 등록값(하위 10%)으로 곱하면 부하가 많은 날의 여력을 버려 샘플에서 **55% 과소평가**된다 |
| 등록 분위수 10% 와 저부하일 5% 를 **다른 상수로** | 쓰임이 다르다. 하나는 계약 등록값, 하나는 무비용 감축일 식별이다 |
| 등록값 × 일수를 `flat_reduction_kwh` 로 **남겨 둔다** | 지우면 왜 그 방식을 안 쓰는지 알 수 없다. 비교값으로 두고 과소평가 폭을 주석에 적는다 |
| **시군구 단위 선택으로 충분** | ERA5 격자가 25~31 km 다. 서울 25개 구의 좌표 폭(남북 20.6·동서 29.0 km)이 격자와 비슷해 같은 셀을 조회한다. 정확도를 희생하지 않는다 |
| 좌표 직접 입력을 **옵션으로 남긴다** | 산악·해안 지형이나 넓은 시군구는 중심점이 부지와 멀 수 있다 |
| 개편된 시도는 **키를 그대로, 표시만 병기** | 키를 바꾸면 기존 케이스 정의가 깨진다. 강원도 → "강원특별자치도 (구 강원도)" |
| 세종 좌표를 **코드에 두고 원본은 손대지 않는다** | 원본은 받은 그대로 두어 출처를 추적할 수 있게 한다. 추가분만 코드에서 밝힌다 |
| **캐시 키를 0.25° 격자로 반올림** | ERA5 해상도라 손실이 없고, 시군구 선택이든 좌표 입력이든 같은 격자면 캐시를 공유한다. 7세션 재조회를 크게 줄인다 |
| **태양광 기본 입력을 3개로** (면적·밀도·지역) | 발전량 예측 R² 가 0.8 이라 입력 정밀도가 기상 오차를 이길 수 없다. 게다가 용량 곡선이 20단계를 훑으므로 면적 환산이 ±20% 틀려도 곡선 위 다른 점을 볼 뿐이다 |
| **모드를 나누지 않는다** — 확장 패널로 접는다 | 상세 설계 모드를 만들면 pvsim 과 경계가 흐려진다 |
| 밀도 하나가 **GCR 과 경사각을 함께** 정한다 | 고밀도 배치는 이격을 좁히는 대신 경사를 낮춰 음영을 줄이는 실무 관행이다 |
| 프리셋을 `data\pv_presets.json` 에 | 요금표·지역과 같은 규약이다. 값을 코드에 두지 않는다 |

### 8차 — 역률 야간 조항과 경제성DR (2026-08-07)

| 결정 | 이유 |
|---|---|
| **야간 진상 페널티는 '콘덴서 과투자의 결과'** | 제43조 ② 2호 나목이 지상이면 100%로 간주한다. 대부분의 건물이 야간 경부하에서 지상이므로 페널티는 0이다. 진상은 고정 콘덴서가 과다할 때만 생긴다 |
| `leading_pct=None` 을 **지상 간주(추가 0)** 로 | 이전에는 "모르니 산출 안 함 + 경고" 였는데, 나목이 답을 준다. 모르는 것이 아니라 **기본값이 0** 인 것이다 |
| 7장 순서를 **투자비 순으로 확정**. 이후 바꾸지 않는다 | 수단을 더할 때마다 번호가 밀리면 코드 docstring 과 문서 참조가 계속 어긋난다 |
| **DR 거래일 판정을 tariff 와 따로 둔다** | 요금은 토요일을 중간부하로 낮출 뿐 공휴일로 보지 않지만 DR 은 토·일·공휴일이 모두 제외다. 같은 함수로 판정하면 두 규칙이 조용히 섞인다 |
| DR 등록 용량은 **평균이 아니라 하위 10%** | 평균으로 등록하면 절반의 날에 미달한다. 미달은 위약금이다(별표26). 평균 기준값도 함께 내되 등록값은 보수적으로 |
| 저부하일 기준선은 **거래 가능일만으로** 계산 | 주말이 섞이면 기준선이 내려가 평일 저부하일이 걸리지 않는다 |
| DR **정산 단가에 기본값을 두지 않는다** | 전력거래소 월별 순편익가격 + 사업자 수수료라 우리가 만들 수 없다. 없으면 감축량(kWh)만 낸다 |
| DR **기본요금 절감을 계산하지 않는다** | SMP 기준 산발 입찰이라 참여일이 연중 최대수요일과 겹칠 확률이 낮다. 편익은 정산금 하나 |
| 0.1 MW-h 문턱은 **참고 표시만** | 제12.4.2.1조 1항 2호는 수요관리사업자가 묶은 **자원 단위** 기준이라 개별 고객 판정이 아니다 |
| DR 확실성 '중간' | 입찰 낙찰 여부와 참여일 수가 운영에 달렸다. 요금표만으로 확정되는 7.1·7.2·7.4 와 다르다 |

### 보완 세션 — 전압구분과 역률 (2026-08-07)

| 결정 | 이유 |
|---|---|
| **전압구분도 현행에 가둔다** (`switchable_selections`) | 고압A 3.3~66 kV / 고압B 154 kV / 고압C 345 kV — 수전설비가 물리적으로 다르다. 단가만 보면 그럴듯한 권고가 나와서 더 위험하다 |
| 드롭다운(`list_selections`)은 가두지 않는다 | 부록 A.3 은 선택지 생성이고, 가두는 것은 **전환 비교**뿐이다 |
| 최적 조합은 `min(selections, ...)` 로 고른다 | 넘겨받은 `option_totals` 에 다른 전압이 섞여도 그것을 뽑지 않는다 |
| **역률 기준은 92%. 초판의 90% 는 오류** | 약관 제41·43조 원문. 감액(97%까지)도 있다 |
| **추가·감액을 금액으로 낸다.** 경고로 두지 않는다 | 기본요금의 −1.0% ~ +6.4%. 샘플에서 1%가 453만원이다 |
| 역률 기본값 92%, **그 값에서 조정액 0** | 제42조의 무효전력계 미설치 간주값이라 근거가 있다. 모르는 채로 금액을 만들어내지 않으면서 3세션 이래 회귀값이 그대로 유지된다 |
| 야간 진상역률은 모르면 **산출하지 않고 경고만** | 무효전력 실측 없이 진상역률을 지어낼 근거가 없다 |
| 역률 판정 창을 **08~22시**로 (발전 시간대 아님) | 약관이 그 창의 지상역률로 요금을 매긴다. 발전 시간대(≈06~19시)로 재면 요금 규칙과 창이 어긋난다 |
| 제43조 ③(첫 달 예고)은 **계산에 넣지 않는다** | 첫 달을 빼면 기간에 따라 금액이 달라져 도입 전후 비교(Δ)가 흔들린다. 주석·부록 D 로만 남긴다 |
| 역률 개선 확실성은 **'높음'** | 요금표와 약관만으로 확정된다. 발전량 예측 같은 불확실성이 없어 감도도 무관하다 |
| 역률 개선 절감액도 **재계산** | 두 역률에서 각각 `calculate_bill()`. 기본요금 비율만 곱해 어림하지 않는다 |

### 준비 세션 — 파서와 요금표 (2026-08-07)

| 결정 | 이유 |
|---|---|
| 열 판정을 3단으로 (헤더 행 → 이름 → **내용**) | 양식은 바뀐다. 열 위치에 기대면 다음 케이스에서 깨진다 |
| 값 열의 상수 배제는 **비율이 아니라 개수**로 | 실측 kWh 의 고유값 비율이 4.1% 다. 5% 문턱을 두면 진짜 사용량 열이 잘린다. 고객번호는 고유값이 1개라 개수로 정확히 걸린다 |
| 판정 단계는 쉼표만 걷어내고 `to_numeric` | 로더의 `parse_usage_energy` 처럼 기호를 다 지우면 계기번호 `55-282007100` 이 숫자로 둔갑한다 |
| CSV 인코딩 채택은 **1차에 이름 매칭만** 허용 | 내용 기반은 헤더가 깨져도 성공해 버려 잘못된 인코딩이 채택된다 (1세션 결정의 연장) |
| 내용 기반 단계의 헤더 후보는 **열이 꽉 찬 행**만 | 제목 행을 헤더로 잡으면 진짜 헤더가 데이터 첫 줄로 섞인다 |
| 요금표는 **엑셀 변환**. 수기 입력 금지 | 60개 값을 손으로 옮기면 오차가 섞인다. 실제로 엑셀의 농사용(을) 단가가 원본과 다르다 |
| 계절 열은 **헤더 이름**으로 매핑 | 산업용 시트만 봄·가을이 여름보다 앞이다. 위치로 읽으면 태양광 평가가 정반대가 된다 |
| 변환 로직은 `src\` 에, CLI 만 `tools\` 에 | 계절 열 매핑이 이 프로젝트에서 가장 조용히 틀릴 수 있는 곳이다. 단위테스트가 닿아야 한다 |
| 검증 실패 시 파일을 **쓰지 않는다** | 검증을 통과 못한 요금표가 저장소에 남으면 다음 세션이 그것으로 계산한다 |
| **갑 종별 기본요금은 계약전력 기준** (`base_fee_basis`) | 을은 요금적용전력, 갑은 계약전력이다. 섞으면 기본요금이 통째로 틀린다. 계약전력을 모르면 만들어내지 않고 멈춘다 |
| 전체시간 종별은 세 시간대에 같은 단가 + `time_of_use=False` | 스키마를 갈라 두면 엔진이 두 갈래가 된다. 검증만 갈라서 `경<중간<최대` 대신 '세 칸이 같은지' 를 본다 |
| 전체시간 종별에 균일성 검사(규칙 2) 미적용 | 계절별로 할인폭을 다르게 설계한다. 요금표가 약속하지 않은 것을 검사하는 셈이다 |
| **선택요금 전환은 같은 계약종별 안에서만** | 종별은 용도로 정해지는 계약이지 고를 수 있는 요금제가 아니다. 종별이 하나일 때는 안 보이던 결함이다 |

### 프로젝트 범위

| 결정 | 이유 |
|---|---|
| 실측 부하 기반만 구현, 신축 부하 예측 제외 | 가치 대비 노력이 유리, 결과 신뢰도 높음 |
| 감도 3종 채택, P90 미도입 | 부하가 1년 실측 고정이라 확률 표기가 통계적으로 부정확 |
| PoC 요금 범위는 일반용(을) 고압A·B | 종별 확대 시 오타 검출이 어려움 |
| 기존 코드는 `reference\` 에 배치 | `src\` 에 넣으면 완성된 코드로 오인 |
| 검증·케이스 스터디를 UI보다 먼저 (7 → 8세션) | 계산이 틀린 채로 UI를 만들면 UI까지 재작업 |
| `input\` 은 `.gitignore` 에서 제외하지 않음 | 케이스 데이터를 이력으로 남겨 검증 재현 |

### 1세션 — 데이터 로더

| 결정 | 이유 |
|---|---|
| 그리드 이탈 행은 kW 산정에서 제외, kWh 합계에는 포함 | 15분 그리드를 벗어난 행은 평균 환산 시 수요가 왜곡됨 (43.2 kWh ÷ 14분 = 185 kW 인데 ×4 하면 172.8 kW). 다만 실사용 전력량이므로 요금 계산에는 필요 |
| 인코딩은 컬럼 매칭 성공까지 확인해야 채택 | 인코딩이 어긋나도 예외 없이 깨진 헤더가 나오는 경우가 있음 |
| 그리드 위상은 최빈값으로 판정 | 첫 행이 비정규 시각이어도 전체가 어긋나지 않게 |
| 총 사용량은 `meta.total_kwh` 를 쓴다 | `kwh` 시리즈 합계는 그리드 정렬분만이다. 이탈분을 더한 값이 요금 계산의 기준 |
| 라벨은 구간 끝 그대로 인덱스에 둔다 | 한 구간 빼는 귀속 판정은 tariff 의 책임. 로더가 미리 옮기면 이중 보정이 난다 |

### 2세션 — 품질 검사

| 결정 | 이유 |
|---|---|
| 정전 판정은 흔적 3종 중 **연속 결측을 뺀 나머지 2개 이상** 요구 | 연속 결측은 모든 결측이 갖는 성질이라 정전의 근거가 못 됨. 이 조건 덕분에 11월 통신 장애(930슬롯)가 정전으로 오인되지 않음 |
| 편중 판정에서 정전 슬롯을 분자·분모 양쪽에서 제외 | 한쪽만 빼면 비율이 왜곡됨. 제외값이 정본, 포함값은 비교용으로 병기 |
| 최장 연속 결측 구간을 편중 배수와 **별도로** 산출 | 며칠짜리 연속 공백은 시간대가 균등하게 빠져 배수가 1 근처가 되지만, 그 달의 최대수요 판정 자체를 무의미하게 만듦 |
| 정전 흔적 2번은 "부분 적산 + 복전 재등록"을 한 종으로 묶음 | 둘 다 그리드 이탈 행이라는 같은 흔적이다. 따로 세면 흔적이 4종이 되어 "3종 중 2개" 규칙이 헐거워짐 |
| 피크 시간대 창은 라벨 `10:15~16:00` | 라벨이 구간 끝이라 `10:00` 슬롯은 09시대 사용량이다. `slot_start()` 로 되돌려 판정 |
| `UsageData.kwh` → `kwh_grid` 로 개명하고 `.sum()` 을 막음 | 이탈분 43.2 kWh 는 전체의 0.0002% 라 눈에 띄지 않는다. 요금 엔진에서 틀리면 발견이 어려움 |

### 6세션 — 요금적용전력 규칙 정정 (요구사항서 5.2 개정)

| 결정 | 이유 |
|---|---|
| **요금적용전력 규칙을 `tariff/demand.py` 로 분리** | 세 조건(대상 시간대·대상 월·하한)이 겹치는데 요금 엔진 안에 섞어 두면 어느 규칙이 걸렸는지 추적이 안 된다 |
| `is_demand_month()` 를 `season_of()` 와 **따로 둔다** | 9월은 전력량요금상 봄·가을철이지만 요금적용전력 대상월이고, 6·11월은 반대다. 같은 함수로 판정하면 두 규칙이 조용히 섞인다 |
| 월별 행에 `demand_basis_kw`·`demand_before_floor_kw` 를 함께 남긴다 | 관측 최대와 대상 최대가 다르고, 하한 적용 전 값이 있어야 계약전력 조정을 재계산할 수 있다 |
| 하한 비율은 종별 속성. 요금 데이터에 `null` 이면 미적용 | 종별마다 다르고(30%/15%), 확인되지 않은 종별에 30%를 들이대면 없는 절감을 만들어낸다 |
| 계약전력 초과는 요금적용전력과 **별개로** 검사 | 경부하 초과는 요금적용전력에 영향이 없지만 초과사용부가금 대상이다 |
| **①경부하 제외와 ②대상월 한정은 PV 에 반대 방향으로 작용한다** | ①은 PV 가 손댈 수 없는 야간·공휴일 피크를 요금적용전력에서 빼 주므로 PV 에 유리하고, ②는 봄·가을 피크 저감의 가치를 없애므로 불리하다. **어느 쪽이 지배하는지는 부하 형태에 달렸다.** 샘플(여름 주간 피크형)에서는 ①이 압도해 PV 기본요금 절감이 49% 늘었다. 봄·가을 피크형이라면 반대가 된다. 케이스 스터디(7세션)에서 두 유형을 모두 넣어 확인할 것 |
| 조합 확실성은 가장 낮은 구성 요소를 따른다 | 태양광과 ESS 를 함께 넣으면 조합 전체가 ESS 등급이다. 표에서 등급을 섞어 보여주면 의사결정이 흐려진다 |
| **태양광 등급 판정 모집단은 요금적용전력 대상 슬롯이다** | 경부하 구간은 아무리 큰 수요가 나도 요금적용전력이 되지 않는다. 그 시각 피크를 판정에 넣으면 태양광의 기본요금 기여를 잘못 읽는다 (5.2 ①) |
| 상위 구간 분포를 **두 벌** 낸다 (`hour_counts` / `demand_hour_counts`) | 부록 B 의 시각 분포는 전 슬롯 기준 원값이고 청구서 관행이다. 판정용 값으로 덮어쓰면 부록 B 대조가 불가능해지고, 한 필드에 섞으면 어느 기준인지 알 수 없게 된다 |

### 5세션 — 개선 수단 평가

| 결정 | 이유 |
|---|---|
| **수단 적용 후 부하는 새 `UsageData` 로 만들어 요금 엔진에 다시 넣는다** | 4세션이 남긴 미결 사항. 요금 엔진을 건드리지 않아도 되고, 순부하에 품질 검사·진단을 그대로 돌릴 수 있다. `with_load()`·`apply_generation()` |
| 수단을 적용해도 그리드 이탈(부분 적산) 행은 건드리지 않는다 | 그 구간의 발전량을 배분할 근거가 없다. 43.2 kWh(전체의 0.0002%)라 보수적으로 남기는 편이 낫다 |
| 결측 슬롯은 결측인 채로 둔다 | 그 시각의 부하를 모르므로 자가소비 판정이 불가능하다 |
| 용량 곡선은 시뮬레이션 1회 + 곱셈 | pvwatts 직류와 인버터 모델이 정격에 선형이다(정격과 인버터 용량이 함께 커진다). 20단계라도 태양 위치 계산은 한 번. 선형성을 테스트로 고정했다 |
| **ESS 용량 산정 기준은 '하루 최대 초과 에너지'** | 충전 기회가 야간뿐이다. 연속 구간 최대로 잡으면 같은 날 초과가 두 번일 때 목표를 놓친다(샘플 5,200 kW 에서 실제로 놓쳤다). 부록 B 의 '총 초과 에너지'는 기간 합계라 배터리 용량으로는 과대(4,500 kW 목표에서 178 MWh) |
| 상계거래 금액은 요금표 단가로 산출, 외부 구매는 단가 없으면 미산출 | 상계는 그 슬롯의 계절·시간대 단가로 상계되므로 데이터에서 나온다. 외부 단가는 지어낼 근거가 없다 |
| `contract_floor_ratio` 는 기본값 없는 필수 키워드 인자 | 기본값을 두면 누군가는 그 가정으로 만든 금액을 근거로 쓴다 |

### 4세션 — 태양광과 진단

| 결정 | 이유 |
|---|---|
| **시각 규약을 둘로 나눠 명시** — 요금 귀속은 구간 시작(`slot_start`), 피크 발생 시각 보고는 검침 라벨 | 부록 B 의 상위 100구간 시각 분포(10시 15개…)와 최대수요 발생 시각(09:30)은 라벨 기준이다. 청구서도 그렇게 적는다. 요금 귀속만 구간 시작으로 판정하면 되고, 섞이지 않게 `top_slots` 에 두 값을 모두 담았다 |
| 부하 라벨 L 에 붙일 발전량은 구간 중앙 `L − Δ/2` 의 순시 출력 | 부하는 구간 평균, 시뮬은 순시값이다. 반 칸을 빠뜨리면 7.5분, 라벨을 그대로 쓰면 15분 밀린다 |
| 일몰 후 발전량을 태양 고도로 잘라냄 | 시간별 → 15분 선형 보간이 일몰을 가로질러 값을 흘린다. 자르지 않으면 일출·일몰 대조가 성립하지 않는다 |
| Open-Meteo 대표 순시각 = 라벨 + 30분 | 라벨이 구간 시작이라는 전제. `WeatherLabel` 로 바꿔 끼울 수 있게 두고 미해결 항목에 남겼다 |
| GCR 만 받고 pitch=1, height=`height_to_pitch` 로 넘김 | `infinite_sheds` 는 높이/열간격 비율에만 의존한다. 사용자에게 GCR 하나만 묻기 위함 |
| PowerShell 폴백 유지 | 3세션까지 미결이던 항목. 프록시 뒤에서 Python HTTPS 만 막히는 사례가 실제로 있고 Windows 전용 경로라 비용이 없다 |
| 계약전력 조정 절감액은 **하한 규정을 모르면 산출하지 않음** | 일반용(을) 기본요금은 요금적용전력 기반이라 계약전력을 낮춰도 하한에 걸리지 않으면 요금이 그대로다. 모르는 채로 금액을 내면 없는 절감을 만들어내는 것이 된다 |
| 진단은 계약 정보가 없어도 동작 | "파일만 올려도 결과가 나온다". 부하 패턴·피크 특성·태양광 등급까지는 계약 정보 없이 나온다 |

### 3세션 — 요금 엔진

| 결정 | 이유 |
|---|---|
| **검침일 라벨은 구간 끝이다** | 하루가 `00:15` 로 시작해 `24:00` 으로 끝나 96행이 됨. 구간 시작 라벨이면 `24:00` 이 나올 수 없음. 계절·시간대·월 귀속은 라벨에서 한 구간을 뺀 시각으로 판정. `15:00` 슬롯은 중간부하이고 첫 최대부하 슬롯은 `15:15`. **4세션에서 PV 시계열을 붙일 때 같은 규약을 써야 15분 어긋나지 않는다** |
| 일요일도 공휴일로 계량 | `holidays` 라이브러리가 일요일을 담지 않음. 관공서 공휴일 규정에 일요일이 포함됨. 누락 시 연 52일이 최대부하로 잘못 계량됨. 토글로 끌 수 있게 함 |
| 선택Ⅲ 단가는 균일성 규칙 미적용 | 요금표 입력값은 정확함. 선택Ⅲ 는 할인이 최대부하에 집중된 설계라 균일할 이유가 없음. 고압B Ⅱ→Ⅲ 는 허용오차 ±0.15 로 통과(1.6~1.7원은 반올림 폭). 고압A Ⅱ→Ⅲ 는 "최대부하 할인폭이 가장 크다"로 대체 |
| 단가를 임의로 고치지 않음 | 요금표 조작이 됨. 검증 실패는 경고로 남기고 규칙 쪽을 검토 |
| 검증 **규칙의 적용 범위·허용오차**는 코드(`validate.DEFAULT_POLICY`)에 둠 | 요금 단가가 아니라 검증 정책이다. 균일성 검사는 데이터에서 뽑은 평균과의 편차로 하고, 5.5·3.8 같은 절대값은 테스트가 못 박는다. 요금표를 갱신해도 검증기가 먼저 깨지지 않는다 |
| 부분 월 기본요금 기본 정책은 `merge` | 샘플에서 정확히 12.00개월분이 된다. 짝이 없으면 일수 비례로 물러서고 그 사실을 `notes` 에 적는다 |
| `energy_kwh()` 의 kW 환산 경로를 막음 | 이탈분이 얹힌 구간은 위상이 달라 수요가 왜곡된다. 막는 것은 ×(60/간격)과 ÷구간시간 둘뿐이라 단가 곱셈은 그대로 된다 |

---

## 확정된 샘플 데이터 수치 (회귀 테스트 기대값)

`input\사용량조회_20240429.csv` — 요구사항서 부록 B 와 동일. 어긋나면 부록 B 를 따른다.

| 항목 | 값 |
|---|---|
| 기간 | 2023-04-25 00:15 ~ 2024-04-27 00:00 |
| 원본 행 수 | 34,358 (그리드 이탈 2건 포함) |
| 결측 | 972 (2.8%) |
| `24:00` 표기 | 357건 |
| 연간 사용량 | 22,285 MWh |
| 최대수요 | 5,293.4 kW @ 2023-07-03(월) 09:30 |
| 부하율 | 49.0% |
| 결측 구간 1 | 2023-11-03 17:30 ~ 11-13 09:45 (930슬롯, 9.69일, 통신 장애 추정) |
| 결측 구간 2 | 2024-04-06 19:30 ~ 04-07 05:45 (42슬롯, 정전) |
| 11월 결측률 | 32.3% → 신뢰 제한 표시 |
| 최적 선택요금 | 선택Ⅱ (현행 선택Ⅰ 대비 절감) |

**기간이 13개 월 버킷이며 앞뒤 두 개가 부분 월이다.** 기본요금을 13개월분 부과하지 말고,
요구사항서 5.3.2 절에 따라 처리한 뒤 결과를 "연간" 대신 실제 기간 또는 12개월 환산으로 표기한다.

---

## 세션 기록

<!-- 아래 형식으로 최신 세션을 위에 추가한다 -->

### 11차 — 감도 명칭·범위 표시, 설치 밀도 라벨, ESS 단가·차익거래 (2026-08-07)

**7세션 케이스 스터디 직전의 마지막 보완이다.** 10차가 계산을 고쳤다면 이번은
**읽히는 방식**을 고쳤다. 숫자가 맞아도 이름이 반대를 가리키면 결론이 뒤집힌다.

---

#### 0. 기상 사전 취득 완료

10차에서 384/405 였던 것을 마저 받았다. **전국 135격자 × 3개년 = 405개 완비,
실패 0개.** 재개는 같은 명령 한 줄이었다 (위 10차 기록의 표 참조).

---

#### 1. 감도 명칭·표시 정정 — 이름이 축을 가리켜야 한다

10차에서 `sharpen()` 을 넣은 뒤 **'낙관'의 기본요금 절감액이 가장 작게 나왔다.**
버그가 아니라 모델이 맞게 작동한 결과다 — 총량을 보존하며 정오를 올리면 아침·저녁
어깨가 내려가고, 샘플은 최대수요가 09:30 인 오전 피크형이라 **어깨 출력이 곧 피크
저감**이기 때문이다.

그러나 이름과 결과가 반대라 오독을 부른다. 이 축은 낙관·비관이 아니라 **프로파일이
얼마나 뾰족한가**다.

| | 이전 | 지금 |
|---|---|---|
| 표시 | 보수 / 기준 / 낙관 | **평탄형 / 기준 / 첨예형** |
| 내부 키 | — | `conservative` / `base` / `optimistic` (**그대로**) |
| s | 0.85 / 1.00 / 1.25 | 같음 |

**내부 키를 바꾸지 않았다.** 설정 파일(`data\pv_presets.json`)과 케이스 정의가
그 이름을 쓰고 있어 바꾸면 기존 정의가 깨진다. 라벨만 `labels` 로 분리했다.

**결과 표시를 3열 나열에서 범위로 바꿨다.**

    기본요금 절감  3,152만원 (프로파일 감도 범위 2,897 ~ 3,266만원)

세 값을 나란히 놓으면 읽는 사람이 "어느 쪽이 좋은 값인가" 를 찾게 되는데 **이 축에는
좋고 나쁨이 없다.** 최댓값·최솟값은 `min`/`max` 로 뽑고 **어느 시나리오가 상한인지
고정하지 않는다** — 회귀 테스트에서 실제로 드러났다. 960 kWp 에서는 상한이
평탄형이지만 500 kWp 에서는 **기준**이 상한이다. 첨예형이 상한인 경우는 없었다.

Excel 은 `감도` 시트가 범위표, `감도 상세` 시트가 원자료 3행이다 (아홉 장이 됐다).
태양광이 없는 케이스는 감도 시트에 사유만 적고 상세를 만들지 않는다.

---

#### 2. 설치 밀도 라벨 — 구어체를 산출물에서 뺐다

'촘촘·듬성' 은 제안서에 그대로 쓸 수 없다.

| 내부 키 | 표시 | GCR | 경사 | 1,000 m² | 상충 관계 |
|---|---|---|---|---|---|
| `high` | 높음 | 0.55 | 15° | **110 kWp** | 면적당 용량 최대 / kWp당 발전량 최소 |
| `normal` | 보통 | 0.40 | 25° | **80 kWp** | 균형 (기본값) |
| `low` | 낮음 | 0.30 | 30° | **60 kWp** | kWp당 발전량 최대 / 면적당 용량 최소 |

**선택지마다 상충 관계를 한 줄로 병기한다.** 밀도만 보아서는 "높으면 좋은 것인지"
를 알 수 없기 때문이다. `capacity_preview()` 로 **고르는 즉시 환산 용량을 함께**
보여 준다 — 밀도라는 말보다 110/80/60 kWp 라는 숫자가 결정에 쓰인다.

---

#### 3. ESS 단가 — kW당 하나로 받는다

**2성분(원/kW + 원/kWh)이 물리적으로는 정확하지만 사용자가 견적서를 그 형태로
받는 일이 드물다.** 입력은 단순하게 간다.

    투자비 = 필요 출력(kW) × 입력단가(원/kW)

방전시간은 이미 kW당 단가에 반영되어 있으므로 **이중으로 곱하지 않는다.**
견적서를 받았으면 총액을 그대로 넣는 경로(`EssCostInput.of_total`)도 뒀다.
손익분기 단가도 원/kWh 에서 **원/kW 로 바꿨다** — 입력과 같은 단위여야 견줄 수 있다.

**참고단가는 2성분 원본을 저장하고 방전시간별로 환산해 제공한다**
(`data\ess_cost_reference.json`). 출처는 에너지경제연구원 LCOS 연구
(2025년 기준, 환율 1,412원/달러). 양수발전은 건물 대상이 아니라 넣지 않았다.

    kW당 단가 = CAPEX_Power + CAPEX_Energy × 방전시간

| 방전시간 | LFP 2025 | LFP 2030 |
|---|---|---|
| 0.5h | 430,263 | 355,312 |
| 1h | 615,231 | 497,400 |
| 2h | 985,167 | 781,575 |
| 4h | 1,725,039 | 1,349,925 |

지시받은 검산값과 정확히 일치한다 (회귀로 고정).

**이 값의 성격이 결론의 강도를 정한다 — 추정치가 아니라 하한선이다.** 계통용 대형
ESS 기준이고 전용실·소화설비 등 안전 규제 대응비와 수배전 연계공사비가 빠져 있다.
그래서 판정이 **비대칭**이다.

    하한선 > 손익분기  →  경제성 없음        (강한 판정. 더 싸질 여지가 없다)
    하한선 < 손익분기  →  견적 받아볼 가치    (약한 판정. 실제 견적은 이보다 높다)

이 비대칭을 `EssCostReference.verdict()` 가 문구로 그대로 낸다.
**참고값이 기본값으로 자동 적용되지 않는다** — 설정에서 `auto_apply: true` 로
바꿔도 로더가 거부한다. 출처가 다른 값이 견적으로 둔갑하는 것을 막는다.

---

#### 4. ESS 방전시간과 차익거래

**방전시간은 산출값이다. 강제하지 않는다.**

    방전시간 = 하루 최대 초과 에너지 ÷ 최대 초과 출력

샘플(목표 5,200 kW)에서 **0.372h = 2.69C** 가 나온다. 0.5h 미만이라 고출력 셀
경고를 낸다 — 정치형 LFP 는 통상 0.5~1C 연속이므로 참고단가보다 비쌀 수 있다.

**차익거래를 넣었다.** 계시별 단가는 요금 엔진에서 자동으로 가져온다.

| 계절 | 최대부하 | 경부하 | 차익(η 반영) | 평일수 |
|---|---|---|---|---|
| 여름 | 227.8 | 92.8 | 122.3 | 64 |
| 겨울 | 203.4 | 99.8 | 90.0 | 83 |
| 봄·가을 | 146.0 | 92.8 | 40.5 | 103 |

**연 19,476원/kWh · 단독 회수기간 19.0년.** CAPEX_Energy 369,936원/kWh 대비이고
배터리 수명(10~15년)을 넘는다. **단독으로는 성립하지 않는다는 근거가 이 숫자다.**

평일 수는 요금 달력에서 **최대부하가 존재하는 날만** 세어 얻는다 (250일). 토요일은
최대부하가 중간부하로 계량되고 일요일·공휴일은 전량 경부하라 자동으로 빠진다.
DR 거래일(6.6)과 규칙이 다르므로 따로 센다.

**차익거래 수익을 절감액에 더하지 않았다.** 피크컷 디스패치가 이미 일부를
실현하고 있어 그대로 더하면 이중 계산이다. 별도 줄로 내고 회수기간은 **두 기준**을
함께 낸다.

**수익 구조 — 이것이 핵심 메시지다** (샘플 93.4 kW)

    피크저감 수익  ∝ 출력(kW)    용량을 늘려도 늘지 않는다
    차익거래 수익  ∝ 용량(kWh)
    투자비         ∝ 용량(kWh)

| 방전시간 | 투자비 | 피크저감만 | 차익거래 포함 |
|---|---|---|---|
| 0.5h | 4,020만원 | 6.0년 | **5.3년** |
| 1h | 5,749만원 | 8.6년 | **6.9년** |
| 2h | 9,205만원 | 13.7년 | **9.2년** |
| 4h | 16,119만원 | 24.1년 | **12.2년** |

지시받은 참고값(5.0 / 6.4 / 8.6 / 11.2년)은 **차익거래를 포함한 쪽**과 거의 같다
(6~9% 차이). 두 기준을 모두 내되 기본값은 이중 계산이 없는 '피크저감만' 쪽이다.

**2030년 전망 단가 기준 회수기간을 함께 낸다.** 샘플에서 현재 6.0년 → 전망 4.4년.
"지금은 경제성 없음" 을 **"언제쯤 성립하는가"** 로 바꾼다.

---

**만든·고친 파일**

- `data\ess_cost_reference.json` — 신규. 2성분 원본 + 출처 + 비대칭 판정 문구
- `src\kwise\measures\ess_cost.py` — 신규. `EssCostInput`(두 경로), 방전시간별 환산,
  `verdict()`, 자동 적용 차단
- `src\kwise\measures\arbitrage.py` — 신규. 계절별 단가차익, 평일수, 단독 회수기간
- `src\kwise\measures\ess.py` — 단가 입력 교체, `required_discharge_hours`,
  C-rate 경고, 전망 단가, `ess_payback_curve()`
- `src\kwise\pv\sensitivity.py` — `labels` 분리, `SCENARIO_KEYS`, `keyed_items()`
- `src\kwise\compare\sensitivity.py` — `SensitivityRange`, `sensitivity_range_frame()`
- `src\kwise\pv\layout.py` — `tradeoff`, `density_label`, `capacity_preview()`
- `data\pv_presets.json` — 밀도 키 high/normal/low, 라벨·상충 관계, 감도 라벨
- `src\kwise\compare\combination.py`, `src\kwise\report\batch.py` —
  `ess_unit_cost_won_per_kw` 로 개명, `CaseSpec.ess_cost`
- `src\kwise\report\excel.py` — 감도 시트를 범위로, `감도 상세` 신설,
  차익거래 별도 줄
- `src\kwise\report\notices.py` — 한계 13→16, ESS 출처
- `tests\test_ess_cost.py` — 신규 26건. `test_compare`·`test_pv_input` 보강
- 요구사항서 3.3, 7.6(7.6.1~7.6.5 신설), 9.2, 10.3, 11.2, 11.3, 부록 D 13→16

**미해결** — 신규 없음

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **감도표는 범위로 읽는다.** 상한이 어느 시나리오에서 나오는지는 부하 형태가
  정한다. 정오 피크형 케이스에서는 첨예형이 상한이어야 한다 — 그 대비를
  케이스 스터디가 보여야 한다
- 케이스 YAML 의 ESS 단가 키가 `ess_unit_cost_won_per_kw` 로 바뀌었다.
  총액을 넣으려면 `ess_total_investment_won`
- ESS 결론을 쓸 때 **판정의 강도를 구분한다.** "경제성 없음" 은 강하게,
  "가능성 있음" 은 "견적을 받아볼 가치가 있다" 로 약하게 쓴다
- 방전시간이 0.5h 미만으로 나오는 케이스가 많을 것이다. 피크가 짧고 뾰족한
  건물일수록 그렇다. 고출력 셀 경고가 붙었는지 확인한다
- 차익거래 단독 회수기간(19년)은 **케이스마다 다시 계산된다** — 계시별 단가와
  평일수가 요금표·달력에서 오기 때문이다. 야간 피크형은 차익이 작을 것이다

**테스트**
- pytest: 620 passed (직전 585 + 이번 35)
- ruff: pass (check + format)
- mypy: pass (strict, 78 files)

---


### 10차 — 감도 첨예도 전환, 기상 사전 취득, 대한민국 전용 명시 (2026-08-07)

**7세션 케이스 스터디 직전의 마지막 보완이다.** 셋 다 케이스 결과의 *해석*을
바꾸므로 케이스를 돌린 뒤에 고치면 전부 다시 돌려야 했다.

---

#### 0. 감도 방식 정정 — 일률 계수를 **첨예도 조정**으로

**이전 방식이 9.1 과 어긋나 있었다.** 감도 3종(0.70/1.00/1.20)을 PV 출력
시계열 **전체에 곱하고** 있었다. 9.1 이 말하는 편향은 "총량 오차"가 아니라
**"프로파일 평탄화"** 다 — 재분석 일사의 공간·시간 평균화로 곡선이 눌리는 것이고,
총량은 실측과 거의 맞는다 (시간별 R² 0.8, 총량은 수렴). 총량이 맞는데 일률 계수를
곱하면 **없는 총량 불확실성을 ±30% 만들어 내는 것**이라 전력량요금 절감액 밴드와
회수기간 밴드가 함께 부풀려진다.

    adjusted(t) = 일평균발전 + (기준(t) − 일평균발전) × s

    보수 s=0.85 / 기준 s=1.00 / 낙관 s=1.25   ← data\pv_presets.json

**일평균발전은 그날 발전이 있는 슬롯만의 평균이고, 조정도 그 슬롯에만 건다.**
여기서 두 번 넘어졌고 둘 다 테스트로 못 박았다.

| 잘못 잡으면 | 무슨 일이 생기나 |
|---|---|
| 야간 0 을 평균에 포함 | 밤의 편차(0 − 평균)가 전체를 지배한다. s 를 키우면 야간이 크게 음수가 되고, 클램프 후 재정규화하면서 **정오 출력이 오히려 낮아진다** |
| 야간 슬롯에도 조정을 적용 | s < 1 일 때 0 이 평균 쪽으로 끌려 올라가 **한밤중에 발전이 생긴다** (실제로 03:00 에 51.5 kW 가 나왔다) |

발전 슬롯 기준으로 잡으면 두 문제가 함께 사라지고, 발전 슬롯 합이 곧 일 발전량이라
**총량이 정확히 보존된다.** 음수는 0 으로 자르고 일별 총량이 원본과 같도록
재정규화한다. `s = 1.0` 이면 원본 객체를 그대로 돌려준다 — 기준 시나리오가
시뮬레이션과 완전히 같아야 회귀값이 흔들리지 않는다.

**샘플 검증 (960 kWp, 일반용(을) 고압A 선택Ⅰ)**

| 지표 | 보수 0.85 | 기준 1.00 | 낙관 1.25 | 편차 |
|---|---|---|---|---|
| 발전량(kWh) | 1,745,276 | 1,745,355 | 1,745,442 | **0.01%** |
| 전력량요금 절감액 | 2억 3,417만 | 2억 3,408만 | 2억 3,380만 | **0.16%** |
| **기본요금 절감액** | **3,266만** | **3,152만** | **2,897만** | **11.30%** |
| 요금적용전력(kW) | 4,921.4 | 4,934.5 | 4,963.8 | — |

의도한 대로 갈렸다. **총량 기반 지표는 거의 불변, 피크 기반 지표만 흔들린다.**

**예상하지 못한 결과 — 시나리오 이름을 절감액으로 읽으면 안 된다**

'낙관'의 기본요금 절감액이 **가장 작다.** 총량이 보존되므로 첨예도를 올리면 정오는
높아지지만 **아침·저녁 어깨가 낮아지고**, 샘플은 최대수요가 09:30 인 오전 피크형이라
어깨 시간대 출력이 곧 피크 저감이기 때문이다. 일률 계수를 곱하던 이전 방식에서는
'낙관 = 절감액 최대'가 항상 성립했으므로 **읽는 방식 자체가 바뀐다.**

`SCENARIO_NAME_CAVEAT` 를 만들어 감도표와 Excel 요약에 함께 싣고 회귀로 고정했다.
**7세션에서 오후 피크형·야간 피크형 케이스를 볼 때 이 방향이 뒤집힐 것이다.**

`sharpen()` 은 양의 상수배에 **동차**다(평균·편차·클램프·재정규화가 모두 비례).
그래서 용량 곡선 20단계마다 다시 걸지 않고 **단위 프로파일에 한 번만** 건다.
5세션에서 고정한 "시뮬레이션 1회 + 곱셈" 구조가 그대로 유지된다. 테스트로 고정.

`sensitivity_factor` → `sharpness` 로 **이름을 바꿨다.** 뜻이 달라졌는데 같은
이름을 두면 다음 세션이 옛 뜻으로 읽는다. `SensitivityFactors` → `SharpnessFactors`,
`DEFAULT_FACTORS` → `DEFAULT_SHARPNESS` 도 같은 이유다.

---

#### 1. 기상 데이터 사전 취득 (`kwise.pv.archive`, `tools\fetch_weather.py`)

**조회 순서를 확정했다.**

| 순서 | 경로 | 비고 |
|---|---|---|
| ① | `PROJECT_CACHE` 캐시 | 반복 호출을 여기서 막는다 |
| ② | Open-Meteo API | 성공분은 ①에 캐시한다 |
| ③ | 사전 취득분 `data\weather\` | **폴백 사실을 결과에 표시한다** |
| ④ | 없으면 **중단** | 0 으로 계산하거나 인접 격자로 대체하지 않는다 |

**전국 3개년치를 실제로 받았다** (이 세션에서 수행).

| 항목 | 값 |
|---|---|
| 격자 | **135개** (시군구 229개를 0.25° 로 접은 결과) |
| 기간 | 2023-01-01 ~ 2025-12-31 (3개년) |
| 파일 | 405개 (격자 × 연도) |
| 행 | 3,551,040 (연 1,182,600 / 윤년 1,185,840) |
| 용량 | **40.4 MB** (예상 21 MB 보다 크다. zstd level 9) |
| 실패 | **0개** |

**호출 제한에 실제로 걸렸다.** 1차 실행에서 384개를 받고 나머지 21개가
`Hourly API request limit exceeded` 로 막혔다. 백오프(2→4→8→16초) 후에도 안 되어
실패 목록에 남기고 계속 진행했고, 한 시간 뒤 **같은 명령**으로 재개해 21개를
마저 받았다 (증분 갱신). 설계한 대로 동작했다 — 이 기능이 없었으면 처음부터
다시 받아야 했다.

**저장 구조**

- 격자 셀 × 연도 parquet (`openmeteo_37.5000_127.0000_2023.parquet`), zstd level 9
- `int16` + 스케일 팩터 — 일사 ×10 (0.1 W/m²), 기온·풍속 ×100 (0.01)
- **적설은 담지 않는다.** pvlib 경로에서 쓰지 않아 호출 비용만 늘린다.
  스키마 호환을 위해 읽을 때 0 으로 채우고 그 사실을 테스트로 못 박았다
- 확보 현황 색인 `index.json` 을 함께 둔다. `archive_status()` 가 이것을 읽는다 (8세션 UI)
- 경로는 `KWISE_WEATHER_DIR` 로 바꿀 수 있다 — 저장소 밖에 두고 배포할 수 있다

**호출 제한 대응이 이 CLI 의 절반이다.** 실제로 걸렸다.

- 호출 간 최소 간격 1초 (설정 가능)
- 429·5xx·연결 실패는 지수 백오프 재시도 (2→4→8→16초, 상한 120초)
- 재시도 후에도 실패한 셀은 `fetch_failures.json` 에 남기고 **계속 진행**
- 이미 받은 셀·연도는 색인을 보고 건너뛴다 → **중단 후 같은 명령으로 재개**
- 부분 확보를 완전 확보로 오인하지 않는다 (일주일치가 있어도 그 해 전체를
  요구하면 다시 받는다)

**폴백을 캐시에 쓰지 않는다.** 쓰면 다음 실행에서 '캐시'로 둔갑해 폴백 표시가
사라진다. 조용히 바뀌는 것이 이 기능에서 가장 위험하다.

**테스트를 사전 취득분에서 격리했다** (`conftest.isolated_weather_archive`).
저장소에 전국 3개년치가 들어 있으므로, 격리하지 않으면 "API 실패 시 멈추는가" 같은
시험이 **조용히 폴백에 성공해** 통과해 버린다. 실제로 기존 시험 하나가 그렇게
통과하기 시작해서 잡았다.

---

#### 2. 적용 범위 — 대한민국 전용 명시

요구사항서 **1.4 절**에 근거를 표로 적고 1.5 절(범위 밖) 첫 항목으로 올렸다.
README 와 부록 D, `KNOWN_LIMITS` 에도 넣었다.

| 요소 | 근거 |
|---|---|
| 요금 체계 | 한전 기본공급약관·전기요금표 |
| 기본요금 | 약관 제68조 요금적용전력 |
| 역률 규정 | 약관 제41~43조 |
| 경제성DR | 전력시장운영규칙 제12장 |
| 공휴일 | 관공서 공휴일 규정 + 일요일 |
| 지역 선택 | 시군구 좌표 229개 |
| 기상 사전 취득 | 국내 135개 격자 |

**지역 데이터팩 같은 다국가 추상화를 만들지 않았다.** 쓰지 않을 구조는 복잡도만
늘린다. 이 판단도 문서에 적어 두었다 — 다음 세션이 다시 꺼내지 않도록.

---

#### 3. PDF 원본 정리

`data\source\` 의 PDF 두 개가 번들 용량의 대부분이었다 (9.4 MB).

- `pdftotext -layout -enc UTF-8` 로 추출 — 약관 0.32 MB, 운영규칙 2.76 MB
- **txt 를 커밋한다.** 조문 검색이 되는지 확인했다 (제43조 ② 2호 나목의
  "지상역률인 경우에는 역률 100%로", 제12.4.2.1조, 미해결 항목의 제38조 모두 검색됨)
- PDF 는 `.gitignore` 에 넣고 `git rm --cached` 로 추적에서 뺐다. 로컬에는 남는다
- 요금표 xlsx 는 변환 원본이므로 그대로 커밋 유지

---

**만든·고친 파일**

- `src\kwise\pv\archive.py` — 신규. 저장·색인·현황·재시도·페이서·작업 목록
- `tools\fetch_weather.py` — 신규. 사전 취득 CLI (rich 진행 표시)
- `src\kwise\pv\sensitivity.py` — `sharpen()` 신설, 첨예도 계수로 전면 교체
- `src\kwise\pv\weather.py` — 폴백 경로, `WeatherData.fallback`/`notes`
- `src\kwise\pv\simulate.py` — 폴백 문구를 경고로 승계
- `src\kwise\measures\solar.py`, `src\kwise\compare\combination.py` — `sharpness` 로 개명
- `src\kwise\compare\sensitivity.py` — 기본요금·전력량요금 절감액 열 분리, 이름 주의 문구
- `src\kwise\report\notices.py` — 한계 2건 추가(11→13), `DATA_SOURCES` 신설
- `src\kwise\report\excel.py`, `batch.py` — 출처·감도 문구, 폴백 표시
- `data\pv_presets.json` — `sensitivity_sharpness` 블록
- `data\weather\` — 전국 사전 취득분 (신규)
- `data\source\*.txt` — 약관·운영규칙 텍스트 (PDF 는 추적 해제)
- `tests\test_pv_archive.py` — 신규 28건. `tests\test_pv.py` 감도 절 전면 재작성
- `tests\conftest.py` — 사전 취득분 격리 픽스처
- 요구사항서 1.4·1.5·7.5·9.1·9.2·11.2·부록 D, README

**미해결** — 신규 없음

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **감도표를 읽는 법이 바뀌었다.** '낙관'은 절감액이 크다는 뜻이 아니라 발전
  프로파일이 뾰족하다는 뜻이다. 오전 피크형(샘플)에서는 '낙관'의 기본요금 절감액이
  가장 작다. 정오 피크형 케이스에서는 반대가 될 것이다 — **이 대비가 케이스
  스터디에서 드러나야 한다**
- 6세션이 남긴 "①경부하 제외 vs ②대상월 한정 중 어느 쪽이 지배하는가" 와 함께
  보면 부하 형태가 결론을 어떻게 뒤집는지 두 축이 모인다
- 기상은 네트워크 없이 돌아간다. 케이스 기간이 2023-01 ~ 2025-12 안이면 폴백도
  필요 없다. 샘플(2023-04 ~ 2024-04)은 범위 안이다
- 요약 시트의 `note` 에 폴백 문구가 있으면 그 케이스는 **API 가 아니라 사전
  취득분으로 계산된 것**이다. 값이 틀린 것은 아니지만 근거를 밝혀야 한다
- 사전 취득분을 갱신하려면 `tools\fetch_weather.py --start ... --end ...`.
  이미 받은 것은 건너뛴다

**테스트**
- pytest: 585 passed (직전 547 + 이번 38)
- ruff: pass (check + format)
- mypy: pass (strict, 75 files)

---


### 9차 — DR 감축량 정정, 지역 선택, 태양광 간략 입력 (2026-08-07)

**7세션 전 마지막 보완이다.**

---

#### 0. DR 감축 가능량 정정 — 등록값으로 곱하면 안 된다

직전 세션에서 연간 감축량을 `등록 권장값 × 거래일 수` 로 냈다. **틀렸다.**
경제성DR 은 하루 전 입찰이라 **매일 다른 양을 입찰한다.** 등록값(하위 10%)으로
곱하면 부하가 많은 날의 여력을 통째로 버린다.

| 방식 | 샘플 (245 거래일, 1h/일) |
|---|---|
| 등록값 × 일수 (이전) | 154,648 kWh |
| **거래일별 여력 합산 (지금)** | **341,921 kWh** |
| 차이 | **55% 과소평가였다** |

두 값을 **용도로 갈라** 반환한다.

    registered_capacity_kw   하위 10% 기준 — 사업자 계약 시 등록 권장값
    annual_reducible_kwh     거래일별 여력 합산 — 연간 수익 추정 기준
    flat_reduction_kwh       등록값 × 일수 — **비교용으로만** 남긴다

등록 권장값을 하위 10% 로 잡은 판단은 그대로 둔다 — 평균으로 등록하면 절반의
날에 미달하고 미달은 위약금이다(별표26). 분위수 상수를 `REGISTRATION_PERCENTILE`
로 빼고, 저부하일 식별의 `LOW_LOAD_PERCENTILE`(5%)과 **쓰임이 다르다**는 것을
주석과 테스트로 못 박았다.

일별 여력은 `DrProfile.daily_reducible_kw` 로 들고 있다 (245일, 평균 1,396 kW,
최소 0 · 최대 2,527 kW). 무비용 감축일 산정도 그날의 여력을 쓴다.

---

#### 1. 지역 선택 (`kwise.pv.region`)

**시군구 단위 선택은 정확도를 희생하지 않는다.** Open-Meteo 의 ERA5 격자가
25~31 km 인데 서울 25개 구의 좌표 폭이 남북 20.6 km · 동서 29.0 km 라, 구를
바꿔도 같은 셀을 조회한다. 강남구와 서초구가 둘 다 (37.5, 127.0) 격자다.

**두 경로를 모두 지원한다**

    시군구 선택 (기본)      시도 → 시군구 2단. 드롭다운은 데이터에서 생성
    위경도 직접 입력 (옵션)  산악·해안 지형이나 넓은 시군구용

**데이터 정리**

- 좌표가 문자열이라 `float` 로 바꾼다
- **세종특별자치시 추가** — 하위 시군구가 없는 단층 자치시라 원본에서 누락되어
  있었다. 세종시청 좌표(36.592369, 127.292199)를 코드에서 더한다.
  **원본 파일은 손대지 않는다** — 다른 228개 좌표가 원본과 같은지 테스트로 대조
- 시도 명칭이 개편 전 기준(강원도, 전라북도)이다. **키는 파일 그대로 두어 호환을
  유지하고** 표시 명칭에만 현행을 병기한다 ("강원특별자치도 (구 강원도) 춘천시")
- 좌표 범위 검증 — 위도 33~39, 경도 124~132 밖이면 실패

**캐시 키를 0.25° 격자로 반올림했다.** ERA5 해상도라 손실이 없고, 시군구 선택이든
좌표 직접 입력이든 같은 격자면 **같은 parquet 을 쓴다.** 7세션 케이스 스터디의
재조회를 크게 줄인다.

---

#### 2. 태양광 간략 입력 (`kwise.pv.layout`)

**기본 입력을 3개로 줄였다 — 설치 가능 면적 · 설치 밀도 · 지역.**

방위·경사·손실률을 정밀하게 받아도 발전량 예측 R² 가 0.8 이라 기상 오차가
지배한다. 게다가 **용량 곡선이 이미 20단계를 훑으므로 면적 환산이 ±20% 틀려도
곡선 위의 다른 점을 보는 것일 뿐** 결론이 바뀌지 않는다 (테스트로 고정).

**밀도 프리셋이 GCR 과 경사각을 함께 정한다**

| 밀도 | GCR | 경사각 | 1,000 m² |
|---|---|---|---|
| 촘촘 | 0.55 | 15° | **110 kWp** |
| 보통 | 0.40 | 25° | **80 kWp** |
| 듬성 | 0.30 | 30° | **60 kWp** |

    설치 용량(kWp) ≈ 설치 가능 면적 × GCR ÷ 5      (모듈 효율 20% 기준)

프리셋과 kWp 당 면적은 `data\pv_presets.json` 에 있다. 하드코딩하지 않았다.
방위각은 남향(180°) 기본.

**모드를 나누지 않았다.** 나머지(방위·경사·GCR·손실률·용량 직접 입력)는
`PvQuickInput` 의 선택 인자로 두고, 안 주면 프리셋·기본값이 자동 적용된다.
8세션 UI 는 한 화면에서 접었다 펴면 된다. 용량을 직접 지정하면 면적 환산을
덮어쓰되 환산값도 함께 보여 준다.

**상세 설계는 범위 밖**임을 요구사항서 1.5 절에 명시했다 — kWise 는 도입 전
검토 도구이고, 모듈 배치·스트링 구성·음영 시뮬레이션은 pvsim 의 몫이다.

---

**만든·고친 파일**

- `src\kwise\pv
egion.py` — 신규. 시군구 229개, 격자 반올림
- `src\kwise\pv\layout.py` — 신규. `PvQuickInput`, 밀도 프리셋
- `data\sigungu_kr.json` — 배치된 원본을 그대로 커밋 (228개)
- `data\pv_presets.json` — 신규. 밀도 3종, kWp 당 면적
- `src\kwise\pv\weather.py` — `WeatherRequest.cache_name` 격자 반올림
- `src\kwise\diagnose\dr.py` — 등록값/연간 감축량 분리, 분위수 상수 2개
- `src\kwise\measures\demand_response.py` — 반환 필드 재구성
- `src\kwise
eport\excel.py` — 진단·수단 시트에 두 값을 나눠 표기
- `tests\test_pv_input.py` — 29건, `tests\test_dr.py` 갱신 + 4건
- 요구사항서 3.3 전면 개정, 1.5 에 상세 설계 범위 밖 추가

**미해결** — 신규 없음

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **기상 캐시가 격자 단위로 공유된다.** 같은 시도 안의 케이스를 여러 개 돌려도
  Open-Meteo 를 다시 부르지 않는다. 케이스를 지역별로 묶어 돌리면 더 빠르다
- 케이스 정의에 지역을 넣을 때 `"서울특별시/강남구"` 키를 쓴다. 좌표가 필요하면
  `find_region(key).latitude/.longitude`
- **PV 케이스는 면적·밀도로 정의하는 것이 자연스럽다.** `PvQuickInput.to_system()`
  이 `PvSystemConfig` 를 만들어 주므로 기존 경로에 그대로 물린다
- DR 수익을 비교할 때 `annual_reducible_kwh` 를 쓴다. `flat_reduction_kwh` 는
  "왜 그 방식을 안 쓰는지" 를 보여 주는 비교값이다
- 부하 유형별로 DR 일별 여력의 분포가 달라진다. 야간 피크형은 주간 여력이 작아
  등록값과 연간 감축량이 함께 낮게 나올 것이다

**테스트**
- pytest: 547 passed (직전 514 + 이번 33)
- ruff: pass (check + format)
- mypy: pass (strict, 72 files)

---

### 8차 — 역률 야간 조항 정정과 경제성DR 신설 (2026-08-07)

---

#### 0. 역률 미해결 항목 정정 — 원문에 있었다

직전 세션에서 "야간 진상역률 하한이 원문에 없어 주간과 같은 60%를 적용했다" 고
남긴 것은 **틀렸다.** 기본공급약관 **제43조 ② 2호 나목**에 명시되어 있다.

> 30분 단위의 역률이 진상역률 60%에 미달하는 경우에는 역률 60%로,
> **지상역률인 경우에는 역률 100%로 간주**하여 1개월간 평균역률을 계산합니다.

60% 하한 판단은 맞았다. `LEADING_FLOOR_PCT` 를 확정하고 근거 조문을 주석에 남겼다.

**더 중요한 것은 뒷문장이다.** 야간에 지상이면 역률이 **100%로 간주**되어 기준
95%를 넘으므로 **진상 추가요금이 0**이다. 대부분의 건물은 야간 경부하에서 지상이고,
진상은 **고정 콘덴서가 부하 대비 과다할 때** 생긴다. 그래서 이 조항은
"야간에도 역률을 관리하라"가 아니라 **"주간 역률을 맞추려 콘덴서를 키우면 야간에
되돌려 준다"** 는 뜻으로 읽어야 한다.

이에 따라 `leading_pct=None` 의 뜻을 바꿨다.

| | 이전 | 지금 |
|---|---|---|
| 의미 | "모르니 산출 안 함" + 경고 | **지상 간주 100%, 추가 0** (나목) |
| 추적성(5.8) | "야간 진상 미상" | "야간 지상 간주 100% (제43조 ② 2호 나목)" |
| 경고 | "진상역률을 알 수 없어…" | "고정 콘덴서를 크게 두었거나 키울 예정이면 진상 위험" |

역률 개선 수단(7.4)의 경고를 다시 썼다 — **APFR(자동역률조정장치)** 를 쓰면
부하에 따라 투입 단수가 조절되어 야간 진상을 피할 수 있다는 것을 넣었다.
**설치비는 만들지 않는다.** 설비 구성에 따라 달라 사용자 입력이다.

---

#### 1. 7장 번호 확정 (투자비 순)

DR 을 넣으면서 순서를 못 박았다. **이후 바꾸지 않는다.**

    7.1 선택요금 전환      투자 0
    7.2 계약전력 조정      투자 0
    7.3 경제성DR           투자 0   ← 신설
    7.4 역률 개선          저투자 (콘덴서·APFR)
    7.5 태양광
    7.6 ESS
    7.7 잉여 활용

코드 docstring 13곳과 요구사항서 참조를 함께 고쳤다.

---

#### 2. 경제성DR 참여 여력 (6.6 진단 + 7.3 수단)

근거는 `data\source\전력시장운영규칙.pdf` 제12장이다.
**신뢰성DR(의무감축)이 아니라 경제성DR(자발적 입찰)만 다룬다.**

**거래일 제약이 이 기능의 전부다 (제12.4.2.1조 제1항 1호)**

토·일·공휴일을 모두 뺀 평일에만 입찰할 수 있다. 일요일은 공휴일 규정에 포함되어
자동으로 빠진다.

| 샘플 | 값 |
|---|---|
| 전체 일수 | 359일 |
| **거래 가능일** | **245일 (68.2%)** |
| 제외일 (토·일·공휴일) | 114일 |

**전체 일수로 재면 46% 부풀려진다.** 회귀로 고정했다.

**요금 계량의 '평일' 과 다르다 — 여기가 조용히 틀리기 쉬운 곳이다**

| | 토요일 | 일요일 | 공휴일 |
|---|---|---|---|
| 요금 계량 | 최대→중간부하 (공휴일 아님) | 전량 경부하 | 전량 경부하 |
| **DR 거래일** | **제외** | 제외 | 제외 |

토요일에서 갈린다. `diagnose.dr.dr_day_mask` 를 `tariff.tou` 와 **따로 두었고**,
두 판정이 실제로 다른 결과를 낸다는 것을 테스트로 못 박았다.

**샘플 진단 결과 (6.6)**

| 항목 | 값 |
|---|---|
| 주간(9~18시) 평균 부하 | 3,967 kW |
| 하위 10% | 3,208 kW |
| 기저부하 (6.1 비율 65.0% 재사용) | 2,577 kW |
| **등록 가능 용량 (보수적)** | **631 kW** |
| 평균 기준 여력 (비교용) | 1,390 kW |
| 무비용 감축 가능일 | 13일 (기준선 2,516 kW) |
| 자원 유형 | 표준DR, 중소형DR |
| 적합성 | 높음 |

**등록 용량을 평균이 아니라 하위 10% 로 잡았다.** 요구사항의 "감축 여력 = 대상일
주간 평균 − 기저부하" 는 `mean_reducible_kw` 로 함께 내되, **등록값은 어느
대상일에나 지킬 수 있어야** 한다 — 평균으로 등록하면 절반의 날에 미달하고
미달은 위약금이다. "보수적으로 산정한다" 는 지시를 이렇게 해석했다.

**기저부하는 6.1 의 비율을 그대로 재사용한다.** 다시 계산하지 않는 것을
테스트로 고정했다 (`profile.base_load_ratio == pattern.base_load_ratio`).

**저부하일 기준선은 거래 가능일만으로 계산한다.** 전 일자로 재면 기준선이
2,516 → 더 낮게 내려가 평일 저부하일이 걸리지 않는다. 이 비교도 회귀로 넣었다.

**7.3 수단**

- 감축량은 거래 가능일 기준으로만. 245일 × 631 kW × 1h = **154,648 kWh** (상한)
- 무비용 감축 가능일만 참여하면 8,206 kWh
- **정산 단가 미입력 시 금액이 아니라 사유**를 돌려준다
- 위약금 리스크 (별표26): `(감축계획량 − 실제감축량) × Max(하루전에너지가격, 0)`
- **기본요금 절감은 계산하지 않는다** — SMP 기준 산발 입찰이라 참여일이 연중
  최대수요일과 겹칠 확률이 낮다
- 확실성 **'중간'**. 필수 안내: "경제성DR은 수요관리사업자를 통해서만 참여할 수
  있습니다…"

**범위 밖 (근거 조문과 함께 1.5절·부록 D 에 명시)**

- 신뢰성DR (의무감축)
- **플러스DR** — 제12.7.3.1조 제3호에 따라 표준DR·중소형DR·제주DR·국민DR·
  주파수DR 은 자발적 수요증대 거래에 참여할 수 없다
- **CBL(고객기준부하)** — 제12.5.2.1조에 따라 전력거래소가 별표28 방식으로 산정한다
- 참여 자격 판정

**만든·고친 파일**

- `src\kwise\diagnose\dr.py` — 신규. 거래일·자원 유형·감축 여력·저부하일
- `src\kwise\measures\demand_response.py` — 신규. 7.3 + 별표26 위약금
- `src\kwise\tariff\power_factor.py` — 나목 반영 (`deemed_leading_pct`,
  `LEADING_LAGGING_DEEMED_PCT`)
- `src\kwise\measures\power_factor.py` — APFR·고정 콘덴서 경고
- `src\kwise\diagnose
eport.py` — `Diagnosis.dr`
- `src\kwise
eport\excel.py`, `batch.py`, `notices.py` — 진단·수단 시트, 부록 D
- `tests\test_dr.py` — 33건
- 요구사항서 — 6.6·7.3 신설, 7장 번호 확정, 1.5 범위 밖 4건, 부록 D 9→11항목

**미해결** — 신규 없음. 직전 세션의 두 건(제38조 확인 / 실측 역률 확보)은 그대로

**다음 세션(7세션)이 알아야 할 것**

- **DR 은 케이스 축이 아니라 모든 케이스에 붙는다.** `diagnose()` 가 항상
  `Diagnosis.dr` 를 채우므로 케이스 스터디에서 거래 가능일 수만 확인하면 된다
- 부하 유형별로 DR 적합성이 갈린다. **야간 피크형은 주간 여력이 작아 등급이
  낮게 나온다** — 세 유형 비교에서 이 대비가 드러날 것이다
- DR 금액을 내려면 케이스 YAML 에 `dr_unit_price_won_per_kwh` 를 넣는다.
  없으면 감축량만 나오고 금액 칸에 사유가 들어간다
- 역률 케이스를 넣을 때 야간 진상은 **기본이 0** 이다. 진상 페널티를 보이려면
  `leading_power_factor_pct=` 를 명시적으로 줘야 한다

**테스트**
- pytest: 514 passed (직전 478 + 이번 36: DR 33 + 역률 나목 3)
- ruff: pass (check + format)
- mypy: pass (strict, 69 files)

---

### 보완 세션 — 전압구분 잠금과 역률 정정 (2026-08-07)

준비 세션이 남긴 미해결 항목 하나를 닫고, 원문 확인으로 드러난 오류 하나를 고쳤다.

---

#### 1. 전압구분 잠금

준비 세션에서 "7세션 케이스 스터디에서 뒤집히는지 보고 정한다" 로 미뤄 두었던
항목이다. **결과를 보고 판단할 문제가 아니었다.**

| 전압구분 | 수전 전압 |
|---|---|
| 고압A | 3,300 ~ 66,000 V |
| 고압B | 154,000 V |
| 고압C | 345,000 V |

수전설비가 물리적으로 다르다. 154 kV 수전 건물이 22.9 kV 로 바꾸려면 변전설비를
새로 지어야 하므로 요금제 비교에 끼울 대상이 아니다. 계약종별을 가둔 것과 같은
이유다 — **고객이 고르는 값이 아니다.**

`switchable_selections(table, current)` 를 만들어 현행 종별·전압에 가뒀다.
바꿀 수 있는 것은 선택요금(Ⅰ·Ⅱ·Ⅲ)뿐이다.

    diagnose()                 option_totals 가 6개 → 3개 (고압A 만)
    evaluate_tariff_switch()   quotes 가 6개 → 3개
    list_selections()          드롭다운(부록 A.3)용으로 그대로 둔다 — 전부 보여 준다

`option_totals` 에 다른 전압이 섞여 들어와도 최적으로 뽑지 않도록
`min(selections, ...)` 로 바꿨다. 이전에는 넘겨받은 사전 전체에서 최소를 골라
`StopIteration` 이 날 수 있었다.

---

#### 2. 역률 정정 — 요구사항서 5.7 이 틀렸다

`data\source\기본공급약관.pdf` 원문 확인 결과 초판의 **"지상 90% 미만"** 은
오류였다. 기준은 **92%** 이고, 미달 시 추가만이 아니라 **초과 시 감액도 있다.**

| 조문 | 내용 |
|---|---|
| 제41조 | 지상역률 **92% 이상** 유지 의무 |
| 제42조 | **30분 누적 계량**. 무효전력계 미설치 고객은 **92%로 간주** |
| 제43조 ① | 대상은 무효전력계 설치 고압 이상 일반용·교육용·산업용 — **우리 종별 전부** |
| 제43조 ② | 08~22시 지상 92% / 22~08시 진상 95%, 매 1%당 기본요금의 0.2% |
| 제43조 ③ | 추가요금 첫 달 예고, 둘째 달부터 청구 |

**금액이 커서 경고로 둘 수 없다** — 기본요금의 −1.0% ~ +6.4% 다.

| 역률 | 조정 | 샘플 금액 (기본요금 4.53억) |
|---|---|---|
| 60% (하한 클램프) | **+6.4%** | +2,898만원 |
| 85% | +1.4% | +634만원 |
| **92% (기본값)** | **0** | **0원** |
| 97% (감액 상한) | **−1.0%** | **−453만원** |
| 야간 진상 90% | +1.0% | +453만원 |

**기본값 92% 에서 조정액이 정확히 0 이다.** 지어낸 값이 아니라 제42조가
무효전력계 미설치 고객에게 적용하는 **간주값**이라 근거가 있고, 덕분에
**3세션 이래의 회귀값이 하나도 바뀌지 않았다** (기본요금 452,832,624원,
요금적용전력 5,293.44 kW, 12.00개월분). 역률을 모르는 채로 금액을 만들어내지
않으면서 기구는 살아 있다.

**만든·고친 것**

- `src\kwise\tariff\power_factor.py` — 신규. 제41·42·43조 산식 전부
  (`lagging_adjustment_ratio`, `leading_adjustment_ratio`, `power_factor_charge`)
- `src\kwise\tariff\engine.py` — `BillingOptions.power_factor_pct` /
  `leading_power_factor_pct`, `BillingResult.total_power_factor_won` /
  `power_factor`, 월별 `power_factor_won` 열, 추적성(5.8)에 적용 역률
- `src\kwise\measures\power_factor.py` — 신규. **7.3 역률 개선 수단**
- `src\kwise\measures\solar.py` — 판정 창을 **08~22시**로 (약관과 같은 창),
  `SolarPoint.power_factor_extra_won` / `saving_after_power_factor_won`
- `src\kwise
eport\excel.py`, `batch.py` — 요약에 역률요금 행,
  수단별 시트에 역률 개선 행, 명세에 `power_factor_won`·`base_demand_kw`
- `tests\test_power_factor.py` — 39건

**역률 개선 수단 (7.3)**

투자 0원(7.1·7.2) 다음, 태양광 앞이다. 확실성 **'높음'** — 요금표와 약관만으로
확정되고 발전량 예측 같은 불확실성이 없다. 감도(9.2)도 적용하지 않는다.

| 케이스 | 절감액 | 근거 |
|---|---|---|
| 92% → 97% | **452.8만원** (기본요금의 정확히 1.0%) | 감액 상한 |
| 85% → 97% | **1,086.8만원** (2.4%) | 추가 1.4% 제거 + 감액 1.0% |

절감액은 두 역률에서 각각 `calculate_bill()` 을 부른 **재계산**이다. 비율만
곱해 어림하지 않는다. 97%를 넘기면 요금이 더 줄지 않으므로 과투자 경고를 내고,
**콘덴서를 키우면 야간에 진상으로 넘어간다**는 경고(제43조 ② 95% 조항)를
언제나 함께 낸다.

**PV 도입 후 역률**

판정 창을 발전 시간대(≈06~19시)에서 **08~22시**로 바꿨다. 약관이 그 창의
지상역률로 요금을 매기기 때문이다. 창이 바뀌어 샘플 960 kWp 의 도입 후 역률이
90.0% → **90.17%** 가 되었다(수치만 바뀜, 판정은 그대로). 기준도 90 → **92%** 라
경고 범위가 넓어졌고, 이제 **예상 추가요금과 그만큼 깎인 절감액을 함께** 낸다.

**계산에 넣지 않은 것 — 제43조 ③**

첫 달 예고·둘째 달 청구를 넣으면 기간에 따라 금액이 달라져 도입 전후 비교(Δ)가
흔들린다. 주석과 부록 D 로만 남기고, 실제 첫 달 청구서와 어긋날 수 있음을
산출물에 적는다.

---

#### 3. 문서

- **요구사항서 5.7 전면 개정** — 제41·42·43조 원문 표, 구현 방침 6개, 한계
- **7장에 7.3 역률 개선 신설**, 이후 번호를 밀었다 (태양광 7.3→**7.4**,
  ESS 7.4→**7.5**, 잉여 7.5→**7.6**). 코드 docstring 11곳도 함께 고쳤다
- **5.2 에 갑 종별 기본요금 기준 주석** — 약관 제68조 ②의 기준은 갑/을 구분이
  아니라 **최대수요전력계 설치 여부**다. 일반용(갑)Ⅱ 는 시간대별 구분계량기
  설치 고객이라 최대수요계가 있을 수 있으므로 제38조 확인이 필요하다.
  미해결 항목에 올렸다
- 부록 D 가 7항목 → **9항목** (역률 관련 2건 추가)
- 3.2 입력 표에 주간 지상역률·야간 진상역률을 나눠 적었다

**미해결** — 위 「미해결 항목」의 신규 3건 (제38조 확인 / 야간 진상 하한 /
실측 역률 확보)

**다음 세션(7세션)이 알아야 할 것**

- **역률이 케이스 축으로 하나 늘었다.** 같은 부하라도 역률 85% 건물과 95% 건물은
  기본요금이 3.4%p 다르다. 케이스 스터디에 역률이 다른 두 건물을 넣으면
  "투자 없이 되는 것" 의 크기가 달라지는 것을 보일 수 있다
- **PV 케이스는 역률 악화분을 반드시 함께 본다.** `SolarPoint` 에
  `power_factor_extra_won` 과 `saving_after_power_factor_won` 이 있다.
  절감액만 보면 과대평가된다
- 갑 종별 케이스는 `BillingOptions(contract_kw=...)` 가 필수다 (없으면 멈춘다).
  다만 갑Ⅱ 의 기본요금 기준은 제38조 확인 전까지 잠정이다
- `switchable_selections()` 를 쓴다. `list_selections()` 는 드롭다운용이다

**테스트**
- pytest: 478 passed (준비 세션 436 + 이번 42: 역률 39 + 전압구분 3)
- ruff: pass (check + format)
- mypy: pass (strict, 66 files)

---

### 준비 세션 — 사용량 파서 강화와 요금표 전 종별 확장 (2026-08-07)

7세션 케이스 스터디의 전제 두 가지를 만들었다. 케이스마다 다른 양식의 파일이
올라오고 종별도 제각각이라, 둘 다 없으면 케이스 스터디가 시작되지 않는다.

**받은 원본 두 개를 저장소에 넣었다.** "이 단가가 어디서 왔나" 를 나중에
추적하려면 원본이 있어야 한다.

    tests\fixtures\사용량조회_양식.xlsx              한전ON 원본 4열 양식 (헤더 + 1행)
    data\source\2026_KEPCO_Electricity_Tariff.xlsx   전 종별 요금표 (6시트)

---

#### A. 사용량 파서 강화 (`kwise.io`)

한전ON 다운로드 원본은 **계기번호 · 고객번호 · 검침일 · 순방향 유효전력량(KWH)**
네 열이다. 지금까지 쓰던 샘플은 앞 두 열을 지운 것이라, 원본 양식 그대로의
경로가 회귀로 고정되어 있지 않았다. 3단으로 찾는다.

| 단 | 방법 | 실패하면 |
|---|---|---|
| 1 | 헤더 행 탐색 — 상위 10행 중 날짜 이름과 kWh 이름이 함께 있는 행 | 2단으로 |
| 2 | 이름 매칭 — NFKC 정규화 후 후보 사전과 대조 (정확 → 부분) | 3단으로 |
| 3 | **내용 기반** — 값으로 판정 | 사유를 적고 예외 |

**3단 판정 기준**

    시각 열   파싱 성공률 ≥95% + 등간격성(연속 차이 최빈값 점유율). 수치형은 감점
    값 열     수치 변환 ≥95% + 음수 없음 + 고유값이 상수가 아닐 것

`ColumnDetection` 으로 **어느 열을 무엇으로, 어느 단계에서** 인식했는지와 후보별
점수·사유를 돌려준다. `UsageMeta.columns` 에 실려 8세션 UI 가 사용자에게 보여
주고 고칠 수 있다. 3단으로 갔거나 후보가 우열을 못 가리면 경고를 붙인다.

**고객번호를 거른 방법 — 여기서 한 번 틀렸다가 고쳤다**

처음에 고유값 **비율** 5% 를 문턱으로 뒀다. 그런데 실측 샘플의 15분 kWh 는
34,358행에 고유값이 1,417개뿐이라 **비율이 4.1%** 다 — 진짜 사용량 열이 잘려
나간다. 비율이 아니라 **개수**로 잘라야 한다.

| 열 | 왜 후보가 아닌가 |
|---|---|
| 고객번호 | 전 파일이 한 값이라 고유값 **1개**. 개수 문턱(3)에 걸린다 |
| 계기번호 | `55-282007100` — 문자열이라 수치 변환이 실패한다 |

계기번호는 함정이 하나 더 있다. 로더 본체의 `parse_usage_energy` 는 숫자 아닌
문자를 **모두 지우므로** `55282007100` 이라는 멀쩡한 수가 된다. 그래서 판정
단계는 쉼표만 걷어내고 `pd.to_numeric` 에 맡긴다 (`columns._numeric`).

**제목 행 처리** — 화면 저장본은 헤더 위에 제목이 한 줄 붙는다. 열 수가 달라
CSV 는 그대로 읽으면 `ParserError` 가 난다. 앞부분을 훑어 열 개수를 세고
`names=` 로 넉넉히 준 뒤, 본문은 `skiprows` 로 다시 읽는다. 내용 기반 단계는
**열이 꽉 찬 행만** 헤더 후보로 삼는다 — 제목 행을 헤더로 잡으면 진짜 헤더가
데이터 첫 줄로 섞여 들어간다.

**CSV 인코딩 채택 규칙을 지켰다.** 1차는 이름 매칭만 허용한다. 내용 기반은
헤더가 깨져도 숫자가 멀쩡해 성공해 버려서, 잘못된 인코딩이 채택된다.
이름으로 아무 인코딩도 통과하지 못했을 때만 2차로 값 판정을 연다.

**만든·고친 파일**

- `src\kwise\io\columns.py` — 신규. 3단 판정 전부
- `src\kwise\io\usage.py` — `_read_raw`/`_read_body` 2회 읽기, `UsageMeta.columns`
- `tests\test_io_columns.py` — 25건
- `tests\_synthetic.write_kepco_file()` / `kepco_month_rows()`

**검증한 경로** — 8가지 조합(csv/xlsx × 정상·제목행·알아볼 수 없는 헤더)이
모두 2,976 슬롯으로 읽힌다. **실측 샘플(2열 CSV)은 부록 B 와 전부 그대로다**
(34,358행 / 972결측 / 5,293.44 kW @ 07-03 09:30 / 22,284.8 MWh / 부하율 49.0%).

---

#### B. 요금표 전 종별 확장 (`data\tariff_kr_20260601.json`)

**수기 입력하지 않았다.** 엑셀을 읽는 변환기를 만들고 돌렸다.

    src\kwise\tariff\source_excel.py   변환 로직 (테스트 대상)
    tools\build_tariff.py              CLI. --check / --contracts 로 단계 진행

**주의 1 — 시트마다 계절 열 순서가 다르다**

| 시트 | 5열 | 6열 | 7열 |
|---|---|---|---|
| 일반용·교육용 | 여름철 | 봄·가을철 | 겨울철 |
| **산업용** | **봄·가을철** | **여름철** | 겨울철 |

위치로 읽으면 산업용의 여름과 봄·가을이 통째로 뒤바뀐다. 3행 헤더를 읽어
이름으로 매핑한다 (`season_columns`). 검산도 회귀로 고정했다 — 산업용(을)
고압A 선택Ⅰ 최대부하 **여름 234.5 / 봄가을 156.4**. 뒤바뀌면 봄·가을 단가가
여름보다 비싸져 **태양광 평가가 정반대로 나온다.**

**주의 2 — 엑셀에 없는 것을 덮어쓰지 않았다**

엑셀에는 단가 테이블과 계절·시간대 구분만 있다. 아래는 `source_excel` 이
코드로 들고 있고 각각 테스트가 붙어 있다.

    토요일 peak_to_mid / 일요일·공휴일 all_to_light
    산업용(을) 봄·가을 주말 할인 특례 (토·일·공휴일 11~14시 50%)
    요금적용전력 3규칙 (경부하 제외 / 대상월 7·8·9·12·1·2 / 하한)
    임계값 — 일반용·산업용 300 kW, **교육용 1,000 kW**
    contract_floor_ratio — 을 종별 30%, **교육용(을) 15% 특례**

**주의 3 — 검증을 건너뛰지 않았다**

부록 A.2 4규칙을 8종별 전부에 돌려 통과시켰다. 변환기는 검증에 걸리면
**파일을 쓰지 않는다.** 기존 일반용(을) 고압A·B **60개 값(기본 6 + 전력량 54)
대조**도 변환기 안에 넣었고 테스트로도 고정했다 — 하나도 바뀌지 않았다.

**넣은 종별과 그 결과**

| 종별 | 키 | 전압 | 기본요금 기준 | 시간대 | 하한 |
|---|---|---|---|---|---|
| 일반용(갑)Ⅰ | `general_a_1` | 저압·고압A·B | 계약전력 | 전체시간 | — |
| 일반용(갑)Ⅱ | `general_a_2` | 고압A·B | 계약전력 | 시간대별 | — |
| 일반용(을) | `general_b` | 고압A·B | 요금적용전력 | 시간대별 | 30% |
| 산업용(갑)Ⅰ | `industrial_a_1` | 저압·고압A·B | 계약전력 | 전체시간 | — |
| 산업용(갑)Ⅱ | `industrial_a_2` | 고압A·B | 계약전력 | 시간대별 | — |
| 산업용(을) | `industrial_b` | 고압A·B·**C** | 요금적용전력 | 시간대별 | 30% |
| 교육용(갑) | `education_a` | 저압·고압A·B | 계약전력 | 전체시간 | — |
| 교육용(을) | `education_b` | 고압A·B | 요금적용전력 | 시간대별 | **15%** |

농사용·가로등·심야·전기차·보완전력·주택용은 범위 밖이라 넣지 않았다 (부록 A.4).

**확장하면서 드러난 것 세 가지 — 코드도 고쳤다**

① **갑 종별의 기본요금은 계약전력 기준이다** (기본공급약관 제68조). 요금적용전력으로
매기면 기본요금이 통째로 틀린다. `ContractType.base_fee_basis` 를 만들고 엔진이
분기하게 했다. 계약전력을 모르면 **만들어내지 않고 멈춘다.** 월별 행에
`base_demand_kw`(기본요금에 실제로 곱한 kW)를 추가했다.

② **'전체시간' 종별은 시간대 구분이 없다.** 세 시간대에 같은 단가를 채우고
`time_of_use=False` 로 표시한다. 검증 규칙 1 의 `경<중간<최대` 대신
**세 칸이 같은지**를 검사한다. 규칙 2(균일성)도 끈다 — 전체시간 종별은
선택Ⅰ→Ⅱ 할인폭을 계절별로 다르게 설계하므로(일반용(갑)Ⅰ 고압A: 여름 4.0 /
봄가을 4.3 / 겨울 5.3원) 요금표가 약속하지 않은 것을 검사하는 셈이 된다.
손익분기(규칙 3)는 그대로 본다.

③ **선택요금 전환이 계약종별을 넘나들고 있었다.** `list_selections(table)` 을
필터 없이 돌아, 종별이 하나일 때는 안 보이던 결함이 8종별이 되자 드러났다.
일반용(을) 건물에 산업용(을) 을 권할 수는 없다 — 용도로 정해지는 계약이지
선택요금이 아니다. `diagnose` 와 `evaluate_tariff_switch` 를 현행 종별로 가뒀다.
**전압구분은 아직 넘나든다** — 위 「미해결 항목」에 남겼다.

**만든·고친 파일**

- `src\kwise\tariff\source_excel.py` — 신규. 변환 로직과 `CONTRACT_RULES`
- `tools\build_tariff.py` — 신규. CLI + 검증 + 60개 값 회귀 대조
- `data\tariff_kr_20260601.json` — 8종별로 재생성 (schema 0.1 → **0.2**)
- `src\kwise\tariff\schema.py` — `base_fee_basis`, `time_of_use`
- `src\kwise\tariff\validate.py` — 전체시간 종별 처리, 산업용(을) 정책 5쌍
- `src\kwise\tariff\engine.py` — 계약전력 기준 기본요금, `base_demand_kw`
- `src\kwise\diagnose\report.py`, `src\kwise\measures\tariff_switch.py` — 종별 한정
- `tests\test_tariff_source.py` — 28건
- `pyproject.toml` — mypy 검사 대상에 `tools` 추가

**미해결**

- 위 「미해결 항목」의 두 신규 항목 (전압구분 넘나듦 / 엑셀 옮김 오차)
- 요금 데이터는 여전히 `verified: false`

**다음 세션(7세션)이 알아야 할 것**

- **케이스 스터디에 부하 유형 세 가지를 넣는다.** 여름 주간 피크형(샘플),
  봄·가을 피크형, 야간 피크형. 6세션 (1) 에서 확인한 정정 ①·②의 비대칭은
  세 유형이 다 있어야 드러난다. 야간 피크형 생성기는
  `tests\_synthetic.night_peak_month()` 에 있다
- **이제 종별도 케이스 축이다.** 산업용(을) 봄·가을 주말 할인은 PV 최성기와
  겹치므로, 같은 부하라도 일반용(을) 과 산업용(을) 의 PV 절감액이 다르게 나온다.
  이 대비를 케이스로 넣으면 특례 반영 여부를 실증할 수 있다
- 갑 종별 케이스를 넣으려면 `BillingOptions(contract_kw=...)` 가 **필수**다.
  없으면 `TariffDataError` 로 멈춘다 (일부러 그렇게 두었다)
- 요금표를 갱신할 때는 엑셀을 바꾸고 `tools\build_tariff.py` 를 다시 돌린다.
  JSON 을 손으로 고치면 `test_shipped_json_equals_a_fresh_conversion` 이 잡는다
- 새 종별을 넣을 때는 `source_excel.CONTRACT_RULES` 에 규칙을 먼저 적는다.
  엑셀에 있어도 규칙이 없으면 변환기가 거부한다 (조용히 기본값을 만들지 않는다)
- 업로드 파일의 열 판정 결과는 `usage.meta.columns` 에 있다. 8세션 UI 는
  `describe()` 를 보여 주고 후보 목록으로 드롭다운을 만들면 된다

**테스트**
- pytest: 436 passed (6세션 382 + 이번 54: 파서 25 + 요금표 28 + 데이터 1)
- ruff: pass (check + format)
- mypy: pass (strict, 63 files — `tools` 포함)

---

### 6세션 (3) — 산출물 회귀와 경부하 제외 (2026-08-07)

6세션 (2) 가 남긴 두 항목을 마무리했다. **6세션은 여기서 끝난다.**

**① `tests\test_report.py` — 36건**

수동으로만 돌려 보던 Excel·CLI 를 회귀로 고정했다.

| 묶음 | 내용 |
|---|---|
| 시트 구성 | 8시트 순서, openpyxl 왕복 후에도 동일, 없는 조각은 **빈 장이 아니라 시트 자체를 만들지 않음**, `--no-timeseries` 로 35,328행 생략 |
| tz 해제 | tz-aware 인덱스·컬럼이 섞인 프레임 저장 시 예외 없음, 원본 불변, Period 인덱스 → 문자열 |
| 요약 필수 문구 | 추적성 5줄(5.8), 미포함 요금요소(5.1), 계약전력 변경 경고(9.4), **알려진 한계 8항목 전부**(부록 D), 결측 미보간, 조합 재계산 주석, 태양광 판정 모집단 |
| 미산출 표기 | `format_won(None)` 이 빈칸이 아니라 사유. 감도 시트도 빈 장 대신 사유 |
| 쓰기 실패 | `PermissionError` → `ReportWriteError("Excel 에서 파일을 닫아 주세요")`. 파일명 접미사 `result_20260807_1430.xlsx` |
| 조합 재계산 | 조합 비교 시트의 `+ 태양광` 절감액 ≠ 요금제 절감 + PV 단독 절감 (차이가 100만원 이상이라 어림 오차가 아니다) |
| 확실성 | 시트에 실린 등급이 기준선·요금제 높음 / +PV 중간 / +PV+ESS **중간~낮음**. 수단별 시트는 각자 등급 |
| CLI | `CliRunner` 로 2케이스 YAML 정상 종료(exit 0), 케이스별 xlsx 2개 + 요약 CSV 1개, CSV 가 **utf-8-sig BOM**, 한글 케이스명 유지, `--resume` 이 두 건 모두 건너뜀 |

CLI 테스트는 합성 한 달치 파일 두 개를 쓴다. 네트워크도 태양광도 타지 않는다.
`PROJECT_CACHE` 를 `monkeypatch.setenv` 로 tmp 에 돌려 저장소를 더럽히지 않는다.

**② 태양광 피크 기여 판정에서 경부하 제외**

`top_slots` 산정에 `demand_eligible` 마스크를 적용했다. 경부하 구간은 애초에
요금적용전력 대상이 아니므로 그 시각에 피크가 몰려도 태양광의 기본요금 기여와
무관하다.

`PeakProfile` 이 상위 구간을 **두 벌** 들고 있게 했다. 섞지 않는다.

| 필드 | 모집단 | 쓰임 |
|---|---|---|
| `top_slots` / `hour_counts` / `weekday_counts` | 관측 전 슬롯 | **부록 B 시각 분포 (라벨 기준 원값)** |
| `demand_top_slots` / `demand_hour_counts` / `demand_weekday_counts` | 요금적용전력 대상 슬롯 | **태양광 등급 판정** |

`hour_share()` 는 원값, `demand_hour_share()` 는 대상 기준이고
`judge_pv_potential()` 은 후자를 쓴다. 마스크를 주지 않으면 두 벌이 같아진다.
판정 모집단은 `ImprovementSummary.pv_basis` 에 문장으로 남아 요약 시트에 실린다.

**샘플에서 결과가 바뀌지 않는다 — 회귀로 고정**

상위 100구간이 07~17시라 딱 한 칸만 교체된다. 라벨 `07:45`(구간 시작 07:30)가
경부하라 빠지고 그 자리를 `09:45` 가 채운다.

| | 시각 분포 | 정오 비율 | 등급 |
|---|---|---|---|
| 전 슬롯 (부록 B) | 7시 1, 8시 6, 9시 5, … | 66% | 높음 |
| 요금적용전력 대상 | 8시 6, **9시 6**, … (7시 없음) | **66%** | **높음** |

**야간 피크형에서는 등급이 뒤바뀐다 — 합성 픽스처로 검증**

`tests\_synthetic.night_peak_month()` — 야간(경부하) 2,000 kW, 정오 1,200 kW,
나머지 200 kW 인 한 달치.

| 모집단 | 정오 비율 | 등급 |
|---|---|---|
| 전 슬롯 | 0% | **낮음** |
| 요금적용전력 대상 | 95% | **높음** |

이 건물의 관측 최대는 2,000 kW 지만 요금적용전력은 1,200 kW 다. 야간 피크는
기본요금에 닿지 않으므로 태양광 판정에서도 빼는 것이 맞다. 마스크를 씌워도
`hour_counts`(원값)는 그대로라는 것도 함께 고정했다.

**고친 파일**

- `src\kwise\diagnose\peak.py` — `_rank_slots` 분리, `demand_*` 세 필드,
  `demand_hour_share()`, `demand_weekend_slots`, `demand_eligible_slots/applied`
- `src\kwise\diagnose\summary.py` — `judge_pv_potential` 이 대상 모집단을 쓴다.
  `pv_basis_label()` 신규, `ImprovementSummary.pv_basis`
- `src\kwise\diagnose\report.py`, `__init__.py` — 배선
- `src\kwise\report\excel.py` — 요약에 '태양광 판정 모집단' 행, 진단 시트에
  두 분포를 `(전 슬롯)` / `(요금적용전력 대상)` 라벨로 나란히

**만든 파일**

- `tests\test_report.py` (36건)
- `tests\_synthetic.night_peak_month()`
- `tests\test_diagnose.py` 에 5.2 ① 절 6건 추가

**미해결**

- 없다. 6세션 범위 항목은 모두 끝냈다. 저장소 전체 항목은 위 「미해결 항목」 참조

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **케이스 스터디에 부하 유형 세 가지를 넣는다.** 여름 주간 피크형(샘플),
  봄·가을 피크형, 야간 피크형. 6세션 (1) 에서 확인한 대로 정정 ①(경부하 제외)과
  ②(대상월 한정)가 PV 에 **반대 방향**으로 작용하며 어느 쪽이 지배하는지는
  부하 형태에 달렸다. 세 유형이 다 있어야 이 비대칭이 드러난다
- 야간 피크형 합성 부하는 `tests\_synthetic.night_peak_month()` 에 이미 있다.
  케이스 스터디용으로 규모만 키우면 된다
- 배치 CLI 는 `run_batch(config, resume=True)` 로 이어 돌릴 수 있다.
  중간 결과는 `PROJECT_CACHE\batch\<정의파일>\<케이스>.json`
- Excel 산출물의 '진단' 시트에는 시각 분포가 두 벌 실린다.
  UI(8세션)에서 표로 그릴 때 어느 쪽을 쓸지 명시할 것 — 부록 B 대조는 전 슬롯,
  태양광 판단 근거는 요금적용전력 대상
- 요금 데이터는 여전히 `verified: false` 다. 청구서를 확보하면 일요일 경부하
  계량부터 대조한다

**테스트**
- pytest: 382 passed (1~6세션 340 + 이번 42)
- ruff: pass (check + format)
- mypy: pass (strict, 58 files)

---

### 6세션 (2) — 조합 비교·출력 (2026-08-06)

요금적용전력 정정을 마친 뒤 이어서 작업했다. 코드는 모두 커밋되어 있고
`tests\test_report.py` 만 남겨 두었다 — **6세션 (3) 에서 마무리했다.**

**만든 파일**

- `src\kwise\compare\combination.py` — `CombinationSpec`, `evaluate_combination`,
  `compare_combinations`, `default_combinations`
- `src\kwise\compare\sensitivity.py` — 조합 하나를 감도 3종으로 평가
- `src\kwise\report\excel.py` — 여덟 시트, tz 해제, 날짜·시각 접미사
- `src\kwise\report\notices.py` — 5.1·9.4·부록 D 문구, 미산출 사유 표기
- `src\kwise\report\batch.py` — YAML 케이스 순차 실행, 재개, 요약 CSV
- `src\kwise\cli.py` — `python -m kwise.cli run --cases cases.yaml`
- `tests\test_compare.py` — 26건

**확인한 것**

- 조합 절감액이 개별 합과 다르다 (재계산이 실제로 일어난다)
- 확실성 등급이 최저 구성 요소를 따른다 (기준선·요금제 높음 / +PV 중간 / +ESS 중간~낮음)
- ESS 야간 충전이 새 피크를 만드는 경로를 재현하고 경고·`charge_limit_kw` 로 막았다
- CLI 2케이스 YAML 이 끝까지 돈다 (수동 확인. 테스트로는 미고정)
- Excel 8시트가 35,328행 시계열까지 1.5초에 저장된다 (수동 확인)

**당시 안 한 것** — 모두 6세션 (3) 에서 처리했다

- ~~`tests\test_report.py`~~ — 36건으로 작성
- ~~Excel·CLI 가 회귀로 고정되지 않음~~ — `CliRunner` 2케이스와 openpyxl 왕복으로 고정
- ~~태양광 피크 기여 판정에서 경부하 제외~~ — `demand_top_slots` 로 분리

---

### 6세션 (1) — 요금적용전력 규칙 정정 (2026-08-06)

요구사항서 5.2 절이 전면 개정되어(한전 기본공급약관 제68조) 3세션 요금 엔진의
빠진 규칙을 먼저 고쳤다. 조합 비교·출력 작업은 그대로 두고 이어간다.

**고친 것**

- ① 대상 시간대 — 중간·최대부하만. 경부하(22:00~08:00) 수요는 아무리 커도
  요금적용전력이 되지 않는다. 공휴일은 전량 경부하로 계량되어 자동 제외된다
- ② 대상 월 — `max(7·8·9월, 12·1·2월, 검침 당월)`. 3~6월·10~11월 피크는
  당월분으로만 잡히고 이월되지 않는다
- ③ 하한 — 계약전력의 30% (종별 속성, 교육용 등 15% 특례)

**만든·고친 파일**

- `src\kwise\tariff\demand.py` — 신규. `is_demand_month`, `demand_eligible_mask`,
  `monthly_demand_basis`, `billing_demands`, `apply_contract_floor`
- `data\tariff_kr_20260601.json` — 종별에 `demand_bands`·`demand_months`·
  `contract_floor_ratio` 추가
- `src\kwise\tariff\schema.py`, `engine.py` — 규칙 적용, 월별 행에
  `demand_basis_kw`·`demand_before_floor_kw` 추가
- `src\kwise\diagnose\peak.py`, `report.py` — 진단도 같은 규칙으로
- `src\kwise\measures\contract.py` — 하한 기본값을 종별 속성에서 가져온다

**샘플 재계산 결과 (일반용(을) 고압A 선택Ⅰ, 계약전력 5,500 kW)**

| 항목 | 정정 전 | 정정 후 | 비고 |
|---|---|---|---|
| 요금적용전력 | 5,293.44 kW | **5,293.44 kW** | 변화 없음 |
| 기본요금 합계 | 452,832,624 원 | **452,832,624 원** | 변화 없음 |
| PV 480 kWp 기본요금 절감 | 11,652,310 원 | **17,401,507 원** | **+49%** |
| PV 960 kWp 기본요금 절감 | 20,380,190 원 | **31,518,402 원** | +55% |
| 계약전력 조정 절감 (5,500 kW) | 미산출 | **0 원** | 하한 1,650 kW 가 안 걸린다 |
| 계약전력 조정 절감 (20,000 kW) | 미산출 | **67,007,376 원** | 하한 6,000 kW 가 걸린다 |

**요금적용전력이 그대로인 이유**

이 건물의 연간 최대(2023-07 5,293.4 kW @ 09:30)가 **대상월이면서 중간부하**라
세 규칙 어디에도 걸리지 않는다. 월별 최대 13개가 모두 08:15~16:30 사이의
중간·최대부하 시간대라 경부하 제외의 영향도 없었다. 하한 1,650 kW 는 훨씬 아래다.
**정정의 효과는 부하 형태에 달렸다** — 야간 피크형이나 봄·가을 피크형 건물에서는
결과가 크게 달라진다.

**PV 기본요금 절감이 오히려 늘어난 이유 (예상과 반대)**

요구사항서는 "봄·가을 피크 저감은 기본요금 가치가 거의 없다"고 경고하지만,
이 건물에서는 절감액이 **줄지 않고 49% 늘었다.** 근거는 데이터에 있다.

PV 960 kWp 도입 후 2023-08 의 **관측** 최대는 5,090.0 kW 인데 요금적용전력은
4,917.9 kW(7월분)다. 8월의 관측 최대가 경부하 시간대에 있어 대상에서 빠졌기 때문이다.
정정 전 엔진은 PV 가 손댈 수 없는 야간·공휴일 피크를 요금적용전력으로 삼아
PV 의 기여를 깎아먹고 있었다. 2023-06(비대상월) 피크 4,934.5 kW 가 7월로 이월되지
않는 것도 확인된다.

즉 **①이 PV 에 유리하게, ②가 불리하게 작용하는데 이 건물에서는 ①이 훨씬 컸다.**
봄·가을 피크형 건물이라면 ②가 지배해 반대 결과가 나온다. 이 비대칭을 산출물
주석으로 넣었다.

**테스트**
- 신규 회귀 8건 (대상월 vs 계절 경계, 이월 안 됨, 경부하 제외, 공휴일 자동 제외,
  하한 적용, 초과사용부가금 검사)
- 정정으로 뒤집힌 기존 테스트 4건을 새 규칙으로 갱신
- pytest 340 passed / ruff pass / mypy strict pass

---

### 5세션 — 개선 수단 평가 (2026-08-06)

**완료**

- `kwise.measures` — 7.1~7.5 전체. 투자비 낮은 순으로 배치
- 절감액은 모두 재계산이다. 수단을 적용한 15분 부하를 만들어 `calculate_bill()` 을 다시 부른다
- 단위테스트 44건 추가 (누적 313건)

**만든 파일**

- `src\kwise\measures\base.py` — 확실성 등급, 12개월 환산, 회수기간
- `src\kwise\measures\netload.py` — `with_load`, `apply_generation` (순부하·잉여 분리)
- `src\kwise\measures\tariff_switch.py` — 7.1
- `src\kwise\measures\contract.py` — 7.2
- `src\kwise\measures\solar.py` — 7.3 용량 곡선, 역률 악화 판정
- `src\kwise\measures\ess.py` — 7.4 초과 분석·디스패치·경제성
- `src\kwise\measures\surplus.py` — 7.5
- `tests\test_measures.py`

**샘플 평가 결과 (일반용(을) 고압A 선택Ⅰ 기준선)**

| 수단 | 결과 |
|---|---|
| 7.1 선택요금 전환 | 선택Ⅱ 로 **5,358만원** 절감. 투자 0원, 확실성 높음 |
| 7.2 계약전력 조정 | 계약 7,000 kW 가정 시 하향 여지 1,177 kW. 하한 미확인이라 **금액 미산출**. 하한 100% 를 주면 1억 198만원으로 재계산됨 |
| 7.3 태양광 | 옥상 20,000 m² → 상한 960 kWp. 자가소비율 100%(부하가 커서 잉여 없음). 역률 92.0% → **90.0%** 로 떨어져 콘덴서 경고 발생 |
| 7.4 ESS (목표 5,200 kW) | 출력 **93.4 kW** / 용량 **41.1 kWh** — 분리해서 낸다. 절감 670만원, 회수 2.5년, 손익분기 단가 163만원/kWh |
| 7.5 잉여 활용 | 샘플은 잉여 0 kWh. 합성 케이스(부하 100 kW × PV 1,000 kWp)로 시나리오 3종을 검증 |

**부록 B 목표 피크별 초과 분석 — 회귀로 고정**

| 목표 | 구간 | 시간 | 최대 초과 출력 | 총 초과 에너지 |
|---|---|---|---|---|
| 5,200 kW | 13 | 3.25 h | 93.4 kW | 91.3 kWh |
| 5,000 kW | 496 | 124.0 h | 293.4 kW | 9,743.0 kWh |
| 4,800 kW | 1,225 | 306.25 h | 493.4 kW | 52,964.2 kWh |
| 4,500 kW | 2,160 | 540.0 h | 793.4 kW | 178,288.6 kWh |

**결정 사항** (위 「결정 사항」 5세션 항목에도 반영)

- 4세션이 남긴 "순부하를 어떻게 넘길지" 를 **새 `UsageData` 를 만드는 쪽**으로 정했다
- ESS 용량 산정 기준을 '하루 최대' 로 잡았다. 처음 '연속 구간 최대' 로 두었더니
  샘플 5,200 kW 목표에서 달성 피크가 5,244 kW 로 목표를 놓쳤다
- ESS 는 전력량요금도 소폭 절감된다. 최대·중간부하에 방전하고 경부하에 충전하므로
  단가 차이가 왕복손실(12%)보다 크기 때문이다. 요금표에 달린 부호라 테스트에 근거를 적어 두었다

**미해결**

- 계약전력 하한 규정 (기존). measures 는 값을 받을 준비가 끝났다.
  `evaluate_contract_adjustment(contract_floor_ratio=...)` 에 넣으면 바로 재계산된다
- Open-Meteo 일사량 라벨 규약 (기존)

**다음 세션이 알아야 할 것**

- **조합 비교(8장)는 수단을 겹칠 때마다 요금을 다시 계산한다.** `apply_generation()` 으로
  PV 를 뺀 뒤 그 결과에 `dispatch_peak_shaving()` 을 물리고, 마지막에 `calculate_bill()` 을
  한 번 부른다. 수단별 절감액을 더하면 안 된다
- 확실성 등급은 `measures.Certainty` 에 있다 (높음/중간/중간~낮음).
  각 수단 결과가 `certainty` 필드로 들고 있으므로 비교표에 그대로 옮기면 된다
- 각 수단 결과에 `warnings`·`notes` 가 있다. Excel 요약 시트에 그대로 싣는다
- Excel 출력 시 `BillingResult.monthly` 는 Period 인덱스다. `to_timestamp()` 로 바꾸고
  tz-aware 컬럼은 `dt.tz_localize(None)` 로 푼다
- 감도 3종은 PV 출력에만 적용한다. 7.1·7.2 결과는 감도와 무관하다는 것을 테스트로 고정했다
- 케이스 스터디(7세션)용 합성 부하 생성기는 `tests\_synthetic.py` 에 있다

**테스트**
- pytest: 313 passed (1~4세션 269 + 이번 44)
- ruff: pass (check + format)
- mypy: pass (strict, 49 files)

---

### 4세션 — 태양광과 진단 (2026-08-06)

**완료**

- `kwise.pv` — 기상 취득·캐시, 다중 어레이 시뮬레이션, 시각 정렬, 감도 3종
- `kwise.diagnose` — 6.1~6.5 전체. 설비 정보를 받지 않는다
- 단위테스트 69건 추가 (누적 269건)

**만든 파일**

- `src\kwise\pv\config.py` — `ArrayConfig`(지붕·벽면), `PvSystemConfig`
- `src\kwise\pv\weather.py` — `load_weather`, parquet 캐시, Open-Meteo
- `src\kwise\pv\simulate.py` — `simulate` (infinite_sheds, GCR 단일 파라미터)
- `src\kwise\pv\alignment.py` — `align_simulation`, `daylight_mask`, `slot_midpoints`
- `src\kwise\pv\sensitivity.py` — 감도 3종
- `src\kwise\diagnose\peak.py` / `contract.py` / `structure.py` / `summary.py` / `report.py`
- `tests\test_pv.py`, `tests\test_diagnose.py`

**시각 정렬 검증 결과 — 여기가 이번 세션의 핵심이다**

맑은 날 남향 어레이의 발전 무게중심은 남중 시각과 같아야 한다. 서울(127.0°E)
2023-07-03 의 남중은 12:36:10 이다.

| 정렬 방식 | 무게중심 | 남중과의 차이 |
|---|---|---|
| **구간 중앙 보정 (채택)** | 12:36:10 | **0.17초** |
| 반 칸 빠뜨림 (라벨을 순시각으로) | 12:43:41 | 7.5분 |
| 한 칸 밀림 | 12:51:10 | 15분 |

세 경우를 모두 테스트로 고정했다. 오차 1분 임계면 반 칸 실수도 걸린다.
일출·일몰 대조도 함께 넣었다 — 시간별을 15분으로 선형 보간하면 일몰 뒤로
발전량이 새므로 태양 고도로 잘라냈다.

**샘플 진단 결과 (일반용(을) 고압A 선택Ⅰ, 계약전력 5,500 kW)**

| 항목 | 값 |
|---|---|
| 월별 최대수요 13개 | 부록 B 표와 전부 일치 |
| 요금적용전력 | 5,293.44 kW (8월 최대수요 5,288 kW 보다 큼 — 12개월 규칙) |
| 상위 100구간 시각 | 10시 15개, 11시 14개, 12시 20개 — 부록 B 와 일치 |
| 상위 100구간 요일 | 월25 화32 수12 목10 금21, **주말 0** |
| 태양광 피크 기여 | **높음** (상위 구간의 66%가 10~15시) |
| 기본요금 : 전력량요금 | 13.5% : 86.5% |
| 시간대별 사용량 | 경 39.8% / 중간 38.3% / 최대 21.9% |
| 선택요금 전환 | 선택Ⅱ 로 **5,358만원** 절감 (조합마다 재계산) |
| 계약전력 | 여유율 96.2% — 하향 여지 없음 (오히려 여유가 10% 미만) |

**결정 사항** (위 「결정 사항」 4세션 항목에도 반영)

- 부록 B 의 시각 분포는 **검침 라벨 기준**이었다 (구간 시작 기준으로 세면
  9시 5→10개, 10시 15→14개로 달라진다). 요금 귀속만 구간 시작으로 하고
  피크 발생 시각 보고는 라벨로 한다. `top_slots` 에 두 값을 모두 담았다
- 계약전력 조정 절감액은 하한 규정을 모르는 한 산출하지 않는다.
  하향 여지(kW)와 위약금 경고는 언제나 낸다
- PowerShell 폴백은 유지하기로 했다 (3세션까지 미결 항목)

**미해결**

- 계약전력 하한 규정 (기존 항목). 6.4 절감액이 여기 걸려 있다
- Open-Meteo 일사량 라벨 규약 (신규). 실측 발전 데이터로 대조해야 확정된다

**다음 세션이 알아야 할 것**

- **PV 절감액은 빼기가 아니라 재계산이다.** `align_simulation()` 으로 부하 라벨에
  맞춘 뒤 `net_kw = load_kw − pv_kw` 를 만들고, 그 시계열로 `calculate_bill()` 을
  다시 부른다. PV 가 사용량을 줄이면 최적 선택요금이 바뀐다
- 순net 부하로 요금을 계산할 때 전력량은 `UsageData.energy_kwh()` 규약을 지켜야 한다.
  PV 를 뺀 새 `UsageData` 를 만들지, 요금 엔진에 시계열을 직접 넣을지는 5세션에서 정한다
- 감도는 PV 출력에만 적용한다. `iter_scenarios()` 는 제너레이터이므로
  시나리오별 요금 재계산도 순차로 돌리고 요약만 남긴다
- 용량 곡선(7.3)은 `PvSystemConfig.scaled(kwp)` 로 비율을 유지한 채 크기만 바꾼다.
  20단계를 순차 처리한다
- ESS 목표 피크 역산(7.4)은 `PeakProfile` 의 월별 최대수요와
  `billing_demands()` 를 쓰면 된다. 초과 구간·에너지는 부록 B 에 기대값이 있다
- 기상 캐시는 `PROJECT_CACHE\weather\openmeteo_*.parquet`. 테스트는
  `load_weather(fetch=...)` 로 합성 데이터를 넣어 네트워크를 타지 않는다
- `tests\_synthetic.clearsky_weather()` 가 맑은 날 합성 기상을 만든다

**테스트**
- pytest: 269 passed (1~3세션 200 + 이번 69)
- ruff: pass (check + format)
- mypy: pass (strict, 41 files)

---

### 3세션 — 요금 엔진 (2026-08-06)

**완료**

- 요금 데이터 `data\tariff_kr_20260601.json` (부록 A.1 스키마, 일반용전력(을) 고압A·B)
- 부록 A.2 검증 규칙 4종을 검증기와 테스트 양쪽으로 구현
- 시간대 분류기 — 계절·시간대·요일 규칙, 공휴일, 임시공휴일 추가·제외, 특례 할인
- 요금 계산기 — 월별 최대수요, 요금적용전력 12개월 규칙, 기본요금·전력량요금,
  결측 월 두 기준, 부분 월 처리, 12개월 환산, 추적성
- `energy_kwh()` 의 kW 환산 차단 (2세션 장치의 나머지 절반)
- 단위테스트 130건 추가 (누적 200건). 요구사항서 11.2 항목을 모두 덮었다

**만든 파일**

- `data\tariff_kr_20260601.json`
- `src\kwise\tariff\schema.py` — 로딩·조회·드롭다운 생성 (부록 A.3)
- `src\kwise\tariff\validate.py` — 부록 A.2 규칙 1~4
- `src\kwise\tariff\holiday.py` — 공휴일 달력 (`calendar` 는 표준 라이브러리와 겹쳐 피했다)
- `src\kwise\tariff\tou.py` — `classify_slots`
- `src\kwise\tariff\engine.py` — `calculate_bill`, `billing_demands`
- `tests\test_tariff_data.py`, `tests\test_tariff_tou.py`, `tests\test_tariff_engine.py`

**요금표 검증 결과 (부록 A.2)**

| 규칙 | 결과 |
|---|---|
| 1 순서 관계 | 통과 — 경<중간<최대, 기본 Ⅰ<Ⅱ<Ⅲ, 전력량 Ⅰ>Ⅱ>Ⅲ, 여름 최대>봄가을 최대 |
| 2 균일성 | 고압A Ⅰ→Ⅱ 전 칸 5.5원 / 고압B Ⅰ→Ⅱ 3.8원 / 고압B Ⅱ→Ⅲ 1.6~1.7원 |
| 2 예외 | 고압A Ⅱ→Ⅲ 0.6~12.4원 — 최대부하 할인폭이 경·중간보다 큼을 대신 확인 |
| 3 손익분기 | 고압A Ⅰ→Ⅱ 200 h / 고압B Ⅰ→Ⅱ 197 h / 고압B Ⅱ→Ⅲ 491 h (요금표 안내 "월 500시간 초과 유리"와 일치) |
| 4 완전성 | 계절별 경 10h + 중간 8h + 최대 6h = 24h, 중복 없음 |

일부러 오타를 낸 요금표(단가 1원 이동, 기본요금 변경, 시간대 중복·공백)가 실제로
걸리는지도 테스트했다. 검증기가 살아 있다는 증거다.

**샘플 계산 결과 (일반용(을) 고압A, 기간 2023-04-25~2024-04-27)**

| 항목 | 값 |
|---|---|
| 요금적용전력 | 5,293.44 kW (2023-07 이후 전 월 고정) |
| 기본요금 개월수 | 12.00 (13개 버킷, 부분 월 2개를 합쳐 한 달) |
| 선택Ⅰ | 기본 452,832,624 + 전력량 2,898,284,725 = 3,351,117,349원 |
| 선택Ⅱ | 3,297,542,069원 — **최적 (부록 B 와 일치)** |
| 선택Ⅲ | 3,325,199,321원 |
| 월 이용시간 | 350.8 h (부록 B "월 351 h" 와 일치) |
| 2023-11 | 결측률 32.3% → 신뢰 제한, 보정 기준이 관측 대비 1.477배 |

**결정 사항** (위 「결정 사항」 3세션 항목에도 반영)

- 부록 A.2 의 **적용 범위와 허용오차는 `validate.DEFAULT_POLICY`(코드)** 에 두었다.
  요금 단가가 아니라 검증 정책이기 때문이다. 검증기는 데이터에서 뽑은 평균과의
  편차만 보고, 5.5·3.8·200h 같은 절대값은 테스트가 못 박는다
- 정전 흔적·특례 등 요일 토큰은 알 수 없는 값이면 예외를 던진다. 조용히 넘기지 않는다
- 산업용(을) 주말 할인 특례는 **기구만 구현**하고 합성 요금표로 시험했다.
  PoC 요금 데이터에 산업용을 넣지 않는다는 부록 A.4 를 지킨 것이다
- 부분 월 기본 정책은 `merge`. 샘플에서 정확히 12.00개월분이 된다

**미해결**

- 요금 데이터 `verified: false` — 청구서 대조 전이다. 계산 결과에 경고가 붙는다
- 요금적용전력의 계약전력 하한 미적용 (기존 미해결 항목). `notes` 에 명시된다

**다음 세션이 알아야 할 것**

- **PV 시계열을 붙일 때 라벨 규약을 맞춘다.** `classify_slots()` 가 쓰는 것과 같은
  `slot_start()` 로 귀속을 판정해야 15분 어긋나지 않는다. `15:00` 슬롯은 중간부하,
  첫 최대부하 슬롯은 `15:15` 이다
- PV 도입 후 요금은 `calculate_bill()` 을 **다시 부른다.** 절감액을 빼기로 구하지 말 것.
  사용량이 줄면 최적 선택요금이 바뀐다 (요구사항서 8장)
- 조합별 요금 비교는 `list_selections()` 로 조합을 돌며 `calculate_bill()` 을 부르면 된다.
  샘플 6조합이 모두 계산되는 것을 테스트로 고정했다
- 감도 3종(0.70/1.00/1.20)은 PV 출력에만 적용한다. 요금제 전환·계약전력 조정은
  확정 계산이라 감도를 적용하지 않는다 (요구사항서 9.2)
- `BillingResult.monthly` 는 Period 인덱스 DataFrame 이다. Excel 출력 시
  `to_timestamp()` 로 바꿔야 한다 (6세션)
- 요금표를 갱신할 때는 `data\tariff_*.json` 을 새 파일로 추가한다.
  `load_tariff()` 는 파일명 순 마지막을 쓴다

**테스트**
- pytest: 200 passed (1세션 33 + 2세션 37 + 이번 130)
- ruff: pass (check + format)
- mypy: pass (strict, 29 files)

---

### 2세션 — 데이터 품질 검사 (2026-08-06)

**완료**

- **`total_kwh` 강제 장치** (1세션 보완). `UsageData.kwh` 를 `kwh_grid` 로 개명하고
  `GridKwhSeries.sum()` 이 이탈분이 있을 때 `OffGridEnergyError` 를 던지게 했다.
  슬라이스에도 `off_grid_kwh` 가 따라붙어 월별·시간대별 부분합까지 막힌다
- `kwise.io.slot_start()` 추가 — 라벨(구간 끝)에서 구간 시작을 만든다.
  quality·tariff·pv 가 모두 이 함수를 써야 15분 어긋남이 생기지 않는다
- `kwise.quality` 구현 — 결측 분석, 정전 검출, 품질 집계, 결측 처리, 부하 패턴
- 단위테스트 37건 추가 (누적 70건). 샘플의 부록 B 수치를 전부 회귀로 고정

**만든 파일**

- `src\kwise\quality\missing.py` — `find_missing_gaps`, `monthly_missing`, `peak_hour_skew`
- `src\kwise\quality\outage.py` — `detect_outages`, `outage_slot_mask`
- `src\kwise\quality\checks.py` — `check_quality`, `QualityReport`
- `src\kwise\quality\fill.py` — `fill_missing` (기본 미보간)
- `src\kwise\quality\pattern.py` — `load_pattern` (6.1)
- `tests\__init__.py`, `tests\_synthetic.py` (합성 데이터 생성기), `tests\test_quality.py`

**샘플 검증 결과 — 전 항목 기대와 일치**

| 항목 | 값 |
|---|---|
| 결측 | 972 (2.8%) — 3% 이하라 경고 없음 |
| 결측 구간 | 정확히 2개. 11-03 17:30~11-13 09:45 (930슬롯 9.69일) / 04-06 19:30~04-07 05:45 (42슬롯 0.44일) |
| 11월 결측률 | 32.3% → 유일한 '신뢰 제한' 월 |
| 정전 | 1건 (4월). 흔적 3종 모두 검출, 결정적 흔적 2종 |
| 11월 통신 장애 | 정전 아님 — 흔적이 연속 결측뿐 |
| 이상치 | 0 kW 0건, 100 kW 미만 1건 (2024-04-07 06:00, 2.88 kW) |
| 편중 배수 | 정본 0.719 (정전 42슬롯 제외) / 비교용 0.688. 둘 다 임계 1.5 미만 |
| 부하 패턴 | 부하율 49.0%, 기저 65.0%, 주말 64.4%, 무인 54.9% |

**결정 사항** (위 「결정 사항」 2세션 항목에도 반영)

- 정전 흔적 2번은 "정전 직전 부분 적산 행"과 "복전 시점 재등록 행"을 **한 종으로 묶었다.**
  둘 다 그리드 이탈 행이라는 같은 흔적이다. 따로 세면 흔적이 4종이 되어
  "3종 중 연속 결측 뺀 2개" 규칙이 헐거워진다. `min_evidence` 로 조절 가능
- 복전 후 저부하 임계는 최대수요의 5% (샘플에서 264.7 kW, 관측 2.88 kW)
- 급변 임계는 최대수요의 30%. 결측을 사이에 둔 차이는 급변으로 세지 않는다
  (관측된 이웃끼리만 `diff`)
- 편중 판정에서 샘플은 정전 제외 여부로 배수가 0.719 ↔ 0.688 로 달라지되
  판정이 뒤집히지는 않는다. **뒤집히는 경로는 합성 픽스처로 별도 검증했다**
  (`test_synthetic_skew_verdict_flips_when_outage_excluded` — 정전 포함 4.13 '위험',
  제외 0.99 '정상')

**미해결**

- 없음. 세션 범위 항목은 모두 끝냈다

**다음 세션이 알아야 할 것**

- **`usage.kwh_grid.sum()` 은 예외를 던진다.** 요금 엔진의 전력량 계산은
  `usage.energy_kwh()` 를 쓴다. 이 시리즈는 이탈 행의 kWh 를 그 행이 속한 구간
  라벨에 얹은 것이라 합계가 `total_kwh` 와 정확히 같다.
  **다만 kW 로 환산하면 안 된다** — 얹힌 구간은 위상이 달라 수요가 왜곡된다
- 계절·시간대·월·요일 귀속은 반드시 `kwise.io.slot_start()` 를 거친다.
  `15:00` 슬롯은 중간부하, 첫 최대부하 슬롯은 `15:15` 이다
- `check_quality()` 결과의 `flagged_months` 가 '신뢰 제한' 월이다.
  요금 명세에 결측률을 병기하고 최대수요에 이 표시를 붙인다 (요구사항서 4.2)
- 결측 보정 기준(5.4 절의 `1 ÷ (1 − 결측률)`)에 쓸 월별 결측률은
  `QualityReport.monthly` 에 이미 있다. 다시 계산하지 말 것
- 샘플은 13개 월 버킷이며 앞뒤가 부분 월이다 (2023-04 는 576슬롯, 2024-04 는 2,496슬롯).
  기본요금을 13개월분 부과하지 않도록 5.5 절을 따른다
- 테스트 합성 데이터 생성기는 `tests\_synthetic.py` 에 있다.
  `month_rows()` 로 한 달치를 만들고 라벨을 지우거나 고쳐 결측·정전을 만든다

**테스트**
- pytest: 70 passed (1세션 33 + 이번 37)
- ruff: pass (check + format)
- mypy: pass (strict, 21 files)

---

### 1세션 — 골격과 데이터 입출력 (2026-08-06)

**완료**

- src 레이아웃 패키지 `kwise` 구성, `pip install -e .` 설치 (Windows 11 `.venv`)
- 요구사항서 2.1 절의 9개 모듈 디렉터리 생성 (io 만 구현, 나머지는 담당 세션 표기한 docstring)
- `pyproject.toml` 에 ruff·mypy·pytest 설정. `reference\` 는 세 도구 모두에서 제외
- `kwise.io` 사용량 로더 구현 — 인코딩 4종 순차 시도, `24:00` 변환, 컬럼 유연 매칭,
  간격 자동 인식(15분·1시간), 그리드 위상 최빈값 판정, 그리드 이탈 행 보존
- 단위테스트 33건. 실측 샘플이 요구사항서 부록 B 와 일치함을 확인

**만든 파일**

- `pyproject.toml`, `.gitattributes`
- `src\kwise\__init__.py`
- `src\kwise\io\__init__.py`, `src\kwise\io\usage.py`
- `src\kwise\{quality,tariff,pv,diagnose,measures,compare,report,ui}\__init__.py` (자리표시)
- `tests\conftest.py`, `tests\test_io_usage.py`

**부록 B 대조 결과 — 전 항목 일치**

| 항목 | 기대 | 산출 |
|---|---|---|
| 원본 행 | 34,358 | 34,358 |
| 유효 행 | 34,356 | 34,356 |
| 그리드 이탈 | 2 | 2 (04-06 19:29 / 04-07 03:51) |
| 예상 행 | 35,328 | 35,328 |
| 결측 | 972 (2.8%) | 972 (2.8%) |
| `24:00` 표기 | 357 | 357 |
| 최대수요 | 5,293.4 kW @ 07-03 09:30 | 5,293.44 kW @ 07-03(월) 09:30 |
| 평균 | 2,594.6 kW | 2,594.57 kW |
| 부하율 | 49.0% | 49.0% |
| 연간 사용량 | 22,285 MWh | 22,284.8 MWh |
| 100 kW 미만 | 1건 (04-07 06:00, 2.88 kW) | 동일 |

**결정 사항** (위 「결정 사항」의 1세션 항목에 반영)

- 반환 타입은 `UsageData(kw, kwh, off_grid, meta)`. `kw`·`kwh` 는 tz-naive
  DatetimeIndex 시리즈이고 결측은 NaN 이다. 보간하지 않는다
  — *2세션에서 `kwh` 를 `kwh_grid` 로 개명하고 합계를 막았다. 위 2세션 기록 참조*
- **총 사용량은 `meta.total_kwh`(그리드 이탈 포함) 를 쓴다.** `kwh` 시리즈 합계는
  그리드 정렬분만이라 요금 계산에 그대로 쓰면 이탈분이 누락된다
- 라벨은 구간 끝 그대로 인덱스에 둔다. 계절·시간대 귀속(한 구간 빼기)은 tariff 가 맡는다
- 그리드 위상은 "하루 시작으로부터의 초 잔여" 최빈값으로 판정한다

**미해결**

- 없음. 세션 범위 항목은 모두 끝냈다. 저장소 전체의 미해결 항목은 위 「미해결 항목」 참조

**다음 세션이 알아야 할 것**

- 로더는 `from kwise.io import load_usage` 하나로 쓴다. Streamlit 업로더용으로
  `load_usage_bytes(data, filename)` 도 있다
- `UsageMeta.warnings` 에 경고 문구가 이미 담긴다. 2세션 품질 검사는 이 위에 얹는다.
  결측·이탈·중복·간격 관련 원시 수치는 모두 `UsageMeta` 에 있다
- 결측 구간 검출(연속 공백 길이, 월별 결측률, 정전 판정)은 2세션 몫이다.
  `usage.kw.isna()` 로 시작하면 된다
- 그리드 이탈 행은 `usage.off_grid` 에 `timestamp`·`kwh` 로 남아 있다.
  4.3 절에 따라 "결측"이 아니라 "부분 계량"으로 분류해야 한다
- 패키지명이 `kwise.io` 라 stdlib `io` 와 이름이 겹친다. 절대 임포트라 문제는 없으나
  모듈 안에서는 `from io import BytesIO` 형태로 쓴다
- mypy 는 `strict = true` 로 돌린다. pandas 가 스텁을 제공하지 않아
  `ignore_missing_imports`, `warn_return_any = false` 를 함께 켰다
- pytest 는 `filterwarnings = ["error::FutureWarning"]` 이다. pandas 3.0.5 를 쓰므로
  경고가 뜨면 바로 실패한다

**테스트**
- pytest: 33 passed
- ruff: pass (check + format)
- mypy: pass (strict, 13 files)


<!--
### N세션 — 제목 (YYYY-MM-DD)

**완료**
-

**만든 파일**
-

**미해결**
-

**다음 세션이 알아야 할 것**
-

**테스트**
- pytest: N passed
- ruff: pass
- mypy: pass
-->
