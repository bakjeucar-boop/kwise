# 진행 이력

kWise 프로젝트의 세션별 작업 기록. **각 세션 종료 시 클로드코드가 직접 추가한다.**

---

## 현재 상태

| 항목 | 값 |
|---|---|
| 환경 | Windows 11 (안드로이드 proot 환경에서 이관) |
| 최근 세션 | 2026-08-13 — **18세션. 버그 둘 (ESS 표·카드 불일치 / 결측 안내 중복)** |
| 다음 작업 | **19세션 — 텍스트 인벤토리** → 재분류 → 보고서 부록 신설 (18세션이 미착수) |
| 테스트 상태 | pytest **1,051 passed** / ruff pass / mypy pass (strict, 100 files) |
| 문서 | 기술서 7장 + 부록 3 · 매뉴얼 8장 + 부록. **md 원본 → html 생성** |
| 기준 데이터 | `rules_kr.json` 31항목 + `assumptions.json` 31항목. **코드 기본값 없음** |
| 케이스 스터디 | **타당성 66/66 통과** · 30.8초 (18세션 재확인 — 요금 계산 값 불변) |
| 성능 | 샘플 한 벌 **10.0초** · 최대 RSS **273 MB** · 요금 재계산 36회 (17세션 재측정) |
| 기상 사전 취득 | **완료** — 전국 135격자 × 2023~2025, 405파일 40.4 MB, 실패 0 |
| 적용 범위 | **대한민국 전용** (요구사항서 1.4) |
| 요금표 | 8종별 (일반용 갑Ⅰ·갑Ⅱ·을 / 산업용 갑Ⅰ·갑Ⅱ·을 / 교육용 갑·을), schema 0.2 |
| 산출물 | Excel 10시트 · Word 5장 (수단별 차트 포함) · **기술서/매뉴얼 html** |
| 미해결 | 8건 (청구서 4 · 참고단가 1 · 약관 확인 1 · ESS 실내형 사례 1 · 실측 1). **블로커 없음** |
| 블로커 | — |

**화면 실행** — `.venv\Scripts\streamlit.exe run src\kwise\ui\app.py`
**문서 생성** — `.venv\Scripts\python.exe tools\build_docs.py`

---

## 이름·목적 변경 (2026-08-06)

프로젝트명을 `peakcut` 에서 **`kWise`** 로 바꾸고 목적을 재정의했다.
태양광·피크컷에 치우친 이름과 범위에서 벗어나, 전력 비용 진단과 개선안 비교로 넓혔다.

| | 이전 | 현재 |
|---|---|---|
| 이름 | peakcut | kWise |
| 목적 | 태양광 도입 시 요금 절감 산출 | 전력 비용 구조 진단 + 개선 수단 비교 |
| 태양광 위치 | 주인공 | 개선 수단 중 하나 |
| 첫 산출물 | PV 피크 저감량 | 투자 없이 가능한 절감액 |
| UI | 업로드 → PV 입력 → 결과 | 1 진단 → 2 수단 선택 → 3 조합 비교 |
| 모듈 | io/quality/tariff/pv/analysis/report/ui | io/quality/tariff/pv/**diagnose**/**measures**/**compare**/report/ui |

근거는 샘플 데이터다. 설비 투자 없이 선택요금 전환만으로 상당한 절감이 산출됐다.
태양광부터 묻는 구조는 이 가치를 가린다.

---

## 이관 메모

안드로이드(Termux proot Debian, ARM64) 환경에서 Windows 11 로 옮겼다.
기존 환경이 자주 중단되어 개발을 지속하기 어려웠다.

**이전 환경에서 3세션까지 완료했다** (pytest 162 통과). 저장소를 통째로 옮겼다면
아래 「세션 기록」에 이전 기록이 남아 있고 1세션부터 다시 할 필요가 없다.
저장소 없이 새로 시작하는 경우, 아래 「결정 사항」에 1~3세션에서 확정한 내용이
모두 담겨 있으므로 같은 결론에 다시 도달할 수 있다.

**환경 차이 요약**

| 항목 | 이전 (안드로이드) | 현재 (Windows) |
|---|---|---|
| 가상환경 | 공용 `~/venv` (용량 문제) | 프로젝트 내 `.venv` |
| 패키지 관리 | `uv` | 표준 `venv` + `pip` |
| 인코딩 | UTF-8 기본 | **cp949 기본 — 명시 필수** |
| 한글 폰트 | `mpl_korean` shim | 맑은 고딕 내장 |
| 경로 | `/sdcard/projects/peakcut` | `C:\work\kwise` |
| Streamlit | 헤드리스, 수동 실행 | 브라우저 자동 실행 |
| RAM | 8 GB 제약 | 여유 있음 (순차 처리 구조는 유지) |

---

## 오늘 (2026-08-13) 한 일 — 18세션

**버그 두 건만 잡은 짧은 세션이다.** 계산은 손대지 않았다 — 케이스 스터디 66/66
과 요금적용전력 5,293.44 kW 가 그대로다. **둘 다 같은 병이다** — 계산은 각각
옳은데 사용자에게는 불일치·되풀이로 보이는 것.

| 절 | 무엇을 했나 |
|---|---|
| 1 | ESS 표와 카드가 같은 값을 낸다 |
| 2 | 결측 안내 다섯 번을 세 줄로 |

### 1절 — 같은 사양이 두 개로 보였다

    표      필요 용량 100.94 kWh · 회수기간 24.56년
    큰 글자 120 kWh · 회수기간 30.8년

14세션에 "곡선은 기본요금 절감만 본 개략치, 카드는 요금 재계산 + 왕복효율·DoD"
로 갈라 놓은 결과다. **둘 다 옳다. 그래서 더 나쁘다** — 어느 쪽이 사양인지
화면만 보고는 알 수 없다.

차이가 둘이었다.

| | 곡선 | 카드 |
|---|---|---|
| 용량 | 100.94 kWh — 계통에 **내보낼** 에너지 | 119.56 kWh — **정격** (÷√0.88÷0.9) |
| 절감액 | 저감량 × 기본요금단가 × 12 | 요금 **재계산** 차액 |
| 회수기간 | 24.56년 | 30.75년 |

**카드를 최종 사양으로 정했다.** 조달하는 것은 정격 용량이고, 절감액은 요금을
다시 계산해야 나온다.

바꾼 것은 **표시뿐이다. 곡선의 산식은 한 줄도 건드리지 않았다.**

- `nameplate_capacity_kwh()` 를 새로 두고 `size_for_target()` 이 쓰던 환산을
  옮겼다. 곡선도 같은 함수를 부른다 — 식이 한 곳에 있어야 또 갈라지지 않는다
- `EssTargetPoint.nameplate_capacity_kwh` 필드 추가. `required_capacity_kwh`
  (내보낼 에너지) 는 **그대로 남아 투자비·회수기간을 낸다.** 화면에 안 낼 뿐이다
- 표에서 **투자비·연간 절감액·회수기간 세 열을 뺐다.** 남은 것은 목표·저감량·
  필요 출력·정격 용량·방전시간. 이 셋(출력·정격 용량·방전시간)은 **카드와 값이
  정확히 같다** — 반올림 오차도 없다
- 최소점 표식에서 회수기간을 뺐다. 표식은 이미 그 축 위에 찍혀 있다
- 곡선 y축을 「개략 회수기간 (년) — 목표 선택용」 으로. 툴팁에서 투자비를 뺐다
- 두 기준의 차이는 **확인사항 한 줄**로 내렸다 (`_ess_basis_note`)

**왜 곡선을 정격 기준으로 다시 계산하지 않았나.** 환산 계수 1.1844 를 투자비에
넣으면 시장 최소 규모 100 kWh 경계가 옮겨 가 **최소 지점이 바뀐다.** 목표가
바뀌면 카드 값이 통째로 달라진다 — 회귀값 불변 조건을 깬다. 곡선은 목표를
고르는 도구이므로 개략치로 두는 편이 맞기도 하다.

### 2절 — `partition` 은 제대로 걸러 왔다

    결측 972구간 / 35,328구간 (2.8%) · 보간하지 않고 계산에서 제외
    최장 연속 9.69일 (2023-11-03 – 11-13)
    결측률이 높은 달 — 2023-11 32.3% → 해당 월 최대수요는 신뢰 제한
    [확인사항 2건]  ← 위 둘째·셋째 줄과 같은 사실

**"왜 안 걸리는가" 의 답은 「걸렸다」 였다.** `partition(…, MISSING_MARKERS)` 는
두 건을 정확히 골라냈다. 문제는 골라낸 쪽을 품질 블록이 **다시 그린 것**이다.

경위가 두 세션에 걸쳐 있다.

- **13세션** — 위쪽 경고 목록에서 결측 문구를 빼 「데이터 품질」 확인사항으로
  내렸다. 그때는 세부를 보여 줄 자리가 거기뿐이었다
- **16세션** — `missing_lines()` 로 같은 사실을 세 줄로 정리했다. 확인사항이
  할 일이 없어졌는데 **지우지 않았다**

각각 옳은 조치가 겹쳐 중복이 됐다. `_quality_block` 의 확인사항 블록을 지웠다.
**패턴을 늘려 막지 않았다** — 문구가 바뀌면 또 새는 방식이고, 20세션이 사실 ID
로 갈음할 자리다.

### 결측 문구 인벤토리 — 20세션 사실 ID 재료

**생성 지점 아홉.** 등급은 `ui.notices.classify()` 판정이다.

| # | 자리 | 등급 | 사실 | 화면에? |
|---|---|---|---|---|
| 1 | `io\usage.py:670` | 참고 | 총 결측 수·비율 + 미보간 원칙 | 아니오 |
| 2 | `quality\checks.py:239` | 참고 | 결측률 임계 초과 | 아니오 |
| 3 | `quality\checks.py:244` | 주의 | 피크 시간대 편중 (결측률 배수) | **예** — 품질 블록이 직접 |
| 4 | `quality\checks.py:251` | 주의 | 최장 연속 결측 | 세 줄 ②가 갈음 |
| 5 | `quality\checks.py:256` | 주의 | 월별 결측률 → 신뢰 제한 | 세 줄 ③이 갈음 |
| 6 | `quality\checks.py:286` | 참고 | 부분 계량 — "결측이 아니다" | 아니오 |
| 7 | `quality\outage.py:85` | 참고 | 연속 결측 — **정전 추정 문구 안의 흔적 조각** | 아니오 |
| 8 | `tariff\engine.py:507` | 주의 | 월별 결측률 + 결측 보정 기준 | 5번과 **지문 중복** |
| 9 | `tariff\engine.py:543` | 참고 | 결측 보정 기준은 참고용 | 아니오 |

**표시 지점 넷** — `views\diagnose.py` 의 `missing_lines()` 세 줄, 지표 「결측」,
꼬리말 「결측은 보간하지 않습니다」, 편중 한 줄. 산출물은 `report\document.py`
(559·566·574) · `report\excel.py` (190·209·591) · `report\notices.py:44`.

**샘플에서 실제로 뜨는 것은 넷** (usage.meta 1 · quality 3 · diagnosis 4 중
지문 중복 제거 후). 4·5번과 8번이 겹치는데 **지문이 살렸다** — `2023-11 결측률
32.3%` 까지가 같아서 하나로 접혔다.

**지문의 약점 둘.** 20세션이 풀 자리다.

- 4번 `최장 연속 결측 9.69일 (930슬롯, 2023-11-03 17:30:00 ~ …)` 은 ` — ` 이
  없어 **문장 전체가 지문**이다. 슬롯 수가 한 자리만 달라져도 중복을 놓친다
- 반대로 줄표 앞이 뭉툭하면 서로 다른 사실이 하나로 뭉개진다

### 판단이 필요했던 곳

**① 곡선 차트를 어떻게 할 것인가.** 표에서 회수기간을 뺐지만 곡선 y축에는
남아 있다. 지시는 「곡선 내부 계산에만 쓰고 화면 숫자로 내지 않는다」 였는데
축 눈금도 화면 숫자다. **곡선은 남겼다** — 목표를 고르는 장치가 그것뿐이고,
지시가 곡선 자체는 보존했다. 대신 축 제목을 「개략 회수기간 — 목표 선택용」
으로 바꿔 결론이 아님을 축에 적었다. **되돌릴 수 있는 판단이다.**

**② `_quality_block` 의 `diagnosis` 인자.** 확인사항을 지우니 쓰이지 않아 뺐다.

**③ 표에서 뺀 열을 보고서에도 뺄 것인가.** 안 뺐다 — `ess_target_table` 은
화면 전용이고 보고서는 `document.py` 가 따로 낸다. 확인만 했다.

**만든 것**

| 이름 | 하는 일 |
|---|---|
| `measures.ess.nameplate_capacity_kwh` | 내보낼 에너지 → 정격 용량. **환산식 정본** |
| `EssTargetPoint.nameplate_capacity_kwh` | 카드와 같은 기준의 용량 |
| `EssTargetCurve.round_trip` · `.dod` | 환산 계수를 곡선에 남긴다 |
| `views.measures._ess_basis_note` | 두 기준의 차이 한 줄 |
| `test_ui_screen._screen_lines` | 접힌 상자 안까지 훑는다 |

**지운 것** — 표의 투자비·연간 절감액·회수기간 열, 최소점 표식의 회수기간,
곡선 툴팁의 투자비, 「데이터 품질」 확인사항 블록.

**미해결** — 늘지 않았다. 8건 그대로다.

**테스트**
- pytest: **1,051 passed** (1,049 + 2)
- ruff: pass (check + format) · mypy: pass (strict, 100 files)
- 케이스 스터디: **타당성 66/66** · 30.8초. **요금 계산 회귀값 불변**
  (C1~C6 요금적용전력 전부 동일)

**다음 세션이 알아야 할 것**

- **ESS 화면의 결론 숫자는 카드 하나다.** 곡선·표에 돈에 관한 숫자를 되돌리지
  말 것. 표의 출력·정격 용량·방전시간은 카드와 **정확히** 같아야 한다
  (`test_표의_최적_행이_카드_요약과_같다` 가 지킨다)
- **곡선의 `required_capacity_kwh` 는 살아 있다.** 투자비·회수기간·최소 지점이
  이 값으로 나온다. 화면용은 `nameplate_capacity_kwh` 다. 둘을 바꿔 쓰면
  최소 지점이 옮겨 가 카드 값이 통째로 달라진다
- **결측 안내를 새로 붙이지 말 것.** `missing_lines()` 세 줄이 정본이다.
  새 사실이 생기면 그 함수에 넣는다 (`MISSING_LINE_LIMIT` 이 세 줄로 막는다)
- 18세션 본래 계획(텍스트 인벤토리 → 재분류 → 보고서 부록)은 **손대지 않았다.**
  아래 「다음 세션」 항목이 그대로 유효하다. 위 결측 인벤토리가 그 예행이다
- 화면 캡처 아홉 장은 여전히 밀려 있다. 이번에 ESS 표·곡선 축·진단 품질 블록이
  또 바뀌었다

---

## 지난 세션 (2026-08-12) — 17세션

**그래프 표시 개선 세션이다.** 계산은 손대지 않았다 — 케이스 스터디 66/66 과
요금적용전력 5,293.44 kW 가 그대로다. 바꾼 것은 **축·색·자리**다.

| 절 | 무엇을 했나 |
|---|---|
| 0 | 스케일 규약 셋을 한 곳에 (`ui.charts`) |
| 1 | 선택요금 — 제도 순서 · 그룹 막대 · **차액 차트** · 선택Ⅲ 조사 |
| 2 | 역률 — 범례를 우측 하단으로 · 각도·역률 직접 표기 |
| 3 | 태양광 — 재계산 차단 · 판정 근거 · 용량 표 · 두 그래프 재작성 |
| 4 | ESS — **2단 그림** · 본문 정리 · 조달 사례 표 삭제 |

### 0절 — 한 축에 억과 백만을 같이 두지 않는다

세 그래프가 같은 병을 앓고 있었다. **차이가 큰 두 값을 한 축에 그리면 변화가
보이지 않는다.** 35억 위의 5천만원, 5,000 kW 부하 위의 수백 kW 발전, 5,000 kW
축에 세운 100 kW 막대가 모두 그렇다.

규약 셋을 `ui.charts` 머리에 모았다.

    ① 금액·전력 축은 **0 부터 시작하지 않는다.** 축 제목에 그 사실을 적는다
    ② 절대값과 변화량은 **차트를 나눈다**
    ③ 두 선이 붙어 보이면 **그 사이를 색으로 채운다**

`_CUT_SCALE`(`zero=False`)과 `_LEGEND_BOTTOM`(우측 하단)을 한 벌로 두어, 다음에
차트를 더할 때 같은 판단을 다시 하지 않게 했다. **보고서 png 도 같은 규약으로
맞췄다** — 화면과 문서가 다른 모양이면 규약이 아니라 취향이 된다.

### 1절 — 선택Ⅲ 「상세 미산출」 은 데이터 문제가 아니었다

**조사 결과부터.** 셋 중 어느 것도 아니었다.

| 가설 | 실제 |
|---|---|
| 요금 데이터에 선택Ⅲ 단가가 없다 | **아니다.** 단가도 합계도 있다 (3,325,199,321원) |
| 적용 조건(월 500시간 등)에 미달한다 | **아니다.** 그런 조건을 우리가 판정하지도 않는다 |
| 계산 오류다 | **가깝다** — 값이 틀린 게 아니라 **쪼개지 않았다** |

`evaluate_tariff_switch` 가 **현행과 최적 둘만** 상세 청구서를 만들고 나머지는
합계만 들고 있었다. 계산을 아끼려는 최적화였는데, 화면이 그것을 「상세 미산출」
이라고 적었다 — **「산출하지 못했다」 로 읽히는 문구였다.**

갈아탈 수 있는 조합은 **같은 계약종별·전압 안의 선택요금뿐**이라 많아야 셋이다.
전부 상세를 내도 늘어나는 것은 요금 재계산 한 벌(35 → 36회, 전체 10.0초)이다.
합계·최적·절감액은 그대로다.

    선택Ⅰ  기본 452,832,624 + 전력량 2,898,284,725 = 3,351,117,349  (현행)
    선택Ⅱ  기본 521,823,744 + 전력량 2,775,718,325 = 3,297,542,069  (최적)
    선택Ⅲ  기본 615,275,352 + 전력량 2,709,924,000 = 3,325,199,321  ← 이제 보인다

세 줄을 나란히 놓으니 **선택요금이 기본요금과 전력량요금을 맞바꾸는 제도**라는
것이 그대로 읽힌다. 그것이 이 카드가 원래 보여야 했던 것이다.

**그래프도 둘로 갈랐다.** 차액을 먼저, 절대값을 나중에 둔다.

    ① 현행 대비 차액   0 을 기준으로 좌우. 왼쪽(초록)이 절감
    ② 그룹 막대       기본요금·전력량요금·합계를 요금제마다 나란히

정렬은 **Ⅰ·Ⅱ·Ⅲ 제도 순서**다 (`tariff.option_sort_key`). 절감액 순으로 두면
자료마다 Ⅱ·Ⅲ·Ⅰ 처럼 뒤섞여 "왜 이 순서인가" 를 먼저 묻게 된다 — 어느 쪽이
유리한지는 표식과 차액 차트가 말한다.

### 2절 — 범례가 값을 가렸다

지상역률을 올리면 삼각형이 커지면서 **우측 상단 범례가 라벨을 덮었다.** 값을
바꿔 보는 화면이라 그 가림이 매번 반복된다. 범례를 우측 하단으로 옮기고, 각도와
역률을 도형 옆에 직접 적어 범례 의존을 줄였다. 원점 쪽에도 각도만 따로 적는다 —
**각이 좁아지는 것이 이 그림의 전부**이기 때문이다.

### 3절 — 옆단을 만지면 시뮬레이션이 돌았다

**3-1 이 가장 큰 것이었다.** 방위 라벨의 상대 발전량은 여덟 방위를 각각 돌려
내는 값이라 0.85초가 든다. 그것을 **살아 있는 위젯 값**으로 매기고 있었으므로,
옆단에서 시군구를 고르는 순간 태양광이 계산되지 않은 상태에서도 0.85초가 돌았다.
태양광은 「계산」 단추를 눌러야 도는 카드인데 라벨만 그 규약 밖에 있었다.

**마지막 계산의 지역·경사각과 지금 고른 값이 같을 때만** 비율을 얹는다. 다르면
방위 이름만 보이고 한 줄 안내가 붙는다.

3-2 판정 근거를 적는다 — 「회수기간이 가장 짧은 용량입니다. 회수기간 차이가 예측
오차 안이면 절감액이 큰 쪽을 고릅니다.」 그리고 **무엇이 최적을 멈춰 세웠는지**를
셋 중 하나로 (`CapacityVerdict.limiter`) — 면적 상한 / 잉여 발생 / 기본요금 절감
포화. 16세션 0-4 로 계산은 이미 옳으므로 근거만 보이면 됐다.

3-3 20단계 곡선을 **대표 다섯 줄 표**로 바꿨다. 면적 상한과 최적은 항상 들어가고
나머지는 고르게 흩어 뽑는다. 곡선은 표 아래 접어 두고 20단계 상세는 Excel 이다.

3-4 연간 그래프의 주인공을 바꿨다. 셋을 쌓아 올리니 **발전량 쪽에 색이 들어가
그쪽이 주인공처럼 보였다.** 원래 사용량 선과 계통 수전 영역 사이를 절감분으로
채우고 절감 비율을 함께 적는다.

3-5 일일 곡선은 축을 자르고 두 선 사이를 채웠다. 피크 저감량은 화살표와 숫자로
직접 가리키고, **피크 앞뒤 세 시간만 확대한 보조 차트**를 곁들였다.

### 4절 — 온통 하얬던 이유

5,000 kW 축에 100 kW 막대를 세우면 **선 굵기만큼도 서지 않는다.** 위 칸에 부하
(축을 자르고 저감분을 채움), 아래 칸에 충전(+)·방전(−)을 제 스케일로 두는 2단
구성으로 갈랐다. 계시별 띠는 배경이므로 투명도를 0.5 → 0.18 로 낮췄다.

투자비 내역·계수·성립 조건 넉 줄을 **확인사항으로 내렸다.** 본문에 있으면 정작
읽어야 할 투자비 합계·절감액·회수기간이 그 사이에 묻힌다. 옮기면서 **경고·메모와
겹치는 줄은 걸러 낸다** — ESS 는 확인사항이 스물이 넘어 그냥 옮기면 같은 말이
두 번 적힌다. 조달 사례 표는 화면에서 뺐다 (데이터와 `tools\fit_ess_cost.py`
에는 그대로 있다).

### 판단이 필요했던 곳

**① 선택Ⅲ 상세를 채우는 것이 「계산 로직 수정」인가.** 세션 규약은 "계산 로직을
건드리지 마라" 였다. 채우기로 했다 — 합계·최적·절감액이 모두 그대로이고 회귀값이
바뀌지 않으며, 사용자가 지시한 셋 중 「계산 오류면 고친다」 에 가장 가깝다.
바꾼 것은 **무엇을 버리지 않고 남기느냐**이지 산식이 아니다.

**② 방위 라벨을 언제 계산할 것인가.** 「계산해 둔 것이 있을 때만」 으로 정했다.
대안은 옛 지역 기준으로 계속 보여 주는 것이었는데, 그러면 화면의 지역과 라벨의
지역이 달라 조용히 틀린다. 첫 사용자는 계산 전까지 비율을 못 보지만, 한 번
누르면 그 뒤로는 늘 보인다.

**③ 보고서 png 도 같이 고칠 것인가.** 지시는 화면 그래프였다. 같이 고쳤다 —
15세션에 "화면과 보고서가 같은 프레임을 본다" 를 규약으로 세웠는데 모양만
갈라지면 그 규약이 반쪽이 된다.

**④ ESS 2단 그림에서 발전량 선을 뺀 것.** 태양광 일일 곡선에서도 발전량 선을
뺐다. 축이 다른 값을 같은 축에 얹으면 스케일이 다시 뭉개진다 — 발전량은 저감분
영역의 면적으로 읽는다.

**만든 것**

| 이름 | 하는 일 |
|---|---|
| `tariff.option_sort_key` | 선택요금 제도 순서 (Ⅰ·Ⅱ·Ⅲ) |
| `frames.tariff_delta_frame` · `charts.tariff_delta_chart` | 현행 대비 차액 |
| `frames.solar_capacity_table` | 대표 용량 다섯 줄 |
| `CapacityVerdict.limiter` · `.basis_sentence()` | 판정 근거 한 줄 |
| `charts.solar_saving_ratio` | 연간 절감 비율 |
| `charts._CUT_SCALE` · `_LEGEND_BOTTOM` | 0절 규약 |

**지운 것** — 화면의 조달 사례 표, 「상세 미산출」 이라는 말, ESS 본문의 투자비
내역·성립 조건 넉 줄.

**미해결** — 늘지 않았다. 8건 그대로다.

**테스트**
- pytest: **1,049 passed** (1,034 + 15)
- ruff: pass (check + format) · mypy: pass (strict, 100 files)
- 케이스 스터디: **타당성 66/66** · 37.4초. **요금 계산 회귀값 불변**
- 성능: 샘플 한 벌 **10.0초** · 최대 RSS **273 MB** · 요금 재계산 35 → **36회**
  (선택Ⅲ 상세 한 벌이 늘었다)

---

## 지난 세션 (2026-08-12) — 16세션

**구조를 고친 세션이다.** 계산은 손대지 않았다 — 케이스 스터디 66/66 과
요금적용전력 5,293.44 kW 가 그대로다. 바꾼 것은 **화면 구조·상태 관리·버그**다.

| 절 | 무엇을 했나 |
|---|---|
| 0 | 작동을 막는 버그 **넷** (다운로드·펼침·3단계 의존·태양광 최적 용량) |
| 1 | 세 화면을 **탭**으로. 옆단 단계 목록과 하단 단추를 없앴다 |
| 2 | 옆단을 **건물 공통 정보**로. 계약 정보는 1단계로 내렸다 |
| 3 | 진단 탭 재구성 — 넣는 것 먼저, 읽는 것 나중 |
| 4 | 화면에서 **링크를 전면 제거**하고 툴팁으로 |
| 5 | 3단계를 **개선안 조합**으로 — 체크박스로 직접 짠다 |
| 6 | 카드 제목 크게 · 「콘덴서」·「자동제어형 역률 개선 설비」 표기 정리 |

### 0절 — 버그 넷은 **한 뿌리**였다

0-1(내려받기가 결과를 지운다) · 0-3(3단계가 2단계 방문에 기댄다)이 같은 원인이다.

> **Streamlit 은 이번 실행에서 그리지 않은 위젯의 상태를 버린다.**

옆단에서 한 화면만 골라 그리니 3단계에 있는 동안 2단계 위젯이 통째로 사라졌다.
내려받기 단추가 일으킨 rerun 에서 켠 수단이 하나도 없는 상태가 되어, 화면의
검토 결과도 파일 속 수단별 결과도 함께 비었다.

**배선으로 막지 않고 구조로 막았다** — 탭은 한 번의 실행에서 셋을 모두 그린다.
2단계 위젯이 늘 살아 있으므로 3단계와 산출물이 같은 객체를 본다. 13세션의
`ui/artifacts.py` 는 그대로 옳았다. 그것이 지키던 것은 **산출물 바이트**였고,
날아간 것은 **입력 상태**였다.

**시험이 이 결함을 못 잡은 이유도 같다.** AppTest 가 세션에 값을 직접 넣으면
위젯이 아니라 일반 키라 버려지지 않는다. 이번에는 **토글을 실제로 눌러** 켜고
내려받기까지 누르는 경로로 다시 썼다.

기준 데이터 화면은 여전히 분석 화면을 갈아 끼우므로, 매 실행에 그림자 사본을
뜨고 없어진 키를 되돌려 놓는다 (`state.carry_inputs`).

**0-2 펼침 상태.** ± 를 한 번 누를 때마다 카드가 접혔다. 펼침 여부를 「방금
켰는가」로 판정했기 때문이다. 세션에 담고 `expanded=` 에 그 값을 넣는다 —
켠 카드는 펼쳐진 채로 남는다. 여유율·시스템 손실 슬라이더는 `number_input` 으로
바꿨다 (슬라이더는 끄는 동안 실행이 이어진다).

**0-4 태양광 최적 용량이 8 kWp** (세션 도중 추가로 지적받았다). 상한 160 kWp 의
1/20 — 곡선의 첫 점이다.

| 용량 | 회수기간 |
|---|---|
| 8 kWp | 8.060년 |
| 80 kWp | 8.086년 |
| 160 kWp | 8.114년 |

**kWp 당 단가면 투자비도 절감액도 용량에 비례해 회수기간이 평평해진다.** 최소점을
그대로 고르니 가장 작은 용량이 답이 됐다. 0.7% 차이는 발전량 예측 오차(R² 0.8)
안에 묻혀 뜻이 없다. **최소 회수기간의 5% 안에 드는 점들 중 절감액이 가장 큰
것**을 고르도록 고쳤다 (`pv.payback_tie_ratio`). 잉여로 회수기간이 실제로 꺾이면
꺾인 뒤 점들이 이 폭을 벗어나므로 **U곡선 판정은 그대로다.**

### 1절 — 탭

```
[ 1단계 · 진단 ]  [ 2단계 · 개선 수단 ]  [ 3단계 · 개선안 조합 ]
```

진행할 수 없는 탭도 **막지 않는다.** 눌러 보고 무엇이 없는지 읽는 편이, 눌리지
않는 이유를 짐작하는 편보다 낫다. 1단계가 `AnalysisContext` 한 벌을 돌려주고
2·3단계가 그것을 그대로 받는다 (`ui/context.py`).

### 2절 — 옆단은 건물 이야기다

건물명 · 용도 · 지역(시도·시군구) · 연면적 · 준공연도. 셋은 선택 항목이고,
건물명을 비우면 산출물 표지가 **「건물명 미입력」** 이다 — 내려받기 절에 있던
건물명 입력칸은 없앴다 (같은 것을 두 곳에서 물으면 두 값이 갈린다).

**용도는 계약종별 후보를 좁힌다.** 판정이 아니라 좁히기라, 고르지 않거나 모르는
용도면 전 종별을 보인다 — 좁히기가 실패했다고 고를 것이 사라지면 입력을 못 한다.
**연면적**을 넣으면 요금 구조 절에 원단위(kWh/m²·년)가 한 줄 붙는다. **국내
평균과 견주지 않는다** — 용도·기후·가동시간이 다른 건물의 평균은 판단 기준이
되지 못한다.

지역은 태양광 카드에서 옆단으로 올렸다. 건물이 어디 있는지는 태양광만의 물음이
아니고, 카드 안에 두면 태양광을 켜야만 고칠 수 있었다.

### 3절 — 진단 탭 재구성

```
① 계약 정보  ② 업로드  ③ 역률
→ 지표 카드 → ④ 데이터 품질 → ⑤ 부하 패턴 → ⑥ 피크 특성 → ⑦ 요금 구조
```

계약 정보를 업로드 위로 올렸다 — 묻는 것이 넷뿐이고 파일과 무관하다. 관측치
추정값은 파일이 올라온 뒤 캡션으로 붙는다 (세션에서 먼저 읽고 그린다). 역률은
계약 정보 안의 접힌 패널에 있어 **있는 줄도 모르고 지나갔다.** 따로 떼고
「역률 반영」 단추를 붙였다.

**계약전력 적정성과 개선 여지 요약을 뺐다.** 같은 금액이 1단계와 2단계 카드에
다른 이름으로 있었다 — 나란히 놓고 보면 어느 쪽을 믿어야 할지 알 수 없다.
적정성은 7.2 카드로 옮겼고, **계약전력 변경 경고도 그리로 옮겼다** (문구는
산출물과 같은 원문 그대로). 바꾸자고 제안하는 자리가 그 경고의 제자리다.

**결측 안내를 세 줄로 못박았다.** 편중된 달마다 한 줄씩 붙어 열두 줄이 되는
자료가 있었다 — 달을 한 줄로 합친다. 부하율·기저부하·주말·무인시간 네 지표에
**산출식 툴팁**을 달았다.

**중복 전수 검사가 결함 셋을 잡았다.** 세 화면이 한 번에 그려지니 겹침이 그대로
드러났다 — 계약전력 경고 둘이 1단계와 2단계에, 그리고 2단계 카드 경고가 3단계
조합 경고에 다시 실렸다. 3단계는 이제 **조합 자체의 경고만** 낸다.

### 4절 — 링크를 걷어냈다

    바꾸기 전   화면 한 줄 + 「자세히」 링크가 정적 사본의 앵커로
    바꾼 뒤     화면 한 줄 + ⓘ 툴팁에 얹힌 요지

새 창으로 나가면 하던 입력을 잃고, 매뉴얼이 없으면 죽은 링크가 되며, 정적 사본을
두는 자리가 실행 방식마다 달라 404 가 났다 (12세션에 겪었다). `manual_href` ·
`detail_suffix` · `app_static_dir` · `src\kwise\ui\static\` 를 지우고
`manual_tip` 하나만 남겼다. 기준 데이터 화면의 「원문 확인」 링크도 **조문 번호와
시행일** 툴팁으로 바꿨다 (주소는 적지 않는다 — 적으면 사람이 링크로 읽는다).

**앵커 목록 32개는 남긴다.** 무엇을 화면에 두지 않기로 했는지가 거기 적혀 있고,
매뉴얼이 그 이름을 `id` 로 쓴다. `tools\build_docs.py` 는 정적 사본을 만들지
않지만 **앵커 대조는 계속한다** — 툴팁의 요지와 매뉴얼의 전문이 어긋나면 툴팁만
읽은 사람과 매뉴얼까지 읽은 사람이 다른 것을 알게 된다.

### 5절 — 3단계는 「개선안 조합」

    ① 개선안별 요약 → ② 합산효과 → ③ 내려받기

**조합을 사용자가 짠다.** 개선안마다 체크박스를 두고 기본은 2단계에서 켠 수단
전부다. 체크를 풀면 그 수단을 뺀 조합으로 합산효과를 다시 계산하고, **단순 합도
고른 것만 더한다** — 뺀 것을 단순 합에 남기면 차이가 상호작용이 아니라 「뺀
만큼」이 되어 뜻이 달라진다. 뺀 수단도 개선안별 요약에는 남는다.

미리 정해 둔 조합 세트 비교 표와 권장안 지표를 없앴다. 쓰는 사람은 「태양광은
빼고 나머지」 처럼 자기 조합을 이미 알고 있는데 목록에 없었다.

### 6절 — 표시 정리

7.1~7.7 제목을 묶음 머리보다 크게 했다 (1.15rem → 1.5rem, 굵게). 카드가 일곱이라
스크롤 중에 경계가 눈에 걸려야 한다.

**「콘덴서」·「APFR」 을 화면·산출물·기술서·매뉴얼에서 지웠다.** 뜻은 남긴다 —
`고정형 역률 개선 설비` · `자동제어형 역률 개선 설비` 로 적는다. 요구사항서는
원본이라 그대로 두었다.

**만든 파일**

| 파일 | 하는 일 |
|---|---|
| `src\kwise\ui\context.py` | 1단계가 만든 한 벌을 2·3단계가 받는다 |
| `src\kwise\ui\building.py` | 옆단 건물 정보 · 용도로 계약종별 좁히기 · 원단위 |

**지운 것** — `src\kwise\ui\static\` (매뉴얼 정적 사본), `nav` 의 단계 진행
표시와 하단 이동 단추, `anchors` 의 링크 생성 기계장치, 3단계 조합 비교 표.

**미해결** — 늘지 않았다. 8건 그대로다.

**테스트**
- pytest: **1,034 passed** (1,028 → 통합·화면 시험 재작성과 신규 포함)
- ruff: pass (check + format) · mypy: pass (strict, 100 files)
- 케이스 스터디: **타당성 66/66** · 40.8초. **요금 계산 회귀값 불변**
- 성능: 샘플 한 벌 **10.4초** · 최대 RSS **271 MB** (탭 구조로 세 화면을 함께
  그려도 캐시에 걸려 9.8초에서 거의 늘지 않았다)

---

## 지난 세션 (2026-08-12) — 15세션

**그래프와 UI 마무리 세션이다.** 계산은 손대지 않았다 — 케이스 스터디 66/66 과
요금적용전력 5,293.44 kW 가 그대로다. 바꾼 것은 **입력·그림·이동·안내**다.

| 절 | 무엇을 했나 |
|---|---|
| 1 | 태양광 — 8방위 선택 · 툴팁 · 용량 **한 줄 판정** |
| 2 | 2단계 그래프 전면 교체. 대표일을 하나로 묶었다 |
| 3 | 옆단을 **단계 진행 표시**로 |
| 4 | 안내에서 **배경색을 뺐다** |
| 5 | 통합 시험 45건 — **결함 셋을 잡았다** |

### 1절 — 태양광 입력

**방위를 각도에서 8방위로.** 45도 간격이면 최대 오차 22.5도이고 발전량 차이가
1~2% 라 예측 오차(R² 0.8) 안에 묻힌다.

**상대 발전량을 라벨에 병기하되 하드코딩하지 않는다.** 고른 지역·경사각으로
여덟 방위를 각각 돌려 남향 대비 비율을 낸다 — 경사가 낮으면 방위 영향이 줄기
때문이다.

| 경사 | 남 | 남동 | 남서 | 동 | 서 | 북 |
|---|---|---|---|---|---|---|
| 25° (밀도 보통) | 기준 | −8% | −1% | −20% | −10% | −31% |
| 15° (밀도 높음) | 기준 | −5% | −1% | −13% | −7% | −19% |
| 90° (벽면) | 기준 | −18% | −1% | −40% | −21% | −64% |

밀도 「높음」이 경사 15° 라 차이가 절반으로 준다 — **하드코딩했으면 거짓말이 될
자리다.** 시뮬레이션 여덟 번이라 0.85초이고, 용량을 1 kWp 로 고정하므로 면적을
만져도 다시 돌지 않는다.

북향 계열은 **고를 수 있게 두고** 주의만 적는다. 벽면 어레이를 더해 지붕과 방위를
따로 고를 수 있게 했다 (경사 90°·GCR 1.0).

**설치 밀도 설명을 툴팁으로 옮겼다.** 환산 용량은 라벨에 남긴다 (`높음 — 110 kWp`).
선택지와 설명이 화면상 떨어져 있으면 고르는 순간에 읽히지 않는다. 같은 방식으로
확실성 등급·감도 범위·방전시간·계약전력 여유율에도 물음표를 붙였다 (`ui.text.TIPS`).

**용량 20단계 표를 없애고 한 줄 판정으로.** 면적이 정해지면 용량이 정해지므로,
잉여가 없고 기본요금 절감이 포화하지 않는 한 곡선은 단조롭게 좋아지기만 한다.
그런 경우 표는 아무것도 알려주지 않는다.

> 설치 가능 면적 전체(80 kWp)를 쓰는 것이 절감액 기준 가장 유리합니다.

**최적이 상한보다 작을 때만** 곡선을 펼치고 최소점에 표식을 찍으며, 이유(잉여
발생 / 기본요금 절감 포화)를 판별해 적는다. **새 산식을 만들지 않았다** —
`SolarCurve.verdict()` 는 이미 훑은 20단계에서 고르기만 한다. 20단계 상세는
Excel 「태양광 용량 곡선」 시트로 보냈다.

### 2절 — 2단계 그래프

**대표일을 하나로 묶은 것이 이 절의 핵심이다.** 역률·태양광·ESS 가 모두 하루
15분 곡선을 쓰는데 카드마다 다른 날을 그리면 세 그림을 나란히 놓고도 견줄 수 없다.
위젯 하나(`report.days`)로 묶고 어느 날인지 차트 제목에 적는다.

    연간 최대수요일  기본. 피크가 어떻게 생겼는지가 세 수단의 공통 관심사다
    여름·겨울 대표일 그 계절 평일 중 일평균이 **중앙값에 가장 가까운 실재하는 날**
    사용자 지정일   직접 고른 날

평균 하루를 그리면 피크가 뭉개져 "이 시각에 방전한다" 가 보이지 않는다.

| 수단 | 그림 |
|---|---|
| 7.1 | 요금제별 기본·전력량 **누적 막대**. 맞바꾸는 제도라 합계만으로는 안 보인다 |
| 7.2 | **만들지 않았다.** 1단계 피크 특성으로 충분하다 |
| 7.3 | 연간 일별 운영시간대 평균 **산점도**. 기준선 근처가 감축 가능일 |
| 7.4 | **전력삼각형**(각이 좁아지는 모습) + 대표일 곡선(판정 창 표시) |
| 7.5 | 연간 일별 수전·자가소비·잉여 + 대표일 원부하·순부하·발전 |
| 7.6 | 대표일 **충·방전 구조** + 계시별 배경 띠. U곡선에 기준을 명시 |
| 7.7 | 연간 일별 잉여량 (주말 구분). 잉여 0 이면 사유만 |

프레임은 `report.frames` 한 곳에 두어 화면(altair)과 보고서(matplotlib)가 **같은
수**를 그린다. 새 차트를 Word 3장에도 넣었다 — 그림 굽기가 실패해도 보고서는 표만
내고 살아남는다 (`_safe_figure`).

`option_label` 을 `report.columns` 로 내렸다가 5절에서 다시 `tariff.labels` 로
내렸다 — report 가 ui 를 import 하면 순환이고, 계산 모듈도 같은 표기가 필요했다.

### 3절 — 옆단을 단계 진행 표시로

라디오는 「고르는 것」으로 보여 진단 → 수단 → 비교가 한 줄기라는 사실이 눈에
들어오지 않았다.

```
── 분석 ──────────────
  ① 진단          ✓ 완료
  ② 개선 수단     ● 현재
  ③ 조합 비교     ○ 대기
── 설정 ──────────────
  기준 데이터
```

완료 판정은 ① 업로드+계약 정보, ② 수단 하나 이상, ③ 방문이다. 진행할 수 없는
단계는 흐리게 두고 **클릭을 막는다** — 눌러도 빈 화면이 나온다. 기준 데이터는
단계가 아니라 설정이라 구분선 아래에 번호 없이 둔다. 본문 상단에도 현재 위치를
한 줄로 적는다.

**라디오를 버튼으로 바꾸면서 12세션의 제약이 사라졌다.** 위젯 키가 아니라 일반
세션 키에 쓰므로 콜백 밖에서도 안전하다. CSS 는 `nav` 한 곳에 모았다.

### 4절 — 안내에서 배경색을 뺐다

확인사항을 펼치면 노란 바탕에 진한 노란 글자가 열 줄 쌓여 **무엇이 중요한지 오히려
알 수 없었다.** `kwise.ui.callout` 하나로 모았다.

    차단   st.error — **색을 남기는 유일한 등급**
    주의   ⚠ + 굵은 글씨. 배경 없음
    참고   · + 작은 글씨. 배경 없음

등급 판정은 그대로 `ui.notices` 가 하고 `callout` 은 그리기만 한다.

### 5절 — 통합 시험이 결함 셋을 잡았다

`tests/test_integration.py` 45건. AppTest 로 실제 화면을 띄워 흐름·산출물·경계·
회귀 네 갈래를 훑는다. **순수 함수 시험이 다 통과해도 배선이 틀리면 화면에서만
드러난다** — 8·12·13세션에서 잡은 결함이 모두 그런 종류였고, 이번에도 셋이 나왔다.

**① 코드 식별자가 화면에 나갔다.** 선택요금 전환 노트가 「최적은
`general_b/high_a/II` 입니다」 를 냈다. 12세션이 지운 규약인데 **계산 모듈이 내는
문구**라 소스 훑기 시험이 닿지 않았다. 표시 이름(`selection_label`)을
`tariff.labels` 로 내려 계산 모듈도 같은 표기를 쓰게 했다 — 화면 쪽에만 두면
계산 모듈이 쓸 수 없다는 것이 원인이었다.

**② 이스케이프되지 않은 물결표.** 1단계 부하 패턴의 `야간 22~8시 · 운영 9~18시`
가 그 사이를 취소선으로 그렸다 (13세션 규약). en dash 로 바꿨다.

**③ 3단계가 2단계 방문에 의존했다.** 옆단에서 3단계로 바로 뛰면 ESS 목표가 세션에
없어 **개선안별 요약에서 ESS 행이 통째로 빠졌다.** 없으면 곡선 최소점을 같은
함수로 다시 찾도록 고쳤다.

시험 자체에서도 둘을 배웠다. `st.cache_data` 는 프로세스 전역이라 앞선 시험이
남긴 값이 「기상 실패」 경로를 조용히 성공시킨다 — 실패를 **모의로** 만든다.
그리고 태양광 실주행에는 사전 취득분이 필요해 `real_weather` 로 격리를 푼다.

### 성능 재측정

| | 10세션 | 15세션 |
|---|---|---|
| 샘플 한 벌 | 9.4초 | **9.8초** |
| 최대 RSS | 273 MB | **272 MB** |
| 요금 재계산 | 35회 | **35회** |

새 경로는 **ESS 목표 곡선 159점 0.13초**(요금 재계산 0회)와 **8방위 비교 0.85초**
다. 둘 다 캐시에 걸린다.

**만든 파일**

| 파일 | 하는 일 |
|---|---|
| `src\kwise\report\days.py` | 대표일 고르기. 세 곡선이 같은 날을 본다 |
| `src\kwise\tariff\labels.py` | 종별·전압·선택요금 표시 이름. **계산 모듈도 쓴다** |
| `src\kwise\ui\callout.py` | 안내 세 등급 그리기. 차단만 색 |
| `tests\test_integration.py` | 통합 시험 45건 (흐름·산출물·경계·회귀) |

**미해결** — 늘지 않았다. 8건 그대로다.

**테스트**
- pytest: **1,028 passed** (982 + 46)
- ruff: pass (check + format) · mypy: pass (strict, 137 files)
- 케이스 스터디: **타당성 66/66** · 35.8초. **요금 계산 회귀값 불변**

---

## 지난 세션 (2026-08-12) — 14세션

**계산·데이터 정정 세션이다.** 요금 엔진은 손대지 않았고 케이스 스터디 66/66 과
요금적용전력·부하율·기본요금 비중이 그대로다. 바뀐 것은 **금액 표기 규칙**과
**DR·ESS 산출 방식**, 그리고 **2·3단계의 구조**다.

| 절 | 무엇을 했나 |
|---|---|
| 1 | 원화 표기를 **천 단위 절사**로 통일. `kwise.money` 한 곳이 규칙을 쥔다 |
| 2 | 2단계를 **독립 평가**로 못박고 카드마다 개요를 넣었다 |
| 4 | 경제성DR **연 60시간 한도를 지우고** 저부하 평일로 산출한다 |
| 3 | ESS **목표를 자동으로 찾고** 단가를 2계수로 받는다 |
| 5 | 3단계를 **개선안별 요약 · 합산효과 · 조합 비교** 셋으로 나눴다 |

### 폐기한 지시 — 코드에서 지웠다

시험(`test_폐기된_배치_지시가_코드에_없다`)이 되돌아가는 것을 막는다.

| 폐기된 것 | 지금 |
|---|---|
| 「2단계 수단을 투자비 순으로 배치」 | **7.1~7.7 원래 순서.** 투자 구분은 읽기 딱지일 뿐 |
| 「독립 수단 / 종속 항목으로 재편」 | **전부 독립 평가라 종속 구분 자체가 없다** |
| 「종속 항목은 선행 수단이 꺼져 있으면 비활성」 | **어떤 카드도 다른 카드 때문에 비활성이 되지 않는다** |
| 「계약전력 조정을 2단계에서 뺀다」 | **7.2 카드 그대로.** 조합 기준 추가 여지만 3단계에서 |
| 「ESS 단가는 kW당 하나」 | **고정비·용량단가 2계수** |
| 「경제성DR 연 60시간 한도」 | **연간 한도 없음** |

### 1절 — 금액 표기를 한 곳으로

`kwise.money` 를 만들고 화면(`ui.text`)·Excel(`report.notices`)·Word
(`report.document`)·진단 요약이 모두 그것을 거치게 했다.

    표시   천 원 단위로 **절사**한다.  1,234,567원 → 1,234,000원
    계산   **원 단위를 그대로 유지한다.** 절사해서 계산하면 합계가 어긋난다

항목을 각각 절사하므로 항목 합과 합계 표시가 1천 원 내외 다를 수 있다.
`TRUNCATION_FOOTNOTE` 를 표 아래에 단다.

**단가는 절사하지 않는다.** 원/kWh 단가를 천 원 단위로 자르면 0 이 된다.
금액(총액·절감액·투자비)만 자르고, 그 경계를 시험이 지킨다 — 소스에
`f"{value:,.0f}원"` 이 남아 있으면 실패한다.

Excel 은 **(원) 으로 끝나는 수치 열**을 내보낼 때 자른다. 계산 프레임은 건드리지
않는다 — 회귀값이 거기 걸려 있다.

### 2절 — 2단계는 전부 독립 평가다

각 카드의 절감액은 **「지금 이 수단만 도입하면 얼마」** 이고, 기준선은 언제나
현행 요금제·현행 사용량이다. 어떤 수단을 켜고 끄든 다른 카드의 숫자가 바뀌지
않는다 — **실주행 시험이 수단을 하나씩 켜고 끄며 지표가 불변인지 확인한다.**

- 카드마다 **개요 두세 줄**을 넣었다 (입력 아래·결과 위)
- **잉여 활용 카드를 잠그지 않는다.** 태양광이 없으면 잉여 0 만 적는다 —
  잠그면 "쓸 수 없는 수단" 으로 읽힌다
- 「외부 구매 단가」 → **「잉여 판매 단가」**. 우리가 파는 쪽이다
- 계약전력 카드에 「3단계 합산효과에서 추가 하향 여지가 계산됩니다」 를 붙였다
- **여유율 슬라이더를 감춘 경우에도 값을 세션에 남긴다.** 3단계가 다른 기본값
  (하드코딩 0.1)을 읽어 2단계와 어긋나던 자리다

### 4절 — 경제성DR: 한도가 아니라 **날**이 제약이다

전력거래소 확인 결과 경제성DR 은 **연간 참여 일수 제한이 없다.** 13세션에 넣었던
연 60시간 한도를 코드·데이터·문서에서 모두 지웠다. 남는 제약은 셋이다.

    하루 한도    최대 2회 × 1회 최대 4시간 = 8시간
    참여 요일·시간  평일 09~20시, 점심 제외
    미이행 제재  **6개월 입찰 제한**

따라서 실질 제약은 **감축할 여력이 있는 날이 며칠이냐** 하나다. 데이터에서 찾는다.

    ① 기준선     주말·공휴일의 운영 시간대 평균 부하 (2,095 kW)
    ② 저부하 평일 대상일 중 그 시간대 평균이 기준선 × 1.2 이하인 날
    ③ 감축 여력   평일 정상 평균(3,735 kW) − 그날 실제 부하
    ④ 참여 시간   하루 8시간 상한, 저부하가 지속되는 시간까지
    ⑤ 감축 가능량 Σ(여력 × 시간) → 365일 환산

**샘플에서 저부하 평일은 이틀이다** — 2023-05-01(근로자의 날)과 2023-10-02(추석
연휴 사이 월요일). 둘 다 사무실을 비운 날이고, 그것이 이 방식의 요점이다.
하위 5% 분위수는 **「항상 그만큼」** 을 뽑아 이 사실을 가렸다. 없으면 없다고 낸다.

등록 권장 용량은 저부하일 여력 분포의 **하위값**이다 — 미이행이 6개월 입찰
제한이라 과대 산정의 대가가 크다. 저부하 평일 **목록을 화면에 낸다.** 맞는 날인지는
사용자가 판정한다.

### 3절 — ESS: 목표를 사용자가 찍게 두지 않는다

피크의 90%에서는 용량이 4,094 kWh 로 사례 범위를 넘고, 조금만 올리면 방전시간이
0.44시간까지 줄어 고출력 셀이 된다. **목표를 훑어 최적점을 찾는다.**

    필요 출력 = 초과분 최대값 (kW)
    필요 용량 = 하루치 초과 에너지 합의 연중 최대값 (kWh)
    과금 용량 = max(필요 용량, 시장 최소 100 kWh)
    투자비   = 고정비 + 용량단가 × 과금 용량 + 전기공사
    절감액   = 저감량(kW) × 기본요금단가 × 12   ← **현행 요금제** 기준
    회수기간 = 투자비 ÷ 절감액

| 목표 | 저감 | 필요 용량 | 과금 용량 | 투자비 | 회수기간 |
|---|---|---|---|---|---|
| 5,200 kW | 93 kW | 35 kWh | 100 kWh | 2.62억 | 32.3년 |
| 5,180 kW | 113 kW | 72 kWh | 100 kWh | 2.62억 | 26.6년 |
| **5,170 kW** | **123 kW** | **101 kWh** | **101 kWh** | **2.63억** | **24.6년** |
| 5,150 kW | 143 kW | 183 kWh | 183 kWh | 3.31억 | 26.7년 |
| 5,100 kW | 193 kW | 432 kWh | 432 kWh | 6.11억 | 36.4년 |

**격자를 10 kW 로 둔다.** 50 kW 간격이면 5,150 kW 26.7년에서 멈춘다 — 시험이 두
격자를 실제로 돌려 이 차이를 못 박는다.

**곡선이 U자인 것은 물리적 최적이 아니라 조달 규격의 산물이다.** 왼쪽은 시장 최소
규모 100 kWh 를 다 못 쓰는 구간이고 오른쪽은 용량이 급증하는 구간이다. 그 사실을
화면에 적는다. **목표 슬라이더는 없앴다.**

- 단가는 **고정비·용량단가 2계수**다. kW 는 설명 변수가 아니다 — 같은 100 kW 인데
  용량이 156.4 kWh 면 2.35억, 400 kWh 면 4.43억이다
- 계수 기본값을 **천 단위로 반올림**했다 (106,925,000 / 840,000). 재현 오차
  −0.85~+1.43% 로 사실상 변화가 없다. `tools\fit_ess_cost.py` 가 반올림해 저장한다
- **판정 문장을 쓰지 않는다.** 「성립하지 않습니다」·「경제성 없음」 대신 두 값을
  나란히 놓는다. 곡선이 근거이므로 판단은 사용자가 한다
- 종합쇼핑몰 사례 문구 정정 — **장치비용만이고 설치공사는 별도**다. 실내형 계수를
  따로 만들지 않았다 (사례가 하나뿐)
- 출력/용량이 잘리던 자리에 **방전시간까지** 넣었다

곡선·표는 **기본요금 절감만 본 개략치**다 (목표를 고르는 데 쓴다). 그 아래 결과는
요금을 다시 계산하고 왕복효율·DoD 를 반영한 값이라 숫자가 다르며, 그 사실을 적는다.

### 5절 — 3단계를 셋으로 나눴다

| 부분 | 무엇을 | 어디서 |
|---|---|---|
| ① 개선안별 요약 | 독립 평가 결과 **그대로**. 재계산 금지 | `report.standalone` |
| ② 합산효과 | 조합 재계산 + **단순 합과의 차이** | `compare.combination` |
| ③ 조합 비교 | 조합을 쌓아 올려 견준다 | `compare.combination` |

샘플에서 **단순 합 6,715만원 / 합산효과 6,912만원 / 차이 +196만원 (+2.9%)**.
차이가 생긴 이유는 **실제로 발생한 것만** 골라 적는다.

**역률을 조합에 넣었다.** 부하를 바꾸지 않고 기본요금 조정액만 바꾸므로
`BillingOptions.power_factor_pct` 로 얹는다 — 넣지 않으면 「기본요금이 줄면 역률
감액도 준다」 는 상충이 아예 보이지 않는다.

**경제성DR·잉여 활용은 합산효과에 넣지 않는다.** 요금이 아니라 별도 정산·수익이라
조합 부하에 얹을 수 없다. ①에는 그대로 있고 ②는 그 사실을 적는다 — 같은 것을
비교하지 않으면 「차이」가 뜻을 잃는다.

**계약전력 추가 하향 여지**는 합산효과에서 한 번, 조합 비교 표에서 조합마다 낸다.
2단계 7.2 카드는 현재 부하 기준이라 다른 수단을 켜도 값이 바뀌지 않는다.

**만든 파일**

| 파일 | 하는 일 |
|---|---|
| `src\kwise\money.py` | 원화 표기 한 곳. 천 단위 절사·억만원·각주 |
| `src\kwise\report\standalone.py` | 개선안별 요약 (독립 평가 결과를 옮기기만) |
| `tests\test_money.py` | 절사 규칙·내부 계산 불변·한 곳 규약 14건 |

**미해결** — 「경제성DR 규칙 확인」이 풀렸다 (전력거래소 확인). 대신
**「ESS 실내 캐비닛형 사례 추가 확보」**가 들어왔다 — 사례가 하나뿐이라 실내형
계수를 만들지 못한다. 총 8건으로 같다.

**테스트**
- pytest: **982 passed** (929 + 53)
- ruff: pass (check + format) · mypy: pass (strict, 133 files)
- 케이스 스터디: **타당성 66/66** · 29.6초. **요금 계산 회귀값 불변**

---

## 지난 세션 (2026-08-11) — 13세션

**오류 두 건을 잡고, 과대 산출 두 곳을 바로잡고, 표시를 정리했다.**
계산이 바뀐 곳은 경제성DR 과 ESS 둘이다. 요금 계산은 손대지 않았고 케이스
스터디 66/66 과 요금적용전력·부하율·기본요금 비중이 그대로다.

| 갈래 | 무엇을 했나 |
|---|---|
| 오류 ① | 3단계 KeyError — 표시 문자열이 수단 키 자리로 흘러들어 화면이 통째로 죽었다 |
| 오류 ② | 물결표 둘이 한 줄에 있으면 그 사이가 취소선이 됐다 |
| 경제성DR | 연 60시간 한도·운영 시간대를 넣었다. 샘플에서 **22배 과대**였다 |
| ESS | 투자비를 조달 사례 회귀로 교체하고 **성립 조건**을 산출한다 |
| 1단계 | 기간 표시·결측 안내 한 묶음·피크 중복 접기·열 이름 한글화 |
| 2단계 | 카드 머리 크기·켜면 펼침·여유율 슬라이더·태양광 계산 버튼·잉여 용어 |
| 3단계 | 표가 먼저, 권장안이 나중. 수단별 그래프는 보고서로 |

### 오류 ① — 키와 라벨을 갈랐다

`CombinationSpec.measures` 가 `계약전력 5,500 kW` 같은 **표시 문자열**을 돌려주고
있었다. 12세션에서 그것을 등록 키로 알고 라벨 조회에 넣었더니
`KeyError: 등록되지 않은 개선 수단입니다` 로 3단계 전체가 죽었다 — 엑셀·워드
단추까지 함께 사라졌다.

    잘못된 구조   measures → ("계약전력 5,500 kW",)        키와 표시가 한 값
    바른 구조     applied  → AppliedMeasure("contract", (("contract_kw", 5500.0),))

`AppliedMeasure` 를 `kwise.measures.catalog` 에 두어 **키·파라미터·라벨**을
갈랐다. 라벨은 조회로 만들고, 등록되지 않은 키가 오면 예외 대신 경고 한 줄과
원문 표시로 물러선다. 표 한 칸 때문에 화면을 잃는 편보다 낫다.

### 오류 ② — 물결표

`야간 22~8시 · 운영 9~18시` 에서 물결표 두 개 사이가 취소선으로 그려졌다.
화면 문구는 en dash(`–`)로 바꾸고, **계산 모듈이 내는 문구는 렌더 직전에
escape** 한다 (`ui.text.markdown_safe`). 계산 모듈의 문구는 Excel·Word 로도
가는데 거기서는 마크다운을 해석하지 않으므로 원본을 고치지 않는다.

### 경제성DR — 4배가 아니라 22배였다

거래 가능일 245일에 하루 1시간씩 참여한다고 계산하고 있었다. 제도에는 한도가
있다.

| 한도 | 값 |
|---|---|
| 연간 총 한도 | 감축 시험 포함 연 60시간 |
| 하루 한도 | 최대 2회 × 1회 1~4시간 |
| 운영 시간대 | 평일 09:00~12:00 · 13:00~20:00 (점심 제외) |

    연간 감축 가능량 = 등록용량 × min(참여시간, 60)

샘플에서 320,530 kWh → **14,184 kWh** 가 됐다. 감축 여력도 하루 전체 평균이
아니라 운영 시간대 부하로 잰다 — 저녁 두 시간이 들어오면서 등록 권장 용량이
632 kW → 236 kW 로 내려갔고 적합성 등급도 '높음' → '보통' 이 됐다.

입력을 「예상 참여일」에서 **「예상 연간 참여시간」**(상한 60, 기본값 없음)으로
바꾸고, 미입력이면 라벨에 「연간 최대 가능량 (연 60시간 한도 적용)」 이라 적는다.

> **연 60시간은 원문 확인이 필요하다.** 전력시장운영규칙에서 확인되는 60시간이
> 신뢰성DR 의무감축 기준일 수 있다. 확인 전까지 보수적으로 적용하고
> `rules_kr.json` 에 **확인일 없는 상태**로 넣었다 — 기준 데이터 화면이
> 「확인 필요」로 표시하고 미해결 항목에 올렸다.

### ESS — 참고단가를 조달 사례로 갈아치웠다

화면에 나오던 2,660,528 원/kW 는 LCOS 연구 기반 계통용 단가였다. 국내 조달
사례로 회귀를 맞췄다 (`tools\fit_ess_cost.py` → `data\ess_cost_model.json`).

    설비비 = 106,924,633원 + 839,891원 × 용량(kWh)      R² 0.99958 (옥외 관급 4건)
    전기공사 = 200 kWh 이하 6,300만~8,300만원 / 400 kWh 약 1.41억원

홀드아웃(한 건씩 빼고 예측) 오차 1.0~2.5%. **계수를 코드에 적지 않는다** —
사례가 늘면 스크립트를 다시 돌리는 것이 갱신 절차다.

- **단일 kWh당 단가로 말하지 않는다.** 고정비가 1.07억이라 총액÷용량이 50 kWh
  440만원, 400 kWh 146만원으로 세 배 변한다
- **kW 는 설명 변수가 아니다.** PCS 가 50·75·100 kW 로 흩어져 있는데도 kWh
  하나로 R² 가 0.9996 이다
- 적용 구간 100~400 kWh. 아래는 100 kWh 로 올려 산정하고 알리며, 위는 참고값

**핵심 산출물은 성립 조건이다.** 값과 무관하게 늘 같은 문장이 나오던 참고단가
판정("경제성 없음 — … 강한 판정입니다")은 지웠다.

    kW당 배터리비 = 용량단가 × 방전시간
    10년 절감/kW  = 기본요금단가 × 12 × 10      (계약종별에서 가져온다)
    마진/kW       = 절감 − 배터리비
    필요 저감량   = (설비 고정비 + 전기공사) ÷ 마진

샘플(목표 5,200 kW, 방전 0.44시간)에서 **필요 저감량 358 kW, 실제 93 kW** 로
성립하지 않는다. 마진이 0 이하면(방전시간 1.5시간 이상) 규모와 무관하게
성립하지 않는다고 적는다. 단가·총액을 넣지 않으면 이 모델이 투자비를 낸다.

### 1단계

- **기간이 잘렸다.** 지표 값 글꼴이 커서 `2023-04-25 – 2024-` 까지만 보였다.
  일수를 값에, 날짜 범위를 **delta 자리**(작은 글씨)에 뒀다
- **결측 안내 세 번을 한 묶음 세 줄로.** 총 결측 / 최장 연속 / 월별 편중.
  세부는 확인사항으로 내리고 위쪽 경고 목록에서 뺐다
- **같은 값이면 피크 지표를 한 줄로 접는다.** 샘플은 연간 최대가 중간부하
  시간대라 관측 최대와 요금적용전력이 같다. 다를 때만 나란히 놓고 이유를 적는다
- **열 이름을 한글로.** 번역표를 `kwise.report.columns` 한 곳에 두고 화면·Excel이
  같이 쓴다. 계산 프레임 자체는 건드리지 않는다 — 낼 때만 바꿔 낸다

### 2단계

- 절 번호·이름을 묶음 머리와 같은 크기로 (굵게 하지 않는다)
- **켜면 펼쳐진다.** 켠 뒤 다시 펼치게 하면 두 번 눌러야 결과가 보인다
- **여유가 없으면 여유율 슬라이더를 감춘다.** 샘플은 5,500 kW 대비 여유 3.8% 라
  움직여도 0% 였다. 여지가 있을 때만 보이고 권장 범위(10~20%)와 근거를 적는다
- **태양광에 계산 버튼.** 값 하나만 바꿔도 다시 도는 구간이라(파이프라인의 절반)
  입력하는 동안 화면이 계속 멈췄다. 입력이 바뀌면 묵은 결과라고 적는다
- **「외부 구매 단가」 → 「잉여 판매 단가」.** 우리가 파는 쪽이다. 잉여가 0 이면
  이유를 적고 입력을 잠근다

### 3단계

표가 먼저, 권장안이 나중이다 — 어느 조합을 왜 권하는지 말하려면 견줄 것이 먼저
보여야 한다. 표는 절감액 내림차순이고, 권장안은 지표 넷 + 이유 한 문단이다.
수단별 그래프는 화면에서 빼고 보고서 4장에만 뒀다.

**만든 파일**

| 파일 | 하는 일 |
|---|---|
| `data\ess_cost_cases.json` | 조달 사례 7건 (회귀 4 · 참고 3) |
| `data\ess_cost_model.json` | 재적합 산출물 — 계수·구간·홀드아웃 |
| `tools\fit_ess_cost.py` | 재적합 스크립트 (`--check` 로 대조만) |
| `src\kwise\report\columns.py` | 표 열 이름 번역표 (화면·Excel 공용) |

**미해결** — 경제성DR 연 60시간·운영 시간대·하루 한도의 **조문 원문 확인** 1건이
늘었다. 확인 전까지 보수적으로 적용하고 화면이 「확인 필요」로 표시한다.

**테스트**
- pytest: **929 passed** (906 + 23)
- ruff: pass (check + format) · mypy: pass (strict, 130 files)
- 케이스 스터디: **타당성 66/66** · 요금 계산 회귀값 불변

---

## 지난 세션 (2026-08-11) — 12세션

**화면만 고쳤다. 계산은 한 줄도 건드리지 않았다.** 케이스 스터디 66/66 이 그대로고
숫자도 그대로다. 바꾼 것은 표시·배치·문구다.

| 갈래 | 무엇을 했나 |
|---|---|
| 버그 둘 | 내려받기 rerun 으로 결과가 날아가던 것 · [자세히] 가 File not found 로 떨어지던 것 |
| 세 등급 | 차단·주의만 화면에. **참고는 Excel 요약과 보고서 5장으로** |
| 사람 말 | 코드 식별자·개발자 지시문을 화면에서 뺐다 |
| 지표 먼저 | 각 단계 위에 카드 3~4개 → 차트 → 표 (순서를 뒤집었다) |
| 배치 | 입력 2~3열 · 단계 하단 이동 단추 · **수단 카드 전부 접힘** |
| 표기 | 세 자리 콤마를 `ui.text` 한 곳에서 |
| 감도 | 화면은 기준값+범위 한 줄. 차트와 3행 원자료는 Excel 로 |
| 경제성DR | 「연간 최대 가능량」으로 고치고 **예상 참여일**을 받는다 |

### 버그 ① — 내려받기 단추가 결과를 지웠다

`st.download_button` 은 **눌리는 순간 rerun** 을 일으킨다. 그 rerun 에서는 바로 앞의
「만들기」 단추가 눌리지 않은 상태다. 만들기 분기 안에서 내려받기 단추를 그렸으니
분기가 통째로 건너뛰어졌고, 단추도 화면 결과도 함께 사라졌다.

    잘못된 순서   [만들기] → 바이트 생성 → 내려받기 단추 → 클릭 → rerun → 사라짐
    바른 순서     [만들기] → 바이트를 **세션에 담는다** → rerun 뒤에도 단추가 남는다

`kwise\ui\artifacts.py` 를 만들어 바이트를 세션에 담고, 내려받기 단추는 **참조만**
한다. 입력이 바뀌면(토큰이 달라지면) 담아 둔 것을 버린다 — 옛 바이트를 내려받게 두면
화면 숫자와 파일 내용이 어긋난다. `AppTest` 로 **실제 3단계 화면에서** 엑셀을 만들고
내려받기를 눌러 지표가 남는지 확인한다.

### 버그 ② — [자세히] 가 File not found 였다

11세션에서 정적 사본을 저장소 뿌리 `static\` 에 만들었다. Streamlit 이 내주는 폴더는
**주 스크립트 옆의 `static\`** 이다 (`file_util.get_app_static_dir` 는
`Path(main_script).parent / "static"` 을 돌려준다). 그래서 링크는 살아 있는데 파일이
없는, 가장 나쁜 상태였다 — 시험은 사본의 **존재**만 봤고 위치를 묻지 않았다.

`anchors.app_static_dir()` 을 만들어 `src\kwise\ui\static\` 을 가리키게 하고
`tools\build_docs.py` 가 거기에 쓰게 했다. 뿌리 `static\` 은 지웠다.
시험도 위치를 묻도록 고쳤다.

### 세 등급 — 화면에는 둘만

경고가 열 줄 넘게 쌓이면 **차단 사유와 참고 사항이 같은 무게로 보인다.** 등급을
셋으로 나누고 화면에는 위 둘만 낸다.

| 등급 | 뜻 | 어디에 |
|---|---|---|
| 차단 | 이대로면 결과를 못 쓴다 | 화면 `st.error` + 산출물 |
| 주의 | 결과 해석이 달라진다 | 화면 `st.warning` + 산출물 |
| 참고 | 알아 두면 좋다 | **Excel 요약 · 보고서 5장만** |

등급 판정은 **UI 층에 둔다** (`kwise\ui\notices.py`). 계산 모듈은 평문 문자열을 내고
이번 세션은 계산을 건드리지 않기로 했으므로, 문구 패턴으로 가른다. **모르는 문구는
주의로 둔다** — 새 경고가 조용히 사라지는 쪽보다 한 번 더 보이는 쪽이 낫다.

같은 사실이 품질 점검과 요금 엔진 양쪽에서 나오는 일이 있어(`2023-11 결측률 32.3%`)
**em 대시 앞부분을 지문으로** 삼아 중복을 접는다. 샘플에서 10건 → 화면 3건이 됐다.

### 사람 말로 — 코드 식별자를 화면에서 뺐다

`최적은 general_b/high_a/II 입니다` 는 개발자 문장이다. 요금제 이름은 **요금 데이터
파일에서** 가져온다 (`kwise\ui\labels.py`). `일반용전력(을) 고압A 선택Ⅱ` 로 읽힌다.

뺀 것 — `청구서를 확보하면 prior_peaks= 로 주입하십시오 (요구사항서 5.2)`,
옆단의 `verified=false` 경고, 화면 문구 속 `(요구사항서 N장)`.
요금표 검증 여부는 **「기준 데이터」 화면 한 곳**에만 둔다. 손댈 곳이 거기뿐이다.

시험은 소스의 **한글이 든 문자열만** 훑어 코드 식별자를 찾는다.
`"tariff_switch" in enabled` 같은 키 비교까지 막으면 사전 키를 못 쓴다.

### 나머지 — 배치와 표기

- **지표 카드가 먼저, 차트, 표가 마지막.** 1단계는 기간·최대수요·부하율·사용량,
  3단계는 절감액·투자비·회수기간·확실성. 카드는 최대 넷, 숫자와 단위만 넣는다
- 1단계 순서를 **품질 → 패턴 → 피크 → 요금 구조 → 계약전력 → 개선 여지**로.
  "그래서 무엇을 할 수 있나" 가 맨 아래, 2단계 단추 바로 위다
- 단계 하단 이동 단추. **`on_click` 콜백으로 옮긴다** — 옆단 라디오가 그려진 뒤에
  같은 키를 건드리면 Streamlit 이 막는다. 실주행 시험이 이걸 잡았다
- 수단 카드 일곱 **전부 접힘**. 경고·참고는 「확인사항 N건」 하나로 합쳤다
- `won_short` 를 `1억 2,340만원` 꼴로. `1.23억원` 은 읽는 사람이 다시 환산해야 한다.
  **버리지 않고 반올림한다** — 31,518,402원은 3,151만원이 아니라 3,152만원이다
- 감도는 `3,152만원 (2,897 ~ 3,266만원)` 한 줄. 차트와 3행 원자료는 Excel 「감도 상세」
- 경제성DR 은 「연간 최대 가능량」이다. **예상 참여일을 받고 기본값을 두지 않는다** —
  기본값을 두면 그 숫자가 근거 없이 결과에 남는다

**만든 파일**

| 파일 | 하는 일 |
|---|---|
| `src\kwise\ui\notices.py` | 안내 세 등급 판정·중복 제거. 순수 모듈 |
| `src\kwise\ui\labels.py` | 요금 데이터에서 표시 이름을 얻는다 |
| `src\kwise\ui\artifacts.py` | 산출물 바이트를 세션에 담는다 (rerun 대비) |
| `src\kwise\ui\nav.py` | 단계 이동. 옆단 라디오와 **같은 세션 키** |
| `src\kwise\ui\static\*.html` | Streamlit 이 내주는 매뉴얼·기술서 사본 |
| `tests\test_ui_screen.py` | 화면 시험 40건 (소스 훑기 + `AppTest` 실주행) |

**미해결** — 없다. 세션 범위 항목은 모두 끝냈다. 저장소 전체 미해결 7건은 그대로다
(청구서 4 · 참고단가 1 · 약관 확인 1 · 실측 1).

**테스트**
- pytest: **906 passed** (866 + 화면 시험 40)
- ruff: pass (check + format) · mypy: pass (strict, 128 files)
- 케이스 스터디: **타당성 66/66** · 30.6초. **모든 숫자가 11세션과 같다**

---

## 지난 세션 (2026-08-11) — 11세션

**문서를 만들었다. 구현은 손대지 않았다.**

| 만든 것 | 분량 | 독자 | 갱신 |
|---|---|---|---|
| `docs\TECHNICAL.md` → `.html` | 7장 + 부록 3 (57절) | 개발자·1년 뒤의 본인 | 코드가 바뀔 때 |
| `docs\MANUAL.md` → `.html` | 8장 + 부록 (65절) | 설계·영업 담당자 | 화면이 바뀔 때 |
| `docs\CAPTURES.md` | 캡처 아홉 장 목록 | 캡처 찍는 사람 | — |

### 변환기를 직접 썼다

`markdown` 패키지를 넣지 않고 `kwise.docsite` 에 필요한 문법만 구현했다.
**이유는 앵커다.** 화면의 [자세히] 링크가 `MANUAL.html#column-detection` 같은
확정된 id 로 걸려 있는데(8세션), 일반 변환기는 한글 제목에서 어떤 슬러그가
나올지 알 수 없다. `## 제목 {#id}` 로 못 박을 수 있어야 했다.

지원하는 것은 **우리가 쓰는 문법뿐**이다 — 제목·문단·목록·표·코드블록·인용·
가로줄·체크박스와 인라인 넷. 쓰지 않는 문법은 넣지 않았다. 넣으면 시험이 닿지
않는 코드가 생긴다.

### [자세히] 링크를 살리다가 걸린 것

문서를 만들면 링크가 살아나도록 8세션에 설계해 두었다(파일 존재로 판정).
그런데 **`docs\MANUAL.html` 을 상대 링크로 걸면 404 가 난다** —
Streamlit 은 임의 경로의 파일을 내주지 않는다. 브라우저가
`localhost:8501/MANUAL.html` 을 찾다가 없다고 한다.

`build_docs.py` 가 `static\` 에 사본을 두고 `.streamlit\config.toml` 에
`enableStaticServing = true` 를 넣는다. 링크는 `app/static/MANUAL.html#<id>` 로
간다. **사본이 없으면 링크를 비활성으로 둔다** — 죽은 링크를 화면에 내느니
비활성이 낫다.

시험이 앵커 32개가 매뉴얼에 모두 있는지 대조하고, `build_docs.py` 도 같은
검사를 해서 빠지면 **종료 코드 1 로 멈춘다.**

### 중복을 어떻게 막았나

두 문서에 같은 내용을 쓰면 한쪽만 고쳐진다. **한 곳을 정하고 나머지는 가리킨다.**

| 내용 | 어디에 | 다른 쪽 |
|---|---|---|
| 계산식 | 기술서 4장 | "산출 근거는 기술서 4.2 참조" |
| 사용법 | 매뉴얼 3~7장 | "실행 방법은 매뉴얼 2장 참조" |
| **근거 조문** | **기술서 부록 C 한 곳** | 양쪽이 가리킨다 |
| 알려진 한계 | 코드(`report.notices`) | 문서는 갈래만 적는다 |

시험이 이 규약을 지킨다 — 매뉴얼이 기술서를 가리키는지, 기술서가 매뉴얼을
가리키는지, 조문 표가 두 곳에 있지 않은지.

### 매뉴얼 부록 — 한 건을 처음부터 끝까지

기능을 하나씩 설명하는 것보다 **실제 한 건을 따라가는 편이 훨씬 빨리 이해된다.**
샘플 건물로 진단 → 수단 → 조합 → 결론까지 적었다.

거기서 감도 표기의 쓸모가 드러난다. 조합 「선택Ⅱ + 태양광」의 **기본요금
절감액이 −4,831만원(음수)**인데, 선택Ⅱ 가 기본요금 단가를 올리는 대신
전력량요금을 낮추는 요금제이기 때문이다. 전력량요금에서 2억 6,827만원을
아껴 순 2.20억원이 남는다. **조합을 통째로 재계산했기에 이 상충이 보인다** —
수단별 절감액을 더했으면 보이지 않았을 것이다.

### 화면 캡처는 넣지 않았다

자리와 캡션만 잡고 `CAPTURES.md` 에 아홉 장 목록을 남겼다. html 에서는 점선
상자로 보인다. 넣을 때는 **base64 로 넣어야** 단일 파일 규약이 지켜지며,
시험이 외부 이미지 참조를 잡는다.

---

## 지난 세션 (2026-08-11) — 10세션

**실측이 최적화 하나를 찾아냈고, 그것이 이 세션에서 가장 큰 결과다.**

| | 전 | 후 |
|---|---|---|
| 샘플 한 벌 (파이프라인 전 구간) | 24.6초 | **9.4초** |
| `calculate_bill` 1회 | 0.63초 | **0.145초** |
| 케이스 스터디 6종 | 114.2초 | **29.9초** |
| pytest 전체 | 324초 | **98초** |

**회귀값은 한 자리도 바뀌지 않았다** (기본요금 452,832,624원 / 요금적용전력
5,293.44 kW / 총액 3,351,117,349원). 케이스 스터디도 66/66 그대로다.

### 1. 실측 — 물어본 것과 답이 달랐다

질문은 "PV 발전량을 20단계 × 감도 3 = 60회 다시 계산하는가" 였다. **아니었다.**

    PV 시뮬레이션(pvlib)   0.091초 · 곡선 전체에서 **1회**
    용량 스케일(곱셈)      0.002초
    PV 용량 곡선 20단계    13.3초 ← 여기

5세션이 정한 "시뮬레이션 1회 + 곱셈"은 지켜지고 있었다. 되풀이되던 것은
**발전량이 아니라 요금**이었다. 프로파일러를 붙이자 원인이 나왔다.

`calculate_bill` 시간의 **74%가 Period 열을 파이썬 객체로 상자에 넣는 일**이었다
(`_box_func` 180만 회). `slots["month"].to_numpy()` 를 월별 루프 안에서 부르고
있었고, 3만 5천 개짜리 배열을 매달 다시 풀었다. 한 줄 위로 끌어올려 한 번만
풀게 했다. 곡선이 이 함수를 21번 부르므로 그대로 21배로 돌아왔다.

**시간만 봤으면 못 찾았다.** "곡선이 느리다"까지는 알아도 그 안에서 무엇이
느린지는 프로파일러가 알려 준다. 그리고 **호출 수를 함께 세었기에** 발전량이
아니라 요금이 곱해지고 있다는 것을 먼저 알 수 있었다.

### 2. 측정 결과 (샘플 34,358행 · 최적화 후)

| 구간 | 소요 | 비중 | RSS 증감 | 요금 재계산 |
|---|---|---|---|---|
| 데이터 읽기 | 0.06초 | 0.6% | +12.2 MB | — |
| 품질 검사 | 0.01초 | 0.1% | +3.1 MB | — |
| 진단 | 0.61초 | 6.5% | +16.0 MB | 3 |
| 기상 — 캐시 미적중(사전 취득분) | 0.08초 | 0.9% | +11.5 MB | — |
| 기상 — 캐시 적중 | 0.003초 | 0.0% | +0.9 MB | — |
| PV 단위 발전량 (pvlib, 1회) | 0.09초 | 1.0% | +1.4 MB | — |
| PV 발전량 스케일 (곱셈) | 0.002초 | 0.0% | +0.0 MB | — |
| **PV 용량 곡선 20단계** | **4.02초** | **42.6%** | +2.2 MB | **21** |
| 7.1 선택요금 전환 | 0.36초 | 3.8% | −2.0 MB | 2 |
| 7.2 계약전력 조정 | 0.003초 | 0.0% | +0.6 MB | — |
| 7.3 경제성DR | 0.001초 | 0.0% | +0.0 MB | — |
| 7.4 역률 개선 | 0.20초 | 2.1% | −0.6 MB | 1 |
| 7.6 ESS | 0.27초 | 2.8% | +1.9 MB | 1 |
| 7.7 잉여 활용 | 0.09초 | 0.9% | +1.0 MB | — |
| 조합 비교 4종 | 0.96초 | 10.1% | −0.3 MB | 4 |
| 감도 3종 | 0.58초 | 6.2% | +1.9 MB | 3 |
| Excel 시트 생성 | 0.007초 | 0.1% | +0.6 MB | — |
| Excel 파일 쓰기 | 1.42초 | 15.0% | **+45.8 MB** | — |
| Word 보고서 생성 | 0.68초 | 7.2% | +17.1 MB | — |
| **전체** | **9.4초** | | **최대 RSS 273 MB** | **35** |

**요금 재계산은 곱해지지 않는다.** 곡선 21회(=단계+1), 조합 4회(=조합 수),
감도 3회(=시나리오 수). 조합 × 감도 × 선택요금이 곱해지는 자리는 없다.
시험 둘이 이 숫자를 못 박는다.

### 3. 진행 표시 — 계산은 누가 보는지 모른다

`ProgressReporter` 프로토콜 하나를 계산 쪽에 두고, 화면은 Streamlit 을, CLI 는
rich 를 붙인다. **계산 모듈에 `import streamlit` 이 들어오면 시험이 깨진다.**

가중치는 실측값이다. 균등 분할(12.5%)했으면 **태양광 구간에서 막대가 4초 동안
멈춘 것처럼** 보인다 — 그 한 구간이 실제로는 52%다.

    read 0.008 · quality 0.001 · diagnose 0.077 · weather 0.000
    solar 0.518 · measures 0.115 · compare 0.194 · export 0.086

**한 가지가 설계를 바꿨다.** 진행을 보여 주는 계산은 `st.cache_data` 로 감쌀 수
없다 — 캐시된 함수 안에서 바깥 상자에 글을 쓰면 Streamlit 이 그 호출을 재생하려다
`CacheReplayClosureError` 로 죽는다. 그 셋(태양광 곡선·조합 비교·감도)만 세션
상태에 직접 기억하도록 옮겼다. **부수 효과로 동시 사용 격리가 좋아졌다** — 가장
무거운 결과가 세션 밖으로 나가지 않는다.

### 4. Streamlit Cloud 점검 — 배포해도 된다

| 항목 | 실측 | 판단 |
|---|---|---|
| 최대 RSS | **273 MB** | 한도 1 GB 의 **27%**. 절반을 넘지 않는다 |
| 최대 구간 | Excel 파일 쓰기 +45.8 MB | 15분 시계열 시트(35,328행)다. 체크박스로 끌 수 있다 |
| 업로드 상한 | 기본 **200 MB** | 3년치 15분 데이터 **2.5 MB** — 79배 여유 |
| 저장소 클론 | 얕은 클론 **23 MB** (weather 제외 시 3 MB) | weather 기여분 20 MB. pip 설치(수백 MB)에 비하면 무시할 수준 |
| 세션 격리 | 아래 참조 | 문제 없음. 캐시 키를 **내용 해시**로 굳혔다 |
| 임시 파일 | 세션별 uuid 폴더, 만든 즉시 삭제 | 휘발성 파일시스템과 맞는다 |

**세션 격리를 자세히 본 이유.** `st.cache_data` 는 프로세스 전역이라 접속자들이
캐시를 공유한다. 키가 요약 정보(파일명·기간·총량)뿐이면 서로 다른 파일이 같은
키를 가질 여지가 남고, 그때 **옆 사람의 결과가 나온다.** 업로드 바이트의 내용
해시를 키에 물려 그 여지를 없앴다. 전역 상태에 기대는 곳은 없고, `lru_cache` 는
기준 데이터·지역 좌표처럼 **모든 사용자가 같은 값을 봐야 하는 것**에만 걸려 있다.

**빼지 않았다** — `data/weather/` 40 MB 는 그대로 둔다. 판단 근거만 만들라는
지시였고, 실제로 빼야 할 만큼 무겁지 않다.

---

## 지난 세션 (2026-08-11) — 9세션

**의사결정자용 Word 보고서를 붙이고, 8세션이 남긴 잔여 둘을 정리했다.**

| 만든 것 | 내용 |
|---|---|
| Word 보고서 | 표지 + 5장. 각 절이 **결론부터**. 표는 **Word 표 객체** |
| 보고서 차트 | matplotlib png 4종 (Malgun Gothic). 표는 그림으로 넣지 않는다 |
| 공용 목록 | `measures.MEASURE_CATALOG` — 화면과 보고서가 같은 7.1~7.7 을 본다 |
| 공용 프레임 | `report.frames` — altair 와 matplotlib 이 같은 표를 그린다 |
| 잔여 ① | 모듈 상수 둘을 기준 데이터로. `rules_kr` 27 / `assumptions` 22 항목 |
| 잔여 ② | 갑 종별 기본요금 기준이 **잠정임을 화면·산출물에 표시** |
| 시험 | `tests\test_document.py` 30건 + 규칙 6건 + 엔진 2건 |

### 잔여 ① — 상수를 옮기면서 **의미가 갈라졌다**

`DEFAULT_POWER_FACTOR_PCT = 92.0` 을 옮기려다 보니, 이 값이
`power_factor.lagging_standard_pct` 와 **같은 92 지만 다른 조문**이었다.

    제41조·제43조 ②   역률요금의 **기준** — 넘으면 감액, 못 미치면 추가
    제42조            무효전력계 미설치 고객의 **간주값** — 우리가 쓰는 기본값

한 항목으로 묶으면 기준이 90 으로 개정됐을 때 **역률을 모르는 고객의 값까지
따라 내려가** 조정액이 0 이 아니게 된다. 실측하지 않은 값으로 금액이 생기는
것이다. `power_factor.deemed_lagging_pct` 를 따로 두고 `power_factor_charge`
와 화면 기본값이 그쪽을 읽게 했다. **오늘 두 값이 같아 숫자는 하나도 바뀌지
않았고**, 케이스 스터디 66/66 도 그대로다.

`DEFAULT_MARGIN_RATIO = 0.10` 은 판단값이라 `contract.margin_ratio` 로 갔다.
데이터클래스 필드 기본값은 `default_factory` 로 바꿨다 — **필드 기본값은 import
시점에 고정되므로** 화면에서 파일을 고쳐도 그 프로세스에서는 옛 값이 쓰인다.

### 잔여 ② — 모르는 것을 **밖으로** 낸다

약관 제38조를 확인하지 못해 갑 종별 기본요금 기준이 여전히 잠정이다. 값을
바꿀 수 없는 동안에도 쓰는 사람은 알아야 하므로, 경고를 계산 결과에 실어
화면과 산출물로 흘려보냈다.

**PoC 범위(일반용(을))가 이 경로를 타지 않는다는 점이 이 표시를 넣은 이유다.**
샘플에서는 아무 일도 일어나지 않으므로, 갑 종별을 실제로 쓰는 첫 사람이 조용히
틀린 금액을 받게 된다. 시험 두 건이 "갑에는 붙고 을에는 붙지 않음" 을 지킨다.

### 보고서 — 무엇을 화면과 다르게 했나

Excel 아홉 시트는 분석자용 데이터다. 의사결정자에게 그대로 내밀 수 없다.

| 원칙 | 이유 |
|---|---|
| **각 절은 결론부터** | 데이터를 앞에 놓으면 읽는 사람이 결론을 스스로 만들어야 한다 |
| **표는 Word 표 객체** | 이미지로 넣으면 자사 제안서에 복사해 쓸 수 없다. 그림은 차트뿐 |
| 감도는 **범위** 한 줄 | 3열 나열은 "어느 쪽이 좋은 값인가" 를 찾게 만든다 (9.2) |
| 미산출은 **사유** | 빈칸도 0원도 아니다. 0원은 '공짜' 로 읽힌다 (7.5) |
| 수단 0개면 **3·4장을 빼고 번호를 당긴다** | 제목만 남은 빈 장은 "검토했는데 결과가 없다" 로 읽힌다 |

**목록을 두 벌 두지 않았다.** 화면(`ui.spec`)과 보고서(`report.document`)가
각자 7.1~7.7 을 들고 있으면 한쪽만 고쳤을 때 어긋나고, 그 어긋남은 두 산출물을
나란히 놓고서야 드러난다. `measures.MEASURE_CATALOG` 하나를 둘이 읽는다.
차트 프레임도 같은 이유로 `report.frames` 로 모았다 — altair 와 matplotlib 이
같은 이름의 차트에서 다른 수를 그리는 일을 막는다.

---

## 지난 세션 (2026-08-11) — 8세션 Streamlit UI

**마지막 구현 세션이다.** 계산에는 손대지 않았다.

| 만든 것 | 내용 |
|---|---|
| 화면 넷 | 1 진단 · 2 개선 수단 · 3 비교 · 기준 데이터 관리 |
| 매뉴얼 앵커 | **32개 확정.** `docs\MANUAL_ANCHORS.md` (생성물). 링크는 비활성 |
| 열 판정 고치기 | `override_columns()` — 자동 탐지 실패 시 드롭다운으로 |
| 시험 | `tests\test_ui.py` 77건 |

### 잡은 것 — 셋 다 화면을 붙이지 않았으면 안 보였을 것들이다

**1. 계약전력이 요금 엔진까지 가지 않았다.** `ContractInfo` 에만 넣고
`BillingOptions` 를 비우면 요금적용전력 **하한(계약전력의 30%)이 적용되지 않는다.**
금액은 그럴듯하게 나오고 저부하 사업장에서만 틀린다. 케이스 스터디는
`ContractInfo` 만 쓰는 경로라 드러나지 않았다. `ContractForm` 이 두 곳을 함께
채우게 하고 회귀 시험을 붙였다.

**2. 수단을 하나도 켜지 않으면 산출물 생성이 통째로 멈췄다.**
`measure_summary_frame()` 이 빈 목록에 `set_index("수단")` 을 걸어 `KeyError` 였다.
진단만 보고 Excel 을 받는 것이 정상 경로인데 거기서 죽는다. 열 구조를 유지한
빈 표를 내도록 고쳤다.

**3. 2단계 입력이 3단계에 닿지 않았다.** ESS 목표 피크·단가를 위젯 키 없이
받아 그 화면에만 남았고, 조합 비교는 목표를 모른 채 ESS 를 빼고 계산했다.
`state.input_key()` 로 세션에 남기고 3단계가 읽는다.

### 화면과 매뉴얼의 역할 분담 — 앵커 32개를 확정했다

판단 기준은 하나다. **없으면 입력을 못 하거나 결과를 오독하는 것만 화면에 둔다.**
출처·근거 조문·제도 설명·원리·배경은 매뉴얼로 보내고 화면에는 한 줄 + [자세히].

**매뉴얼이 아직 없으므로 링크를 비활성으로 두었다.** 없는 문서로 보내면 화면을
못 믿게 된다. 앵커 이름은 `src\kwise\ui\anchors.py` 가 정본이고
`docs\MANUAL_ANCHORS.md` 는 생성물이며, **두 벌이 어긋나면 테스트가 깨진다.**

**예외 — 경고는 화면에 남겼다.** 역률 92% 미달, 고출력 셀 사양(방전시간 0.5h
미만), 결측 편중, 계약전력 변경 위험 넷이다. 결과 해석을 바꾸는 것을 매뉴얼로
보내면 안 읽는다.

### 캐시 비움을 두 겹으로 걸었다

지난 세션의 `reload_rules()` 는 rules 쪽 lru_cache 만 비운다. 그것만 믿으면
**파일은 바뀌었는데 화면은 옛 값**이 된다.

    ① apply_rule_edit()  편집이 성공했을 때만 st.cache_data.clear()
    ② rules_stamp()      47항목의 지문을 모든 캐시 키에 물린다

①만 두면 다른 경로(엑셀 왕복·손편집)로 파일이 바뀐 것을 놓친다. ②만 두면
지문을 빠뜨린 캐시가 생겼을 때 조용히 새어 나간다.

---

## 지난 세션 (2026-08-07) 한 일

**7세션을 마치고 8세션 UI 앞까지 왔다.** 커밋 다섯 개다.

| 커밋 | 내용 |
|---|---|
| `dd47c6e` | 태양광 투자비를 kWp당 단가로 통일 |
| `d66674b` | **7세션 — 검증과 케이스 스터디. 타당성 66/66 통과** |
| `3d5997e` | 케이스 스터디 결과를 저장소에 남김 |
| `6680662` | 기준 데이터 외부화 — 근거·확인일·원복·만료 감지 |

앞선 세션(10·11차 보완)까지 합하면 감도 첨예도 전환, 기상 사전 취득 완료,
ESS 단가 재설계도 오늘 안에 들어갔다.

### 만든 것

| 항목 | 결과 |
|---|---|
| 태양광 단가 | kWp당 단가 + 총액 두 경로. **참고단가는 만들지 않았다** (인용할 자료가 없다) |
| 케이스 스터디 | 6종 × PV 4단계 × 감도 3종 × 선택요금 3종. **66/66 통과**, 95.9초 |
| 케이스 결과 | `output\casestudy_20260807.xlsx` 커밋 |
| 기준 데이터 | `rules_kr.json` **26항목** / `assumptions.json` **21항목** |
| 화면 편집 API | `describe_items` · `set_value` · `confirm` · `diff_from_defaults` |
| 원복 3층 | `defaults\` (읽기 전용) / `data\` (현재) / `backup\` (최근 10개) |
| 만료 감지 | 요금 12개월 / 약관 24개월 / 참고단가 24개월 / 기상 전년도 |

### 오늘 잡은 것 — **셋 다 조용히 틀렸을 것들이다**

**1. 모듈 상수 → 함수 조회.** 값을 파일로 옮기고도 `LAGGING_STANDARD_PCT = 92.0`
처럼 import 시점에 붙잡아 두면 **UI 로 파일을 고쳐도 그 프로세스에서는 옛 값으로
계산된다.** 코드에 기본값을 남기는 것과 같은 사고이고, 값이 그럴듯해서 결과를 다
쓰고 나서야 발견된다. 관련 상수 스무남은 개를 전부 함수로 바꿨다.

**2. 원복 버그 2건.**
- 직전 복원이 **안전 백업을 되돌려** 방금 만든 상태로 돌아갔다. 되돌릴 대상을
  미리 잡아 두는 것으로 고쳤다
- 같은 분에 두 번 백업하면 파일명이 겹쳐 **편집 직전 상태를 덮어썼다.** 접미사를
  붙이도록 고쳤다

둘 다 테스트가 없었으면 실사용에서, 그것도 "되돌렸는데 안 돌아간다" 는 형태로
드러났을 것이다.

**3. 계절·시간대를 요금표 파일에서 읽었다.** 손으로 옮기려다 실제로 틀렸다 —
내가 적은 여름 중간부하는 `[[8,11],[12,13],[18,22]]` 였고 **실제는
`[[8,15],[21,22]]`** 였다. 옮겨 적기를 그만두고 요금표에서 읽어 넣었으며, 두 벌이
어긋나지 않는지 테스트가 지킨다.

### 케이스 스터디에서 드러난 것

- **C6 야간 피크형** — 관측 최대 10,921 kW → 요금적용전력 **2,801 kW** (74.4% 낮다).
  경부하 제외 규칙(5.2 ①)이 파이프라인 전체에서 작동한다
- **C2 오후 피크형** — 500 kWp 에서 이미 포화. PV 를 아무리 키워도 기본요금이 더
  줄지 않는다
- **감도 상한이 18개 조합 어디에서도 '첨예형'이 아니었다** (평탄형 16 / 기준 2).
  11차에 단조성 요구를 뺀 판단이 데이터로 확인됐다
- 판정 잣대를 "기본요금 절감액 ÷ 기본요금" 에서 **요금적용전력 저감 폭(kW)** 으로
  바꿨다. 기본요금 자체가 피크에 비례하므로 앞의 잣대는 뜻이 뒤집힌다

---

## 다음 세션 — **19세션. 텍스트 인벤토리 → 재분류 → 보고서 부록 신설**

> 18세션이 버그 두 건만 잡는 짧은 세션이라 **이 계획은 그대로 남았다.**
> 18세션 2절의 결측 문구 인벤토리가 ①의 예행이다 — 같은 방식으로 넓힌다.

**화면의 글을 전수로 세어 본 적이 없다.** 12·15·16세션이 "이 문구는 화면에 두고
저 문구는 산출물로" 를 그때그때 판단해 왔고, 규약(10.2·10.7)은 있지만 **지금
화면에 무엇이 얼마나 있는지**를 아무도 모른다. 세 걸음으로 간다.

**① 텍스트 인벤토리**

화면에 나가는 문자열을 **전부 뽑아 표로 만든다.** 이미 있는 재료가 둘이다.

- `tests\test_ui_screen.py` 의 `_strings()` — AST 로 한글 문자열만 골라낸다
  (문서 문자열은 뺀다). 이것을 도구로 승격해 `tools\` 로 옮기는 것이 출발점이다
- 계산 모듈이 내는 `warnings` · `notes` — **소스 훑기가 닿지 않는 자리다**
  (15세션에 `general_b/high_a/II` 가 이 경로로 새어 나갔다). 화면을 실제로 띄워
  긁어야 한다

축은 넷이다 — **어느 화면 / 어느 등급(차단·주의·참고) / 몇 글자 / 중복인가.**
16세션에 붙인 「동일 지문 전수 검사」 가 중복 축의 씨앗이다.

**② 재분류**

인벤토리를 놓고 **한 줄씩 판정한다.** 기준은 이미 있다 (10.2).

    화면에 둘 것     없으면 입력을 못 하거나 결과를 오독하는 것
    툴팁으로 보낼 것  요지 (16세션 4절에 `manual_tip` 으로 자리를 만들었다)
    매뉴얼로 보낼 것  출처·근거 조문·제도 설명·원리·배경
    보고서로 보낼 것  근거·한계·전제 — **③ 이 그 자리다**

ESS 확인사항이 스물둘이다 (17세션에 세어 보았다). 그중 몇이 실제로 「없으면
결과를 오독하는 것」 인지 물어야 한다.

**③ 보고서 부록 신설**

②에서 「보고서로」 로 분류된 것이 갈 자리가 지금 없다. Word 5장은 의사결정자용
이라 근거를 길게 실을 데가 아니고, Excel 「요약」 시트는 표다. **부록 한 장**을
새로 만들어 전제·한계·근거를 모은다.

- 어디에 붙일지부터 정한다 (Word 6장인가, Excel 새 시트인가, 둘 다인가)
- **매뉴얼과 겹치지 않게** 한다 — 매뉴얼은 「제도가 어떻게 생겼나」, 보고서
  부록은 「이 건물의 이 산출에 무엇을 전제했나」다
- 요구사항서 부록 D(알려진 한계)가 이미 있다. 그것과의 관계를 먼저 정리한다

**손댈 때 지킬 것 — 17세션이 남긴 규약**

- **금액·전력 축은 0 부터 시작하지 않는다.** 축 제목에 그 사실을 적는다
  (`charts._CUT_SCALE`). 절대값과 변화량은 차트를 나눈다
- **두 선이 붙어 보이면 그 사이를 색으로 채운다.** 그 면적이 곧 변화다
- **범례는 우측 하단이다** (`charts._LEGEND_BOTTOM`). 우측 상단은 값이 커질 때
  라벨을 가린다
- **스케일이 다른 값을 같은 축에 얹지 않는다.** 패널을 나눈다 (ESS 2단)
- **화면과 보고서 png 가 같은 모양이어야 한다.** 프레임을 공유하는 것만으로는
  부족하다 — 축을 자르면 양쪽 다 자른다
- 16세션 규약(탭 · `AnalysisContext` · `carry_inputs` · 링크 없음 · 중복 검사)과
  15세션 규약(`report.frames` · `report.days` · `tariff.labels` · `ui.callout`)은
  그대로다

**미룬 것 — 화면 캡처 아홉 장**

16·17세션이 연달아 화면을 바꿔 캡처를 미뤘다. `docs\CAPTURES.md` 목록이 **세
세션분** 어긋나 있다 (옆단·탭·조합 체크박스·그래프 전부 + 18세션의 ESS 표·곡선
축·진단 품질 블록). **19세션이 텍스트를 다시 건드리므로 그 뒤에 찍는 편이 낫다.**

**자료가 들어오면 언제든**

- **ESS 실내 캐비닛형 사례** — 둘 이상 모이면 `data\ess_cost_cases.json` 에 넣고
  `tools\fit_ess_cost.py` 를 다시 돌린다
- **ESS 옥외 조달 사례를 더 모은다.** 회귀가 네 건이라 홀드아웃 오차가 2.5% 다
- **한전 청구서 한 벌** — 미해결 8건 중 4건이 여기 걸려 있다. 확보하면
  `verified` 를 true 로 바꾸고 **일요일 경부하 계량을 가장 먼저 대조한다**
- **약관 제38조 원문** — 갑 종별 기본요금 기준이 잠정이다
- **태양광 설치 단가 공개 자료** · **모듈 열화율** — 확보하면 참고단가와 20년
  발전량 그래프를 만든다. 지금은 근거 없는 기본값을 만들지 않는다

문서를 고쳤으면 **반드시 html 을 다시 만든다.** md 가 원본이고 html 은
생성물이다 — 시험이 저장소에 생성물이 있는지 본다.

---

## 미해결 항목

세션을 넘어 남아 있는 것. **해결된 항목은 지운다** — 경위는 아래 「세션 기록」에 있다.

**현황 (2026-08-13, 18세션 종료) — 8건. 어느 것도 다음 작업을 막지 않는다.**

| 갈래 | 건수 | 무엇이 있어야 풀리나 | 막나 |
|---|---|---|---|
| 청구서 | 4 | 한전 청구서 한 벌 | 아니오 — 값이 없으면 사유를 표시한다 |
| 참고단가 | 1 | 태양광 단가 공개 자료 | 아니오 — 구조는 열려 있다 |
| 약관 확인 | 1 | 제38조(계기 설치기준) 원문 | 아니오 — **잠정임을 화면·산출물에 표시한다** |
| ESS 사례 | 1 | 실내 캐비닛형 조달 사례 (지금 1건) | 아니오 — 옥외 계수로 산정하고 표에 밝힌다 |
| 실측 데이터 | 1 | 실측 발전 곡선 | 아니오 — `WeatherLabel` 로 바꿔 끼운다 |

**여덟 다 "모르는 것을 모른다고 표시하는" 상태로 처리되어 있다.** 금액을
지어내지 않으므로 화면과 보고서는 그대로 쓸 수 있고, 자료가 들어오면 값만
바꿔 끼우면 된다.

**9세션에서 지운 것 — 코드 상수 1건.** `diagnose\contract.py` 의
`DEFAULT_POWER_FACTOR_PCT`·`DEFAULT_MARGIN_RATIO` 를 기준 데이터로 옮겼다
(아래 세션 기록 참조). 값이 같아 숫자는 바뀌지 않았고 케이스 스터디 66/66 이
그대로다.

### 참고단가를 확보해야 풀리는 것

- [ ] **태양광 설치 단가 참고값** — ESS 는 13세션에 조달 사례 4건으로 회귀 모델을
      만들었지만 (`data\ess_cost_cases.json`) 태양광은 확보하지 못했다. 근거 없는
      기본값을 만들지 않기로 하고 단가 미입력 시 사유를 반환하도록 했다 (7.5). 공개 자료(신재생에너지
      보급통계·공공조달 단가 등)를 확보하면 `PvCostReference` 와
      `data\pv_cost_reference.json` 을 ESS 와 같은 방식으로 채우면 된다.
      **구조는 열어 두었다** (`src\kwise\measures\pv_cost.py`)

### 청구서를 받아야 풀리는 것 (한 묶음)

자사 사옥이나 현장 사무소의 한전 청구서 한 벌이면 아래가 동시에 풀린다.
**7세션 케이스 스터디의 신뢰도가 여기 걸려 있다.**

- [ ] **요금 엔진 검증** — 확보하면 `data\tariff_kr_20260601.json` 의 `verified` 를
      true 로 바꾼다. **일요일 경부하 계량을 가장 먼저 대조한다**
      (3세션 수기 케이스에서 월 6.2% 차이가 났다)
- [ ] **요금적용전력 직전 이력** — 샘플에 없어 첫 11개월이 과소 산출된다.
      `prior_peaks=` 로 주입 가능하다. 청구서와 함께 받는다
- [ ] **실측 역률** — 약관 제42조는 30분 누적 계량을 요구하는데 우리 데이터는
      15분 유효전력뿐이다. 기본값 92% 는 같은 조항의 무효전력계 미설치 간주값이라
      근거가 있고 그 값에서 조정액이 0 이지만, 청구서의 역률 항목을 `power_factor_pct=`
      로 넣으면 바로 재계산된다
- [ ] **엑셀 요금표 옮김 오차 대조** — 농사용(을) 고압 여름철 단가가 엑셀 66.6원 /
      요금표 원본 68.6원으로 다르다. 범위 밖 종별이라 넣지 않았지만
      **같은 표에 다른 오차가 더 있을 수 있다.** 일반용(을)부터 대조한다

### ESS 사례를 더 모아야 풀리는 것

- [ ] **실내 캐비닛형 사례 추가 확보** (14세션에 추가) — 종합쇼핑몰 50 kWh
      1.18억원 한 건뿐이라 **실내형 계수를 따로 만들지 못한다.** 그 한 건은
      옥외 컨테이너·소방·공조가 빠진 **장치비용**이고 설치공사는 별도라, 우리
      회귀(옥외 기준)의 관급 설비비와 같은 층위다. `category="catalog"` 로 두고
      표에만 싣는다. 사례가 둘 이상 모이면 `data\ess_cost_cases.json` 에 넣고
      `toolsit_ess_cost.py` 를 다시 돌린다

### 약관 조문 확인이 필요한 것

- [ ] **갑 종별 기본요금 기준** — 약관 제68조 ②의 기준은 갑/을 구분이 아니라
      **최대수요전력계 설치 여부**다. 현재 구현(갑=계약전력, 을=요금적용전력)은
      관행적 대응이다. 특히 **일반용(갑)Ⅱ 는 시간대별 구분계량기 설치 고객**이라
      최대수요계를 함께 갖추었을 수 있고, 그렇다면 갑Ⅱ 기본요금은 요금적용전력
      기준이어야 한다. **제38조(계기의 설치) 설치기준을 확인해 확정한다.**
      확인 전까지 갑 종별은 계약전력 기준으로 계산하고, 계약전력이 없으면 금액을
      만들지 않고 멈춘다

      **8세션 조사 결과 — 의심이 짙어졌으나 확정하지 못했다.** 웹 검색에서
      "고압 이상으로 공급받는 고객에게는 최대수요전력·무효전력을 계량할 수 있는
      계기를 설치한다" 는 취지의 서술을 여러 2차 자료에서 찾았다. 사실이라면
      **고압 갑 종별도 최대수요전력계를 갖추므로 기본요금이 요금적용전력 기준**
      이어야 하고 현재 구현이 틀린 것이 된다. 다만 한전 공개 페이지는 조문 번호
      체계가 달라(전기공급약관 vs 기본공급약관) 제38조 원문을 확인하지 못했다.
      **2차 자료만 보고 요금 기준을 바꾸지 않았다** — 요금표를 손으로 옮겨 적다
      틀린 전례가 있고(준비 세션), 근거 없이 그럴듯한 값이 들어가는 것이 이
      프로젝트가 가장 경계하는 사고다. 원문 한 장을 확보하면 끝난다

      **9세션 — 잠정임을 밖으로 냈다.** 값을 바꾸지 못하는 동안에도 쓰는 사람은
      알아야 한다. `TENTATIVE_BASE_FEE_BASIS_WARNING` 을 갑 종별 계산에 붙여
      `BillingResult.warnings` → 진단 경고 → 산출물로 흘려보내고, 화면 1단계에는
      **접히지 않는 경고**로 띄운다. PoC 범위(일반용(을))가 이 경로를 타지 않아
      **샘플에서는 아무 일도 일어나지 않는 것**이 이 표시를 넣은 이유다 — 갑
      종별을 실제로 쓰는 첫 사람이 조용히 틀린 금액을 받게 된다.
      확정되면 지울 것: 상수 하나, 엔진 한 줄, 화면 블록 하나, 시험 두 건

### 실측 데이터를 얻어야 풀리는 것

- [ ] **Open-Meteo 일사량의 라벨 규약** — 라벨이 구간 시작이라는 전제로 대표
      순시각을 라벨 + 30분으로 두었다. 실측 발전 데이터를 얻으면 발전 곡선이
      통째로 30분 밀렸는지 대조한다. `WeatherLabel` 로 바꿔 끼울 수 있다

### 해결된 항목 (경위는 세션 기록 참조)

| 항목 | 해결 |
|---|---|
| 계약전력 하한 규정 | 6세션 — 30%, 교육용(을) 15% 특례. `contract_floor_ratio` |
| 선택요금 전환의 전압구분 넘나듦 | 보완 세션 — `switchable_selections()` 로 현행 전압에 가둠 |
| 야간 진상역률 하한 | 8차 — 약관 제43조 ② 2호 나목에 명시. 지상이면 100% 간주 |

---

## 결정 사항

세션 중 내린 판단 중 나중에 다시 들출 만한 것.

### 13세션 — 제도 한도·조달 사례·표시 (2026-08-11)

| 결정 | 이유 |
|---|---|
| 조합 수단은 **키 + 파라미터** | 표시 문자열을 키 자리에 담았다가 라벨 조회가 막혀 3단계가 통째로 죽었다. 라벨은 조회로 만든다 |
| 등록되지 않은 키는 **경고 후 원문 표시** | 표 한 칸 때문에 화면 전체를 잃는 편보다 낫다 |
| 화면 문구는 **en dash**, 계산 문구는 **렌더 시 escape** | 물결표 둘이 한 줄에 있으면 취소선이 된다. 계산 문구는 Excel·Word 로도 가므로 원본을 고치지 않는다 |
| 경제성DR 에 **연 60시간 한도** | 없으면 샘플에서 22배 과대다. 제도 한도가 산출을 지배한다 |
| 60시간을 **확인일 없이** 넣었다 | 신뢰성DR 의무감축 기준일 수 있다. 보수적으로 적용하되 「확인 필요」로 표시한다 |
| 감축 여력을 **운영 시간대**로만 잰다 | 참여할 수 없는 시간대의 부하를 여력으로 세면 안 된다 |
| 참여시간 **기본값 없음** | 지어내면 그 값으로 금액이 난다. 없으면 「연간 최대」라고 적는다 |
| ESS 투자비를 **조달 사례 회귀**로 | LCOS 계통용 단가는 건물 조달 현실과 맞지 않았다 |
| 계수는 **스크립트가 만든다** | 사례가 늘 때 코드를 고치게 두면 갱신이 안 된다. 홀드아웃을 함께 낸다 |
| **kWh당 단가로 말하지 않는다** | 고정비가 1.07억이라 총액÷용량이 세 배 변한다. 작은 설비가 부당하게 나빠 보인다 |
| **kW 를 설명 변수에서 뺀다** | kWh 하나로 R² 0.9996 이다. 사례가 넷뿐이라 자유도를 아낀다 |
| 고정 판정 문구 **삭제** | 값과 무관하게 늘 같은 문장이 나오는 것은 판정이 아니다 |
| **성립 조건**을 낸다 | 회수기간 하나로는 "왜 안 되는가" 를 알 수 없다. 마진이 음수면 규모와 무관하게 성립하지 않는다 |
| 기본요금 단가는 **요금표에서** | 성립 조건에 단가를 하드코딩하면 종별이 바뀔 때 조용히 틀린다 |
| 적용 구간 밖은 **올려 산정 / 참고값** | 고정비가 지배적이라 작게 만들어도 투자비가 그만큼 줄지 않는다 |
| 결측 안내를 **한 묶음**으로 | 같은 사실이 세 문장으로 흩어져 있으면 세 가지 문제로 읽힌다 |
| 같은 값이면 지표를 **접는다** | 두 칸을 나란히 두면 다른 개념인 줄 알고 차이를 찾게 된다 |
| 열 이름 번역표는 **한 곳** | 화면·Excel·보고서가 각자 붙이면 같은 열이 세 이름으로 불린다 |
| 여유가 없으면 **슬라이더를 감춘다** | 움직여도 0% 인 슬라이더는 고장으로 보인다 |
| 태양광에 **계산 버튼** | 파이프라인의 절반이라 값 하나 바꿀 때마다 화면이 멈췄다 |
| 3단계는 **표가 먼저** | 어느 조합을 왜 권하는지 말하려면 견줄 것이 먼저 보여야 한다 |

### 12세션 — 화면 (2026-08-11)

| 결정 | 이유 |
|---|---|
| 산출물 바이트를 **세션에 담는다** | `download_button` 클릭이 rerun 을 일으켜 만들기 분기가 건너뛰어진다. 담아 두지 않으면 단추도 결과도 사라진다 |
| 담아 둔 것에 **입력 토큰**을 붙인다 | 입력이 바뀐 뒤 옛 바이트를 내려받게 두면 화면 숫자와 파일 내용이 어긋난다 |
| 정적 사본은 **주 스크립트 옆** | Streamlit 은 `Path(main_script).parent / "static"` 만 내준다. 저장소 뿌리에 두면 링크는 살고 파일은 없는 상태가 된다 |
| 등급 판정을 **UI 층에** | 계산 모듈은 평문을 낸다. 이번 세션은 계산을 건드리지 않기로 했으므로 문구 패턴으로 가른다 |
| **모르는 문구는 주의** | 새 경고가 조용히 사라지는 쪽보다 한 번 더 보이는 쪽이 낫다 |
| 중복 판정은 **em 대시 앞** 지문 | 같은 사실을 품질 점검과 요금 엔진이 각자 낸다. 뒤쪽 사유만 다르다 |
| 참고 등급은 **산출물에만** | 화면에서 차단 사유와 같은 무게로 보이면 진짜 경고가 묻힌다 |
| 요금제 이름은 **요금 데이터에서** | 화면에 코드 식별자를 내지 않는다. 매핑을 코드에 또 두면 요금표가 바뀔 때 어긋난다 |
| 검증 여부는 **기준 데이터 화면에만** | 옆단·진단에도 걸었더니 같은 문장을 매 화면에서 읽게 됐다. 손댈 곳은 한 곳뿐이다 |
| `won_short` 는 **억+만원 병기** | `1.23억원` 은 만원 자리를 감춘다. 읽는 사람이 다시 환산해야 한다 |
| 금액 표기를 **버리지 않고 반올림** | 31,518,402원을 3,151만원으로 적으면 감도 범위와 어긋나 보인다 |
| 감도는 **범위 한 줄**만 화면에 | 3행 원자료와 차트를 계산 표 옆에 두면 어느 것이 결과인지 알 수 없다. 근거는 Excel 「감도 상세」 |
| 경제성DR **참여일 기본값 없음** | 기본값을 두면 근거 없는 숫자가 결과에 남는다. 안 넣으면 「연간 최대 가능량」이라고 적는다 |
| 이동 단추는 **`on_click` 콜백** | 라디오가 그려진 뒤 같은 세션 키를 건드리면 Streamlit 이 막는다. 콜백은 다음 실행 전에 돈다 |
| 「연간 사용량」 라벨을 **기간에 맞춘다** | 1년치가 아닌 자료를 "연간" 이라 적는 것 자체가 오독이다. 350일 미만이면 「기간 사용량」 |
| 화면 시험은 **소스 훑기 + 실주행** | 문구 규약(코드 식별자·접힘 상태)은 소스로, 배치와 rerun 은 `AppTest` 로 잡는다 |

### 11세션 — 문서 (2026-08-11)

| 결정 | 이유 |
|---|---|
| **기술서와 매뉴얼을 나눈다** | 독자와 갱신 주기가 다르다. 코드가 바뀌면 기술서, 화면이 바뀌면 매뉴얼이다. 한 문서로 묶으면 둘 다 낡는다 |
| **md 가 원본, html 은 생성물** | 두 벌을 손으로 유지하면 반드시 어긋난다. docx 는 만들지 않는다 |
| **변환기를 직접 썼다** | 화면 링크가 확정된 id 로 걸려 있다. 일반 변환기는 한글 제목에서 어떤 슬러그가 나올지 알 수 없다. `{#id}` 를 못 박을 수 있어야 했다 |
| 지원 문법을 **쓰는 것으로 한정** | 넣지 않은 문법은 시험이 닿지 않는 코드가 된다 |
| 링크를 **`app/static/`** 으로 | Streamlit 이 임의 경로를 내주지 않는다. `docs\` 상대 링크는 404 다 |
| 사본이 없으면 **비활성 유지** | 죽은 링크를 화면에 내느니 비활성이 낫다. 없는 문서로 보내면 화면을 못 믿게 된다 |
| 앵커 누락 시 **빌드 실패** | 경고로 두면 넘어간다. 종료 코드 1 로 멈춘다 |
| **근거 조문은 기술서 부록 C 한 곳** | 두 곳에 적으면 한쪽만 고쳐진다. 양쪽이 가리킨다 |
| 알려진 한계는 **코드가 원본** | `report.notices` 가 산출물에도 실린다. 문서는 갈래만 적는다 |
| 매뉴얼에 **부록(한 건 따라가기)** | 기능을 하나씩 보는 것보다 한 건을 끝까지 따라가는 편이 빨리 이해된다 |
| 캡처는 **자리만** | 사람이 찍는다. base64 로 넣어야 단일 파일이 유지되고 시험이 외부 참조를 잡는다 |
| 목차에 **h4 까지** | 링크가 걸린 소절(설치 밀도·참고단가 등)을 목차에서 못 찾으면 곤란하다 |
| 체크박스를 **저장하지 않는다** | 저장하면 어느 브라우저의 상태가 정본인지 물어야 한다. 사라진다고 문서에 적었다 |

### 10세션 — 성능·진행 표시 (2026-08-11)

| 결정 | 이유 |
|---|---|
| **소요와 함께 `calculate_bill` 호출 수를 센다** | 시간만 보면 "곡선이 느리다"까지다. 호출 수를 세었기에 **발전량이 아니라 요금이 곱해진다**는 것을 먼저 알았다. 조합 × 감도 × 용량이 곱해지는 사고는 호출 수로만 보인다 |
| 메모리는 **작업 집합을 직접 읽는다** | `tracemalloc` 은 파이썬 할당자만 본다. 이 도구에서 메모리를 쥐는 것은 NumPy 배열이고 그것이 빠진다 |
| 못 읽으면 **`None`** | 0 을 돌려주면 "메모리를 안 썼다"로 읽힌다. 실제로 `argtypes` 를 빠뜨려 조용히 0 이 나왔다 |
| Period 열을 **한 번만 푼다** | 월별 루프 안의 `to_numpy()` 가 3만 5천 개를 매달 상자에 넣고 있었다. `calculate_bill` 의 74%였고 곡선이 21번 부르므로 21배로 돌아왔다 |
| 진행 가중치를 **실측값으로** | 균등 분할하면 태양광 구간(실제 52%)에서 막대가 4초 멈춘 것처럼 보인다 |
| 가중치를 `assumptions.json` 에 | 코드에 두면 "왜 이 값인가"를 물을 곳이 없다. 12장 규약 그대로다 |
| 콜백은 **선택 인자**, 기본값은 아무 일도 안 하는 보고자 | `if reporter is not None` 을 곳곳에 흩뿌리면 빠뜨리는 자리가 생긴다 |
| **진행을 보여 주는 계산은 `st.cache_data` 로 감싸지 않는다** | 캐시된 함수가 바깥 상자에 글을 쓰면 재생 시 죽는다(`CacheReplayClosureError`). 그 셋만 세션 기억으로 옮겼다 — 부수 효과로 동시 사용 격리가 좋아진다 |
| 건너뛴 단계도 **진행률을 채운다** | 채우지 않으면 막대가 멈춘 것처럼 보인다. 사유(`캐시 적중`)를 함께 낸다 |
| 캐시 키에 **업로드 내용 해시**를 물린다 | `st.cache_data` 는 프로세스 전역이라 동시 접속자가 공유한다. 요약 정보만으로 키를 만들면 다른 파일이 같은 키를 가질 여지가 남고, 그때 옆 사람 결과가 나온다 |
| `data/weather/` 40 MB 를 **빼지 않는다** | 얕은 클론 23 MB 중 20 MB 지만, pip 설치(수백 MB)에 비하면 무시할 수준이다. 빼면 첫 조회가 네트워크에 묶인다 |

### 9세션 — 보고서 (2026-08-11)

| 결정 | 이유 |
|---|---|
| **역률 기준(제41조)과 간주값(제42조)을 두 항목으로** | 오늘 둘 다 92% 라 묶고 싶어지지만 조문이 다르다. 기준만 개정되면 **역률을 모르는 고객의 값까지 따라 움직여** 실측하지 않은 값으로 조정액이 생긴다 |
| 데이터클래스 기본값은 **`default_factory`** | 필드 기본값은 import 시점에 고정된다. 화면에서 파일을 고쳐도 그 프로세스에서 만든 객체는 옛 값을 쓴다 — 8세션 준비의 '모듈 상수' 사고와 같은 형태다 |
| 갑 종별 잠정 경고를 **계산 결과에 싣는다** | 화면에만 두면 Excel·Word 로 나간 결과에는 남지 않는다. `BillingResult.warnings` 에 실으면 세 갈래로 함께 흐른다 |
| 잠정 경고를 **접히지 않게** 화면에 | PoC 범위가 이 경로를 타지 않아 회귀로 잡히지 않는다. 갑 종별을 쓰는 첫 사람이 조용히 틀린 금액을 받는다 |
| **보고서는 결론부터, 매뉴얼은 원리** | 독자가 다르다. 보고서에 원리를 넣으면 의사결정자가 결론을 찾아 헤매고, 매뉴얼에 결론을 넣으면 다음 건물에서 못 쓴다 |
| **표는 Word 표 객체. 이미지 금지** | 자사 제안서에 복사해 쓰는 것이 이 산출물의 용도다. 이미지는 그 용도를 통째로 막는다 |
| 수단 0개면 **3·4장을 빼고 번호를 당긴다** | 제목만 남은 빈 장은 "검토했는데 결과가 없다" 로 읽힌다. 1·2·5장으로 끝내고 검토 범위가 밝힌다 |
| 장 번호를 **위치로 매긴다** | 3·4장이 빠졌을 때 1·2·5 로 건너뛰면 문서가 깨져 보인다 |
| Word 금액에 **단위를 값에 붙인다** | Excel 은 열 이름이 `(원)` 을 달지만 Word 표 머리글에는 없다. 숫자만 있는 칸은 읽는 사람이 단위를 되물어야 한다 |
| 수단 목록을 **`measures.catalog` 하나로** | 화면과 보고서가 각자 들면 한쪽만 고쳤을 때 어긋나고, 두 산출물을 나란히 놓고서야 드러난다 |
| 차트 프레임도 **한 곳에** (`report.frames`) | altair 와 matplotlib 이 같은 이름의 차트에서 다른 수를 그리는 일을 막는다 |
| 그림은 **바이트로** 돌려준다 | 보고서 한 벌에 그림이 넷이다. 임시 파일을 만들면 지우는 책임이 생긴다 (8세션 규약) |
| matplotlib 폰트를 **그릴 때마다 건다** | `rcParams` 는 전역이라 다른 코드가 바꿔 놓을 수 있다. 산출물이 두부(□)로 나가는 것은 되돌릴 수 없다 |

### 8세션 — UI (2026-08-11)

| 결정 | 이유 |
|---|---|
| **판단이 들어가는 것은 전부 순수 모듈에** | 화면 순서·정렬·표기·조합 구성은 틀리면 결과가 달라지는 판단이다. 뷰 안에 두면 시험이 닿지 않아 "화면을 띄워 눈으로 보는" 검증밖에 못 한다. 순수 모듈에 `import streamlit` 이 들어오는 것을 시험이 막는다 |
| Streamlit `pages\` 자동 배치를 쓰지 않는다 | 화면 순서를 요구사항서가 정한다. 파일 이름순 자동 배치에 맡기면 이름을 고칠 때마다 순서가 흔들린다. 폴더도 `views\` 로 두어 자동 인식과 겹치지 않게 했다 |
| **세션에는 원재료만 둔다** | 업로드 바이트·계약 정보·수단 입력뿐이고 계산 결과는 캐시에서 꺼낸다. 결과까지 세션에 쌓으면 기준 데이터를 고쳤을 때 어느 쪽이 옛 값인지 알 수 없다 |
| 캐시 비움을 **두 겹으로** | 편집 훅(`apply_rule_edit`)만 두면 엑셀 왕복·손편집을 놓치고, 지문(`rules_stamp`)만 두면 지문을 빠뜨린 캐시가 조용히 샌다 |
| 무거운 값은 `_` 인자, 대표 토큰을 캐시 키로 | Streamlit 규약이다. 부하 시계열을 해시하면 매 조회가 느려진다 |
| **[자세히] 링크를 비활성으로 둔다** | 매뉴얼이 10세션이다. 없는 문서로 보내면 화면 전체를 못 믿게 된다. 앵커 이름만 먼저 확정해 문서가 생기는 순간 살아나게 했다 |
| 앵커 정본은 코드, 문서는 생성물 | 두 벌을 손으로 맞추면 반드시 어긋난다. 테스트가 대조한다 |
| **경고 넷은 화면에 남긴다** | 역률 미달·고출력 셀·결측 편중·계약전력 변경 위험. 결과 해석을 바꾸는 것을 매뉴얼로 보내면 안 읽는다 |
| 열 판정 덮어쓰기는 **바뀐 것만** `user` 로 표시 | 화면이 두 드롭다운을 늘 함께 넘긴다. 넘겼다는 사실만으로 표시를 바꾸면 손대지 않은 판정까지 사용자 몫이 되어, 산출물만 보고는 자동 판정인지 알 수 없다 |
| 조합은 **켠 수단만으로 누적**한다 | 켜지 않은 수단이 조합에 섞이면 "보지 않은 것" 이 "검토했더니 이만큼" 으로 둔갑한다. 반대로 조용히 빼면 "효과가 없었다" 로 읽히므로 「검토 범위」로 명시한다 |
| 수단 입력에 위젯 키를 준다 | 2단계 입력을 3단계가 읽어야 한다. 키가 없으면 그 화면에만 남아 조합 비교가 목표 피크·단가를 모른 채 돈다 |
| 빈 「수단별 결과」도 **열 구조를 유지**한다 | 진단만 보고 내려받는 것이 정상 경로다. 빈 표에 `set_index` 를 걸면 산출물 생성이 통째로 멈춘다 |
| 값 편집은 **형을 지켜 되돌린다** | 정수 항목을 실수로 저장하면 스키마에 걸리거나, 걸리지 않으면 산출물에 `12.0` 으로 찍힌다 |
| 목록·사전 항목은 화면에서 고치지 않는다 | 구조가 있는 값을 텍스트로 받으면 오타 한 자에 파일이 깨진다. 엑셀 왕복이 이미 있다 (12.2) |
| **기상 현황에 만료 개념을 넣지 않는다** | 부분 취득이 정상 상태다. 없는 격자를 오류로 칠하면 멀쩡한 상태를 고장으로 읽게 된다 |
| 원문 확인처를 **전 항목에** 붙인다 | 만료 경고가 뜬 것에만 달면 나머지는 근거를 따라갈 데가 없다. 근거를 값 옆에 두는 것이 그 화면의 존재 이유다 |
| 내려받기 Excel 을 **만들고 바로 지운다** | 업로드·산출 데이터를 서버에 남기지 않는다. 세션 종료 훅이 없으므로 켤 때 묵은 폴더를 쓸어내는 그물을 함께 둔다 |

### 9차 — DR 감축량·지역·태양광 입력 (2026-08-07)

| 결정 | 이유 |
|---|---|
| **등록 권장값과 연간 감축량을 분리** | 경제성DR 은 하루 전 입찰이라 매일 다른 양을 입찰한다. 등록값(하위 10%)으로 곱하면 부하가 많은 날의 여력을 버려 샘플에서 **55% 과소평가**된다 |
| 등록 분위수 10% 와 저부하일 5% 를 **다른 상수로** | 쓰임이 다르다. 하나는 계약 등록값, 하나는 무비용 감축일 식별이다 |
| 등록값 × 일수를 `flat_reduction_kwh` 로 **남겨 둔다** | 지우면 왜 그 방식을 안 쓰는지 알 수 없다. 비교값으로 두고 과소평가 폭을 주석에 적는다 |
| **시군구 단위 선택으로 충분** | ERA5 격자가 25~31 km 다. 서울 25개 구의 좌표 폭(남북 20.6·동서 29.0 km)이 격자와 비슷해 같은 셀을 조회한다. 정확도를 희생하지 않는다 |
| 좌표 직접 입력을 **옵션으로 남긴다** | 산악·해안 지형이나 넓은 시군구는 중심점이 부지와 멀 수 있다 |
| 개편된 시도는 **키를 그대로, 표시만 병기** | 키를 바꾸면 기존 케이스 정의가 깨진다. 강원도 → "강원특별자치도 (구 강원도)" |
| 세종 좌표를 **코드에 두고 원본은 손대지 않는다** | 원본은 받은 그대로 두어 출처를 추적할 수 있게 한다. 추가분만 코드에서 밝힌다 |
| **캐시 키를 0.25° 격자로 반올림** | ERA5 해상도라 손실이 없고, 시군구 선택이든 좌표 입력이든 같은 격자면 캐시를 공유한다. 7세션 재조회를 크게 줄인다 |
| **태양광 기본 입력을 3개로** (면적·밀도·지역) | 발전량 예측 R² 가 0.8 이라 입력 정밀도가 기상 오차를 이길 수 없다. 게다가 용량 곡선이 20단계를 훑으므로 면적 환산이 ±20% 틀려도 곡선 위 다른 점을 볼 뿐이다 |
| **모드를 나누지 않는다** — 확장 패널로 접는다 | 상세 설계 모드를 만들면 pvsim 과 경계가 흐려진다 |
| 밀도 하나가 **GCR 과 경사각을 함께** 정한다 | 고밀도 배치는 이격을 좁히는 대신 경사를 낮춰 음영을 줄이는 실무 관행이다 |
| 프리셋을 `data\pv_presets.json` 에 | 요금표·지역과 같은 규약이다. 값을 코드에 두지 않는다 |

### 8차 — 역률 야간 조항과 경제성DR (2026-08-07)

| 결정 | 이유 |
|---|---|
| **야간 진상 페널티는 '콘덴서 과투자의 결과'** | 제43조 ② 2호 나목이 지상이면 100%로 간주한다. 대부분의 건물이 야간 경부하에서 지상이므로 페널티는 0이다. 진상은 고정 콘덴서가 과다할 때만 생긴다 |
| `leading_pct=None` 을 **지상 간주(추가 0)** 로 | 이전에는 "모르니 산출 안 함 + 경고" 였는데, 나목이 답을 준다. 모르는 것이 아니라 **기본값이 0** 인 것이다 |
| 7장 순서를 **투자비 순으로 확정**. 이후 바꾸지 않는다 | 수단을 더할 때마다 번호가 밀리면 코드 docstring 과 문서 참조가 계속 어긋난다 |
| **DR 거래일 판정을 tariff 와 따로 둔다** | 요금은 토요일을 중간부하로 낮출 뿐 공휴일로 보지 않지만 DR 은 토·일·공휴일이 모두 제외다. 같은 함수로 판정하면 두 규칙이 조용히 섞인다 |
| DR 등록 용량은 **평균이 아니라 하위 10%** | 평균으로 등록하면 절반의 날에 미달한다. 미달은 위약금이다(별표26). 평균 기준값도 함께 내되 등록값은 보수적으로 |
| 저부하일 기준선은 **거래 가능일만으로** 계산 | 주말이 섞이면 기준선이 내려가 평일 저부하일이 걸리지 않는다 |
| DR **정산 단가에 기본값을 두지 않는다** | 전력거래소 월별 순편익가격 + 사업자 수수료라 우리가 만들 수 없다. 없으면 감축량(kWh)만 낸다 |
| DR **기본요금 절감을 계산하지 않는다** | SMP 기준 산발 입찰이라 참여일이 연중 최대수요일과 겹칠 확률이 낮다. 편익은 정산금 하나 |
| 0.1 MW-h 문턱은 **참고 표시만** | 제12.4.2.1조 1항 2호는 수요관리사업자가 묶은 **자원 단위** 기준이라 개별 고객 판정이 아니다 |
| DR 확실성 '중간' | 입찰 낙찰 여부와 참여일 수가 운영에 달렸다. 요금표만으로 확정되는 7.1·7.2·7.4 와 다르다 |

### 보완 세션 — 전압구분과 역률 (2026-08-07)

| 결정 | 이유 |
|---|---|
| **전압구분도 현행에 가둔다** (`switchable_selections`) | 고압A 3.3~66 kV / 고압B 154 kV / 고압C 345 kV — 수전설비가 물리적으로 다르다. 단가만 보면 그럴듯한 권고가 나와서 더 위험하다 |
| 드롭다운(`list_selections`)은 가두지 않는다 | 부록 A.3 은 선택지 생성이고, 가두는 것은 **전환 비교**뿐이다 |
| 최적 조합은 `min(selections, ...)` 로 고른다 | 넘겨받은 `option_totals` 에 다른 전압이 섞여도 그것을 뽑지 않는다 |
| **역률 기준은 92%. 초판의 90% 는 오류** | 약관 제41·43조 원문. 감액(97%까지)도 있다 |
| **추가·감액을 금액으로 낸다.** 경고로 두지 않는다 | 기본요금의 −1.0% ~ +6.4%. 샘플에서 1%가 453만원이다 |
| 역률 기본값 92%, **그 값에서 조정액 0** | 제42조의 무효전력계 미설치 간주값이라 근거가 있다. 모르는 채로 금액을 만들어내지 않으면서 3세션 이래 회귀값이 그대로 유지된다 |
| 야간 진상역률은 모르면 **산출하지 않고 경고만** | 무효전력 실측 없이 진상역률을 지어낼 근거가 없다 |
| 역률 판정 창을 **08~22시**로 (발전 시간대 아님) | 약관이 그 창의 지상역률로 요금을 매긴다. 발전 시간대(≈06~19시)로 재면 요금 규칙과 창이 어긋난다 |
| 제43조 ③(첫 달 예고)은 **계산에 넣지 않는다** | 첫 달을 빼면 기간에 따라 금액이 달라져 도입 전후 비교(Δ)가 흔들린다. 주석·부록 D 로만 남긴다 |
| 역률 개선 확실성은 **'높음'** | 요금표와 약관만으로 확정된다. 발전량 예측 같은 불확실성이 없어 감도도 무관하다 |
| 역률 개선 절감액도 **재계산** | 두 역률에서 각각 `calculate_bill()`. 기본요금 비율만 곱해 어림하지 않는다 |

### 준비 세션 — 파서와 요금표 (2026-08-07)

| 결정 | 이유 |
|---|---|
| 열 판정을 3단으로 (헤더 행 → 이름 → **내용**) | 양식은 바뀐다. 열 위치에 기대면 다음 케이스에서 깨진다 |
| 값 열의 상수 배제는 **비율이 아니라 개수**로 | 실측 kWh 의 고유값 비율이 4.1% 다. 5% 문턱을 두면 진짜 사용량 열이 잘린다. 고객번호는 고유값이 1개라 개수로 정확히 걸린다 |
| 판정 단계는 쉼표만 걷어내고 `to_numeric` | 로더의 `parse_usage_energy` 처럼 기호를 다 지우면 계기번호 `55-282007100` 이 숫자로 둔갑한다 |
| CSV 인코딩 채택은 **1차에 이름 매칭만** 허용 | 내용 기반은 헤더가 깨져도 성공해 버려 잘못된 인코딩이 채택된다 (1세션 결정의 연장) |
| 내용 기반 단계의 헤더 후보는 **열이 꽉 찬 행**만 | 제목 행을 헤더로 잡으면 진짜 헤더가 데이터 첫 줄로 섞인다 |
| 요금표는 **엑셀 변환**. 수기 입력 금지 | 60개 값을 손으로 옮기면 오차가 섞인다. 실제로 엑셀의 농사용(을) 단가가 원본과 다르다 |
| 계절 열은 **헤더 이름**으로 매핑 | 산업용 시트만 봄·가을이 여름보다 앞이다. 위치로 읽으면 태양광 평가가 정반대가 된다 |
| 변환 로직은 `src\` 에, CLI 만 `tools\` 에 | 계절 열 매핑이 이 프로젝트에서 가장 조용히 틀릴 수 있는 곳이다. 단위테스트가 닿아야 한다 |
| 검증 실패 시 파일을 **쓰지 않는다** | 검증을 통과 못한 요금표가 저장소에 남으면 다음 세션이 그것으로 계산한다 |
| **갑 종별 기본요금은 계약전력 기준** (`base_fee_basis`) | 을은 요금적용전력, 갑은 계약전력이다. 섞으면 기본요금이 통째로 틀린다. 계약전력을 모르면 만들어내지 않고 멈춘다 |
| 전체시간 종별은 세 시간대에 같은 단가 + `time_of_use=False` | 스키마를 갈라 두면 엔진이 두 갈래가 된다. 검증만 갈라서 `경<중간<최대` 대신 '세 칸이 같은지' 를 본다 |
| 전체시간 종별에 균일성 검사(규칙 2) 미적용 | 계절별로 할인폭을 다르게 설계한다. 요금표가 약속하지 않은 것을 검사하는 셈이다 |
| **선택요금 전환은 같은 계약종별 안에서만** | 종별은 용도로 정해지는 계약이지 고를 수 있는 요금제가 아니다. 종별이 하나일 때는 안 보이던 결함이다 |

### 프로젝트 범위

| 결정 | 이유 |
|---|---|
| 실측 부하 기반만 구현, 신축 부하 예측 제외 | 가치 대비 노력이 유리, 결과 신뢰도 높음 |
| 감도 3종 채택, P90 미도입 | 부하가 1년 실측 고정이라 확률 표기가 통계적으로 부정확 |
| PoC 요금 범위는 일반용(을) 고압A·B | 종별 확대 시 오타 검출이 어려움 |
| 기존 코드는 `reference\` 에 배치 | `src\` 에 넣으면 완성된 코드로 오인 |
| 검증·케이스 스터디를 UI보다 먼저 (7 → 8세션) | 계산이 틀린 채로 UI를 만들면 UI까지 재작업 |
| `input\` 은 `.gitignore` 에서 제외하지 않음 | 케이스 데이터를 이력으로 남겨 검증 재현 |

### 1세션 — 데이터 로더

| 결정 | 이유 |
|---|---|
| 그리드 이탈 행은 kW 산정에서 제외, kWh 합계에는 포함 | 15분 그리드를 벗어난 행은 평균 환산 시 수요가 왜곡됨 (43.2 kWh ÷ 14분 = 185 kW 인데 ×4 하면 172.8 kW). 다만 실사용 전력량이므로 요금 계산에는 필요 |
| 인코딩은 컬럼 매칭 성공까지 확인해야 채택 | 인코딩이 어긋나도 예외 없이 깨진 헤더가 나오는 경우가 있음 |
| 그리드 위상은 최빈값으로 판정 | 첫 행이 비정규 시각이어도 전체가 어긋나지 않게 |
| 총 사용량은 `meta.total_kwh` 를 쓴다 | `kwh` 시리즈 합계는 그리드 정렬분만이다. 이탈분을 더한 값이 요금 계산의 기준 |
| 라벨은 구간 끝 그대로 인덱스에 둔다 | 한 구간 빼는 귀속 판정은 tariff 의 책임. 로더가 미리 옮기면 이중 보정이 난다 |

### 2세션 — 품질 검사

| 결정 | 이유 |
|---|---|
| 정전 판정은 흔적 3종 중 **연속 결측을 뺀 나머지 2개 이상** 요구 | 연속 결측은 모든 결측이 갖는 성질이라 정전의 근거가 못 됨. 이 조건 덕분에 11월 통신 장애(930슬롯)가 정전으로 오인되지 않음 |
| 편중 판정에서 정전 슬롯을 분자·분모 양쪽에서 제외 | 한쪽만 빼면 비율이 왜곡됨. 제외값이 정본, 포함값은 비교용으로 병기 |
| 최장 연속 결측 구간을 편중 배수와 **별도로** 산출 | 며칠짜리 연속 공백은 시간대가 균등하게 빠져 배수가 1 근처가 되지만, 그 달의 최대수요 판정 자체를 무의미하게 만듦 |
| 정전 흔적 2번은 "부분 적산 + 복전 재등록"을 한 종으로 묶음 | 둘 다 그리드 이탈 행이라는 같은 흔적이다. 따로 세면 흔적이 4종이 되어 "3종 중 2개" 규칙이 헐거워짐 |
| 피크 시간대 창은 라벨 `10:15~16:00` | 라벨이 구간 끝이라 `10:00` 슬롯은 09시대 사용량이다. `slot_start()` 로 되돌려 판정 |
| `UsageData.kwh` → `kwh_grid` 로 개명하고 `.sum()` 을 막음 | 이탈분 43.2 kWh 는 전체의 0.0002% 라 눈에 띄지 않는다. 요금 엔진에서 틀리면 발견이 어려움 |

### 6세션 — 요금적용전력 규칙 정정 (요구사항서 5.2 개정)

| 결정 | 이유 |
|---|---|
| **요금적용전력 규칙을 `tariff/demand.py` 로 분리** | 세 조건(대상 시간대·대상 월·하한)이 겹치는데 요금 엔진 안에 섞어 두면 어느 규칙이 걸렸는지 추적이 안 된다 |
| `is_demand_month()` 를 `season_of()` 와 **따로 둔다** | 9월은 전력량요금상 봄·가을철이지만 요금적용전력 대상월이고, 6·11월은 반대다. 같은 함수로 판정하면 두 규칙이 조용히 섞인다 |
| 월별 행에 `demand_basis_kw`·`demand_before_floor_kw` 를 함께 남긴다 | 관측 최대와 대상 최대가 다르고, 하한 적용 전 값이 있어야 계약전력 조정을 재계산할 수 있다 |
| 하한 비율은 종별 속성. 요금 데이터에 `null` 이면 미적용 | 종별마다 다르고(30%/15%), 확인되지 않은 종별에 30%를 들이대면 없는 절감을 만들어낸다 |
| 계약전력 초과는 요금적용전력과 **별개로** 검사 | 경부하 초과는 요금적용전력에 영향이 없지만 초과사용부가금 대상이다 |
| **①경부하 제외와 ②대상월 한정은 PV 에 반대 방향으로 작용한다** | ①은 PV 가 손댈 수 없는 야간·공휴일 피크를 요금적용전력에서 빼 주므로 PV 에 유리하고, ②는 봄·가을 피크 저감의 가치를 없애므로 불리하다. **어느 쪽이 지배하는지는 부하 형태에 달렸다.** 샘플(여름 주간 피크형)에서는 ①이 압도해 PV 기본요금 절감이 49% 늘었다. 봄·가을 피크형이라면 반대가 된다. 케이스 스터디(7세션)에서 두 유형을 모두 넣어 확인할 것 |
| 조합 확실성은 가장 낮은 구성 요소를 따른다 | 태양광과 ESS 를 함께 넣으면 조합 전체가 ESS 등급이다. 표에서 등급을 섞어 보여주면 의사결정이 흐려진다 |
| **태양광 등급 판정 모집단은 요금적용전력 대상 슬롯이다** | 경부하 구간은 아무리 큰 수요가 나도 요금적용전력이 되지 않는다. 그 시각 피크를 판정에 넣으면 태양광의 기본요금 기여를 잘못 읽는다 (5.2 ①) |
| 상위 구간 분포를 **두 벌** 낸다 (`hour_counts` / `demand_hour_counts`) | 부록 B 의 시각 분포는 전 슬롯 기준 원값이고 청구서 관행이다. 판정용 값으로 덮어쓰면 부록 B 대조가 불가능해지고, 한 필드에 섞으면 어느 기준인지 알 수 없게 된다 |

### 5세션 — 개선 수단 평가

| 결정 | 이유 |
|---|---|
| **수단 적용 후 부하는 새 `UsageData` 로 만들어 요금 엔진에 다시 넣는다** | 4세션이 남긴 미결 사항. 요금 엔진을 건드리지 않아도 되고, 순부하에 품질 검사·진단을 그대로 돌릴 수 있다. `with_load()`·`apply_generation()` |
| 수단을 적용해도 그리드 이탈(부분 적산) 행은 건드리지 않는다 | 그 구간의 발전량을 배분할 근거가 없다. 43.2 kWh(전체의 0.0002%)라 보수적으로 남기는 편이 낫다 |
| 결측 슬롯은 결측인 채로 둔다 | 그 시각의 부하를 모르므로 자가소비 판정이 불가능하다 |
| 용량 곡선은 시뮬레이션 1회 + 곱셈 | pvwatts 직류와 인버터 모델이 정격에 선형이다(정격과 인버터 용량이 함께 커진다). 20단계라도 태양 위치 계산은 한 번. 선형성을 테스트로 고정했다 |
| **ESS 용량 산정 기준은 '하루 최대 초과 에너지'** | 충전 기회가 야간뿐이다. 연속 구간 최대로 잡으면 같은 날 초과가 두 번일 때 목표를 놓친다(샘플 5,200 kW 에서 실제로 놓쳤다). 부록 B 의 '총 초과 에너지'는 기간 합계라 배터리 용량으로는 과대(4,500 kW 목표에서 178 MWh) |
| 상계거래 금액은 요금표 단가로 산출, 외부 구매는 단가 없으면 미산출 | 상계는 그 슬롯의 계절·시간대 단가로 상계되므로 데이터에서 나온다. 외부 단가는 지어낼 근거가 없다 |
| `contract_floor_ratio` 는 기본값 없는 필수 키워드 인자 | 기본값을 두면 누군가는 그 가정으로 만든 금액을 근거로 쓴다 |

### 4세션 — 태양광과 진단

| 결정 | 이유 |
|---|---|
| **시각 규약을 둘로 나눠 명시** — 요금 귀속은 구간 시작(`slot_start`), 피크 발생 시각 보고는 검침 라벨 | 부록 B 의 상위 100구간 시각 분포(10시 15개…)와 최대수요 발생 시각(09:30)은 라벨 기준이다. 청구서도 그렇게 적는다. 요금 귀속만 구간 시작으로 판정하면 되고, 섞이지 않게 `top_slots` 에 두 값을 모두 담았다 |
| 부하 라벨 L 에 붙일 발전량은 구간 중앙 `L − Δ/2` 의 순시 출력 | 부하는 구간 평균, 시뮬은 순시값이다. 반 칸을 빠뜨리면 7.5분, 라벨을 그대로 쓰면 15분 밀린다 |
| 일몰 후 발전량을 태양 고도로 잘라냄 | 시간별 → 15분 선형 보간이 일몰을 가로질러 값을 흘린다. 자르지 않으면 일출·일몰 대조가 성립하지 않는다 |
| Open-Meteo 대표 순시각 = 라벨 + 30분 | 라벨이 구간 시작이라는 전제. `WeatherLabel` 로 바꿔 끼울 수 있게 두고 미해결 항목에 남겼다 |
| GCR 만 받고 pitch=1, height=`height_to_pitch` 로 넘김 | `infinite_sheds` 는 높이/열간격 비율에만 의존한다. 사용자에게 GCR 하나만 묻기 위함 |
| PowerShell 폴백 유지 | 3세션까지 미결이던 항목. 프록시 뒤에서 Python HTTPS 만 막히는 사례가 실제로 있고 Windows 전용 경로라 비용이 없다 |
| 계약전력 조정 절감액은 **하한 규정을 모르면 산출하지 않음** | 일반용(을) 기본요금은 요금적용전력 기반이라 계약전력을 낮춰도 하한에 걸리지 않으면 요금이 그대로다. 모르는 채로 금액을 내면 없는 절감을 만들어내는 것이 된다 |
| 진단은 계약 정보가 없어도 동작 | "파일만 올려도 결과가 나온다". 부하 패턴·피크 특성·태양광 등급까지는 계약 정보 없이 나온다 |

### 3세션 — 요금 엔진

| 결정 | 이유 |
|---|---|
| **검침일 라벨은 구간 끝이다** | 하루가 `00:15` 로 시작해 `24:00` 으로 끝나 96행이 됨. 구간 시작 라벨이면 `24:00` 이 나올 수 없음. 계절·시간대·월 귀속은 라벨에서 한 구간을 뺀 시각으로 판정. `15:00` 슬롯은 중간부하이고 첫 최대부하 슬롯은 `15:15`. **4세션에서 PV 시계열을 붙일 때 같은 규약을 써야 15분 어긋나지 않는다** |
| 일요일도 공휴일로 계량 | `holidays` 라이브러리가 일요일을 담지 않음. 관공서 공휴일 규정에 일요일이 포함됨. 누락 시 연 52일이 최대부하로 잘못 계량됨. 토글로 끌 수 있게 함 |
| 선택Ⅲ 단가는 균일성 규칙 미적용 | 요금표 입력값은 정확함. 선택Ⅲ 는 할인이 최대부하에 집중된 설계라 균일할 이유가 없음. 고압B Ⅱ→Ⅲ 는 허용오차 ±0.15 로 통과(1.6~1.7원은 반올림 폭). 고압A Ⅱ→Ⅲ 는 "최대부하 할인폭이 가장 크다"로 대체 |
| 단가를 임의로 고치지 않음 | 요금표 조작이 됨. 검증 실패는 경고로 남기고 규칙 쪽을 검토 |
| 검증 **규칙의 적용 범위·허용오차**는 코드(`validate.DEFAULT_POLICY`)에 둠 | 요금 단가가 아니라 검증 정책이다. 균일성 검사는 데이터에서 뽑은 평균과의 편차로 하고, 5.5·3.8 같은 절대값은 테스트가 못 박는다. 요금표를 갱신해도 검증기가 먼저 깨지지 않는다 |
| 부분 월 기본요금 기본 정책은 `merge` | 샘플에서 정확히 12.00개월분이 된다. 짝이 없으면 일수 비례로 물러서고 그 사실을 `notes` 에 적는다 |
| `energy_kwh()` 의 kW 환산 경로를 막음 | 이탈분이 얹힌 구간은 위상이 달라 수요가 왜곡된다. 막는 것은 ×(60/간격)과 ÷구간시간 둘뿐이라 단가 곱셈은 그대로 된다 |

---

## 확정된 샘플 데이터 수치 (회귀 테스트 기대값)

`input\사용량조회_20240429.csv` — 요구사항서 부록 B 와 동일. 어긋나면 부록 B 를 따른다.

| 항목 | 값 |
|---|---|
| 기간 | 2023-04-25 00:15 ~ 2024-04-27 00:00 |
| 원본 행 수 | 34,358 (그리드 이탈 2건 포함) |
| 결측 | 972 (2.8%) |
| `24:00` 표기 | 357건 |
| 연간 사용량 | 22,285 MWh |
| 최대수요 | 5,293.4 kW @ 2023-07-03(월) 09:30 |
| 부하율 | 49.0% |
| 결측 구간 1 | 2023-11-03 17:30 ~ 11-13 09:45 (930슬롯, 9.69일, 통신 장애 추정) |
| 결측 구간 2 | 2024-04-06 19:30 ~ 04-07 05:45 (42슬롯, 정전) |
| 11월 결측률 | 32.3% → 신뢰 제한 표시 |
| 최적 선택요금 | 선택Ⅱ (현행 선택Ⅰ 대비 절감) |

**기간이 13개 월 버킷이며 앞뒤 두 개가 부분 월이다.** 기본요금을 13개월분 부과하지 말고,
요구사항서 5.3.2 절에 따라 처리한 뒤 결과를 "연간" 대신 실제 기간 또는 12개월 환산으로 표기한다.

---

## 세션 기록

<!-- 아래 형식으로 최신 세션을 위에 추가한다 -->

### 17세션 — 그래프 표시 개선 (2026-08-12)

**계산은 손대지 않았다.** 케이스 스터디 66/66 · 요금적용전력 5,293.44 kW 불변.
자세한 경위는 위 「오늘 한 일」에 있다.

---

#### 1. 세 그래프가 같은 병을 앓았다

**차이가 큰 두 값을 한 축에 그리면 변화가 보이지 않는다.** 35억 위의 5천만원,
5,000 kW 부하 위의 수백 kW 발전, 5,000 kW 축에 세운 100 kW 막대가 모두 그렇다.
규약 셋(축을 자른다 · 차트를 나눈다 · 사이를 채운다)을 `ui.charts` 한 곳에 모아
다음에 차트를 더할 때 같은 판단을 다시 하지 않게 했다.

#### 2. 「상세 미산출」 은 최적화의 흔적이었다

선택Ⅲ 는 합계가 멀쩡히 있었다. `evaluate_tariff_switch` 가 현행·최적 둘만 상세
청구서를 만들었을 뿐인데, 화면이 그것을 「산출하지 못했다」 로 적었다. 갈아탈 수
있는 조합은 많아야 셋이라 전부 채워도 요금 재계산 한 벌이 는다 (35 → 36회).

**아끼는 최적화가 화면 문구를 통해 거짓말이 되는 자리**가 또 있는지 봐야 한다.

#### 3. 라벨 하나가 계산 규약 밖에 있었다

태양광은 「계산」 단추를 눌러야 도는 카드인데, 방위 라벨의 상대 발전량만 살아
있는 위젯 값으로 매기고 있었다. 옆단에서 시군구를 고르는 것만으로 여덟 방위
시뮬레이션(0.85초)이 돌았다. **비싼 계산에 단추를 달았으면 그 계산을 쓰는 표시도
같은 단추 뒤에 있어야 한다.**

#### 4. 옮길 때는 겹치는 줄을 걸러야 한다

ESS 본문 넉 줄을 확인사항으로 내렸더니 이미 있던 경고·메모(스물둘)와 같은 말이
두 번 적혔다. 라벨을 떼고 알맹이로 견주어 걸러 낸다.

---

### 16세션 — UI 구조 개편과 버그 수정 (2026-08-12)

**계산은 손대지 않았다.** 케이스 스터디 66/66 · 요금적용전력 5,293.44 kW 불변.
자세한 경위는 위 「오늘 한 일」에 있다.

---

#### 1. 뿌리 하나가 버그 셋을 만들었다

Streamlit 은 **이번 실행에서 그리지 않은 위젯의 상태를 버린다.** 화면 하나만
그리는 구조에서 3단계에 서 있으면 2단계 위젯이 통째로 사라졌다.

    내려받기 rerun → 켠 수단 0개 → 화면 결과도 파일 속 수단별 결과도 빈다
    3단계로 바로 뛰면 → 2단계를 거치지 않아 값이 없다

**탭으로 셋을 함께 그려 구조에서 없앴다.** 배선으로 막으면 다음에 화면을 하나
더 붙일 때 같은 자리에서 다시 난다.

#### 2. 시험이 못 잡은 이유

AppTest 가 세션에 값을 **직접** 넣으면 위젯이 아니라 일반 키라 버려지지 않는다.
15세션 통합 시험 45건이 전부 그 방식이었다. 이번에는 **토글을 실제로 눌러** 켜고
내려받기까지 누르는 경로로 다시 썼다.

#### 3. 평평한 곡선에서 최소점을 고르면 안 된다

kWp 당 단가면 회수기간이 용량과 거의 무관해진다 (8 kWp 8.060년 · 160 kWp
8.114년). 최소점을 그대로 고르니 **상한의 1/20** 이 최적으로 나왔다. 0.7% 차이는
예측 오차 안이라 뜻이 없다 — 최소 회수기간의 5% 안에서 절감액이 큰 쪽을 고른다.

#### 4. 세 화면을 함께 그리니 중복이 드러났다

같은 경고가 1단계와 2단계에, 2단계 카드 경고가 3단계에 다시 실려 있었다.
페이지가 갈려 있을 때는 보이지 않던 것이다. **동일 지문 전수 검사**를 시험으로
남겼다.

---

### 11세션 — 기술서·매뉴얼 (2026-08-11)

**구현은 손대지 않았다.** 자세한 경위는 위 「오늘 한 일」에 있다.

---

#### 1. 만든 것

    src\kwise\docsite.py     Markdown → 단일 HTML. 변환 로직은 src 에 둔다
    tools\build_docs.py     CLI. 정적 사본과 앵커 대조까지 한다
    docs\TECHNICAL.md       7장 + 부록 3 (57절)
    docs\MANUAL.md          8장 + 부록 (65절)
    docs\CAPTURES.md        캡처 아홉 장 목록
    static\                 Streamlit 이 내주는 사본
    .streamlit\config.toml   enableStaticServing = true

#### 2. 기술서에 남긴 것

**결론만 적지 않고 근거를 남겼다.** PROCEED 의 결정 사항이 4장의 원재료였고,
"조용히 틀렸을 것들"로 묶어 둔 항목을 특히 자세히 적었다.

- **모듈 상수 → 함수 조회** (5.1) — import 시점에 고정하면 파일을 고쳐도
  반영되지 않는다. 데이터클래스 필드 기본값도 같다
- **92% 가 두 조문에서 오는 이유** (4.2) — 제41조 기준과 제42조 간주값.
  묶으면 기준 개정 시 모르는 고객의 값까지 따라 움직인다
- **계절·시간대를 요금표에서 읽는 이유** (4.2) — 옮겨 적다 실제로 틀렸다
- **요금적용전력 3규칙이 태양광에 반대로 작용하는 것** (4.2)
- **Period 박싱 제거** (6.4) — 시간만 봤으면 못 찾았다는 교훈까지

#### 3. 매뉴얼에 남긴 것

앵커 32개를 절 id 로 그대로 썼다. 상위 100구간 시각 분포로 태양광 적합성을
판단하는 법, 확실성 등급의 쓰임, **감도를 왜 3열이 아니라 범위로 내는지**를
사용자 말로 풀었다.

부록에서 샘플 한 건을 처음부터 끝까지 따라가며 결론 넷을 냈다.

---

**만든·고친 파일**

- `src\kwise\docsite.py` — 신규. 변환기
- `src\kwise\ui\anchors.py` — 정적 사본 경로, `manual_href` 가 서빙 경로로
- `tools\build_docs.py` — 신규. 생성 + 사본 + 앵커 대조
- `docs\TECHNICAL.md`·`MANUAL.md`·`CAPTURES.md` 와 생성된 html 둘
- `tests\test_docsite.py` — 신규 39건
- 요구사항서 **13장 신설**, README 갱신

**미해결** — 신규 없음. 7건 그대로

**테스트**
- pytest: 866 passed (직전 827 + 이번 39)
- ruff: pass (check + format)
- mypy: pass (strict, 123 files)

---


### 10세션 — 성능 실측 · 진행 표시 · 배포 점검 (2026-08-11)

**측정이 최적화를 찾아냈다.** 자세한 표는 위 「오늘 한 일」에 있다.

---

#### 1. 실측 도구 (`kwise.benchmark`, `tools\run_benchmark.py`)

구간마다 **소요 · RSS 증감 · `calculate_bill` 호출 수** 셋을 함께 잰다.
세 번째가 없었으면 이번 최적화를 못 찾았다 — 시간만으로는 "곡선이 느리다"에서
멈춘다. 호출 수가 "곱해지는 축"을 가리킨다.

호출 수 세기는 `sys.modules` 를 훑어 `calculate_bill` 을 가리키는 이름을 모두
바꿔 끼운다. 여러 모듈이 `from ... import calculate_bill` 로 **각자 이름을 묶어
두었으므로** 원본 하나만 바꿔서는 세어지지 않는다. 끝나면 되돌린다.

**메모리를 재는 데서 한 번 걸렸다.** `ctypes` 로 `GetProcessMemoryInfo` 를 부를 때
`argtypes` 를 지정하지 않아 64비트에서 핸들이 잘렸고, 호출이 **조용히 실패해
0 이 돌아왔다.** 표에 "0 MB" 로 찍혀 있었다 — 못 잰 것을 안 썼다고 읽을 뻔했다.
지금은 못 재면 `None` 이고 표에 `—` 로 나온다.

---

#### 2. 최적화 — 되풀이된 것은 발전량이 아니라 요금이었다

물음은 "PV 발전량을 60회 다시 계산하는가" 였고 답은 **아니오** 였다. 5세션이
정한 "시뮬레이션 1회 + 곱셈"이 지켜지고 있다 (pvlib 0.091초, 곡선 안에서 0회 —
시험이 못 박는다).

프로파일러가 진짜 자리를 찍었다. `calculate_bill` 시간의 **74%가
`PeriodArray._box_func` 180만 회** 였다.

```
for month in months:                      # 13개월
    month_mask = slots["month"].to_numpy() == month   # 3만 5천 개를 매달 상자에
```

`to_numpy()` 를 루프 밖으로 한 번만 끌어올렸다. `monthly_demand_basis` 도 이미
푼 배열을 받도록 열었다.

    calculate_bill  0.63초 → 0.145초 (4.3배)
    파이프라인 전체  24.6초 → 9.4초  (2.6배)
    케이스 스터디    114.2초 → 29.9초 (3.8배)
    pytest 전체      324초 → 98초   (3.3배)

**회귀값은 한 자리도 바뀌지 않았고** 케이스 스터디 66/66 그대로다. 시험이
부록 B 의 세 수치를 못 박는다.

곁들여 `demand.py` 의 중복 대입 한 줄(`span = ...` 두 번)을 지웠다. 이전부터
있던 죽은 코드다.

---

#### 3. 진행 표시 (요구사항서 10.6 신설)

    kwise.progress      프로토콜 · 가중치 · 상태 (streamlit 없음)
      NullProgress        기본값. 콜백 없이 부르는 것이 정상 경로다
      CallbackProgress    가중치로 전체 진행률을 계산한다. 화면·CLI 가 공유
      RichProgress        CLI. **rich 를 함수 안에서 늦게 부른다**
      StageRunner         stage/done 짝을 대신 그어 준다
    kwise.ui.progress   st.status + st.progress 어댑터

계산 함수는 `progress` 를 **선택 인자**로 받고 `step()` 만 낸다. 단계 경계는
부르는 쪽이 긋는다 — 한 함수가 여러 단계에 쓰일 수 있기 때문이다.

**기상 건너뜀은 부르는 쪽이 판정한다.** `load_weather` 에 콜백을 넣지 않았다.
반환값의 `source` 를 보고 오케스트레이터가 `skipped("weather", "캐시 적중")` 을
낸다 — 판정이 필요한 곳과 아는 곳이 같아진다.

**설계를 바꾼 것 하나.** 진행을 보여 주는 계산은 `st.cache_data` 로 감쌀 수 없다
(`CacheReplayClosureError`). 태양광 곡선·조합 비교·감도 셋을 `kwise.ui.memo` 의
세션 기억으로 옮겼다. 캐시 비움은 이제 **두 갈래를 함께** 비운다.

---

#### 4. Streamlit Cloud 점검 — 배포 가능

넷 다 확인했고 막는 것이 없다 (표는 위 「오늘 한 일」).

**세션 격리에서 한 가지를 굳혔다.** `st.cache_data` 는 프로세스 전역이라 접속자가
캐시를 공유한다. 키가 요약 정보(파일명·기간·총량)뿐이면 다른 파일이 같은 키를
가질 여지가 남는다 — 업로드 바이트의 **내용 해시**를 키에 물렸다.

임시 파일은 8세션 설계 그대로 안전하다. 업로드 바이트는 메모리에만 있고,
산출물은 세션별 uuid 폴더에 만들었다가 **바이트로 읽은 즉시 지운다.** 휘발성
파일시스템과 어긋나지 않는다.

**`data/weather/` 는 그대로 둔다.** 판단 근거만 만들라는 지시였고, 실제로 뺄 만큼
무겁지 않다 (얕은 클론 23 MB 중 20 MB 지만 pip 설치가 수백 MB 다).

---

**만든·고친 파일**

- `src\kwise\progress.py` — 신규. 프로토콜·가중치·CLI 보고자
- `src\kwise\benchmark.py` — 신규. 구간 측정·RSS·요금 호출 수
- `src\kwise\ui\progress.py` — 신규. Streamlit 어댑터
- `src\kwise\ui\memo.py` — 신규. 세션 기억 (캐시 재생 회피)
- `tools\run_benchmark.py` — 신규. 실측 CLI
- `src\kwise\tariff\engine.py` — **월 라벨 한 번만 풀기** (성능 4.3배)
- `src\kwise\tariff\demand.py` — 푼 배열 받기, 죽은 줄 제거
- `src\kwise\measures\solar.py`·`compare\combination.py`·`compare\sensitivity.py` — `progress`
- `src\kwise\report\casestudy.py` — 8단계 진행 (CLI 경로)
- `src\kwise\ui\cache.py` — 내용 해시 키, 세션 기억, 두 갈래 캐시 비움
- `tools\run_casestudy.py` — `--progress` (rich)
- `data\assumptions.json` 25항목 (+ `progress.*` 셋)
- `tests\test_progress.py` — 신규 35건
- 요구사항서 **10.6·11.4 신설**

**미해결** — 신규 없음. 7건 그대로

**테스트**
- pytest: 827 passed (직전 792 + 이번 35)
- ruff: pass (check + format)
- mypy: pass (strict, 120 files)
- 케이스 스터디: **66/66 통과** (29.9초)

---


### 9세션 — Word 보고서와 잔여 정리 (2026-08-11)

**구현의 마지막이다.** 케이스 스터디를 시작·종료 두 번 돌려 **66/66** 을 확인했다.

---

#### 0. 잔여 ① — 모듈 상수 둘을 기준 데이터로

`diagnose\contract.py` 의 `DEFAULT_POWER_FACTOR_PCT`·`DEFAULT_MARGIN_RATIO` 다.
8세션 준비에서 스무남은 개를 옮길 때 빠진 둘이며, 값이 파일과 같아 **숫자는
하나도 바뀌지 않았다.**

**옮기다가 의미가 갈라지는 것을 발견했다.** 92% 가 두 조문에서 온다.

| 조문 | 뜻 | 항목 |
|---|---|---|
| 제41조·제43조 ② | 역률요금의 **기준** — 넘으면 감액, 못 미치면 추가 | `power_factor.lagging_standard_pct` |
| 제42조 | 무효전력계 미설치 고객의 **간주값** — 우리가 쓰는 기본값 | `power_factor.deemed_lagging_pct` (신규) |

한 항목으로 묶어 두면 **기준이 90 으로 개정됐을 때 역률을 모르는 고객의 값까지
따라 내려가** 조정액이 0 이 아니게 된다. 실측하지 않은 값으로 금액이 생기는
것이고, 이 프로젝트가 가장 경계하는 사고다. 둘로 갈라
`power_factor_charge(lagging_pct=None)` 과 화면 기본값이 **간주값** 쪽을 읽게 했다.

`DEFAULT_MARGIN_RATIO` 는 판단값이라 `contract.margin_ratio` 로 갔다.
**데이터클래스 필드 기본값은 `default_factory` 로 바꿨다** — 필드 기본값은
import 시점에 고정되므로, 그대로 두면 화면에서 파일을 고쳐도 그 프로세스에서는
옛 값으로 객체가 만들어진다. 시험이 "파일을 고치면 다음 객체가 새 값을 쓴다" 를
지킨다.

---

#### 1. 잔여 ② — 갑 종별 기본요금 기준이 잠정임을 밖으로 낸다

약관 제38조 원문을 아직 확보하지 못했다. 값을 바꿀 수 없는 동안에도 **쓰는
사람은 알아야 한다.**

    TENTATIVE_BASE_FEE_BASIS_WARNING
      → 갑 종별 계산에서 BillingResult.warnings 에 실린다
      → 진단 경고 → Excel·Word 산출물
      → 화면 1단계에 **접히지 않는 경고**로

**PoC 범위(일반용(을))가 이 경로를 타지 않는다는 점이 이 표시를 넣은 이유다.**
샘플에서는 아무 일도 일어나지 않으므로 회귀로는 잡히지 않고, 갑 종별을 실제로
쓰는 첫 사람이 조용히 틀린 금액을 받는다. 시험 둘이 "갑에는 붙고 을에는 붙지
않음" 을 지킨다.

---

#### 2. Word 보고서 (요구사항서 10.5 신설)

Excel 아홉 시트는 **분석자용 데이터**다. 의사결정자에게 그대로 내밀 수 없다.

    표지   건물명 · 분석 기간 · 작성일 · 적용 요금표 시행일 · 계약종별
    1장 요약    이 장만 읽어도 판단이 되어야 한다
    2장 진단    품질 · 부하 패턴[차트] · 피크 특성[차트 2] · 요금 구조 · 계약전력
    3장 수단    7.1~7.7 순서, 켠 것만. 결론→절감액→투자비→회수기간→확실성→주의
    4장 조합    비교표 · 권장안 · [차트] · 감도(범위)
    5장 범위    검토함/미검토 · 미포함 요금요소 · 계약전력 경고 · 한계 · 추적성

| 원칙 | 이유 |
|---|---|
| **각 절은 결론부터** | 데이터를 앞에 놓으면 읽는 사람이 결론을 스스로 만들어야 한다 |
| **표는 Word 표 객체** | 이미지로 넣으면 자사 제안서에 복사해 쓸 수 없다. 그림은 차트뿐이다 |
| 감도는 **범위 한 줄** | 3열 나열은 "어느 쪽이 좋은 값인가" 를 찾게 만든다 (9.2) |
| 미산출은 **사유** | 빈칸도 0원도 아니다. 0원은 '공짜' 로 읽힌다 (7.5) |
| 수단 0개면 **3·4장을 빼고 번호를 당긴다** | 제목만 남은 빈 장은 "검토했는데 결과가 없다" 로 읽힌다 |

**한글 폰트를 두 군데에 지정했다.** Word 본문은 동아시아 글꼴(`w:eastAsia`)을
따로 주어야 하고, matplotlib 은 지정하지 않으면 축 이름이 통째로 두부(□)가
된다. **둘 다 화면에서는 안 쓰이는 경로라 조용히 깨진 채 산출물로 나간다.**
`axes.unicode_minus = False` 도 함께 걸었다 — 한글 폰트에 유니코드 마이너스가
없어 음수 축이 깨진다.

---

#### 3. 목록을 두 벌 두지 않았다

화면과 보고서가 각자 7.1~7.7 을 들고 있으면 한쪽만 고쳤을 때 어긋나고, **그
어긋남은 두 산출물을 나란히 놓고서야 드러난다.**

    kwise.measures.catalog   MEASURE_CATALOG — 키·번호·라벨·투자 구분
      ui.spec.MeasureSpec      여기에 앵커·한 줄 설명만 더한다
      report.document          그대로 쓴다

차트 프레임도 같은 이유로 `report.frames` 로 모았다. altair(화면)와
matplotlib(보고서)이 같은 이름의 차트에서 다른 수를 그리는 일을 막는다.
시험이 두 목록의 동일성을 지킨다.

---

**만든·고친 파일**

- `src\kwise\report\document.py` — 신규. Word 보고서
- `src\kwise\report\figures.py` — 신규. matplotlib png 4종
- `src\kwise\report\frames.py` — 신규. 화면·보고서 공용 차트 프레임
- `src\kwise\measures\catalog.py` — 신규. 공용 수단 목록
- `src\kwise\diagnose\contract.py` — 상수 둘 → `deemed_power_factor_pct()`·`default_margin_ratio()`
- `src\kwise\tariff\power_factor.py` — `deemed_lagging_pct()` 분리
- `src\kwise\tariff\engine.py` — 갑 종별 잠정 경고
- `src\kwise\ui\charts.py` — 프레임을 `report.frames` 에서 읽는다
- `src\kwise\ui\spec.py` — 공용 목록 위에 다시 세웠다
- `src\kwise\ui\views\compare.py` — 산출물 탭 둘 (Excel · Word)
- `src\kwise\ui\views\diagnose.py` — 갑 종별 잠정 경고 블록
- `data\rules_kr.json` 27항목 · `data\assumptions.json` 22항목 (+ `defaults\`)
- `tests\test_document.py` 신규 30건, `test_rules.py` +6, `test_tariff_engine.py` +2
- 요구사항서 **10.5 신설**, README 실행 안내
- `pyproject.toml` — `python-docx`, `matplotlib` 추가

**미해결** — 신규 없음. **코드 상수 1건을 지웠다** (8 → 7건)

**다음 세션(10세션 — 기술서·매뉴얼)이 알아야 할 것** — 위 「다음 세션」 참조

**테스트**
- pytest: 792 passed (직전 754 + 이번 38)
- ruff: pass (check + format)
- mypy: pass (strict, 114 files)
- 케이스 스터디: **66/66 통과** (시작·종료 두 번 확인)

---


### 8세션 — Streamlit UI (2026-08-11)

**마지막 구현 세션이다.** 계산에는 손대지 않았다. 시작할 때 케이스 스터디를
다시 돌려 **66/66 통과**를 확인했고, 미해결 7건이 모두 외부 자료(청구서·공개
단가·약관 원문·실측)를 기다리는 것이라 UI 를 막지 않음을 확인한 뒤 시작했다.
끝내고 다시 돌려 **66/66 이 그대로**임을 확인했다.

---

#### 0. 모듈 갈래 — Streamlit 의존 여부로 가른다

    순수      anchors · text · spec · charts · rules_view · pipeline · session
    Streamlit  app · state · cache · views\

**판단이 들어가는 것은 전부 순수 쪽에 두었다.** 화면 순서, 정렬, 표기, 조합
구성, 임시 파일 정리가 그것이다. 그래야 시험이 닿는다 —
`tests\test_ui.py` 77건이 Streamlit 을 띄우지 않고 돈다. 뷰는 그 결과를 그리기만
한다. 테스트 하나가 **순수 모듈에 `import streamlit` 이 들어오는 것을 막는다.**

화면 자체는 `streamlit.testing.v1.AppTest` 로 네 화면을 모두 띄워 예외 0 을
확인했다 (업로드·계약 확정·태양광·조합·감도·Excel 내려받기까지).

---

#### 1. 배선에서 조용히 틀린 것 셋 — **전부 화면을 붙였기에 드러났다**

**① 계약전력이 요금 엔진까지 가지 않았다.** `ContractInfo(contract_kw=...)` 만
채우고 `BillingOptions` 를 비우면 `calculate_bill` 이 계약전력을 모른 채 돌아
**요금적용전력 하한(계약전력의 30%)이 적용되지 않는다** (5.2 ③). 금액은
그럴듯하게 나오고 **저부하 사업장에서만** 과소 산출된다. 케이스 스터디는
`ContractInfo` 만 쓰는 경로라 드러나지 않았다.

`ContractForm.billing_options()` 가 두 곳을 함께 채우게 하고, "계약전력을 넣으면
하한 미적용 경고가 사라진다 / 비우면 남는다" 를 회귀 시험 두 건으로 못 박았다.

**② 수단을 하나도 켜지 않으면 산출물 생성이 멈췄다.**
`measure_summary_frame()` 이 빈 목록에 `set_index("수단")` 을 걸어 `KeyError` 다.
**진단만 보고 Excel 을 받는 것이 정상 경로**인데 거기서 죽는다. 열 구조를
유지한 빈 표를 내도록 고쳤다 (`MEASURE_SHEET_COLUMNS`).

**③ 2단계 입력이 3단계에 닿지 않았다.** ESS 목표 피크와 단가를 위젯 키 없이
받아 그 화면에만 남았고, 조합 비교는 목표를 모른 채 **ESS 를 빼고** 계산했다.
빠졌다는 표시도 없이 그냥 없는 것이 된다. `state.input_key()` 로 세션에 남기고
3단계가 읽는다.

---

#### 2. 화면과 매뉴얼의 역할 분담 — **앵커 32개 확정**

판단 기준은 하나다.

    화면에 둘 것     없으면 입력을 못 하거나 결과를 오독하는 것
    매뉴얼로 보낼 것  출처·근거 조문·제도 설명·원리·배경

형태는 화면에 한 줄 + [자세히] 링크다. **매뉴얼이 없으므로 링크를 비활성으로
두었다** — 없는 문서로 보내면 화면을 못 믿게 된다.

앵커 이름의 정본은 `src\kwise\ui\anchors.py` 이고 `docs\MANUAL_ANCHORS.md` 는
생성물이다. **두 벌이 어긋나면 테스트가 깨진다.** 뷰에서 부르는 앵커 키가 모두
등록되어 있는지도 소스를 훑어 확인한다 — 죽은 링크를 화면에 내지 않는다.

**예외 — 경고 넷은 화면에 남겼다.** 역률 92% 미달, 고출력 셀 사양(방전시간
0.5h 미만), 결측 편중, 계약전력 변경 위험. 결과 해석을 바꾸는 것을 매뉴얼로
보내면 안 읽는다.

---

#### 3. 캐시 비움 — **두 겹으로 걸었다**

지난 세션의 `reload_rules()` 는 rules 쪽 lru_cache 만 비운다. 그것만 믿으면
파일은 바뀌었는데 화면은 옛 값이 된다.

| 겹 | 무엇 | 없으면 |
|---|---|---|
| ① | `apply_rule_edit()` 이 편집 성공 시 `st.cache_data.clear()` | 화면 편집이 반영되지 않는다 |
| ② | `rules_stamp()` (47항목 지문)을 모든 캐시 키에 물린다 | 엑셀 왕복·손편집이 반영되지 않는다 |

①만 두면 다른 경로로 바뀐 것을 놓치고, ②만 두면 지문을 빠뜨린 캐시가 생겼을 때
조용히 샌다. **편집이 실패하면 캐시를 건드리지 않는다** — 실패는 저장되지
않았다는 뜻이다. 이것도 시험이 지킨다.

---

#### 4. 기준 데이터 화면 — 근거를 값 옆에 둔다

한 줄에 **구분 배지 · 라벨 · 값(편집) · 출고값(다를 때만) · 근거와 시행일 ·
확인일과 경과 개월 · [확인함] · 비고 · 원문 확인처**를 함께 둔다. 접지 않는다.

정렬은 **만료 경고 → 출고값과 다른 항목 → 분류** 순이다. 손댈 것이 위로 온다.
상단에 "전체 47개 · 변경됨 ○개 · 확인 필요 ○개".

**원문 확인처를 만료 경고가 뜬 항목에만 달지 않았다.** 경고가 없어도 근거를
따라갈 데가 있어야 하므로 `expiry.source_link_of()` 를 공개해 전 항목에 붙였다.

**출고 복원은 `confirmed=False` 로 먼저 불러 차이 목록을 보여 주고 확인받는다.**
`RuleSet.recovered_from` 이 비어 있지 않으면 모든 화면 위에 띄운다.

값 편집은 **형을 지켜서 되돌려 준다** — 정수 항목(대상월·판정 창)을 실수로
저장하면 산출물에 `12.0` 으로 찍힌다. 목록·사전은 화면에서 고치지 않고 엑셀
왕복으로 보낸다.

**기상 현황에는 만료 개념을 넣지 않았다.** 부분 취득이 정상 상태이므로 없는
격자를 오류로 표시하면 멀쩡한 상태를 고장으로 읽게 된다.

---

#### 5. 업로드 데이터를 남기지 않는다

업로드 바이트는 **메모리에만** 둔다. 디스크로 내려가는 것은 내려받기용 Excel
한 벌뿐이고 **바이트로 읽은 즉시 지운다.** Streamlit 은 "세션이 끝났다" 를
알려 주지 않으므로, 앱을 켤 때 묵은 세션 폴더(6시간)를 쓸어내는 그물을 함께
두었다. 지운 뒤 폴더가 비어 있는지 시험이 확인한다.

---

**만든·고친 파일**

- `src\kwise\ui\` — 화면. 순수(`anchors`·`text`·`spec`·`charts`·`rules_view`·
  `pipeline`·`session`) / Streamlit(`app`·`state`·`cache`·`views\`)
- `src\kwise\io\columns.py` — `override_columns()` 신규. `strategy_label` 분리
- `src\kwise\io\usage.py` — `load_usage(_bytes)` 에 `date_column`·`energy_column`
- `src\kwise\rules\expiry.py` — `source_link_of()` 공개
- `src\kwise\report\excel.py` — 빈 「수단별 결과」 시트 처리 (`MEASURE_SHEET_COLUMNS`)
- `tools\export_manual_anchors.py`, `docs\MANUAL_ANCHORS.md` — 앵커 32개
- `tests\test_ui.py` — 신규 77건
- `README.txt` — 실행하는 법

**미해결** — 신규 1건 (`diagnose\contract.py` 의 모듈 상수 둘). 값이 같아
숫자는 바뀌지 않으므로 9세션에 옮긴다

**다음 세션(9세션 — 보고서 출력)이 알아야 할 것**

- 「수단별 결과」는 3단계 `_measure_rows()` 가 **켠 수단만** 담는다. 하나도
  켜지 않으면 빈 표가 나가고 「검토 범위」가 무엇을 안 봤는지 밝힌다
- 내려받기는 `ui.session.build_report_bytes()` 다 — 만들고 바로 지운다.
  파일로 남기려면 `report.export_report()` 쪽이다
- 감도 문구(`SensitivityRange.text()`)와 금액 표기(`ui.text`)를 화면과 보고서가
  함께 쓴다. **모르는 값은 빈칸·0원이 아니라 사유**다
- 계산·화면 모두 손대지 않는 것이 기본이다. 보고서는 이미 있는 값을 엮는다

**테스트**
- pytest: 754 passed (직전 677 + 이번 77)
- ruff: pass (check + format)
- mypy: pass (strict, 109 files)
- 케이스 스터디: **66/66 통과** (시작·종료 두 번 확인)

---


### 8세션 준비 — 기준 데이터 외부화·원복·만료 감지 (2026-08-07)

**법령 유래 수치가 코드 상수로 흩어져 있어 사람이 볼 수도 고칠 수도 없었다.**
근거와 확인일을 붙여 파일로 옮기고, 화면에서 고칠 수 있게 만들었다.

---

#### 0. 케이스 스터디 결과 커밋

`output\casestudy_20260807.xlsx` 를 저장소에 남겼다. **`output/` 을 통째로
제외하면 git 이 그 아래의 예외(!)를 무시한다** — `output/*` 로 바꾸고
`!output/casestudy_*.xlsx` 를 열었다. 95.9초짜리 실행을 매번 돌리는 것보다 낫다.

---

#### 1. 기준 데이터 외부화 — 두 파일로 가른다

| 파일 | 항목 | 근거 |
|---|---|---|
| `data\rules_kr.json` | **26개** | 조문 (약관 제41·43·56·57·58·68조, 운영규칙 제12장) |
| `data\assumptions.json` | **21개** | `"판단값"` |

**섞지 않는다.** 섞으면 "이 숫자를 우리가 정한 것인가 법이 정한 것인가" 를 되물을
수 없는데, 그 물음이 갱신 판단의 전부다. 스키마가 이를 강제한다 — 법령 유래
항목에 근거 조문이 비면 파일 읽기가 **거부된다.**

**코드에서 뽑아낸 상수** (전부 함수 조회로 바꿨다)

    역률 8개    92 / 60 / 97 / 95 / 60 / 100 / 0.002 / 08~22시
    요금적용전력 4개  대상월·대상 시간대·판정 창 12개월·하한 비율
    계약종별 임계 3개  일반 300 / 산업 300 / 교육 1,000 kW
    DR 3개      0.1 MW-h / 200 kW / 2 MW
    계절·시간대·요일 규칙 5개  (요금표 파일에서 읽어 넣었다)
    판단값 21개  감도 s·PV 밀도·DR 분위수·ESS 효율·만료 임계 등

**모듈 상수로 붙잡지 않았다.** `LAGGING_STANDARD_PCT = 92.0` 을
`lagging_standard_pct()` 로 바꿨다. import 시점에 고정하면 UI 로 파일을 고쳐도
**그 프로세스에서는 옛 값으로 계산된다** — 코드에 기본값을 남기는 것과 같은
사고다. 데이터클래스 필드 기본값(`SharpnessFactors`)도 없앴다.

**계절·시간대·요일 규칙은 손으로 옮기지 않았다.** 요금표 파일에 이미 있으므로
생성 시점에 읽어 넣고, 두 벌이 어긋나지 않는지 테스트가 지킨다. 손으로 옮겼다면
내가 처음에 적은 시간대(mid `[[8,11],[12,13],[18,22]]`)가 그대로 들어갔을 텐데
**실제 값은 `[[8,15],[21,22]]` 였다.** 옮겨 적기가 왜 위험한지 그 자리에서 드러났다.

`data\pv_presets.json` 은 없앴다 — 내용이 전부 `assumptions.json` 으로 갔다.

---

#### 2. 편집 방식 — 데이터 성격에 따라

| 방식 | 대상 | 이유 |
|---|---|---|
| 엑셀 왕복 | 요금 단가, 시군구 좌표 | 항목이 많고 표 구조다 |
| **화면 편집** | 기준 데이터 | 항목이 적고 **근거가 함께 보여야** 한다 |

기준 데이터에도 엑셀 왕복을 **보조 경로**로 뒀다 (`export_rules_xlsx.py` /
`build_rules.py`). 강제하지 않는다. 되읽기는 **항목 키를 대조해** 행이 빠지거나
키가 바뀌면 거부한다 — 조용히 항목이 사라지면 코드에 기본값이 없으므로 다음
실행이 멈추고, 원인을 찾기 어렵다.

**화면 편집 API** (`kwise.rules`) — 8세션 UI 가 그대로 쓴다.

    describe_items()   값·라벨·근거·시행일·확인일·경과 개월·출고값·변경 여부
    set_value()        값 수정 + **확인일 자동 갱신**
    confirm()          값은 그대로, "확인함"만 기록
    diff_from_defaults()  출고값과 다른 항목

`set_value`·`confirm` 은 **검증 → 백업 → 저장 → 이력 → 캐시 비움** 순서를 지킨다.
**④ 캐시 비움이 없으면 파일은 바뀌었는데 계산은 옛 값으로 돈다.**

검증에 실패하면 **저장하지 않고 어느 항목이 왜 틀렸는지** 돌려준다.

---

#### 3. 원복 — 세 층. **버그를 두 개 잡았다**

    data\defaults\   출고 기본값. 커밋. 읽기 전용
    data\            현재 값
    data\backup\     편집 직전 스냅샷 (최근 10개, .gitignore)

| 원복 | 확인한 것 |
|---|---|
| ① 직전 상태 | 되돌리기 자체도 백업한다 — 되돌린 것을 되돌릴 수 있어야 한다 |
| ② 출고 상태 | **확인 전에는 실행하지 않고** 달라지는 항목만 돌려준다 |
| ③ 항목별 | 한 항목만. 나머지는 건드리지 않는다 |

**버그 1 — 직전 복원이 방금 만든 상태를 되돌렸다.** 안전 백업을 쓰면 그것이 목록
맨 앞으로 올라오는데 `restore_latest_backup()` 이 목록을 다시 읽어 그 안전 백업을
복원했다. 되돌릴 대상을 **미리 잡아 두는** 것으로 고쳤다.

**버그 2 — 같은 분에 두 번 백업하면 앞엣것을 덮어썼다.** 파일명이 분 단위라
편집 백업과 안전 백업이 같은 이름이 되어, 편집 **직전** 상태가 사라졌다. 이름이
겹치면 접미사를 붙이도록 고쳤다. 테스트가 없었으면 실사용에서야 드러났을 것이다.

교차 판정은 여섯 케이스를 모두 돌려야 성립하므로, 일부만 실행하면 **건너뛴
사실을 판정 목록에 남긴다.** 조용히 빼면 "전부 통과" 로 읽힌다.

**손상 대응** — 최근 백업 → 출고값 순으로 복구하고 **반드시 알린다**
(`RuleSet.recovered_from`). 손상본은 `<이름>.json.damaged` 로 남긴다. 조용히
넘어가면 갱신한 값으로 계산되는 줄 알고 결과를 쓰게 된다.

---

#### 4. 만료 감지

확인일(없으면 시행일)로부터 경과 기간을 재고 임계를 넘으면 경고한다. **임계값도
`assumptions.json` 에 있다** — 코드에 두면 "왜 12개월인가" 를 물을 곳이 없다.

    요금 단가 12개월 / 약관·규칙 24개월 / 참고단가 24개월 / 기상 전년도 미확보 시

**자동 수집은 하지 않는다.** 한전 요금표는 API 가 없고 연 1~2회 개정이라 수동
갱신이 현실적이다. 자동화하면 원문이 바뀐 것을 알아채지 못한 채 파싱만 실패하고
**그 실패는 조용하다.** 경고에 원문 확인처(한전 요금표·약관, 전력거래소,
에너지경제연구원, Open-Meteo)를 함께 싣는다.

갱신 절차를 README 에 적었다 — 무엇을 언제, 셋 중 어느 방법으로, 되돌리는 법,
깨졌을 때. **1년 뒤의 본인이 읽을 문서다.**

---

**외부화 후에도 케이스 스터디 66/66 이 그대로 통과한다.** 회귀값(기본요금
452,832,624원, 요금적용전력 5,293.44 kW)도 하나도 바뀌지 않았다.

**만든·고친 파일**

- `src\kwise\rules\` — 신규 패키지. `schema` / `store` / `validate` / `expiry` / API
- `data\rules_kr.json`, `data\assumptions.json` + `data\defaults\` 사본
- `tools\export_rules_xlsx.py`, `tools\build_rules.py` — 엑셀 왕복 (보조 경로)
- 상수를 뽑아낸 곳 — `tariff\power_factor.py`·`demand.py`·`schema.py`·`engine.py`,
  `diagnose\dr.py`·`peak.py`·`report.py`, `measures\ess.py`·`arbitrage.py`·
  `power_factor.py`·`solar.py`, `pv\sensitivity.py`·`layout.py`
- `data\pv_presets.json` — 삭제 (assumptions.json 으로 흡수)
- `tests\test_rules.py` — 신규 30건
- 요구사항서 **12장 신설**, 11.2 시험 항목 10건 추가, README 갱신 절차

**미해결** — 신규 없음

**다음 세션(8세션 — Streamlit UI)이 알아야 할 것**

- 「기준 데이터 관리」 화면은 `describe_items()` → 표, `set_value()`/`confirm()` →
  편집, `diff_from_defaults()` → 원복 미리보기, `restore_*()` → 원복이면 된다.
  **API 는 다 있다. UI 는 호출만 한다**
- **출고 복원은 반드시 미리보기를 먼저 보여 준다.** `confirmed=False` 로 부르면
  달라지는 항목만 돌려주고 실행하지 않는다
- `RuleSet.recovered_from` 이 비어 있지 않으면 **화면 상단에 띄운다.** 손상 복구를
  조용히 넘기면 안 된다
- `expiry_warnings()` 를 시작 화면에 건다. 원문 확인처 링크가 함께 온다
- 편집 뒤 `reload_rules()` 는 API 가 알아서 부른다. Streamlit 캐시(`st.cache_data`)를
  쓴다면 **그쪽도 함께 비워야** 한다 — 안 그러면 파일은 바뀌었는데 화면은 옛 값이다

**테스트**
- pytest: 677 passed (직전 646 + 이번 31)
- ruff: pass (check + format)
- mypy: pass (strict, 92 files)

---


### 7세션 — 검증과 케이스 스터디 (2026-08-07)

**UI 없이 순수 함수와 CLI 로 돌렸다.** 케이스 6종 × PV 4단계 × 감도 3종 ×
선택요금 3종. 타당성 판정 **66건 전부 통과**.

결과 통합문서는 `output\casestudy_20260807.xlsx` 다. `output\` 은 `.gitignore`
대상이라 커밋하지 않는다 — **부하 데이터(`input\cases\`)와 코드가 커밋되어 있으므로
`tools\run_casestudy.py` 로 언제든 그대로 재현된다.** 핵심 수치는 아래에 옮겨 둔다.

---

#### 0. 태양광 투자비 단가 (선행 작업)

ESS 와 같은 규약으로 **kWp당 단가**로 통일했다. 총액 직접 입력 경로도 뒀다.

**참고단가는 만들지 않았다.** ESS 는 에너지경제연구원 LCOS 연구를 인용할 수
있지만 태양광은 확보하지 못했다. 단가를 넣지 않으면 투자비가 **0원이 아니라
None** 이고 사유를 돌려준다. `PvCostReference` 자리만 열어 두고 미해결 항목에
올렸다. kWp 가 DC 정격임과 규모의 경제 미반영을 산출물에 적는다.

곁들여 조합 비교의 투자비도 `float | None` 로 바꿨다. 0원으로 두면 회수기간이
0년으로 나와 **"즉시 회수"** 로 읽힌다.

---

#### 1. 요금 엔진 검증 (11.1)

수기 케이스를 5건 → **7건**으로 늘렸다. 새로 넣은 둘이 케이스 스터디가 태우는
조항과 짝을 이룬다.

| # | 케이스 | 짝이 되는 케이스 |
|---|---|---|
| 6 | 2023-04 **산업용(을)** 봄·가을 주말 할인 | C4 |
| 7 | **경부하 야간 최대**는 요금적용전력이 아니다 | C6 |

**수기 케이스 6 에서 손계산이 틀렸다.** 토·일 11~14시를 모두 중간부하로 보고
할인액 833,400원을 적었는데 엔진은 781,200원을 냈다. **일요일은 전량 경부하**라
그 시각 단가가 138.9원이 아니라 121.5원이다 (3세션 결정).

    토 11~14시  60슬롯 × 138.9 × 50% = 416,700
    일 11~14시  60슬롯 × 121.5 × 50% = 364,500
    합계                              781,200 ✓

**엔진이 맞았고 손계산이 틀렸다.** 수기 케이스가 제 역할을 했다.

**1~3번(청구서·한전 공개 서비스 대조)은 여전히 미확보다.** 한전 공개 요금계산
서비스는 15분 계량 데이터를 받지 않고 월 사용량과 계약 정보만 받는다.
**시간대별 배분이 핵심인 본 엔진과 대조할 축이 없다.** 청구서 한 벌이 있어야
풀린다 — 미해결 항목에 그대로 둔다.

---

#### 2. 케이스 부하 생성 (`tools\make_cases.py`, `input\cases\`)

원본의 계절성·결측·그리드 이탈은 그대로 두고 **시간대별 분포만** 바꿨다.
총량이 함께 흔들리면 전력량요금까지 움직여 어느 규칙이 작동했는지 알 수 없다.

| 케이스 | 총 사용량 | 관측 최대 | 요금적용전력 | 부하율 | 종별 |
|---|---|---|---|---|---|
| C1 오전 피크형 | 22,285 MWh | 5,293 kW | 5,293 kW | 49.0% | 일반용(을) |
| C2 오후 피크형 | 22,241 MWh | 5,293 kW | 5,293 kW | 49.0% | 일반용(을) |
| C3 평탄부하형 | 22,285 MWh | 3,052 kW | 3,052 kW | 85.0% | 일반용(을) |
| C4 주말 가동형 | 24,051 MWh | 5,293 kW | 5,293 kW | 52.9% | **산업용(을)** |
| C5 겨울 피크형 | 20,994 MWh | 6,175 kW | 6,175 kW | 39.6% | 일반용(을) |
| C6 야간 피크형 | 22,285 MWh | **10,921 kW** | **2,801 kW** | 23.8% | 일반용(을) |

**두 번 고쳤다.**

1. **C2 의 이동 방향이 반대였다.** `new[m] = old[m+5h]` 로 짜서 10시 피크가
   05시로 갔다. `new[m] = old[m−5h]` 가 맞다. 상위 200슬롯 시각 분포로 잡았다
   (10·12·9·11시 → 15·17·14·16시)
2. **C5 의 겨울 피크 창이 넓었다.** 07~12시로 잡았더니 10~12시에는 겨울에도 PV 가
   발전해 피크를 깎아 버려 "대상월인데 PV 가 약하다" 는 케이스의 뜻이 사라졌다.
   **07~09시·17~20시(난방 램프)로 좁혔다.** 실제 겨울 피크 건물도 그 시각에 걸린다

---

#### 3. 타당성 판정 (`kwise.report.validity`) — 66/66 통과

**잣대를 바로잡았다.** 처음에 "기본요금 절감액 ÷ 기본요금" 으로 케이스를
견줬는데, **기본요금 자체가 요금적용전력에 비례하므로** 피크가 낮은 케이스일수록
같은 kW 를 깎아도 비율이 커져 뜻이 뒤집힌다. C3 평탄부하형이 그래서 "절감이
거의 없다" 를 통과하지 못했다 — 계산 오류가 아니라 잣대 오류였다.

**PV 가 실제로 하는 일은 kW 를 깎는 것**이므로 요금적용전력 저감 폭(kW)과
저감률로 바꿨다.

**요금적용전력 3규칙이 케이스마다 갈렸다 (PV 1,000 kWp)**

| 판정 | 근거 |
|---|---|
| C1 > C2 (저감 폭) | **95.4 kW vs 5.8 kW** — C2 는 PV 가 오후 피크를 깎으면 저녁 슬롯이 새 최대가 된다 |
| C3 은 절감의 대부분이 전력량요금 | 기본요금 비중 **C3 3.6% vs C1 10.2%** |
| C5 저감률이 C1 의 절반 미만 | **0.28% vs 1.80%** — 겨울 피크가 07~09·17~20시라 PV 가 닿지 않는다 |
| C6 요금적용전력 < 관측 최대 | **10,921 → 2,801 kW (74.4% 낮다)** — 경부하 제외 (5.2 ①) |
| C6 저감률이 C1 보다 크다 | **3.05% vs 1.80%** — 대상 슬롯이 주간뿐이라 PV 가 그대로 듣는다 |
| C4 전력량요금 절감 비율이 낮다 | **4.95% vs 5.44%** — 봄·가을 주말 11~14시 할인 단가가 이미 낮다 |

**C2 는 500 kWp 에서 이미 포화한다** (1,250,215원 고정). 오후 피크형에서 PV 를
아무리 키워도 기본요금이 더 줄지 않는다는 뜻이다. 6세션이 남긴 "①경부하 제외와
②대상월 한정이 부하 형태에 따라 반대로 작용한다" 가 여기서 숫자로 드러났다.

---

#### 4. 감도 — **어느 케이스에서도 '첨예형'이 상한이 아니었다**

6케이스 × 3용량 = 18개 조합에서 기본요금 절감액의 상한 시나리오는
**평탄형 16건, 기준 2건, 첨예형 0건**이었다.

총량이 보존되므로 첨예도를 올리면 정오가 높아지는 대신 **아침·저녁 어깨가
낮아지는데, 피크 저감은 어깨 출력에 걸린다.** 정오에 정확히 피크가 있는 부하가
아니라면 첨예형이 불리하다. 11차에서 이름을 평탄형/기준/첨예형으로 바꾸고
단조성 요구를 뺀 판단이 옳았음이 확인됐다.

발전량은 세 시나리오에서 **±0.01% 이내**로 보존됐다 (전 케이스).

---

#### 5. 경계 케이스 (11.3.2) — 전부 통과

| 케이스 | 확인한 것 |
|---|---|
| 12개월 미만 (90일) | `base_fee_months` 3.0, '연간'이라 부르지 않는다 |
| 역년이 아닌 기간 | 앞뒤가 부분 월, `base_fee_factor` 합이 개월수 |
| 1시간 간격 | 간격 자동 판정, 400 kWh/1h = 400 kW |
| 결측률 10% | 미보간, 경고 발생, 계산은 완주 |
| 계약전력 초과 | 경부하 초과는 요금적용전력과 **무관**하지만 초과 슬롯으로 잡힌다 |
| 2022년 + API 실패 | **중단하고 안내** — 사전 취득 범위(2023-01~) 밖이다 |

---

#### 6. 성능

| 항목 | 값 |
|---|---|
| 전체 (6케이스 × PV 4단계 × 감도 3종) | **95.9 초** |
| 케이스당 | 14.3 ~ 17.4 초 |
| 기상 취득 호출 | **0회** (사전 취득분·캐시 적중 6/6) |

케이스가 모두 같은 좌표·기간이라 격자 캐시가 그대로 듣는다. 9차의 0.25° 격자
반올림과 10차의 사전 취득이 여기서 값을 했다. **느린 구간은 `calculate_bill`**
이고 케이스당 약 20회 부른다 — 캐싱 지점을 옮길 만한 병목은 없었다.

---

**만든·고친 파일**

- `src\kwise\measures\pv_cost.py` — 신규. `PvCostInput`, 참고단가 자리
- `src\kwise\report\casestudy.py` — 신규. 케이스 실행·집계
- `src\kwise\report\validity.py` — 신규. 타당성 판정 66건
- `tools\make_cases.py` — 신규. 합성 부하 생성기
- `tools\run_casestudy.py` — 신규. 실행 CLI (판정 실패 시 종료 코드 1)
- `input\cases\C1~C6*.csv` — 신규. 합성 케이스 6종
- `src\kwise\measures\solar.py` — 단가 입력 교체, 규모의 경제 주석
- `src\kwise\compare\combination.py`, `report\batch.py` — 투자비 `float | None`
- `tests\test_casestudy.py` — 신규 18건 (경계 케이스 포함)
- `tests\test_tariff_engine.py` — 수기 케이스 6·7 추가
- 요구사항서 7.5, 11.1, 11.3(11.3.1·11.3.2 신설), 부록 D 17·18

**미해결** — 신규 1건 (태양광 참고단가). 청구서 관련 4건은 그대로

**다음 세션(8세션 — Streamlit UI)이 알아야 할 것**

- **케이스 스터디가 통과했으므로 계산은 더 손대지 않는다.** UI 는 순수 함수를
  호출만 한다
- 감도는 **범위**로 보여 준다. '첨예형이 좋은 값' 이라는 인상을 주면 안 된다 —
  18개 조합에서 한 번도 상한이 아니었다
- `kwise.report.casestudy` 의 표 구성이 UI 화면 구성과 거의 같다. 시트 이름을
  화면 탭 이름으로 그대로 써도 된다
- ESS 참고단가 표는 `reference_table(highlight_hours=...)` 로 **산출된 방전시간
  행을 강조**해서 보여 준다 (11차 결정)
- 태양광 단가를 넣지 않으면 투자비 칸에 **사유**가 들어간다. 빈칸이나 0원으로
  보이면 안 된다

**테스트**
- pytest: 646 passed (직전 626 + 이번 20)
- ruff: pass (check + format)
- mypy: pass (strict, 84 files)

---


### 11차 — 감도 명칭·범위 표시, 설치 밀도 라벨, ESS 단가·차익거래 (2026-08-07)

**7세션 케이스 스터디 직전의 마지막 보완이다.** 10차가 계산을 고쳤다면 이번은
**읽히는 방식**을 고쳤다. 숫자가 맞아도 이름이 반대를 가리키면 결론이 뒤집힌다.

---

#### 0. 기상 사전 취득 완료

10차에서 384/405 였던 것을 마저 받았다. **전국 135격자 × 3개년 = 405개 완비,
실패 0개.** 재개는 같은 명령 한 줄이었다 (위 10차 기록의 표 참조).

---

#### 1. 감도 명칭·표시 정정 — 이름이 축을 가리켜야 한다

10차에서 `sharpen()` 을 넣은 뒤 **'낙관'의 기본요금 절감액이 가장 작게 나왔다.**
버그가 아니라 모델이 맞게 작동한 결과다 — 총량을 보존하며 정오를 올리면 아침·저녁
어깨가 내려가고, 샘플은 최대수요가 09:30 인 오전 피크형이라 **어깨 출력이 곧 피크
저감**이기 때문이다.

그러나 이름과 결과가 반대라 오독을 부른다. 이 축은 낙관·비관이 아니라 **프로파일이
얼마나 뾰족한가**다.

| | 이전 | 지금 |
|---|---|---|
| 표시 | 보수 / 기준 / 낙관 | **평탄형 / 기준 / 첨예형** |
| 내부 키 | — | `conservative` / `base` / `optimistic` (**그대로**) |
| s | 0.85 / 1.00 / 1.25 | 같음 |

**내부 키를 바꾸지 않았다.** 설정 파일(`data\pv_presets.json`)과 케이스 정의가
그 이름을 쓰고 있어 바꾸면 기존 정의가 깨진다. 라벨만 `labels` 로 분리했다.

**결과 표시를 3열 나열에서 범위로 바꿨다.**

    기본요금 절감  3,152만원 (프로파일 감도 범위 2,897 ~ 3,266만원)

세 값을 나란히 놓으면 읽는 사람이 "어느 쪽이 좋은 값인가" 를 찾게 되는데 **이 축에는
좋고 나쁨이 없다.** 최댓값·최솟값은 `min`/`max` 로 뽑고 **어느 시나리오가 상한인지
고정하지 않는다** — 회귀 테스트에서 실제로 드러났다. 960 kWp 에서는 상한이
평탄형이지만 500 kWp 에서는 **기준**이 상한이다. 첨예형이 상한인 경우는 없었다.

Excel 은 `감도` 시트가 범위표, `감도 상세` 시트가 원자료 3행이다 (아홉 장이 됐다).
태양광이 없는 케이스는 감도 시트에 사유만 적고 상세를 만들지 않는다.

---

#### 2. 설치 밀도 라벨 — 구어체를 산출물에서 뺐다

'촘촘·듬성' 은 제안서에 그대로 쓸 수 없다.

| 내부 키 | 표시 | GCR | 경사 | 1,000 m² | 상충 관계 |
|---|---|---|---|---|---|
| `high` | 높음 | 0.55 | 15° | **110 kWp** | 면적당 용량 최대 / kWp당 발전량 최소 |
| `normal` | 보통 | 0.40 | 25° | **80 kWp** | 균형 (기본값) |
| `low` | 낮음 | 0.30 | 30° | **60 kWp** | kWp당 발전량 최대 / 면적당 용량 최소 |

**선택지마다 상충 관계를 한 줄로 병기한다.** 밀도만 보아서는 "높으면 좋은 것인지"
를 알 수 없기 때문이다. `capacity_preview()` 로 **고르는 즉시 환산 용량을 함께**
보여 준다 — 밀도라는 말보다 110/80/60 kWp 라는 숫자가 결정에 쓰인다.

---

#### 3. ESS 단가 — kW당 하나로 받는다

**2성분(원/kW + 원/kWh)이 물리적으로는 정확하지만 사용자가 견적서를 그 형태로
받는 일이 드물다.** 입력은 단순하게 간다.

    투자비 = 필요 출력(kW) × 입력단가(원/kW)

방전시간은 이미 kW당 단가에 반영되어 있으므로 **이중으로 곱하지 않는다.**
견적서를 받았으면 총액을 그대로 넣는 경로(`EssCostInput.of_total`)도 뒀다.
손익분기 단가도 원/kWh 에서 **원/kW 로 바꿨다** — 입력과 같은 단위여야 견줄 수 있다.

**참고단가는 2성분 원본을 저장하고 방전시간별로 환산해 제공한다**
(`data\ess_cost_reference.json`). 출처는 에너지경제연구원 LCOS 연구
(2025년 기준, 환율 1,412원/달러). 양수발전은 건물 대상이 아니라 넣지 않았다.

    kW당 단가 = CAPEX_Power + CAPEX_Energy × 방전시간

| 방전시간 | LFP 2025 | LFP 2030 |
|---|---|---|
| 0.5h | 430,263 | 355,312 |
| 1h | 615,231 | 497,400 |
| 2h | 985,167 | 781,575 |
| 4h | 1,725,039 | 1,349,925 |

지시받은 검산값과 정확히 일치한다 (회귀로 고정).

**이 값의 성격이 결론의 강도를 정한다 — 추정치가 아니라 하한선이다.** 계통용 대형
ESS 기준이고 전용실·소화설비 등 안전 규제 대응비와 수배전 연계공사비가 빠져 있다.
그래서 판정이 **비대칭**이다.

    하한선 > 손익분기  →  경제성 없음        (강한 판정. 더 싸질 여지가 없다)
    하한선 < 손익분기  →  견적 받아볼 가치    (약한 판정. 실제 견적은 이보다 높다)

이 비대칭을 `EssCostReference.verdict()` 가 문구로 그대로 낸다.
**참고값이 기본값으로 자동 적용되지 않는다** — 설정에서 `auto_apply: true` 로
바꿔도 로더가 거부한다. 출처가 다른 값이 견적으로 둔갑하는 것을 막는다.

---

#### 4. ESS 방전시간과 차익거래

**방전시간은 산출값이다. 강제하지 않는다.**

    방전시간 = 하루 최대 초과 에너지 ÷ 최대 초과 출력

샘플(목표 5,200 kW)에서 **0.372h = 2.69C** 가 나온다. 0.5h 미만이라 고출력 셀
경고를 낸다 — 정치형 LFP 는 통상 0.5~1C 연속이므로 참고단가보다 비쌀 수 있다.

**차익거래를 넣었다.** 계시별 단가는 요금 엔진에서 자동으로 가져온다.

| 계절 | 최대부하 | 경부하 | 차익(η 반영) | 평일수 |
|---|---|---|---|---|
| 여름 | 227.8 | 92.8 | 122.3 | 64 |
| 겨울 | 203.4 | 99.8 | 90.0 | 83 |
| 봄·가을 | 146.0 | 92.8 | 40.5 | 103 |

**연 19,476원/kWh · 단독 회수기간 19.0년.** CAPEX_Energy 369,936원/kWh 대비이고
배터리 수명(10~15년)을 넘는다. **단독으로는 성립하지 않는다는 근거가 이 숫자다.**

평일 수는 요금 달력에서 **최대부하가 존재하는 날만** 세어 얻는다 (250일). 토요일은
최대부하가 중간부하로 계량되고 일요일·공휴일은 전량 경부하라 자동으로 빠진다.
DR 거래일(6.6)과 규칙이 다르므로 따로 센다.

**차익거래 수익을 절감액에 더하지 않았다.** 피크컷 디스패치가 이미 일부를
실현하고 있어 그대로 더하면 이중 계산이다. 별도 줄로 내고 회수기간은 **두 기준**을
함께 낸다.

**수익 구조 — 이것이 핵심 메시지다** (샘플 93.4 kW)

    피크저감 수익  ∝ 출력(kW)    용량을 늘려도 늘지 않는다
    차익거래 수익  ∝ 용량(kWh)
    투자비         ∝ 용량(kWh)

| 방전시간 | 투자비 | 피크저감만 | 차익거래 포함 |
|---|---|---|---|
| 0.5h | 4,020만원 | 6.0년 | **5.3년** |
| 1h | 5,749만원 | 8.6년 | **6.9년** |
| 2h | 9,205만원 | 13.7년 | **9.2년** |
| 4h | 16,119만원 | 24.1년 | **12.2년** |

지시받은 참고값(5.0 / 6.4 / 8.6 / 11.2년)은 **차익거래를 포함한 쪽**과 거의 같다
(6~9% 차이). 두 기준을 모두 내되 기본값은 이중 계산이 없는 '피크저감만' 쪽이다.

**2030년 전망 단가 기준 회수기간을 함께 낸다.** 샘플에서 현재 6.0년 → 전망 4.4년.
"지금은 경제성 없음" 을 **"언제쯤 성립하는가"** 로 바꾼다.

---

**만든·고친 파일**

- `data\ess_cost_reference.json` — 신규. 2성분 원본 + 출처 + 비대칭 판정 문구
- `src\kwise\measures\ess_cost.py` — 신규. `EssCostInput`(두 경로), 방전시간별 환산,
  `verdict()`, 자동 적용 차단
- `src\kwise\measures\arbitrage.py` — 신규. 계절별 단가차익, 평일수, 단독 회수기간
- `src\kwise\measures\ess.py` — 단가 입력 교체, `required_discharge_hours`,
  C-rate 경고, 전망 단가, `ess_payback_curve()`
- `src\kwise\pv\sensitivity.py` — `labels` 분리, `SCENARIO_KEYS`, `keyed_items()`
- `src\kwise\compare\sensitivity.py` — `SensitivityRange`, `sensitivity_range_frame()`
- `src\kwise\pv\layout.py` — `tradeoff`, `density_label`, `capacity_preview()`
- `data\pv_presets.json` — 밀도 키 high/normal/low, 라벨·상충 관계, 감도 라벨
- `src\kwise\compare\combination.py`, `src\kwise\report\batch.py` —
  `ess_unit_cost_won_per_kw` 로 개명, `CaseSpec.ess_cost`
- `src\kwise\report\excel.py` — 감도 시트를 범위로, `감도 상세` 신설,
  차익거래 별도 줄
- `src\kwise\report\notices.py` — 한계 13→16, ESS 출처
- `tests\test_ess_cost.py` — 신규 26건. `test_compare`·`test_pv_input` 보강
- 요구사항서 3.3, 7.6(7.6.1~7.6.5 신설), 9.2, 10.3, 11.2, 11.3, 부록 D 13→16

**미해결** — 신규 없음

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **감도표는 범위로 읽는다.** 상한이 어느 시나리오에서 나오는지는 부하 형태가
  정한다. 정오 피크형 케이스에서는 첨예형이 상한이어야 한다 — 그 대비를
  케이스 스터디가 보여야 한다
- 케이스 YAML 의 ESS 단가 키가 `ess_unit_cost_won_per_kw` 로 바뀌었다.
  총액을 넣으려면 `ess_total_investment_won`
- ESS 결론을 쓸 때 **판정의 강도를 구분한다.** "경제성 없음" 은 강하게,
  "가능성 있음" 은 "견적을 받아볼 가치가 있다" 로 약하게 쓴다
- 방전시간이 0.5h 미만으로 나오는 케이스가 많을 것이다. 피크가 짧고 뾰족한
  건물일수록 그렇다. 고출력 셀 경고가 붙었는지 확인한다
- 차익거래 단독 회수기간(19년)은 **케이스마다 다시 계산된다** — 계시별 단가와
  평일수가 요금표·달력에서 오기 때문이다. 야간 피크형은 차익이 작을 것이다

**테스트**
- pytest: 620 passed (직전 585 + 이번 35)
- ruff: pass (check + format)
- mypy: pass (strict, 78 files)

---


### 10차 — 감도 첨예도 전환, 기상 사전 취득, 대한민국 전용 명시 (2026-08-07)

**7세션 케이스 스터디 직전의 마지막 보완이다.** 셋 다 케이스 결과의 *해석*을
바꾸므로 케이스를 돌린 뒤에 고치면 전부 다시 돌려야 했다.

---

#### 0. 감도 방식 정정 — 일률 계수를 **첨예도 조정**으로

**이전 방식이 9.1 과 어긋나 있었다.** 감도 3종(0.70/1.00/1.20)을 PV 출력
시계열 **전체에 곱하고** 있었다. 9.1 이 말하는 편향은 "총량 오차"가 아니라
**"프로파일 평탄화"** 다 — 재분석 일사의 공간·시간 평균화로 곡선이 눌리는 것이고,
총량은 실측과 거의 맞는다 (시간별 R² 0.8, 총량은 수렴). 총량이 맞는데 일률 계수를
곱하면 **없는 총량 불확실성을 ±30% 만들어 내는 것**이라 전력량요금 절감액 밴드와
회수기간 밴드가 함께 부풀려진다.

    adjusted(t) = 일평균발전 + (기준(t) − 일평균발전) × s

    보수 s=0.85 / 기준 s=1.00 / 낙관 s=1.25   ← data\pv_presets.json

**일평균발전은 그날 발전이 있는 슬롯만의 평균이고, 조정도 그 슬롯에만 건다.**
여기서 두 번 넘어졌고 둘 다 테스트로 못 박았다.

| 잘못 잡으면 | 무슨 일이 생기나 |
|---|---|
| 야간 0 을 평균에 포함 | 밤의 편차(0 − 평균)가 전체를 지배한다. s 를 키우면 야간이 크게 음수가 되고, 클램프 후 재정규화하면서 **정오 출력이 오히려 낮아진다** |
| 야간 슬롯에도 조정을 적용 | s < 1 일 때 0 이 평균 쪽으로 끌려 올라가 **한밤중에 발전이 생긴다** (실제로 03:00 에 51.5 kW 가 나왔다) |

발전 슬롯 기준으로 잡으면 두 문제가 함께 사라지고, 발전 슬롯 합이 곧 일 발전량이라
**총량이 정확히 보존된다.** 음수는 0 으로 자르고 일별 총량이 원본과 같도록
재정규화한다. `s = 1.0` 이면 원본 객체를 그대로 돌려준다 — 기준 시나리오가
시뮬레이션과 완전히 같아야 회귀값이 흔들리지 않는다.

**샘플 검증 (960 kWp, 일반용(을) 고압A 선택Ⅰ)**

| 지표 | 보수 0.85 | 기준 1.00 | 낙관 1.25 | 편차 |
|---|---|---|---|---|
| 발전량(kWh) | 1,745,276 | 1,745,355 | 1,745,442 | **0.01%** |
| 전력량요금 절감액 | 2억 3,417만 | 2억 3,408만 | 2억 3,380만 | **0.16%** |
| **기본요금 절감액** | **3,266만** | **3,152만** | **2,897만** | **11.30%** |
| 요금적용전력(kW) | 4,921.4 | 4,934.5 | 4,963.8 | — |

의도한 대로 갈렸다. **총량 기반 지표는 거의 불변, 피크 기반 지표만 흔들린다.**

**예상하지 못한 결과 — 시나리오 이름을 절감액으로 읽으면 안 된다**

'낙관'의 기본요금 절감액이 **가장 작다.** 총량이 보존되므로 첨예도를 올리면 정오는
높아지지만 **아침·저녁 어깨가 낮아지고**, 샘플은 최대수요가 09:30 인 오전 피크형이라
어깨 시간대 출력이 곧 피크 저감이기 때문이다. 일률 계수를 곱하던 이전 방식에서는
'낙관 = 절감액 최대'가 항상 성립했으므로 **읽는 방식 자체가 바뀐다.**

`SCENARIO_NAME_CAVEAT` 를 만들어 감도표와 Excel 요약에 함께 싣고 회귀로 고정했다.
**7세션에서 오후 피크형·야간 피크형 케이스를 볼 때 이 방향이 뒤집힐 것이다.**

`sharpen()` 은 양의 상수배에 **동차**다(평균·편차·클램프·재정규화가 모두 비례).
그래서 용량 곡선 20단계마다 다시 걸지 않고 **단위 프로파일에 한 번만** 건다.
5세션에서 고정한 "시뮬레이션 1회 + 곱셈" 구조가 그대로 유지된다. 테스트로 고정.

`sensitivity_factor` → `sharpness` 로 **이름을 바꿨다.** 뜻이 달라졌는데 같은
이름을 두면 다음 세션이 옛 뜻으로 읽는다. `SensitivityFactors` → `SharpnessFactors`,
`DEFAULT_FACTORS` → `DEFAULT_SHARPNESS` 도 같은 이유다.

---

#### 1. 기상 데이터 사전 취득 (`kwise.pv.archive`, `tools\fetch_weather.py`)

**조회 순서를 확정했다.**

| 순서 | 경로 | 비고 |
|---|---|---|
| ① | `PROJECT_CACHE` 캐시 | 반복 호출을 여기서 막는다 |
| ② | Open-Meteo API | 성공분은 ①에 캐시한다 |
| ③ | 사전 취득분 `data\weather\` | **폴백 사실을 결과에 표시한다** |
| ④ | 없으면 **중단** | 0 으로 계산하거나 인접 격자로 대체하지 않는다 |

**전국 3개년치를 실제로 받았다** (이 세션에서 수행).

| 항목 | 값 |
|---|---|
| 격자 | **135개** (시군구 229개를 0.25° 로 접은 결과) |
| 기간 | 2023-01-01 ~ 2025-12-31 (3개년) |
| 파일 | 405개 (격자 × 연도) |
| 행 | 3,551,040 (연 1,182,600 / 윤년 1,185,840) |
| 용량 | **40.4 MB** (예상 21 MB 보다 크다. zstd level 9) |
| 실패 | **0개** |

**호출 제한에 실제로 걸렸다.** 1차 실행에서 384개를 받고 나머지 21개가
`Hourly API request limit exceeded` 로 막혔다. 백오프(2→4→8→16초) 후에도 안 되어
실패 목록에 남기고 계속 진행했고, 한 시간 뒤 **같은 명령**으로 재개해 21개를
마저 받았다 (증분 갱신). 설계한 대로 동작했다 — 이 기능이 없었으면 처음부터
다시 받아야 했다.

**저장 구조**

- 격자 셀 × 연도 parquet (`openmeteo_37.5000_127.0000_2023.parquet`), zstd level 9
- `int16` + 스케일 팩터 — 일사 ×10 (0.1 W/m²), 기온·풍속 ×100 (0.01)
- **적설은 담지 않는다.** pvlib 경로에서 쓰지 않아 호출 비용만 늘린다.
  스키마 호환을 위해 읽을 때 0 으로 채우고 그 사실을 테스트로 못 박았다
- 확보 현황 색인 `index.json` 을 함께 둔다. `archive_status()` 가 이것을 읽는다 (8세션 UI)
- 경로는 `KWISE_WEATHER_DIR` 로 바꿀 수 있다 — 저장소 밖에 두고 배포할 수 있다

**호출 제한 대응이 이 CLI 의 절반이다.** 실제로 걸렸다.

- 호출 간 최소 간격 1초 (설정 가능)
- 429·5xx·연결 실패는 지수 백오프 재시도 (2→4→8→16초, 상한 120초)
- 재시도 후에도 실패한 셀은 `fetch_failures.json` 에 남기고 **계속 진행**
- 이미 받은 셀·연도는 색인을 보고 건너뛴다 → **중단 후 같은 명령으로 재개**
- 부분 확보를 완전 확보로 오인하지 않는다 (일주일치가 있어도 그 해 전체를
  요구하면 다시 받는다)

**폴백을 캐시에 쓰지 않는다.** 쓰면 다음 실행에서 '캐시'로 둔갑해 폴백 표시가
사라진다. 조용히 바뀌는 것이 이 기능에서 가장 위험하다.

**테스트를 사전 취득분에서 격리했다** (`conftest.isolated_weather_archive`).
저장소에 전국 3개년치가 들어 있으므로, 격리하지 않으면 "API 실패 시 멈추는가" 같은
시험이 **조용히 폴백에 성공해** 통과해 버린다. 실제로 기존 시험 하나가 그렇게
통과하기 시작해서 잡았다.

---

#### 2. 적용 범위 — 대한민국 전용 명시

요구사항서 **1.4 절**에 근거를 표로 적고 1.5 절(범위 밖) 첫 항목으로 올렸다.
README 와 부록 D, `KNOWN_LIMITS` 에도 넣었다.

| 요소 | 근거 |
|---|---|
| 요금 체계 | 한전 기본공급약관·전기요금표 |
| 기본요금 | 약관 제68조 요금적용전력 |
| 역률 규정 | 약관 제41~43조 |
| 경제성DR | 전력시장운영규칙 제12장 |
| 공휴일 | 관공서 공휴일 규정 + 일요일 |
| 지역 선택 | 시군구 좌표 229개 |
| 기상 사전 취득 | 국내 135개 격자 |

**지역 데이터팩 같은 다국가 추상화를 만들지 않았다.** 쓰지 않을 구조는 복잡도만
늘린다. 이 판단도 문서에 적어 두었다 — 다음 세션이 다시 꺼내지 않도록.

---

#### 3. PDF 원본 정리

`data\source\` 의 PDF 두 개가 번들 용량의 대부분이었다 (9.4 MB).

- `pdftotext -layout -enc UTF-8` 로 추출 — 약관 0.32 MB, 운영규칙 2.76 MB
- **txt 를 커밋한다.** 조문 검색이 되는지 확인했다 (제43조 ② 2호 나목의
  "지상역률인 경우에는 역률 100%로", 제12.4.2.1조, 미해결 항목의 제38조 모두 검색됨)
- PDF 는 `.gitignore` 에 넣고 `git rm --cached` 로 추적에서 뺐다. 로컬에는 남는다
- 요금표 xlsx 는 변환 원본이므로 그대로 커밋 유지

---

**만든·고친 파일**

- `src\kwise\pv\archive.py` — 신규. 저장·색인·현황·재시도·페이서·작업 목록
- `tools\fetch_weather.py` — 신규. 사전 취득 CLI (rich 진행 표시)
- `src\kwise\pv\sensitivity.py` — `sharpen()` 신설, 첨예도 계수로 전면 교체
- `src\kwise\pv\weather.py` — 폴백 경로, `WeatherData.fallback`/`notes`
- `src\kwise\pv\simulate.py` — 폴백 문구를 경고로 승계
- `src\kwise\measures\solar.py`, `src\kwise\compare\combination.py` — `sharpness` 로 개명
- `src\kwise\compare\sensitivity.py` — 기본요금·전력량요금 절감액 열 분리, 이름 주의 문구
- `src\kwise\report\notices.py` — 한계 2건 추가(11→13), `DATA_SOURCES` 신설
- `src\kwise\report\excel.py`, `batch.py` — 출처·감도 문구, 폴백 표시
- `data\pv_presets.json` — `sensitivity_sharpness` 블록
- `data\weather\` — 전국 사전 취득분 (신규)
- `data\source\*.txt` — 약관·운영규칙 텍스트 (PDF 는 추적 해제)
- `tests\test_pv_archive.py` — 신규 28건. `tests\test_pv.py` 감도 절 전면 재작성
- `tests\conftest.py` — 사전 취득분 격리 픽스처
- 요구사항서 1.4·1.5·7.5·9.1·9.2·11.2·부록 D, README

**미해결** — 신규 없음

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **감도표를 읽는 법이 바뀌었다.** '낙관'은 절감액이 크다는 뜻이 아니라 발전
  프로파일이 뾰족하다는 뜻이다. 오전 피크형(샘플)에서는 '낙관'의 기본요금 절감액이
  가장 작다. 정오 피크형 케이스에서는 반대가 될 것이다 — **이 대비가 케이스
  스터디에서 드러나야 한다**
- 6세션이 남긴 "①경부하 제외 vs ②대상월 한정 중 어느 쪽이 지배하는가" 와 함께
  보면 부하 형태가 결론을 어떻게 뒤집는지 두 축이 모인다
- 기상은 네트워크 없이 돌아간다. 케이스 기간이 2023-01 ~ 2025-12 안이면 폴백도
  필요 없다. 샘플(2023-04 ~ 2024-04)은 범위 안이다
- 요약 시트의 `note` 에 폴백 문구가 있으면 그 케이스는 **API 가 아니라 사전
  취득분으로 계산된 것**이다. 값이 틀린 것은 아니지만 근거를 밝혀야 한다
- 사전 취득분을 갱신하려면 `tools\fetch_weather.py --start ... --end ...`.
  이미 받은 것은 건너뛴다

**테스트**
- pytest: 585 passed (직전 547 + 이번 38)
- ruff: pass (check + format)
- mypy: pass (strict, 75 files)

---


### 9차 — DR 감축량 정정, 지역 선택, 태양광 간략 입력 (2026-08-07)

**7세션 전 마지막 보완이다.**

---

#### 0. DR 감축 가능량 정정 — 등록값으로 곱하면 안 된다

직전 세션에서 연간 감축량을 `등록 권장값 × 거래일 수` 로 냈다. **틀렸다.**
경제성DR 은 하루 전 입찰이라 **매일 다른 양을 입찰한다.** 등록값(하위 10%)으로
곱하면 부하가 많은 날의 여력을 통째로 버린다.

| 방식 | 샘플 (245 거래일, 1h/일) |
|---|---|
| 등록값 × 일수 (이전) | 154,648 kWh |
| **거래일별 여력 합산 (지금)** | **341,921 kWh** |
| 차이 | **55% 과소평가였다** |

두 값을 **용도로 갈라** 반환한다.

    registered_capacity_kw   하위 10% 기준 — 사업자 계약 시 등록 권장값
    annual_reducible_kwh     거래일별 여력 합산 — 연간 수익 추정 기준
    flat_reduction_kwh       등록값 × 일수 — **비교용으로만** 남긴다

등록 권장값을 하위 10% 로 잡은 판단은 그대로 둔다 — 평균으로 등록하면 절반의
날에 미달하고 미달은 위약금이다(별표26). 분위수 상수를 `REGISTRATION_PERCENTILE`
로 빼고, 저부하일 식별의 `LOW_LOAD_PERCENTILE`(5%)과 **쓰임이 다르다**는 것을
주석과 테스트로 못 박았다.

일별 여력은 `DrProfile.daily_reducible_kw` 로 들고 있다 (245일, 평균 1,396 kW,
최소 0 · 최대 2,527 kW). 무비용 감축일 산정도 그날의 여력을 쓴다.

---

#### 1. 지역 선택 (`kwise.pv.region`)

**시군구 단위 선택은 정확도를 희생하지 않는다.** Open-Meteo 의 ERA5 격자가
25~31 km 인데 서울 25개 구의 좌표 폭이 남북 20.6 km · 동서 29.0 km 라, 구를
바꿔도 같은 셀을 조회한다. 강남구와 서초구가 둘 다 (37.5, 127.0) 격자다.

**두 경로를 모두 지원한다**

    시군구 선택 (기본)      시도 → 시군구 2단. 드롭다운은 데이터에서 생성
    위경도 직접 입력 (옵션)  산악·해안 지형이나 넓은 시군구용

**데이터 정리**

- 좌표가 문자열이라 `float` 로 바꾼다
- **세종특별자치시 추가** — 하위 시군구가 없는 단층 자치시라 원본에서 누락되어
  있었다. 세종시청 좌표(36.592369, 127.292199)를 코드에서 더한다.
  **원본 파일은 손대지 않는다** — 다른 228개 좌표가 원본과 같은지 테스트로 대조
- 시도 명칭이 개편 전 기준(강원도, 전라북도)이다. **키는 파일 그대로 두어 호환을
  유지하고** 표시 명칭에만 현행을 병기한다 ("강원특별자치도 (구 강원도) 춘천시")
- 좌표 범위 검증 — 위도 33~39, 경도 124~132 밖이면 실패

**캐시 키를 0.25° 격자로 반올림했다.** ERA5 해상도라 손실이 없고, 시군구 선택이든
좌표 직접 입력이든 같은 격자면 **같은 parquet 을 쓴다.** 7세션 케이스 스터디의
재조회를 크게 줄인다.

---

#### 2. 태양광 간략 입력 (`kwise.pv.layout`)

**기본 입력을 3개로 줄였다 — 설치 가능 면적 · 설치 밀도 · 지역.**

방위·경사·손실률을 정밀하게 받아도 발전량 예측 R² 가 0.8 이라 기상 오차가
지배한다. 게다가 **용량 곡선이 이미 20단계를 훑으므로 면적 환산이 ±20% 틀려도
곡선 위의 다른 점을 보는 것일 뿐** 결론이 바뀌지 않는다 (테스트로 고정).

**밀도 프리셋이 GCR 과 경사각을 함께 정한다**

| 밀도 | GCR | 경사각 | 1,000 m² |
|---|---|---|---|
| 촘촘 | 0.55 | 15° | **110 kWp** |
| 보통 | 0.40 | 25° | **80 kWp** |
| 듬성 | 0.30 | 30° | **60 kWp** |

    설치 용량(kWp) ≈ 설치 가능 면적 × GCR ÷ 5      (모듈 효율 20% 기준)

프리셋과 kWp 당 면적은 `data\pv_presets.json` 에 있다. 하드코딩하지 않았다.
방위각은 남향(180°) 기본.

**모드를 나누지 않았다.** 나머지(방위·경사·GCR·손실률·용량 직접 입력)는
`PvQuickInput` 의 선택 인자로 두고, 안 주면 프리셋·기본값이 자동 적용된다.
8세션 UI 는 한 화면에서 접었다 펴면 된다. 용량을 직접 지정하면 면적 환산을
덮어쓰되 환산값도 함께 보여 준다.

**상세 설계는 범위 밖**임을 요구사항서 1.5 절에 명시했다 — kWise 는 도입 전
검토 도구이고, 모듈 배치·스트링 구성·음영 시뮬레이션은 pvsim 의 몫이다.

---

**만든·고친 파일**

- `src\kwise\pv
egion.py` — 신규. 시군구 229개, 격자 반올림
- `src\kwise\pv\layout.py` — 신규. `PvQuickInput`, 밀도 프리셋
- `data\sigungu_kr.json` — 배치된 원본을 그대로 커밋 (228개)
- `data\pv_presets.json` — 신규. 밀도 3종, kWp 당 면적
- `src\kwise\pv\weather.py` — `WeatherRequest.cache_name` 격자 반올림
- `src\kwise\diagnose\dr.py` — 등록값/연간 감축량 분리, 분위수 상수 2개
- `src\kwise\measures\demand_response.py` — 반환 필드 재구성
- `src\kwise
eport\excel.py` — 진단·수단 시트에 두 값을 나눠 표기
- `tests\test_pv_input.py` — 29건, `tests\test_dr.py` 갱신 + 4건
- 요구사항서 3.3 전면 개정, 1.5 에 상세 설계 범위 밖 추가

**미해결** — 신규 없음

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **기상 캐시가 격자 단위로 공유된다.** 같은 시도 안의 케이스를 여러 개 돌려도
  Open-Meteo 를 다시 부르지 않는다. 케이스를 지역별로 묶어 돌리면 더 빠르다
- 케이스 정의에 지역을 넣을 때 `"서울특별시/강남구"` 키를 쓴다. 좌표가 필요하면
  `find_region(key).latitude/.longitude`
- **PV 케이스는 면적·밀도로 정의하는 것이 자연스럽다.** `PvQuickInput.to_system()`
  이 `PvSystemConfig` 를 만들어 주므로 기존 경로에 그대로 물린다
- DR 수익을 비교할 때 `annual_reducible_kwh` 를 쓴다. `flat_reduction_kwh` 는
  "왜 그 방식을 안 쓰는지" 를 보여 주는 비교값이다
- 부하 유형별로 DR 일별 여력의 분포가 달라진다. 야간 피크형은 주간 여력이 작아
  등록값과 연간 감축량이 함께 낮게 나올 것이다

**테스트**
- pytest: 547 passed (직전 514 + 이번 33)
- ruff: pass (check + format)
- mypy: pass (strict, 72 files)

---

### 8차 — 역률 야간 조항 정정과 경제성DR 신설 (2026-08-07)

---

#### 0. 역률 미해결 항목 정정 — 원문에 있었다

직전 세션에서 "야간 진상역률 하한이 원문에 없어 주간과 같은 60%를 적용했다" 고
남긴 것은 **틀렸다.** 기본공급약관 **제43조 ② 2호 나목**에 명시되어 있다.

> 30분 단위의 역률이 진상역률 60%에 미달하는 경우에는 역률 60%로,
> **지상역률인 경우에는 역률 100%로 간주**하여 1개월간 평균역률을 계산합니다.

60% 하한 판단은 맞았다. `LEADING_FLOOR_PCT` 를 확정하고 근거 조문을 주석에 남겼다.

**더 중요한 것은 뒷문장이다.** 야간에 지상이면 역률이 **100%로 간주**되어 기준
95%를 넘으므로 **진상 추가요금이 0**이다. 대부분의 건물은 야간 경부하에서 지상이고,
진상은 **고정 콘덴서가 부하 대비 과다할 때** 생긴다. 그래서 이 조항은
"야간에도 역률을 관리하라"가 아니라 **"주간 역률을 맞추려 콘덴서를 키우면 야간에
되돌려 준다"** 는 뜻으로 읽어야 한다.

이에 따라 `leading_pct=None` 의 뜻을 바꿨다.

| | 이전 | 지금 |
|---|---|---|
| 의미 | "모르니 산출 안 함" + 경고 | **지상 간주 100%, 추가 0** (나목) |
| 추적성(5.8) | "야간 진상 미상" | "야간 지상 간주 100% (제43조 ② 2호 나목)" |
| 경고 | "진상역률을 알 수 없어…" | "고정 콘덴서를 크게 두었거나 키울 예정이면 진상 위험" |

역률 개선 수단(7.4)의 경고를 다시 썼다 — **APFR(자동역률조정장치)** 를 쓰면
부하에 따라 투입 단수가 조절되어 야간 진상을 피할 수 있다는 것을 넣었다.
**설치비는 만들지 않는다.** 설비 구성에 따라 달라 사용자 입력이다.

---

#### 1. 7장 번호 확정 (투자비 순)

DR 을 넣으면서 순서를 못 박았다. **이후 바꾸지 않는다.**

    7.1 선택요금 전환      투자 0
    7.2 계약전력 조정      투자 0
    7.3 경제성DR           투자 0   ← 신설
    7.4 역률 개선          저투자 (콘덴서·APFR)
    7.5 태양광
    7.6 ESS
    7.7 잉여 활용

코드 docstring 13곳과 요구사항서 참조를 함께 고쳤다.

---

#### 2. 경제성DR 참여 여력 (6.6 진단 + 7.3 수단)

근거는 `data\source\전력시장운영규칙.pdf` 제12장이다.
**신뢰성DR(의무감축)이 아니라 경제성DR(자발적 입찰)만 다룬다.**

**거래일 제약이 이 기능의 전부다 (제12.4.2.1조 제1항 1호)**

토·일·공휴일을 모두 뺀 평일에만 입찰할 수 있다. 일요일은 공휴일 규정에 포함되어
자동으로 빠진다.

| 샘플 | 값 |
|---|---|
| 전체 일수 | 359일 |
| **거래 가능일** | **245일 (68.2%)** |
| 제외일 (토·일·공휴일) | 114일 |

**전체 일수로 재면 46% 부풀려진다.** 회귀로 고정했다.

**요금 계량의 '평일' 과 다르다 — 여기가 조용히 틀리기 쉬운 곳이다**

| | 토요일 | 일요일 | 공휴일 |
|---|---|---|---|
| 요금 계량 | 최대→중간부하 (공휴일 아님) | 전량 경부하 | 전량 경부하 |
| **DR 거래일** | **제외** | 제외 | 제외 |

토요일에서 갈린다. `diagnose.dr.dr_day_mask` 를 `tariff.tou` 와 **따로 두었고**,
두 판정이 실제로 다른 결과를 낸다는 것을 테스트로 못 박았다.

**샘플 진단 결과 (6.6)**

| 항목 | 값 |
|---|---|
| 주간(9~18시) 평균 부하 | 3,967 kW |
| 하위 10% | 3,208 kW |
| 기저부하 (6.1 비율 65.0% 재사용) | 2,577 kW |
| **등록 가능 용량 (보수적)** | **631 kW** |
| 평균 기준 여력 (비교용) | 1,390 kW |
| 무비용 감축 가능일 | 13일 (기준선 2,516 kW) |
| 자원 유형 | 표준DR, 중소형DR |
| 적합성 | 높음 |

**등록 용량을 평균이 아니라 하위 10% 로 잡았다.** 요구사항의 "감축 여력 = 대상일
주간 평균 − 기저부하" 는 `mean_reducible_kw` 로 함께 내되, **등록값은 어느
대상일에나 지킬 수 있어야** 한다 — 평균으로 등록하면 절반의 날에 미달하고
미달은 위약금이다. "보수적으로 산정한다" 는 지시를 이렇게 해석했다.

**기저부하는 6.1 의 비율을 그대로 재사용한다.** 다시 계산하지 않는 것을
테스트로 고정했다 (`profile.base_load_ratio == pattern.base_load_ratio`).

**저부하일 기준선은 거래 가능일만으로 계산한다.** 전 일자로 재면 기준선이
2,516 → 더 낮게 내려가 평일 저부하일이 걸리지 않는다. 이 비교도 회귀로 넣었다.

**7.3 수단**

- 감축량은 거래 가능일 기준으로만. 245일 × 631 kW × 1h = **154,648 kWh** (상한)
- 무비용 감축 가능일만 참여하면 8,206 kWh
- **정산 단가 미입력 시 금액이 아니라 사유**를 돌려준다
- 위약금 리스크 (별표26): `(감축계획량 − 실제감축량) × Max(하루전에너지가격, 0)`
- **기본요금 절감은 계산하지 않는다** — SMP 기준 산발 입찰이라 참여일이 연중
  최대수요일과 겹칠 확률이 낮다
- 확실성 **'중간'**. 필수 안내: "경제성DR은 수요관리사업자를 통해서만 참여할 수
  있습니다…"

**범위 밖 (근거 조문과 함께 1.5절·부록 D 에 명시)**

- 신뢰성DR (의무감축)
- **플러스DR** — 제12.7.3.1조 제3호에 따라 표준DR·중소형DR·제주DR·국민DR·
  주파수DR 은 자발적 수요증대 거래에 참여할 수 없다
- **CBL(고객기준부하)** — 제12.5.2.1조에 따라 전력거래소가 별표28 방식으로 산정한다
- 참여 자격 판정

**만든·고친 파일**

- `src\kwise\diagnose\dr.py` — 신규. 거래일·자원 유형·감축 여력·저부하일
- `src\kwise\measures\demand_response.py` — 신규. 7.3 + 별표26 위약금
- `src\kwise\tariff\power_factor.py` — 나목 반영 (`deemed_leading_pct`,
  `LEADING_LAGGING_DEEMED_PCT`)
- `src\kwise\measures\power_factor.py` — APFR·고정 콘덴서 경고
- `src\kwise\diagnose
eport.py` — `Diagnosis.dr`
- `src\kwise
eport\excel.py`, `batch.py`, `notices.py` — 진단·수단 시트, 부록 D
- `tests\test_dr.py` — 33건
- 요구사항서 — 6.6·7.3 신설, 7장 번호 확정, 1.5 범위 밖 4건, 부록 D 9→11항목

**미해결** — 신규 없음. 직전 세션의 두 건(제38조 확인 / 실측 역률 확보)은 그대로

**다음 세션(7세션)이 알아야 할 것**

- **DR 은 케이스 축이 아니라 모든 케이스에 붙는다.** `diagnose()` 가 항상
  `Diagnosis.dr` 를 채우므로 케이스 스터디에서 거래 가능일 수만 확인하면 된다
- 부하 유형별로 DR 적합성이 갈린다. **야간 피크형은 주간 여력이 작아 등급이
  낮게 나온다** — 세 유형 비교에서 이 대비가 드러날 것이다
- DR 금액을 내려면 케이스 YAML 에 `dr_unit_price_won_per_kwh` 를 넣는다.
  없으면 감축량만 나오고 금액 칸에 사유가 들어간다
- 역률 케이스를 넣을 때 야간 진상은 **기본이 0** 이다. 진상 페널티를 보이려면
  `leading_power_factor_pct=` 를 명시적으로 줘야 한다

**테스트**
- pytest: 514 passed (직전 478 + 이번 36: DR 33 + 역률 나목 3)
- ruff: pass (check + format)
- mypy: pass (strict, 69 files)

---

### 보완 세션 — 전압구분 잠금과 역률 정정 (2026-08-07)

준비 세션이 남긴 미해결 항목 하나를 닫고, 원문 확인으로 드러난 오류 하나를 고쳤다.

---

#### 1. 전압구분 잠금

준비 세션에서 "7세션 케이스 스터디에서 뒤집히는지 보고 정한다" 로 미뤄 두었던
항목이다. **결과를 보고 판단할 문제가 아니었다.**

| 전압구분 | 수전 전압 |
|---|---|
| 고압A | 3,300 ~ 66,000 V |
| 고압B | 154,000 V |
| 고압C | 345,000 V |

수전설비가 물리적으로 다르다. 154 kV 수전 건물이 22.9 kV 로 바꾸려면 변전설비를
새로 지어야 하므로 요금제 비교에 끼울 대상이 아니다. 계약종별을 가둔 것과 같은
이유다 — **고객이 고르는 값이 아니다.**

`switchable_selections(table, current)` 를 만들어 현행 종별·전압에 가뒀다.
바꿀 수 있는 것은 선택요금(Ⅰ·Ⅱ·Ⅲ)뿐이다.

    diagnose()                 option_totals 가 6개 → 3개 (고압A 만)
    evaluate_tariff_switch()   quotes 가 6개 → 3개
    list_selections()          드롭다운(부록 A.3)용으로 그대로 둔다 — 전부 보여 준다

`option_totals` 에 다른 전압이 섞여 들어와도 최적으로 뽑지 않도록
`min(selections, ...)` 로 바꿨다. 이전에는 넘겨받은 사전 전체에서 최소를 골라
`StopIteration` 이 날 수 있었다.

---

#### 2. 역률 정정 — 요구사항서 5.7 이 틀렸다

`data\source\기본공급약관.pdf` 원문 확인 결과 초판의 **"지상 90% 미만"** 은
오류였다. 기준은 **92%** 이고, 미달 시 추가만이 아니라 **초과 시 감액도 있다.**

| 조문 | 내용 |
|---|---|
| 제41조 | 지상역률 **92% 이상** 유지 의무 |
| 제42조 | **30분 누적 계량**. 무효전력계 미설치 고객은 **92%로 간주** |
| 제43조 ① | 대상은 무효전력계 설치 고압 이상 일반용·교육용·산업용 — **우리 종별 전부** |
| 제43조 ② | 08~22시 지상 92% / 22~08시 진상 95%, 매 1%당 기본요금의 0.2% |
| 제43조 ③ | 추가요금 첫 달 예고, 둘째 달부터 청구 |

**금액이 커서 경고로 둘 수 없다** — 기본요금의 −1.0% ~ +6.4% 다.

| 역률 | 조정 | 샘플 금액 (기본요금 4.53억) |
|---|---|---|
| 60% (하한 클램프) | **+6.4%** | +2,898만원 |
| 85% | +1.4% | +634만원 |
| **92% (기본값)** | **0** | **0원** |
| 97% (감액 상한) | **−1.0%** | **−453만원** |
| 야간 진상 90% | +1.0% | +453만원 |

**기본값 92% 에서 조정액이 정확히 0 이다.** 지어낸 값이 아니라 제42조가
무효전력계 미설치 고객에게 적용하는 **간주값**이라 근거가 있고, 덕분에
**3세션 이래의 회귀값이 하나도 바뀌지 않았다** (기본요금 452,832,624원,
요금적용전력 5,293.44 kW, 12.00개월분). 역률을 모르는 채로 금액을 만들어내지
않으면서 기구는 살아 있다.

**만든·고친 것**

- `src\kwise\tariff\power_factor.py` — 신규. 제41·42·43조 산식 전부
  (`lagging_adjustment_ratio`, `leading_adjustment_ratio`, `power_factor_charge`)
- `src\kwise\tariff\engine.py` — `BillingOptions.power_factor_pct` /
  `leading_power_factor_pct`, `BillingResult.total_power_factor_won` /
  `power_factor`, 월별 `power_factor_won` 열, 추적성(5.8)에 적용 역률
- `src\kwise\measures\power_factor.py` — 신규. **7.3 역률 개선 수단**
- `src\kwise\measures\solar.py` — 판정 창을 **08~22시**로 (약관과 같은 창),
  `SolarPoint.power_factor_extra_won` / `saving_after_power_factor_won`
- `src\kwise
eport\excel.py`, `batch.py` — 요약에 역률요금 행,
  수단별 시트에 역률 개선 행, 명세에 `power_factor_won`·`base_demand_kw`
- `tests\test_power_factor.py` — 39건

**역률 개선 수단 (7.3)**

투자 0원(7.1·7.2) 다음, 태양광 앞이다. 확실성 **'높음'** — 요금표와 약관만으로
확정되고 발전량 예측 같은 불확실성이 없다. 감도(9.2)도 적용하지 않는다.

| 케이스 | 절감액 | 근거 |
|---|---|---|
| 92% → 97% | **452.8만원** (기본요금의 정확히 1.0%) | 감액 상한 |
| 85% → 97% | **1,086.8만원** (2.4%) | 추가 1.4% 제거 + 감액 1.0% |

절감액은 두 역률에서 각각 `calculate_bill()` 을 부른 **재계산**이다. 비율만
곱해 어림하지 않는다. 97%를 넘기면 요금이 더 줄지 않으므로 과투자 경고를 내고,
**콘덴서를 키우면 야간에 진상으로 넘어간다**는 경고(제43조 ② 95% 조항)를
언제나 함께 낸다.

**PV 도입 후 역률**

판정 창을 발전 시간대(≈06~19시)에서 **08~22시**로 바꿨다. 약관이 그 창의
지상역률로 요금을 매기기 때문이다. 창이 바뀌어 샘플 960 kWp 의 도입 후 역률이
90.0% → **90.17%** 가 되었다(수치만 바뀜, 판정은 그대로). 기준도 90 → **92%** 라
경고 범위가 넓어졌고, 이제 **예상 추가요금과 그만큼 깎인 절감액을 함께** 낸다.

**계산에 넣지 않은 것 — 제43조 ③**

첫 달 예고·둘째 달 청구를 넣으면 기간에 따라 금액이 달라져 도입 전후 비교(Δ)가
흔들린다. 주석과 부록 D 로만 남기고, 실제 첫 달 청구서와 어긋날 수 있음을
산출물에 적는다.

---

#### 3. 문서

- **요구사항서 5.7 전면 개정** — 제41·42·43조 원문 표, 구현 방침 6개, 한계
- **7장에 7.3 역률 개선 신설**, 이후 번호를 밀었다 (태양광 7.3→**7.4**,
  ESS 7.4→**7.5**, 잉여 7.5→**7.6**). 코드 docstring 11곳도 함께 고쳤다
- **5.2 에 갑 종별 기본요금 기준 주석** — 약관 제68조 ②의 기준은 갑/을 구분이
  아니라 **최대수요전력계 설치 여부**다. 일반용(갑)Ⅱ 는 시간대별 구분계량기
  설치 고객이라 최대수요계가 있을 수 있으므로 제38조 확인이 필요하다.
  미해결 항목에 올렸다
- 부록 D 가 7항목 → **9항목** (역률 관련 2건 추가)
- 3.2 입력 표에 주간 지상역률·야간 진상역률을 나눠 적었다

**미해결** — 위 「미해결 항목」의 신규 3건 (제38조 확인 / 야간 진상 하한 /
실측 역률 확보)

**다음 세션(7세션)이 알아야 할 것**

- **역률이 케이스 축으로 하나 늘었다.** 같은 부하라도 역률 85% 건물과 95% 건물은
  기본요금이 3.4%p 다르다. 케이스 스터디에 역률이 다른 두 건물을 넣으면
  "투자 없이 되는 것" 의 크기가 달라지는 것을 보일 수 있다
- **PV 케이스는 역률 악화분을 반드시 함께 본다.** `SolarPoint` 에
  `power_factor_extra_won` 과 `saving_after_power_factor_won` 이 있다.
  절감액만 보면 과대평가된다
- 갑 종별 케이스는 `BillingOptions(contract_kw=...)` 가 필수다 (없으면 멈춘다).
  다만 갑Ⅱ 의 기본요금 기준은 제38조 확인 전까지 잠정이다
- `switchable_selections()` 를 쓴다. `list_selections()` 는 드롭다운용이다

**테스트**
- pytest: 478 passed (준비 세션 436 + 이번 42: 역률 39 + 전압구분 3)
- ruff: pass (check + format)
- mypy: pass (strict, 66 files)

---

### 준비 세션 — 사용량 파서 강화와 요금표 전 종별 확장 (2026-08-07)

7세션 케이스 스터디의 전제 두 가지를 만들었다. 케이스마다 다른 양식의 파일이
올라오고 종별도 제각각이라, 둘 다 없으면 케이스 스터디가 시작되지 않는다.

**받은 원본 두 개를 저장소에 넣었다.** "이 단가가 어디서 왔나" 를 나중에
추적하려면 원본이 있어야 한다.

    tests\fixtures\사용량조회_양식.xlsx              한전ON 원본 4열 양식 (헤더 + 1행)
    data\source\2026_KEPCO_Electricity_Tariff.xlsx   전 종별 요금표 (6시트)

---

#### A. 사용량 파서 강화 (`kwise.io`)

한전ON 다운로드 원본은 **계기번호 · 고객번호 · 검침일 · 순방향 유효전력량(KWH)**
네 열이다. 지금까지 쓰던 샘플은 앞 두 열을 지운 것이라, 원본 양식 그대로의
경로가 회귀로 고정되어 있지 않았다. 3단으로 찾는다.

| 단 | 방법 | 실패하면 |
|---|---|---|
| 1 | 헤더 행 탐색 — 상위 10행 중 날짜 이름과 kWh 이름이 함께 있는 행 | 2단으로 |
| 2 | 이름 매칭 — NFKC 정규화 후 후보 사전과 대조 (정확 → 부분) | 3단으로 |
| 3 | **내용 기반** — 값으로 판정 | 사유를 적고 예외 |

**3단 판정 기준**

    시각 열   파싱 성공률 ≥95% + 등간격성(연속 차이 최빈값 점유율). 수치형은 감점
    값 열     수치 변환 ≥95% + 음수 없음 + 고유값이 상수가 아닐 것

`ColumnDetection` 으로 **어느 열을 무엇으로, 어느 단계에서** 인식했는지와 후보별
점수·사유를 돌려준다. `UsageMeta.columns` 에 실려 8세션 UI 가 사용자에게 보여
주고 고칠 수 있다. 3단으로 갔거나 후보가 우열을 못 가리면 경고를 붙인다.

**고객번호를 거른 방법 — 여기서 한 번 틀렸다가 고쳤다**

처음에 고유값 **비율** 5% 를 문턱으로 뒀다. 그런데 실측 샘플의 15분 kWh 는
34,358행에 고유값이 1,417개뿐이라 **비율이 4.1%** 다 — 진짜 사용량 열이 잘려
나간다. 비율이 아니라 **개수**로 잘라야 한다.

| 열 | 왜 후보가 아닌가 |
|---|---|
| 고객번호 | 전 파일이 한 값이라 고유값 **1개**. 개수 문턱(3)에 걸린다 |
| 계기번호 | `55-282007100` — 문자열이라 수치 변환이 실패한다 |

계기번호는 함정이 하나 더 있다. 로더 본체의 `parse_usage_energy` 는 숫자 아닌
문자를 **모두 지우므로** `55282007100` 이라는 멀쩡한 수가 된다. 그래서 판정
단계는 쉼표만 걷어내고 `pd.to_numeric` 에 맡긴다 (`columns._numeric`).

**제목 행 처리** — 화면 저장본은 헤더 위에 제목이 한 줄 붙는다. 열 수가 달라
CSV 는 그대로 읽으면 `ParserError` 가 난다. 앞부분을 훑어 열 개수를 세고
`names=` 로 넉넉히 준 뒤, 본문은 `skiprows` 로 다시 읽는다. 내용 기반 단계는
**열이 꽉 찬 행만** 헤더 후보로 삼는다 — 제목 행을 헤더로 잡으면 진짜 헤더가
데이터 첫 줄로 섞여 들어간다.

**CSV 인코딩 채택 규칙을 지켰다.** 1차는 이름 매칭만 허용한다. 내용 기반은
헤더가 깨져도 숫자가 멀쩡해 성공해 버려서, 잘못된 인코딩이 채택된다.
이름으로 아무 인코딩도 통과하지 못했을 때만 2차로 값 판정을 연다.

**만든·고친 파일**

- `src\kwise\io\columns.py` — 신규. 3단 판정 전부
- `src\kwise\io\usage.py` — `_read_raw`/`_read_body` 2회 읽기, `UsageMeta.columns`
- `tests\test_io_columns.py` — 25건
- `tests\_synthetic.write_kepco_file()` / `kepco_month_rows()`

**검증한 경로** — 8가지 조합(csv/xlsx × 정상·제목행·알아볼 수 없는 헤더)이
모두 2,976 슬롯으로 읽힌다. **실측 샘플(2열 CSV)은 부록 B 와 전부 그대로다**
(34,358행 / 972결측 / 5,293.44 kW @ 07-03 09:30 / 22,284.8 MWh / 부하율 49.0%).

---

#### B. 요금표 전 종별 확장 (`data\tariff_kr_20260601.json`)

**수기 입력하지 않았다.** 엑셀을 읽는 변환기를 만들고 돌렸다.

    src\kwise\tariff\source_excel.py   변환 로직 (테스트 대상)
    tools\build_tariff.py              CLI. --check / --contracts 로 단계 진행

**주의 1 — 시트마다 계절 열 순서가 다르다**

| 시트 | 5열 | 6열 | 7열 |
|---|---|---|---|
| 일반용·교육용 | 여름철 | 봄·가을철 | 겨울철 |
| **산업용** | **봄·가을철** | **여름철** | 겨울철 |

위치로 읽으면 산업용의 여름과 봄·가을이 통째로 뒤바뀐다. 3행 헤더를 읽어
이름으로 매핑한다 (`season_columns`). 검산도 회귀로 고정했다 — 산업용(을)
고압A 선택Ⅰ 최대부하 **여름 234.5 / 봄가을 156.4**. 뒤바뀌면 봄·가을 단가가
여름보다 비싸져 **태양광 평가가 정반대로 나온다.**

**주의 2 — 엑셀에 없는 것을 덮어쓰지 않았다**

엑셀에는 단가 테이블과 계절·시간대 구분만 있다. 아래는 `source_excel` 이
코드로 들고 있고 각각 테스트가 붙어 있다.

    토요일 peak_to_mid / 일요일·공휴일 all_to_light
    산업용(을) 봄·가을 주말 할인 특례 (토·일·공휴일 11~14시 50%)
    요금적용전력 3규칙 (경부하 제외 / 대상월 7·8·9·12·1·2 / 하한)
    임계값 — 일반용·산업용 300 kW, **교육용 1,000 kW**
    contract_floor_ratio — 을 종별 30%, **교육용(을) 15% 특례**

**주의 3 — 검증을 건너뛰지 않았다**

부록 A.2 4규칙을 8종별 전부에 돌려 통과시켰다. 변환기는 검증에 걸리면
**파일을 쓰지 않는다.** 기존 일반용(을) 고압A·B **60개 값(기본 6 + 전력량 54)
대조**도 변환기 안에 넣었고 테스트로도 고정했다 — 하나도 바뀌지 않았다.

**넣은 종별과 그 결과**

| 종별 | 키 | 전압 | 기본요금 기준 | 시간대 | 하한 |
|---|---|---|---|---|---|
| 일반용(갑)Ⅰ | `general_a_1` | 저압·고압A·B | 계약전력 | 전체시간 | — |
| 일반용(갑)Ⅱ | `general_a_2` | 고압A·B | 계약전력 | 시간대별 | — |
| 일반용(을) | `general_b` | 고압A·B | 요금적용전력 | 시간대별 | 30% |
| 산업용(갑)Ⅰ | `industrial_a_1` | 저압·고압A·B | 계약전력 | 전체시간 | — |
| 산업용(갑)Ⅱ | `industrial_a_2` | 고압A·B | 계약전력 | 시간대별 | — |
| 산업용(을) | `industrial_b` | 고압A·B·**C** | 요금적용전력 | 시간대별 | 30% |
| 교육용(갑) | `education_a` | 저압·고압A·B | 계약전력 | 전체시간 | — |
| 교육용(을) | `education_b` | 고압A·B | 요금적용전력 | 시간대별 | **15%** |

농사용·가로등·심야·전기차·보완전력·주택용은 범위 밖이라 넣지 않았다 (부록 A.4).

**확장하면서 드러난 것 세 가지 — 코드도 고쳤다**

① **갑 종별의 기본요금은 계약전력 기준이다** (기본공급약관 제68조). 요금적용전력으로
매기면 기본요금이 통째로 틀린다. `ContractType.base_fee_basis` 를 만들고 엔진이
분기하게 했다. 계약전력을 모르면 **만들어내지 않고 멈춘다.** 월별 행에
`base_demand_kw`(기본요금에 실제로 곱한 kW)를 추가했다.

② **'전체시간' 종별은 시간대 구분이 없다.** 세 시간대에 같은 단가를 채우고
`time_of_use=False` 로 표시한다. 검증 규칙 1 의 `경<중간<최대` 대신
**세 칸이 같은지**를 검사한다. 규칙 2(균일성)도 끈다 — 전체시간 종별은
선택Ⅰ→Ⅱ 할인폭을 계절별로 다르게 설계하므로(일반용(갑)Ⅰ 고압A: 여름 4.0 /
봄가을 4.3 / 겨울 5.3원) 요금표가 약속하지 않은 것을 검사하는 셈이 된다.
손익분기(규칙 3)는 그대로 본다.

③ **선택요금 전환이 계약종별을 넘나들고 있었다.** `list_selections(table)` 을
필터 없이 돌아, 종별이 하나일 때는 안 보이던 결함이 8종별이 되자 드러났다.
일반용(을) 건물에 산업용(을) 을 권할 수는 없다 — 용도로 정해지는 계약이지
선택요금이 아니다. `diagnose` 와 `evaluate_tariff_switch` 를 현행 종별로 가뒀다.
**전압구분은 아직 넘나든다** — 위 「미해결 항목」에 남겼다.

**만든·고친 파일**

- `src\kwise\tariff\source_excel.py` — 신규. 변환 로직과 `CONTRACT_RULES`
- `tools\build_tariff.py` — 신규. CLI + 검증 + 60개 값 회귀 대조
- `data\tariff_kr_20260601.json` — 8종별로 재생성 (schema 0.1 → **0.2**)
- `src\kwise\tariff\schema.py` — `base_fee_basis`, `time_of_use`
- `src\kwise\tariff\validate.py` — 전체시간 종별 처리, 산업용(을) 정책 5쌍
- `src\kwise\tariff\engine.py` — 계약전력 기준 기본요금, `base_demand_kw`
- `src\kwise\diagnose\report.py`, `src\kwise\measures\tariff_switch.py` — 종별 한정
- `tests\test_tariff_source.py` — 28건
- `pyproject.toml` — mypy 검사 대상에 `tools` 추가

**미해결**

- 위 「미해결 항목」의 두 신규 항목 (전압구분 넘나듦 / 엑셀 옮김 오차)
- 요금 데이터는 여전히 `verified: false`

**다음 세션(7세션)이 알아야 할 것**

- **케이스 스터디에 부하 유형 세 가지를 넣는다.** 여름 주간 피크형(샘플),
  봄·가을 피크형, 야간 피크형. 6세션 (1) 에서 확인한 정정 ①·②의 비대칭은
  세 유형이 다 있어야 드러난다. 야간 피크형 생성기는
  `tests\_synthetic.night_peak_month()` 에 있다
- **이제 종별도 케이스 축이다.** 산업용(을) 봄·가을 주말 할인은 PV 최성기와
  겹치므로, 같은 부하라도 일반용(을) 과 산업용(을) 의 PV 절감액이 다르게 나온다.
  이 대비를 케이스로 넣으면 특례 반영 여부를 실증할 수 있다
- 갑 종별 케이스를 넣으려면 `BillingOptions(contract_kw=...)` 가 **필수**다.
  없으면 `TariffDataError` 로 멈춘다 (일부러 그렇게 두었다)
- 요금표를 갱신할 때는 엑셀을 바꾸고 `tools\build_tariff.py` 를 다시 돌린다.
  JSON 을 손으로 고치면 `test_shipped_json_equals_a_fresh_conversion` 이 잡는다
- 새 종별을 넣을 때는 `source_excel.CONTRACT_RULES` 에 규칙을 먼저 적는다.
  엑셀에 있어도 규칙이 없으면 변환기가 거부한다 (조용히 기본값을 만들지 않는다)
- 업로드 파일의 열 판정 결과는 `usage.meta.columns` 에 있다. 8세션 UI 는
  `describe()` 를 보여 주고 후보 목록으로 드롭다운을 만들면 된다

**테스트**
- pytest: 436 passed (6세션 382 + 이번 54: 파서 25 + 요금표 28 + 데이터 1)
- ruff: pass (check + format)
- mypy: pass (strict, 63 files — `tools` 포함)

---

### 6세션 (3) — 산출물 회귀와 경부하 제외 (2026-08-07)

6세션 (2) 가 남긴 두 항목을 마무리했다. **6세션은 여기서 끝난다.**

**① `tests\test_report.py` — 36건**

수동으로만 돌려 보던 Excel·CLI 를 회귀로 고정했다.

| 묶음 | 내용 |
|---|---|
| 시트 구성 | 8시트 순서, openpyxl 왕복 후에도 동일, 없는 조각은 **빈 장이 아니라 시트 자체를 만들지 않음**, `--no-timeseries` 로 35,328행 생략 |
| tz 해제 | tz-aware 인덱스·컬럼이 섞인 프레임 저장 시 예외 없음, 원본 불변, Period 인덱스 → 문자열 |
| 요약 필수 문구 | 추적성 5줄(5.8), 미포함 요금요소(5.1), 계약전력 변경 경고(9.4), **알려진 한계 8항목 전부**(부록 D), 결측 미보간, 조합 재계산 주석, 태양광 판정 모집단 |
| 미산출 표기 | `format_won(None)` 이 빈칸이 아니라 사유. 감도 시트도 빈 장 대신 사유 |
| 쓰기 실패 | `PermissionError` → `ReportWriteError("Excel 에서 파일을 닫아 주세요")`. 파일명 접미사 `result_20260807_1430.xlsx` |
| 조합 재계산 | 조합 비교 시트의 `+ 태양광` 절감액 ≠ 요금제 절감 + PV 단독 절감 (차이가 100만원 이상이라 어림 오차가 아니다) |
| 확실성 | 시트에 실린 등급이 기준선·요금제 높음 / +PV 중간 / +PV+ESS **중간~낮음**. 수단별 시트는 각자 등급 |
| CLI | `CliRunner` 로 2케이스 YAML 정상 종료(exit 0), 케이스별 xlsx 2개 + 요약 CSV 1개, CSV 가 **utf-8-sig BOM**, 한글 케이스명 유지, `--resume` 이 두 건 모두 건너뜀 |

CLI 테스트는 합성 한 달치 파일 두 개를 쓴다. 네트워크도 태양광도 타지 않는다.
`PROJECT_CACHE` 를 `monkeypatch.setenv` 로 tmp 에 돌려 저장소를 더럽히지 않는다.

**② 태양광 피크 기여 판정에서 경부하 제외**

`top_slots` 산정에 `demand_eligible` 마스크를 적용했다. 경부하 구간은 애초에
요금적용전력 대상이 아니므로 그 시각에 피크가 몰려도 태양광의 기본요금 기여와
무관하다.

`PeakProfile` 이 상위 구간을 **두 벌** 들고 있게 했다. 섞지 않는다.

| 필드 | 모집단 | 쓰임 |
|---|---|---|
| `top_slots` / `hour_counts` / `weekday_counts` | 관측 전 슬롯 | **부록 B 시각 분포 (라벨 기준 원값)** |
| `demand_top_slots` / `demand_hour_counts` / `demand_weekday_counts` | 요금적용전력 대상 슬롯 | **태양광 등급 판정** |

`hour_share()` 는 원값, `demand_hour_share()` 는 대상 기준이고
`judge_pv_potential()` 은 후자를 쓴다. 마스크를 주지 않으면 두 벌이 같아진다.
판정 모집단은 `ImprovementSummary.pv_basis` 에 문장으로 남아 요약 시트에 실린다.

**샘플에서 결과가 바뀌지 않는다 — 회귀로 고정**

상위 100구간이 07~17시라 딱 한 칸만 교체된다. 라벨 `07:45`(구간 시작 07:30)가
경부하라 빠지고 그 자리를 `09:45` 가 채운다.

| | 시각 분포 | 정오 비율 | 등급 |
|---|---|---|---|
| 전 슬롯 (부록 B) | 7시 1, 8시 6, 9시 5, … | 66% | 높음 |
| 요금적용전력 대상 | 8시 6, **9시 6**, … (7시 없음) | **66%** | **높음** |

**야간 피크형에서는 등급이 뒤바뀐다 — 합성 픽스처로 검증**

`tests\_synthetic.night_peak_month()` — 야간(경부하) 2,000 kW, 정오 1,200 kW,
나머지 200 kW 인 한 달치.

| 모집단 | 정오 비율 | 등급 |
|---|---|---|
| 전 슬롯 | 0% | **낮음** |
| 요금적용전력 대상 | 95% | **높음** |

이 건물의 관측 최대는 2,000 kW 지만 요금적용전력은 1,200 kW 다. 야간 피크는
기본요금에 닿지 않으므로 태양광 판정에서도 빼는 것이 맞다. 마스크를 씌워도
`hour_counts`(원값)는 그대로라는 것도 함께 고정했다.

**고친 파일**

- `src\kwise\diagnose\peak.py` — `_rank_slots` 분리, `demand_*` 세 필드,
  `demand_hour_share()`, `demand_weekend_slots`, `demand_eligible_slots/applied`
- `src\kwise\diagnose\summary.py` — `judge_pv_potential` 이 대상 모집단을 쓴다.
  `pv_basis_label()` 신규, `ImprovementSummary.pv_basis`
- `src\kwise\diagnose\report.py`, `__init__.py` — 배선
- `src\kwise\report\excel.py` — 요약에 '태양광 판정 모집단' 행, 진단 시트에
  두 분포를 `(전 슬롯)` / `(요금적용전력 대상)` 라벨로 나란히

**만든 파일**

- `tests\test_report.py` (36건)
- `tests\_synthetic.night_peak_month()`
- `tests\test_diagnose.py` 에 5.2 ① 절 6건 추가

**미해결**

- 없다. 6세션 범위 항목은 모두 끝냈다. 저장소 전체 항목은 위 「미해결 항목」 참조

**다음 세션(7세션 — 검증·케이스 스터디)이 알아야 할 것**

- **케이스 스터디에 부하 유형 세 가지를 넣는다.** 여름 주간 피크형(샘플),
  봄·가을 피크형, 야간 피크형. 6세션 (1) 에서 확인한 대로 정정 ①(경부하 제외)과
  ②(대상월 한정)가 PV 에 **반대 방향**으로 작용하며 어느 쪽이 지배하는지는
  부하 형태에 달렸다. 세 유형이 다 있어야 이 비대칭이 드러난다
- 야간 피크형 합성 부하는 `tests\_synthetic.night_peak_month()` 에 이미 있다.
  케이스 스터디용으로 규모만 키우면 된다
- 배치 CLI 는 `run_batch(config, resume=True)` 로 이어 돌릴 수 있다.
  중간 결과는 `PROJECT_CACHE\batch\<정의파일>\<케이스>.json`
- Excel 산출물의 '진단' 시트에는 시각 분포가 두 벌 실린다.
  UI(8세션)에서 표로 그릴 때 어느 쪽을 쓸지 명시할 것 — 부록 B 대조는 전 슬롯,
  태양광 판단 근거는 요금적용전력 대상
- 요금 데이터는 여전히 `verified: false` 다. 청구서를 확보하면 일요일 경부하
  계량부터 대조한다

**테스트**
- pytest: 382 passed (1~6세션 340 + 이번 42)
- ruff: pass (check + format)
- mypy: pass (strict, 58 files)

---

### 6세션 (2) — 조합 비교·출력 (2026-08-06)

요금적용전력 정정을 마친 뒤 이어서 작업했다. 코드는 모두 커밋되어 있고
`tests\test_report.py` 만 남겨 두었다 — **6세션 (3) 에서 마무리했다.**

**만든 파일**

- `src\kwise\compare\combination.py` — `CombinationSpec`, `evaluate_combination`,
  `compare_combinations`, `default_combinations`
- `src\kwise\compare\sensitivity.py` — 조합 하나를 감도 3종으로 평가
- `src\kwise\report\excel.py` — 여덟 시트, tz 해제, 날짜·시각 접미사
- `src\kwise\report\notices.py` — 5.1·9.4·부록 D 문구, 미산출 사유 표기
- `src\kwise\report\batch.py` — YAML 케이스 순차 실행, 재개, 요약 CSV
- `src\kwise\cli.py` — `python -m kwise.cli run --cases cases.yaml`
- `tests\test_compare.py` — 26건

**확인한 것**

- 조합 절감액이 개별 합과 다르다 (재계산이 실제로 일어난다)
- 확실성 등급이 최저 구성 요소를 따른다 (기준선·요금제 높음 / +PV 중간 / +ESS 중간~낮음)
- ESS 야간 충전이 새 피크를 만드는 경로를 재현하고 경고·`charge_limit_kw` 로 막았다
- CLI 2케이스 YAML 이 끝까지 돈다 (수동 확인. 테스트로는 미고정)
- Excel 8시트가 35,328행 시계열까지 1.5초에 저장된다 (수동 확인)

**당시 안 한 것** — 모두 6세션 (3) 에서 처리했다

- ~~`tests\test_report.py`~~ — 36건으로 작성
- ~~Excel·CLI 가 회귀로 고정되지 않음~~ — `CliRunner` 2케이스와 openpyxl 왕복으로 고정
- ~~태양광 피크 기여 판정에서 경부하 제외~~ — `demand_top_slots` 로 분리

---

### 6세션 (1) — 요금적용전력 규칙 정정 (2026-08-06)

요구사항서 5.2 절이 전면 개정되어(한전 기본공급약관 제68조) 3세션 요금 엔진의
빠진 규칙을 먼저 고쳤다. 조합 비교·출력 작업은 그대로 두고 이어간다.

**고친 것**

- ① 대상 시간대 — 중간·최대부하만. 경부하(22:00~08:00) 수요는 아무리 커도
  요금적용전력이 되지 않는다. 공휴일은 전량 경부하로 계량되어 자동 제외된다
- ② 대상 월 — `max(7·8·9월, 12·1·2월, 검침 당월)`. 3~6월·10~11월 피크는
  당월분으로만 잡히고 이월되지 않는다
- ③ 하한 — 계약전력의 30% (종별 속성, 교육용 등 15% 특례)

**만든·고친 파일**

- `src\kwise\tariff\demand.py` — 신규. `is_demand_month`, `demand_eligible_mask`,
  `monthly_demand_basis`, `billing_demands`, `apply_contract_floor`
- `data\tariff_kr_20260601.json` — 종별에 `demand_bands`·`demand_months`·
  `contract_floor_ratio` 추가
- `src\kwise\tariff\schema.py`, `engine.py` — 규칙 적용, 월별 행에
  `demand_basis_kw`·`demand_before_floor_kw` 추가
- `src\kwise\diagnose\peak.py`, `report.py` — 진단도 같은 규칙으로
- `src\kwise\measures\contract.py` — 하한 기본값을 종별 속성에서 가져온다

**샘플 재계산 결과 (일반용(을) 고압A 선택Ⅰ, 계약전력 5,500 kW)**

| 항목 | 정정 전 | 정정 후 | 비고 |
|---|---|---|---|
| 요금적용전력 | 5,293.44 kW | **5,293.44 kW** | 변화 없음 |
| 기본요금 합계 | 452,832,624 원 | **452,832,624 원** | 변화 없음 |
| PV 480 kWp 기본요금 절감 | 11,652,310 원 | **17,401,507 원** | **+49%** |
| PV 960 kWp 기본요금 절감 | 20,380,190 원 | **31,518,402 원** | +55% |
| 계약전력 조정 절감 (5,500 kW) | 미산출 | **0 원** | 하한 1,650 kW 가 안 걸린다 |
| 계약전력 조정 절감 (20,000 kW) | 미산출 | **67,007,376 원** | 하한 6,000 kW 가 걸린다 |

**요금적용전력이 그대로인 이유**

이 건물의 연간 최대(2023-07 5,293.4 kW @ 09:30)가 **대상월이면서 중간부하**라
세 규칙 어디에도 걸리지 않는다. 월별 최대 13개가 모두 08:15~16:30 사이의
중간·최대부하 시간대라 경부하 제외의 영향도 없었다. 하한 1,650 kW 는 훨씬 아래다.
**정정의 효과는 부하 형태에 달렸다** — 야간 피크형이나 봄·가을 피크형 건물에서는
결과가 크게 달라진다.

**PV 기본요금 절감이 오히려 늘어난 이유 (예상과 반대)**

요구사항서는 "봄·가을 피크 저감은 기본요금 가치가 거의 없다"고 경고하지만,
이 건물에서는 절감액이 **줄지 않고 49% 늘었다.** 근거는 데이터에 있다.

PV 960 kWp 도입 후 2023-08 의 **관측** 최대는 5,090.0 kW 인데 요금적용전력은
4,917.9 kW(7월분)다. 8월의 관측 최대가 경부하 시간대에 있어 대상에서 빠졌기 때문이다.
정정 전 엔진은 PV 가 손댈 수 없는 야간·공휴일 피크를 요금적용전력으로 삼아
PV 의 기여를 깎아먹고 있었다. 2023-06(비대상월) 피크 4,934.5 kW 가 7월로 이월되지
않는 것도 확인된다.

즉 **①이 PV 에 유리하게, ②가 불리하게 작용하는데 이 건물에서는 ①이 훨씬 컸다.**
봄·가을 피크형 건물이라면 ②가 지배해 반대 결과가 나온다. 이 비대칭을 산출물
주석으로 넣었다.

**테스트**
- 신규 회귀 8건 (대상월 vs 계절 경계, 이월 안 됨, 경부하 제외, 공휴일 자동 제외,
  하한 적용, 초과사용부가금 검사)
- 정정으로 뒤집힌 기존 테스트 4건을 새 규칙으로 갱신
- pytest 340 passed / ruff pass / mypy strict pass

---

### 5세션 — 개선 수단 평가 (2026-08-06)

**완료**

- `kwise.measures` — 7.1~7.5 전체. 투자비 낮은 순으로 배치
- 절감액은 모두 재계산이다. 수단을 적용한 15분 부하를 만들어 `calculate_bill()` 을 다시 부른다
- 단위테스트 44건 추가 (누적 313건)

**만든 파일**

- `src\kwise\measures\base.py` — 확실성 등급, 12개월 환산, 회수기간
- `src\kwise\measures\netload.py` — `with_load`, `apply_generation` (순부하·잉여 분리)
- `src\kwise\measures\tariff_switch.py` — 7.1
- `src\kwise\measures\contract.py` — 7.2
- `src\kwise\measures\solar.py` — 7.3 용량 곡선, 역률 악화 판정
- `src\kwise\measures\ess.py` — 7.4 초과 분석·디스패치·경제성
- `src\kwise\measures\surplus.py` — 7.5
- `tests\test_measures.py`

**샘플 평가 결과 (일반용(을) 고압A 선택Ⅰ 기준선)**

| 수단 | 결과 |
|---|---|
| 7.1 선택요금 전환 | 선택Ⅱ 로 **5,358만원** 절감. 투자 0원, 확실성 높음 |
| 7.2 계약전력 조정 | 계약 7,000 kW 가정 시 하향 여지 1,177 kW. 하한 미확인이라 **금액 미산출**. 하한 100% 를 주면 1억 198만원으로 재계산됨 |
| 7.3 태양광 | 옥상 20,000 m² → 상한 960 kWp. 자가소비율 100%(부하가 커서 잉여 없음). 역률 92.0% → **90.0%** 로 떨어져 콘덴서 경고 발생 |
| 7.4 ESS (목표 5,200 kW) | 출력 **93.4 kW** / 용량 **41.1 kWh** — 분리해서 낸다. 절감 670만원, 회수 2.5년, 손익분기 단가 163만원/kWh |
| 7.5 잉여 활용 | 샘플은 잉여 0 kWh. 합성 케이스(부하 100 kW × PV 1,000 kWp)로 시나리오 3종을 검증 |

**부록 B 목표 피크별 초과 분석 — 회귀로 고정**

| 목표 | 구간 | 시간 | 최대 초과 출력 | 총 초과 에너지 |
|---|---|---|---|---|
| 5,200 kW | 13 | 3.25 h | 93.4 kW | 91.3 kWh |
| 5,000 kW | 496 | 124.0 h | 293.4 kW | 9,743.0 kWh |
| 4,800 kW | 1,225 | 306.25 h | 493.4 kW | 52,964.2 kWh |
| 4,500 kW | 2,160 | 540.0 h | 793.4 kW | 178,288.6 kWh |

**결정 사항** (위 「결정 사항」 5세션 항목에도 반영)

- 4세션이 남긴 "순부하를 어떻게 넘길지" 를 **새 `UsageData` 를 만드는 쪽**으로 정했다
- ESS 용량 산정 기준을 '하루 최대' 로 잡았다. 처음 '연속 구간 최대' 로 두었더니
  샘플 5,200 kW 목표에서 달성 피크가 5,244 kW 로 목표를 놓쳤다
- ESS 는 전력량요금도 소폭 절감된다. 최대·중간부하에 방전하고 경부하에 충전하므로
  단가 차이가 왕복손실(12%)보다 크기 때문이다. 요금표에 달린 부호라 테스트에 근거를 적어 두었다

**미해결**

- 계약전력 하한 규정 (기존). measures 는 값을 받을 준비가 끝났다.
  `evaluate_contract_adjustment(contract_floor_ratio=...)` 에 넣으면 바로 재계산된다
- Open-Meteo 일사량 라벨 규약 (기존)

**다음 세션이 알아야 할 것**

- **조합 비교(8장)는 수단을 겹칠 때마다 요금을 다시 계산한다.** `apply_generation()` 으로
  PV 를 뺀 뒤 그 결과에 `dispatch_peak_shaving()` 을 물리고, 마지막에 `calculate_bill()` 을
  한 번 부른다. 수단별 절감액을 더하면 안 된다
- 확실성 등급은 `measures.Certainty` 에 있다 (높음/중간/중간~낮음).
  각 수단 결과가 `certainty` 필드로 들고 있으므로 비교표에 그대로 옮기면 된다
- 각 수단 결과에 `warnings`·`notes` 가 있다. Excel 요약 시트에 그대로 싣는다
- Excel 출력 시 `BillingResult.monthly` 는 Period 인덱스다. `to_timestamp()` 로 바꾸고
  tz-aware 컬럼은 `dt.tz_localize(None)` 로 푼다
- 감도 3종은 PV 출력에만 적용한다. 7.1·7.2 결과는 감도와 무관하다는 것을 테스트로 고정했다
- 케이스 스터디(7세션)용 합성 부하 생성기는 `tests\_synthetic.py` 에 있다

**테스트**
- pytest: 313 passed (1~4세션 269 + 이번 44)
- ruff: pass (check + format)
- mypy: pass (strict, 49 files)

---

### 4세션 — 태양광과 진단 (2026-08-06)

**완료**

- `kwise.pv` — 기상 취득·캐시, 다중 어레이 시뮬레이션, 시각 정렬, 감도 3종
- `kwise.diagnose` — 6.1~6.5 전체. 설비 정보를 받지 않는다
- 단위테스트 69건 추가 (누적 269건)

**만든 파일**

- `src\kwise\pv\config.py` — `ArrayConfig`(지붕·벽면), `PvSystemConfig`
- `src\kwise\pv\weather.py` — `load_weather`, parquet 캐시, Open-Meteo
- `src\kwise\pv\simulate.py` — `simulate` (infinite_sheds, GCR 단일 파라미터)
- `src\kwise\pv\alignment.py` — `align_simulation`, `daylight_mask`, `slot_midpoints`
- `src\kwise\pv\sensitivity.py` — 감도 3종
- `src\kwise\diagnose\peak.py` / `contract.py` / `structure.py` / `summary.py` / `report.py`
- `tests\test_pv.py`, `tests\test_diagnose.py`

**시각 정렬 검증 결과 — 여기가 이번 세션의 핵심이다**

맑은 날 남향 어레이의 발전 무게중심은 남중 시각과 같아야 한다. 서울(127.0°E)
2023-07-03 의 남중은 12:36:10 이다.

| 정렬 방식 | 무게중심 | 남중과의 차이 |
|---|---|---|
| **구간 중앙 보정 (채택)** | 12:36:10 | **0.17초** |
| 반 칸 빠뜨림 (라벨을 순시각으로) | 12:43:41 | 7.5분 |
| 한 칸 밀림 | 12:51:10 | 15분 |

세 경우를 모두 테스트로 고정했다. 오차 1분 임계면 반 칸 실수도 걸린다.
일출·일몰 대조도 함께 넣었다 — 시간별을 15분으로 선형 보간하면 일몰 뒤로
발전량이 새므로 태양 고도로 잘라냈다.

**샘플 진단 결과 (일반용(을) 고압A 선택Ⅰ, 계약전력 5,500 kW)**

| 항목 | 값 |
|---|---|
| 월별 최대수요 13개 | 부록 B 표와 전부 일치 |
| 요금적용전력 | 5,293.44 kW (8월 최대수요 5,288 kW 보다 큼 — 12개월 규칙) |
| 상위 100구간 시각 | 10시 15개, 11시 14개, 12시 20개 — 부록 B 와 일치 |
| 상위 100구간 요일 | 월25 화32 수12 목10 금21, **주말 0** |
| 태양광 피크 기여 | **높음** (상위 구간의 66%가 10~15시) |
| 기본요금 : 전력량요금 | 13.5% : 86.5% |
| 시간대별 사용량 | 경 39.8% / 중간 38.3% / 최대 21.9% |
| 선택요금 전환 | 선택Ⅱ 로 **5,358만원** 절감 (조합마다 재계산) |
| 계약전력 | 여유율 96.2% — 하향 여지 없음 (오히려 여유가 10% 미만) |

**결정 사항** (위 「결정 사항」 4세션 항목에도 반영)

- 부록 B 의 시각 분포는 **검침 라벨 기준**이었다 (구간 시작 기준으로 세면
  9시 5→10개, 10시 15→14개로 달라진다). 요금 귀속만 구간 시작으로 하고
  피크 발생 시각 보고는 라벨로 한다. `top_slots` 에 두 값을 모두 담았다
- 계약전력 조정 절감액은 하한 규정을 모르는 한 산출하지 않는다.
  하향 여지(kW)와 위약금 경고는 언제나 낸다
- PowerShell 폴백은 유지하기로 했다 (3세션까지 미결 항목)

**미해결**

- 계약전력 하한 규정 (기존 항목). 6.4 절감액이 여기 걸려 있다
- Open-Meteo 일사량 라벨 규약 (신규). 실측 발전 데이터로 대조해야 확정된다

**다음 세션이 알아야 할 것**

- **PV 절감액은 빼기가 아니라 재계산이다.** `align_simulation()` 으로 부하 라벨에
  맞춘 뒤 `net_kw = load_kw − pv_kw` 를 만들고, 그 시계열로 `calculate_bill()` 을
  다시 부른다. PV 가 사용량을 줄이면 최적 선택요금이 바뀐다
- 순net 부하로 요금을 계산할 때 전력량은 `UsageData.energy_kwh()` 규약을 지켜야 한다.
  PV 를 뺀 새 `UsageData` 를 만들지, 요금 엔진에 시계열을 직접 넣을지는 5세션에서 정한다
- 감도는 PV 출력에만 적용한다. `iter_scenarios()` 는 제너레이터이므로
  시나리오별 요금 재계산도 순차로 돌리고 요약만 남긴다
- 용량 곡선(7.3)은 `PvSystemConfig.scaled(kwp)` 로 비율을 유지한 채 크기만 바꾼다.
  20단계를 순차 처리한다
- ESS 목표 피크 역산(7.4)은 `PeakProfile` 의 월별 최대수요와
  `billing_demands()` 를 쓰면 된다. 초과 구간·에너지는 부록 B 에 기대값이 있다
- 기상 캐시는 `PROJECT_CACHE\weather\openmeteo_*.parquet`. 테스트는
  `load_weather(fetch=...)` 로 합성 데이터를 넣어 네트워크를 타지 않는다
- `tests\_synthetic.clearsky_weather()` 가 맑은 날 합성 기상을 만든다

**테스트**
- pytest: 269 passed (1~3세션 200 + 이번 69)
- ruff: pass (check + format)
- mypy: pass (strict, 41 files)

---

### 3세션 — 요금 엔진 (2026-08-06)

**완료**

- 요금 데이터 `data\tariff_kr_20260601.json` (부록 A.1 스키마, 일반용전력(을) 고압A·B)
- 부록 A.2 검증 규칙 4종을 검증기와 테스트 양쪽으로 구현
- 시간대 분류기 — 계절·시간대·요일 규칙, 공휴일, 임시공휴일 추가·제외, 특례 할인
- 요금 계산기 — 월별 최대수요, 요금적용전력 12개월 규칙, 기본요금·전력량요금,
  결측 월 두 기준, 부분 월 처리, 12개월 환산, 추적성
- `energy_kwh()` 의 kW 환산 차단 (2세션 장치의 나머지 절반)
- 단위테스트 130건 추가 (누적 200건). 요구사항서 11.2 항목을 모두 덮었다

**만든 파일**

- `data\tariff_kr_20260601.json`
- `src\kwise\tariff\schema.py` — 로딩·조회·드롭다운 생성 (부록 A.3)
- `src\kwise\tariff\validate.py` — 부록 A.2 규칙 1~4
- `src\kwise\tariff\holiday.py` — 공휴일 달력 (`calendar` 는 표준 라이브러리와 겹쳐 피했다)
- `src\kwise\tariff\tou.py` — `classify_slots`
- `src\kwise\tariff\engine.py` — `calculate_bill`, `billing_demands`
- `tests\test_tariff_data.py`, `tests\test_tariff_tou.py`, `tests\test_tariff_engine.py`

**요금표 검증 결과 (부록 A.2)**

| 규칙 | 결과 |
|---|---|
| 1 순서 관계 | 통과 — 경<중간<최대, 기본 Ⅰ<Ⅱ<Ⅲ, 전력량 Ⅰ>Ⅱ>Ⅲ, 여름 최대>봄가을 최대 |
| 2 균일성 | 고압A Ⅰ→Ⅱ 전 칸 5.5원 / 고압B Ⅰ→Ⅱ 3.8원 / 고압B Ⅱ→Ⅲ 1.6~1.7원 |
| 2 예외 | 고압A Ⅱ→Ⅲ 0.6~12.4원 — 최대부하 할인폭이 경·중간보다 큼을 대신 확인 |
| 3 손익분기 | 고압A Ⅰ→Ⅱ 200 h / 고압B Ⅰ→Ⅱ 197 h / 고압B Ⅱ→Ⅲ 491 h (요금표 안내 "월 500시간 초과 유리"와 일치) |
| 4 완전성 | 계절별 경 10h + 중간 8h + 최대 6h = 24h, 중복 없음 |

일부러 오타를 낸 요금표(단가 1원 이동, 기본요금 변경, 시간대 중복·공백)가 실제로
걸리는지도 테스트했다. 검증기가 살아 있다는 증거다.

**샘플 계산 결과 (일반용(을) 고압A, 기간 2023-04-25~2024-04-27)**

| 항목 | 값 |
|---|---|
| 요금적용전력 | 5,293.44 kW (2023-07 이후 전 월 고정) |
| 기본요금 개월수 | 12.00 (13개 버킷, 부분 월 2개를 합쳐 한 달) |
| 선택Ⅰ | 기본 452,832,624 + 전력량 2,898,284,725 = 3,351,117,349원 |
| 선택Ⅱ | 3,297,542,069원 — **최적 (부록 B 와 일치)** |
| 선택Ⅲ | 3,325,199,321원 |
| 월 이용시간 | 350.8 h (부록 B "월 351 h" 와 일치) |
| 2023-11 | 결측률 32.3% → 신뢰 제한, 보정 기준이 관측 대비 1.477배 |

**결정 사항** (위 「결정 사항」 3세션 항목에도 반영)

- 부록 A.2 의 **적용 범위와 허용오차는 `validate.DEFAULT_POLICY`(코드)** 에 두었다.
  요금 단가가 아니라 검증 정책이기 때문이다. 검증기는 데이터에서 뽑은 평균과의
  편차만 보고, 5.5·3.8·200h 같은 절대값은 테스트가 못 박는다
- 정전 흔적·특례 등 요일 토큰은 알 수 없는 값이면 예외를 던진다. 조용히 넘기지 않는다
- 산업용(을) 주말 할인 특례는 **기구만 구현**하고 합성 요금표로 시험했다.
  PoC 요금 데이터에 산업용을 넣지 않는다는 부록 A.4 를 지킨 것이다
- 부분 월 기본 정책은 `merge`. 샘플에서 정확히 12.00개월분이 된다

**미해결**

- 요금 데이터 `verified: false` — 청구서 대조 전이다. 계산 결과에 경고가 붙는다
- 요금적용전력의 계약전력 하한 미적용 (기존 미해결 항목). `notes` 에 명시된다

**다음 세션이 알아야 할 것**

- **PV 시계열을 붙일 때 라벨 규약을 맞춘다.** `classify_slots()` 가 쓰는 것과 같은
  `slot_start()` 로 귀속을 판정해야 15분 어긋나지 않는다. `15:00` 슬롯은 중간부하,
  첫 최대부하 슬롯은 `15:15` 이다
- PV 도입 후 요금은 `calculate_bill()` 을 **다시 부른다.** 절감액을 빼기로 구하지 말 것.
  사용량이 줄면 최적 선택요금이 바뀐다 (요구사항서 8장)
- 조합별 요금 비교는 `list_selections()` 로 조합을 돌며 `calculate_bill()` 을 부르면 된다.
  샘플 6조합이 모두 계산되는 것을 테스트로 고정했다
- 감도 3종(0.70/1.00/1.20)은 PV 출력에만 적용한다. 요금제 전환·계약전력 조정은
  확정 계산이라 감도를 적용하지 않는다 (요구사항서 9.2)
- `BillingResult.monthly` 는 Period 인덱스 DataFrame 이다. Excel 출력 시
  `to_timestamp()` 로 바꿔야 한다 (6세션)
- 요금표를 갱신할 때는 `data\tariff_*.json` 을 새 파일로 추가한다.
  `load_tariff()` 는 파일명 순 마지막을 쓴다

**테스트**
- pytest: 200 passed (1세션 33 + 2세션 37 + 이번 130)
- ruff: pass (check + format)
- mypy: pass (strict, 29 files)

---

### 2세션 — 데이터 품질 검사 (2026-08-06)

**완료**

- **`total_kwh` 강제 장치** (1세션 보완). `UsageData.kwh` 를 `kwh_grid` 로 개명하고
  `GridKwhSeries.sum()` 이 이탈분이 있을 때 `OffGridEnergyError` 를 던지게 했다.
  슬라이스에도 `off_grid_kwh` 가 따라붙어 월별·시간대별 부분합까지 막힌다
- `kwise.io.slot_start()` 추가 — 라벨(구간 끝)에서 구간 시작을 만든다.
  quality·tariff·pv 가 모두 이 함수를 써야 15분 어긋남이 생기지 않는다
- `kwise.quality` 구현 — 결측 분석, 정전 검출, 품질 집계, 결측 처리, 부하 패턴
- 단위테스트 37건 추가 (누적 70건). 샘플의 부록 B 수치를 전부 회귀로 고정

**만든 파일**

- `src\kwise\quality\missing.py` — `find_missing_gaps`, `monthly_missing`, `peak_hour_skew`
- `src\kwise\quality\outage.py` — `detect_outages`, `outage_slot_mask`
- `src\kwise\quality\checks.py` — `check_quality`, `QualityReport`
- `src\kwise\quality\fill.py` — `fill_missing` (기본 미보간)
- `src\kwise\quality\pattern.py` — `load_pattern` (6.1)
- `tests\__init__.py`, `tests\_synthetic.py` (합성 데이터 생성기), `tests\test_quality.py`

**샘플 검증 결과 — 전 항목 기대와 일치**

| 항목 | 값 |
|---|---|
| 결측 | 972 (2.8%) — 3% 이하라 경고 없음 |
| 결측 구간 | 정확히 2개. 11-03 17:30~11-13 09:45 (930슬롯 9.69일) / 04-06 19:30~04-07 05:45 (42슬롯 0.44일) |
| 11월 결측률 | 32.3% → 유일한 '신뢰 제한' 월 |
| 정전 | 1건 (4월). 흔적 3종 모두 검출, 결정적 흔적 2종 |
| 11월 통신 장애 | 정전 아님 — 흔적이 연속 결측뿐 |
| 이상치 | 0 kW 0건, 100 kW 미만 1건 (2024-04-07 06:00, 2.88 kW) |
| 편중 배수 | 정본 0.719 (정전 42슬롯 제외) / 비교용 0.688. 둘 다 임계 1.5 미만 |
| 부하 패턴 | 부하율 49.0%, 기저 65.0%, 주말 64.4%, 무인 54.9% |

**결정 사항** (위 「결정 사항」 2세션 항목에도 반영)

- 정전 흔적 2번은 "정전 직전 부분 적산 행"과 "복전 시점 재등록 행"을 **한 종으로 묶었다.**
  둘 다 그리드 이탈 행이라는 같은 흔적이다. 따로 세면 흔적이 4종이 되어
  "3종 중 연속 결측 뺀 2개" 규칙이 헐거워진다. `min_evidence` 로 조절 가능
- 복전 후 저부하 임계는 최대수요의 5% (샘플에서 264.7 kW, 관측 2.88 kW)
- 급변 임계는 최대수요의 30%. 결측을 사이에 둔 차이는 급변으로 세지 않는다
  (관측된 이웃끼리만 `diff`)
- 편중 판정에서 샘플은 정전 제외 여부로 배수가 0.719 ↔ 0.688 로 달라지되
  판정이 뒤집히지는 않는다. **뒤집히는 경로는 합성 픽스처로 별도 검증했다**
  (`test_synthetic_skew_verdict_flips_when_outage_excluded` — 정전 포함 4.13 '위험',
  제외 0.99 '정상')

**미해결**

- 없음. 세션 범위 항목은 모두 끝냈다

**다음 세션이 알아야 할 것**

- **`usage.kwh_grid.sum()` 은 예외를 던진다.** 요금 엔진의 전력량 계산은
  `usage.energy_kwh()` 를 쓴다. 이 시리즈는 이탈 행의 kWh 를 그 행이 속한 구간
  라벨에 얹은 것이라 합계가 `total_kwh` 와 정확히 같다.
  **다만 kW 로 환산하면 안 된다** — 얹힌 구간은 위상이 달라 수요가 왜곡된다
- 계절·시간대·월·요일 귀속은 반드시 `kwise.io.slot_start()` 를 거친다.
  `15:00` 슬롯은 중간부하, 첫 최대부하 슬롯은 `15:15` 이다
- `check_quality()` 결과의 `flagged_months` 가 '신뢰 제한' 월이다.
  요금 명세에 결측률을 병기하고 최대수요에 이 표시를 붙인다 (요구사항서 4.2)
- 결측 보정 기준(5.4 절의 `1 ÷ (1 − 결측률)`)에 쓸 월별 결측률은
  `QualityReport.monthly` 에 이미 있다. 다시 계산하지 말 것
- 샘플은 13개 월 버킷이며 앞뒤가 부분 월이다 (2023-04 는 576슬롯, 2024-04 는 2,496슬롯).
  기본요금을 13개월분 부과하지 않도록 5.5 절을 따른다
- 테스트 합성 데이터 생성기는 `tests\_synthetic.py` 에 있다.
  `month_rows()` 로 한 달치를 만들고 라벨을 지우거나 고쳐 결측·정전을 만든다

**테스트**
- pytest: 70 passed (1세션 33 + 이번 37)
- ruff: pass (check + format)
- mypy: pass (strict, 21 files)

---

### 1세션 — 골격과 데이터 입출력 (2026-08-06)

**완료**

- src 레이아웃 패키지 `kwise` 구성, `pip install -e .` 설치 (Windows 11 `.venv`)
- 요구사항서 2.1 절의 9개 모듈 디렉터리 생성 (io 만 구현, 나머지는 담당 세션 표기한 docstring)
- `pyproject.toml` 에 ruff·mypy·pytest 설정. `reference\` 는 세 도구 모두에서 제외
- `kwise.io` 사용량 로더 구현 — 인코딩 4종 순차 시도, `24:00` 변환, 컬럼 유연 매칭,
  간격 자동 인식(15분·1시간), 그리드 위상 최빈값 판정, 그리드 이탈 행 보존
- 단위테스트 33건. 실측 샘플이 요구사항서 부록 B 와 일치함을 확인

**만든 파일**

- `pyproject.toml`, `.gitattributes`
- `src\kwise\__init__.py`
- `src\kwise\io\__init__.py`, `src\kwise\io\usage.py`
- `src\kwise\{quality,tariff,pv,diagnose,measures,compare,report,ui}\__init__.py` (자리표시)
- `tests\conftest.py`, `tests\test_io_usage.py`

**부록 B 대조 결과 — 전 항목 일치**

| 항목 | 기대 | 산출 |
|---|---|---|
| 원본 행 | 34,358 | 34,358 |
| 유효 행 | 34,356 | 34,356 |
| 그리드 이탈 | 2 | 2 (04-06 19:29 / 04-07 03:51) |
| 예상 행 | 35,328 | 35,328 |
| 결측 | 972 (2.8%) | 972 (2.8%) |
| `24:00` 표기 | 357 | 357 |
| 최대수요 | 5,293.4 kW @ 07-03 09:30 | 5,293.44 kW @ 07-03(월) 09:30 |
| 평균 | 2,594.6 kW | 2,594.57 kW |
| 부하율 | 49.0% | 49.0% |
| 연간 사용량 | 22,285 MWh | 22,284.8 MWh |
| 100 kW 미만 | 1건 (04-07 06:00, 2.88 kW) | 동일 |

**결정 사항** (위 「결정 사항」의 1세션 항목에 반영)

- 반환 타입은 `UsageData(kw, kwh, off_grid, meta)`. `kw`·`kwh` 는 tz-naive
  DatetimeIndex 시리즈이고 결측은 NaN 이다. 보간하지 않는다
  — *2세션에서 `kwh` 를 `kwh_grid` 로 개명하고 합계를 막았다. 위 2세션 기록 참조*
- **총 사용량은 `meta.total_kwh`(그리드 이탈 포함) 를 쓴다.** `kwh` 시리즈 합계는
  그리드 정렬분만이라 요금 계산에 그대로 쓰면 이탈분이 누락된다
- 라벨은 구간 끝 그대로 인덱스에 둔다. 계절·시간대 귀속(한 구간 빼기)은 tariff 가 맡는다
- 그리드 위상은 "하루 시작으로부터의 초 잔여" 최빈값으로 판정한다

**미해결**

- 없음. 세션 범위 항목은 모두 끝냈다. 저장소 전체의 미해결 항목은 위 「미해결 항목」 참조

**다음 세션이 알아야 할 것**

- 로더는 `from kwise.io import load_usage` 하나로 쓴다. Streamlit 업로더용으로
  `load_usage_bytes(data, filename)` 도 있다
- `UsageMeta.warnings` 에 경고 문구가 이미 담긴다. 2세션 품질 검사는 이 위에 얹는다.
  결측·이탈·중복·간격 관련 원시 수치는 모두 `UsageMeta` 에 있다
- 결측 구간 검출(연속 공백 길이, 월별 결측률, 정전 판정)은 2세션 몫이다.
  `usage.kw.isna()` 로 시작하면 된다
- 그리드 이탈 행은 `usage.off_grid` 에 `timestamp`·`kwh` 로 남아 있다.
  4.3 절에 따라 "결측"이 아니라 "부분 계량"으로 분류해야 한다
- 패키지명이 `kwise.io` 라 stdlib `io` 와 이름이 겹친다. 절대 임포트라 문제는 없으나
  모듈 안에서는 `from io import BytesIO` 형태로 쓴다
- mypy 는 `strict = true` 로 돌린다. pandas 가 스텁을 제공하지 않아
  `ignore_missing_imports`, `warn_return_any = false` 를 함께 켰다
- pytest 는 `filterwarnings = ["error::FutureWarning"]` 이다. pandas 3.0.5 를 쓰므로
  경고가 뜨면 바로 실패한다

**테스트**
- pytest: 33 passed
- ruff: pass (check + format)
- mypy: pass (strict, 13 files)


<!--
### N세션 — 제목 (YYYY-MM-DD)

**완료**
-

**만든 파일**
-

**미해결**
-

**다음 세션이 알아야 할 것**
-

**테스트**
- pytest: N passed
- ruff: pass
- mypy: pass
-->
