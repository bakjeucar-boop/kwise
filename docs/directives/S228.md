S228 — 도구 판 2 · S227 pytest 소요를 가르고, 덱 글자 도구 · 도구 셋을 고친다

**이 판은 도구 판이다. src\ 0줄 · 산출물 0벌 · 금액 0.** 고치는 것은
tools\ · tests\ · 도구 규약뿐이다.

**사람이 정했다 — S225 뒤 웹 대화창에서.** 다음 판들은 도구 판이다.
**웹 대화창이 S226 · S227 값으로 정했다 — 둘째 묶음.** 사람 결정이 아니다.
  ㅈ `tools\deck_words.py` 가 투명하게 감춘 글자를 센다 — 알파를 안 본다.
    S225 그림 안 글자 가운데 18줄이 보이지 않는 투명 눈금이다(S226 1-4).
    **ㄷ 자릿수 뿌리 판보다 앞이어야 한다.**
  ㅅ `tools\run_tool.py` 는 끝나야 출력을 쓴다 — 뒤로 돌린 일의 진행을 못
    본다(S227 이 셸로 약 12번 들여다봤다).
  ㅂ 캐시 · 스냅 · runs 파일 목록과 크기 · 수정 시각을 뜨는 도구가 없다.
  ㄷ · ㅁ 기록 칸의 글자 수와 긴 칸 머리를 뜨는 도구가 없다(열한 판 · 세 판).
    같은 칸을 읽는 일이면 한 도구로 닫아도 된다 — 1-4 에서 센다.
  **빼는 것** — ㅍ(덱 소요) · ㅇ · ㅊ · ㅋ2 · ㅋ3 · ㅋ4 · ㅎ.

**S227 보고가 적은 것 — 전부 재게 할 것이다.** 원인을 미리 적지 않았다.
  · 갈래 넷이 S225 보다 1.56 ~ 1.82배 길었고 ① · ② · ④ 가 상한 8분을 넘겼다 ·
    남의 python 은 다섯 자리 다 0 · 까닭은 못 갈랐다
  · ② · ④ 는 백그라운드 셸로 돌았다
  · ② 가 예상을 넘겼는데 멈추지 않았다 — `CLAUDE.md` 6항과 어긋났다
  · `CLAUDE.md` 9항 표의 2번 PC 칸을 이 판 값으로 갈았다

**울타리에 하한이 있다** — 2절이 센 「고칠 자리 수」. 적게 닫으면 자리마다
까닭을 값으로.

이 판의 못 박힌 안전한 쪽.
  · **src\ 0줄.** 하나라도 갈리면 멈춘다.
  · **도구가 내는 수의 뜻을 바꾸지 않는다** — 틀리지 않던 입력에서의 출력은
    한 글자도 안 바뀌어야 한다(2-3 의 전 출력과 맞댄다). 갈리면 멈춘다.
  · **ㅈ 은 보이지 않는 글자를 빼는 것만** — 그림 밖 수치 조각(14,183) 차 0 ·
    그림 안은 투명하게 감춘 글자만 빠져야 한다. 그 밖의 줄이 하나라도
    서거나 빠지면 멈춘다.
  · **ㅅ 은 도는 동안 읽을 수 있게 하는 것만** — 끝난 뒤 받은 파일 · 꼬리 ·
    종료 코드는 전과 한 글자도 같아야 한다.
  · **예상 소요를 넘기면 `CLAUDE.md` 6항대로 한다** — S227 이 넘긴 자리에서
    멈추지 않았다.
  · **pytest 갈래는 하나씩 차례로 돌린다 · 겹치게 돌리지 않는다.** 갈래마다
    시작 · 끝 시각을 적는다.
  · **지시서에 없는 pytest 플래그를 어느 판에도 붙이지 않는다** — 못 확인
    판 · 되돌림 판 포함(S227 이 못 확인 판에 `-p no:randomly` 를 붙였다).
    전체가 빨갛게 끝나면 고친 뒤 전체를 한 판 더 — 2번 PC 면 네 갈래 전부.
  · 세션 도중 사람에게 묻지 않는다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. 되돌림이 실제로
    섰는지 값으로 먼저 본다.
  · **git stash 를 쓰지 않는다.** 1번 PC 면 S140 스태시를 안 지운다.
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    개시 값은 `tools\open_values.py` 로 · `pytest_counts.py` 는 인자 없이
    불러도 된다 · 긴 출력은 `tools\run_tool.py --tail` · `deck_words.py` 는
    온 경로. **도구로 되는 자리에 파이프를 쓰지 않는다.** 썼으면 쓴 자리마다
    「무엇을 보려고 · 왜 도구로 못 했나」. 커밋 메시지는 편집 도구로 지은
    `.txt` 로 `git commit -F`.
  · **덱 19벌 뜨기는 한 판** — ㅈ 을 고친 도구로 뜨는 판이다. 케이스 스터디 ·
    화면 감사는 안 돌린다. 앱을 띄우면 포트를 적고 내린 뒤 닫힌 것을 확인한다.
    **벽시계를 절마다 잰다.**


0절 — 개시

0-1  `tools\open_values.py` — PC · cpu · RAM · 일꾼 수 · 남의 python.
     **2번 PC 면 3-3 은 갈래 넷(`PROCEED.md` 「pytest 분할 실행」)을 하나씩 ·
     1번 PC 면 통째.**
0-2  `git pull` · HEAD 와 origin(S227 보고는 1c4ac51) · 워크트리 ·
     `git stash list`.
0-3  미해결 — 건수(S227 보고 155) · 갈래별 수 · 세 방법 검산. **이 판이 닫을
     항목을 이름으로**(ㅈ · ㅅ · ㅂ · ㄷ · ㅁ 에 묶인 「라」 항목 전부).
0-4  회귀 기준선 — 수집 · passed · xfail · skip · 정적 검사 다섯(ruff 는 .py
     기준). **2번 PC 의 깨끗한 판은 1절 판정 뒤에 정한다 — 여기서는 S225 ·
     S227 을 나란히.**
0-5  **덱 스냅 기준선** — 이 PC 캐시에 S225 뒤 스냅이 있는지 · 그 뒤 src\ 가
     안 바뀌었는지(S226 · S227 은 src\ 0줄). 있으면 그것이 「옛 도구로 뜬
     기준선」 이다. 없으면 2절 앞에 옛 도구로 한 판 떠야 하므로 덱 판 수를
     둘로 적는다.

커밋: S228 0절: 개시


1절 — S227 pytest 소요를 가른다

**이 절은 재고 판정만 한다. 도구 · 규약을 고치지 않는다.**

1-1  **갈래 넷의 시작 · 끝 시각** — runs\ 의 S227 출력 파일 · `PROCEED.md`
     S227 절에서. **갈래끼리 겹쳤는가** — 겹친 구간이 있으면 분 단위로.
1-2  **갈래마다 느려진 곳** — `--durations=0` 출력으로 S225 와 S227 을 맞댄다.
     갈래마다 위 열 개 · 고르게 느려졌나, 몇 개가 느려졌나.
1-3  **`CLAUDE.md` 6항과 소요 상한을 글자 그대로 옮긴다** — 그 글이 2번 PC
     갈래 넷에 어떻게 닿는가(갈래마다인가 · 합인가 · 1번 PC 만인가). 판정이
     안 서면 안 선다고 적는다. **6항은 고치지 않는다.**
1-4  **S227 이 `CLAUDE.md` 9항 2번 PC 칸에 무엇을 적었나** — 깨끗한 판 칸을
     갈았나.
1-5  1-1 · 1-2 로 판정이 안 서면 **갈래 ① 하나를 홀로 한 번 돌린다**
     (`PROCEED.md` 명령 그대로 · 플래그를 더하지 않는다 · 남의 python 을 앞 ·
     뒤에). S225 · S227 과 맞댄다.
1-6  **판정 한 줄 — S227 판이 깨끗한 판인가.** 아니면 4-6 에서 9항의 깨끗한
     판 칸을 그 PC 의 앞 깨끗한 판 값으로 되돌린다(기록만).

커밋: S228 1절: S227 소요


2절 — 재현하고 센다 · 고친다

2-1  **자리마다 증상을 지금 저장소에서 재현한다.**
       ㅈ S225 가 투명하게 감춘 역률 100 벌 눈금이 `deck_words` 에 몇 줄로
          잡히나(S226 은 18줄) — 스냅에서 벌 · 줄로.
       ㅅ `run_tool` 로 오래 도는 명령을 뒤로 돌리고 도는 동안 받은 파일을 읽는다.
       ㅂ 셸 없이 runs\ · 스냅 캐시의 파일 목록 · 크기 · 수정 시각을 뜰 수 있나.
       ㄷ · ㅁ 셸 없이 `PROCEED.md` 「다음 작업」 칸 · 미해결 칸의 글자 수와
          머리 N줄을 뜰 수 있나.
     재현마다 명령 · 출력 · 기대값.
2-2  **ㄷ · ㅁ · ㅂ 은 있는 도구에 붙일 수 있는지 먼저 본다** — 있는 도구의
     이름 · 하는 일과 맞으면 붙이고, 아니면 새 도구. 새로 짓는 값은 없어야
     한다(칸의 경계는 기록 묶음 시험 · 브리핑이 이미 쓰는 경계를 쓴다).
2-3  **전 출력을 떠 둔다** — 고칠 도구마다 틀리지 않던 입력 둘 이상.
2-4  **고칠 자리 수를 값으로 못 박는다** — 코드 자리 · 규약 자리(`CLAUDE.md`
     9항). 그것이 울타리이고 하한이다.
2-5  **2-4 가 센 자리만 고친다.** 자리마다 고친 줄 수와 전 · 후. 울타리를
     넘는 자리가 보이면 멈추고 값을 낸다.
2-6  자리마다 2-1 의 재현을 다시 돈다 · 2-3 의 전 출력과 맞댄다.
2-7  **ㅈ — 고친 도구로 덱 19벌을 한 판 뜨고 0-5 의 기준선과 `--diff`.**
     그림 밖 차 0 · 그림 안 빠진 줄이 2-1 의 투명 글자와 벌 · 줄까지 같은지.
     그 밖의 줄이 갈리면 멈춘다. 덱 소요와 PC 를 적는다.
2-8  1절 · 2절에서 새 도구를 쓸 수 있게 된 뒤로는 셸 대신 그 도구를 쓴다.
2-9  적게 닫았으면 자리마다 까닭을 값으로.
2-10 **되돌려 확인한다** — 되돌리면 2-1 의 증상이 다시 서는지. 되돌림이 실제로
     섰는지 값으로 먼저 본다.

커밋: S228 2절: 도구 고침


3절 — 못 · 회귀

3-1  **자리마다 증상을 무는 못** — 도구를 실제로 불러 낸 값을 본다(소스
     리터럴을 찾는 못은 안 박는다). ㅈ 은 투명 글자를 안 세는 것과 보이는 글자는
     그대로 세는 것 · ㅅ 은 도는 동안 받은 파일에 줄이 있는 것 · ㄷ · ㅁ · ㅂ 은
     출력 꼴. **이미 있는 못을 넓혀 닫히면 새 못을 박지 않는다.** 박은 못마다
     되돌려 확인 · 소요 · 합이 20초를 넘으면 줄일 수 있는지 값으로. 앞 판
     딱지를 걷고 **걷은 딱지 몫과 새 못 몫을 갈라 적는다.** xfail 이 새로
     빨개지면 멈춘다. 되돌림 원복을 `git status` · `git diff` 로.
3-2  **`git diff --numstat` 으로 src\ 0줄을 값으로 보인다** — 폴더마다.
3-3  pytest — 0-1 이 정한 대로. **2번 PC 면 `PROCEED.md` 「pytest 분할 실행」
     의 네 명령 그대로 · 하나씩 차례로 · 갈래마다 시작 · 끝 시각. 1번 PC 면
     아래 명령 그대로. 플래그를 더하지 않는다.**

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no --durations=0

     남의 python 은 `open_values.py` 로 판 앞 · (2번 PC 면 갈래 사이) · 뒤.
     0 이 아니면 기다리거나 죽이지 말고 적기만 한다. `run_tool` 로 받고
     `pytest_counts.py` 로 센다 — `--base` 에 0-4 의 수집 건수. skip 수를 함께.
3-4  소요 — PC · 일꾼 수 · 남의 python 값 전부 · 깨끗한 판인지 · 갈래마다
     S225 · S227(1절 판정과 함께)과 맞댄다.
3-5  정적 검사 다섯 — 0-4 와 맞댄다.

커밋: S228 3절: 못 · 회귀


4절 — 기록 · 닫기

`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.

4-1  `PROCEED.md` 에 228세션 절 — 둘째 묶음이 웹 대화창 판단이라는 것 · 1절 판정.
4-2  **미해결 건수 대조를 이름으로.** 이름에 「 · 」 를 넣지 않는다. 이 판이
     닫은 항목을 지운다. 덜 닫았으면 남은 몫을 이름으로.
4-3  **HANDOVER — ㅈ 로 그림 안 글자 기준선이 움직인 것을 한 줄로**(어느 판부터
     · 몇 줄). 옛 판 절은 고치지 않는다.
4-4  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
4-5  `docs\HANDOVER.md` 를 이 판 값으로 — 머리 사슬에 S228 · 1절 「앞 값」
     사슬은 두 판까지 · 6절 앞 판 딱지를 걷는다 · **2번 PC 판이면 2절을 이 판
     값으로.**
4-6  `CLAUDE.md` — 9항 도구 규약(이 판이 고친 · 세운 도구) · **9항 이 판을 돈 PC
     칸 — 1-6 판정이 「S227 은 깨끗한 판이 아니다」 면 깨끗한 판 칸을 앞 깨끗한
     판 값으로 되돌리고, 이 판이 깨끗하면 이 판 값으로.** 6항은 고치지 않는다.
4-7  「다음 작업」 칸 — 남은 도구 자리 이름(ㅍ · ㅇ · ㅊ · ㅋ2 · ㅋ3 · ㅋ4 · ㅎ)과
     뿌리 판 후보(ㄷ 자릿수 · ㄱ 절사). **고르지 않는다.** 글자 수와 여유.
4-8  이 지시서 본문을 `docs\directives\S228.md` 로 그대로 앉힌다.
4-9  **기록을 다 고친 뒤에** 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-10 커밋하고 `git push`. **HEAD 와 origin 이 같은지 · 워크트리가 깨끗한지를
     보고 1항에 적는다.**

커밋: S228 4절: 기록 · 미해결 대조 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — 절·항마다 한 줄. **1-6 판정을 한 줄로.** 울타리(하한)와 고친 수를
    나란히. 「멈춘다」 가 걸린 자리를 멈춘 자리 · 안 멈춘 자리로 갈라서.
    마지막 줄에 HEAD = origin · 워크트리.
  2 만든/바꾼 파일 — +/− 줄 수. src\ 폴더마다 0줄을 값으로.
  3 수치가 움직인 곳 — 그림 안 글자 전 → 후(벌 · 줄) · 그림 밖 조각 · pytest 수 ·
    도구 출력이 바뀐 입력. 금액 0.
  4 판단한 것 — 1-3 · 1-6 · 2-2 · 2-4 · 2-9 · 3-1.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패 · 덱 판 수(적은 것과 실제) ·
    전체 pytest 마지막 판이 초록인지.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 가운데 고친 것과 9항을 고쳤는지.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로 · 소요 · xfail 전후 ·
    확인 안 한 못 전후와 딱지 몫 · 새 못 몫.

  **그 뒤에 「소요 표」 — 갈래마다 S225 · S227 · 이 판(시작 · 끝 · 길이 ·
  겹침).**
  **그 뒤에 「재현 표」 — 자리마다 재현 명령 · 전 출력 · 후 출력 한 줄씩.**
  **그 뒤에 「사람이 정할 것」 을 한 표로** — 멈춘 자리가 있으면 그 자리.
  없으면 없다고.
  **그 뒤에 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다) — 그 PC 의 앞 판들과만 나란히 ·
덱과 pytest 가 몇 % 인지. ctx 셋(종전 → 지금 · 가장 크게 먹은 것 · 줄일 수
있었던 것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
