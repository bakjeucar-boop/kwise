S237 — 고침 판 · 역률 투자비 0 과 비움 · 뿌리 「ㄹ 0원 거르기」 세 자리를 고친다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 자릿수는 닫혔다. 「정의가
다른 두 수」 는 사람 결정 대기(이 판 몫이 아니다). S236 이 「ㄹ 0원 거르기」 를 쟀고 · 투자비 고침은
멈춤 조건(입력 기본값 0)에 걸려 0줄이었다. 이 판은 **투자비 표기를 고치고 · ㄹ 의 세 자리(조합
이름 · 역률 카드 안내 · 이유 줄)를 고친다.** ㄹ 의 넷째 자리(조합마다 같은 계약전력 문장이 되풀이
된다)는 「ㅁ 겹침 거르기」 몫이라 건드리지 않는다. 이 밖으로 가지 않는다 — 다른 뿌리 · 도구 ·
측정은 이 판 몫이 아니다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).**
절사 A · 단위 기준 · 측정 중단 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지
않는다 · S154(역률이 감액 상한 이상이면 「개선할 것이 없다」) · S207(「그 벌에서 참인 말만」).

**웹 대화창이 정했다(S236 뒤 · 이 판이 HANDOVER 5절에 두 행으로 앉힌다).**
  ㄱ 역률 투자비 — 비움과 0 을 가르지 않는다. 같은 화면에서 투자비 칸이 있는 다른 수단과 같이 0 을
    「미입력」 으로 읽고 「미산출 — 투자비 미입력」 으로 적는다. 칸 기본값은 바꾸지 않는다. 0 이 아닌
    투자비를 넣은 경로의 글자는 안 바뀐다. 투자비 칸이 없는 수단이 적는 「즉시」 는 건드리지 않는다.
    근거 — HANDOVER 5절 S235' 행(다른 수단 방식을 따른다) · 하나의 기준으로 통일.
  ㄴ 0원 거르기 잣대 — 「여지가 없어 0」 과 「계산해 보니 0」 을 가른다. 여지가 없는 수단(역률은 S154 ·
    계약은 S205 여지 판정)은 카드에서 「없음」 으로 말하고 · 조합 이름과 이유 줄에서 그 수단을 말하지
    않는다. 계산해 보니 0원인 것(S205 의 선택요금 0원 등)은 그대로 둔다. 이유 줄은 그 몫이 0 이면
    세우지 않는다. **조합의 계산 구성(어느 수단을 켜고 계산하나)은 바꾸지 않는다 — 이름 · 안내 ·
    이유 줄 글자와 거르는 판정만.** 근거 — S154 · S205 · S207.

**S236 보고가 적었다 — 실물에서 먼저 확인하라.** 태양광 · ESS · DR 투자비 칸 기본값 0.0 · 0 을 미입력
으로 읽는다 / 선택요금 · 계약 · DR 은 코드가 0.0 을 「투자 없음(즉시)」 으로 박는다(DR 이 두 쪽에 다
섰다) / 투자비 고칠 자리 6(계산 폴더 2) / 증상 8 역률 100 두 벌의 권장 조합 이름에 역률 개선이 선다 ·
증상 10 역률 몫 0원 두 벌에 역률 이유 줄이 선다 / 역률 카드 안내 「절감액은 0원」 3벌.

**울타리에 하한이 있다** — 1-4 가 센 「고칠 자리 수」. 결정 ㄱ · ㄴ 을 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **금액 · 계산값은 한 자리도 안 움직인다** — 회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 ·
    케이스 스터디 타당성. 움직이면 멈춘다.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-4 가 센 자리 밖 0줄.**
    그 자리 안에서도 금액을 만드는 식은 0줄.
  · **조합 구성은 안 바꾼다.** 이름에서 수단을 빼서 다른 조합과 같은 이름이 되는 자리는 고치지 않고 두
    조합의 이름 · 금액을 값으로 낸다.
  · **0 이 아닌 투자비를 넣은 경로의 산출물 글자는 안 바뀐다.** 바뀌면 멈춘다.
  · 1-1 의 기록 글자가 결정 ㄴ 과 어긋나는 자리는 고치지 않고 글자를 낸다.
  · 조합마다 되풀이되는 계약전력 문장(ㅁ 몫)은 0줄.
  · 화면 문구(글자)는 늘리지 않는다 — 「미산출 — 투자비 미입력」 · 「없음」 은 이미 있는 글자를 쓴다.
    CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · git log)에서 찾는다 — 없으면 「기록
    없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 절이 끝날 때마다 시계를 보고 적는다. 커밋 메시지는 편집 도구로 지은
    .txt 로 git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · 지시서에 없는 pytest 플래그를 붙이지 않고 · 있는 플래그를 바꾸지 않는다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. 도구로
    되는 자리에 파이프를 쓰지 않는다. 조사 에이전트를 띄우면 이 금지를 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S236 보고는 e38485e) · 워크트리.
0-2  미해결 건수(146) · 회귀값 여덟 · 덱 스냅 기준선(S235 뒤 스냅 — S236 은 코드 0줄).

커밋: S237 0절: 개시


1절 — 고칠 자리를 못 박는다

1-1  **기록** — S154 · S205 · S207 · HANDOVER 5절 S235' 행을 글자 그대로 · 결정 주인. 결정 ㄱ · ㄴ 과
     어긋나는 글자가 있는지.
1-2  **투자비** — (a) 투자비 칸이 있는 수단과 없는 수단을 값으로 가른다(DR 이 어느 쪽인지 · 두 쪽에
     다 서면 자리마다). (b) 아홉 경로(화면 카드 · 3단계 표 · 조합 · PPT 8 · 12 · 16장 · Excel 수단별
     결과 · Excel 조합 비교 · Word 표 · 일괄 생성)마다 비움 · 0 · 넣음 세 경우의 지금 글자 · 고친 뒤
     글자(예정) · 그 글자를 만드는 코드 자리 · 캐시 열쇠 · 조합 인자에 닿는 자리. Excel 조합 비교는
     같은 시트에서 다른 수단이 미입력일 때 적는 글자를 따른다.
1-3  **0원 거르기 세 자리** — 조합 이름 · 역률 카드 안내 · 이유 줄마다 지금 잣대 · 결정 ㄴ 뒤 잣대 ·
     서는 벌과 글자 · 코드 자리. 조합 이름은 수단을 빼면 다른 조합과 같은 이름이 되는 벌이 있는지.
     역률 카드 안내는 계약 카드가 이미 「없음」 을 적는 꼴을 따른다 — 그 꼴과 자리.
1-4  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S237 1절: 고칠 자리


2절 — 고친다

2-1  **1-4 가 센 자리를 고친다.** 결정 ㄱ · ㄴ 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S237 2절: 투자비 · 0원 거르기


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 역률 100 벌과 역률 카드가 서는 벌을 가장 적게 골라(고른 벌과 까닭) 투자비
     비움 · 0 · 넣음 세 경우로 뜨고 맞댄다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓혀 (1) 「투자비 비움과 0 이면 아홉 경로가 「미산출 — 투자비 미입력」 · 넣음이면
     전과 같은 글자」 (2) 「여지가 없는 수단은 조합 이름과 이유 줄에 안 서고 카드가 「없음」 을 적는다」
     를 문다. 넓혀서 안 닫히면 새 못. 산출물을 실제로 만든 글자를 본다. 되돌려 확인한다(스크래치
     monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S235 뒤 스냅과 맞댄다. 갈린 자리가 전부 이 판 자리(역률 회수기간 · 투자비 ·
     조합 이름 · 역률 카드 안내 · 이유 줄)인지. 금액 칸이 갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(이 판 자리 밖이
     갈리면 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-4 자리 밖 0줄을 값으로 · 폴더마다.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S237 3절: 못 · 실물 · 회귀


4절 — 기록 · 닫기

4-1  PROCEED.md 에 237세션 절 — 웹 대화창 판단(ㄱ · ㄴ)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 3절 값.
4-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다. 역률 투자비 항목(제목이 틀렸던 것은 닫힘의
     까닭에 한 줄). 0원 거르기 항목은 남는 몫(조합마다 되풀이되는 계약전력 문장)이 있으면 지우지 않고
     남은 몫으로 이름을 고친다. 미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
4-3  docs\HANDOVER.md — 4절 ㄹ 행을 이 판 값으로 · 5절에 웹 대화창 판단 두 행(ㄱ · ㄴ) · 머리 사슬에
     S237 단락 · 2절에는 pytest 건수 · 초록만.
4-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(뿌리 「정의가 다른
     두 수」 사람 결정 대기 · 겹침 거르기(조합마다 되풀이되는 계약전력 문장 포함) · 개발 말과 그물 ·
     이 판이 고치지 않고 값으로 낸 자리).
4-5  CLAUDE.md 는 고치지 않는다.
4-6  이 지시서 본문을 docs\directives\S237.md 로 그대로 앉힌다.
4-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S237 4절: 기록 · 뿌리 진행표 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-4 자리 몫과 그 밖 0줄).
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를 따로.
  6 예상과 달랐던 것
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「투자비 경로표」 — 경로 아홉 · 비움 · 0 · 넣음 각각 전 → 후.
  그 뒤에 「0원 거르기 자리표」 — 세 자리 · 잣대 전 → 후 · 글자가 바뀐 벌 수 · 고치지 않은 자리와 까닭.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시계를 보고) · 백그라운드 셸 확인 결과.
