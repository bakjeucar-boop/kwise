S242 — 고침 판 · 뿌리 「ㅂ 개발 말과 그물」 남은 몫을 닫는다 · 다음 뿌리 후보를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 두 수 · 자릿수 · 0원 거르기 · 겹침
거르기는 닫혔다. 「ㅂ 개발 말과 그물」 은 S241 이 세션 번호와 산출물 내부 경로를 닫았고 · 말투 · 「우리」 ·
기준 데이터 비고의 세션 번호 · 화면 경로 · 기상 예외 글이 남았다. 이 판은 **ㅂ 의 남은 몫을 닫고 · 다음
뿌리 후보를 재기만 한다**(고치지 않는다 · 고르지 않는다). 이 밖으로 가지 않는다.

**사람이 정했다.**
  결정 1 (S241 뒤 · 이 판이 HANDOVER 5절에 한 행으로 앉힌다) — 고객 산출물(PPT · Word · Excel · 일괄 생성 ·
    부록)의 말투는 「~습니다」 로 맞추고 「우리」 를 뺀다. 화면(툴팁 포함)은 이 판 몫이 아니다.
  앞서 정한 것(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 가리키기만) — 절사 A · 단위 기준 · 측정
    중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지
    않는다 · S207 · 운영시간 외 부하는 비중(S238).

**웹 대화창이 정했다(S241 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 2 (말투 적용 범위) — 결정 1 은 서술어로 끝나는 문장에만 적용한다. 표 칸 · 제목 · 명사로 끝나는
    개조식 줄은 건드리지 않는다. 끝맺음만 「~습니다」 꼴로 바꾸고 뜻을 바꾸지 않는다. 「우리」 는 빼서
    문장이 서면 빼고, 주어가 꼭 필요하면 산출물이 이미 쓰는 주어 말(1절이 센다) 가운데 하나로 갈음한다.
  결정 3 — 기준 데이터 비고(data\ 의 두 기준 파일과 data\defaults\ 사본)에서 세션 번호를 지운다. S241
    결정 2(화면에서도 세션 번호를 지운다)를 데이터 파일까지 적용한다. 이력은 저장소 기록이 쥔다.
  결정 4 — 화면의 내부 경로는 운영자가 파일을 넣거나 찾아야 하는 안내이면 둔다. 그 밖은 S241 결정 3 꼴로
    지운다. 운영자 안내는 고객 산출물에 싣지 않는다 — 화면과 같은 글자를 쓰는 기상 예외 글이 고객
    산출물에 서면 그 글을 산출물에서 뺀다(경로만 떼는 자리를 새로 짓지 않는다).

**S240 · S241 보고가 적었다 — 실물에서 먼저 확인하라.** 고객 산출물 반말 약 121줄 · 「우리」 약 38줄(끝
글자로 세는 그물이라 넘겨 센 줄이 섞였다) / 화면 반말 213줄 / 기준 데이터 비고의 세션 번호 20항목 /
화면 경로 13자리 / 기상 예외 글의 경로는 화면과 같은 글자라 S241 이 산출물에서 안 고쳤다 / 한글 없는
칸(날짜 · 불리언 · 열쇠)은 S240 4-4 판정으로 그대로 둔다.

**울타리에 하한이 있다** — 1-6 이 센 「고칠 자리 수」. 결정 1 ~ 4 를 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한
    자리도 안 움직인다.** 움직이면 멈춘다.
  · **갈리는 글자는 1-2 ~ 1-5 목록 안뿐이다.** 목록 밖 글자가 갈리면 멈춘다.
  · 표 칸 · 제목 · 명사로 끝나는 줄 · 숫자 · 단위 · 고유 이름 0줄. 문장에 새 사실을 더하지 않는다.
  · 기준 데이터 파일은 비고 글자만 — 값 · 키 · 항목 수 · source · source_date · verified_on 0줄. 기준
    파일과 data\defaults\ 사본이 어긋나면 멈춘다.
  · 화면 말투 · 매뉴얼 · 기술서 · 코드 주석 · 독스트링 · 시험 · 기록 문서의 말투는 0줄.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-6 이 센 자리 밖 0줄.**
    요금 · 절감액을 만드는 식은 0줄 — 고객에게 나가는 문자열만.
  · **그물 도구(tools\*.py)는 0줄.** 다시 서지 않게 하는 장치는 산출물 실물을 무는 못(시험)이다.
  · **5절(다음 뿌리 후보)은 고치지 않는다 · 고르지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다
    — 없으면 「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집
    도구로 지은 .txt 로 git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다. 지시서가 꼴을 적지 않은 판(못 하나 · 되돌려 확인 · 넓힌
    못 한 판)도 3-1 꼴 그대로 — `-rf --tb=no` 밖 플래그를 붙이지 않는다.**
  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은
    도구 · 같은 옵션으로 나눠 뜬다(나눈 묶음과 까닭을 적는다). 도구가 벌을 골라 뜰 수 없거나 나눠 뜬 판도
    끊기면 멈춘다. 전체 pytest 갈래가 끊기면 다시 돌지 않고 멈춘다. 멈추면 3절까지 커밋하고(push 하지
    않는다) 끊긴 자리를 보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를
    쓰지 않는다 — `| Out-Null` 도 파이프다.** 파일을 볼 때는 읽기 도구로 본다. 조사 에이전트를 띄우면
    이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S241 보고는 1b0eee7) · 워크트리.
0-2  미해결 건수(142) · 회귀값 여덟 · 덱 스냅 기준선(S241 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S242 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — CLAUDE.md 「화면 문구」 절 · S207 · S240 4-4 판정 · S241 결정 2 · 3 을 글자 그대로 · 결정
     주인. 결정 1 ~ 4 와 어긋나는 글자가 있는지.
1-2  **말투(결정 1 · 2)** — 고객 산출물의 반말 끝맺음 줄 전수를 문장 / 표 칸 · 제목 · 명사 끝 줄로 가른다
     (넘겨 센 줄은 따로) · 문장마다 만드는 코드 자리 · 바꾼 뒤 글자(예정). 「우리」 줄 전수 · 빼면 서는가 /
     주어가 필요한가 · 산출물이 이미 쓰는 주어 말 목록.
1-3  **기준 데이터 비고(결정 3)** — 세션 번호가 든 비고 항목 전수(S241 20항목 — 다시 센다) · 파일마다 ·
     사본과 짝 · 그 비고가 서는 곳(화면 · 부록 등) · 지운 뒤 글자(예정).
1-4  **화면 경로(결정 4)** — 13자리 전수 · 자리마다 운영자가 파일을 넣거나 찾아야 하는 안내인가 · 둘 / 지울
     것 · 지운 뒤 글자(예정).
1-5  **기상 예외 글(결정 4)** — 그 글이 서는 조건 · 고객 산출물에 서는 경로 · 화면과 같은 글자를 만드는
     자리 · 산출물에서 뺀 뒤 모습(예정).
1-6  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 데이터 파일 자리 · 시험 자리.
     울타리이고 하한이다.

커밋: S242 1절: 고칠 자리


2절 — 고친다

2-1  **1-6 이 센 자리를 고친다.** 결정 1 ~ 4 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S242 2절: 말투 · 비고 · 화면 경로 · 기상 예외 글


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면
     시험과 한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지
     않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 결정 1 ~ 4 가 다 서는 벌을 가장 적게 골라(고른 벌과 까닭) 뜬다. 기상 예외 글은
     그 조건을 세워 일괄 생성으로 뜬다. **산출물 넷(PPT · Excel · Word · 일괄)을 통째로 S241 뒤 스냅과
     맞대** 갈린 줄이 전부 1-2 ~ 1-5 목록 안인지 본다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못(S241 의 산출물 실물 못 · 화면 세션 번호 인자 · 기상 폴백 못)을 넓히거나 새 못으로 (1)
     「고객 산출물 실물 문장에 반말 끝맺음과 「우리」 가 0」 — 1-2 가 가른 잣대로 · 표 칸 · 제목은 안 문다
     (2) 「기준 데이터 비고에 세션 번호가 0 · 사본과 같다」 (3) 「화면 경로는 운영 안내 자리에만 선다」 (4)
     「기상 예외 글이 고객 산출물에 서지 않는다」 를 문다. 산출물을 실제로 만든 글자를 본다. 되돌려
     확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S241 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 ~ 1-5 목록 안인지 · 반말 끝맺음 ·
     「우리」 가 19벌 문장에서 0 인지. 금액 칸이 갈리면 멈춘다. 이 판의 19벌 실물은 5절이 쓴다 — 지우지
     않는다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-6
     자리 밖이 갈리면 멈춘다).
3-6  screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-6 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S242 3절: 못 · 실물 · 회귀


4절 — 기록 · 닫기 (5절을 먼저 끝내고 그 값을 함께 적는다)

4-1  PROCEED.md 에 242세션 절 — 사람 결정(결정 1 · 앞서 정한 것은 가리키기만)과 웹 대화창 판단(결정
     2 ~ 4)을 갈라서 · 1 ~ 3절 · 5절 값. 세션 목록표에 242 행.
4-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(고객 산출물 개발 말 항목의 남은 몫 · 한글 없는
     칸은 S240 4-4 판정으로 닫는다고 닫힘의 까닭에 한 줄 · 닫힌 것이 더 있으면 이름으로). 미해결 건수
     대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
4-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 ·
     4절 ㅂ 행(닫힘)과 5절이 낸 후보 · 5절에 사람 결정 한 행(결정 1)과 웹 대화창 판단 행(결정 2 ~ 4) · 머리
     사슬에 S242 단락 · 2절에는 pytest 건수 · 초록만.
4-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(5절이 낸 뿌리 후보 ·
     이 판이 고치지 않고 값으로 낸 자리 · 여지가 없는 수단이 조합의 마지막 줄인 경우 · 화면 말투는 사람이
     화면을 열어 잡는 문구 판 몫).
4-5  CLAUDE.md 는 고치지 않는다.
4-6  이 지시서 본문을 docs\directives\S242.md 로 그대로 앉힌다.
4-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S242 4절: 기록 · 뿌리 진행표 · 지시서 앉히기


5절 — 다음 뿌리 후보 재기 (고치지 않는다 · 고르지 않는다 · 읽기만 · 새 도구를 짓지 않는다)

5-1  **추림** — 미해결 전부 가운데 계산값 · 고객 산출물 글자에 닿는 항목을 이름으로 뽑는다. 닿지 않는
     항목(도구 안쪽 · 기록 정리 · 자료 대기)은 수만 센다.
5-2  **묶음** — 5-1 항목 가운데 같은 코드 자리 · 같은 사실(tools\fact_sites.json · tools\count_sites.py 로
     본다)을 공유하는 것끼리 묶는다. 둘 이상 돋은 묶음이 뿌리 후보다. 묶음마다 항목 이름 · 공유하는 자리 ·
     결함 유형(collaboration.md 3절의 다섯 가운데) · 3-4 의 19벌 실물에 서는지와 벌 수.
5-3  **걸린 결정** — 묶음마다 기록된 결정이 있는지(글자 그대로 · 결정 주인) · 사람 결정이 필요한지.
5-4  5절 값은 4-1 의 242세션 절에 넣는다(4절 커밋 전에 5절을 먼저 끝낸다).


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-6 자리 몫과 그 밖 0줄) · data\ · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를
    따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「개발 말 고침 표」 — 자리(말투 · 「우리」 · 비고 · 화면 경로 · 기상 예외 글)마다 전 → 후 줄 수 ·
    건드리지 않은 줄(표 칸 · 제목 등)의 수 · 고치지 않은 줄과 까닭.
  그 뒤에 「다음 뿌리 후보」(5절) — 추린 수 · 묶음마다 항목 이름 · 결함 유형 · 19벌에 서는 벌 수 · 걸린
    결정 · 사람 결정 필요 여부. 사람이 읽는 말로. 코드 자리는 꼬리표로만. 고르지 않는다.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
