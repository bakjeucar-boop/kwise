S169 — PPT 장04·장07 이 피크 여지를 반대로 말하는 것을 고친다.
       rules_kr.json 이름 근거를 확인하고, 파일별 소요를 한 판에 잰다.

이 판은 산출물 문장을 고친다. 계산 엔진(tariff·measures·diagnose·compare)의
값을 바꾸지 않는다 — 값이 한 원이라도 움직이면 범위를 벗어난 것이다.
문턱 값(0.70 · 0.20) 자체도 이 판에서 바꾸지 않는다.

시작 전에 git pull 한다. 어느 PC 인지 먼저 확인하고 보고에 적는다.
2번 PC 면 전체 pytest 를 앞단에서 통째로 돌리지 말고 PROCEED.md 의
「pytest 분할 실행」 갈래 넷으로 나눈다.

절마다 커밋한다.
파일 수정은 편집 도구로만 한다 — heredoc · echo · sed · awk 금지.
재는 스크립트도 셸에 태우지 않는다. 스크래치 .py 파일로 만들어
.venv\Scripts\python.exe 로 돌린다.

소요를 재는 판마다 앞뒤로 다른 프로젝트 python 을 본다. 돌면 죽이지 말고
그 사실을 적고 「견주는 데 쓰지 않는다」 를 함께 적는다. 안 보고 잰 판은
「오염 여부 모름」 이라 적는다.

         Get-CimInstance Win32_Process -Filter "Name='python.exe'"

세션 중간에 묻지 않는다. 걸리면 아래 안전한 쪽으로 가고 값을 보고에 낸다.
  · 기대값은 갱신하지 않는다. 새 사실은 xfail(strict=True) 못으로 박는다
  · 조문 해석은 좁은 쪽
  · 범위 밖은 안 고치고 미해결에 이름만 올린다
  · 값이 갈리면 고르지 말고 갈린 값 전수와 갈린 까닭을 보고에 낸다

이 지시서 본문을 그대로 docs\directives\S169.md 에 앉힌다.
아래 수는 앞 세션 보고에서 옮긴 이차 자료다. 맞대기 전에 실물에서 다시 읽는다.
아래에 원인도 해법도 적지 않았다. 먼저 재고 나서 값으로 판단한다.


0절 — 착수. 그리고 파일별 소요를 한 판에 잰다

0-1. pull 한 뒤 다른 python 을 보고 전체 pytest 를 --durations=0 으로 한 판
     돌린다. 통과·xfail·skip·수집 건수와 소요, 판 상태를 적는다.
     파일별 건수와 파일별 도구 합을 표로 편다. 상위 다섯을 적고, 도구 합과
     벽시계의 격차도 적는다.

0-2. 0-1 값을 CLAUDE.md 9항 「무엇이 먹나」 칸의 앞 판 값(S137)과 맞댄다.
     · 시험이 늘었는데 소요가 준 것이 어느 파일에서 왔는지 값으로 가른다
     · 갈리지 않으면 안 갈린다고 적는다. 짐작해 적지 마라
     이 판이 깨끗하면 CLAUDE.md 9항에 파일별 값을 갱신한다. 오염되었으면
     안 옮기고 그 사실을 적는다.

커밋: S169 0절


1절 — 두 문턱이 무엇을 가르는지 다시 잰다 (조사)

S165 가 덱 벌 열여덟에서 부하율·기본요금 비중을 재어 네 갈래를 냈다
(둘 다 아래 15 · 부하율만 아래 3 · 비중만 아래 0 · 둘 다 위 0).
이 판이 다시 재고, 그 표가 못 본 것까지 본다.

1-1. 장04 와 장07 의 문장을 만드는 함수를 각각 찾아 조건식 원문을 그대로
     옮겨 적는다. 파일과 함수 이름을 함께 적는다.
     한 장이 낼 수 있는 문장이 몇 갈래인지 전수로 세고 갈래마다 글자를
     그대로 적는다 — 지금 안 서는 갈래도 함께 센다.

1-2. 덱 벌 열여덟에서 두 문턱 값을 다시 잰다. S165 표와 맞대고 다르면
     어디가 다른지 적는다. 벌마다 장04 문장과 장07 문장이 실제로 무엇이
     나오는지 글자로 함께 적는다 — 문턱 값만 재고 문장을 짐작하지 마라.

1-3. 벌마다 두 문장이 부딪치는지 판정한다. 부딪치는 벌 수와 이름을 적는다.
     S165 는 열다섯이라 적었다 — 같은지 본다.

1-4. 두 문장이 각각 무엇을 근거로 말하는지 가른다.
     · 장04 가 보는 값과 그 값이 어디서 오는지
     · 장07 이 보는 값과 그 값이 어디서 오는지
     · 두 값이 서로를 아는 자리가 코드에 있는지. 없으면 없다고 적는다

1-5. 같은 두 문장이 화면·Excel·Word 에도 서는지 전수로 훑는다. 서면 자리를
     적는다. 그물을 먼저 적고 한 번에 뽑는다 — 앞 세 판에서 이 종류의 수가
     판마다 달라졌으므로(넷 → 열셋 → 열넷) 그물이 무엇을 못 보는지도 함께
     적는다.

커밋: S169 1절


2절 — 두 장이 부딪치지 않게 고친다 (수정)

1절 값을 보고 고친다. 고치는 길을 여기에 적지 않았다 — 값을 보고 정한다.
정할 때 잣대는 셋이다. 셋을 다 적고 무엇을 골랐는지 근거와 함께 보고한다.

  · 계산을 바꾸지 않는다. 문턱 값도 바꾸지 않는다
  · 새 안내를 붙여 푸는 길과 문장을 고쳐 푸는 길이 있으면 문장 쪽을 고른다
  · 고친 문장이 열여덟 벌 전부에서 참이어야 한다. 한 벌에서라도 거짓이면
    그 길을 고르지 않는다

2-1. 고르기 전에 길을 둘 이상 적는다. 길마다 다음을 적는다 — 무엇을
     건드리나 · 열여덟 벌에서 참인가 · 지금 안 서는 갈래에서도 참인가 ·
     되돌리기. 그런 뒤 하나를 고르고 까닭을 적는다.

2-2. 고친다. PPT 두 장만 고친다. 1-5 에서 같은 문장이 다른 산출물에도 선
     것이 있으면 함께 고친다 — 한 산출물만 고치면 산출물끼리 갈린다.

2-3. 고친 뒤 덱 벌 열여덟에서 두 문장을 다시 뽑아 전수로 본다. 벌마다
     새 문장이 참인지 값으로 판정한 표를 편다. 한 벌이라도 거짓이면
     되돌리고 그 사실을 적는다.

2-4. 안 서는 갈래(비중만 아래 · 둘 다 위)에서도 새 문장이 참인지 본다.
     그 갈래를 세우는 벌이 저장소에 없으므로 값을 손으로 넣어 함수만
     불러 본다. 새 덱 벌을 짓지 않는다.

2-5. 살아 있는 못을 걷는다.
     tests\test_slides.py::test_두_장이_피크_여지를_반대로_말하지_않는다
     — 고치면 XPASS(strict) 로 빨개진다. 빨개지는 것을 먼저 보고 나서
     xfail 을 걷고 보통 시험으로 남긴다. 무는 뜻이 뒤집혔으면 이름과 본문을
     새 사실에 맞게 고친다.
     걷은 뒤 되돌려 확인한다. 한 번에 안 물면 안 문 것도 적는다 —
     S165·S168 이 못이 첫 판에 안 무는 것을 두 번 봤다.

2-6. 고치기 전과 후에 tools\screen_audit.py 와 tools\screen_budget.py 를
     돌린다. 1-5 에서 화면에도 선 것이 있을 때만 수가 움직인다 — 안 움직여야
     할 자리가 움직이면 그 사실을 적는다.

2-7. Excel·PPT·Word 를 고치기 전후로 뽑아 글자를 대조한다. 고친 자리 말고
     다른 곳이 갈리면 그 사실을 적는다.

2-8. 미해결에서 이 사실을 가리키는 항목을 닫는다. 이름으로 찾아 닫고
     닫은 이름을 보고에 적는다.

커밋: S169 2절


3절 — rules_kr.json 이름의 근거 (조사 → 값을 보고 수정)

S168 이 기준 데이터의 「경제성DR 시장 운영 시간대 (시)」 를 「입찰 시간대」 로
갈았다. 그 파일은 법령 유래 파일이라 항목마다 조문 근거가 붙는다.

3-1. 그 항목의 지금 이름·값·source·source_date·verified_on 을 그대로
     옮겨 적는다. data\defaults\ 사본과 글자가 같은지도 본다.

3-2. S168 이 연 규칙 원문(2026-04 [별표 26])에서 그 시간대를 부르는 말을
     찾아 원문 그대로 옮겨 적는다. 「입찰 시간대」 가 규칙이 쓰는 말인지,
     우리가 고른 말인지 판정한다.

3-3. 그 항목이 rules_kr.json 에 있는 것이 맞는지 판정한다. 잣대는
     collaboration.md 가 적은 것이다 — 조문을 못 찾으면 판단값 쪽이다.
     가르는 것은 키 이름이 아니라 어느 파일에 있는가다.
     · 조문 근거가 서면: 그대로 두고 근거가 선다고 적는다
     · 안 서면: data\assumptions.json 으로 옮긴다. 두 파일의 항목 수와
       data\defaults\ 사본이 함께 맞는지 확인한다
     판정이 안 서면 옮기지 말고 안 선다고 적은 뒤 미해결에 이름으로 올린다.

3-4. 고쳤으면 항목 수를 다시 세어 project-overview.md 7절의 수
     (rules_kr.json 35항목 · assumptions.json 49항목)와 맞댄다. 다르면
     문서를 갱신한다.

커밋: S169 3절


4절 — 마감

4-1. 전체 pytest. 통과·xfail·skip·수집 건수. 0-1 과 수집 건수를 맞대고
     다르면 왜 다른지 적는다. 판 상태를 적는다.
4-2. 케이스 스터디. 160/160 과 회귀값 여덟 불변. 판 상태를 적는다.
4-3. 화면 감사 네 조건. 2-6 값과 맞대고 project-overview.md 4절의 네 수와
     같은지 본다. 다르면 문서를 갱신하고 못이 통과하는지 본다.
4-4. ruff · mypy 셋 · scan_ctrl. 어느 명령의 무슨 수인지 함께 적는다
     (맨 mypy 170파일 · src 111파일 · tests tools 59파일).
4-5. 실물을 연다 — tools\render_deck.py 로 장04·장07 이 서는 벌을 뽑아
     png 로 두 장을 본다. 그림 실패 0 을 확인한다. 두 문장이 한 덱 안에서
     어떻게 읽히는지 보고 적는다.
     docs\OPEN_ITEMS.md 갈래 표를 갱신하고 못이 통과하는지 본다.
4-6. 미해결 건수 대조 — 전 N건 → 후 M건. 늘어난 것과 줄어든 것을 각각
     이름으로 적는다. 수만 적지 않는다. 보고와 PROCEED.md 양쪽에 적는다.
4-7. PROCEED.md 에 이 세션 절을 적고 커밋·push 한다. 항목 본문 상한 600자를
     지킨다. 「미해결」·「다음 작업」 두 칸에 갈래 번호를 쓰지 않는다.
4-8. HEAD 와 origin 이 같은지, 돌고 있는 셸·python·streamlit 이 없는지
     확인한다.

커밋: S169 4절


마지막 절 — 대조표

위 지시 항목 전부를 하나씩 대조한 표를 만든다. 항목 수는 여기 안 적는다 —
지시서를 세어 그 수만큼 줄을 만든다. 결과 칸은 수치나 판정으로 적고 30자
안쪽으로 쓴다. 길어질 내용은 아래 절에 표로 따로 편다. 표가 다 차야 끝난다.


보고

마지막에 한 번만 보고한다. 중간 보고와 중간 진행표를 내지 않는다.
낼 것은 여덟이다. 이 밖의 서술을 덧붙이지 않는다.
  1 대조표
  2 만든/바꾼 파일 — 파일마다 +/− 줄 수. 안 건드린 것도 한 줄로.
    src·data·tools·docs 를 빠짐없이 적는다
  3 수치가 움직인 곳 — 종전/지금. 없으면 「없음」
  4 판단한 것과 그 근거 — 2-1 에서 적은 길들과 고른 까닭을 포함한다
  5 돌린 것 / 안 돌린 것 — 안 돌린 것은 왜. 판마다 오염 여부를 적는다
  6 예상과 달랐던 것 — 없으면 「없음」
  7 프로젝트 지식 재업로드 — 이 판은 CLAUDE.md·project-overview.md 가 걸릴 수 있다
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로. 확인 안 한 못은 안 했다고

마지막 한 줄에 백그라운드 셸 확인 결과를 적는다.
ctx 는 세션 예산 종전 → 지금 · 가장 크게 먹은 것 하나 · 줄일 수 있었다고
보는 것 하나 셋으로 적는다.
