S180 — 9.4 뒤 문장의 참·거짓을 갈래 전수로 재고, 그 값이 허락하면 뺀다

이 판은 재는 절이 고치는 절을 막는 꼴이다. 순서와 멈추는 조건이 아래 못 박혀
있으므로 세션 도중 사람에게 묻지 않는다. 막히면 4절을 건너뛰고 값만 내고 닫는다.

원인·전제를 미리 적지 않았다. 무엇을 볼지만 적었고 무엇이 나올지는 안 적었다.
앞 판 보고의 수는 값이 아니라 다시 잴 것으로 다룬다.


0절 — 판 열기

0-1  어느 PC 인지 확인한다. os.cpu_count() · RAM · -n auto 가 고른 일꾼 수를
     값으로 적는다. 이 판의 모든 소요·메모리에 PC 를 함께 적는다.
0-2  git pull. HEAD 와 origin 이 같은지, 워크트리가 깨끗한지 본다.
0-3  남의 python 을 센다.

         Get-CimInstance Win32_Process -Filter "Name='python.exe'"

     0 이 아니면 그 사실을 적고, 이 판의 소요에 「견주는 데 쓰지 않는다」 를
     함께 적는다. 남의 세션을 죽이지 않는다. 판 앞뒤뿐 아니라 회귀 절 앞뒤로도
     한 번씩 센다.
0-4  브리핑을 뜬다.

         .venv\Scripts\python.exe tools\daily_brief.py

     미해결 건수와 「다음 작업」 칸 글자 수를 적는다.
0-5  기상 캐시에 용인 벌이 있는지 본다. 없으면 케이스 스터디 한 판을 먼저
     돌려 채운다.
0-6  앞 판이 이름으로 올렸는지 확인할 것 둘. docs\OPEN_ITEMS.md 와
     PROCEED.md 미해결 칸을 이름으로 훑어 아래 둘이 서 있는지 값으로 낸다.
     서 있으면 이름을 그대로 옮겨 적고, 없으면 7절에서 올린다.
       가) 부딪침 못이 짝의 한쪽(장04)만 문다
       나) 1번 PC 워크트리 CRLF 가 53 에서 52 로 줄었고 어느 파일인지 안 갈렸다

커밋하지 않는다.


1절 — 「한 번의 초과가 12개월간 적용된다」 가 참인 벌을 전수로 잰다

앞 판이 벌 넷만 쟀다. 이 절은 전수로 재고, 앞 판이 코드로만 읽은 것을 벌로
확인한다. 재는 스크립트는 스크래치 .py 로 만들어
.venv\Scripts\python.exe 로 돌린다 — 셸에 태우지 않는다.

1-1  9.4 글자가 서는 자리 여섯을 다시 센다. 앞 판 보고가 여섯이라 적었다 —
     그 수가 지금도 맞는지 tools\count_sites.py 또는 전수 grep 으로 값을 낸다.
     자리마다 어느 산출물인지 · 서는 조건이 무엇인지 한 줄씩.
1-2  덱 벌 전수에서 갈래(피크형 · 하한형 · 계약형)를 값으로 낸다. S170 이
     열여덟 벌을 셋으로 갈랐다 — 그 판정이 지금 벌 목록에도 그대로 서는지
     다시 재고, 벌마다 갈래를 표로 적는다.
1-3  1-2 의 벌마다 「9.4 경고가 서는가」 를 값으로 낸다. 서는 벌만 아래를 잰다.
1-4  서는 벌마다 아래를 값으로 낸다. 표 한 줄에 벌 하나.
       · 계약전력을 넘긴 달이 몇인가
       · 그 넘긴 값이 뒤 열두 달 요금에 실린 달이 몇인가 (분자/분모로 적는다)
       · 실리지 않았다면 무엇이 막았나 (계약전력 곱하기 · 비대상월 · 경부하 ·
         그 밖)
1-5  앞 판이 코드로만 읽고 벌로 안 잰 것을 벌로 잰다 — 요금적용전력 기준
     갈래에서 경부하 구간 초과 · 비대상월 초과가 뒤 달에 이월되는가. 이월되는
     벌과 안 되는 벌을 이름으로 낸다. 앞 판 보고의 9/9 와 어긋나면 어긋난 채로
     둘 다 적는다 — 한쪽을 지우지 않는다.
1-6  뒤 문장을 뺐을 때 남는 것을 값으로 본다.
       · 남는 첫 문장만으로 경고가 성립하는가 — 글자를 그대로 옮겨 적는다
       · 화면에서 매뉴얼로 가는 문(ⓘ 요지 · anchors)이 남는가. 남지 않으면
         그 사실을 적는다 — 이 판에서 고치지 않는다
       · docs\MANUAL.md 가 같은 사실의 전문을 쥐고 있는 줄 번호와 본문

커밋: S180 1절: 9.4 뒤 문장이 참인 벌을 전수로 쟀다


2절 — 이력으로 갈라낸다

고치기 전에 git 으로 셋을 값으로 낸다 — 언제 들어왔나 · 어느 판이 넣었나 ·
그때 조건이 무엇이었나.

2-1  원문 docs\REQUIREMENTS_kwise.md 9.4 뒤 문장의 이력. git log · git blame ·
     git show 로 그 줄이 들어온 커밋과 그때 글자를 낸다.
2-2  사본 셋(report\notices.py 의 CONTRACT_CHANGE_WARNING ·
     measures\contract.py 의 MARGIN_NOTICE · diagnose\contract.py 의
     _MARGIN_NOTICE)의 이력을 같은 방식으로 낸다. 셋이 같은 커밋에서 왔는지,
     따로 왔는지.
2-3  그 문장을 일부러 세운 판단이 이력에 있는가. 커밋 메시지와 그 판의
     PROCEED.md 절을 읽어 값으로 낸다. 있으면 본문을 옮겨 적는다.
2-4  S177 이 「빼는 갈래가 서지 않는다」 로 닫은 근거를 PROCEED.md 177세션
     절에서 그대로 옮겨 적는다. 그 결론이 매단 조건이 무엇인지 문장으로 낸다.

커밋: S180 2절: 9.4 뒤 문장과 사본 셋의 이력


3절 — 멈추는 조건 판정

아래 넷을 값으로 판정한다. 하나라도 「막는다」 면 4절을 건너뛰고 5절로 간다.
건너뛴 사실과 어느 조건이 막았는지를 보고에 적는다. 사람에게 묻지 않는다.

  가) 1-4 에서 계약형 벌의 분자가 0 이 아니다 (앞 판 값이 뒤집힌다) → 막는다
  나) 2-3 에서 그 문장을 일부러 세운 판단이 이력에 있다 → 막는다
  다) 1-1 에서 자리 수가 여섯이 아니고, 새로 는 자리가 이 판에서 함께 못
      고치는 곳이다 → 막는다
  라) 1-6 에서 남는 첫 문장이 경고로 성립하지 않는다 (문장 조각만 남거나
      비문이 된다) → 막는다

위 넷 어디에도 안 걸리면 4절로 간다. 1-5 가 앞 판의 9/9 를 뒤집는 것은
막는 조건이 아니다 — 방향이 굳는 쪽이므로 값만 적고 4절로 간다.

커밋하지 않는다.


4절 — 뒤 문장을 뺀다

원문과 사본 셋을 한 판에 함께 고친다. 하나만 고치면 S177 못이 문다.
파일 수정은 편집 도구로만 한다.

4-1  docs\REQUIREMENTS_kwise.md 9.4 에서 뒤 문장을 뺀다. 남는 글자를 보고에
     그대로 옮겨 적는다.
4-2  사본 셋을 같은 글자로 맞춘다 — report\notices.py · measures\contract.py ·
     diagnose\contract.py.
4-3  그 글자를 무는 시험 넷의 기대값을 함께 고친다 — tests\test_report.py ·
     tests\test_document.py · tests\test_diagnose.py · tests\test_measures.py.
     뒤 둘은 온 문장이 아니라 「12개월간 적용」 조각으로 문다. 줄 번호는 지금
     파일에서 다시 찾는다 — 앞 판 보고의 줄 번호를 그대로 쓰지 않는다.
4-4  앞 판이 박은 xfail 못
     tests\test_diagnose.py::test_계약형에서_12개월간_적용을_말하면_요금이_넘은_값을_싣는다
     이 XPASS(strict) 로 빨개진다. 그 못을 지우지 말고 보통 시험으로 갈아
     「계약형 벌의 산출물 글자에 12개월간 적용이 없다」 를 물게 한다. 이름도
     그 사실에 맞게 고친다.
4-5  4-4 의 보통 못을 되돌려 확인한다. 사본 하나씩 옛 글자로 되돌려 빨개지는지
     본다 — 셋 다 각각 한 판씩. 안 무는 사본이 있으면 그 사실을 적는다.
4-6  S177 못 test_요구사항서_9_4_원문과_글자_사본_셋이_한_글자다 가 초록인지
     보고, 원문만 옛 글자로 되돌린 판에서 빨개지는지 확인한다.

커밋: S180 4절: 9.4 뒤 문장을 뺐다 — 원문과 사본 셋


5절 — 부딪침 못이 짝의 한쪽만 무는 것을 메운다

5-1  tests\test_slides.py::test_두_장이_피크_여지를_반대로_말하지_않는다 가
     지금 무엇을 읽는지 값으로 낸다 — 장04 글자만 보는지, 장07 글자도 보는지.
5-2  짝의 양쪽을 다 물게 고친다. 장07 글자만 갈려도 빨개져야 한다.
5-3  되돌려 확인 둘 — 장04 만 옛 글자로 되돌린 판 · 장07 만 옛 글자로 되돌린
     판. 둘 다 빨개지는 것을 보고 git checkout HEAD -- 경로 로 되돌린 뒤
     git status 를 본다.
5-4  4절을 돌았으면 살아 있는 못이 0 이 된다. 미해결 「매뉴얼이 기본요금을
     요금적용전력으로 단정한다」 를 xfail(strict=True) 로 박는다 —
     docs\MANUAL.md 424 · 476 · 527 · 750행이 기본요금을 요금적용전력으로
     단정하지 않는다를 무는 시험이다. 매뉴얼 글자는 고치지 않는다. 그 못이
     평소 xfail 이고 --runxfail 에서 빨개지는 것을 값으로 확인한다.
     4절을 건너뛰었으면 5-4 도 건너뛴다 — 앞 판 xfail 이 그대로 서 있다.

커밋: S180 5절: 부딪침 못이 짝의 양쪽을 문다


6절 — 회귀

6-1  화면 감사를 고치기 전과 후에 돌린다. 네 수(을 · 갑Ⅰ · 교육갑 ·
     교육갑저압)와 규칙 위반 · 중복 후보를 적는다. 수가 움직였으면 까닭을 적는다.

         .venv\Scripts\python.exe tools\screen_audit.py

6-2  pytest. 0-1 에서 잰 PC 에 따라 갈린다.
     1번 PC 면 앞단에서 통째로 돌린다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     2번 PC 면 PROCEED.md 「pytest 분할 실행」 의 갈래 넷으로 나눠 돌린다.
     종료 코드로 판정하지 않는다 — 출력에서 실패 줄과 수집 건수를 직접 읽고,
     앞 판 건수(수집 1,762 · 1,761 passed · 1 xfailed)와 맞댄다. skip 수를
     함께 적는다.
6-3  소요를 적는다. PC · 일꾼 수 · 깨끗한 판인지를 함께 적는다. 앞뒤로 남의
     python 을 세고, 붙어 있으면 「견주는 데 쓰지 않는다」 를 적는다.
6-4  케이스 스터디.

         .venv\Scripts\python.exe tools\run_casestudy.py

     160/160 과 회귀값 여덟(C1 5,293 · 49.0% · 13.5% · R1 132 · 41.1% ·
     20.0% · R2 18.3% · R3 27.4%)을 값으로 확인한다. 기상 취득 횟수를 적는다.
     output\casestudy_YYYYMMDD.xlsx 를 저장소 판과 칸 단위로 맞댄다 —
     NaN 짝을 같은 것으로 세도록 fillna 로 메운 뒤에 센다. 소요 칸만 갈렸으면
     git checkout 으로 저장소 판을 되살린다. 이 맞대기는 7절 커밋 앞에 한다.
6-5  정적 검사 — ruff check · ruff format · 맨 mypy · mypy tests tools ·
     tools\scan_ctrl.py. 앞 판 수와 맞댄다.
6-6  워크트리 CRLF 를 센다. 0-6 나) 가 미해결에 서 있으면, 이 판에서
     53 → 52 로 줄어든 파일이 어느 것인지 값으로 낸다. 안 갈리면 안 갈렸다고
     적는다 — 짐작해 적지 않는다.

커밋: S180 6절: 회귀


7절 — 기록

기록을 다 고친 뒤에 묶음을 돌린다. 앞에 돌리면 옛 기록을 본다.

7-1  PROCEED.md 에 180세션 절을 쓴다. 「현재 상태」 표의 수(pytest · 케이스
     스터디 · 화면 감사 · 살아 있는 못 · 정적 검사)를 이 판 값으로 갈아 끼운다.
7-2  미해결 건수 대조를 이름으로 적는다 — 전 N건 → 후 M건, 늘어난 것과
     줄어든 것을 각각 이름으로. 수만 적지 않는다. 세션 목록표 마지막 행도
     이름으로 적는다.
7-3  docs\OPEN_ITEMS.md 를 갈래별로 맞춘다. 절 제목 합 · total_items · 줄 수
     셋으로 교차 검산하고 세 수를 보고에 적는다.
7-4  0-6 에서 안 서 있던 것을 이름으로 올린다.
7-5  docs\HANDOVER.md 를 이어 쓴다 — 다시 쓰지 않는다. 5절 「다시 열지 마라」
     표에 이 판이 정하거나 뒤집은 것을 조건과 함께 한 행씩 적는다. 4절을
     돌았으면 S177 행의 조건이 떨어진 사실을 그 행 곁에 적는다 — 행을 지우지
     않는다. 6절 못 표를 이 판 값으로 갈아 끼운다.
7-6  「다음 작업」 칸에 갈래 번호(②-N)를 쓰지 않는다. 이름으로만 적는다.
7-7  이 지시서 본문을 docs\directives\S180.md 로 그대로 앉힌다.
7-8  기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

7-9  커밋하고 git push. HEAD 와 origin 이 같은지 확인한다.

커밋: S180 7절: 기록 · 미해결 대조 · 지시서 앉히기


8절 — 보고

마지막에 한 번만 보고한다. 중간 보고를 내지 않는다. 정확히 여덟 항목이고
그 밖의 서술을 덧붙이지 않는다.

  1 대조표 — 절·항마다 한 줄. 결과는 수치나 판정으로. 칸은 30자 안쪽.
  2 만든/바꾼 파일 — 파일마다 +/− 줄 수. 건드리지 않은 것도 한 줄.
  3 수치가 움직인 곳 — 종전/지금 표. 없으면 없음.
  4 판단한 것 — 스스로 고른 갈래와 그 근거.
  5 돌린 것 / 안 돌린 것 — 안 돌린 것은 까닭.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 셋 중 이 판에 고친 것. 없으면 없음.
  8 박은 못 · 되돌려 확인한 못 — 이름으로. 못마다 「어디를 어떻게 틀렸더니
    빨개졌나」 한 줄. 확인 안 한 못은 확인 안 했다고 적는다.

ctx 칸을 셋으로 적는다 — 세션 예산 종전 → 지금(약 차이) · 가장 크게 먹은 것
하나 · 줄일 수 있었다고 보는 것 하나.

마지막 한 줄로 돌고 있는 백그라운드 셸이 없는지 확인한 결과를 적는다.
