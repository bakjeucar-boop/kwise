S153 지시서 — small-ind-a1 을 산업용 실측으로 갈아 끼운다

사람이 정했다 (S152). 지금 그 벌은 용인 부하에 산업용 저압을 씌운 합성이고
(render_deck.py:362 · csv=YONGIN_XLSX · 290 kW), 실측이 들어왔으므로 실측이 이긴다.

이 판은 덱 벌 하나를 갈고 산출물을 실물로 연다.
계산은 안 고친다 — src\kwise\tariff\ · measures\ · diagnose\ 를 한 줄도 안 바꾼다.
3절만 주석 한 자리를 고치고, 그 절이 계산에 닿으면 멈춤 규칙을 따른다.

## 지킬 것

- 세션 중간에 묻지 않는다. 갈림길은 이 지시서가 안전한 쪽을 못 박았다.
  그 밖에 걸리면 안전한 쪽 셋을 따른다 — 기대값은 갱신하지 않는다 ·
  조문 해석은 좁은 쪽 · 범위 밖은 안 고치고 이름만 올린다.
- 저장소가 정본이다. 이 지시서와 인수인계에 적힌 수·자리도 이차 자료다 —
  실물에서 먼저 확인하고 어긋나면 어긋남을 값으로 적는다.
- 조문은 원문에서 읽는다. 기억으로 적지 마라 — 어제 웹 대화창이 역률 상한과
  제7조 ① 을 기억으로 적어 둘 다 틀렸고 원문이 잡았다.
- 셸이 낸 수를 그대로 믿지 마라. 수는 두 방법으로 재서 같은지 본다.
  core.quotepath · grep -c $'\r*$' · 덱 벌을 key= 로 세는 것이 각각 거짓을 냈다.
- 파일 수정은 편집 도구로 한다. heredoc · echo · printf · sed/awk 치환 금지.
  스크래치 파일도 같다.
- PROCEED.md 에 grep 을 걸지 마라. 미해결 칸이 27,083자짜리 한 줄이다.
  daily_brief.open_items 로 이름을 떠 온다.
- 시험을 부를 때 -k 를 쓰지 마라. 노드 id 를 통째로 준다.
- 항목은 번호가 아니라 이름으로 부른다. 소요는 PC 를 함께 적는다.
- ruff format 어긋남 여덟과 맨 mypy 10건은 고치지 마라.
- 절마다 커밋한다. 보고는 마지막에 한 번만 한다.

## 0절 · 준비와 착수 회귀

git pull. 어느 PC 인지 확인해 적는다. 다른 프로젝트의 python 이 도는지 본다.

0-1. **HEAD 가 두 자리에서 다르게 적혀 있다** — docs\HANDOVER.md 는 52f96a0,
     브리핑과 S152 보고는 7f285e6 이다. 지금 HEAD 와 origin 을 값으로 보고
     어느 쪽이 맞는지, 52f96a0 이 무엇인지(있는 커밋인지) 짚는다.
     HANDOVER.md 가 틀렸으면 그 값을 고친다.
0-2. **착수 앞단 전체 pytest 를 먼저 돌린다.** 마감 회귀보다 먼저 낸다.

    .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     빨간 것이 있으면 이름과 실패 줄을 그대로 옮겨 적는다.
     S152 0-2 가 짚었다 — 마감 차례가 「회귀 → 세션 행 적기」 라 세션 행을 무는
     못은 언제나 다음 판 착수에서 처음 뜬다. 그 모양이면 그 사실을 적고 고친다.
0-3. daily_brief 를 돌려 아래와 맞댄다. 이차 자료다. 자수·바이트를 갈라 적는다.

    pytest 수집 1,701 · 케이스 11벌 160/160 · 미해결 80건 (① 7 · ② 72 · ③ 1)
    미해결 칸 27,083자 · 「다음 작업」 칸 2,897자 (상한 3,000)
    화면 감사 957 · 807 · 961 · 808 · 덱 벌 17 · 살아 있는 못 0건
    ruff format 8 어긋남 / 202 통과 (합 210) · 맨 mypy 10건 (checked 168)

0-4. 자료가 있는지 값으로 본다 — input\전력사용량_산업용(갑)저압.xlsx.
     168,506바이트여야 한다. **이 파일은 .gitignore:32 로 저장소 밖이고
     1번 PC 에만 있다.** 없으면 이 판을 열지 말고 그 사실을 보고하고 멈춘다.

커밋 — S153 0절: 착수 회귀와 HEAD 어긋남을 값으로 봤다

## 1절 · 덱 벌을 갈아 끼운다

1-1. render_deck.py 의 small-ind-a1 정의를 값으로 읽어 보고에 옮겨 적는다.
     줄 번호로 답하지 마라 — 본문을 옮기고 어디서 왔는지 꼬리표로 붙인다.
     같은 파일의 small-ind-a2(용인 · 산업용(갑)Ⅱ 고압A)도 함께 읽어
     무엇이 같고 무엇이 다른지 짚는다.

1-2. **갈림길 하나가 있다. 안전한 쪽을 미리 못 박는다 — 묻지 마라.**
     S150 이 small-ind-a1 과 케이스 R3 을 **같은 조건**으로 세워 덱과 회귀를
     맞대 볼 수 있게 했다. 이 벌을 갈면 그 짝이 깨진다 —
     R3 은 용인 자료(290 kW)에 남고 덱만 새 자료(75 kW)로 간다.
     **안전한 쪽 — 갈아 끼우되 짝이 깨진 사실을 미해결에 이름으로 올린다.**
     R3 은 건드리지 마라(넉 달이라 12개월 환산이 안 서서 회귀에 못 올린다).
     벌 이름이 지금 그대로여도 되는지 값으로 판단하고 근거를 적는다 —
     이름이 자료를 가리키고 있으면 바꾸고, 종별만 가리키면 그대로 둔다.

1-3. 벌을 갈아 끼운다. 값은 이렇다 (전부 S152 가 값으로 확인한 것이다) —
     자료 input\전력사용량_산업용(갑)저압.xlsx · 산업용(갑)Ⅰ 저압 ·
     option='single' · 계약전력 75 kW · 역률 100.
     역률을 어디에 넣는지 값으로 확인한다 — 지상 역률 칸이고,
     기존 벌들이 92(간주) 또는 85 를 든다.

1-4. **지역 좌표는 사람이 안 정했다. 묻지 마라.**
     안전한 쪽 — 지금 좌표(경기도/용인시)를 그대로 두고, **벌 정의 곁에
     「좌표는 자료에 없어 남겨 둔 값이다」 를 한 줄로 적는다.**
     그리고 미해결에 이름으로 올린다. 좌표를 지어내지 마라.
     이 벌이 태양광을 안 켜면 좌표가 값에 안 닿는지도 값으로 확인해 적는다.

커밋 — S153 1절: small-ind-a1 을 산업용 실측으로 갈아 끼웠다

## 2절 · 산출물을 실물로 연다

2-1. 덱을 뽑는다 (tools\render_deck.py --case small-ind-a1 --png).
     장 수 · 실패 수 · 소요를 적고 갈기 전과 견준다.
     **덱이 죽거나 장이 빠지면 그 자체가 값이다** — 오류 문장을 그대로 옮긴다.

2-2. png 를 펴서 눈으로 본다. 볼 것 다섯 —
     ㄱ 종별 이름이 「산업용전력(갑)Ⅰ」 로 뜨는가
     ㄴ 단가가 산업용 저압 행에서 오는가
     ㄷ 기본요금 기준이 **계약전력**인가 (요금표의 base_fee_basis 와 맞대라)
     ㄹ 기간이 넉 달로 뜨는가 — **12개월이나 「연간」 으로 적힌 자리가 없는가**
     ㅁ 역률 100 이 어떻게 뜨는가 — 접힌 값(97)인가 원값(100)인가
     맞지 않으면 값으로 적는다. 계산에 닿는 것은 고치지 마라.

2-3. **자릿수를 본다.** 이 벌은 기존 벌보다 세 자릿수 작다
     (일평균 397.6 kWh · 관측 최대 52.88 kW).
     큰 벌에서 안 보이던 것이 여기서 처음 보일 수 있다 —
     MWh/kWh 표기가 뒤집히는 자리 · 반올림으로 0 이 되는 자리 ·
     막대 조각 합이 합계와 어긋나는 자리. 전수로 보지 말고
     **png 에 실제로 보이는 것만** 값으로 적는다.

2-4. Excel 과 Word 도 뽑아 같은 다섯을 본다. 뽑는 데 드는 소요를 적는다.

2-5. 초과사용부가금 안내가 이 벌에서 뜨는지 값으로 본다.
     계약 75 kW 에 관측 최대 52.88 kW 라 초과가 0 이므로 안 뜰 것이다 —
     뜨든 안 뜨든 값으로 적는다. **조건을 만들려고 계약전력을 지어내지 마라.**

2-6. 화면 감사를 돌려 넷이 안 움직인 것을 확인한다.

커밋 — S153 2절: 새 벌의 산출물 넷을 실물로 열었다

## 3절 · 저압 역률 주석을 원문에 맞춘다

S152 가 값으로 봤다 — power_factor.py:11 주석이 제43조 ① 1호를 통째로
빠뜨리고 「고압 이상」 이라 적었다. 실제로는 저압도 대상이다
(제43조 ① 1호 — 저압 계약전력 20kW 이상 일반용·산업용·농사용·임시전력).

3-1. 원문을 다시 읽어 주석에 적을 문장을 정한다. 조문 번호와 본문을
     보고에 원문 그대로 옮긴다. 요약하지 마라.
3-2. 주석만 고친다. **코드는 한 줄도 바꾸지 마라.**
     고친 뒤 값이 안 갈린 것을 값으로 확인한다 —
     케이스 스터디와 앞단 전체가 갈기 전과 같아야 한다.
3-3. 제43조 ② 2호 다목(제42조 ② 단서에 걸리는 고객은 진상역률 요금을
     적용하지 않는다)이 코드에도 rules_kr.json 에도 없다.
     **이 판에서 세우지 마라** — 사람이 아직 정할 자리다.
     미해결 항목이 이미 있으면 본문에 이 판이 확인한 값을 보태고,
     없으면 이름으로 올린다.
3-4. 역률 100 이 새 벌에서 실제로 어떻게 계산되는지 값으로 본다.
     92% 초과 5%p × 0.2% = 1.0% 감액이 맞는지, 97% 상한에 걸리는지.
     S133 검산이 그 값을 적어 두었다 — 이차 자료이므로 값으로 다시 본다.
     **이 갈래가 회귀에 한 번도 안 섰다** (케이스 열하나가 전부 92 아니면 85).
     이 벌이 그것을 처음 세우는지 값으로 적는다.

**멈춤 규칙** — 3절에서 계산이 틀린 것이 드러나면 거기서 멈춘다.
계산을 고치지 마라. 갈린 값 전수와 어느 벌에서 얼마가 갈리는지를 적고,
시험을 xfail 로 박아 「살아 있는 못」 칸에 이름으로 올린다. 고치는 것은 다음 판이다.

커밋 — S153 3절: 저압 역률 주석을 원문에 맞추고 역률 100 갈래를 값으로 봤다

## 4절 · 문서를 값에 맞춘다

4-1. 미해결에서 「small-ind-a1 을 실측으로 갈아 끼운다」 를 닫는다.
     1-2·1-4 가 낸 것(덱↔케이스 짝 · 좌표)을 이름으로 올린다.
4-2. docs\TEST_DATA.md 7절(S152 가 세운 자리)에 이 판이 낸 값을 보탠다 —
     이 자료가 어느 덱 벌에서 쓰이는지, 회귀에는 안 올라간다는 것.
4-3. project-overview.md 가 이 벌이나 R3 을 적고 있으면 값에 맞게 고친다.
     고쳤는지 여부를 「프로젝트 지식 재업로드」 칸에 적는다.
     **안 어긋났으면 안 고친다.**
4-4. docs\HANDOVER.md 를 이 판 값으로 갈아 끼운다.
     **값만 담는다 — 판단·다음 판·지킬 것은 웹 대화창이 쓴다.**
     담을 것 — HEAD · pytest 수집과 결과 · 케이스 벌 수와 판정 ·
     회귀값 여덟(대형 넷 + 용인 넷 · **관측 최대 5,293.44 · 132.28 을 빼지 마라**) ·
     화면 감사 넷 · 덱 벌 수 · 미해결 건수와 갈래별 수 · 살아 있는 못 ·
     ruff·mypy·scan_ctrl · 요금표 파일과 schema · 이 판이 쓴 PC.
     값마다 언제 어느 판에서 잰 것인지 붙인다.
     **앞 인수인계에서 문장을 옮겨 적지 마라.**

커밋 — S153 4절: 문서를 값에 맞췄다

## 5절 · 회귀와 마감

5-1. 앞단 전체 pytest. 0-2 값과 맞대 같은지 본다. 수집 건수와 skip 수를 적는다.
     1번 PC 면 앞단 전체, 2번 PC 면 갈래별 분할.
5-2. 케이스 스터디. 160/160 · 회귀값 여덟 불변 · 기상 취득 횟수 · 소요(PC 함께).
     **이 판은 덱 벌만 갈았으므로 케이스가 움직였으면 그 자체가 결함이다.**
5-3. screen_audit 넷을 0절 값과 맞댄다.
5-4. ruff check · ruff format --check . · mypy · mypy src · scan_ctrl.
     곁수를 적을 때 「무엇을 앞이라 부르는지」 를 함께 적는다.
5-5. docs\OPEN_ITEMS.md 를 함께 고친다. 미해결 대조를 이름으로 낸다 —
     전 80건 → 후 M건, 늘어난 것과 줄어든 것을 각각 이름으로.
5-6. PROCEED.md 에 153세션 절을 적고 「현재 상태」·「다음 작업」 칸을 고친다.
     「다음 작업」 칸이 2,897자로 상한 3,000자까지 103자밖에 안 남았다.
     걸리면 상한을 올리지 말고 가장 오래된 판치를 걷는다 — 걷기 전에 그 판치가
     적은 것이 미해결이나 세션 절에 다 있는지 전수로 확인하고,
     없는 것이 있으면 먼저 등재한다. 걷은 판치와 남긴 판치 수를 적는다.
5-7. 이 지시서를 받은 본문 그대로 docs\directives\S153.md 에 앉힌다.
5-8. 커밋하고 push 한다. HEAD 와 origin 이 같은 것을 값으로 확인한다.
     백그라운드 셸이 없는 것을 확인한다.

커밋 — S153 5절: 마감

## 마지막 보고

대조표부터 낸다. 지시서 항목마다 한 줄, 결과는 수치나 판정으로.
2절 실물 확인(다섯 항목 × 산출물)은 대조표 뒤에 표로 따로 편다.
그 뒤에 여덟을 낸다 — 만든/바꾼 파일 · 수치가 움직인 곳 · 판단한 것 ·
돌린 것/안 돌린 것 · 예상과 달랐던 것 · 프로젝트 지식 재업로드 ·
박은 못과 되돌려 확인한 못 · ctx.
표는 칸을 30자 안쪽으로 좁게 유지한다. 이 밖의 서술을 덧붙이지 마라.
