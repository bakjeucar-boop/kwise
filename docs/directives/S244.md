S244 — 고침 판 · 뿌리 후보 「A 단순 합과 합산효과」 가운데 기록으로 서는 넷을 고치고 · 후보 「F 카드 머리 상수」 · 「J 조합 역률 경고」 를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 과 후보 B 는 닫혔다. S243 이 후보 A 를
쟀다. 이 판은 **A 의 네 항목(나-22 · 마-14 · 나-2 · 마-15)을 고치고 · 후보 F 와 J 를 재기만 한다**(고치지
않는다). A 의 나-18 은 사람이 화면을 여는 문구 판 몫이라 0줄. A 의 마-16 은 사람이 정했으나 계산이 바뀌는 판이라
다음 판 몫이다 — 이 판은 기록만 한다(0줄). 이 밖으로 가지 않는다. 항목 이름은 243세션 절 4-4 표에서 찾는다.

**사람이 정했다.**
  마-16 (S243 뒤 · 이 판이 HANDOVER 5절에 한 행으로 앉힌다) — 조합에서 DR 감축 가능량은 태양광 · ESS 를 반영한 조합
    부하로 다시 잰다. ESS 는 피크 저감에 쓴 뒤 남은 부하 기준이고 · ESS 를 DR 전용으로 돌리는 운전 전략은 넣지
    않는다. 고침은 다음 판(계산 변경).
  앞서 정한 것(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만) — 절사 A · 단위
    기준 · 측정 중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지
    않는다 · S165(요약표) · S182 귀속 기준 ㄱ(카드 절감액 = 총액 차 + 청구 밖 몫 · 태양광 잉여 · DR 정산금) · S207 ·
    운영시간 외 부하는 비중(S238) · 고객 산출물 말투는 「~습니다」(S242).

**웹 대화창이 정했다(S243 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (나-22) — 적힌 차이(표기 글자)가 0 이면 그 이유 줄을 세우지 않는다(S233 ㄴ · S237 결정 ㄴ).
  결정 2 (마-14 · 용어) — 「단순 합」 은 한 가지 뜻이다 — 3단계에서 합산효과와 맞대는 수, 곧 그 조합에 넣은(체크한)
    수단의 카드 절감액 합. 요약표의 켠 수단 전부 합계는 다른 사실이라 「단순 합」 이라 부르지 않는다 — 그 표가
    이미 쓰는 합계 이름을 쓰고, 없으면 「합계」 로 적는다. 요약표는 그대로 선다(S165). 계산값은 안 바뀐다.
  결정 3 (나-2) — 단순 합에서 빠진 미산출 수단을 알리는 글을 표 밑에 더하지 않는다(화면 문구 늘리지 않는다). 그
    수단이 같은 표에 「미산출」 줄로 서 있으면 참이다. 안 서 있는 자리와 · 단순 합과 합산효과가 서로 다른 수단
    묶음을 세는 자리는 고치지 않고 값을 낸다.
  결정 4 (마-15) — 합산효과와 그 회수기간은 S182 귀속 기준 ㄱ 대로 청구 밖 몫(DR 정산금 · 태양광 잉여)을 담는다 —
    화면 · PPT · Excel · Word 가 같은 정의다. 기간 값 / 12개월 값은 S220(ㅅ 기간 대 12개월) 결정 글자대로 산출물마다
    따른다 — S220 이 그 자리를 정하지 않았으면 그 자리는 고치지 않고 값을 낸다. 같은 자리에서 그 몫을 말하는 글은
    셈과 맞게 이미 있는 이름(「DR 정산금」 등)으로 적는다.

**S243 보고가 적었다 — 실물에서 먼저 확인하라.** 「단순 합」 — 요약표는 켠 수단 전부 · 지표는 체크한 수단만 /
「합산효과」 — 화면은 12개월 · DR 정산금 포함 · PPT 는 기간 값 · DR 제외 / 나-22 는 지금 2벌에 선다 / 후보 F 는 세
항목 · 유형 ① · 19벌 · 후보 J 는 두 항목 · 유형 ① · 16벌(S242 5-2).

**울타리에 하한이 있다** — 1-5 가 센 「고칠 자리 수」. 결정 1 ~ 4 를 끝까지 적용하는 데 필요한 번짐은 울타리
안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한 자리도 안
    움직인다.** 움직이면 멈춘다.
  · **값이 움직여도 되는 칸은 결정 4 가 정의를 맞추는 합산효과 · 그 회수기간 칸뿐이다** — 1-4 목록 안. 그 밖의
    금액 · 기간 칸이 갈리면 멈춘다. 움직인 칸은 전 → 후를 값으로.
  · **갈리는 글자는 1-2 ~ 1-4 목록 안뿐이다.** 목록 밖 글자가 갈리면 멈춘다.
  · 요약표를 내리거나 줄을 빼지 않는다(S165).
  · **나-18 · 마-16 은 코드 0줄** — 마-16 은 기록(5절)만.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-5 가 센 자리 밖 0줄.** 요금 ·
    절감액을 만드는 식은 0줄.
  · **4절(F · J)은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄 · tools\ 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면
    「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다 — 짐작한 시각을 커밋에 넣지
    않는다(S243 이 한 번 그랬다). 커밋 메시지는 편집 도구로 지은 .txt 로 git commit -F.** 절마다 따로 커밋한다 —
    다른 절 몫을 앞 절 커밋에 싣지 않는다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다. 좁은 판 · 못 하나 · 되돌려 확인도 3-1 꼴 그대로 — `-n` · `--dist` ·
    `--tb=short` · `-s` 를 붙이지 않는다(S242 · S243 이 붙였다).**
  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 ·
    같은 옵션으로 나눠 뜬다(나눈 묶음과 까닭을 적는다). 도구가 벌을 골라 뜰 수 없거나 나눠 뜬 판도 끊기면 멈춘다.
    전체 pytest 갈래가 끊기면 다시 돌지 않고 멈춘다. 멈추면 3절까지 커밋하고(push 하지 않는다) 끊긴 자리를
    보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를 쓰지 않는다
    — `| Out-Null` · `| ForEach-Object` 도 파이프다(S243 이 썼다). 여러 줄을 훑을 때는 찾기 도구(Grep)나 읽기
    도구로 본다.** 조사 에이전트를 띄우면 이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S243 보고는 858bc0e) · 워크트리.
0-2  미해결 건수(137) · 회귀값 여덟 · 덱 스냅 기준선(S243 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S244 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — S165 · S182 · S220(ㅅ 기간 대 12개월) · S233 ㄴ · S237 결정 ㄴ · 243세션 절 4-4 를 글자 그대로 · 결정
     주인. S220 이 합산효과 · 회수기간 자리마다 기간 / 12개월을 어떻게 정했는지. 결정 1 ~ 4 와 어긋나는 글자가
     있는지.
1-2  **나-22(결정 1) · 나-2(결정 3)** — 이유 줄이 서는 자리와 조건 · 적힌 차이가 0 인 벌 · 고친 뒤 글자(예정). 단순
     합이 빼는 미산출 수단이 서는 벌 · 그 수단이 같은 표에 「미산출」 줄로 서는지 · 단순 합과 합산효과가 세는 수단
     묶음이 같은지.
1-3  **마-14(결정 2)** — 「단순 합」 을 적는 자리 전수(tools\count_sites.py · 그물 밖 자리 따로) · 자리마다 무엇을
     더하나 · 요약표 합계가 이미 쓰는 이름 · 고친 뒤 이름(예정).
1-4  **마-15(결정 4)** — 합산효과와 그 회수기간을 만드는 자리 · 적는 자리 전수 · 산출물마다 지금 정의(청구 밖 몫
     포함 여부 · 기간 / 12개월) · S220 이 정한 기준 · 바뀌는 칸과 전 → 후(예정) · 그 몫을 말하는 같은 자리의 글.
1-5  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S244 1절: 고칠 자리


2절 — 고친다

2-1  **1-5 가 센 자리를 고친다.** 결정 1 ~ 4 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S244 2절: 단순 합과 합산효과


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 화면
     시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과 한 프로세스에서 돌면
     기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이
     빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 결정 1 ~ 4 가 다 서는 벌(DR 정산금이 서는 벌 · 나-22 가 서는 벌 포함)을 가장 적게 골라(고른
     벌과 까닭) 뜬다. **산출물 넷(PPT · Excel · Word · 일괄)을 통째로 S243 뒤 스냅과 맞대** 갈린 줄이 전부 1-2 ~ 1-4
     목록 안인지 · 합산효과 · 회수기간이 네 산출물에서 같은 정의인지 본다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만
     3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「적힌 차이가 0 이면 이유 줄이 없다」 (2) 「「단순 합」 은 합산효과와 맞대는
     한 자리에만 선다」 (3) 「합산효과와 그 회수기간은 네 산출물이 같은 정의다(청구 밖 몫 포함)」 를 문다. 산출물을
     실제로 만든 글자를 본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고
     빨간 것을 본다. 재료 확인 글자가 고친 뒤 글자를 담아 되돌림이 얕아지지 않게 한다.
3-4  **덱 19벌 한 판** — S243 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 ~ 1-4 목록 안인지 · 값이 움직인 칸이 결정 4 칸뿐인지.
     그 밖의 금액 칸이 갈리면 멈춘다. 이 판의 19벌 실물은 4절이 쓴다 — 지우지 않는다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-5 자리 밖이 갈리면
     멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-5 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로. 1번 PC 면 아래
     명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S244 3절: 못 · 실물 · 회귀


4절 — 뿌리 후보 「F 카드 머리 상수」 · 「J 조합 역률 경고」 재기 (고치지 않는다 · 3-4 가 뜬 19벌 실물로 잰다)

4-1  **증상** — F 세 항목 · J 두 항목마다 몸 · 지금 실물에서 서는지 · 벌 수 · 예시 글자(서는 산출물).
4-2  **이력과 자리** — 항목마다 언제 어느 세션이 올렸고 무엇을 재거나 고쳤나(PROCEED.md · HANDOVER · git log). 그
     글자를 만드는 자리 전수(계산 폴더 몫 따로) · 자리마다 무엇을 보고 서나(상수 · 입력 · 계산값).
4-3  **걸린 결정** — 저장소 전체에서 이 두 묶음에 닿는 결정(S207 「그 벌에서 참인 말만」 · S154 · S206 · S237 결정
     ㄴ · CLAUDE.md 「화면 문구」 절 · HANDOVER 5절 등) 글자 그대로 · 결정 주인. 「사람이 정한다」 가 선 세션과 사람이
     답한 기록이 있는지.
4-4  **판정** — 항목마다 4-3 의 기록된 결정으로 잣대가 서는가 / 안 서는가. 안 서는 항목만 선택지를 길마다 한 줄씩:
     무엇을 건드리나 · 금액 · 계산값이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 되돌리기 · 드는 판 수. **사람이 읽는
     말로 · 고르지 않는다.**
4-5  4절 값은 5-1 의 244세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 244세션 절 — 사람 결정(마-16 · 앞서 정한 것은 가리키기만)과 웹 대화창 판단(결정 1 ~ 4)을 갈라서 ·
     1 ~ 4절 값. 나-18 은 문구 판 몫 · 마-16 은 「사람이 정함(S243 뒤) · 고침은 다음 판(계산 변경)」 으로 한 줄씩. 세션
     목록표에 244 행.
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다. 마-16 항목은 닫지 않는다(고침 전). 미해결 건수 대조를 이름으로.
     이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리
     진행표 A · F · J 행(A 는 마-16 고침 대기 · 나-18 문구 판) · 5절에 사람 결정 한 행(마-16)과 웹 대화창 판단 행(결정
     1 ~ 4) · 머리 사슬에 S244 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(마-16 조합 부하로 DR 다시 재기(계산
     변경 · 사람이 정함) · 나-18 문구 판 · F · J 의 판정 · 묶음 C · D · E · G · H · I · 이 판이 고치지 않고 값으로 낸 자리).
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S244.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S244 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-5 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 값이 움직인 칸(결정 4) 전 → 후 · 회귀값 여덟 · 총액 · 케이스
    갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를 따로. 다시 돈
    판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ J 포함).
  그 뒤에 「단순 합과 합산효과 고침 표」 — 항목(나-22 · 마-14 · 나-2 · 마-15)마다 전 → 후 · 값이 움직인 칸 · 고치지
    않은 자리와 까닭.
  그 뒤에 「F 카드 머리 상수 · J 조합 역률 경고」(4절) — 항목마다 증상 · 이력 · 자리 · 걸린 결정 · 판정(서는 / 안
    서는) · 안 서는 항목의 선택지 표. 사람이 읽는 말로. 코드 자리는 꼬리표로만.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.


---

S244 보정 — 3-8 을 이어서 돈다

· 다시 돌려도 된다. 차례는 아래 그대로.
    ② → ③ → ④ (PROCEED.md 「pytest 분할 실행」 명령 그대로 · 하나씩) → 3-9 정적 검사 다섯 → 3절 커밋 →
    4절(F · J 재기) → 5절 기록(243 행 날짜 꼴 「(09-25 ~ 26)」 을 브리핑이 읽는 꼴로 고치고 244 행을 단다) →
    ① 한 판(명령 그대로) → 5-7 기록 묶음 → 5-8 push → 6절 보고.
· ① 의 앞 판 실패 셋은 기록 · 생성물 탓이었다 — 코드를 고치지 않는다. 5절 뒤 ① 이 코드 탓으로 빨가면 지시서
  3-8 대로(고친 뒤 3-1 부터).
· ② 가 또 메모리 부족으로 끊기면 곧바로 다시 돌지 않는다. ② 명령의 시험 파일 다섯(test_casestudy · slides ·
  document · report · base_fee_basis_words)을 같은 플래그로 한 파일씩 차례로 돈다(끊긴 판은 판 수에 넣고 까닭 한
  줄). 한 파일 판도 끊기면 거기서 멈춰 보고한다(push 하지 않는다). ③ · ④ 가 끊겨도 같은 꼴이다.
· 돌리기 전에 이 판이 띄운 python 이 남아 있으면 내린다. 남의 python 은 건드리지도 · 재지도 · 적지도 않는다.
  파이프 · `| Out-Null` · `| ForEach-Object` 금지는 그대로다.
· PPT · Word 기간 합산효과의 DR 정산금은 이 판에서 고치지 않는다(값으로 낸 그대로). 5-4 「다음 작업」 칸의 「마-16
  조합 부하로 DR 다시 재기」 후보 이름 옆에 「기간 합산효과의 DR 정산금 셈 포함」 을 붙인다.
· 이 보정문도 docs\directives\S244.md 에 본문 뒤에 붙여 앉힌다.
