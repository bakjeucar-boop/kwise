S202 — 조합에서 역률 개선 몫이 0 인 한 줄을 고친다

**계산을 고치는 판이고 고치는 자리는 하나다.**
S200 이 값으로 냈다 — compare\combination.py:522 가 켠 역률 수단의 목표를
원 부하 역률로 덮어, 조합에 역률 개선을 켜도 몫이 19벌 모두 0.0 이다.
2단계 카드가 452만원(large-b)이라 말하는데 3단계는 0원이라 말한다.
사람이 갈래 ㄱ(켠 수단이 목표를 쓴다)를 골랐다 — **새 산식을 만들지 않고
이미 2단계가 쓰는 잣대를 3단계에 쓴다.**
S201 이 잰 예상 폭은 16벌 +257,871 ~ +15,370,595원이다 — **값이 아니라
다시 잴 것이다.**
세션 도중 사람에게 묻지 않는다.

이 판의 못 박힌 안전한 쪽 열넷.
  · **고치는 계산 자리는 하나다** — compare\combination.py 의 그 한 줄
    (줄 번호는 지금 파일에서 다시 찾는다). 그 밖의 계산 자리를 고쳐야
    하면 **멈추고 값만 낸다.** 울타리는 폴더가 아니라 자리 수다.
  · **1절이 멈추는 자리다.** S198 정본과 ㄱ 가 다른 말이면 **2절을 안 열고
    값만 낸다.** 정본(docs\REQUIREMENTS_kwise.md 8.1)을 이 판에서 안 고친다.
  · **ui\ 는 0줄이다.** 화면 문구를 이 판에서 안 고친다 — 문구는 다음 판이
    한 판에 모은다. **금액이 움직여 글자가 거짓이 되는 자리는 이름으로 올린다.**
  · **태양광 뒤 역률 가정을 건드리지 않는다** — measures\solar.py 282~293행의
    식과 power_factor_after_pct 는 0줄이다. 사람이 그 갈래(ㅁ)를 접었다.
  · **원인·전제를 미리 적지 않았다.** 앞 판 보고의 수(19벌 폭 · 452만원 ·
    줄 번호 522)는 전부 **다시 잴 것**이다. S201 이 「다음 작업 칸 2,991자」 를
    값으로 옮겨 적었다가 실제 2,261자였다 — 같은 병을 반복하지 않는다.
  · **멈추는 잣대 넷.** 하나라도 걸리면 그 자리에서 멈추고 되돌린 뒤 값을 낸다.
      · 회귀값 여덟이 움직인다
      · 케이스 판정 174 가 174 가 아니다
      · **2단계 역률 개선 카드 값이 19벌 가운데 하나라도 움직인다**
      · **역률 수단이 안 든 조합의 값이 움직인다**
  · **역률 100 세 벌은 0 이 옳다** — S201 이 요금 비율 상한 97 로 값을 냈다.
    그 셋에서 몫이 0 이 아니게 되면 멈추고 값을 낸다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. **얕은 되돌림을
    조심한다** — 되돌림이 실제로 그 조각을 세웠는지 값으로 먼저 확인한다.
    부득이 src\ 를 더 돌렸으면 git checkout HEAD -- 경로 로 되돌리고
    git status 를 본다.
  · **git stash 를 쓰지 않는다.** S140 스태시(2026-09-07)를 지우지 않는다.
  · **셸에 문자열을 태우지 않는다 — 세 판 연속 샜다**(S199 python -c ·
    S200 커밋 메시지 here-string 다섯과 파이프 · S201 python -c 와 파이프).
    **python -c 를 한 번도 쓰지 않는다.** 재는 것 · 세는 것 · 한 줄 확인 ·
    버전 찍기까지 전부 스크래치 .py 로 만들어 .venv\Scripts\python.exe 로
    돌린다(시스템 python 을 부르지 않는다). 커밋 메시지는 스크래치 .txt 에
    써서 git commit -F 로 단다. **수를 세는 일에 PowerShell 파이프를 쓰지
    않는다** — 세는 것도 .py 다. 셸에는 파일 이름만 싣는다.
  · 백그라운드로 보내기 전에 예상 소요를 말한다. 근거는 S201 값이다 —
    덱 76판을 여덟 묶음으로 7분 13초 · ㅁ 통째 판 292.9초 · 케이스 130.6초.
    상한을 걸고, 넘기면 스스로 멈추고 보고한다. 대기 루프를 만들지 않는다.
  · 앱을 띄운 채 끝내지 않는다. 케이스 결과 xlsx 는 저장소 판을 먼저 뜬
    뒤에 돌린다(같은 날 덮인다).
  · **권하지 않는다.** 값을 내고, 스스로 본 쪽이 있으면 근거와 함께 따로 한 줄.
  · 미해결 이름에 「 · 」 를 넣지 않는다.


0절 — 판 열기

0-1  어느 PC 인지 확인한다. os.cpu_count() · RAM · -n auto 가 고른 일꾼 수.
     1번 PC 면 pytest 를 통째로, 2번 PC 면 갈래 넷으로.
0-2  git pull. HEAD 와 origin 이 같은지, 워크트리가 깨끗한지.
     git stash list 를 보고 수만 적는다 — 건드리지 않는다.
0-3  남의 python 을 센다. 세는 규칙은 CLAUDE.md 2항이 쥔다 — 센 자리마다
     값을 다 적는다. 0 이 아니면 소요에 「견주는 데 쓰지 않는다」 를 붙인다.
0-4  브리핑을 뜬다. 미해결 건수 · 미해결 칸 글자 수 · 「다음 작업」 칸 글자 수.
     **지금 값을 잰다 — 앞 판 보고의 수를 옮기지 않는다.**
0-5  기상 캐시에 용인 벌이 있는지 본다. PROJECT_CACHE 가 가리키는 자리를 본다.
0-6  이 판이 건드릴 미해결의 지금 이름을 docs\OPEN_ITEMS.md 에서 그대로
     옮겨 적는다. 이름이 다르면 문서의 이름을 따른다.
0-7  **고칠 한 줄을 지금 파일에서 다시 찾는다.** 파일 · 줄 번호 · 그 줄의
     글자. S200 이 522 라 적었으나 S198 이 513 이라 적었다가 주석 줄로
     드러난 일이 있다 — **이름과 글자로 찾고 줄 번호는 결과로 적는다.**

커밋하지 않는다.


1절 — ㄱ 가 S198 정본과 같은 말인가 (이 절이 멈추는 자리다)

**이 절은 가르기만 한다. 고치지 않는다.**

1-1  **S198 정본 원문을 옮겨 적는다** — docs\REQUIREMENTS_kwise.md 8.1 의
     글자 그대로. 그 글자가 **출발점을 말하나 도달점을 말하나** 값으로 가른다.
1-2  **ㄱ 가 무엇을 말하는지 적는다** — 켠 역률 수단이 3단계 조합에서
     목표(97)를 도달점으로 쓴다. **출발점은 원 부하 역률 그대로다.**
1-3  **둘이 같은 말인지 다른 말인지 판정한다.** 셋 중 하나로.
       ㄱ **같은 말이다** — 정본은 출발점만 정하고 도달점을 안 정한다.
         2절을 연다
       ㄴ **다른 말이다** — 정본이 도달점까지 정하고 있고 ㄱ 와 어긋난다.
         **2절을 안 연다.** 어느 글자가 어긋나는지 값으로 내고 멈춘다
       ㄷ **못 가른다** — 무엇이 더 있어야 갈리는지
1-4  **S200 이 「갈래 셋 모두 S198 정본 못 202행을 빨갛게 한다」 고 적었다.
     지금 값으로 다시 잰다** — 그 못
     (test_조합의_태양광_전_역률은_원_부하_역률이고_수단의_목표가_아니다)이
     **무엇을 무는지** 값으로 낸다. 출발점을 무나 도달점을 무나.
     **ㄱ 에서 초록인지 빨간지** 스크래치로 갈아 끼워 값으로 본다.
     빨가면 어느 줄이 왜 빨간지 적는다. **못을 이 절에서 안 고친다.**
1-5  **2단계 역률 개선 카드가 쓰는 잣대를 값으로 적는다** — 출발점과 도달점
     각각. ㄱ 가 그것과 같은 잣대인지 값으로 맞댄다.
     **다르면 다르다고 적는다** — 같다는 것이 이 갈래를 고른 근거였으므로
     다르면 그 근거가 무너진다. 그때는 **2절을 안 열고 멈춘다.**

커밋: S202 1절: ㄱ 와 정본의 관계


2절 — 한 줄을 고친다

**1-3 이 ㄱ 이고 1-5 가 「같은 잣대」 일 때만 이 절을 연다.**
안 서면 이 절을 안 열고 그 사실과 까닭을 값으로 적는다.

2-1  **저장소 판 값을 먼저 뜬다** — 덱 19벌의 조합 합산효과 · 조합 속
     역률 몫 · 2단계 역률 카드. 고치기 전 값이 있어야 맞댈 수 있다.
2-2  **그 한 줄을 고친다.** 고친 줄 수를 적는다. **한 줄을 넘으면 몇 줄이고
     왜인지 적는다** — 두 줄을 넘으면 멈추고 값을 낸다.
2-3  **덱 19벌 전수로 값을 낸다.** 벌마다 다섯 —
     조합 속 역률 몫(전 / 후) · 조합 합산효과(전 / 후) ·
     **2단계 역률 카드(전 / 후 — 불변이어야 한다)** ·
     태양광 뒤 역률(불변이어야 한다) · 역률 수단이 안 든 조합(불변이어야 한다).
     **불변이어야 하는 셋 가운데 하나라도 움직이면 멈추고 되돌린 뒤 값을 낸다.**
2-4  **역률 100 세 벌이 여전히 0 인지** 값으로 확인한다. 0 이 아니면 멈춘다.
2-5  **2단계 카드와 3단계 몫이 같은 벌에서 몇 대 몇인지** 낸다. 같아야 하는
     것이 아니다 — 밑이 다르므로(S200 1-3 · large-b 452.8M 대 498.6M)
     **다른 것이 옳다.** 두 수와 그 밑을 함께 적는다.
2-6  **되돌려 확인한다** — 그 줄을 고치기 전으로 되돌리면 몫이 다시 0 이
     되는지 값으로. 되돌림이 실제로 섰는지 값으로 먼저 본다.
2-7  **회귀값 여덟이 안 움직인 것을 값으로 확인한다.**

커밋: S202 2절: 조합에서 역률 개선이 목표를 쓴다


3절 — 움직인 값이 산출물에 다 따라오나

**이 절은 실물을 본다. 화면 글자를 안 고친다.**

3-1  **S200 이 센 열일곱 자리를 지금 값으로 다시 센다**(화면 9 · PPT 2 ·
     Excel 3 · Word 3). 그물을 적고 **그물이 못 보는 것**을 함께 적는다.
     **S200 이 「PPT 장16 조합 그림은 차트가 아니라 그림 파일이라 그물에
     안 잡혔다」 고 적었다** — 그 자리를 어떻게 볼지 적고 본다.
3-2  **네 산출물이 같은 수를 적는지** 실물로 본다. 벌 하나를 골라
     화면 · PPT · Excel · Word 를 떠서 네 값을 나란히 적는다.
     **어느 벌을 골랐는지와 왜 그 벌인지 적는다.**
     **화면은 png 를 함께 본다 — DOM 텍스트를 실물로 읽지 않는다.**
3-3  **안 따라온 자리가 있으면 이름으로 올린다.** 이 판에서 안 고친다 —
     고치면 자리 수가 늘어 울타리를 넘는다.
3-4  **금액이 움직여 글자가 거짓이 되는 자리를 전수로 센다.** S201 이
     `large-b-pf85` 2단계 경고를 예로 냈다 — 지금 값으로 다시 보고,
     거짓이 되는 자리를 이름으로 올린다. **고치지 않는다.**

커밋: S202 3절: 열일곱 자리 실물 확인


4절 — 못

4-1  **이 판이 고친 자리를 무는 못을 박는다** — 조합에 역률 개선을 켜면
     몫이 0 이 아니라는 것을 **실물 값으로** 무는 못. 소스 글자나 식을
     다시 적는 못은 안 박는다. **부호나 「0이 아니다」 로 묶지 않는다** —
     갈리는 값으로 문다.
4-2  **역률 100 벌에서 0 인 것도 함께 무는지** 본다. 한 못으로 둘을 물면
     둘로 갈릴 때 못이 못 잡는다 — 갈라 박을지 값으로 판정하고 적는다.
4-3  **박은 못마다 되돌려 확인한다** — 어디를 어떻게 틀렸더니 빨개졌는지
     한 줄씩. 확인 안 한 못은 확인 안 했다고 적는다.
4-4  **1-4 가 잰 S198 정본 못이 지금 초록인지** 다시 확인한다.
     빨가면 멈추고 값을 낸다.
4-5  **xfail 넷이 어떻게 됐는지** 값으로 낸다. 닫힌 것 · 그대로인 것 ·
     새로 빨개진 것. **xfail 을 이 판에서 새로 달지 않는다** — 새로
     빨개지면 멈추고 값을 낸다.
4-6  되돌림이 원복됐는지 git status · git diff 로 확인한다.

커밋: S202 4절: 못


5절 — 회귀

5-1  git diff 로 **고친 자리가 계산 한 줄뿐**인지 확인한다. 폴더마다 한 줄 —
     **ui\ · data\ · measures\ 가 0줄**인 것을 명시한다.
5-2  pytest. 1번 PC 면 앞단에서 통째로.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     2번 PC 면 갈래 넷으로. 종료 코드로 판정하지 않는다 — 실패 줄과 수집
     건수를 직접 읽고 앞 판(수집 1,781 · 1,777 passed · 4 xfailed · skip 0)과
     맞댄다. skip 수를 함께. **수집 건수를 세는 것도 .py 로 한다.**
5-3  소요를 적는다. PC · 일꾼 수 · 0-3 의 값 전부 · 깨끗한 판인지 ·
     상한 8분까지 얼마 남는지. 깨끗하면 --durations=0 을 붙여 도구 합과
     상위 스물다섯을 낸다. 앞 세 판과 맞댄다 — S199 4분 54초(도구 합
     1,567.06초) · S200 5분 18초 · S201 4분 58초.
5-4  화면 감사. 네 수를 앞 판(955 · 806 · 962 · 807)과 맞댄다.
     **ui\ 가 0줄이므로 불변이어야 한다** — 갈렸으면 어느 글자가 갈렸는지
     적고 까닭을 값으로 낸다.
5-5  케이스 스터디. 174/174 과 회귀값 여덟. 기상 취득 횟수.
     **저장소 판을 먼저 뜬 뒤에 돌린다.** xlsx 를 저장소 판과 칸 단위로
     맞대고(fillna 뒤에 센다) **소요 칸 밖이 갈렸으면 그것이 이 판이
     움직인 값인지 값으로 가른다.** 케이스 스터디에 조합을 계산하는 자리가
     없으므로(project-overview.md) 안 갈리는 것이 예상이다 — 갈렸으면 적는다.
5-6  정적 검사 다섯.

커밋: S202 5절: 회귀


6절 — 기록 · 닫기

6-1  PROCEED.md 에 202세션 절을 쓴다. 1·2·3절 표는 이 절에 둔다.
6-2  미해결 건수 대조를 이름으로 적는다 — 늘어난 것과 줄어든 것을 각각
     이름으로. 이름에 「 · 」 를 넣지 않는다. 세션 목록표 마지막 행도 이름으로.
6-3  docs\OPEN_ITEMS.md 를 갈래별로 맞춘다. 세 방법 교차 검산 값을 다 적는다.
6-4  **자료 대기 항목 하나를 올린다** — 「태양광 뒤 역률 가정이 건물 상황에
     따라 상쇄될 수 있다」. 사람이 그 갈래를 접었고, 가르려면 태양광 전
     역률과 순부하 여부가 있어야 한다(S201 2-3). **갈래는 자료 대기다.**
6-5  docs\HANDOVER.md 를 이 판 값으로 간다 — 제목의 끝 판 이름 · 3절 표의
     S202 행 · 5절 「다시 열지 마라」(이 판이 정한 것이 있으면) · 6절 못 표.
     고친 자리와 안 고친 자리를 갈라 적는다.
6-6  **S192 뿌리표에 이 뿌리의 칸을 세운다.** S201 6-5 가 「뿌리표는 있으나
     이 뿌리의 칸이 없다」 고 적었다 — 어느 뿌리 줄에 붙일지 값으로 정하고
     적는다. 못 정하면 못 정한다고 적고 안 만든다.
6-7  CLAUDE.md 9항 1번 PC 칸을 이 판 값으로 간다. 5-3 이 --durations=0 을
     붙인 깨끗한 판이 아니면 그 사실을 적고 안 고친다.
6-8  「다음 작업」 칸에 갈래 번호를 쓰지 않는다. 이름으로만. **상한 3,000자를
     넘지 않는지 값으로 확인하고 지금 글자 수를 적는다.**
6-9  이 지시서 본문을 docs\directives\S202.md 로 그대로 앉힌다.
6-10 기록을 다 고친 뒤에 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

6-11 커밋하고 git push. HEAD 와 origin 이 같은지 확인한다.
     **커밋 메시지는 스크래치 .txt 에 써서 git commit -F 로 단다.**

커밋: S202 6절: 기록 · 미해결 대조 · 지시서 앉히기


7절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — **절·항마다 한 줄이고 하나도 빠뜨리지 않는다.**
    0-3 칸에 센 자리마다 값을 다 적는다. **1-3 은 ㄱ/ㄴ/ㄷ 로 한 줄.**
    **1-5 는 같은 잣대/다른 잣대로 한 줄.** 2절을 열었는지 안 열었는지
    한 줄. **2-3 의 「불변이어야 하는 셋」 은 셋을 각각 한 줄씩.**
  2 만든/바꾼 파일 — **고친 계산 자리가 하나인 것과 ui\ · data\ ·
    measures\ 가 0줄인 것을 폴더마다 명시한다.** 건드리지 않은 것도.
  3 수치가 움직인 곳 — **종전/지금 표.** 16벌이 움직였으면 벌마다.
    **움직이면 안 되는 것이 움직였으면 그것은 멈춘 자리다.**
  4 판단한 것 — 1-3·1-5 의 판정 근거 · 1-4 가 잰 못이 무는 것 ·
    3-1 그물이 못 보는 것 · 4-2 의 갈라 박기 판정. **권고는 적지 않는다.**
  5 돌린 것 / 안 돌린 것 — 안 돌린 것은 까닭. 백그라운드로 보낸 것이
    있으면 예상 소요를 미리 적었는지 함께.
  6 예상과 달랐던 것 — 없으면 없음. **S201 이 잰 폭(+257,871 ~
    +15,370,595원)과 이 판 실측이 다르면 그 차를 값으로.**
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 셋 중 이 세션에 고친 것. 셋 밖의 파일은 적지 않는다.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로. **xfail 이 몇에서 몇이
    됐는지 한 줄.** 확인 안 한 못 수도.

마지막에 셸에 문자열을 태운 자리가 있었는지 한 줄로 적는다 — **python -c ·
커밋 메시지 · 수를 세는 파이프를 모두 센다. 세 판 연속 샌 자리다.**
없으면 없다고 적는다. ctx 칸을 셋으로 적는다. 그리고 띄운 앱을 내렸는지와
포트, 백그라운드 셸 확인 결과를 적는다.
