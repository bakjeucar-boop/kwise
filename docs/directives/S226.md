S226 — 도구 조사 판 · 고치지 않는다

**이 판은 조사 판이다. src\ · tools\ · tests\ 0줄 · 산출물 0벌 · 새 못 0.**
재는 코드는 스크래치에서만. 다음 판(도구 판) 지시서를 쓰기 전에, 도구
빈자리마다 무엇이 얼마나 새고 무엇을 잃는지 값으로 센다.

**사람이 정했다 — S225 뒤 웹 대화창에서.** 다음 판들은 도구 판이다. 첫 판은
이 조사 판이다. 무엇부터 고칠지는 이 판 값을 보고 웹 대화창이 정한다.

**S222 ~ S225 보고가 적은 것 — 전부 재게 할 것이다.** 원인을 미리 적지 않았다.
증상만 옮긴다.
  ㄱ 인자 없이 부른 `pytest_counts.py` 가 `run_tool` 이 받은 판(python_* 이름)을
    못 집고 앞 파일을 집었다(S222 · S223 이 규칙으로 설명 · 「라」 에 올랐다)
  ㄴ `run_tool` 에 `.py` 를 주면 WinError 193(S224' · 규약은 이미 있다)
  ㄷ 칸 글자 수 · 갈래 수를 재는 도구가 없다(S222 개시 절 평가 · S223 셸 자리)
  ㄹ 남의 python 을 세는 도구가 없다(S223 · S224 · S224' · S225 셸 자리)
  ㅁ 긴 칸 머리를 뜨는 도구가 없다(S222 · 「도구가 없어 셸이 새는 자리」 여섯째)
  ㅂ 캐시 · 스냅 파일 목록과 수정 시각을 뜨는 도구가 없다(S224 · S225)
  ㅅ `run_tool` 은 끝나야 출력을 쓴다 — 뒤로 돌린 덱의 진행을 못 본다(S225)
  ㅇ 화면 캡처 도구가 역률 85 벌에서 역률 자리가 없고 앵커가 안 서 5분 넘게
    기다리다 죽었다(S223)
  ㅈ `deck_words.py` 가 투명하게 감춘 글자를 센다 — 알파를 안 본다(S225)
  ㅊ 화면 좌표 재기 · 되돌려 확인 뼈대 · 케이스 xlsx 칸 맞대기가 판마다 새
    스크래치이고, 1번 PC 에서 지은 것을 2번 PC 가 다시 지었다(S225 · 미해결
    두 항목)
  ㅋ 브리핑 — 「224'」 를 못 잡아 직전 세션이 S224 로 뜬다 · 「마」 갈래를 안
    보인다 · 「내가 밟아야 할 것」 이 사람 결정 몫을 안 센다 · 「다시 열지
    마라」 가 S160 · 「최근 세션」 이 109세션에 멈췄다 · 클립보드 복사
    「Access is denied」(S223 · S224')
  ㅌ ruff format 통과 수가 지시서 `.md` 로 판마다 하나씩 는다(S224')
  ㅍ 덱 19벌 한 판이 2번 PC 에서 1,080.7초 · 판 벽시계의 약 40%(S225)
  ㅎ 판마다 보고의 「셸 자리」 표와 2번 PC 판 벽시계가 기록에 안 남는다
    (S224' · S225)

이 판의 못 박힌 안전한 쪽.
  · **저장소 코드 · 도구 · 설정을 안 고친다.** 재는 코드는 스크래치 `.py`.
  · **자리를 새로 넓히지 않는다** — 위 열넷과 미해결 「라」 갈래 · 「도구가
    없어 셸이 새는 자리」 항목이 대상이다. 조사하다 새 자리가 보이면 이름만
    적고 재지 않는다.
  · **고칠 방법을 설계하지 않는다.** 자리마다 「무엇을 보면 고쳤다고 할 수
    있나」 한 줄과 코드 자리 수까지만.
  · 세션 도중 사람에게 묻지 않는다.
  · **git stash 를 쓰지 않는다.** 1번 PC 면 S140 스태시를 안 지운다.
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    재는 것은 스크래치 `.py` 로. 긴 출력은 `tools\run_tool.py --tail` ·
    `pytest_counts.py` 에는 받은 출력 파일을 인자로 · `deck_words.py` 는
    온 경로 · `run_tool` 에 `.py` 를 직접 주지 않는다. **도구로 되는 자리에
    파이프를 쓰지 않는다.** 썼으면 쓴 자리마다 「무엇을 보려고 · 왜 도구로
    못 했나」 — **이 판의 셸 자리도 조사 대상에 넣는다.** 커밋 메시지는 편집
    도구로 지은 `.txt` 로 `git commit -F`.
  · **덱 19벌을 뜨지 않는다.** ㅍ 은 기록과 이미 남은 출력으로 잰다 — 벌 하나를
    뜨는 것은 되고, 그 소요를 적는다.
  · 전체 pytest 는 안 돌린다 — 코드를 안 고친 판이다. 기록 묶음만 돌린다.
  · 앱을 띄우면 포트를 적고 내린 뒤 닫힌 것을 확인한다. **벽시계를 절마다 잰다.**


0절 — 개시

0-1  PC(스크래치 `.py`) · `git pull` · HEAD 와 origin(S225 보고는 28f203e) ·
     워크트리 · `git stash list`.
0-2  남의 python — 값과 어느 프로젝트인지. **어떤 명령으로 셌는지 그대로
     적는다**(ㄹ 의 재료).
0-3  미해결 — 건수 · 갈래별 수 · 세 방법 검산. **「라」 갈래 항목을 이름으로
     전수로 옮긴다** — 그 가운데 도구에 걸린 것을 표시한다(어느 도구인지).

커밋: S226 0절: 개시


1절 — 도구 빈자리를 센다

1-1  **자리를 모은다** — 위 열넷 · 0-3 의 도구 항목 · `PROCEED.md` 에 남은
     셸 · 파이프 · 도구 실패 기록(S200 이후). 같은 자리를 하나로 묶고 묶은
     근거를 적는다.
1-2  **자리마다 갈래 셋 가운데 하나로 판정한다** — 값으로.
       · 없다 — 그 일을 하는 도구가 저장소에 없다
       · 안 불렀다 — 도구가 있는데 판이 안 썼다(도구 이름 · 규약에 적혀
         있는지 · CLAUDE.md 도구 목록에 있는지)
       · 틀리게 잰다 — 도구가 있고 불렀는데 실물과 다른 값을 낸다
     **증상을 지금 저장소에서 다시 재 본다**(재현 되나 · 안 되나). 재현 못
     하면 못 했다고.
1-3  **자리마다 센다** — 기록에 남은 판 수(몇 판에서 샜나 · 어느 판) · 잃은
     시간(기록에 값이 있는 것만 · 없으면 「기록 없음」) · 뒤 뿌리 판(ㄱ 절사 ·
     ㄷ 자릿수)이 그 도구로 재는가 · 그 도구가 내는 수가 기록 · 못에 들어가는가.
1-4  **「틀리게 잰다」 자리는 영향을 값으로** — 그 도구의 수가 들어간 기록 ·
     못 · 판정이 어디 몇 곳인지. 이미 틀린 수가 기록에 앉았으면 이름으로.
     **고치지 않는다.**
1-5  **ㅍ 덱 소요** — 두 PC 의 덱 한 판 소요를 기록에서 판마다 · 벌마다 어디에
     시간이 드는지(기록이나 이미 남은 출력으로 · 벌 하나 뜨기는 된다).
1-6  **자리마다 「무엇을 보면 고쳤다고 할 수 있나」 한 줄과 코드 자리 수** —
     어느 파일인지까지. 계산 폴더 여섯에 닿는 자리가 있으면 이름으로.

커밋: S226 1절: 도구 빈자리


2절 — 묶음을 제안한다

2-1  **자리를 한 표로** — 자리 · 갈래 · 판 수 · 잃은 시간 · 뒤 뿌리 판이 쓰나 ·
     틀린 수가 기록에 앉았나 · 코드 자리 수.
2-2  **한 판 분량의 첫 묶음을 스스로 제안하고 근거를 적는다** — 한 판 분량은
     앞 판들(S222 · S224 · S225)의 고친 코드 자리 수와 소요로 잡는다. 「틀리게
     잰다」 를 어떻게 두었는지 따로 적는다.
2-3  「안 불렀다」 자리는 도구를 만들지 않고 닫을 수 있는지 — 규약 · 도구
     목록의 어느 글자가 그 판에 안 닿았는지 값으로.

커밋: S226 2절: 묶음


3절 — 기록 · 닫기

`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.

3-1  `PROCEED.md` 에 226세션 절 — 1절 · 2절 값. 미해결 건수 대조를 이름으로.
3-2  **1-1 에서 미해결에 이름이 없는 자리는 「라」 에 이름으로 올린다**(이름에
     「 · 」 를 넣지 않는다 · 본문 600자 상한). 이미 있는 항목과 같은 자리면
     새로 세우지 않는다.
3-3  **「도구가 없어 셸이 새는 자리」 항목을 1-2 판정에 맞춘다** — 그 항목의
     자리가 「없다」 가 아니면 이름과 몸을 고친다.
3-4  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
3-5  `docs\HANDOVER.md` — 머리 사슬에 S226 · 1절 값. **2번 PC 판이면 2절을
     이 판 값으로.**
3-6  「다음 작업」 칸 — 「도구 판 — 묶음은 웹 대화창이 S226 값으로 정한다」 를
     이름으로. 글자 수와 여유.
3-7  이 지시서 본문을 `docs\directives\S226.md` 로 그대로 앉힌다.
3-8  **기록을 다 고친 뒤에** 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

3-9  커밋하고 `git push`. **HEAD 와 origin 이 같은지 · 워크트리가 깨끗한지를
     보고 1항에 적는다.**

커밋: S226 3절: 기록 · 미해결 대조 · 지시서 앉히기


4절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — 절·항마다 한 줄. 마지막 줄에 HEAD = origin · 워크트리.
  2 만든/바꾼 파일 — +/− 줄 수. src\ · tools\ · tests\ 0줄을 값으로.
  3 수치가 움직인 곳 — 기록의 수가 고쳐진 자리만. 없으면 없음.
  4 판단한 것 — 1-1 묶은 근거 · 1-2 판정이 갈린 자리 · 1-4 · 2-2.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 가운데 고친 것.
  8 못 — 박은 못 0 을 값으로.

  **그 뒤에 「도구 자리 표」(2-1) — 자리마다 한 줄.**
  **그 뒤에 「첫 묶음 제안」(2-2) — 넣을 자리 · 뺄 자리 · 근거 한 줄씩 ·
  「틀리게 잰다」 를 어떻게 두었는지.**
  **그 뒤에 「틀린 수가 앉은 자리」(1-4) — 없으면 없다고.**
  **그 뒤에 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다). ctx 셋(종전 → 지금 · 가장 크게
먹은 것 · 줄일 수 있었던 것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
