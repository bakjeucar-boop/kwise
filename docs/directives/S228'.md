S228' — 오늘 판 넷(S225 · S226 · S227 · S228)을 닫는 확인 · 기록 판

**이 판은 확인 · 기록 판이다. src\ · tools\ · tests\ 0줄 · 산출물 0벌 · 새 못 0.**
전체 pytest 는 안 돌린다 — 코드를 안 고친 판이다. 기록 묶음만 돌린다.

이 판의 못 박힌 안전한 쪽.
  · **기록끼리 어긋나면 실물(코드 · git · 실행 값)에 맞게 기록을 고친다.
    코드 · 도구 · 설정 · 규약(`CLAUDE.md` 6항 · 9항 규약 글)은 안 고친다.**
  · **미해결 항목을 새로 세우는 것은 이 지시서가 적은 둘과, 어긋남이 새
    사실일 때만** — 이름으로, 이름에 「 · 」 를 넣지 않는다, 본문 600자 상한.
  · 사람이 정할 것을 새로 만들지 않는다. 세션 도중 사람에게 묻지 않는다.
  · 앞 판 보고의 수는 전부 다시 잴 것이다.
  · **git stash 를 쓰지 않는다.** 1번 PC 면 S140 스태시를 안 지운다.
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    개시 값은 `tools\open_values.py` · 칸 글자 수 · 머리는 `daily_brief.py`
    칸 인자 · 파일 목록은 `tools\cache_files.py` · 긴 출력은
    `tools\run_tool.py --tail`. **도구로 되는 자리에 파이프를 쓰지 않는다.**
    썼으면 쓴 자리마다 「무엇을 보려고 · 왜 도구로 못 했나」.
  · **지시서에 없는 pytest 플래그를 붙이지 않는다** — 기록 묶음 명령은 아래
    그대로(S228 이 미리보기에 `--tb=short` 를 붙였다).
  · **절마다 따로 커밋한다**(S228 이 0절 · 1절을 한 커밋에 담았다).
    커밋 메시지는 편집 도구로 지은 `.txt` 로 `git commit -F`.
  · 미해결은 이름으로 떠 읽는다 — 칸을 통째로 흘리지 않는다. **벽시계를
    절마다 잰다.**


0절 — 개시

0-1  `tools\open_values.py` — PC · 남의 python. `git pull` · HEAD 와
     origin(S228 보고는 c725805) · 워크트리 · `git stash list`.
0-2  `tools\cache_files.py` 로 `runs\도는중\` 이 비었는지.

커밋: S228' 0절: 개시


1절 — 오늘 기록끼리 맞댄다

항목마다 「어느 파일 어디 글자 ↔ 무엇(실물 · 다른 기록)」 과 맞는지 · 어긋나면
어느 쪽이 맞는지 값으로. **고치는 것은 3절에서 한 번에.**

1-1  **미해결 건수 사슬** — S225 앞 152 → S225 뒤 → S226 뒤 → S227 뒤 →
     S228 뒤. 판마다 늘어난 것 · 줄어든 것을 이름으로 · 갈래별 수 · 세 방법
     검산.
1-2  **`docs\HANDOVER.md`** — 제목 · 머리 사슬이 S225 ~ S228 을 담는지 ·
     1절 HEAD 행 · **5절에 S225 가 앉힌 사람 결정(역률 100 건물 전력삼각형은
     그림 안 글자로 말한다)이 있는지** · 틀린 수가 앉은 자리 셋(S227)과 그림 안
     글자 기준선 이동(S228) 줄이 있는지 · 6절 딱지가 S228 판 것만 남았는지 ·
     **2절(2번 PC 절)이 S228 값인지** · 7절 기준 예시.
1-3  **`CLAUDE.md` 9항** — 2번 PC 칸이 S228 판 값(수집 · 결과 · 갈래 넷 소요 ·
     깨끗한 판)과 `PROCEED.md` S228 절이 같은지 · S227 이 오염된 판 칸에
     까닭(전원)과 함께 섰는지 · 1번 PC 칸이 S224 판 그대로인지 · 도구 규약에
     S227 · S228 이 세우고 고친 도구(`open_values` · `cache_files` ·
     `run_tool` 이 `.py` 를 받고 도는 동안 쓴다 · `pytest_counts` 인자 없이 ·
     `daily_brief` 칸 인자)가 다 있는지.
1-4  `PROCEED.md` 세션 목록표의 S225 ~ S228 행이 각 절과 맞는지 · 「현재 상태」
     머리가 S228 을 담는지.
1-5  `docs\directives\` 에 S225 ~ S228 이 있는지.
1-6  **정적 검사 기준** — 기록의 ruff format 값이 .py 기준(S227 부터)으로 서
     있는지 · 옛 기준과 섞인 자리가 없는지.
1-7  확인 안 한 못 — 수(S228 보고 69) · 못 총수(73) · 딱지가 어느 판 것인지.
1-8  **S228 보고의 「지시서와 어긋난 자리」 넷이 `PROCEED.md` S228 절에
     있는지** — 없으면 3절에서 적는다.

커밋: S228' 1절: 기록 맞대기


2절 — 브리핑

2-1  `tools\daily_brief.py` 를 돌려 머리 · 「직전 세션」 · 「오늘 첫 작업」 ·
     미해결 칸이 이 판 기록 뒤 값을 담는지(3절 뒤에 한 번 더 돌린다).
     **「직전 세션」 이 「228'」 로 뜨는지**(S227 이 고친 자리).
2-2  클립보드 복사가 이 판에 나는지만.

커밋: S228' 2절: 브리핑


3절 — 기록 · 닫기

3-1  **1절 · 2절의 어긋남을 고친다** — 자리마다 무엇을 무엇에 맞췄는지.
     고치지 않은 자리는 까닭과 함께.
3-2  **미해결 「라」 에 이름 둘을 올린다**(이미 같은 자리가 있으면 새로 세우지
     않고 그 항목 몸에 적는다).
       · 판 개시 값 도구가 전원 상태를 안 내 깨끗한 판을 가를 수 없다 —
         S227 이 배터리로 돌아 1.6 ~ 1.8배 느렸고 S228 이 셸로 가렸다.
       · 2번 PC 갈래 넷의 소요 상한이 규약에 없다 — `CLAUDE.md` 6항은 실행마다의
         예상 소요이고 8분은 1번 PC 전체 실행의 글이다(S228 1-3). 갈래 명령에
         `--durations=0` 이 없어 느려진 시험을 못 맞댄다(S228 1-2)는 것도 함께.
3-3  「다음 작업」 칸 — 이름으로만. **후보 — 고르지 않는다**: 도구 판 셋째
     묶음(남은 도구 자리 이름 · 3-2 의 둘 포함) · ㄷ 자릿수 뿌리(ㅈ 이 닫혀
     앞 조건이 섰다) · ㄱ 절사 뿌리. 글자 수와 여유.
3-4  `PROCEED.md` 에 S228' 절 — 어긋남 수 · 고친 자리 · 미해결 대조를 이름으로.
     세션 목록표 행.
3-5  `docs\HANDOVER.md` 머리 사슬 · 1절을 이 판 값으로. 2절은 1-2 에서
     어긋남이 있을 때만 고친다. 6절 딱지는 이 판에 새로 달 것이 없다.
3-6  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
3-7  이 지시서 본문을 `docs\directives\S228'.md` 로 그대로 앉힌다.
3-8  **기록을 다 고친 뒤에** 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

3-9  `daily_brief.py` 를 한 번 더 돌려 2-1 을 다시 본다.
3-10 커밋하고 `git push`. **HEAD 와 origin 이 같은지 · 워크트리가 깨끗한지 ·
     `runs\도는중\` 이 비었는지 · 남의 python 을 보고 1항에 적는다.**

커밋: S228' 3절: 기록 · 미해결 대조 · 지시서 앉히기


4절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — 절·항마다 한 줄. 1-1 은 건수 사슬로. 마지막 줄에 HEAD = origin ·
    워크트리 · 도는중 · 남의 python.
  2 만든/바꾼 파일 — +/− 줄 수. src\ · tools\ · tests\ 0줄을 값으로.
  3 수치가 움직인 곳 — 기록의 수가 고쳐진 자리만. 없으면 없음.
  4 판단한 것 — 1-6 · 1-8 · 2-1 · 3-1 · 3-2.
  5 돌린 것 / 안 돌린 것.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 가운데 고친 것.
  8 못 — 박은 못 0 을 값으로 · 확인 안 한 못 수.

  **그 뒤에 「기록 어긋남」 표** — 자리 · 무엇과 무엇이 · 고침 / 안 고침.
  **그 뒤에 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다). ctx 셋(종전 → 지금 · 가장 크게
먹은 것 · 줄일 수 있었던 것). 백그라운드 셸 확인 결과.
