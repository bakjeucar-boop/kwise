S250 — 고침 판 · 후보 N(Excel 표기 문 밖 시트) · K 앞 둘 · M 앞 하나 · 일괄 생성 옛 입력 이름을 고치고 · L · M 뒤 항목 재료와 홀로 남은 항목 가르기를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 과 후보 B ~ G 는 닫혔고 · A · H · J 와 I 의 마-15 는 문구
판 · I 의 고압 계약형은 단가 자료 대기다. S249 4절이 새 뿌리 후보 K ~ N 과 홀로 남은 25 항목을 냈다. 이 판은 **N 과 K 의 앞 두
항목 · M 의 앞 항목 · 일괄 생성 옛 입력 이름을 고치고 · ESS 사양 표 만원 칸은 기록으로 잣대가 서면 고치고 · L 과 M 의 뒤 항목의
사람 결정 재료와 홀로 남은 항목 가르기를 재기만 한다.** K 의 셋째 항목(문구 판)과 문구 판 몫은 0줄. 이 밖으로 가지 않는다. 항목
이름은 249세션 절 4절에서 찾아 이름으로 대조한다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).** 절사 A(같은 표 안에서 적힌
수끼리의 셈이 맞게 · 계산값 불변) · 단위 기준(모든 단위를 통일할 필요는 없다 · 같은 표 안에서는 단위가 달라선 안 된다 · 가독성
있게 쉼표 등) · 측정 중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지
않는다 · S143 · S154 · S182 · S198 · S205 · S206 · S207 · S220 · 운영시간 외 부하는 비중 · 고객 산출물 말투는 「~습니다」 ·
마-16 · 마-35 · 기간 합산효과에 기간 DR 정산금 · 마-33 · 나-17 · 마-24.

**웹 대화창이 정했다(S249 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (후보 N) — Excel 에서 표기 문을 거치지 않는 시트도 표기 값을 사실마다 한 자리에서 만든 것으로 적는다(S233) · 단위 기준을
    따른다. 계산값은 안 바뀐다 — 표기 글자만.
  결정 2 (K 앞 둘 · M 앞 하나) — 249세션 절 4-2 가 「기록된 결정으로 잣대가 선다」 고 판정한 그 기준대로 고친다. 1-1 이 확인한
    기준 글자와 어긋나는 자리는 고치지 않고 값을 낸다.
  결정 3 (ESS 사양 표 만원 칸) — 사람 결정 절사 A · 단위 기준으로 잣대가 서면 고친다. 안 서면 고치지 않고 4절 재료로 낸다.
  결정 4 (위약금 음수) — 육지 식은 규칙 원문대로 두고 원문에 없는 하한(0)을 짓지 않는다. 코드 0줄 · 기록만.
  결정 5 (일괄 생성 입력 이름) — S249 가 바꾼 DR 가격 입력의 옛 열쇠도 같은 뜻으로 읽는다(일괄 생성은 늘 육지 가격이다). 새 열쇠는
    그대로 두고 · 둘 다 주어지면 새 열쇠를 쓴다. 사람 자료가 읽기에서 멈추지 않게 하는 것이다.

**S249 보고가 적었다 — 실물에서 먼저 확인하라.** K 계약전력 조정 판정 — 항목 셋 · 「하한이 요금적용전력에 걸리지 않아」 10벌 ·
「상향 검토」 2벌 · 셋째만 사람 결정(문구 판) / L PPT 주의사항 걸름 — 항목 둘 · PPT 0벌 · 사람 결정 필요 / M 조합 절감액 숫자 열
— 항목 둘 · Excel 조합 비교에 칸 「0」 19벌 51줄 · 뒤 항목만 사람 결정 / N Excel 표기 문 밖 시트 — 항목 둘 · 19벌 전부 · 사람
결정 불요 / 홀로 남은 25 / ESS 사양 표 만원 칸 1벌(large-b-over) / 일괄 생성 DR 가격 입력 이름을 dr_smp_won_per_kwh 로 바꿨다 ·
옛 열쇠는 읽기에서 멈춘다.

**울타리에 하한이 있다** — 1-6 이 센 「고칠 자리 수」. 결정 1 ~ 3 · 5 를 끝까지 적용하는 데 필요한 번짐은 울타리 안이다 — 자리와
까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **계산값은 한 자리도 안 바뀐다 — 회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 · 케이스 스터디 타당성.** 움직이면
    멈춘다. 19벌에서 갈려도 되는 것은 1-2 ~ 1-5 목록의 표기 글자뿐이다 — 목록 밖이 갈리면 멈춘다.
  · 표기가 바뀌는 칸은 같은 계산값의 적는 꼴만 바뀐다. 절사 A 조정으로 갈리는 줄은 갈린 줄과 크기를 값으로.
  · K 셋째 · L · M 뒤 항목 · 문구 판 몫 · 고압 계약형 부가금은 코드 0줄.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-6 이 센 자리 밖 0줄.**
  · **4절은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄 · tools\ 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면 「기록 없음」. 이미
    기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집 도구로 지은 .txt 로
    git commit -F.** 절마다 따로 커밋한다 — 다른 절 몫을 앞 절 커밋에 싣지 않는다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다 — 3-1 꼴 · 3-8 꼴(② 꼴 포함) · 아래 디버그 꼴 · 5-7 꼴. 그 밖에 `-n` · `--dist` ·
    `--tb` · `-s` 를 붙이지 않는다.** 빨간 시험 하나의 까닭을 볼 때만 아래 디버그 꼴을 쓴다 — 판 수에 넣되 초록 판정에 쓰지 않는다.
    node id 는 수집 목록에서 글자 그대로 복사해 옮긴다 — 손으로 다시 치지 않는다(S249 가 한글 node id 를 옮겨 못 찾았다).

         .venv\Scripts\python.exe -m pytest <빨간 시험 하나의 node id> -rf --tb=short

  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 · 같은 옵션으로 나눠
    뜬다. ③ · ④ 가 끊기면 그 갈래도 3-8 의 ② 꼴로 한 파일씩 돈다. ② 꼴 한 파일 판도 끊기면 멈춘다. 끊긴 판은 판 수에 넣고 까닭
    한 줄. 멈추면 3절까지 커밋하고(push 하지 않는다) 끊긴 자리를 보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File · sed 로 파일 고치기 · 셸 리다이렉트(> · >>)를 쓰지
    않는다 — 파일 수정은 편집 도구로만 한다. **파이프를 쓰지 않는다 — `| Out-Null` · `| ForEach-Object` · `| Select-Object` 도
    파이프다(S249 가 0절에서 둘을 썼다). 명령 출력이 길면 그대로 받아 읽고 · 파일은 찾기 도구나 읽기 도구로 본다.** 조사 에이전트를
    띄우면 이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S249 보고는 fe88e31) · 워크트리.
0-2  미해결 건수(118) · 회귀값 여덟 · 덱 스냅 기준선(S249 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다) · 케이스 스터디 기준선(S249 뒤
     값).

커밋: S250 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — 절사 A · 단위 기준(사람 결정 원문) · S233 · 249세션 절 4-2 의 K · M · N 판정과 그 기준 글자 · 결정 주인. 결정 1 ~ 3 ·
     5 와 어긋나는 글자가 있는지.
1-2  **후보 N(결정 1)** — 표기 문을 거치지 않는 Excel 시트 전수 · 시트마다 칸과 지금 표기 · 같은 사실을 다른 산출물이 적는 표기 · 한
     표 안 단위 섞임 · 고친 뒤 표기(예정) · 서는 벌.
1-3  **K 앞 둘 · M 앞 하나(결정 2)** — 항목마다 몸 · 지금 글자 · 기준 글자 · 서는 벌과 자리 · 고친 뒤 글자(예정).
1-4  **ESS 사양 표 만원 칸(결정 3)** — 칸의 계산값 · 지금 표기 · 같은 표의 다른 칸 단위와 셈 · 절사 A · 단위 기준으로 잣대가 서는가 ·
     서면 고친 뒤 표기(예정) · 안 서면 4절로.
1-5  **일괄 생성 옛 입력 이름(결정 5)** — 입력을 읽는 자리 · 옛 열쇠 글자 · 둘 다 주어졌을 때 · 확인 사례(옛 열쇠만 · 새 열쇠만 · 둘 다).
1-6  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S250 1절: 고칠 자리


2절 — 고친다

2-1  **1-6 이 센 자리를 고친다.** 결정 1 ~ 3 · 5 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S250 2절: Excel 표기 · 계약전력 판정 · 조합 비교 칸 · 일괄 입력 이름


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 기록을 무는 시험 파일은
     좁은 판에 넣지 않는다 — 5-7 이 본다. 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과
     한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이
     빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — (가) 결정 1 ~ 3 이 서는 벌(large-b-over 포함)을 가장 적게 골라(고른 벌과 까닭) 뜬다 · 산출물 넷(화면 · PPT ·
     Excel · Word)을 통째로 S249 뒤 스냅과 맞대 갈린 줄이 전부 1-2 ~ 1-4 목록 안인지 · 계산값이 같은지(표기만 갈렸는지). (나) 1-5 의
     확인 사례로 일괄 생성을 돈다 — 세 경우 다 읽고 같은 값인지. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「Excel 의 모든 시트는 표기 값을 한 자리에서 받는다 · 한 표 안 단위가 같다」 (2) K 앞 둘 ·
     M 앞 하나를 각각 기준 글자로 (3) ESS 사양 표를 고쳤으면 그 칸 (4) 「일괄 생성은 옛 · 새 DR 가격 열쇠를 같은 뜻으로 읽는다」 를 문다.
     산출물을 실제로 만든 글자와 값을 본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을
     본다. 재료 확인 글자가 고친 뒤 글자를 담아 되돌림이 얕아지지 않게 한다.
3-4  **덱 19벌 한 판** — S249 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 ~ 1-4 목록 안인지 · 계산값이 같은지. 계산값이 갈리면 멈춘다. 이
     판의 19벌 실물은 4절이 쓴다 — 지우지 않는다.
3-5  케이스 스터디 한 판 — 0-2 기준선과 맞댄다. 갈린 칸은 이름으로(소요 칸은 두고 · 그 밖의 칸 · 회귀값 여덟 · 타당성 판정이 갈리면
     멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-6 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 대로 하나씩 차례로 — ② 는 처음부터 그 갈래의 시험 파일을 한
     파일씩 아래 꼴로(작업자 2).

         .venv\Scripts\python.exe -m pytest <② 의 시험 파일 하나> -n 2 --dist load -rf --tb=no

     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S250 3절: 못 · 실물 · 회귀


4절 — 재기 (고치지 않는다 · 고르지 않는다 · 3-4 가 뜬 19벌 실물로 잰다)

4-1  **사람 결정 재료 — L PPT 주의사항 걸름 두 항목 · M 뒤 항목(· 결정 3 이 안 섰으면 ESS 사양 표 만원 칸)** — 항목마다 지금 모습(예시
     글자 · 서는 벌과 산출물) · 걸린 결정 글자 그대로와 주인 · 「사람이 정한다」 가 선 세션과 사람이 답한 기록 · 길마다: 바뀌는
     글자(예시) · 금액이 갈리나 · 고객 보고서에서 무엇이 달라지나 · 근거가 저장소에 있나 / 현장에만 있나 · 되돌리기 · 드는 판 수.
4-2  **홀로 남은 25 항목 가르기** — 항목마다 이름 · 결함 유형 · 19벌에 서는지와 벌 수 · 기록된 결정으로 잣대가 서는가(선다면 그 결정 ·
     주인) / 사람 결정이 필요한가 / 문구 판 · 자료 대기 · 도구 안쪽인가 — 한 줄씩. 기록으로 서는 것끼리 같은 자리를 공유하면 묶어
     적는다.
4-3  세 재료는 **사람이 읽는 말로 · 코드 용어 없이** 적는다. 코드 자리는 꼬리표로만. 고르지 않는다.
4-4  4절 값은 5-1 의 250세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 250세션 절 — 웹 대화창 판단(결정 1 ~ 5)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 4절 값. 세션 목록표에 250 행
     (브리핑이 읽는 날짜 꼴 · 「늘어난 것」 낱말 포함).
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(N · K 앞 둘 · M 앞 하나 · ESS 사양 표 만원 칸 가운데 닫힌 것). 미해결 건수 대조를
     이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리 진행표 K ~ N 행 · 5절에
     웹 대화창 판단 행(결정 1 ~ 5) · 머리 사슬에 S250 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(L · M 뒤 항목 사람 결정 대기 · 4-2 의 기록으로 서는
     묶음 · 문구 판(마-15 · 잘리는 지표 · 마-29 · 후보 H · K 셋째) · 고압 계약형 부가금(단가 자료 대기) · 이 판이 고치지 않고 값으로 낸
     자리). 상한 3,000자.
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S250.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S250 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-6 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 계산값 불변 확인 · 케이스 스터디 · 회귀값 여덟 · 총액 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수(② 는 파일 판 수) · 디버그 꼴 판 수를
    따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ N 포함).
  그 뒤에 「고침 표」 — N · K 앞 둘 · M 앞 하나 · ESS 사양 표 · 일괄 입력 이름마다 전 → 후 · 서는 벌 수 · 고치지 않은 자리와 까닭.
  그 뒤에 「사람이 정할 것 — 재료」(4-1) — 항목마다 지금 모습 · 걸린 결정 · 길 표. 사람이 읽는 말로 · 코드 자리는 꼬리표로만 · 고르지
    않는다.
  그 뒤에 「홀로 남은 항목 가르기」(4-2) — 기록으로 서는 것(묶음) · 사람 결정 · 문구 판 · 자료 대기 · 도구 안쪽의 수와 이름.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
