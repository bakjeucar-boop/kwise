S229 — 도구 판 3 · 전원을 보고, 갈래 소요를 가르고, 미해결 이름을 뜬다

**이 판은 도구 판이다. src\ 0줄 · 산출물 0벌 · 금액 0.** 고치는 것은
tools\ · tests\ · 도구 규약 · pytest 갈래 명령 글뿐이다.

**사람이 정했다 — S225 뒤 웹 대화창에서.** 다음 판들은 도구 판이다.
**웹 대화창이 정했다 — 셋째 묶음.** 사람 결정이 아니다.
  ㄱ `tools\open_values.py` 가 전원 상태를 낸다 — 전원 연결 여부 · 배터리
    잔량 · 절전 여부 가운데 OS 가 주는 값만. **깨끗한 판 판정 규칙은 안
    바꾼다** — 1-1 값을 보고 웹 대화창이 정한다(미해결 「판 개시 값 도구가
    전원 상태를 안 내 깨끗한 판을 가를 수 없다」 의 도구 몫).
  ㄴ `PROCEED.md` 「pytest 분할 실행」 네 명령에 `--durations=0` 을 붙인다 —
    1번 PC 통째 명령과 같게. **이 판이 붙이라고 한 플래그는 이것 하나다.**
    상한 글은 안 고친다 — 1-2 가 출처를 잰다(미해결 「2번 PC 갈래 넷의 소요
    상한이 규약에 없다」 의 durations 몫). **1번 PC 판이면 ㄴ 은 고치지 않고
    남긴다 — 그 PC 에서 갈래를 돌려 볼 수 없다.**
  ㄷ `tools\cache_files.py` 가 캐시 밖 저장소 폴더(`docs\directives\` 등)의
    목록도 뜬다(S228' 셸 자리).
  ㄹ `tools\daily_brief.py` 에 미해결 갈래별 이름을 뜨는 인자(S228' 셸 자리).
  **빼는 것** — 브리핑 「내가 밟아야 할 것」 에 「마」 싣기(1-4 값을 보고 정한다) ·
  덱 소요 · 캡처 · 스크래치 저장소화 · 못 세기 · 칸 줄 번호.

**브리핑 · HANDOVER 가 적은 것 — 전부 재게 할 것이다.** 원인을 미리 적지 않았다.
  · S227 은 배터리 33% → 15% 판이라 1.6 ~ 1.8배 느렸다 · 전원이 19:32 ~ 23:58
    빠져 있었다(HANDOVER 2절) — 그렇다면 S225(19:51 ~ 21:21)도 그 안이다.
    S225 소요는 S228(AC)과 비슷했다.
  · HANDOVER 2절 표 「상한 8분까지」 열 · 1절 「넷 다 상한 8분 안」 · 2절 S227
    단락 「앞단 상한 600초」 · 미해결 「2번 PC 갈래 넷의 소요 상한이 규약에 없다」.
  · 미해결 「도구가 없어 셸이 새는 자리가 셋이다」 — 셋이 무엇인가.
  · 미해결 갈래 「마」 41건이 브리핑 「내가 밟아야 할 것」 에 안 뜬다.

**울타리에 하한이 있다** — 1-6 이 센 「고칠 자리 수」. 적게 닫으면 자리마다
까닭을 값으로.

이 판의 못 박힌 안전한 쪽.
  · **src\ 0줄.** 하나라도 갈리면 멈춘다.
  · **도구가 내던 출력은 한 글자도 안 바뀐다** — ㄱ 은 전원 줄이 늘 뿐 있던
    줄은 그대로 · ㄷ 은 캐시 목록 출력 그대로 · ㄹ 은 인자 없는 브리핑 출력
    그대로. 1-5 의 전 출력과 맞대 갈리면 멈춘다.
  · **깨끗한 판 규칙 · `CLAUDE.md` 6항 · 상한 글은 안 고친다.**
  · **새 값을 짓지 않는다** — 전원 값은 OS 가 주는 것, 미해결 이름과 갈래는
    `docs\OPEN_ITEMS.md` · 브리핑이 이미 쓰는 경계 그대로.
  · 세션 도중 사람에게 묻지 않는다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. 되돌림이 실제로
    섰는지 값으로 먼저 본다.
  · **지시서에 없는 pytest 플래그를 어느 판에도 붙이지 않는다**(못 확인 ·
    되돌림 · 기록 묶음 미리보기 포함). 전체가 빨갛게 끝나면 고친 뒤 전체를 한
    판 더 — 2번 PC 면 네 갈래 전부.
  · **2번 PC 갈래는 하나씩 차례로 · 겹치게 돌리지 않는다 · 갈래마다 시작 · 끝.**
  · **git stash 를 쓰지 않는다.** 1번 PC 면 S140 스태시를 안 지운다.
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    ㄱ 이 서기 전 전원 상태는 스크래치 `.py` 로 뜬다 — 셸로 뜨지 않는다.
    개시 값은 `open_values` · 파일 목록은 `cache_files` · 칸은 `daily_brief`
    칸 인자 · 긴 출력은 `run_tool --tail`. **도구로 되는 자리에 파이프를 쓰지
    않는다.** 썼으면 쓴 자리마다 「무엇을 보려고 · 왜 도구로 못 했나」.
  · **절마다 따로 커밋한다.** 커밋 메시지는 편집 도구로 지은 `.txt` 로
    `git commit -F`.
  · **덱 · 케이스 스터디 · 화면 감사를 안 돌린다.** **벽시계를 절마다 잰다.**


0절 — 개시

0-1  `tools\open_values.py` — PC · cpu · RAM · 일꾼 수 · 남의 python ·
     **전원(스크래치 `.py`)**. **2번 PC 면 3-3 은 갈래 넷을 하나씩 · 1번 PC 면
     통째(회귀 동안 사람이 site-energy 를 멈춘다).**
0-2  `git pull` · HEAD 와 origin(S228' 보고는 b557e7b) · 워크트리 ·
     `git stash list`.
0-3  미해결 — 건수(156) · 갈래별 수(가 · 나 · 다 · 라 · 마) · 세 방법 검산.
     **이 판이 몫을 닫을 항목을 이름으로.**
0-4  회귀 기준선 — 그 PC 의 깨끗하고 플래그 없는 마지막 판(9항) · 정적 검사
     다섯(ruff 는 .py 기준).

커밋: S229 0절: 개시


1절 — 잰다

**이 절은 재고 판정만 한다. 고치지 않는다.**

1-1  **전원 이력** — 시스템 로그로 S225 · S226 · S227 · S228 판 시간대의 전원
     연결 · 배터리 잔량 · 절전 전환(OS 가 남긴 것만). `PROCEED.md` 판 절 시각과
     맞대 **판마다 한 줄**: AC 였나 · 잔량 구간 · 절전이 켜졌나. **S225 와 S227
     을 가른 것이 무엇인지 값으로** — 판정이 안 서면 안 선다고 적는다.
     **1번 PC 판이면 이 항은 뜰 수 없다고 적는다.**
1-2  **상한 글의 출처** — 「상한 8분」 · 「앞단 상한 600초」 · `CLAUDE.md` 6항
     「예상 소요」 를 글자 그대로 · 어느 파일 어디 · 처음 선 판. 셋이 각각
     갈래마다인가 · 통째인가 · 도구의 기한인가. HANDOVER 2절 표 · 1절 글과 미해결
     「2번 PC 갈래 넷의 소요 상한이 규약에 없다」 가 부딪치는지 한 줄로.
1-3  **미해결 「도구가 없어 셸이 새는 자리가 셋이다」** — 셋의 이름 · S227 · S228
     이 닫은 자리와 겹치는지 · 이 판 ㄷ · ㄹ 과 겹치는지.
1-4  **미해결 갈래 「마」** — 갈래 제목 글자 그대로 · 항목 수 · 항목마다 이름과
     무엇을 기다리는지 한 줄(항목 몸에서 · 지어 쓰지 않는다) · 브리핑 어디에
     보이는지.
1-5  **ㄱ ~ ㄹ 재현과 전 출력** — 자리마다 명령 · 지금 출력 · 기대. 고칠 도구마다
     틀리지 않던 입력 둘 이상의 전 출력을 떠 둔다.
1-6  **고칠 자리 수를 값으로 못 박는다** — 코드 자리 · 규약 자리(`CLAUDE.md`
     9항) · 명령 글 자리(`PROCEED.md`). 그것이 울타리이고 하한이다.

커밋: S229 1절: 재기


2절 — 고친다

2-1  **1-6 이 센 자리만 고친다.** 자리마다 고친 줄 수와 전 · 후. 울타리를
     넘는 자리가 보이면 멈추고 값을 낸다.
2-2  자리마다 1-5 의 재현을 다시 돈다 · 전 출력과 맞댄다.
2-3  **ㄹ 새 인자로 1-4 의 「마」 이름을 다시 떠 1-4 와 같은지 본다.**
2-4  적게 닫았으면 자리마다 까닭을 값으로.
2-5  **되돌려 확인한다** — 되돌리면 1-5 의 증상이 다시 서는지. 되돌림이 실제로
     섰는지 값으로 먼저 본다.

커밋: S229 2절: 도구 고침


3절 — 못 · 회귀

3-1  **자리마다 증상을 무는 못** — 도구를 실제로 불러 낸 값을 본다(소스
     리터럴을 찾는 못은 안 박는다). ㄱ 은 전원 줄 꼴과 있던 줄이 그대로인 것 ·
     ㄷ 은 캐시 밖 폴더 목록 · ㄹ 은 갈래별 이름 수가 갈래 수와 같은 것 · ㄴ 은
     명령 글에 `--durations=0` 이 있는 것. **이미 있는 못을 넓혀 닫히면 새 못을
     박지 않는다.** 박은 못마다 되돌려 확인 · 소요. 앞 판 딱지를 걷고 **걷은
     딱지 몫과 새 못 몫을 갈라 적는다.** xfail 이 새로 빨개지면 멈춘다. 되돌림
     원복을 `git status` · `git diff` 로.
3-2  **`git diff --numstat` 으로 src\ 0줄을 값으로 보인다** — 폴더마다.
3-3  pytest — 0-1 이 정한 대로. **2번 PC 면 이 판이 고친 `PROCEED.md` 「pytest
     분할 실행」 네 명령 그대로(`--durations=0` 포함) · 하나씩 차례로 · 갈래마다
     시작 · 끝. 1번 PC 면 아래 명령 그대로. 그 밖의 플래그를 더하지 않는다.**

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no --durations=0

     **판 앞 · (2번 PC 면 갈래 사이) · 뒤에 `open_values` 로 남의 python 과 전원을
     센다.** 0 이 아니거나 배터리면 기다리거나 죽이지 말고 적기만 한다.
     `pytest_counts.py` 로 센다 — `--base` 에 0-4 의 수집 건수. skip 수를 함께.
3-4  소요 — PC · 일꾼 수 · 남의 python · 전원 값 전부 · 갈래마다 위 열 개 시험
     (`--durations`) · 그 PC 의 깨끗한 판과 맞댄다.
3-5  정적 검사 다섯 — 0-4 와 맞댄다.

커밋: S229 3절: 못 · 회귀


4절 — 기록 · 닫기

`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.

4-1  `PROCEED.md` 에 229세션 절 — 셋째 묶음이 웹 대화창 판단이라는 것 · 1-1 ~
     1-4 값.
4-2  **미해결 건수 대조를 이름으로.** 이름에 「 · 」 를 넣지 않는다. 이 판이
     닫은 몫만 지우고, 규칙 몫(전원 판정 · 갈래 상한)이 남으면 **이름을 남은 몫에
     맞게 고친다.** 1-3 판정에 맞춰 「셸이 새는 자리」 항목의 수와 몸을 고친다.
4-3  **HANDOVER 2절에 곁 줄 둘** — S225 전원 사실(1-1 값대로) · S227 단락 「앞단
     상한 600초 — 처음부터 뒤로 보내고 기한을 건다」 가 배터리 판 근거라는 것.
     옛 줄은 고치지 않는다. 1-2 로 상한 글이 부딪치면 그 사실을 한 줄로.
4-4  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
4-5  `docs\HANDOVER.md` 를 이 판 값으로 — 머리 사슬에 S229 · 1절 「앞 값」
     사슬은 두 판까지 · 6절 앞 판 딱지를 걷는다 · **2번 PC 판이면 2절을 이 판
     값으로.**
4-6  `CLAUDE.md` — 9항 도구 규약(ㄱ · ㄷ · ㄹ) · 9항 이 판을 돈 PC 칸(전원 값
     함께). 깨끗한 판 규칙 · 6항은 안 고친다.
4-7  「다음 작업」 칸 — 후보를 이름으로만(ㄷ 자릿수 뿌리 · ㄱ 절사 뿌리 · 남은
     도구 자리). **고르지 않는다.** 글자 수와 여유.
4-8  이 지시서 본문을 `docs\directives\S229.md` 로 그대로 앉힌다.
4-9  **기록을 다 고친 뒤에** 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-10 커밋하고 `git push`. **HEAD 와 origin 이 같은지 · 워크트리가 깨끗한지 ·
     `runs\도는중\` · 남의 python 을 보고 1항에 적는다.**

커밋: S229 4절: 기록 · 미해결 대조 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽(아래 「마」 목록은 예외).

  1 대조표 — 절·항마다 한 줄. 울타리(하한)와 고친 수를 나란히. 「멈춘다」 가
    걸린 자리를 멈춘 자리 · 안 멈춘 자리로 갈라서. 마지막 줄에 HEAD = origin ·
    워크트리 · 도는중 · 남의 python.
  2 만든/바꾼 파일 — +/− 줄 수. src\ 폴더마다 0줄을 값으로.
  3 수치가 움직인 곳 — pytest 수 · 도구 출력이 바뀐 입력. 금액 0.
  4 판단한 것 — 1-1 · 1-2 · 1-3 · 1-6 · 2-4 · 3-1.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패 · 전체 pytest 마지막 판이
    초록인지.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 가운데 고친 것과 9항을 고쳤는지.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로 · 소요 · xfail 전후 ·
    확인 안 한 못 전후와 딱지 몫 · 새 못 몫.

  **그 뒤에 「전원 이력 표」(1-1) — 판마다 한 줄.**
  **그 뒤에 「상한 글 표」(1-2) — 글자 · 자리 · 처음 선 판 · 무엇에 닿나.**
  **그 뒤에 「마 목록」(1-4) — 항목마다 이름 · 무엇을 기다리나 한 줄씩.**
  **그 뒤에 「소요 표」 — 갈래마다(또는 통째) 이 판 · 그 PC 깨끗한 판 · 위 시험 셋.**
  **그 뒤에 「재현 표」 — 자리마다 명령 · 전 · 후.**
  **그 뒤에 「사람이 정할 것」 — 멈춘 자리가 있으면 그 자리. 없으면 없다고.**
  **그 뒤에 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다). ctx 셋(종전 → 지금 · 가장 크게
먹은 것 · 줄일 수 있었던 것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
