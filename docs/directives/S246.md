S246 — 고침 판 · PPT · Word 기간 합산효과에 DR 정산금(계산 변경) · F 의 남은 근거 줄 · 후보 D 를 고치고 · 후보 C · G · I 재료와 E 고칠 자리를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 과 후보 B 는 닫혔다. 후보 A 는 S245 가 마-16 을
닫아 기간 합산효과의 DR(사람이 정함)과 나-18(문구 판)이 남았다. 후보 F 는 근거 줄 하나(245세션 절 · 지금 번호 나-24)가
남았다. 후보 J(마-33)는 사람이 「그대로 둔다」 로 정했다. 이 판은 **기간 합산효과의 DR 정산금을 고치고(계산 변경은 이
하나) · F 의 근거 줄과 후보 D 를 고치고 · 마-33 을 기록으로 닫고 · 후보 C · G · I 의 사람 결정 재료와 후보 E 의 고칠
자리를 재기만 한다.** 이 밖으로 가지 않는다. 항목 이름은 242 ~ 245세션 절에서 찾는다.

**사람이 정했다(이 판이 HANDOVER 5절에 행으로 앉힌다).**
  기간 합산효과의 DR (S245 뒤) — PPT · Word 의 기간 합산효과는 관측 기간 감축 가능량으로 셈한 기간 DR 정산금을 담는다.
    화면 합산효과(12개월 · 정산금 포함)와 같은 정의이고(S182) · 기간 / 12개월의 차이는 S220 대로다. S219 「근거 없이
    채우지 않는다」 행에는 곁 줄을 단다 — 옛 글자는 고치지 않는다. DR 단가를 넣은 판에서 권장안이 바뀔 수 있다.
  마-33 (S245 뒤) — 역률 개선을 함께 켠 조합의 역률 경고는 그대로 둔다(97% 를 맞추려면 설비가 태양광 뒤 낮아진 역률까지
    받아야 해 그 조합에서도 참인 주의다). 코드 0줄 · 항목은 이 결정으로 닫는다.
  앞서 정한 것(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만) — 절사 A · 단위 기준 ·
    측정 중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지 않는다 ·
    S154 · S165 · S182 귀속 기준 ㄱ · S198 · S202 · S205 · S206 · S207 · S220 · 운영시간 외 부하는 비중(S238) · 고객 산출물
    말투는 「~습니다」(S242) · 마-16 조합 부하로 DR · 마-35 「없음」 카드 40자 예외.

**웹 대화창이 정했다(S245 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (기간 DR 정산금 적용) — 기간 정산금은 이미 코드에 있는 관측 기간 감축 가능량과 12개월 정산금에 쓰는 단가로만
    셈한다 — 새 가정(발령 횟수 · 달 몫 등)을 더하지 않는다. 조합이면 조합 부하로 잰 감축량이다(마-16). 기간 값을 쓰는
    산출물은 기간 정산금 · 12개월 값을 쓰는 산출물은 12개월 정산금을 담는다(Excel 도 그 칸의 기준대로). 같은 자리에서
    그 몫을 말하는 글은 이미 있는 이름(「DR 정산금」 등)으로 셈과 맞게 적는다.
  결정 2 (F 근거 줄) — S245 결정 3 을 계산 폴더의 근거 줄까지 적용한다 — 여지가 없는 벌에서 거짓이 되는 앞머리를
    세우지 않는다(S154 · S206 · S207).
  결정 3 (후보 D) — 검토함에 켠 ESS 가 결과를 못 내면 그 까닭대로 요약표와 Excel 수단별 결과에 줄을 세운다. 여지가
    없으면(초과 구간이 없어 곡선이 안 서면) 「없음」(S205 · S237 결정 ㄴ 꼴) · 산출하지 못했으면 같은 화면이 이미 쓰는
    「미산출」 글자와 사유. 새 글자를 짓지 않는다 — 이미 있는 글자로 안 되는 자리는 고치지 않고 값을 낸다.
  결정 4 (운영) — 2번 PC 의 전체 pytest ② 갈래는 처음부터 그 갈래의 시험 파일을 같은 플래그로 한 파일씩 차례로 돈다
    (S238 · S244 · S245 가 첫 판에서 메모리로 끊겼다). 빨간 시험 하나의 까닭을 볼 때만 아래 디버그 꼴을 쓴다 — 그 판은
    판 수에 넣되 초록 판정에 쓰지 않는다.

         .venv\Scripts\python.exe -m pytest <빨간 시험 하나의 node id> -rf --tb=short

**S245 보고가 적었다 — 실물에서 먼저 확인하라.** PPT · Excel · Word 합산효과와 권장안에는 DR 이 안 들어간다 · 화면
합산효과만 DR 정산금을 담는다 / 정산금은 12개월 값뿐(S219) · 관측 기간 감축 가능량은 코드에 있다 / 꼬리표 slides.py:2190
· document.py:1555 / 확인 사례 large-b(DR 120원/kWh · 태양광 1,500,000원/kWp · 역률 투자비 300만원) — 화면 합산효과
3억 3,646만원/년 · 권장안 「선택Ⅱ 전환 + 태양광 1,600 kWp + 역률 개선 97%」 · 2단계 DR 카드 34,350 kWh · 412만원 / F 의
남은 근거 줄은 계산 폴더 안 / 후보 D 는 두 항목 · 19벌 0벌(S242 5-2) / 후보 E 는 두 항목 · 덱 밖 · S198.

**울타리에 하한이 있다** — 1-5 가 센 「고칠 자리 수」. 결정 1 ~ 3 을 끝까지 적용하는 데 필요한 번짐은 울타리 안이다 —
자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **기본 19벌은 금액 · 판정 칸이 한 자리도 안 움직인다**(DR 단가가 없다). 움직이면 멈춘다. 19벌에서 갈려도 되는 것은
    1-3 · 1-4 목록의 글자뿐이다.
  · **값이 움직여도 되는 칸은 DR 단가를 넣은 판의 기간 합산효과 · 그 회수기간 · 권장안(과 권장안을 따라 바뀌는 칸)뿐이다**
    — 1-2 목록 안. 그 밖의 금액 칸이 갈리면 멈춘다. 움직인 칸은 전 → 후를 값으로.
  · **화면 합산효과(12개월)와 2단계 DR 카드 값은 한 자리도 안 움직인다.** 움직이면 멈춘다.
  · **회귀값 여덟 가운데 하나라도 움직이면 기대값을 고치지 않고 멈춘다** — 이름 · 전 → 후를 값으로. 케이스 스터디 타당성
    판정 칸이 갈려도 멈춘다.
  · 기간 정산금에 새 가정이 들어야 하면 고치지 않고 멈춰 값을 낸다.
  · 마-33 · 나-18 · 후보 C · E · G · I 는 코드 0줄.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-5 가 센 자리 밖 0줄.**
  · **4절은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄 · tools\ 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면 「기록
    없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집 도구로 지은 .txt 로
    git commit -F.** 절마다 따로 커밋한다 — 다른 절 몫을 앞 절 커밋에 싣지 않는다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다 — 3-1 꼴 · 3-8 꼴 · 결정 4 의 디버그 꼴 · 5-7 꼴. 그 밖에 `-n` · `--dist` ·
    `--tb` · `-s` 를 붙이지 않는다.**
  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 · 같은
    옵션으로 나눠 뜬다. 전체 pytest 갈래가 끊기면 곧바로 다시 돌지 않고 그 갈래의 시험 파일을 같은 플래그로 한 파일씩
    돈다. 한 파일 판도 끊기면 멈춘다. 끊긴 판은 판 수에 넣고 까닭 한 줄. 멈추면 3절까지 커밋하고(push 하지 않는다)
    끊긴 자리를 보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를 쓰지 않는다 —
    `| Out-Null` · `| ForEach-Object` 도 파이프다.** 여러 줄을 훑을 때는 찾기 도구나 읽기 도구로 본다. 조사 에이전트를
    띄우면 이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S245 보고는 0a77ad0) · 워크트리.
0-2  미해결 건수(131) · 회귀값 여덟 · 덱 스냅 기준선(S245 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S246 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — S182 · S219 · S220 · S245 결정 3 · S205 · S237 결정 ㄴ · S242 5-2 의 후보 D · 마-33 · 기간 합산효과 DR
     항목을 글자 그대로 · 결정 주인. 이 판의 결정과 어긋나는 글자가 있는지.
1-2  **기간 DR 정산금(사람 결정 · 결정 1)** — 합산효과를 만드는 자리 · 적는 자리 전수(화면 · PPT · Excel · Word) · 산출물
     마다 기간 / 12개월 기준 · 관측 기간 감축 가능량이 있는 자리 · 12개월 정산금의 단가 자리 · 권장안을 고르는 자리와 그
     셈에 합산효과가 들어가는지 · 캐시 열쇠에 닿는 자리. 확인 사례는 S245 의 large-b 입력 그대로 — 산출물마다 합산효과 ·
     회수기간 · 권장안 전 → 후(예정).
1-3  **F 근거 줄(결정 2)** — 근거 줄의 글자 그대로 · 여지가 없는 벌에서 거짓이 되는 앞머리 · 서는 벌 · 고친 뒤 글자(예정).
1-4  **후보 D(결정 3)** — 두 항목의 몸 · ESS 가 결과를 못 내는 까닭별 갈래(여지 없음 / 산출 못 함) · 요약표 · Excel 수단별
     결과 · 화면 · PPT 가운데 줄이 안 서는 자리 · 같은 화면이 이미 쓰는 「없음」 · 「미산출」 글자와 사유 · 확인 사례(초과
     구간이 없는 입력 · 산출 못 하는 입력 — 넣은 값과 까닭) · 고친 뒤 글자(예정).
1-5  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S246 1절: 고칠 자리


2절 — 고친다

2-1  **1-5 가 센 자리를 고친다.** 사람 결정과 결정 1 ~ 3 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S246 2절: 기간 DR 정산금 · 근거 줄 · ESS 없음 줄


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 화면 시험
     파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과 한 프로세스에서 돌면 기온 기준선
     못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은
     판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — (가) F 근거 줄이 서는 벌을 가장 적게 골라 기본 입력으로 뜬다 · 산출물 넷(화면 · PPT · Excel ·
     Word)을 통째로 S245 뒤 스냅과 맞대 갈린 줄이 전부 1-3 목록 안인지 · 금액 칸 0 인지. (나) 1-2 의 확인 사례를 옛 코드 ·
     새 코드로 뜬다(옛 코드 덱은 238세션 절의 길) — 산출물마다 합산효과 · 회수기간 · 권장안 전 → 후 · 화면 합산효과와
     2단계 DR 카드가 안 움직였는지 · 움직인 칸이 1-2 목록 안인지. (다) 1-4 의 확인 사례를 옛 코드 · 새 코드로 뜬다 — 갈린
     줄이 1-4 목록 안인지. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「기간 합산효과는 관측 기간 감축량으로 셈한 기간 DR 정산금을 담고 · 12개월
     합산효과는 12개월 정산금을 담는다」 (2) 「여지가 없는 벌의 근거 줄에 거짓 앞머리가 없다」 (3) 「켠 ESS 가 결과를 못 내면
     그 까닭대로 「없음」 · 「미산출」 줄이 선다」 를 문다. 산출물을 실제로 만든 글자와 값을 본다. 되돌려 확인한다(스크래치
     monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다. 재료 확인 글자가 고친 뒤 글자를 담아
     되돌림이 얕아지지 않게 한다.
3-4  **덱 19벌 한 판** — S245 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-3 · 1-4 목록 안인지 · 금액 · 판정 칸 0 인지. 금액 칸이
     갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로 — DR 단가가 든 케이스의 합산효과 ·
     회수기간 · 권장안 칸이 갈리면 전 → 후를 값으로 내고 · 회귀값 여덟이나 타당성 판정 칸이 갈리면 멈춘다(소요 칸은 두고 ·
     1-5 자리 밖이 갈려도 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-5 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로 — ② 는 결정 4 대로
     처음부터 그 갈래의 시험 파일을 같은 플래그로 한 파일씩. 1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를
     멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S246 3절: 못 · 실물 · 회귀


4절 — 재기 (고치지 않는다 · 고르지 않는다)

4-1  **후보 C · G · I 재료** — 묶음마다 항목의 몸 · 지금 실물(3-4 의 19벌)에서 서는지 · 벌 수 · 예시 글자 · 이력 · 걸린
     결정(글자 그대로 · 결정 주인 · 「사람이 정한다」 가 선 세션과 사람이 답한 기록) · 기록으로 잣대가 서는 항목 / 안 서는
     항목. 안 서는 항목만 길마다 한 줄씩: 무엇을 건드리나 · 금액이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 근거가 저장소에
     있나 / 현장에만 있나 · 되돌리기 · 드는 판 수. **사람이 읽는 말로 · 코드 용어 없이.**
4-2  **후보 E 고칠 자리** — 두 항목의 몸 · S198 글자 그대로 · 고칠 자리 전수(계산 폴더 몫 따로) · 움직일 값의 자리(케이스
     스터디 · 일괄 생성 · 회귀값 여덟에 닿는지) · 확인 사례로 쓸 입력.
4-3  4절 값은 5-1 의 246세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 246세션 절 — 사람 결정(기간 합산효과의 DR · 마-33 · 앞서 정한 것은 가리키기만)과 웹 대화창 판단(결정
     1 ~ 4)을 갈라서 · 1 ~ 4절 값. 세션 목록표에 246 행(브리핑이 읽는 날짜 꼴 · 「늘어난 것」 낱말 포함).
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(기간 합산효과의 DR · F 근거 줄 · 후보 D · 마-33 가운데 닫힌 것).
     미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리 진행표
     A · D · E · F · J 와 C · G · I 행 · 5절에 사람 결정 행(기간 합산효과의 DR · 마-33 · S219 행 곁 줄)과 웹 대화창 판단
     행(결정 1 ~ 4) · 머리 사슬에 S246 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(후보 E 고침(계산 변경) · C · G · I 사람
     결정 여부 · 문구 판(나-18 · 마-34 · 후보 H) · 이 판이 고치지 않고 값으로 낸 자리). PROCEED.md 「pytest 분할 실행」
     절에는 결정 4 를 한 줄로 단다.
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S246.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S246 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-5 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 확인 사례에서 값이 움직인 칸 전 → 후 · 권장안 · 회귀값 여덟 · 총액 ·
    케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수(② 는 파일 판 수) ·
    디버그 꼴 판 수를 따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ J 포함).
  그 뒤에 「기간 DR 정산금 표」 — 확인 사례 · 산출물마다 합산효과 · 회수기간 · 권장안 전 → 후 · 화면 합산효과와 2단계 DR
    카드 불변 확인.
  그 뒤에 「글자 고침 표」 — F 근거 줄 · 후보 D 의 전 → 후 · 서는 벌 수 · 고치지 않은 자리와 까닭.
  그 뒤에 「후보 C · G · I」(4-1) · 「후보 E 고칠 자리」(4-2) — 사람이 읽는 말로 · 코드 자리는 꼬리표로만 · 고르지 않는다.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
