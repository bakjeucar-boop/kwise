S243 — 고침 판 · 뿌리 후보 「B 계산 근거 표의 남는 몫」 을 고치고 · 「ㅂ」 꼬리 둘을 닫고 · 뿌리 후보 「A 단순 합과 합산효과」 를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 은 닫혔다. S242 5절이 미해결 가운데
계산값 · 고객 글자에 닿는 60항목을 열 묶음(A ~ J)으로 묶었다. 이 판은 **묶음 B 를 고치고 · ㅂ 의 꼬리 둘(Excel
부록 줄 번호 열 · 부록 B 객체 글자)을 닫고 · 묶음 A 를 재기만 한다**(고치지 않는다). 이 밖으로 가지 않는다 —
다른 묶음 · 도구 · 측정은 이 판 몫이 아니다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).** 절사 A
(같은 표 안에서 적힌 수끼리의 셈이 맞게 · 계산값 불변) · 단위 기준 · 측정 중단 · 도구 판은 S230 으로 끝 ·
CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지 않는다 · S182 귀속 기준 ㄱ · S207 ·
운영시간 외 부하는 비중(S238) · 고객 산출물 말투는 「~습니다」(S242).

**웹 대화창이 정했다(S242 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (묶음 B) — 계산 근거 표는 그 표 안에서 적힌 수끼리 셈이 맞는다(절사 A · S159 태양광 표 선례). 합계에
    들었는데 표에 안 적힌 몫(역률 · 초과사용부가금 등)은 줄로 세운다. 줄 이름은 산출물이 이미 쓰는 이름을
    쓰고 · 줄 이름이 그 줄의 사실과 다르면 사실에 맞는 이미 있는 이름으로 고친다. 0원 몫은 세우지 않는다
    (S159 · S237 결정 ㄴ). 계산값은 안 바뀐다. 그 표를 무는 못은 허용 폭 없이 적힌 수끼리의 셈이 맞는지를 문다.
  결정 2 (ㅂ 꼬리) — 고객 산출물에 개발 흔적이 서지 않는다(S240 4-4 · S241 · S242 를 끝까지). Excel 부록의 판다스
    줄 번호 열은 뺀다. 부록 B 값 칸의 객체 글자(「description=…」 꼴)는 그 값의 사람 글자로 적는다 — 이미 있는
    이름 · 값을 쓴다.
  결정 3 — S242 결정 2 의 「표 칸」 은 서술어로 끝나지 않는 칸이다(S242 가 읽은 대로 · 되돌리지 않는다).

**S242 보고가 적었다 — 실물에서 먼저 확인하라.** 묶음 B 는 세 항목 · 결함 유형 ③ · 19벌 가운데 4 · 6벌에 선다 ·
사람 결정 불요 / 묶음 A 는 여섯 항목 · 유형 ③ · 19벌 다 선다 · 사람 결정 필요 / 부록 B 객체 글자는 19벌 0 ·
합성 재료에서만 선다 / Excel 부록 첫 열 판다스 줄 번호. 묶음마다 항목 이름은 242세션 절에서 찾는다.

**울타리에 하한이 있다** — 1-4 가 센 「고칠 자리 수」. 결정 1 · 2 를 끝까지 적용하는 데 필요한 번짐은 울타리
안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한 자리도 안
    움직인다.** 움직이면 멈춘다.
  · **계산 근거 표의 합계 줄 글자는 안 바뀐다.** 다른 줄 글자는 새 줄이 서면서 절사 A 조정으로만 갈린다 — 갈린
    줄과 크기를 값으로. 그 밖으로 갈리면 멈춘다.
  · 줄 이름을 새로 짓지 않는다. 사실에 맞는 이미 있는 이름이 없는 줄은 고치지 않고 값을 낸다.
  · **갈리는 글자는 1-2 · 1-3 목록 안뿐이다.** 목록 밖 글자가 갈리면 멈춘다.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-4 가 센 자리 밖 0줄.** 요금 ·
    절감액을 만드는 식은 0줄.
  · **4절(묶음 A)은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄 · tools\ 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면
    「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집 도구로 지은
    .txt 로 git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다. 좁은 판은 3-1 꼴 그대로 — `-n` · `--dist` · `--tb=short` 를 붙이지
    않는다(S242 가 좁은 판에 -n auto --dist load 를 붙였다). 지시서가 꼴을 적지 않은 판(못 하나 · 되돌려 확인)도
    3-1 꼴이다.**
  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 ·
    같은 옵션으로 나눠 뜬다(나눈 묶음과 까닭을 적는다). 도구가 벌을 골라 뜰 수 없거나 나눠 뜬 판도 끊기면 멈춘다.
    전체 pytest 갈래가 끊기면 다시 돌지 않고 멈춘다. 멈추면 3절까지 커밋하고(push 하지 않는다) 끊긴 자리를
    보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를 쓰지 않는다
    — `| Out-Null` 도 파이프다.** 파일을 볼 때는 읽기 도구로 본다. 조사 에이전트를 띄우면 이 금지를 글자 그대로
    전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S242 보고는 e1eed41) · 워크트리.
0-2  미해결 건수(141) · 회귀값 여덟 · 덱 스냅 기준선(S242 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S243 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — 절사 A(사람 결정) · S159 · S193 · S194 의 태양광 계산 근거 표 판정 · S237 결정 ㄴ · S240 4-4 · 242세션
     절 5-2 의 묶음 A · B 항목 이름을 글자 그대로 · 결정 주인. 결정 1 ~ 3 과 어긋나는 글자가 있는지.
1-2  **묶음 B(결정 1)** — 세 항목마다 몸 · 계산 근거 표 전수(수단마다 · 화면 · PPT · 부록 A · Excel 가운데 서는 곳) ·
     표마다 합계와 적힌 줄의 셈이 어긋나는 벌과 크기 · 빠진 몫과 그 크기 · 세울 줄의 이미 있는 이름 · 사실과 다른
     줄 이름 · 그 표를 무는 못과 허용 폭 · 고친 뒤 글자(예정).
1-3  **ㅂ 꼬리(결정 2)** — Excel 부록마다 줄 번호 열이 서는 시트 전수 · 부록 B 객체 글자가 서는 조건과 값 · 만드는
     코드 자리 · 고친 뒤 글자(예정).
1-4  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S243 1절: 고칠 자리


2절 — 고친다

2-1  **1-4 가 센 자리를 고친다.** 결정 1 · 2 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S243 2절: 계산 근거 표 · 개발 흔적


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 화면
     시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과 한 프로세스에서 돌면
     기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이
     빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 묶음 B 가 서는 벌을 가장 적게 골라(고른 벌과 까닭) 뜬다. **산출물 넷(PPT · Excel · Word ·
     일괄)을 통째로 S242 뒤 스냅과 맞대** 갈린 줄이 전부 1-2 · 1-3 목록 안인지 · 계산 근거 표마다 적힌 수끼리의 셈이
     맞는지 본다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「계산 근거 표는 적힌 수끼리 셈이 맞는다 · 허용 폭 없음」 (2) 「Excel
     부록에 줄 번호 열이 없다」 (3) 「부록 B 값 칸에 객체 글자가 없다」 — 합성 재료에서만 서면 그 재료로 문다. 산출물을
     실제로 만든 글자를 본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고
     빨간 것을 본다. 재료 확인 글자가 고친 뒤 글자를 담아 되돌림이 얕아지지 않게 한다(S242 가 한 번 그랬다).
3-4  **덱 19벌 한 판** — S242 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 · 1-3 목록 안인지 · 19벌 계산 근거 표가 다 셈이
     맞는지. 금액 칸 · 합계 줄이 갈리면 멈춘다. 이 판의 19벌 실물은 4절이 쓴다 — 지우지 않는다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-4 자리 밖이 갈리면
     멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-4 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로. 1번 PC 면 아래
     명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S243 3절: 못 · 실물 · 회귀


4절 — 뿌리 후보 「A 단순 합과 합산효과」 재기 (고치지 않는다 · 3-4 가 뜬 19벌 실물로 잰다)

4-1  **증상** — 묶음 A 여섯 항목마다 몸 · 지금 실물에서 서는지 · 벌 수 · 예시 글자(화면 · PPT · Excel · Word 가운데
     서는 곳).
4-2  **이력과 자리** — 항목마다 언제 어느 세션이 올렸고 무엇을 재거나 고쳤나(PROCEED.md · HANDOVER · git log). 「단순
     합」 과 「합산효과」 를 만드는 자리 · 적는 자리 전수(tools\count_sites.py · 그물 밖 자리는 따로 · 계산 폴더 몫
     따로) · 자리마다 무엇을 더하고 무엇을 빼나(청구 밖 몫 · 체크를 푼 수단 · 정산금 등) · 같은 이름이 다른 셈을
     가리키는 곳.
4-3  **걸린 결정** — 저장소 전체에서 이 묶음에 닿는 결정(collaboration.md 「2단계는 독립 평가 · 3단계는 수단마다
     처음부터 다시 계산 · 절감액을 더하지 않는다」 · S182 귀속 기준 ㄱ · S233 · HANDOVER 5절 등) 글자 그대로 · 결정
     주인. 「사람이 정한다」 가 선 세션과 사람이 답한 기록이 있는지.
4-4  **판정** — 항목마다 4-3 의 기록된 결정으로 잣대가 서는가 / 안 서는가. 안 서는 항목만 선택지를 길마다 한 줄씩:
     무엇을 건드리나 · 금액 · 계산값이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 되돌리기 · 드는 판 수. **사람이 읽는
     말로 · 고르지 않는다.**
4-5  4절 값은 5-1 의 243세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 243세션 절 — 웹 대화창 판단(결정 1 ~ 3)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 4절 값. 세션 목록표에
     243 행.
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(묶음 B 가운데 닫힌 것 · 개발 흔적 항목 · 닫힌 것이 더 있으면
     이름으로). 미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리
     진행표에 뿌리 후보 A ~ J 를 행으로 올리고 B 는 이 판 값 · A 는 재기 끝 · 5절에 웹 대화창 판단 행(결정 1 ~ 3) ·
     머리 사슬에 S243 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(묶음 A 의 사람 결정 대기 여부 ·
     묶음 C ~ J · 이 판이 고치지 않고 값으로 낸 자리 · 여지가 없는 수단이 조합의 마지막 줄인 경우).
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S243.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S243 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-4 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를 따로. 다시 돈
    판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ J 포함).
  그 뒤에 「계산 근거 표」 — 표마다 셈이 어긋났던 벌 수 · 세운 줄 이름 · 고친 줄 이름 · 절사 A 조정으로 갈린 줄 ·
    고치지 않은 표와 까닭.
  그 뒤에 「단순 합과 합산효과」(4절) — 항목마다 증상 · 이력 · 자리와 셈 · 걸린 결정 · 판정(서는 / 안 서는) · 안 서는
    항목의 선택지 표. 사람이 읽는 말로. 코드 자리는 꼬리표로만.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
