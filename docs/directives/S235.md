S235 — 고침 판 · 자릿수 뿌리를 닫는다 · 다음 뿌리 「정의가 다른 두 수」 를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표 — 「ㄱ 줄마다 절사」 는 S234 에 닫혔다. 「ㄷ 자릿수
손으로」 는 S234 가 재기를 끝내 결함 묶음 넷(①~④)을 못 박았다. 이 판은 **ㄷ 을 고쳐 닫고**,
끝에 다음 뿌리 **「ㄴ 정의가 다른 두 수」 를 재기만 한다**(사람 결정 대기라 물을 선택지를 값으로
만든다 · 고치지 않는다). 이 둘 밖으로 가지 않는다 — 다른 뿌리 · 역률 투자비 · 도구는 이 판
몫이 아니다.

**사람이 정했다(기록 자리는 HANDOVER 5절 · S234 가 적었다).** 절사는 A 단수 차이 조정. 모든
단위를 통일할 필요는 없다 · 같은 표 안에서는 단위가 달라선 안 된다 · 가독성 있게 적는다.
소요 · 깨끗한 판 · 전원 · 남의 python 은 재지도 적지도 않는다. CLAUDE.md 는 규칙이 바뀔 때만
고친다.

**웹 대화창이 정했다 — S234 뒤 · 사람 결정이 아니다.**
  ㄱ ㄷ 의 결함은 S234 3-3 의 넷이다. 넷 다 이 판에서 고친다.
      ① Excel 수 칸 — 셀의 수는 그대로 두고 **셀 서식으로** 쉼표를 단다. 같은 열의 소수 자리는
         하나로 — 그 열 사실을 화면 · Word 가 적는 자릿수를 따르고, 없으면 그 열에서 가장 많이
         쓰인 자릿수. 서식만 바꾸므로 셀 값 · 합 · 정렬은 그대로다.
      ② 그림 눈금에 쉼표.
      ③ 부록 B 값에 쉼표.
      ④ 화면 카드 — 만원으로 적는 카드 안의 증감도 그 카드의 단위(만원)로 적는다.
  ㄴ S234 3-4 물음 — 같은 표 · 카드 안에서 **다른 사실**이 같은 단위를 다른 자릿수로 적는 것
    (요금적용전력 5,293 kW 대 최대수요 5,293.4 kW)은 **결함이 아니다.** 사실마다 제 정의의
    자릿수다. 이 판에서 고치지 않는다(146 · 19벌은 결함 아님으로 닫는다).

**울타리에 하한이 있다** — 1-2 가 센 「고칠 자리 수」.

이 판의 못 박힌 안전한 쪽.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality) 0줄.**
  · **회귀값 여덟 · 케이스 스터디 타당성 · 요금 총액은 한 자리도 안 움직인다.** 움직이면 멈춘다.
  · **Excel 셀의 수(값)는 바뀌지 않는다** — 서식만. 셀 값이 갈리면 멈춘다.
  · **덱 19벌 전 · 후 수치 조각을 맞대, 울타리 밖 자리의 뜻이 갈리면 멈춘다.** 쉼표 · 소수 서식만
    달라진 조각은 「표기만 갈림」 으로 따로 센다. 기준선은 S234 뒤 스냅.
  · **1-1 목록에 없던 곳으로 번지면 멈춘다.**
  · **CLAUDE.md 는 고치지 않는다.**
  · **5절(ㄴ 재기)은 고치지 않는다.**
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · git log)에서 찾는다. 저장소에
    없으면 「기록 없음」. 이미 기록에 있는 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **센 값만 적는다. 벽시계는 시계를 보고 적는다.**
  · 세션 도중 사람에게 묻지 않는다.
  · **지시서에 없는 pytest 플래그를 붙이지 않고 · 있는 플래그를 바꾸지 않는다**(S234 가 기록
    묶음에 --tb=short 를 붙였다).
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다.
    도구로 되는 자리에 파이프를 쓰지 않는다. 조사 에이전트를 띄우면 이 금지를 그대로 전한다.
  · 절마다 따로 커밋한다. 커밋 메시지는 편집 도구로 지은 .txt 로 git commit -F.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S234 보고는 3f5e3d6) · 워크트리.
0-2  미해결 건수(S234 보고 145).
0-3  회귀값 여덟 · 덱 스냅 기준선(S234 뒤).
0-4  **실행 방식 이력** — 저장소에서 찾는다. (a) pytest 를 고친 곳만 좁게 돌리다 판마다 전체를
     돌게 바꾼 세션 · 그 세션 절 글자 · 까닭 (b) 덱을 몇 벌만 뜨다 19벌 전체로 바꾼 세션 · 글자 ·
     까닭. 자리 · 글자 그대로. 없으면 「기록 없음」. **이 판의 실행 방식은 이 값과 무관하게
     4절대로 한다**(전체 한 판을 빼지 않으므로 어떤 까닭과도 어긋나지 않는다).

커밋: S235 0절: 개시


1절 — 고칠 자리를 못 박는다

1-1  ①~④ 자리마다 — 산출물 · 벌 수 · 예시 글자 전 · 후(예정) · 그 글자를 만드는 코드 자리.
     ① 은 열마다 정할 소수 자리와 그 근거(화면 · Word 자릿수 또는 열 안 최빈값).
1-2  **고칠 자리 수를 값으로 못 박는다** — 코드 자리 · 시험 자리. 울타리이고 하한이다.

커밋: S235 1절: 고칠 자리


2절 — 고친다

2-1  **1-2 가 센 자리만 고친다.** 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았으면 자리마다 까닭을 값으로.

커밋: S235 2절: 자릿수 고침


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돌린다. 빨가면 고친 뒤
     이 좁은 판을 다시 돈다. 초록일 때만 3-2 로 간다.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

     어느 시험 파일을 골랐는지와 고른 까닭을 적는다.
3-2  **덱 몇 벌 먼저** — ①~④ 가 모두 서는 벌을 가장 적게 골라(고른 벌과 까닭을 적는다) 뜨고
     S234 뒤 스냅과 맞댄다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로 간다.
3-3  못 — 교차 못이나 절사 못을 넓혀 ①~④ 를 문다(Excel 은 셀 서식 문자열과 셀 값 불변을 함께
     본다). 새 못은 넓혀서 안 닫히는 경우에만 하나. 되돌려 확인한다(스크래치 monkeypatch ·
     plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S234 뒤 스냅과 수치 조각을 맞댄다. 뜻이 갈린 조각 0 · 표기만 갈린 조각 수 ·
     ①~④ 결함이 0 이 됐는지.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-3 과 같은지.
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 0줄을 값으로 — 폴더마다.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩
     차례로. 1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를
     더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S235 3절: 못 · 실물 · 회귀


4절 — 기록 · 닫기

4-1  PROCEED.md 에 235세션 절 — 사람 결정(가리키기만)과 웹 대화창 판단(ㄱ · ㄴ)을 갈라서 · 0-4 ·
     5절 값.
4-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(ㄷ 뿌리 몫 · ㄴ 판단으로 닫히는 것). 미해결
     건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
4-3  docs\HANDOVER.md — 뿌리 진행표를 이 판 값으로 갈아 끼운다(ㄷ 단계 · ㄴ 은 「재기 끝 · 사람
     결정 대기」). 2절에는 pytest 건수 · 초록만. 5절에 웹 대화창 판단(ㄱ · ㄴ)을 사람 결정과 갈라 적는다.
4-4  CLAUDE.md 는 고치지 않는다.
4-5  이 지시서 본문을 docs\directives\S235.md 로 그대로 앉힌다.
4-6  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-7  커밋 · git push. HEAD = origin · 워크트리 깨끗.

커밋: S235 4절: 기록 · 뿌리 진행표 · 지시서 앉히기


5절 — 다음 뿌리 「ㄴ 정의가 다른 두 수」 재기 (고치지 않는다)

5-1  **두 수가 무엇인가** — 이름 · 정의(코드 자리) · 192세션 절 증상 글자 그대로 · 두 수가 갈리는
     크기(19벌마다 값 둘과 차).
5-2  **어디에 서나** — 산출물(화면 · PPT · Excel · Word)마다 두 수가 서는 자리 · 이름표 글자 · 같은
     이름표로 다른 수가 서는 자리가 있는지.
5-3  **계산에 닿나** — 두 수 가운데 어느 것이 요금 계산에 들어가나(코드 자리) · 모으면 계산값이
     갈리는지 · 요금 약관 조문이 어느 쪽을 정하는지(data\source\ · rules_kr.json 에서 글자 그대로).
5-4  **걸린 결정 이력** — 저장소에서 이 뿌리에 걸린 결정 · 결정 주인 · 「둘 둘지 모을지」 물음이
     처음 선 세션과 글자.
5-5  **선택지** — 「둘 다 둔다(이름표만 고친다)」 · 「하나로 모은다」 등 길마다: 무엇을 건드리나 ·
     계산값이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 되돌리기 · 드는 판 수 · 걸린 앞선 결정.
     **사람이 읽는 말로 · 고르지 않는다.**
5-6  5절 값은 4-1 의 235세션 절에 넣는다(4절 커밋 전에 5절을 먼저 끝낸다).


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지
끝에 적는다. 소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에
    HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 0줄.
  3 수치가 움직인 곳 — 뜻이 갈린 조각 · 표기만 갈린 조각 · 회귀값 여덟 · 총액 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — **좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수**를
    따로.
  6 예상과 달랐던 것
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「0-4 실행 방식 이력」 — (a) · (b) 마다 세션 · 글자 · 까닭 또는 「기록 없음」.
  그 뒤에 「자릿수 전후」 — ①~④ 마다 예시 글자 전 → 후 · 결함 수 전 → 후.
  그 뒤에 「두 수」(5절) — 5-1 ~ 5-4 요약과 5-5 선택지 표. 사람이 읽는 말로. 코드 자리는 꼬리표로만.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고.

마지막에 벽시계(절마다 · 시계를 보고) · 백그라운드 셸 확인 결과.
