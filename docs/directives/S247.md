S247 — 고침 판 · 후보 E 입력 역률(계산 변경) · 나-18 · 나-1 · 나-24 · 나-22 를 고치고 · 나-19 · 나-21 · 마-24 의 사람 결정 재료를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 과 후보 B · F 는 닫혔다. 후보 A · D 는 고쳐 몫이
남았고 · C · G · I 는 S246 이 쟀고 · E 는 고칠 자리를 셌다 · H 와 J 의 남은 몫은 문구 판이다. 이 판은 **후보 E 를 고치고
(계산 변경은 이 하나) · 나-18(D) · 나-1 · 나-24(C) · 나-22(G) 를 고치고 · 나-19 · 나-21 · 마-24 의 사람 결정 재료를 재기만
한다.** 문구 판 몫(마-15 · 나-16 · 마-30 · 후보 H)은 0줄. 이 밖으로 가지 않는다. 항목 번호는 S246 보고의 지금 번호다 —
이름은 246세션 절 4-1 · 4-2 와 「다음 작업」 칸에서 찾아 이름으로 대조한다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).** 절사 A · 단위 기준 ·
측정 중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지 않는다 · S143 ·
S154 · S165 · S182 귀속 기준 ㄱ · S198 · S205 · S206 · S207 · S220 · 운영시간 외 부하는 비중(S238) · 고객 산출물 말투는
「~습니다」(S242) · 마-16 조합 부하로 DR · 마-35 「없음」 카드 40자 예외 · 기간 합산효과에 기간 DR 정산금(S246) · 마-33
조합 역률 경고는 그대로(S246).

**웹 대화창이 정했다(S246 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (후보 E) — 케이스 스터디와 일괄 생성은 건물마다 넣은 역률을 모든 자리에서 읽는다(S198). 92% 로 계산하던 자리를
    없애고 · 판정 한 줄도 넣은 역률을 읽는다. 역률 계산 방법(식 · 잣대)은 바꾸지 않는다 — 읽는 입력만 바꾼다.
  결정 2 (나-18) — 켠 ESS 가 초과 구간이 없어 「없음」 일 때 PPT · Word 는 같은 산출물이 다른 「없음」 수단(역률 100 벌의
    역률 개선 · 여지가 없는 계약전력)을 적는 꼴을 그대로 따른다 — 그 수단들이 장 · 절을 세우면 ESS 도 세우고 · 요약표 한
    줄뿐이면 ESS 도 한 줄이다. 두 수단의 꼴이 서로 다르면 고치지 않고 값을 낸다. 새 글자를 짓지 않는다.
  결정 3 (나-1 · 나-24 · 나-22) — 246세션 절 4-1 이 「이미 정한 기준으로 고칠 수 있다」 고 판정한 그 기준대로 고친다. 나-1 은
    여지가 없으면 「없음」 · 산출하지 못했으면 「미산출」(S205 · S237 결정 ㄴ). 나-24 는 같은 사실을 한 자리에서 만들어 1장 ·
    2장이 같은 글자를 쓴다(S233). 나-22 는 규칙 원문(기준 데이터 · 약관 조문) 글자대로 적는다. 1-1 이 확인한 기준 글자와
    어긋나는 자리는 고치지 않고 값을 낸다.

**S246 보고가 적었다 — 실물에서 먼저 확인하라.** 후보 E — 케이스 스터디와 일괄 생성이 넣은 역률을 일부 자리에서만 읽고
나머지는 92% 로 계산 · 고칠 자리는 계산 폴더 밖 넷(casestudy.py 셋 · batch.py:223)과 판정 한 줄(validity.py:269-277) ·
움직일 값은 케이스 C8(역률 85) · R4(역률 100)의 수단 표와 판정 두 줄 · 회귀값 여덟과 저장소 일괄 생성은 안 움직인다 /
나-18 은 PPT · Word 에서 줄 하나를 세우면 수단 장이 따라 선다 · 19벌 가운데 1벌 / 나-1 · 나-24 는 1벌 · 나-22 는 19벌 /
나-19 는 4벌 · 나-21 은 19벌 글자(금액은 배치로 가격을 넣은 판에서만) · 마-24 는 S143 을 다시 여는 일 · 길별 표는
246세션 절 4-1.

**울타리에 하한이 있다** — 1-6 이 센 「고칠 자리 수」. 결정 1 ~ 3 을 끝까지 적용하는 데 필요한 번짐은 울타리 안이다 — 자리와
까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **기본 19벌은 금액 · 판정 칸이 한 자리도 안 움직인다.** 움직이면 멈춘다. 19벌에서 갈려도 되는 것은 1-3 ~ 1-5 목록의
    글자와 나-18 이 세우는 줄 · 장 · 절뿐이다.
  · **값이 움직여도 되는 칸은 케이스 스터디 C8 · R4 의 수단 표와 판정 두 줄(1-2 목록)뿐이다.** 그 밖의 케이스 칸 · 타당성
    판정 칸이 갈리면 멈춘다. 움직인 칸은 전 → 후를 값으로.
  · **회귀값 여덟 가운데 하나라도 움직이면 기대값을 고치지 않고 멈춘다** — 이름 · 전 → 후를 값으로. 저장소 일괄 생성
    산출물이 갈려도 멈춘다.
  · 문구 판 몫과 나-19 · 나-21 · 마-24 는 코드 0줄.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-6 이 센 자리 밖 0줄.** 요금 · 절감액을
    만드는 식은 0줄.
  · **4절은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다. 케이스 스터디 C8 · R4 를
    92% 로 문 시험은 결정 1 로 고친다(전 · 후).
  · **CLAUDE.md 0줄 · tools\ 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면 「기록 없음」.
    이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집 도구로 지은 .txt 로
    git commit -F.** 절마다 따로 커밋한다 — 다른 절 몫을 앞 절 커밋에 싣지 않는다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다 — 3-1 꼴 · 3-8 꼴 · 아래 디버그 꼴 · 5-7 꼴. 그 밖에 `-n` · `--dist` · `--tb` ·
    `-s` 를 붙이지 않는다.** 빨간 시험 하나의 까닭을 볼 때만 아래 디버그 꼴을 쓴다 — 판 수에 넣되 초록 판정에 쓰지 않는다.
    인자가 붙은 시험은 node id 를 수집 목록에서 글자 그대로 옮긴다(S246 이 두 판을 못 찾았다).

         .venv\Scripts\python.exe -m pytest <빨간 시험 하나의 node id> -rf --tb=short

  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 · 같은
    옵션으로 나눠 뜬다. 전체 pytest 갈래가 끊기면 곧바로 다시 돌지 않고 그 갈래의 시험 파일을 같은 플래그로 한 파일씩
    돈다. 한 파일 판도 끊기면 멈춘다. 끊긴 판은 판 수에 넣고 까닭 한 줄. 멈추면 3절까지 커밋하고(push 하지 않는다) 끊긴
    자리를 보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를 쓰지 않는다 —
    `| Out-Null` · `| ForEach-Object` 도 파이프다.** 여러 줄을 훑을 때는 찾기 도구나 읽기 도구로 본다. 조사 에이전트를
    띄우면 이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(마감 판 보고는 2b9f505) · 워크트리.
0-2  미해결 건수(127) · 회귀값 여덟 · 덱 스냅 기준선(S246 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다) · 케이스 스터디 기준선
     (S246 뒤 값).

커밋: S247 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — S198 · S205 · S233 · S237 결정 ㄴ · 246세션 절 4-1 · 4-2 의 판정과 기준 글자를 글자 그대로 · 결정 주인.
     결정 1 ~ 3 과 어긋나는 글자가 있는지.
1-2  **후보 E(결정 1)** — 케이스 스터디 · 일괄 생성이 역률을 읽는 자리와 92% 로 계산하는 자리 전수 · 판정 한 줄의 글자 ·
     움직일 칸 전수(C8 · R4 수단 표 · 판정 두 줄 · 그 밖에 닿는 칸이 있는지) · 전 → 후(예정) · 회귀값 여덟 · 저장소 일괄
     생성에 닿는지. 확인 사례 — 일괄 생성에 역률을 넣은 입력(넣은 값과 까닭).
1-3  **나-18(결정 2)** — PPT · Word 가 역률 100 벌의 역률 개선과 여지가 없는 계약전력을 적는 꼴(요약표 줄 · 수단 장 · Word
     절) · 두 꼴이 같은지 · ESS 「없음」 에 따를 꼴 · 서는 벌 · 고친 뒤 모습(예정).
1-4  **나-1 · 나-24(결정 3)** — 목표 계약전력 「없음」 · 「미산출」 이 서는 자리와 조건 · 미산출 사유가 1장 · 2장에 서는
     자리와 두 글자 · 그 사실을 만드는 한 자리 · 서는 벌 · 고친 뒤 글자(예정).
1-5  **나-22(결정 3)** — DR 정산 단가 설명의 글자 · 규칙 원문 글자(기준 데이터 · 약관 조문 · 출처와 확인일) · 서는 자리 ·
     고친 뒤 글자(예정).
1-6  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S247 1절: 고칠 자리


2절 — 고친다

2-1  **1-6 이 센 자리를 고친다.** 결정 1 ~ 3 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S247 2절: 입력 역률 · ESS 없음 · 계약 목표 · 미산출 사유 · DR 단가 설명


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 화면 시험 파일과
     기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과 한 프로세스에서 돌면 기온 기준선 못이
     빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은 판을
     다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — (가) 나-18 · 나-1 · 나-24 · 나-22 가 서는 벌을 가장 적게 골라(고른 벌과 까닭) 기본 입력으로 뜬다 ·
     산출물 넷(화면 · PPT · Excel · Word)을 통째로 S246 뒤 스냅과 맞대 갈린 줄이 전부 1-3 ~ 1-5 목록 안인지 · 금액 · 판정 칸
     0 인지. (나) 1-2 의 일괄 생성 확인 사례를 옛 코드 · 새 코드로 돈다(옛 코드는 238세션 절의 길) — 넣은 역률을 모든
     자리에서 읽는지 · 갈린 칸이 1-2 목록 안인지. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「케이스 스터디와 일괄 생성은 넣은 역률을 모든 자리에서 읽는다(92% 고정 자리
     0)」 (2) 「켠 ESS 의 「없음」 은 PPT · Word 에서 다른 「없음」 수단과 같은 꼴로 선다」 (3) 「목표 계약전력은 여지 없음이면
     「없음」 · 산출 못 하면 「미산출」」 (4) 「미산출 사유는 1장 · 2장이 같은 글자」 (5) 「DR 정산 단가 설명은 규칙 원문 글자」
     를 문다. 산출물을 실제로 만든 글자와 값을 본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로
     먼저 보고 빨간 것을 본다. 재료 확인 글자가 고친 뒤 글자를 담아 되돌림이 얕아지지 않게 한다.
3-4  **덱 19벌 한 판** — S246 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-3 ~ 1-5 목록 안인지 · 금액 · 판정 칸 0 인지. 금액 칸이
     갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 0-2 기준선과 맞댄다. 갈린 칸은 이름으로 — C8 · R4 의 수단 표와 판정 두 줄은 전 → 후를 값으로
     내고 · 그 밖의 칸(소요 칸은 두고) · 회귀값 여덟 · 다른 케이스의 타당성 판정이 갈리면 멈춘다.
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-6 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로 — ② 는 처음부터 그
     갈래의 시험 파일을 같은 플래그로 한 파일씩. 1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의
     플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S247 3절: 못 · 실물 · 회귀


4절 — 사람 결정 재료 재기 (고치지 않는다 · 고르지 않는다 · 3-4 가 뜬 19벌 실물로 잰다)

4-1  **나-19 계약형 건물의 계약전력 조정 까닭 문장** — 지금 글자 그대로 · 서는 벌과 산출물 · 그 문장이 어느 벌에서 왜 참이
     아닌가 · 걸린 결정 글자 그대로와 주인 · 246세션 절 4-1 의 길마다: 바뀌는 글자(예시) · 금액이 갈리나 · 고객 산출물에
     무엇이 바뀌나 · 근거가 저장소에 있나 / 현장에만 있나 · 되돌리기 · 드는 판 수.
4-2  **나-21 DR 위약금 식을 육지(SMP) · 제주로 가를지** — 지금 식과 글자 · 규칙 원문(기준 데이터 · 약관 · 시장 규칙 조문 —
     출처와 확인일 · 글자 그대로) · 육지와 제주가 원문에서 어떻게 다른가 · 금액이 갈리는 조건(배치로 가격을 넣은 판) · 길마다
     위와 같은 꼴.
4-3  **마-24 계약전력 기준 종별의 초과사용부가금을 계속 산출하지 않을지** — S143 결정 원문과 그때의 까닭 · 지금 그 종별에서
     화면 · 산출물이 부가금을 어떻게 적나 · 약관 조문(부가금이 그 종별에 걸리는지 · 글자 그대로) · 산출하려면 필요한 입력과
     근거가 저장소에 있나 · 길마다 위와 같은 꼴.
4-4  세 재료는 **사람이 읽는 말로 · 코드 용어 없이** 적는다. 길마다 사람이 고를 수 있게 「이 길을 고르면 고객 보고서에서
     무엇이 달라지나」 를 한 줄로. 코드 자리는 꼬리표로만. 고르지 않는다.
4-5  4절 값은 5-1 의 247세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 247세션 절 — 웹 대화창 판단(결정 1 ~ 3)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 4절 값. 세션 목록표에
     247 행(브리핑이 읽는 날짜 꼴 · 「늘어난 것」 낱말 포함).
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(후보 E · 나-18 · 나-1 · 나-24 · 나-22 가운데 닫힌 것). 미해결 건수
     대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리 진행표
     C · D · E · G · I 행 · 5절에 웹 대화창 판단 행(결정 1 ~ 3) · 머리 사슬에 S247 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(나-19 · 나-21 · 마-24 사람 결정 대기 ·
     문구 판(마-15 · 나-16 · 마-30 · 후보 H) · 이 판이 고치지 않고 값으로 낸 자리). 상한 3,000자.
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S247.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S247 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-6 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 케이스 스터디 C8 · R4 에서 움직인 칸 전 → 후 · 회귀값 여덟 · 총액 ·
    pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수(② 는 파일 판 수) · 디버그
    꼴 판 수를 따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ J 포함).
  그 뒤에 「입력 역률 표」 — 케이스 · 일괄 생성 확인 사례마다 역률 · 칸 · 전 → 후 · 판정 두 줄 전 → 후.
  그 뒤에 「글자 고침 표」 — 나-18 · 나-1 · 나-24 · 나-22 의 전 → 후 · 서는 벌 수 · 따른 꼴 · 고치지 않은 자리와 까닭.
  그 뒤에 「사람이 정할 것 — 재료」(4절) — 나-19 · 나-21 · 마-24 마다 지금 모습 · 걸린 결정 · 원문 · 길 표. 사람이 읽는 말로 ·
    코드 자리는 꼬리표로만 · 고르지 않는다.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
