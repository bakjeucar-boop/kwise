S236 — 고침 판 · 역률 투자비 비움 표기 · 다음 뿌리 「0원 거르기」 를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 자릿수는 닫혔다. 「정의가
다른 두 수」 는 사람 결정 대기(웹 대화창이 묻는다). 이 판은 **이미 정한 역률 투자비 비움 표기를
고치고**, 다음 뿌리 **「ㄹ 0원 거르기」 를 재기만 한다**(고치지 않는다). 그리고 HANDOVER 기록
결함 셋을 고친다. 이 셋 밖으로 가지 않는다 — 다른 뿌리 · 도구 · 측정은 이 판 몫이 아니다.

**사람이 정했다(HANDOVER 5절이 쥔다 · 다시 적지 않는다).** 절사 A · 단위 기준 · 측정 중단 ·
CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지 않는다.

**웹 대화창이 정했다(HANDOVER 5절 S235' 행).** 역률 개선 투자비를 비웠을 때는 「미산출 — 투자비
미입력」 으로 적는다 — 같은 화면의 다른 수단이 비웠을 때 쓰는 방식을 그대로 따른다(S231 1절 값이
근거 · 지금은 비우면 0 으로 읽혀 모든 경로가 「즉시」). 투자비를 넣은 경로는 한 글자도 안 바뀐다.

**울타리에 하한이 있다** — 1-3 이 센 「고칠 자리 수」.

이 판의 못 박힌 안전한 쪽.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-3 이 센 자리 밖 0줄.**
  · **회귀값 여덟 · 요금 총액 · 케이스 스터디 타당성은 한 자리도 안 움직인다.** 움직이면 멈춘다.
  · **투자비를 넣은 경로의 산출물 글자는 안 바뀐다.** 바뀌면 멈춘다.
  · 덱 19벌 전 · 후를 맞대 역률 카드 회수기간 · 투자비 자리 밖이 갈리면 멈춘다. 기준선은 S235 뒤 스냅.
  · **1-2 목록에 없던 곳으로 번지면 멈춘다.**
  · 비움과 「0원을 넣음」 을 코드가 가르지 못하면(입력 기본값이 0 등) 고치지 않고 멈춰 값을 낸다.
  · **4절(0원 거르기)은 고치지 않는다.**
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · git log)에서 찾는다 — 없으면 「기록
    없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · 화면 문구(글자)는 늘리지 않는다 — 「미산출 — 투자비 미입력」 은 이미 있는 글자를 쓴다. CLAUDE.md
    「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **센 값만 적는다. 벽시계는 시계를 보고 적는다. 커밋 메시지는 편집 도구로 지은 .txt 로
    git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · 지시서에 없는 pytest 플래그를 붙이지 않고 · 있는 플래그를 바꾸지 않는다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. 도구로
    되는 자리에 파이프를 쓰지 않는다. 조사 에이전트를 띄우면 이 금지를 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S235' 보고는 1169614) · 워크트리.
0-2  미해결 건수(146) · 회귀값 여덟 · 덱 스냅 기준선(S235 뒤).

커밋: S236 0절: 개시


1절 — 투자비 고칠 자리를 못 박는다

1-1  **같은 화면의 다른 수단은 비움을 어떻게 가르나** — 입력 칸 기본값 · 비움이 None 인가 0 인가 ·
     「미산출 — 투자비 미입력」 글자를 만드는 자리 · 회수기간 계산이 비움을 받는 자리(코드 자리).
1-2  **역률 카드 경로 전부** — S231 1-2 표의 아홉 경로(화면 카드 · 3단계 표 · 조합 · PPT 8 · 12 · 16장 ·
     Excel 수단별 결과 · Excel 조합 비교 · Word 표 · 일괄 생성)마다 비움일 때 지금 글자 · 고친 뒤 글자
     (예정) · 그 글자를 만드는 코드 자리 · 캐시 서명 · 일괄 생성 · 조합 인자에 닿는 자리.
1-3  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S236 1절: 투자비 고칠 자리


2절 — 고친다

2-1  **1-3 이 센 자리만 고친다.** 1-1 의 방식을 그대로 따른다. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았으면 자리마다 까닭을 값으로.

커밋: S236 2절: 투자비 비움 표기


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 역률 카드가 서는 벌을 가장 적게 골라(고른 벌과 까닭) 투자비 비움 · 넣음 두
     경우로 뜨고 맞댄다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓혀 「비움이면 아홉 경로가 「미산출 — 투자비 미입력」 · 넣음이면 전과 같은 글자」
     를 문다. 넓혀서 안 닫히면 새 못 하나. 산출물을 실제로 만든 글자를 본다. 되돌려 확인한다
     (스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S235 뒤 스냅과 맞댄다. 갈린 자리가 전부 역률 카드 회수기간 · 투자비 자리인지.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(역률 회수기간
     칸 밖이 갈리면 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-3 자리 밖 0줄을 값으로 · 폴더마다.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S236 3절: 못 · 실물 · 회귀


4절 — 다음 뿌리 「ㄹ 0원 거르기」 재기 (고치지 않는다)

4-1  **증상** — 192세션 절의 증상 8 · 9 · 10 글자 그대로 · 지금 실물(S235 뒤 스냅)에서 서는지 · 벌 수 ·
     예시 글자. 미해결 「절감이 0 이거나 여지가 없는 수단을 거르는 잣대가 자리마다 달라 조합 이름과
     문장에 선다」 의 몸.
4-2  **자리와 잣대** — S196 · S205 · S208 이 센 자리와 그 뒤 모인 자리 · 남은 자리(코드 자리 · 계산
     폴더 몫 따로) · 자리마다 무엇을 0 으로 보나(여지 · 절감액 · 투입량 · 미산출 등).
4-3  **걸린 결정** — 저장소 전체에서 이 뿌리에 닿는 결정(HANDOVER 5절 S205 행 · 귀속 기준 ㄱ ·
     collaboration.md · CLAUDE.md 등) 글자 그대로 · 결정 주인. 「잣대를 사람이 정한다」 가 처음 선
     세션과 글자 · 사람이 답한 기록이 있는지.
4-4  **판정** — 남은 자리마다 4-3 의 기록된 결정으로 잣대가 서는가 / 안 서는가. 안 서는 자리만
     선택지를 길마다 한 줄씩: 무엇을 건드리나 · 금액 · 계산값이 갈리나 · 고객 산출물에 무엇이
     바뀌나 · 되돌리기 · 드는 판 수. **사람이 읽는 말로 · 고르지 않는다.**
4-5  4절 값은 5-1 의 236세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 236세션 절 — 웹 대화창 판단(투자비)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 4절 값.
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(투자비 항목 · 제목이 틀렸던 것도 닫힘의 까닭에
     한 줄). 미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md
       · 4절 뿌리 진행표를 이 판 값으로 갈아 끼운다(ㄹ 행 · 투자비는 뿌리가 아니라 행 없음).
         **「ㅂ 개발 말」 행과 「ㅅ 기간 대 12개월」 행이 한 줄로 붙어 표가 깨졌다 — 두 행으로 가른다.**
       · 머리 사슬에 S235' 한 단락(미해결 144 → 146 · 올린 둘 · 기록만)과 S236 단락.
       · 7절 두 줄에 곁 줄 — 「1번 PC 는 통째로 돌린다(깨끗하고 플래그 없는 마지막 판 …)」 와 「남의
         python 을 세는 횟수는 PC 로 갈린다 …」 에 「[S236 곁 — S234 사람 결정으로 소요 · 깨끗한 판 ·
         남의 python 을 재지도 적지도 않는다 · 5절]」. 옛 글자는 고치지 않는다.
       · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(뿌리 「정의가 다른
     두 수」 사람 결정 대기 · 0원 거르기 고침 · 겹침 거르기 · 개발 말과 그물).
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S236.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S236 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-3 자리 몫과 그 밖 0줄).
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를 따로.
  6 예상과 달랐던 것
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「투자비 경로표」 — 경로 아홉 · 비움 전 → 후 · 넣음 전 → 후.
  그 뒤에 「0원 거르기」(4절) — 증상 · 남은 자리와 잣대 · 걸린 결정 · 판정(서는 자리 / 안 서는 자리) ·
    안 서는 자리의 선택지 표. 사람이 읽는 말로. 코드 자리는 꼬리표로만.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고.

마지막에 벽시계(절마다 · 시계를 보고) · 백그라운드 셸 확인 결과.
