S240 — 고침 판 · 뿌리 「ㅁ 겹침 거르기」 두 자리를 고친다 · 다음 뿌리 「ㅂ 개발 말과 그물」 을 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 두 수 · 자릿수 · 0원 거르기는
닫혔다. S239 가 「ㅁ 겹침 거르기」 를 쟀다. 이 판은 **ㅁ 의 남은 두 자리(Excel 요약 조합 묶음 · 부록
C)를 고치고 · 다음 뿌리 「ㅂ 개발 말과 그물」 을 재기만 한다**(고치지 않는다). 이 밖으로 가지 않는다 —
다른 뿌리 · 도구 · 측정은 이 판 몫이 아니다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).**
절사 A · 단위 기준 · 측정 중단 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지
않는다 · S207 · 운영시간 외 부하는 비중(S238).

**웹 대화창이 정했다(S239 뒤 · 이 판이 HANDOVER 5절에 세 행으로 앉힌다).**
  결정 1 (잣대) — 한 산출물의 한 층 안에서 같은 사실은 한 번만 선다. 같은 사실인지는 글자가 아니라 사실
    ID 로 가른다(S210 이 거르는 잣대를 사실 ID 하나로 모았다). 층이 다르면(요지와 전문 · Word 본문과
    부록 계산 근거) 겹침이 아니다(collaboration.md 「층은 중복이 아니다」 · S239 4-4 판정).
  결정 2 (Excel 요약 조합 묶음) — 조합 이름 앞머리를 뗀 글자가 같고 같은 사실 ID 인 줄은 한 번만 앞머리
    없이 세운다(S220 의 같은 글자 접기를 앞머리 너머로 적용). 그 벌의 조합 줄 전부에 서는 줄만 접는다.
    같은 사실이 요약의 다른 자리에 이미 서 있으면 조합 묶음 쪽을 뺀다. 앞머리를 떼도 글자가 다르면
    (조합마다 값이 다르면) 다른 사실이라 그대로 둔다.
  결정 3 (부록 C) — 한계 글에 사실 ID 를 달아 사실로 거른다 — 앞 30자 견줌을 갈음한다. 짝 가운데
    남기는 글은 같은 사실을 다른 산출물이 이미 쓰는 글자, 다른 산출물에 없으면 그 사실을 만드는
    자리(tools\fact_sites.json) 쪽 글자다. 글자를 새로 짓지 않는다 — 다듬기는 사람이 화면을 열어 한
    판에 잡는 문구 판 몫이다.

**S239 보고가 적었다 — 실물에서 먼저 확인하라.** 계약 하한 문장이 10벌 71줄 · 그 가운데 Excel 요약 조합
묶음 31줄 / 부록 C 에 뜻은 같고 글자가 다른 짝이 19벌 다섯 쌍 · 6벌은 ESS 두 쌍 더 / S210 이 잣대를 사실
ID 하나로 모았고 S220 이 Excel 요약의 같은 글자 행을 접었다 / 부록 C 는 앞 30자로만 견준다 / 여지가 없는
수단의 조합 줄이 조합의 마지막 줄이면 S239 가 남겼다(기본 19벌 0벌) / HANDOVER 1절이 S238 값이다.

**울타리에 하한이 있다** — 1-4 가 센 「고칠 자리 수」. 결정 1 ~ 3 을 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한
    자리도 안 움직인다.** 움직이면 멈춘다.
  · **빠지는 줄마다 같은 산출물 같은 층에 같은 사실 ID 로 남은 줄이 있어야 한다.** 없는 줄이 빠지면(뜻이
    다른 말이 사라지면) 멈춘다.
  · 한 사실 ID 에 글자가 다른 줄이 서서 어느 쪽을 남길지 결정 3 으로 안 갈리는 짝은 고치지 않고 두
    글자를 값으로 낸다.
  · 사실 ID 를 새로 지을 때는 tools\fact_sites.json 과 S210 이 모은 자리의 꼴을 따른다 — 그물 도구
    자체를 고치지 않는다.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-4 가 센 자리 밖 0줄.**
    요금 · 절감액을 만드는 식은 0줄.
  · **4절(개발 말과 그물)은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다 — 새 글자를 짓지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로
    따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다
    — 없으면 「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다 — 출력을 보기 전에
    적지 않는다(S236 · S237 · S239 가 어겼다). 커밋 메시지는 편집 도구로 지은 .txt 로 git commit -F.**
    절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다. 지시서가 꼴을 적지 않은 판(못 하나 · 되돌려 확인 · 넓힌
    못 한 판)도 3-1 꼴 그대로 — `-rf --tb=no` 밖 플래그를 붙이지 않는다(S237 · S239 가 --tb=short 를
    붙였다).**
  · 전체 pytest 갈래가 메모리 부족으로 끊기면 스스로 다시 돌리지 않고 플래그도 바꾸지 않는다. 거기서
    멈춰 3절까지 커밋하고(push 하지 않는다) · 끊긴 자리(수집 몇 건 가운데 몇 건째 · 그때까지 빨강 수)를
    보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파일을 볼
    때는 읽기 도구로 본다 — 머리 몇 줄을 볼 때도 파이프를 쓰지 않는다(S239 가 브리핑 머리에 썼다).**
    조사 에이전트를 띄우면 이 금지를 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S239 보고는 196dd87) · 워크트리.
0-2  미해결 건수(144) · 회귀값 여덟 · 덱 스냅 기준선(S239 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S240 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — S210 · S220 판정 · collaboration.md 「층은 중복이 아니다」 · S239 4-4 판정을 글자 그대로 ·
     결정 주인. 결정 1 ~ 3 과 어긋나는 글자가 있는지.
1-2  **Excel 요약 조합 묶음(결정 2)** — 앞머리를 떼면 같은 글자가 되는 줄 전수 · 줄마다 사실 ID · 그 벌의
     조합 줄 전부에 서나 일부에만 서나 · 같은 사실이 요약의 다른 자리에 이미 서나 · 접힌 뒤 글자(예정).
     조합마다 값이 다른 줄은 따로 센다.
1-3  **부록 C(결정 3)** — 한계 글 전수와 만드는 자리 · 지금 사실 ID 가 달린 것 / 안 달린 것 · 뜻이 같은
     짝 전수(S239 의 다섯 쌍 · ESS 두 쌍과 그 밖) · 짝마다 다른 산출물이 쓰는 글자 · fact_sites 쪽 글자 ·
     남길 글(예정).
1-4  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S240 1절: 고칠 자리


2절 — 고친다

2-1  **1-4 가 센 자리를 고친다.** 결정 1 ~ 3 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S240 2절: 겹침 거르기


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면
     시험과 한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지
     않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 결정 2 · 3 이 다 서는 벌(ESS 두 쌍이 서는 벌 포함)을 가장 적게 골라(고른 벌과
     까닭) 뜬다. **산출물 넷(PPT · Excel · Word · 일괄)을 통째로 S239 뒤 스냅과 맞대** 갈린 줄이 전부
     1-4 자리인지 · 빠진 줄마다 같은 층에 같은 사실 ID 로 남은 줄이 있는지 본다. 옛 코드 덱은 238세션
     절의 길로 뜬다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓혀 「한 산출물 한 층에서 같은 사실 ID 는 한 번만 선다(Excel 요약 · 부록 C)」 와
     「빠진 줄의 사실은 같은 층에 남는다」 를 문다. 넓혀서 안 닫히면 새 못. 산출물을 실제로 만든 글자를
     본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을
     본다.
3-4  **덱 19벌 한 판** — S239 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-4 자리인지 · 빠진 줄 전수가 3-2 의
     잣대를 지키는지. 금액 칸이 갈리면 멈춘다. 이 판의 19벌 실물은 4절이 쓴다 — 지우지 않는다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-4
     자리 밖이 갈리면 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-4 자리 밖 0줄을 값으로 · 폴더마다.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S240 3절: 못 · 실물 · 회귀


4절 — 다음 뿌리 「ㅂ 개발 말과 그물」 재기 (고치지 않는다 · 3-4 가 뜬 19벌 실물로 잰다)

4-1  **증상** — 이 뿌리에 묶인 미해결 항목(이름으로)의 몸 · 지금 실물에서 서는지 · 벌 수 · 예시 글자.
4-2  **이력과 자리** — 이 뿌리가 진행표에 「재기」 인데 어느 세션이 무엇을 재고 · 고쳤고 · 무엇이
     남았나(PROCEED.md · HANDOVER 4절 · git log). 남은 자리(코드 자리 · 계산 폴더 몫 따로)와 자리마다
     무엇을 개발 말로 보나 · 그물이 무엇을 보고 무엇을 못 보나.
4-3  **걸린 결정** — 저장소 전체에서 이 뿌리에 닿는 결정(HANDOVER 5절 · collaboration.md · CLAUDE.md
     「화면 문구」 절 등) 글자 그대로 · 결정 주인. 「잣대를 사람이 정한다」 가 선 세션과 사람이 답한
     기록이 있는지.
4-4  **판정** — 남은 자리마다 4-3 의 기록된 결정으로 잣대가 서는가 / 안 서는가. 안 서는 자리만 선택지를
     길마다 한 줄씩: 무엇을 건드리나 · 금액 · 계산값이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 되돌리기 ·
     드는 판 수. **사람이 읽는 말로 · 고르지 않는다.**
4-5  4절 값은 5-1 의 240세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 240세션 절 — 웹 대화창 판단(결정 1 ~ 3)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 4절
     값. 세션 목록표에 240 행.
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(안내 겹침 항목 · 닫힌 것이 더 있으면 이름으로).
     미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 ·
     4절 ㅁ · ㅂ 행 · 5절에 웹 대화창 판단 세 행(결정 1 ~ 3) · 머리 사슬에 S240 단락 · 2절에는 pytest
     건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(개발 말과 그물 ·
     이 판이 고치지 않고 값으로 낸 자리 · 여지가 없는 수단이 조합의 마지막 줄인 경우).
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S240.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S240 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-4 자리 몫과 그 밖 0줄).
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를
    따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「겹침 거르기 표」 — 자리(Excel 요약 · 부록 C)마다 빠진 줄 수 · 남긴 글의 출처 · 고치지 않은
    짝과 까닭.
  그 뒤에 「개발 말과 그물」(4절) — 증상 · 이력(잰 것 / 고친 것 / 남은 것) · 남은 자리와 잣대 · 걸린
    결정 · 판정(서는 자리 / 안 서는 자리) · 안 서는 자리의 선택지 표. 사람이 읽는 말로. 코드 자리는
    꼬리표로만.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
