S245 — 고침 판 · 마-16(조합 부하로 DR 다시 재기 · 계산 변경) · 나-26 · 나-27 을 고치고 · DR 정산금 기간 값과 마-33 재료를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 과 후보 B 는 닫혔고 · 후보 A 는 S244 가
고쳐 마-16(사람이 정함 · 계산 변경)과 나-18(문구 판)이 남았다 · 후보 F · J 는 S244 가 쟀다. 이 판은 **마-16 을
고치고(계산 변경은 이 하나) · F 의 나-26 · 나-27 을 고치고 · DR 정산금 기간 값과 마-33 의 사람 결정 재료를
재기만 한다**(고치지 않는다). 마-35 는 나-27 에 필요한 예외만 적용한다. 마-33 · 마-34 · 나-18 은 0줄. 이 밖으로
가지 않는다. 항목 이름은 243 · 244세션 절에서 찾는다.

**사람이 정했다(이 판이 HANDOVER 5절에 행으로 앉힌다 · 마-16 행은 S244 가 앉혔으면 가리키기만).**
  마-16 (S243 뒤) — 조합에서 DR 감축 가능량은 태양광 · ESS 를 반영한 조합 부하로 다시 잰다. ESS 는 피크 저감에 쓴
    뒤 남은 부하 기준이고 · ESS 를 DR 전용으로 돌리는 운전 전략은 넣지 않는다.
  마-35 예외 (S244 뒤) — 여지가 없는 카드(「없음」 판정)는 개요 40자 하한의 예외다 — 짧아도 참인 말만 남긴다.
    다른 카드의 하한은 그대로다.
  앞서 정한 것(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만) — 절사 A · 단위
    기준 · 측정 중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로
    빠지지 않는다 · S154 · S165 · S182 귀속 기준 ㄱ · S202 · S206 · S207 · S220 · 운영시간 외 부하는 비중(S238) · 고객
    산출물 말투는 「~습니다」(S242).

**웹 대화창이 정했다(S244 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (마-16 적용) — 3단계는 수단마다 처음부터 다시 계산한다(collaboration.md). 조합 계산이 DR 감축 가능량을 잴
    때 그 조합에서 태양광 · ESS 를 반영한 부하를 넣는다. 감축 가능량을 재는 방법은 2단계 DR 카드와 같은 것을 쓰고 ·
    넣는 부하만 바꾼다. 2단계 DR 카드(원 부하)는 그대로다.
  결정 2 (나-26) — DR 카드 캡션은 그 벌의 값으로 참이 되는 말만 한다(S207). 벌 값에 기대는 말은 그 벌 값을 읽고 ·
    값이 없으면(단가 미입력 등) 같은 화면이 이미 쓰는 미입력 글자를 따른다. 새 글자를 짓지 않는다 — 이미 있는
    글자로 안 되는 자리는 고치지 않고 값을 낸다.
  결정 3 (나-27) — 역률 카드 개요는 여지가 없는 벌에서 거짓이 되는 앞머리를 세우지 않는다(S154 · S206 · S207). 남는
    말이 40자 미만이어도 된다(마-35 예외). 40자 하한이 사는 자리(시험 · 감사 규칙 · 규약)에는 「없음」 판정 카드
    예외만 단다.

**S244 보고가 적었다 — 실물에서 먼저 확인하라.** 덱 19벌에 DR 단가가 든 벌이 없어 19벌 회수기간이 다 「미산출」
이고 DR 정산금 값이 서는 벌이 0 / 나-26 은 화면 19벌에 서는 상수 캡션(spec.py:142) · 거짓이 된 판은 아직 0 /
나-27 은 역률 100 세 벌에서 앞머리가 거짓(spec.py:111) · 고치면 남는 말이 40자 하한에 걸린다 / PPT · Word 의 기간
합산효과는 DR 정산금을 안 담는다 — 정산금은 12개월 값뿐(S219) · S220 이 그 자리의 기간 / 12개월을 정하지 않았다 /
마-33 은 Excel 요약 조합 행 16벌 · 「경고 조건과 문구를 사람이 정한다」(S202 · combination.py:542) · 길 표는 244세션
절 4-4 / 3-2 스냅이 담는 산출물은 화면 · Excel · PPT · Word 넷이다.

**울타리에 하한이 있다** — 1-5 가 센 「고칠 자리 수」. 결정 1 ~ 3 과 마-35 예외를 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **기본 19벌은 금액 · 판정 칸이 한 자리도 안 움직인다**(DR 단가가 없다). 움직이면 멈춘다. 19벌에서 갈려도 되는
    것은 1-3 · 1-4 목록의 글자와 · DR 감축 가능량을 적는 칸(1-2 목록)뿐이다.
  · **값이 움직여도 되는 칸은 DR 이 든 조합의 DR 감축 가능량 · DR 정산금 · 그 조합의 절감액 · 회수기간뿐이다** —
    1-2 목록 안. 권장안이 바뀌면 멈추지 않고 전 → 후를 값으로 낸다. 그 밖의 금액 칸이 갈리면 멈춘다.
  · **2단계 DR 카드 값은 한 자리도 안 움직인다.** 움직이면 멈춘다.
  · **회귀값 여덟 가운데 하나라도 움직이면 기대값을 고치지 않고 멈춘다** — 이름 · 전 → 후를 값으로 낸다. 케이스
    스터디 타당성 판정 칸이 갈려도 멈춘다.
  · **DR 감축 가능량을 재는 방법(식 · 잣대)은 바꾸지 않는다** — 넣는 부하만 바꾼다.
  · 마-33 · 마-34 · 나-18 · PPT · Word 기간 합산효과는 0줄.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-5 가 센 자리 밖 0줄.**
  · **4절은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 는 40자 하한이 거기 살 때만 그 줄에 「없음」 판정 카드 예외 한 줄을 단다(사람이 규칙을 바꿨다) —
    그 밖 0줄. tools\ 는 40자 하한이 거기 살 때만 그 규칙의 예외 줄 — 그 밖 0줄.** 소요 · 깨끗한 판 · 전원 · 남의
    python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면
    「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집 도구로 지은
    .txt 로 git commit -F.** 절마다 따로 커밋한다 — 다른 절 몫을 앞 절 커밋에 싣지 않는다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다. 좁은 판 · 못 하나 · 되돌려 확인도 3-1 꼴 그대로 — `-n` · `--dist` ·
    `--tb=short` · `-s` 를 붙이지 않는다.**
  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 ·
    같은 옵션으로 나눠 뜬다. 전체 pytest 갈래가 끊기면 곧바로 다시 돌지 않고 그 갈래의 시험 파일을 같은 플래그로
    한 파일씩 차례로 돈다. 나눠 돈 판도 끊기면 멈춘다. 끊긴 판은 판 수에 넣고 까닭 한 줄. 멈추면 3절까지 커밋하고
    (push 하지 않는다) 끊긴 자리를 보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를 쓰지 않는다
    — `| Out-Null` · `| ForEach-Object` 도 파이프다.** 여러 줄을 훑을 때는 찾기 도구나 읽기 도구로 본다. 조사
    에이전트를 띄우면 이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S244 보고는 6d7c34f) · 워크트리.
0-2  미해결 건수(133) · 회귀값 여덟 · 덱 스냅 기준선(S244 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S245 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — collaboration.md 「3단계는 수단마다 처음부터 다시 계산」 · S154 · S202 · S206 · S207 · S219 · 마-16 · 마-35
     를 글자 그대로 · 결정 주인. 결정 1 ~ 3 과 어긋나는 글자가 있는지.
1-2  **마-16(결정 1)** — DR 감축 가능량을 재는 자리(2단계 · 3단계)와 넣는 부하 · 조합 계산이 태양광 · ESS 를 반영한
     부하를 갖는 자리 · 그 감축 가능량이 흘러가는 칸 전수(조합 DR 정산금 · 조합 절감액 · 회수기간 · 권장안 · 화면 ·
     PPT · Excel · Word · 케이스 스터디) · 캐시 열쇠에 닿는 자리. 확인 사례 — DR 이 서고 태양광 · ESS 가 든 조합이
     서는 벌을 가장 적게 골라 DR 단가를 넣은 입력(넣은 단가와 까닭을 적는다) · 그 사례의 조합마다 전 → 후(예정).
1-3  **나-26(결정 2)** — DR 카드 캡션의 글자 그대로 · 벌 값에 기대는 부분 · 읽어야 할 값 · 값이 없을 때 같은 화면이
     쓰는 미입력 글자 · 고친 뒤 글자(예정).
1-4  **나-27 · 마-35 예외(결정 3)** — 역률 카드 개요의 글자 그대로 · 여지가 없는 벌에서 거짓이 되는 앞머리 · 남는 말과
     글자 수 · 40자 하한이 사는 자리 전수(시험 · 감사 규칙 · CLAUDE.md · tools\) · 예외를 다는 자리.
1-5  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리 · CLAUDE.md / tools\ 자리가 있으면
     따로. 울타리이고 하한이다.

커밋: S245 1절: 고칠 자리


2절 — 고친다

2-1  **1-5 가 센 자리를 고친다.** 결정 1 ~ 3 과 마-35 예외 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S245 2절: 조합 부하 DR · DR 캡션 · 역률 개요


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 화면
     시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과 한 프로세스에서 돌면
     기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이
     빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — (가) 나-26 · 나-27 이 서는 벌을 가장 적게 골라(고른 벌과 까닭) 기본 입력으로 뜬다. 산출물
     넷(화면 · PPT · Excel · Word)을 통째로 S244 뒤 스냅과 맞대 갈린 줄이 전부 1-2 ~ 1-4 목록 안인지 · 금액 칸 0 인지.
     (나) 1-2 의 확인 사례를 옛 코드 · 새 코드로 뜬다(옛 코드 덱은 238세션 절의 길) · 조합마다 DR 감축 가능량 · 정산금 ·
     절감액 · 회수기간 · 권장안 전 → 후 · 2단계 DR 카드가 안 움직였는지 · 움직인 칸이 1-2 목록 안인지. 빨가면 고친 뒤
     그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「조합의 DR 감축 가능량은 그 조합에서 태양광 · ESS 를 반영한 부하로 잰
     값이다 · 2단계 DR 카드는 원 부하 그대로」 (2) 「DR 카드 캡션은 그 벌 값으로 참이다(단가 미입력 · 입력 두 경우)」 (3)
     「여지가 없는 역률 카드 개요는 거짓 앞머리가 없고 40자 하한에서 빠진다 · 다른 카드는 하한 그대로」 를 문다.
     산출물을 실제로 만든 글자와 값을 본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로
     먼저 보고 빨간 것을 본다. 재료 확인 글자가 고친 뒤 글자를 담아 되돌림이 얕아지지 않게 한다.
3-4  **덱 19벌 한 판** — S244 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 ~ 1-4 목록 안인지 · 금액 · 판정 칸 0 인지. 금액
     칸이 갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로 — DR 이 든 조합 칸이 갈리면 전 →
     후를 값으로 내고 · 회귀값 여덟이나 타당성 판정 칸이 갈리면 멈춘다(소요 칸은 두고 · 1-5 자리 밖이 갈려도 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-5 자리 밖 0줄을 값으로 · 폴더마다 · tools\ · CLAUDE.md 줄 수.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로. 1번 PC 면 아래
     명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S245 3절: 못 · 실물 · 회귀


4절 — 재기 (고치지 않는다 · 고르지 않는다)

4-1  **DR 정산금 기간 값** — 정산금을 셈하는 자리와 식 · S219 가 12개월 값만 둔 까닭(기록 글자 그대로) · 기간 값을 낼
     근거가 저장소에 있는지(발령 달 · 횟수 가정 · 기준 데이터 · 약관 조문) · PPT · Word 기간 합산효과 자리와 지금 글자.
     길마다 한 줄씩: 무엇을 건드리나 · 금액이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 근거가 저장소에 있나 / 현장에만
     있나 · 되돌리기 · 드는 판 수. **사람이 읽는 말로.**
4-2  **마-33 조합 역률 경고 재료** — 지금 경고가 서는 조건(식 · 문턱 글자 그대로) · 경고 글자 · 서는 벌 16 과 예시 · S202
     가 사람에게 넘긴 글자 그대로 · 244세션 절 4-4 의 길 표. 길마다 한 줄씩 위와 같은 꼴로. **사람이 읽는 말로 · 코드
     용어 없이.**
4-3  4절 값은 5-1 의 245세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 245세션 절 — 사람 결정(마-16 · 마-35 예외 · 앞서 정한 것은 가리키기만)과 웹 대화창 판단(결정
     1 ~ 3)을 갈라서 · 1 ~ 4절 값. 세션 목록표에 245 행(브리핑이 읽는 날짜 꼴).
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(마-16 · 나-26 · 나-27 · 마-35 가운데 닫힌 것). 미해결 건수 대조를
     이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리
     진행표 A · F · J 행 · 5절에 사람 결정 행(마-35 예외 · 마-16 이 없으면 그것도)과 웹 대화창 판단 행(결정 1 ~ 3) · 머리
     사슬에 S245 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(DR 정산금 기간 값 · 마-33 사람 결정
     대기 · 문구 판(나-18 · 마-34 · 후보 H) · 묶음 C · D · E · G · I · 이 판이 고치지 않고 값으로 낸 자리).
5-5  CLAUDE.md 는 1-4 가 찾은 40자 하한 자리의 예외 한 줄 말고는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S245.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S245 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-5 자리 몫과 그 밖 0줄) · tools\ · CLAUDE.md 줄 수.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 확인 사례에서 값이 움직인 칸 전 → 후 · 권장안 · 회귀값 여덟 ·
    총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를 따로. 다시 돈
    판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ J 포함).
  그 뒤에 「조합 부하 DR 표」 — 확인 사례 · 넣은 단가 · 조합마다 DR 감축 가능량 · 정산금 · 절감액 · 회수기간 · 권장안
    전 → 후 · 2단계 DR 카드 불변 확인.
  그 뒤에 「글자 고침 표」 — 나-26 · 나-27 전 → 후 · 서는 벌 수 · 40자 하한 예외를 단 자리.
  그 뒤에 「DR 정산금 기간 값」(4-1) · 「마-33 조합 역률 경고」(4-2) — 지금 모습 · 걸린 결정 · 길 표. 사람이 읽는
    말로 · 코드 자리는 꼬리표로만 · 고르지 않는다.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
