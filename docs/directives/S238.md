S238 — 고침 판 · 뿌리 「ㄴ 정의가 다른 두 수」 를 사람 결정으로 닫고 · 「ㄹ 0원 거르기」 남은 몫 둘을 닫는다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 자릿수는 닫혔고 · 「ㄹ 0원
거르기」 는 S237 이 고쳤다. 사람이 「ㄴ 정의가 다른 두 수」 를 정했다. 이 판은 **ㄴ 을 닫고 · ㄹ 이
남긴 몫 둘(조합 투자비 셈 · Word 조합 투자비 칸 사유)을 닫는다.** 조합마다 되풀이되는 계약전력
문장은 「ㅁ 겹침 거르기」 몫이라 건드리지 않는다. 이 밖으로 가지 않는다 — 다른 뿌리 · 도구 · 측정은
이 판 몫이 아니다.

**사람이 정했다.**
  결정 1 (S237 뒤 · 이 판이 HANDOVER 5절에 한 행으로 앉힌다) — 「운영시간 외 부하」 는 어디서나
    비중(운영시간 밖 사용량 ÷ 전체 사용량)이다. Excel 한 칸을 비중으로 바꾸고 화면 · PPT · Word 는
    그대로 둔다. 235세션 절 5-5 선택지 가운데 「다」 다 — 선택지는 가리키기만 한다.
  앞서 정한 것(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 가리키기만) — 절사 A · 단위 기준 ·
    측정 중단 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지 않는다 · S154 ·
    S207.

**웹 대화창이 정했다(S237 뒤 · 이 판이 HANDOVER 5절에 두 행으로 앉힌다).**
  결정 2 — 여지가 없는 수단은 투자 대상이 아니다(S154 「개선할 것이 없다」). 조합 투자비 · 회수기간
    셈은 여지가 없는 수단의 투자비를 요구하지 않는다 — 그 수단의 투자비 미입력이 조합을 「미산출」 로
    만들지 않는다. 여지가 있는 역률의 투자비 미입력은 S237 그대로(조합도 「미산출」).
  결정 3 — 미산출 사유는 실제로 빠진 입력을 말한다(S207). Word 조합 표 투자비 칸이 태양광 단가를 비운
    조합에 계약전력 사유를 적는 것을 고친다. 글자는 같은 조합에서 다른 산출물이 적는 사유를 따른다
    (S233 「표기 값은 사실마다 한 자리에서」). 이 칸은 역률 투자비를 넣은 경로에서도 글자가 바뀐다 —
    거짓 사유가 참 사유로 바뀌는 것만 허용한다.

**S235 · S237 보고가 적었다 — 실물에서 먼저 확인하라.** 「운영시간 외 부하」 가 화면 · PPT · Word 는
비중 · Excel 은 비율(운영시간 밖 평균 수요 ÷ 운영시간 안 평균 수요) · 19벌 차 +10.7 ~ −18.2%p · 요금
계산에 안 닿는다 / 조합에 역률 투자비가 없으면 조합 투자비도 「미산출」 · 19벌 조합 투자비 · 회수기간이
「미산출」 로 번졌다 · 조합 여지는 태양광 뒤 역률로 판정 / Word 조합 칸은 태양광 단가 미입력 조합에
「하한 규정 미확인」 을 적는다 / 좁은 판 기온 두 건 빨강은 옛 판 코드로도 같은 글(미해결 「라」).

**울타리에 하한이 있다** — 1-5 가 센 「고칠 자리 수」. 결정 1 ~ 3 을 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한
    자리도 안 움직인다.** 움직이면 멈춘다.
  · **「운영시간 외 부하」 값을 문턱으로 쓰는 판정(무엇이 서고 안 서나)이 있으면 그 자리는 고치지 않고**
    값을 바꿨을 때 갈리는 벌과 글자를 값으로 낸다.
  · **결정 2 로 값이 서는 조합 투자비 · 회수기간 칸은 S235 뒤 스냅 값과 같아야 한다** — 「미산출」 로
    남으면 사유가 역률 밖 입력이어야 한다. 아니면 멈춘다.
  · 역률 투자비를 넣은 경로의 산출물 글자는 결정 3 자리 말고는 안 바뀐다. 바뀌면 멈춘다.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-5 가 센 자리 밖 0줄.**
    요금 · 절감액을 만드는 식은 0줄.
  · 이 판이 부르는 곳을 0 으로 만든 코드는 지운다(그것만 무는 시험도). 다른 곳이 부르면 그대로 둔다.
  · 조합마다 되풀이되는 계약전력 문장(ㅁ 몫)은 0줄.
  · 화면 문구(글자)는 늘리지 않는다 — 사유 글자는 이미 있는 글자를 쓴다. CLAUDE.md 「화면 문구」 절이
    걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · git log)에서 찾는다 — 없으면 「기록
    없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 절이 끝날 때마다 시계를 보고 적는다. 커밋 메시지는 편집 도구로 지은
    .txt 로 git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · **지시서에 없는 pytest 플래그를 붙이지 않고 · 있는 플래그를 바꾸지 않는다 — 좁은 판도 아래 꼴
    그대로다(S237 이 --tb 를 바꾼 판을 한 번 띄웠다).**
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. 도구로
    되는 자리에 파이프를 쓰지 않는다. 조사 에이전트를 띄우면 이 금지를 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S237 보고는 e2451dd) · 워크트리.
0-2  미해결 건수(145) · 회귀값 여덟 · 덱 스냅 기준선 둘 — S237 뒤 스냅(없으면 이 절에서 HEAD 로
     뜬다) · S235 뒤 스냅(결정 2 대조용).

커밋: S238 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — 235세션 절 5-5 · 요구사항서 6.1 · S154 · S207 · S233 을 글자 그대로 · 결정 주인. 결정
     1 ~ 3 과 어긋나는 글자가 있는지.
1-2  **운영시간 외 부하** — 이 사실을 만드는 자리와 적는 자리를 전수로(tools\count_sites.py · 그물이
     못 보는 자리는 따로). 산출물마다 비중 / 비율 어느 값을 쓰나 · 비율을 부르는 곳 전부(판정 문턱 ·
     다른 칸 · 그림 · 문장 · 케이스 스터디 칸 · 회귀값) · 19벌 Excel 값 전 → 후(예정).
1-3  **조합 투자비(결정 2)** — 여지가 없는 수단의 투자비 미입력 때문에 조합 투자비 · 회수기간이
     「미산출」 인 벌과 조합 · 그 사실이 서는 경로를 전수로(화면 · PPT · Excel · Word · 일괄 생성 ·
     그물 밖 자리) · 경로마다 지금 글자 · 고친 뒤 글자(예정) · S235 뒤 스냅 값.
1-4  **Word 조합 투자비 칸 사유(결정 3)** — 이 칸이 사유를 고르는 갈래 전부 · 갈래마다 서는 벌 · 같은
     조합에서 다른 산출물이 적는 사유 글자.
1-5  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S238 1절: 고칠 자리


2절 — 고친다

2-1  **1-5 가 센 자리를 고친다.** 결정 1 ~ 3 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S238 2절: 두 수 · 조합 투자비 · 사유


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면
     시험과 한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지
     않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 결정 1 ~ 3 이 다 서는 벌을 가장 적게 골라(고른 벌과 까닭) 뜬다. **산출물
     넷(PPT · Excel · Word · 일괄)을 통째로 S237 뒤 스냅과 맞대** 갈린 줄이 전부 1-5 자리인지 본다 —
     목표 칸만 맞대지 않는다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓혀 (1) 「운영시간 외 부하는 네 산출물이 같은 값」 (2) 「여지가 없는 수단의
     투자비 미입력이 조합 투자비를 「미산출」 로 만들지 않는다」 (3) 「Word 조합 투자비 칸 사유는
     빠진 입력을 말한다」 를 문다. 넓혀서 안 닫히면 새 못. 산출물을 실제로 만든 글자를 본다. 되돌려
     확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S237 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-5 자리인지 · Excel 운영시간 외
     부하가 19벌 다 화면 값과 같은지 · 결정 2 칸이 S235 뒤 스냅 값과 같은지. 금액 칸이 갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-5
     자리 밖이 갈리면 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-5 자리 밖 0줄을 값으로 · 폴더마다.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S238 3절: 못 · 실물 · 회귀


4절 — 기록 · 닫기

4-1  PROCEED.md 에 238세션 절 — 사람 결정(결정 1 · 앞서 정한 것은 가리키기만)과 웹 대화창 판단(결정
     2 · 3)을 갈라서 · 1 ~ 3절 값.
4-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(운영시간 외 부하가 산출물마다 다른 값인 항목 ·
     닫힌 것이 더 있으면 이름으로). 미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
4-3  docs\HANDOVER.md — 4절 ㄴ · ㄹ 행을 이 판 값으로 · 5절에 사람 결정 한 행(결정 1)과 웹 대화창 판단
     두 행(결정 2 · 3) · 머리 사슬에 S238 단락 · 2절에는 pytest 건수 · 초록만.
4-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(겹침 거르기(조합마다
     되풀이되는 계약전력 문장 포함) · 개발 말과 그물 · 이 판이 고치지 않고 값으로 낸 자리).
4-5  CLAUDE.md 는 고치지 않는다.
4-6  이 지시서 본문을 docs\directives\S238.md 로 그대로 앉힌다.
4-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S238 4절: 기록 · 뿌리 진행표 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-5 자리 몫과 그 밖 0줄).
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를
    따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「두 수 표」 — 19벌 Excel 운영시간 외 부하 전 → 후 범위 · 화면 값과 같은 벌 수 · 비율을 부르던
    곳과 처리.
  그 뒤에 「조합 투자비 표」 — 결정 2 로 바뀐 벌 · 조합 · 전 → 후 · S235 뒤 스냅과 같은지.
  그 뒤에 「Word 사유 표」 — 갈래 · 전 → 후 · 서는 벌 수.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시계를 보고) · 백그라운드 셸 확인 결과.


---

S238 보정 — 3-8 을 이어서 돈다

· 다시 돌려도 된다. ② 산출물 갈래부터 PROCEED.md 「pytest 분할 실행」 명령 그대로 · 하나씩 차례로
  ② → ③ → ④(mypy 둘 포함). 플래그를 더하거나 바꾸지 않는다.
· ① 갈래와 3-1 은 다시 돌지 않는다 — ① 앞 두 판 빨강 뒤 고친 것은 PROCEED.md 기록뿐이다. ② ~ ④ 에서
  코드 탓 빨강이 나오면 지시서 3-8 대로 고친 뒤 3-1 부터 다시.
· 돌리기 전에 이 판이 띄운 python 이 남아 있으면 내린다. 남의 python 은 건드리지도 · 재지도 · 적지도
  않는다.
· ② 가 또 메모리 부족으로 끊기면 스스로 다시 돌리지 않고 플래그도 바꾸지 않는다. 거기서 멈춰 3절까지
  커밋하고(push 는 하지 않는다) · 끊긴 판 수와 끊긴 자리(수집 몇 건 가운데 몇 건째 · 그때까지 빨강
  수)를 보고한다.
· 끝까지 초록이면 지시서대로 3절 커밋 → 4절 기록 · push → 5절 보고. 5절 5항에 끊긴 판을 판 수에 넣고
  까닭을 한 줄로 적는다. scratchpad\wt_before 는 4절에서 지운다.
