S251 — 고침 판 · 사람 결정 셋(상향 권고 PPT 한 줄 · 주의사항 · 조합 비교 「없음」) · 기록으로 서는 넷 · Word 감도 표를 고치고 · 사람 결정 열 항목 재료와 문구 판 목록을 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — ㄱ ~ ㅂ 과 후보 B · C · E · G · N 은 닫혔고 · K · M 은 앞 항목을
고쳤다. S250 4-2 가 홀로 남은 25 항목을 갈랐다(기록으로 선다 4 · 사람 결정 10 · 문구 판 8 · 자료 대기 1 · 도구 안쪽 1). 이 판은
**사람이 정한 셋과 기록으로 서는 넷과 Word 감도 표를 고치고 · 사람 결정 열 항목의 재료와 문구 판 목록을 재기만 한다.** 계산값은
안 바꾼다. 이 밖으로 가지 않는다. 항목 번호는 S250 판 시작 때 번호다 — 이름은 250세션 절 4절에서 찾아 이름으로 대조한다.

**사람이 정했다(S250 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  상향 권고(마-17) — 계약전력을 넘겨 쓰는 건물의 계약전력 상향 권고를 PPT 에 한 줄 남긴다. 글자와 값은 화면 · Word 가 이미 쓰는
    것과 같은 한 자리에서 받는다(S233) — 새 글자를 짓지 않는다. 자리는 PPT 에서 계약전력 조정을 말하는 장이다.
  수단별 주의사항(135세션 「정책이라 사람이 정한다」 의 답) — 주의사항마다 「빼도 PPT 전체 맥락을 이해하고 오해하지 않는가」 로
    가른다. 오해하지 않으면 싣지 않는다. 중대한 것만 있는 글자를 줄여 PPT 마지막 장 하나에 둔다(새 사실을 더하지 않는다). 고칠 것이
    많거나 다른 장과 부딪치면 그 장은 안 나오게 하고 · 무엇을 왜 뺐는지 값으로 낸다. Word 받기 단추는 되살리지 않는다.
  조합 비교 「없음」(마-21) — Excel 조합 비교에서 여지가 없는 칸만 「없음」 글자로 적는다(다른 산출물과 같은 글자 · S237 결정 ㄴ).
    그 밖의 칸은 숫자 그대로. S161 행에는 곁 줄을 단다 — 옛 글자는 고치지 않는다.
  앞서 정한 것(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만) — 절사 A · 단위 기준 · 측정 중단 ·
    도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지 않는다 · S143 · S154 · S182 ·
    S198 · S205 · S206 · S207 · S220 · 운영시간 외 부하는 비중 · 고객 산출물 말투는 「~습니다」 · 마-16 · 마-35 · 기간 합산효과에
    기간 DR 정산금 · 마-33 · 나-17(S247) · 마-24(S247).

**웹 대화창이 정했다(S250 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (기록으로 서는 넷 — 나-3 · 나-4 · 나-14 · 나-18) — 250세션 절 4-2 가 댄 기록된 결정 그대로 고친다. 그 절에 기준 글자가
    없으면 항목 몸과 그 항목을 올린 판의 기록에서 찾는다(S250 이 한 방식). 기준 글자를 못 찾거나 고치면 계산값이 움직이는 자리는
    고치지 않고 값을 낸다.
  결정 2 (Word 감도 표) — Word 감도 표는 Excel 감도 시트와 같은 한 자리의 표기 값을 쓴다(S233 · S250 결정 1).
  결정 3 (운영) — 되돌려 확인할 때 plugin 을 싣는 꼴을 지시서 꼴로 둔다(아래 3-3 꼴).

**S250 보고가 적었다 — 실물에서 먼저 확인하라.** 상향 권고 — 화면과 Word 에는 「5,294 kW 로 올리면 1억 2,245만원 줄어듭니다」 가
서고 PPT 에는 0줄 · Excel 요약에는 조합 줄로 목표와 몫이 선다(2벌 9줄) / 수단별 주의사항 표가 어느 산출물에도 안 선다 / Excel 조합
비교 23줄 가운데 「계약전력 조정」 4줄은 뜻이 「없음」 / Word 감도 표는 Excel 감도 시트와 같은 사실을 다른 글자로 적는다 / 기록으로
서는 넷에는 같은 자리를 나누는 묶음이 없다.

**울타리에 하한이 있다** — 1-6 이 센 「고칠 자리 수」. 사람 결정과 결정 1 · 2 를 끝까지 적용하는 데 필요한 번짐은 울타리 안이다 —
자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **계산값은 한 자리도 안 바뀐다 — 회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 · 케이스 스터디 타당성.** 움직이면
    멈춘다. 19벌에서 갈려도 되는 것은 1-2 ~ 1-5 목록의 글자와 · 상향 권고 한 줄 · 주의사항 장(서면 하나)뿐이다 — 목록 밖이 갈리면
    멈춘다.
  · **PPT 장 수가 바뀌는 것은 주의사항 장 하나(서면)뿐이다.** 그 밖의 장이 늘거나 줄거나 번호가 밀리면(주의사항 장이 마지막이라
    밀릴 것이 없다) 멈춘다.
  · 주의사항 장은 있는 글자만 쓴다 — 새 사실 · 새 권고를 더하지 않는다.
  · 사람 결정 열 항목 · 문구 판 몫 · 고압 계약형 부가금은 코드 0줄.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-6 이 센 자리 밖 0줄.**
  · **4절은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄 · tools\ 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다 — 없으면 「기록 없음」. 이미
    기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집 도구로 지은 .txt 로
    git commit -F.** 절마다 따로 커밋한다 — 다른 절 몫을 앞 절 커밋에 싣지 않는다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다 — 3-1 꼴 · 3-3 되돌림 꼴 · 3-8 꼴(② 꼴 포함) · 아래 디버그 꼴 · 5-7 꼴. 그 밖에 `-n` ·
    `--dist` · `--tb` · `-s` 를 붙이지 않는다.** python 은 늘 `.venv\Scripts\python.exe` 로 부른다(S250 이 맨 python 으로 시스템
    python 을 한 번 띄웠다). 빨간 시험 하나의 까닭을 볼 때만 아래 디버그 꼴을 쓴다 — 판 수에 넣되 초록 판정에 쓰지 않는다. node id 는
    수집 목록에서 글자 그대로 복사해 옮긴다.

         .venv\Scripts\python.exe -m pytest <빨간 시험 하나의 node id> -rf --tb=short

  · **메모리 부족으로 끊기면** — 덱 19벌은 곧바로 다시 돌지 않고, 이미 스냅이 선 벌은 두고 남은 벌만 같은 도구 · 같은 옵션으로 나눠
    뜬다. ③ · ④ 가 끊기면 그 갈래도 3-8 의 ② 꼴로 한 파일씩 돈다. ② 꼴 한 파일 판도 끊기면 멈춘다. 끊긴 판은 판 수에 넣고 까닭 한
    줄. 멈추면 3절까지 커밋하고(push 하지 않는다) 끊긴 자리를 보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File · sed 로 파일 고치기 · 셸 리다이렉트(> · >>)를 쓰지 않는다
    — 파일 수정은 편집 도구로만 한다. **파이프를 쓰지 않는다 — `| Out-Null` · `| ForEach-Object` · `| Select-Object` 도 파이프다.**
    명령 출력이 길면 그대로 받아 읽고 · 파일은 찾기 도구나 읽기 도구로 본다. 조사 에이전트를 띄우면 이 금지를 글자 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S250 보고는 488ed2b) · 워크트리.
0-2  미해결 건수(113) · 회귀값 여덟 · 덱 스냅 기준선(S250 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다) · 케이스 스터디 기준선(S250 뒤 값).

커밋: S251 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — S161 · S233 · S237 결정 ㄴ · 135 · 143 · 144세션의 주의사항 · 상향 권고 판정 · 250세션 절 4-1 · 4-2 를 글자 그대로 ·
     결정 주인. 이 판의 결정과 어긋나는 글자가 있는지.
1-2  **상향 권고(마-17)** — 화면 · Word 가 쓰는 글자와 그 글자를 만드는 한 자리 · PPT 에서 계약전력 조정을 말하는 장 · 서는 벌 ·
     넣은 뒤 모습(예정).
1-3  **수단별 주의사항** — 주의사항 전수(수단마다 글자 그대로) · 항목마다 「빼면 PPT 전체 맥락을 오해할 수 있는가」 판정과 까닭 한
     줄 · 중대로 가른 것 · 줄인 글자(있는 글자에서만 · 예정) · PPT 마지막 장 자리 · 다른 장과 부딪치는 자리 · 장을 세울지 / 안
     세울지(예정)와 까닭.
1-4  **조합 비교 「없음」(마-21)** — Excel 조합 비교에서 여지가 없는 칸 전수(수단 · 벌) · 여지 판정을 읽는 자리 · 다른 산출물이 같은
     칸을 적는 글자 · 고친 뒤 모습(예정).
1-5  **기록으로 서는 넷 · Word 감도 표(결정 1 · 2)** — 항목마다 몸 · 기준 글자와 출처 · 지금 글자 · 서는 벌과 자리 · 고친 뒤
     글자(예정) · 계산값에 닿는지. Word 감도 표의 칸마다 지금 글자와 Excel 감도 시트 표기.
1-6  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S251 1절: 고칠 자리


2절 — 고친다

2-1  **1-6 이 센 자리를 고친다.** 사람 결정과 결정 1 · 2 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로. 주의사항 장을 안 세웠으면 그 까닭을 값으로.

커밋: S251 2절: 상향 권고 · 주의사항 · 조합 비교 없음 · 기록으로 서는 넷 · Word 감도 표


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을 적는다). 기록을 무는 시험 파일은
     좁은 판에 넣지 않는다 — 5-7 이 본다. 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면 시험과
     한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지 않고 3-2 로 간다 — 3-8 이 본다. 그 밖이
     빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 상향 권고가 서는 벌 · 주의사항 장이 서는 벌 · 조합 비교 「없음」 이 서는 벌 · 기록으로 서는 넷이 서는 벌을
     가장 적게 골라(고른 벌과 까닭) 뜬다. 산출물 넷(화면 · PPT · Excel · Word)을 통째로 S250 뒤 스냅과 맞대 갈린 줄이 전부 1-2 ~ 1-5
     목록 안인지 · 계산값이 같은지 · PPT 장 수가 주의사항 장 하나 말고 같은지. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못 — 있는 못을 넓히거나 새 못으로 (1) 「상향 권고는 화면 · Word · PPT 가 같은 글자」 (2) 「주의사항 장은 중대로 가른 것만 · 마지막
     장 하나 · 있는 글자만」 (장을 안 세웠으면 「주의사항 장이 없다」) (3) 「Excel 조합 비교의 여지 없는 칸은 「없음」」 (4) 기록으로 서는
     넷을 각각 기준 글자로 (5) 「Word 감도 표는 Excel 감도 시트와 같은 표기」 를 문다. 산출물을 실제로 만든 글자와 값을 본다. 되돌려
     확인한다 — 아래 꼴로 · 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다. 재료 확인 글자가 고친 뒤 글자를 담아 되돌림이 얕아지지
     않게 한다.

         .venv\Scripts\python.exe -m pytest <시험 파일들> -p <되돌림 plugin> -rf --tb=no

3-4  **덱 19벌 한 판** — S250 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 ~ 1-5 목록 안인지 · 계산값이 같은지. 계산값이 갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 0-2 기준선과 맞댄다. 갈린 칸은 이름으로(소요 칸은 두고 · 그 밖의 칸 · 회귀값 여덟 · 타당성 판정이 갈리면
     멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-6 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 대로 하나씩 차례로 — ② 는 처음부터 그 갈래의 시험 파일을 한
     파일씩 아래 꼴로(작업자 2).

         .venv\Scripts\python.exe -m pytest <② 의 시험 파일 하나> -n 2 --dist load -rf --tb=no

     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S251 3절: 못 · 실물 · 회귀


4절 — 재기 (고치지 않는다 · 고르지 않는다 · 읽기만)

4-1  **사람 결정 열 항목 재료** — 가-2 · 가-3 · 나-1 · 나-17 · 마-8 · 마-10 · 마-14 · 마-20 · 마-22 · 마-24(S250 판 시작 번호 · 이름으로
     대조) 마다: 지금 모습(예시 글자 · 서는 벌과 산출물) · 걸린 결정 글자 그대로와 주인 · 「사람이 정한다」 가 선 세션과 사람이 답한
     기록 · 길마다: 바뀌는 글자(예시) · 금액이 갈리나 · 고객 보고서에서 무엇이 달라지나 · 근거가 저장소에 있나 / 현장에만 있나 ·
     되돌리기 · 드는 판 수. 재료를 모으다 기록된 결정으로 잣대가 선다고 드러난 항목은 「기록으로 선다」 로 옮겨 적는다(그 결정 · 주인).
4-2  **문구 판 목록** — 문구 판 몫 전부(후보 A · H · J 의 남은 몫 · K 의 나-7 · 마-17 밖 몫 · 마-15 · 홀로 남은 문구 판 여덟 등)를 사람이
     화면을 열어 한 판에 잡을 수 있게 적는다: 항목 이름 · 화면 어디(단계 · 카드 · 칸)와 산출물 어디 · 지금 글자 그대로 · 무엇이 걸리나
     (잘림 · 어색함 · 두 글자 · 거짓 여지 등) 한 줄 · 걸린 결정. 화면 순서대로.
4-3  두 재료는 **사람이 읽는 말로 · 코드 용어 없이** 적는다. 코드 자리는 꼬리표로만. 고르지 않는다.
4-4  4절 값은 5-1 의 251세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다). 문구 판 목록은 docs\ 아래 한 파일로도 앉힌다(이름은 저장소
     꼴을 따른다) — 사람이 그 파일을 열어 쓴다.


5절 — 기록 · 닫기

5-1  PROCEED.md 에 251세션 절 — 사람 결정(상향 권고 · 주의사항 · 조합 비교 「없음」 · 앞서 정한 것은 가리키기만)과 웹 대화창 판단(결정
     1 ~ 3)을 갈라서 · 1 ~ 4절 값. 세션 목록표에 251 행(브리핑이 읽는 날짜 꼴 · 「늘어난 것」 낱말 포함).
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(마-17 몫 · 주의사항 항목 · 마-21 · 기록으로 서는 넷 · Word 감도 표 가운데 닫힌 것).
     미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 · 4절 뿌리 진행표 K · L · M 행 ·
     5절에 사람 결정 행(셋 · S161 곁 줄)과 웹 대화창 판단 행(결정 1 ~ 3) · 머리 사슬에 S251 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(사람 결정 열 항목 가운데 남은 것 · 4-1 에서 기록으로
     선다고 옮긴 것 · 문구 판(4-2 목록 파일을 가리킨다) · 고압 계약형 부가금(단가 자료 대기) · 이 판이 고치지 않고 값으로 낸 자리). 상한
     3,000자.
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S251.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S251 5절: 기록 · 뿌리 진행표 · 문구 판 목록 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-6 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · PPT 장 수 전 → 후 · 계산값 불변 확인 · 케이스 스터디 · 회귀값 여덟 · 총액 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 되돌림 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수(② 는 파일 판 수) ·
    디버그 꼴 판 수를 따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로(후보 A ~ N 포함).
  그 뒤에 「고침 표」 — 상향 권고 · 조합 비교 「없음」 · 기록으로 서는 넷 · Word 감도 표마다 전 → 후 · 서는 벌 수 · 고치지 않은 자리와 까닭.
  그 뒤에 「주의사항 표」 — 주의사항마다 수단 · 글자(줄여 쓴 것) · 중대 / 아님 · 까닭 한 줄 · 장을 세웠나 / 안 세웠나와 까닭.
  그 뒤에 「사람이 정할 것 — 재료」(4-1) — 항목마다 지금 모습 · 걸린 결정 · 길 표 · 기록으로 선다고 옮긴 것. 사람이 읽는 말로 · 코드
    자리는 꼬리표로만 · 고르지 않는다.
  그 뒤에 「문구 판 목록」(4-2) — 항목 수와 앉힌 파일 이름.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.
