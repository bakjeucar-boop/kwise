S133 지시서 — 산식 한 자리 (묶음 ①)

전제. 클로드코드 CLI 를 /clear 하고 이 지시서만 받는다. 아래 넷을 먼저 읽는다 —
CLAUDE.md · docs\project\collaboration.md · docs\project\project-overview.md ·
docs\REVIEW_S130.md. 브리핑은 직접 돌린다.

    .venv\Scripts\python.exe tools\daily_brief.py

이 판이 무엇인가. 리뷰 6절 23번의 묶음 ① 「산식 한 자리」 다. 기본요금 비중을
만드는 산식이 다섯 자리에 세 꼴로 흩어져 있다(리뷰 1절 5번). 한 속성으로 모으고
grep 못을 박는다. S132 가 역률 85% 벌(large-b-pf85)을 세워 두어 모으기 전후를
값으로 맞댈 수 있다 — 그 벌이 없을 때는 다섯이 같은 값이라 볼 길이 없었다.

앞에 두 절이 붙는다. 앞 판이 값으로 드러낸 것 가운데 이름이 실물과 어긋난 것과
이름이 아예 없는 것을 세우고(1절), 처음으로 값을 낸 갈래를 검산한다(3절).

금액은 건드리지 마라. 이 판이 모으는 것은 「비중」 하나다. 「기본요금」 이라는
금액이 역률을 접느냐는 사람이 정할 자리이고, Excel 요약 시트의 금액도 그쪽이다.

중간에 묻지 않는다. 되돌리기 어려운 것이 걸리면 아래 정한 안전한 쪽으로 가고
값을 보고에 낸다. 기대값은 갱신하지 않는다 · 조문 해석은 좁은 쪽 · 범위 밖은
안 고치고 이름만 올린다.

수의 출처를 낱말 하나로 붙인다 — 「저장소」·「스크래치」·「문서에 적힌 값」·
「앞 세션 보고」.

파일 수정은 편집 도구로만 한다. 스크래치도 예외가 아니다.

지시서를 저장소에 남긴다 — docs\directives\S133.md. 그물에는 넣지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

0절 — 자리 확인 (커밋 없음)

1. git pull. HEAD 를 적는다. 앞 세션 보고는 c26b95d 라 적었다.

2. 어느 PC 인가. CPU·코어·RAM. 앞 판은 2번 PC 였고 갈래 넷으로 돌았다.

3. 다른 프로젝트의 python 이 도는지 본다. 죽이지 말고 적는다.

       Get-CimInstance Win32_Process -Filter "Name='python.exe'"

4. data\source\SOURCES.md 13줄 삭제는 정상이다. 손대지 마라.

5. 이 지시서가 옮겨 적는 수를 실물과 맞댄다.

   | 무엇 | 지시서가 적은 값 | 시점 | 출처 |
   |---|---|---|---|
   | 수집 건수 | 1,677 | S132 마감 | 앞 세션 보고 |
   | 미해결 | 64건 (① 7 · ② 56 · ③ 1) | S132 마감 | 앞 세션 보고 |
   | 살아 있는 못 | 1건 — tests\test_excess.py::test_월별_요금_구성_막대의_조각_합이_합계와_맞는다 | S132 마감 | 앞 세션 보고 |
   | 덱 벌 | 열둘 | S132 | 앞 세션 보고 |
   | 비중 산식 | 5자리 · 3꼴 | S130 리뷰 1절 5번 | 문서에 적힌 값 |

   어긋나면 실물을 따르고 두 값을 6항에 적는다. 비중 산식 다섯 자리는 2절
   1번에서 전수로 다시 뽑으므로 여기서는 수만 맞댄다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1절 — 이름을 실물에 맞추고, 이름 없는 것을 세운다 (기록만 · 코드 무변)

S131 이 막대 항목에서 겪은 것과 같은 모양이 하나 더 있다. 2절이 코드를 모으기
전에 이름을 실물에 맞춘다 — 이름이 참조의 정본이라 틀린 이름으로 모으면
모은 뒤에도 다음 판이 옛 사실을 읽는다.

1. 미해결 「「기본요금」 이 산출물마다 다른 몫을 센다 — Excel 만 역률을 안
   접는다」 의 이름이 실물과 어긋난다. S132 3절이 값으로 본 것 — 같은 Excel
   안에서 요약 시트는 금액에서 역률을 빼고 진단 시트는 비중에서 담는다
   (앞 세션 보고). 「Excel 만」 이 아니라 「Excel 안에서도 시트마다」 다.

   먼저 저장소에서 값으로 확인한다 — 앞 세션 보고는 이차 자료다. Excel 요약
   시트와 진단 시트가 각각 어느 속성을 읽는지 소스에서 읽고, 어긋남이 실물인지
   본다. 실물이면 이름을 고친다. 이름을 부르는 자리를 전수로 뽑아 함께 고친다
   — 미해결 칸 · 「다음 작업」 칸 · 다른 항목 본문 · 시험 주석. 사진(리뷰 본문 ·
   지난 세션 절)은 고치지 마라.

2. PPT 7장 각주가 그 벌에서 거짓이 되는데 미해결에 이름이 없다. S132 3절 —
   각주가 「기본요금 = 요금적용전력 × 단가 × 개월수」 라 적는데 그 식의 값은
   452,804,556원이고 같은 장의 큰 글자는 459,143,820원이다(앞 세션 보고).
   역률요금 6,339,264원이 큰 글자에는 실리고 각주 식에는 안 실린다.

   large-b-pf85 로 PPT 를 뽑아 두 수를 값으로 확인한다. 실물이면 미해결에
   이름으로 세운다 — 결함 유형 ①(화면이 사실과 다른 말을 한다)이다.
   고치지 마라. 각주 문구를 고치는 것은 화면·산출물 문구 판의 몫이다.

3. 부가금에 같은 모양이 있는지 함께 본다. 109세션이 「부가금을 기본요금에
   접지 마라」 로 정했는데 역률요금은 접고 있다(앞 세션 보고). 그 각주가
   부가금이 서는 벌(large-b-short)에서도 거짓이 되는지 값으로 본다.
   거짓이면 위 항목 본문에 그 사실을 한 줄로 적는다 — 새 항목을 세우지 마라.

커밋 — S133 1절: ②-51 이름 · PPT 각주 등재

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2절 — 비중 산식을 한 자리로 모은다 (묶음 ① · 리뷰 6절 ㄱ A)

리뷰 1절 5번이 센 다섯 자리 — 화면 ui\views\diagnose.py:899(직접 나눗셈) ·
PPT report\slides.py:1393(같은 나눗셈을 또 적음) · Excel report\excel.py:688
(base_with_power_factor_share 속성) · Word report\document.py:1540(같은 속성) ·
케이스 스터디 report\casestudy.py:214(total_base_won / total_won — 역률을 안
접는다). 산식 세 꼴이고 못이 없다. S124 못은 Excel·Word 만 문다.

1. 다섯 자리를 저장소에서 전수로 다시 뽑는다. 리뷰의 줄 번호는 문서에 적힌
   값이라 밀렸을 수 있다. 자리마다 산식을 그대로 적고, 다섯 밖에 여섯째가
   있는지 함께 본다 — 리뷰가 못 센 자리가 있으면 그것이 발견이다.

2. 한 속성으로 모은다. base_with_power_factor_share 가 이미 서 있으므로
   새 속성을 만들지 말고 그것을 읽게 한다. 화면 · PPT · 케이스 스터디 세 줄이
   바뀐다. Excel · Word 는 이미 그 속성이라 안 건드린다.

   속성 이름이 실물과 다르면 실물을 따르고 6항에 적는다.

3. 값이 어떻게 움직이는지 미리 안다. 역률요금이 0 인 벌에서는 다섯이 같은
   값이라 화면·PPT 는 안 움직이고, 케이스 스터디 한 줄만 분자에 역률이 붙는다
   — 그런데 케이스 여덟이 전부 간주 92% 라 역률요금 0 이므로 그 줄도 값이
   안 움직여야 한다. 회귀값 비중 13.5%(C1) · 20.0%(R1) 이 그것이다.

   움직이면 그것이 발견이다. 기대값을 갱신하지 마라 — 움직인 값과 자리를
   6항에 적고, 원인을 값으로 짚는다.

4. large-b-pf85 로 모으기 전후를 맞댄다. 이 벌이 이 절의 눈이다.
   모으기 전 화면·PPT 비중 13.7% 와 모은 뒤 값이 같아야 한다 — 자릿수까지
   같은지 본다(앞 판이 13.4866% ↔ 13.6755% 로 쟀다). 인라인 나눗셈과 속성이
   반올림 자리가 다르면 화면 숫자가 조용히 갈린다.

5. 못을 하나 박는다. 소스에서 비중을 만드는 나눗셈이 그 속성이 사는 파일
   밖에 서면 빨개진다 — S119 「자리 하나」 못과 같은 꼴이다. 못이 무엇을
   그물로 쓰는지(문자열인지 정규식인지) 적고, 되돌려 확인한다 — 다섯 자리
   가운데 하나에 나눗셈을 도로 심어 빨개지는지 보고 되돌린다.

   못 이름은 고유어를 피한다. S132 가 「열하나」 라는 고유어 때문에 덱 벌 수를
   무는 못이 없다는 것을 봤다 — 그물에 안 걸리는 이름을 짓지 마라.

6. 케이스 스터디 산식이 바뀌므로 그 항목을 닫을 수 있다 — 「케이스 스터디
   산출물이 기본요금 비중을 역률 없이 센다」. 값이 안 움직인 것을 확인한 뒤에
   닫는다. 「기본요금 비중을 화면·PPT 는 각자 나눗셈으로 적는다」 도 같다.

7. 금액은 건드리지 마라. Excel 요약 시트의 「기본요금」 금액(total_base_won)은
   이 절 밖이다. 비중만 모은다.

커밋 — S133 2절: 비중 산식 한 자리 · grep 못

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3절 — 역률 개선 카드 값을 검산한다 (재고 멈춘다)

S132 덤 — 역률 개선 수단이 처음으로 값을 냈다. 카드 절감액 10,867,000원 ·
덱 12장 · 부록 「역률 개선」 장(앞 세션 보고). 앞서 벌 스물셋이 다 간주 92% 라
그 장이 늘 0원이었다. 결함 유형 ② 의 자리다 — 만든 갈래가 이제야 실물에
섰고 아직 아무도 값을 안 봤다.

이 절은 검산만 한다. 틀렸어도 이 판에서 고치지 마라.

1. 약관 산식을 원문에서 읽는다. data\source\ 의 약관 정본(합본 2026-06-01)
   에서 역률 조항을 열어 기준 역률 · 가감 폭 · 상하한 · 곱하는 밑이 무엇인지
   조문 번호와 함께 적는다. 지상과 진상을 갈라 적는다.

   기억으로 적지 마라. 이 지시서는 앞 판에서 기준을 90% 로 잘못 적었다 —
   실물은 92% 이고 하한은 60% 다. 원문을 연 결과를 쓴다.

2. 그 산식으로 손계산을 한다. large-b-pf85 에서 역률 85% 를 92% 로 올렸을 때
   역률요금이 얼마 주는지 원 단위로 낸다. 밑이 되는 기본요금이 어느 값인지
   (역률 포함인지 아닌지) 함께 적는다 — 이 판의 축이 거기다.

3. 카드가 낸 10,867,000원과 맞댄다. 어긋나면 어디서 갈리는지 값으로 짚는다.
   목표 역률을 몇 %로 잡았는지, 투자비·회수기간이 무엇을 근거로 서는지도
   함께 본다.

4. 판정까지가 이번 몫이다. 맞으면 「맞다」 를 값과 함께 적고, 틀리면 갈린
   값과 자리를 적어 미해결에 이름으로 세운다. 고치는 것은 다음 판이다.

5. 못을 박지 마라. 옳은 값이 무엇인지 이 절이 정하고 있는 중이다.

커밋 — S133 3절: 역률 개선 카드 검산

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4절 — 마감

1. 대조표. 위 절의 항목마다 한 줄. 결과는 수치나 판정. 칸은 30자 안쪽.

2. 지시서를 저장소에 앉힌다 — docs\directives\S133.md. 받은 본문 그대로,
   편집 도구로만. 앉힌 뒤 scan_ctrl 로 확인한다.

3. pytest. 0절 2번에서 정한 갈래로 돈다. 2번 PC 면 갈래 넷.

       .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

   갈래마다 건수·skip·xfailed 를 적고 합이 수집 건수와 맞는지 본다.
   xfailed 1 이 뜬다 — 안 뜨면 그것이 발견이다. 종료 코드로 판정하지 마라.

4. 회귀. 이 판은 src\ 를 고친다 — 케이스 스터디를 돌린다.

       .venv\Scripts\python.exe tools\run_casestudy.py

   회귀값 여덟이 안 움직여야 한다 — C1 관측 최대 5,293.44 kW · 요금적용전력
   5,293 kW · 부하율 49.0% · 기본요금 비중 13.5% / R1 132.28 kW · 132 kW ·
   41.1% · 20.0%. 판정 118/118. 소요는 PC 를 함께 적고, 앞뒤로 남의 python 이
   있었는지 적는다.

   화면 감사를 돌린다 — 넷(957 · 807 · 961 · 808)이 불변이어야 한다. 이 판은
   화면 문구를 안 고치므로 움직이면 그것이 발견이다.

   scan_ctrl 을 돌린다.

5. PROCEED.md 를 적고 절마다 커밋했는지 본다.

6. 미해결 건수 대조를 이름으로 적는다. 예상은 이렇다. 실물이 다르면 실물을 따른다.

   - 닫는다 — 「케이스 스터디 산출물이 기본요금 비중을 역률 없이 센다」 ·
     「기본요금 비중을 화면·PPT 는 각자 나눗셈으로 적는다」 (2절이 모았다)
   - 닫지 않는다 — 「「기본요금」 이 산출물마다 다른 몫을 센다」 (이름만 고쳤다 ·
     금액은 사람이 정할 자리) · 「월별 요금 구성 막대가 초과사용부가금을 안
     담는다」 · 「S124 가 적은 317,220원이 그 벌에서 안 나온다」
   - 늘 수 있다 — PPT 7장 각주 항목(1절 2번) · 3절에서 카드 값이 어긋나면
     그 이름 · 2절 1번에서 여섯째 자리가 나오면 그 이름

7. 살아 있는 못 칸. 1건 그대로일 것이다 — 값으로 확인한다.

8. push. 돌고 있는 셸이 없는지 확인한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

보고 — 마지막에 한 번만. 여덟 항목 밖의 서술을 덧붙이지 않는다.

1. 대조표 — 항목마다 한 줄. 결과는 수치나 판정
2. 만든/바꾼 파일 — 파일마다 +/− 줄 수. 안 건드린 것도 한 줄로
3. 수치가 움직인 곳 — 종전/지금. 없으면 「없음」
4. 판단한 것 — 스스로 고른 갈래와 근거
5. 돌린 것 / 안 돌린 것 — 안 돌린 것은 왜
6. 예상과 달랐던 것 — 없으면 「없음」. 아래 여섯은 반드시 이 항에 적는다
   - 1절 — Excel 두 시트가 실제로 갈렸나. 옛 이름을 부르던 자리가 몇이었나
   - 1절 — PPT 각주 두 수가 얼마였나. 부가금 벌에서도 거짓이었나
   - 2절 — 비중을 만드는 자리가 다섯이었나 여섯이었나
   - 2절 — 회귀값 비중 둘이 움직였나. large-b-pf85 에서 자릿수까지 같았나
   - 2절 — grep 못을 되돌려 확인했나. 무엇을 심어 빨개졌나
   - 3절 — 약관 원문의 기준 역률·가감 폭·상하한. 손계산과 카드 값의 차
7. 박은 못 · 되돌려 확인한 못 — 이름으로. 없으면 「없음」
8. 프로젝트 지식 재업로드 — CLAUDE.md · docs\project\collaboration.md ·
   docs\project\project-overview.md 셋 중 이 판에 고친 것. 없으면 「없음」

보고 뒤에 daily_brief.py 결과를 그대로 붙인다.

ctx 는 셋 — 세션 예산 종전 → 지금(차이) · 가장 크게 먹은 것 하나 ·
줄일 수 있었다고 보는 것 하나.

마지막 한 줄로 백그라운드 셸 확인 결과.
