S136 지시서 — 1번 PC 를 재고, 남은 자리 둘을 모은다

전제. 클로드코드 CLI 를 /clear 하고 이 지시서만 받는다. 아래 넷을 먼저 읽는다 —
CLAUDE.md · docs\project\collaboration.md · docs\project\project-overview.md ·
docs\REVIEW_S130.md. 브리핑은 직접 돌린다.

    .venv\Scripts\python.exe tools\daily_brief.py

이 판이 무엇인가. 리뷰 6절 23번 표의 판 묶음은 S135 로 다 돌았다. 다음부터는
미해결에서 고른다. 이 판은 둘이다 — 1번 PC 에서만 잴 수 있는 셋을 재는 것과,
「같은 사실을 만드는 자리」 가운데 아직 안 모은 둘(MWh · 금액 한 칸)을 모으는 것.

PC 가 바뀐다. 앞 다섯 판이 2번 PC 였고 이 판은 1번 PC 다. 그 PC 는 pytest 를
갈래로 안 나눈다 — 앞단 전체가 상한 안쪽이다(CLAUDE.md 9항). 표에 「모름」 으로
비어 있는 칸이 셋 있고 그것이 1절이다.

재는 것과 고치는 것을 섞지 마라. 1절은 재기만 하고 2·3절이 고친다.

중간에 묻지 않는다. 되돌리기 어려운 것이 걸리면 아래 정한 안전한 쪽으로 가고
값을 보고에 낸다. 기대값은 갱신하지 않는다 · 범위 밖은 안 고치고 이름만 올린다.

수의 출처를 낱말 하나로 붙인다 — 「저장소」·「스크래치」·「문서에 적힌 값」·
「앞 세션 보고」.

파일 수정은 편집 도구로만 한다. 스크래치도 예외가 아니다.

지시서를 저장소에 남긴다 — docs\directives\S136.md. 그물에는 넣지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

0절 — 자리 확인 (커밋 없음)

1. git pull 을 먼저 한다. 앞 판이 다른 PC 였으므로 당기지 않고 시작하면
   그 판의 일을 덮는다. HEAD 를 적는다 — 앞 세션 보고는 a3f9f66 이라 적었다.

2. 어느 PC 인가. CPU·코어·RAM 을 값으로 적는다. 1번 PC(Core Ultra 7 258V ·
   8코어 · 31.5 GB)여야 한다. 2번 PC 면 1절 ㄱ·ㄴ 을 돌리지 말고 그 사실을
   6항에 적는다 — 그 셋은 1번 PC 에서만 잴 수 있다.

3. 다른 프로젝트의 python 이 도는지 본다. 죽이지 말고 적는다. 이 판은 소요를
   재는 판이라 이것이 판정에 든다 — 돌고 있으면 잰 값에 「견주는 데 쓰지
   않는다」 를 함께 적는다.

       Get-CimInstance Win32_Process -Filter "Name='python.exe'"

4. data\source\SOURCES.md 13줄 삭제는 정상이다. 손대지 마라.

5. 이 지시서가 옮겨 적는 수를 실물과 맞댄다.

   | 무엇 | 지시서가 적은 값 | 시점 | 출처 |
   |---|---|---|---|
   | 수집 건수 | 1,681 | S135 | 앞 세션 보고 |
   | 미해결 | 67건 (① 7 · ② 59 · ③ 1) | S135 마무리 | 앞 세션 보고 |
   | 살아 있는 못 | 1건 — tests\test_excess.py::test_월별_요금_구성_막대의_조각_합이_합계와_맞는다 | S135 | 앞 세션 보고 |
   | 덱 벌 | 열둘 | S132 | 앞 세션 보고 |
   | MWh 자리 | 15 (한 자리 밖 14) | S135 2절 | 앞 세션 보고 |
   | 금액 한 칸 `_won` 자리 | 4 | S135 2절 | 앞 세션 보고 |
   | 미해결 칸 글자 | 20,023 | S135 마무리 | 앞 세션 보고 |

   뒤 셋은 count_sites.py 로 다시 센다 — 손으로 세지 마라. 어긋나면 실물을
   따르고 두 값을 6항에 적는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1절 — 1번 PC 를 잰다 (재기만 한다 · 코드 무변)

「모름」 은 아무도 안 재면 영원히 「모름」 이다(68세션). 미해결에 이름 셋으로
서 있는 것이 이 절이다.

1. 기상 캐시를 먼저 본다. 이 PC 의 마지막 케이스 판이 98세션이고 그 뒤에
   C7 이 붙었다(project-overview.md). run_casestudy.py 가 시작할 때 벌마다
   캐시를 한 줄로 찍는다(CLAUDE.md 4항).

   캐시가 없으면 취득이 일어난다. 그 판의 소요는 견주는 데 쓰지 않는다 —
   한 판 돌려 캐시를 채운 뒤 다시 돌려 그 값을 쓴다. 취득 횟수를 적는다.
   네트워크가 끼는 자리이므로 상한을 걸고, 넘기면 스스로 멈추고 보고한다.

2. 케이스 스터디 소요를 잰다.

       .venv\Scripts\python.exe tools\run_casestudy.py

   판정 118/118 과 회귀값 여덟이 그대로여야 한다 — C1 5,293.44 / 5,293 /
   49.0% / 13.5% · R1 132.28 / 132 / 41.1% / 20.0%. 도구 소요와 벽시계를
   갈라 적고, 깨끗한 판인지(앞뒤로 남의 python 0개)와 기상 취득 횟수를
   함께 적는다.

3. 앞단 전체 pytest 를 상한 걸고 돈다. 이 PC 는 갈래로 안 나눈다.

       .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

   auto 가 고른 일꾼 수 · 수집 건수 · passed · skip · xfailed · 소요를 적는다.
   xfailed 1 이 뜬다 — 안 뜨면 그것이 발견이다. 종료 코드로 판정하지 마라.
   상한 8분을 넘기면 그 사실을 적는다 — 넘기면 이 PC 도 갈래 분할 대상이 된다.

4. 잰 값을 표에 앉힌다. 두 자리다.

   - CLAUDE.md 9항 표의 1번 PC 열 — 잰 건수 · 깨끗한 판 수와 소요 ·
     상한까지 남은 시간. 「-n 4 소요」 와 메모리 칸은 안 쟀으므로 「모름」 을
     그대로 둔다. 짐작해 채우지 마라.
   - project-overview.md 6절 표의 「케이스 여덟 (판정 118)」 1번 PC 칸.

   두 PC 의 값을 견주지 마라. 사양이 다르다 — 각자의 칸에 적는다.
   건수가 다른 옛 판과 나란히 두지 마라.

5. 미해결 셋을 닫는다 — 「1번 PC 의 앞단 전체 pytest 소요를 1,681건에서 안
   쟀다」 · 「1번 PC 의 케이스 여덟(판정 118) 소요를 아무도 안 쟀다」 ·
   「1번 PC 의 기상 캐시가 지금 벌 여덟을 다 담는지 아무도 안 봤다」.
   잰 값을 세션 절에 적고 칸에는 이름만 남긴다.

커밋 — S136 1절: 1번 PC 실측

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2절 — MWh 표기를 한 자리로 모은다

S135 의 도구가 낸 수 — MWh 자리 15, 한 자리 밖 14(앞 세션 보고). 리뷰는
11자리라 적었고 도우미(ui\text.py:328)를 쓰는 것은 화면뿐이라 했다.

1. count_sites.py 로 지금 수를 다시 센다. 손으로 grep 하지 마라 — 그러라고
   S135 가 도구를 세웠다. 자리마다 파일·줄 번호와 꼴을 적는다.

2. 값이 갈리는지 먼저 본다. 리뷰가 「값은 같다 — 소수 자리만 다르다
   (.0f/.1f 섞임)」 고 적었다(문서에 적힌 값). 자리마다 소수 자리를 적어
   실제로 몇 꼴인지 낸다.

   모으면 표기가 바뀌는 자리가 생긴다. 어느 산출물의 어느 줄이 몇 자리에서
   몇 자리로 바뀌는지 모으기 전에 표로 적는다. 그 표에 없는 것이 움직이면
   멈추고 6항에 적는다 — S134 가 쓴 방식이다.

3. 한 자리로 모은다. 도우미가 이미 있으므로 새로 만들지 말고 그것을 부르게
   한다. 모듈 경계를 본다 — ui\text.py 가 kwise.report.notices 를 부르므로
   report\* 가 ui\text.py 를 부르면 순환이다(S134 가 겪었다). 옮겨야 하면
   어디로 왜 옮겼는지 적는다. 계산 모듈이 화면을 import 하지 않는다.

4. 소수 자리를 하나로 세운다면 어느 쪽인지와 근거를 적는다. 산출물마다 다른
   자리가 필요하면 인자로 받게 하고 그 사실을 적는다 — 「하나로 못 모은다」 가
   답일 수도 있다. 그때는 왜인지를 값으로 적고 모으지 마라.

5. 못을 하나 박는다. `/ 1000` 을 손으로 적는 자리가 그 한 자리 밖에 서면
   빨개진다. count_sites.py 를 부르는 꼴로 짜면 S135 의 못과 겹치는지 보고,
   겹치면 무엇이 다른지 한 줄로 적는다.

   되돌려 확인한다 — 한 자리 밖에 도로 심어 빨개지는지 보고 되돌린다.
   그 못이 주석을 무는지도 값으로 보고 적는다(S133·S134·S135 가 다 물었다).
   이름에 고유어를 쓰지 마라.

6. 미해결 「MWh 표기와 부분 합을 만드는 자리가 리뷰가 센 것보다 많다」 는
   닫지 마라 — 부분 합이 남는다. 본문에서 MWh 쪽만 지우고 남은 것을 적는다.

커밋 — S136 2절: MWh 한 자리 · 못

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3절 — 금액 한 칸 `_won` 을 본다 (판정까지)

리뷰 1절 5번 — `_won` 이 4자리이고 셋은 money.won* 에 넘기는데 Word 만
format_won + 「원」 을 제 손으로 붙인다(문서에 적힌 값). S135 도구도 4로
셌다. 자리가 안 늘어난 몇 안 되는 줄이다.

1. count_sites.py 로 네 자리를 뽑고 몸통을 맞댄다. 같은 값을 내는지, 표기만
   다른지, 어느 자리가 무엇을 다르게 하는지 값으로 적는다.

2. 모을지 말지를 판정한다. 표기만 다르고 값이 같다면 모으는 이득이 작을 수
   있다 — 그때는 「모으지 않는다」 와 근거를 적는다. 이름이 같다고 다 같은
   일은 아니다(CLAUDE.md).

3. 모으기로 판정했고 한 판에 들어가면 모으고 못을 박는다. 안 들어가 보이면
   모으지 말고 크기를 값으로 적어 미해결에 이름으로 올린다 — 무엇이 몇
   자리이고 무엇이 딸려 오는지.

4. 화면 문구를 늘리지 마라. 표기가 바뀌면 화면 감사가 움직인다 — 움직일
   것으로 보면 어디가 몇 자 갈리는지 미리 적는다.

커밋 — S136 3절: 금액 한 칸 판정

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4절 — 마감

1. 대조표. 위 절의 항목마다 한 줄. 결과는 수치나 판정. 칸은 30자 안쪽.

2. 지시서를 저장소에 앉힌다 — docs\directives\S136.md. 받은 본문 그대로,
   편집 도구로만. 앉힌 뒤 scan_ctrl 로 확인한다.

3. pytest 를 앞단 전체로 다시 돈다(1번 PC). 1절 3번이 고치기 전 판이고
   이것이 고친 뒤 판이다 — 두 값을 갈라 적는다.

       .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

   수집 건수가 1절 값에 이 판이 더한 시험 수만큼 늘었는지 본다.
   skip 과 xfailed 를 함께 적는다.

4. 회귀. 2·3절이 src\ 를 고쳤으면 케이스 스터디를 다시 돌린다.

       .venv\Scripts\python.exe tools\run_casestudy.py

   회귀값 여덟과 118/118 이 그대로여야 한다. MWh 는 표기라 이 여덟에 안
   닿아야 한다 — 닿으면 그것이 발견이다. 기대값을 갱신하지 마라.

   화면 감사를 돌린다 — 넷(957 · 807 · 961 · 808). 2절 2번 표의 예상과
   맞는지 본다. 예상 밖으로 움직였으면 그 자리와 두 값을 6항에 적는다.

   덱을 하나 뽑아 MWh 가 적히는 장을 글자로 확인한다.

   scan_ctrl 을 돌린다.

5. PROCEED.md 를 적고 절마다 커밋했는지 본다.

6. 미해결 건수 대조를 이름으로 적는다. 예상은 이렇다. 실물이 다르면 실물을 따른다.

   - 닫는다 — 1번 PC 셋(1절 5번)
   - 닫지 않는다 — 「MWh 표기와 부분 합을 만드는 자리가 리뷰가 센 것보다
     많다」 (부분 합이 남는다 · 본문만 줄인다)
   - 늘 수 있다 — 3절에서 「모으지 않는다」 나 「안 들어간다」 로 판정하면
     그 이름 · 2절에서 소수 자리를 하나로 못 세우면 그 이름

7. 살아 있는 못 칸. 1건 그대로일 것이다 — 값으로 확인한다.

8. push. 돌고 있는 셸이 없는지 확인한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

보고 — 마지막에 한 번만. 여덟 항목 밖의 서술을 덧붙이지 않는다.

1. 대조표 — 항목마다 한 줄. 결과는 수치나 판정
2. 만든/바꾼 파일 — 파일마다 +/− 줄 수. 안 건드린 것도 한 줄로
3. 수치가 움직인 곳 — 종전/지금. 없으면 「없음」
4. 판단한 것 — 스스로 고른 갈래와 근거
5. 돌린 것 / 안 돌린 것 — 안 돌린 것은 왜
6. 예상과 달랐던 것 — 없으면 「없음」. 아래 여섯은 반드시 이 항에 적는다
   - 1절 — 기상 캐시가 차 있었나. 취득이 몇 번 일어났나
   - 1절 — 1번 PC 의 케이스 소요와 pytest 소요. 상한 안쪽이었나
   - 2절 — count_sites 가 낸 MWh 자리 수가 앞 판 값과 맞았나
   - 2절 — 소수 자리를 하나로 세웠나. 표기가 바뀐 자리가 몇이었나
   - 2절 — 모듈 경계에 걸렸나
   - 3절 — `_won` 을 모았나 안 모았나. 근거는 무엇인가
7. 박은 못 · 되돌려 확인한 못 — 이름으로. 없으면 「없음」
8. 프로젝트 지식 재업로드 — CLAUDE.md · docs\project\collaboration.md ·
   docs\project\project-overview.md 셋 중 이 판에 고친 것.
   1절 4번이 CLAUDE.md 와 project-overview.md 를 고친다

보고 뒤에 daily_brief.py 결과를 그대로 붙인다.

ctx 는 셋 — 세션 예산 종전 → 지금(차이) · 가장 크게 먹은 것 하나 ·
줄일 수 있었다고 보는 것 하나.

마지막 한 줄로 백그라운드 셸 확인 결과.
