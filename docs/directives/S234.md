S234 — 절사 뿌리 마무리 · 자릿수 결함을 사람 기준으로 다시 가른다

**지금 어디를 잡고 있나.** 뿌리 진행표 「ㄱ 줄마다 절사」 는 S233 이 셈 어긋남을 0 으로
만들었고 남은 것은 번져 멈춘 두 자리(1벌씩)와 원값이 같은 다른 사실 3벌이다. 「ㄷ 자릿수
손으로」 는 증상 자리를 닫았고 교차 후보 776(진짜 422 · 아님 169 · 모름 185)이 남았다 —
그러나 이 셈은 「자리마다 글자가 다르면 어긋남」 으로 셌다. 사람이 기준을 줬다(아래).
이 판은 **ㄱ 을 끝내고 · ㄷ 후보를 사람 기준으로 다시 갈라 고칠 자리를 못 박는다.** ㄷ 은
이 판에서 고치지 않는다. 이 둘 밖으로 가지 않는다 — 다른 뿌리 · 역률 투자비 · 도구는 이 판
몫이 아니다. (5-4 의 CLAUDE.md 두 줄은 사람이 정한 것이라 이 판에 넣는다.)

**사람이 정했다.**
  · (S231 뒤) 절사는 A 단수 차이 조정 — 천원 단위 표기 · 합계는 실제 계산값의 천원 절사
    그대로 · 어긋난 만큼 줄을 조정해 같은 표 안에서 적힌 수끼리의 셈이 맞게 · 계산값 불변.
  · (단위 · 가독성) **원래 기록을 0-5 에서 찾아 그 자리를 근거로 쓴다.** 사람이 S233 뒤에
    확인해 준 줄거리 — 사람은 「원 단위까지 쉼표도 없이 적혀 눈에 안 들어오니 가독성 있게
    적으라」 고 했고, 만 · 천 단위는 클로드가 정했다. 모든 단위를 통일할 필요는 없다. 같은
    표 안에서는 단위가 달라선 안 된다. **이 줄거리를 새 규칙으로 옮겨 적지 않는다.**
  · (CLAUDE.md) **CLAUDE.md 는 프로젝트의 중심 규약이다 — 규칙이 바뀔 때만 고친다. 판마다의
    값(PC 별 소요 · 깨끗한 판 · 회귀 수)은 CLAUDE.md 에 적지 않는다.** 그 값은 HANDOVER
    2절이 쥔다.

**웹 대화창이 정했다 — S233 뒤 · 사람 결정이 아니다.**
  ㄱ S233 이 번져 멈춘 두 자리를 따라 고친다 — small-a2 화면 3단계 「차이」 −7,000 대 계산
    근거 −8,000 · small-b-sell 태양광 기본요금 절감 계산 근거 1,004,000 대 용량 곡선 1,003,000.
  ㄴ 원값이 같은 다른 사실은 같은 글자로 적는다(S233 same_won 을 넓힌다) — 조합 절감 대
    태양광 · 역률 단독 절감 3벌.
  ㄷ 자릿수 결함은 셋뿐이다(사람 기준에서 나온다):
      (가) 같은 표 · 같은 카드 · 같은 그림 안에서 같은 양이 다른 단위나 자릿수로 적힌다.
      (나) 읽기 어려운 표기 — 네 자리 이상 수에 쉼표가 없다 등.
      (다) 같은 사실이 서로 다른 자리에서, 단위를 맞춰 보면 그 자리 표기 한 칸보다 크게
           갈린다(반올림 · 절사 차이 안쪽은 결함이 아니다).
    자리마다 단위 · 자릿수가 다른 것 자체는 결함이 아니다. 「양 종류마다 표기 규칙 하나로
    모은다」 는 길은 가지 않는다. S232 의 교차 못은 그대로 둔다.

**S233 이 적은 것 — 재게 할 것이다.** 교차 후보 776 · 23묶음(사실 이름을 낱말로 잡아 틀린
묶음이 섞였다). 케이스 스터디 「소요 칸 25만 갈림」.

**울타리에 하한이 있다** — 1-2 가 센 「고칠 자리 수」.

이 판의 못 박힌 안전한 쪽.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality) 0줄.**
  · **회귀값 여덟 · 케이스 스터디 타당성 · 요금 총액은 한 자리도 안 움직인다.** 움직이면 멈춘다.
  · **덱 19벌 전 · 후 수치 조각을 맞대 울타리 밖 자리가 갈리면 멈춘다.** 기준선은 S233 뒤 스냅.
  · **1-1 목록에 없던 곳으로 번지면 멈춘다.**
  · **ㄷ 은 고치지 않는다** — 3절은 재기만.
  · **CLAUDE.md 는 5-4 의 두 줄 밖으로 고치지 않는다.**
  · **이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · git log)에서 찾는다.** 사람에게
    이력을 묻는 항목을 보고에 올리지 않는다 — 저장소에 없으면 「기록 없음」.
  · **이미 기록에 있는 결정을 다른 자리에 다시 적지 않는다** — 가리키기만 한다.
  · 덱은 고친 뒤 한 판. 빨가면 고친 뒤 한 판 더. 그 밖에 덱을 뜨지 않는다.
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **센 값만 적는다. 벽시계는 시계를 보고 적는다.**
  · 세션 도중 사람에게 묻지 않는다.
  · 지시서에 없는 pytest 플래그를 붙이지 않는다. 전체가 빨갛게 끝나면 고친 뒤 전체를
    한 판 더 — 2번 PC 면 네 갈래 전부.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다.
    도구로 되는 자리에 파이프를 쓰지 않는다. 조사 에이전트를 띄우면 이 금지를 그대로 전한다.
  · 절마다 따로 커밋한다. 커밋 메시지는 편집 도구로 지은 .txt 로 git commit -F.
  · 전원 · 소요는 적기만 한다.


0절 — 개시

0-1  open_values · git pull · HEAD 와 origin(S233 보고는 1b2a5cb) · 워크트리.
0-2  미해결 건수(S233 보고 151).
0-3  회귀값 여덟 · 덱 스냅 기준선(S233 뒤) · 회귀 기준선 — **그 PC 의 깨끗한 판을
     docs\HANDOVER.md 2절에서 읽는다**(CLAUDE.md 9항 값이 아니다). 두 자리가 다르면 그 사실을 적는다.
0-4  **S233 3-3 「소요 칸 25만 갈림」** — 어느 칸 · 어느 벌 · 전 · 후 · 금액인가 시간인가.
     금액이 갈렸으면 멈춘다.
0-5  **단위 · 가독성 이력을 저장소에서 찾는다** — PROCEED.md · docs\HANDOVER.md · CLAUDE.md ·
     collaboration.md · git log 에서 (a) 사람의 가독성 지시가 든 세션과 글자 (b) 만 · 천 단위를
     정한 세션 · 글자 · 결정 주인 (c) 「같은 표 안에서는 한 단위」 에 해당하는 규칙이 이미
     있는지. 자리 · 글자 그대로. 찾지 못하면 「기록 없음」. 사람 확인과 기록이 어긋나면 그
     사실을 적는다(사람 확인이 앞선다).

커밋: S234 0절: 개시


1절 — 절사 남은 자리

1-1  ㄱ 두 자리와 ㄴ 3벌 — 자리마다 벌 · 산출물 · 적힌 글자 · 그 글자를 만드는 코드 자리 ·
     그 값이 서는 다른 산출물 자리 전부.
1-2  **고칠 자리 수를 값으로 못 박는다** — 코드 자리 · 시험 자리. 울타리이고 하한이다.

커밋: S234 1절: 절사 남은 자리


2절 — 절사 고침

2-1  **1-2 가 센 자리만 고친다.** S233 이 만든 한 자리(표기 값을 사실마다 한 번 만드는 함수 ·
     same_won)를 쓴다. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았으면 자리마다 까닭을 값으로.

커밋: S234 2절: 절사 마무리


3절 — 자릿수 결함 다시 가르기 (고치지 않는다)

3-1  **후보 776 전부를 ㄷ 의 (가) · (나) · (다) · 「결함 아님」 · 「모름」 으로 다시 가른다.**
     사실은 낱말이 아니라 그 글자를 만드는 코드 자리와 값으로 잡는다. (가) 는 같은 표 · 카드 ·
     그림 안인지 산출물 실물에서 본다. (다) 는 단위를 맞춘 차이와 그 자리 표기 한 칸을 값으로
     적는다. 결과 — 결함 종류 × 산출물(화면 · PPT · Excel · Word)마다 수 · 벌 수. 모름은 줄이고
     남은 모름은 까닭.
3-2  **후보 밖 (가) · (나) 전수** — 교차 후보는 자리 사이 비교라 (가) 같은 표 안 · (나) 쉼표 없음을
     놓칠 수 있다. 덱 스냅(S233 뒤)에서 표 · 카드 · 그림마다 (가), 모든 수 글자에서 (나)를
     따로 센다. 3-1 과 겹치는 것은 한 번만.
3-3  **결함마다 코드 자리** — (가) · (나) · (다) 묶음마다 그 글자를 만드는 코드 자리 · 고칠
     자리 수 · 바뀌는 산출물 글자 줄 수(센 값) · 계산 폴더에 닿는지 · 한 판에 되는지.
     (다) 가운데 뿌리 「정의가 다른 두 수」 에 걸리는 것은 수만 세고 따로 적는다(재지 않는다).
3-4  **사람에게 물을 것이 남는지** — 사람 기준과 0-5 기록으로 판정이 안 서는 자리만. 없으면
     없다고. **고칠 길을 고르지 않는다.**

커밋: S234 3절: 자릿수 결함 가르기


4절 — 못 · 실물 · 회귀

4-1  못 — S233 의 두 못을 넓혀 1-1 자리를 문다. 새 못은 박지 않는다. 되돌려 확인한다
     (스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다. 소요.
4-2  **덱 19벌을 한 판 새로 뜬다** — S233 뒤 스냅과 수치 조각을 맞댄다. 갈린 조각이 전부
     울타리 안인지 · 절사 셈 어긋남 0 · 번진 자리 0 인지.
4-3  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-3 과 같은지.
4-4  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
4-5  `git diff --numstat` 으로 계산 폴더 여섯 0줄을 값으로 — 폴더마다.
4-6  pytest — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로 ·
     갈래마다 시작 · 끝. 1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다).
     그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no --durations=0

     pytest_counts 로 센다. 첫 판 초록인지.
4-7  정적 검사 다섯 — 저장소 기준.

커밋: S234 4절: 못 · 실물 · 회귀


5절 — 기록 · 닫기

5-1  PROCEED.md 에 234세션 절 — 사람 결정과 웹 대화창 판단(ㄱ ~ ㄷ)을 갈라서 · 0-5 · 3절 값.
     S233 뒤 사람 확인(단위 · 가독성)은 이 절에 한 번만 적는다.
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다. 미해결 건수 대조를 이름으로.
     이름에 「 · 」 를 넣지 않는다.
5-3  docs\HANDOVER.md — 뿌리 진행표를 이 판 값으로 갈아 끼운다. 이 판 회귀 값은 2절에 적는다.
     **단위 · 가독성은 5절에 새 행을 달지 않는다** — 0-5 에서 찾은 원래 자리가 있으면 그 행에
     「S234 에 사람이 확인했다」 곁 줄 하나만. 기록이 없던 것만 5절에 한 행으로(한 자리에만).
     CLAUDE.md 사람 결정은 5절에 한 행 — 5-4 의 자리를 가리키기만 한다. 웹 대화창 판단
     (ㄱ ~ ㄷ)은 사람 결정과 갈라 적는다.
5-4  **CLAUDE.md — 이 두 줄만 고친다.**
       · 「세션 종료 시」 절의 「PC 별 소요는 9항 표가 쥔다」 를 「PC 별 소요 · 깨끗한 판 ·
         회귀 수는 docs\HANDOVER.md 2절이 쥔다 — CLAUDE.md 는 규칙이 바뀔 때만 고친다」 로.
       · 9항 PC 별 값 칸 머리에 한 줄 — 「이 칸의 값은 S233 에서 멈췄다 · 지금 값은
         docs\HANDOVER.md 2절」.
     그 밖 0줄. 9항에 쌓인 옛 값은 옮기지도 지우지도 않는다.
5-5  이 지시서 본문을 docs\directives\S234.md 로 그대로 앉힌다.
5-6  기록을 다 고친 뒤 기록 묶음.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

     CLAUDE.md 글자를 무는 기록 못이 빨개지면 5-4 두 줄에 맞춰 못을 고친다(전 · 후).
5-7  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 남의 python.

커밋: S234 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지
끝에 적는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에
    HEAD = origin · 워크트리 · 남의 python.
  2 바꾼 파일 — +/− · 계산 폴더마다 0줄 · CLAUDE.md 줄 수.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · pytest 수.
  4 판단한 것   5 돌린 것 / 안 돌린 것   6 예상과 달랐던 것
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것 · 소요.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「0-4 소요 칸 25만」 — 한 줄.
  그 뒤에 「0-5 단위 · 가독성 이력」 — (a) · (b) · (c) 마다 자리 · 글자 · 결정 주인 또는 「기록 없음」.
  그 뒤에 「절사 남은 자리 전후」 — 자리 · 전 → 후.
  그 뒤에 「자릿수 결함 표」(3-1 · 3-2) — (가) · (나) · (다) · 결함 아님 · 모름 × 산출물 · 수 · 벌.
  그 뒤에 「결함 묶음과 고칠 자리」(3-3) — 묶음마다 예시 글자 하나 · 고칠 자리 수 · 바뀌는 줄 수 ·
    한 판에 되나. 사람이 읽는 말로. 코드 자리는 꼬리표로만. 두 수에 걸린 수 따로.
  그 뒤에 「사람이 정할 것」(3-4) — 없으면 없다고.

마지막에 벽시계(절마다 · 시계를 보고) · 백그라운드 셸 확인 결과.


S234 보정 — 사람이 정했다(진행 중에 덧붙인다 · 이미 한 것은 되돌리지 않는다)

1  **이 판부터 PC 별 소요 · 깨끗한 판 · 전원 · 남의 python 을 재지도 적지도 않는다** — 코드와
   무관하고 개선으로 이어지지 않았다. 회귀는 초록인지와 건수만 본다. 2번 PC 갈래 넷 나눠 돌리기는
   그대로다. 시험이 이상하게 빨개질 때만 다른 python 이 떠 있었는지 한 번 본다.
2  4-6 전에 PROCEED.md 「pytest 분할 실행」 네 명령에서 `--durations=0` 을 뗀다. 4-6 은 고친
   명령으로 돈다. 1번 PC 명령도 `--durations=0` 없이 돈다.
3  5-2 — 소요 · 깨끗한 판 · 전원을 재는 일로 남은 미해결 항목이 있으면 사람 결정(측정 중단)으로
   이름을 들어 닫는다.
4  5-3 — HANDOVER 2절에는 이 판 pytest 건수 · 초록만 적는다(소요 · 깨끗한 판 칸은 더 적지 않는다).
   측정 중단 사람 결정은 5절에 한 행 — 5-4 의 자리를 가리키기만 한다.
5  5-4 를 이 두 줄로 바꾼다(그 밖 0줄 · 옛 글자는 옮기지도 지우지도 않는다):
     · 「세션 종료 시」 절의 「PC 별 소요는 9항 표가 쥔다」 를 「PC 별 소요 · 깨끗한 판은 S234 부터
       재지도 적지도 않는다 — CLAUDE.md 는 규칙이 바뀔 때만 고친다(사람 결정)」 로.
     · 「백그라운드 실행」 절 머리에 한 줄 — 「S234 사람 결정: 이 절 가운데 소요 · 깨끗한 판 · 전원 ·
       남의 python 을 재고 적으라는 글은 따르지 않는다. 갈래 나눠 돌리기 · 백그라운드 셸 규칙은
       그대로다.」
   이미 5-4 를 옛 글로 고쳤으면 그 두 줄을 위 글로 바꾼다.
6  6절 보고에 소요 · 깨끗한 판 · 전원을 적지 않는다.
7  5-5 — 이 보정문을 docs\directives\S234.md 끝에 그대로 덧붙여 앉힌다.
