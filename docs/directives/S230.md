S230 — 규약 판 · 깨끗한 판에 전원을 넣고, 갈래 상한을 닫고, 「마」 를 가른다

**이 판은 규약 · 기록 판이다. src\ · tools\ · tests\ 0줄 · 산출물 0벌 · 금액 0.**
고치는 것은 CLAUDE.md · PROCEED.md · docs\ 기록 글뿐이다.

**사람이 정했다 — S229 뒤 웹 대화창에서.**
  ㄱ 깨끗한 판은 전원 연결(AC) 판만이다 — 「남의 python 0」 에 「AC」 를 더한다.
    1번 PC · 2번 PC 둘 다. 세는 자리마다 open_values 의 전원 줄로 본다.
    옛 판은 다시 돌리지 않는다 — 전원이 값으로 서면 그 값, 안 서면 「전원 모름」.

**웹 대화창이 정했다 — 사람 결정이 아니다.**
  ㄴ 2번 PC 갈래마다의 소요 상한은 두지 않는다. 「8분」 은 통째 실행을 갈래로
    나누는 갈림길로만 읽는다(S229 1-2). 2번 PC 갈래가 앞단으로 돌 수 있는 한계는
    셸 도구 기한 600초다 — 앞으로 2번 PC 갈래 여유는 「600초까지」 로 적는다.
    갈래마다 「상한 8분까지」 를 쓴 옛 자리는 고치지 않고 곁 줄을 단다.
  ㄷ 미해결 「판 개시 값 도구가 전원 상태를 안 내 깨끗한 판을 가를 수 없다」 와
    「2번 PC 갈래 넷의 소요 상한이 규약에 없다」 (S229 가 남긴 이름 그대로 찾는다)
    는 ㄱ · ㄴ 으로 닫는다. 1절 값으로 남은 몫이 있으면 이름을 남은 몫에 맞게
    고치고 남긴다.
  ㄹ 미해결 갈래 「마」 는 이 판이 가르기만 한다 — 닫지도 몸을 고치지도 않는다.
    한 글머리에 여러 항목이 든 자리만 항목마다 한 글머리로 편다(이름 · 수 그대로).

**S229 보고가 적은 것 — 전부 재게 할 것이다.**
  · 대조표가 0-1 ~ 0-4 · 4-1 ~ 4-9 를 한 줄씩으로 묶었다 · 1-3 「셸이 새는 자리
    셋」 의 이름이 보고에 없다.
  · 「상한 8분」 을 갈래마다 쓴 자리로 HANDOVER 만 짚었다.
  · 「마」 첫 글머리가 일곱 건을 담았다 — 펴지 않으면 도구가 41 이 아니라 35.
  · 9항 2번 PC 칸을 전원 값과 함께 고쳤다.
  · 전원 이력 — S225 배터리 100 → 약 45% · S227 배터리 35.4 → 14.6% · S228 AC ·
    S229 AC 100%.

**울타리에 하한이 있다** — 1-6 이 센 「고칠 자리 수」. 적게 닫으면 자리마다
까닭을 값으로.

이 판의 못 박힌 안전한 쪽.
  · **src\ · tools\ · tests\ 0줄.** 하나라도 갈리면 멈춘다.
  · **「마」 항목을 닫거나 몸을 고치지 않는다** — 이미 선 결정으로 답이 나와
    보여도 값만 낸다. 편 글머리는 이름과 수가 그대로인지 값으로 본다.
  · **사람이 정한 ㄱ 에 세부를 덧붙이지 않는다** — 잔량 문턱 · 절전 조건 · 예외를
    넣지 않는다.
  · **옛 판 절(PROCEED 세션 절)은 고치지 않는다 — 곁 줄로.** 지금 상태를 적는
    칸(9항 PC 칸 · PROCEED 「현재 상태」 · HANDOVER 1 · 2절)은 새 규칙으로 맞춘다
    — S228 이 S227 을 오염된 판 칸으로 옮긴 꼴 그대로.
  · **새 값을 짓지 않는다** — 전원은 OS 기록, 600초는 출처 글자와 함께.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 전체 · 덱 · 케이스 스터디 · 화면 감사를 안 돌린다.** pytest 는 기록
    묶음만(0-4 · 4-8). 지시서에 없는 pytest 플래그를 어느 판에도 붙이지 않는다.
  · **git stash 를 쓰지 않는다.** 1번 PC 면 S140 스태시를 안 지운다.
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.** 개시 값은
    `open_values` · 파일 목록은 `cache_files` · 칸은 `daily_brief` 칸 인자 · 미해결
    갈래 이름은 `daily_brief --branch` · 긴 출력은 `run_tool --tail`. **도구로 되는
    자리에 파이프를 쓰지 않는다.** 썼으면 쓴 자리마다 「무엇을 보려고 · 왜 도구로
    못 했나」. 스크래치 출력은 거른 것만 낸다 — 통째 덤프를 내지 않는다.
  · **절마다 따로 커밋한다.** 커밋 메시지는 편집 도구로 지은 `.txt` 로
    `git commit -F`. **벽시계를 절마다 잰다.**


0절 — 개시

0-1  `tools\open_values.py` — PC · cpu · RAM · 일꾼 수 · 남의 python · 전원.
0-2  `git pull` · HEAD 와 origin(S229 보고는 86ee2d6) · 워크트리 · `git stash list`.
0-3  미해결 — 건수 · 갈래별 수 · 세 방법 검산. **이 판이 닫을 둘을 이름으로.**
0-4  기록 묶음 기준선 — 아래 명령 그대로 한 판.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

커밋: S230 0절: 개시


1절 — 잰다

**이 절은 재고 판정만 한다. 고치지 않는다.**

1-1  **깨끗한 판 규칙 글** — 「깨끗한 판」 을 정의하거나 판정 잣대로 쓰는 글
     전수. CLAUDE.md · collaboration.md · project-overview.md · PROCEED.md
     「현재 상태」 · docs\HANDOVER.md · docs\OPEN_ITEMS.md. 자리마다 글자 그대로 ·
     정의인가 쓰임인가 · ㄱ 을 넣으려면 고쳐야 하는가.
1-2  **전원을 모르는 기준선** — 9항 두 PC 칸이 깨끗한 판으로 적은 판마다 전원
     값. 229세션 절 1-1 값과 이 PC 시스템 로그로 설 수 있는 것만 쓴다. 2번 PC 는
     로그가 닿는 가장 이른 날을 값으로 낸다. 1번 PC 판은 이 PC 에서 잴 수 없다고
     적는다(2번 PC 판이면). 판마다 새 규칙 ㄱ 으로 깨끗한가 · 아닌가 · 모름.
1-3  **「상한 8분」 을 갈래마다 쓴 자리 전수** — CLAUDE.md 9항 표 2번 PC 칸 ·
     갈래 표 · PROCEED 「현재 상태」 · HANDOVER. 자리 · 글자 그대로. 그리고
     **「600초」 의 출처** — 실물에서(도구 문서 · 오류 글자 등) 글자와 함께.
1-4  **S229 보고 확인** — 229세션 절에서 「셸이 새는 자리 셋」 의 이름을 글자
     그대로 · 9항 2번 PC 칸에 S225 가 배터리로 표시됐는가 · S229 가
     `--durations=0` 이 붙은 새 분할 명령의 첫 판으로 적혔는가 · 새 규칙 ㄱ 으로
     S228 · S229 가 깨끗한 판인가(값으로).
1-5  **「마」 가르기** — 항목마다 아래 일곱. 항목 몸 · 코드 · 기록에서만 뜬다 ·
     지어 쓰지 않는다 · 모르면 「모름」. ② ~ ⑤ 는 코드 안 용어 없이 사람이 아는
     말로 쓴다.
       ① 이름 — 글자 그대로
       ② 산출물(화면 · PPT · Excel · Word)에 무엇이 어떻게 보이나 — 한 줄 ·
          안 보이면 「안 보인다」
       ③ 고객에게 나가는 금액이나 계산값이 갈리나 — 예 · 아니오 · 모름 · 근거 한 줄
       ④ 되돌리기 어려운 것(스키마 · API · 회귀 기대값)에 닿나 — 예 · 아니오
       ⑤ 근거가 사람에게만 있나 — 있으면 무엇(청구서 · 실측 · 현장 사정 등)
       ⑥ 이미 선 결정 · 원칙(HANDOVER 5절 · collaboration.md · CLAUDE.md)으로
          답이 나오나 — 나오면 그 결정 이름과 글자 · 아니면 「없다」
       ⑦ 표현 · 자리 · 길이만 정하면 되는 화면 문구 항목인가 — 예 · 아니오
     그리고 한 글머리에 여러 항목이 든 자리 — 자리 · 든 이름 · 수.
1-6  **고칠 자리 수를 값으로 못 박는다** — 규칙 글 자리(1-1) · 지금 상태 칸
     자리(1-2 · 1-4) · 곁 줄 자리(1-3) · 미해결 닫을 자리(ㄷ) · 글머리 펼 자리
     (1-5). 그것이 울타리이고 하한이다.

커밋: S230 1절: 재기


2절 — 고친다

2-1  **1-6 이 센 자리만 고친다.** 자리마다 고친 줄 수와 전 · 후. 울타리를
     넘는 자리가 보이면 멈추고 값을 낸다.
2-2  규칙 글 — ㄱ 을 사람이 정한 말 그대로 넣는다: 깨끗한 판은 세는 자리마다
     남의 python 이 0 이고 전원이 AC 인 판(1번 · 2번 PC).
2-3  지금 상태 칸 — 1-2 · 1-4 판정대로 옮긴다 · 「전원 모름」 곁 줄 · 2번 PC
     갈래 여유를 「600초까지」 로 S229 값부터 적는다 · 갈래마다 「상한 8분까지」
     를 쓴 옛 자리에 ㄴ 곁 줄.
2-4  「마」 글머리 펴기 — 전 · 후 이름이 같은지 · 수가 41 그대로인지 세 방법과
     `daily_brief --branch 마` 로 본다.
2-5  적게 닫았으면 자리마다 까닭을 값으로.

커밋: S230 2절: 규약 · 칸 고침


3절 — 확인

3-1  **`git diff --numstat` 으로 src\ · tools\ · tests\ 0줄을 값으로 보인다** —
     폴더마다.
3-2  `daily_brief` 인자 없는 출력에서 미해결 수 · 「마」 수가 기록과 같은지.

커밋: 3절은 고친 것이 없으면 커밋하지 않고 그 사실을 적는다.


4절 — 기록 · 닫기

`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.

4-1  `PROCEED.md` 에 230세션 절 — ㄱ 은 사람 결정 · ㄴ ~ ㄹ 은 웹 대화창 판단으로
     갈라서 · 1-1 ~ 1-5 값(1-5 「마」 가르기 전부).
4-2  **미해결 건수 대조를 이름으로.** 이름에 「 · 」 를 넣지 않는다.
4-3  docs\HANDOVER.md — 머리 사슬에 S230 · 1절 「앞 값」 사슬은 두 판까지 ·
     5절 「다시 열지 마라」 에 ㄱ(사람 결정)과 ㄴ(웹 대화창 판단)을 갈라 적는다 ·
     2번 PC 판이면 2절을 이 판 값으로 · 6절 앞 판 딱지를 걷는다.
4-4  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
4-5  `CLAUDE.md` — 1-1 규칙 글 자리 · 9항 두 PC 칸 · 9항 이 판을 돈 PC 칸.
4-6  「다음 작업」 칸 — 후보를 이름으로만(「마」 를 사람과 보기 · ㄷ 자릿수
     뿌리 · ㄱ 절사 뿌리 · 남은 도구 자리). **고르지 않는다.** 글자 수와 여유.
4-7  이 지시서 본문을 `docs\directives\S230.md` 로 그대로 앉힌다.
4-8  **기록을 다 고친 뒤에** 기록 묶음을 0-4 명령 그대로 돌린다.
4-9  커밋하고 `git push`. **HEAD 와 origin 이 같은지 · 워크트리가 깨끗한지 ·
     `runs\도는중\` · 남의 python 을 보고 1항에 적는다.**

커밋: S230 4절: 기록 · 미해결 대조 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — **항마다 한 줄 · 여러 항을 한 줄로 묶지 않는다.** 울타리(하한)와
    고친 수를 나란히. 「멈춘다」 가 걸린 자리를 멈춘 자리 · 안 멈춘 자리로
    갈라서. 마지막 줄에 HEAD = origin · 워크트리 · 도는중 · 남의 python.
  2 만든/바꾼 파일 — +/− 줄 수. src\ · tools\ · tests\ 폴더마다 0줄을 값으로.
  3 수치가 움직인 곳 — 미해결 수 · 기록 묶음 수. 금액 0.
  4 판단한 것 — 1-1 · 1-2 · 1-4 · 1-6 · 2-5.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 가운데 고친 것과 9항을 고쳤는지.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 없으면 없음 · 확인 안 한 못 전후와
    딱지 몫.

  **그 뒤에 「규칙 글 표」(1-1) — 자리 · 전 · 후.**
  **그 뒤에 「기준선 전원 표」(1-2) — PC · 판 · 전원 · 새 규칙 판정.**
  **그 뒤에 「8분 곁 줄 표」(1-3) — 자리 · 글자 · 단 곁 줄 · 600초 출처 한 줄.**
  **그 뒤에 「셸이 새는 자리 셋」(1-4) — 이름 글자 그대로.**
  **그 뒤에 「마 가르기」(1-5) — 표로 그리지 않고 항목마다 한 덩이로:
    이름 / 보이는 것 / 금액 갈림 / 되돌리기 / 사람 근거 / 선 결정 / 문구.
    끝에 셈 — ③ 예 · ④ 예 · ⑤ 예 · ⑥ 나옴 · ⑦ 예 각각 몇 건. 편 글머리 전 · 후.**
  **그 뒤에 「사람이 정할 것」 — 멈춘 자리가 있으면 그 자리. 없으면 없다고.**
  **그 뒤에 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다). ctx 셋(종전 → 지금 · 가장 크게
먹은 것 · 줄일 수 있었던 것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
