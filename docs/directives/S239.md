S239 — 고침 판 · 「ㄹ 0원 거르기」 마무리 둘 · 다음 뿌리 「ㅁ 겹침 거르기」 를 잰다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 두 수 · 자릿수는 닫혔고 ·
「ㄹ 0원 거르기」 는 S237 · S238 이 고쳤다. 이 판은 **ㄹ 이 남긴 두 자리를 고치고 · 다음 뿌리 「ㅁ 겹침
거르기」 를 재기만 한다**(고치지 않는다). 이 밖으로 가지 않는다 — 다른 뿌리 · 도구 · 측정은 이 판
몫이 아니다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).**
절사 A · 단위 기준 · 측정 중단 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리 작업은 다른 갈래로 빠지지
않는다 · S154 · S207 · 운영시간 외 부하는 비중(S238).

**웹 대화창이 정했다(S238 뒤 · 이 판이 HANDOVER 5절에 두 행으로 앉힌다).**
  결정 1 — 여지가 없는 수단의 투자비는 넣었어도 조합 투자비 · 회수기간 셈에 더하지 않는다. S238 결정
    2(여지가 없는 수단은 투자 대상이 아니다)를 투자비를 넣은 경로까지 끝까지 적용한다. 여지가 있는
    수단의 투자비는 그대로 더한다.
  결정 2 — 여지가 없는 수단만 더해 앞 조합과 금액이 같은 조합 줄은 세우지 않는다. S237 결정 ㄴ(여지가
    없는 수단은 조합 이름에서 말하지 않는다) · S206(역률 상한 이상에서는 목표를 말하지 않는다)을 끝까지
    적용한다. 다른 줄의 이름 · 금액은 그대로다.

**S238 보고가 적었다 — 실물에서 먼저 확인하라.** 역률 투자비를 넣으면 역률 100 벌의 조합 투자비에도 그
금액이 더해진다(compare\combination.py) / 「+ 역률 97%」 줄이 pf100-offset · pf100-offset-area ·
small-ind-a1 에 서고 앞 줄과 금액이 같다 / 옛 코드로 덱을 뜨는 길 — render_deck 이 저장소 src 를 앞에
넣어 두 판을 버렸고 뒤에 선 길은 238세션 절에 있다(찾아 그대로 쓴다) / 세션 목록표에 235 행이 없다.

**울타리에 하한이 있다** — 1-4 가 센 「고칠 자리 수」. 결정 1 · 2 를 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한
    자리도 안 움직인다.** 움직이면 멈춘다.
  · **결정 2 로 빠지는 줄이 권장안이거나 · 빠지면 다른 줄의 이름 · 금액 · 순서 글자가 바뀌는 자리는
    고치지 않고** 그 줄과 앞 줄의 이름 · 금액을 값으로 낸다.
  · 투자비를 비운 경로의 글자는 결정 2 자리 말고는 안 바뀐다. 투자비를 넣은 경로는 결정 1 · 2 자리
    말고는 안 바뀐다. 바뀌면 멈춘다.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-4 가 센 자리 밖 0줄.**
    요금 · 절감액을 만드는 식은 0줄.
  · **4절(겹침 거르기)은 고치지 않는다.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다
    — 없으면 「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 절이 끝날 때마다 시계를 보고 적는다. 커밋 메시지는 편집 도구로 지은
    .txt 로 git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · **지시서에 없는 pytest 플래그를 붙이지 않고 · 있는 플래그를 바꾸지 않는다 — 좁은 판도 아래 꼴 그대로다.**
  · 전체 pytest 갈래가 메모리 부족으로 끊기면 스스로 다시 돌리지 않고 플래그도 바꾸지 않는다. 거기서
    멈춰 3절까지 커밋하고(push 하지 않는다) · 끊긴 자리(수집 몇 건 가운데 몇 건째 · 그때까지 빨강 수)를
    보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. 도구로
    되는 자리에 파이프를 쓰지 않는다. 조사 에이전트를 띄우면 이 금지를 그대로 전한다.


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S238 보고는 c17fbc8) · 워크트리.
0-2  미해결 건수(144) · 회귀값 여덟 · 덱 스냅 기준선(S238 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S239 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — S154 · S206 · S207 · S237 결정 ㄴ · S238 결정 2 를 글자 그대로 · 결정 주인. 결정 1 · 2 와
     어긋나는 글자가 있는지.
1-2  **결정 1** — 여지가 없는 수단의 투자비가 조합 투자비 · 회수기간에 더해지는 자리를 전수로(화면 ·
     PPT · Excel · Word · 일괄 생성 · 그물 밖 자리). 서는 벌과 조합 · 경로마다 투자비를 넣었을 때 지금
     글자 · 고친 뒤 글자(예정) · 그 수단 카드가 투자비 · 회수기간을 무엇으로 적는지.
1-3  **결정 2** — 여지가 없는 수단만 더한 조합 줄이 서는 벌 · 조합 · 경로 전수. 줄마다 앞 줄과 금액이
     같은지 · 권장안인지 · 빠지면 다른 줄의 글자가 바뀌는지.
1-4  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S239 1절: 고칠 자리


2절 — 고친다

2-1  **1-4 가 센 자리를 고친다.** 결정 1 · 2 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S239 2절: 0원 거르기 마무리


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면
     시험과 한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지
     않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 결정 1 · 2 가 다 서는 벌을 가장 적게 골라(고른 벌과 까닭) 투자비 비움 · 넣음
     두 경우로 뜬다. **산출물 넷(PPT · Excel · Word · 일괄)을 통째로 S238 뒤 스냅과 맞대** 갈린 줄이
     전부 1-4 자리인지 본다. 옛 코드 덱은 238세션 절의 길로 뜬다. 빨가면 고친 뒤 그 벌만 다시. 초록일
     때만 3-3 으로.
3-3  못 — 있는 못을 넓혀 (1) 「여지가 없는 수단의 투자비는 넣어도 조합 투자비에 안 더해진다」 (2)
     「여지가 없는 수단만 더한 조합 줄은 서지 않는다」 를 문다. 넓혀서 안 닫히면 새 못. 산출물을 실제로
     만든 글자를 본다. 되돌려 확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저
     보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S238 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-4 자리인지. 금액 칸이 갈리면 멈춘다.
     이 판의 19벌 실물은 4절이 쓴다 — 지우지 않는다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-4
     자리 밖이 갈리면 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-4 자리 밖 0줄을 값으로 · 폴더마다.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S239 3절: 못 · 실물 · 회귀


4절 — 다음 뿌리 「ㅁ 겹침 거르기」 재기 (고치지 않는다 · 3-4 가 뜬 19벌 실물로 잰다)

4-1  **증상** — 미해결 「안내 겹침을 거르는 잣대가 자리마다 달라 같은 말이 Excel 요약과 부록 C 에 여러
     번 선다」 의 몸 · 조합마다 되풀이되는 계약전력 문장(S236 이 10벌 33줄로 셌다 — 다시 센다). 지금
     실물에서 서는지 · 벌 수 · 예시 글자.
4-2  **이력과 자리** — 이 뿌리가 진행표에 「고침 중」 인데 어느 세션이 무엇을 고쳤고 무엇이 남았나
     (PROCEED.md · HANDOVER 4절 · git log). 남은 자리(코드 자리 · 계산 폴더 몫 따로)와 자리마다 무엇을
     겹침으로 보나(같은 글자 · 같은 사실 · 한 산출물 안 · 다른 장 · 요지와 전문).
4-3  **걸린 결정** — 저장소 전체에서 이 뿌리에 닿는 결정(collaboration.md 「층은 중복이 아니다」 · S207 ·
     HANDOVER 5절 등) 글자 그대로 · 결정 주인. 「잣대를 사람이 정한다」 가 선 세션과 사람이 답한 기록이
     있는지.
4-4  **판정** — 남은 자리마다 4-3 의 기록된 결정으로 잣대가 서는가 / 안 서는가. 안 서는 자리만 선택지를
     길마다 한 줄씩: 무엇을 건드리나 · 금액 · 계산값이 갈리나 · 고객 산출물에 무엇이 바뀌나 · 되돌리기 ·
     드는 판 수. **사람이 읽는 말로 · 고르지 않는다.**
4-5  4절 값은 5-1 의 239세션 절에 넣는다(5절 커밋 전에 4절을 먼저 끝낸다).


5절 — 기록 · 닫기

5-1  PROCEED.md 에 239세션 절 — 웹 대화창 판단(결정 1 · 2)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 4절 값.
     세션 목록표에 239 행을 달고 · 빠진 235 행도 다른 행과 같은 꼴로 단다.
5-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다. 미해결 건수 대조를 이름으로. 이름에 「 · 」 를
     넣지 않는다.
5-3  docs\HANDOVER.md — 4절 ㄹ · ㅁ 행을 이 판 값으로 · 5절에 웹 대화창 판단 두 행(결정 1 · 2) · 머리
     사슬에 S239 단락 · 2절에는 pytest 건수 · 초록만.
5-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(겹침 거르기 고침 ·
     개발 말과 그물 · 이 판이 고치지 않고 값으로 낸 자리).
5-5  CLAUDE.md 는 고치지 않는다.
5-6  이 지시서 본문을 docs\directives\S239.md 로 그대로 앉힌다.
5-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S239 5절: 기록 · 뿌리 진행표 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-4 자리 몫과 그 밖 0줄).
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를
    따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「0원 거르기 마무리 표」 — 결정 1 · 2 마다 벌 · 조합 · 비움 / 넣음 전 → 후.
  그 뒤에 「겹침 거르기」(4절) — 증상 · 이력(고친 것 / 남은 것) · 남은 자리와 잣대 · 걸린 결정 · 판정(서는
    자리 / 안 서는 자리) · 안 서는 자리의 선택지 표. 사람이 읽는 말로. 코드 자리는 꼬리표로만.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시계를 보고) · 백그라운드 셸 확인 결과.
