S241 — 고침 판 · 「ㅁ 겹침 거르기」 남은 29줄 · 뿌리 「ㅂ 개발 말과 그물」 가운데 세션 번호와 내부 경로를 고친다

**지금 어디를 잡고 있나.** 뿌리 진행표(docs\HANDOVER.md 4절) — 절사 · 두 수 · 자릿수 · 0원 거르기는
닫혔고 · 「ㅁ 겹침 거르기」 는 S240 이 고쳐 계약 하한 문장 29줄이 남았다 · 「ㅂ 개발 말과 그물」 은 S240
이 쟀다. 이 판은 **ㅁ 의 남은 29줄을 닫고 · ㅂ 가운데 세션 번호와 내부 경로를 고친다.** 말투와 「우리」 는
사람 결정 대기라 이 판 몫이 아니다(0줄). 이 밖으로 가지 않는다 — 다른 뿌리 · 도구 · 측정은 이 판 몫이
아니다.

**사람이 정했다(HANDOVER 5절 · 브리핑 「다시 열지 마라」 가 쥔다 · 다시 적지 않는다 · 가리키기만).**
절사 A · 단위 기준 · 측정 중단 · 도구 판은 S230 으로 끝 · CLAUDE.md 는 규칙이 바뀔 때만 고친다 · 뿌리
작업은 다른 갈래로 빠지지 않는다 · S207 · 운영시간 외 부하는 비중(S238).

**웹 대화창이 정했다(S240 뒤 · 이 판이 HANDOVER 5절에 행으로 앉힌다).**
  결정 1 (S240 결정 2 를 고친다) — 앞머리를 뗀 글자가 같고 같은 사실 ID 인 줄이 조합이 아니라 수단(계약
    전력 등)에 대한 말이면, 일부 조합에만 서도 Excel 요약 조합 묶음에 한 번만 앞머리 없이 세운다. 앞머리를
    떼도 글자가 다르거나(조합마다 값이 다르면) 조합에 대한 말이면 그대로 둔다. S240 결정 2 행에 곁 줄을
    단다 — 옛 글자는 고치지 않는다.
  결정 2 — 고객 산출물(PPT · Word · Excel · 일괄 생성 · 부록)과 화면에서 세션 번호를 지운다. 잣대는 CLAUDE.md
    「화면 문구」 절 감사 규칙의 「내부 문서 번호」 다(기록된 잣대 · S240 4-4 판정).
  결정 3 — 고객 산출물에서 내부 경로(저장소 폴더 · 파일 경로)를 지운다. 경로가 문장의 뜻 전부면 그
    문장을 빼고(배경 · 근거는 매뉴얼 몫 · S207), 경로 밖에 뜻이 남으면 경로만 뺀다. 화면은 이 판 몫이 아니다
    — 세기만 한다.
  결정 4 — 다시 서지 않게 산출물 실물 글자를 무는 못(시험)으로 박는다. 그물 도구(screen_audit 등)는
    고치지 않는다.

**S240 보고가 적었다 — 실물에서 먼저 확인하라.** 대형 벌은 「선택요금 전환」 조합에만 계약 하한 문장이
안 서서 나머지 조합마다 되풀이된다(29줄) · 미해결 「여지가 없는 계약전력 판정 문장이 …」 가 남아 있다 /
덱 19벌에서 세션 번호 95줄(예 「(14세션에 바로잡았습니다)」) · 내부 경로 57줄(예 「data\weather\」) /
감사 도구에 세션 번호 규칙이 없고 경로는 못 잡는다 / 계산 폴더에도 그 문자열 자리가 있다(diagnose\dr ·
pv\weather · pv\archive · measures\solar · base · contract).

**울타리에 하한이 있다** — 1-5 가 센 「고칠 자리 수」. 결정 1 ~ 4 를 끝까지 적용하는 데 필요한 번짐은
울타리 안이다 — 자리와 까닭을 적고 고친다.

이 판의 못 박힌 안전한 쪽.
  · **회귀값 여덟 · 요금 총액 · 카드 절감액 · 조합 절감액 · 권장안 선택 · 케이스 스터디 타당성은 한
    자리도 안 움직인다.** 움직이면 멈춘다.
  · **갈리는 글자는 1-2 ~ 1-4 목록 안뿐이다.** 목록 밖 글자가 갈리면 멈춘다.
  · 경로 밖에 뜻이 남는 문장은 문장째 빼지 않는다. 세션 번호 꼴이지만 개발 말이 아닌 쓰임은 지우지 않고
    값으로 낸다.
  · **새 글자를 짓지 않는다.** 지운 자리의 앞뒤를 잇는 조사 · 구두점만 다듬고 새 사실을 더하지 않는다.
  · **말투(반말)와 「우리」 는 0줄** — 그 글자가 이 판이 고치는 줄에 함께 있어도 그대로 둔다.
  · 코드 주석 · 독스트링 · 시험 · 기록 문서의 세션 번호는 산출물이 아니다 — 건드리지 않는다.
  · **계산 폴더 여섯(tariff · measures · diagnose · compare · pv · quality)은 1-5 가 센 자리 밖 0줄.**
    요금 · 절감액을 만드는 식은 0줄 — 고객에게 나가는 문자열만.
  · **그물 도구(tools\*.py)는 0줄.**
  · 화면 문구(글자)는 늘리지 않는다. CLAUDE.md 「화면 문구」 절이 걸리면 그대로 따른다.
  · 옛 표기를 문 시험이 빨개지면 시험을 고친다(전 · 후). 회귀값 여덟을 문 시험은 고치지 않는다.
  · **CLAUDE.md 0줄.** 소요 · 깨끗한 판 · 전원 · 남의 python 을 재거나 적지 않는다.
  · 이력 · 결정 주인은 저장소(PROCEED.md · docs\ · CLAUDE.md · collaboration.md · git log)에서 찾는다
    — 없으면 「기록 없음」. 이미 기록된 결정을 다른 자리에 다시 적지 않는다 — 가리키기만 한다.
  · **센 값만 적는다. 벽시계는 시각을 찍는 셸 명령의 출력을 보고 그 값만 옮긴다. 커밋 메시지는 편집
    도구로 지은 .txt 로 git commit -F.** 절마다 따로 커밋한다.
  · 세션 도중 사람에게 묻지 않는다.
  · **pytest 는 지시서에 적힌 명령 꼴만 쓴다. 지시서가 꼴을 적지 않은 판(못 하나 · 되돌려 확인 · 넓힌
    못 한 판)도 3-1 꼴 그대로 — `-rf --tb=no` 밖 플래그를 붙이지 않는다.**
  · 전체 pytest 갈래가 메모리 부족으로 끊기면 스스로 다시 돌리지 않고 플래그도 바꾸지 않는다. 거기서
    멈춰 3절까지 커밋하고(push 하지 않는다) · 끊긴 자리(수집 몇 건 가운데 몇 건째 · 그때까지 빨강 수)를
    보고한다.
  · git stash 를 쓰지 않는다. heredoc · python -c · Set-Content · Out-File 을 쓰지 않는다. **파이프를
    쓰지 않는다 — 출력을 버리는 `| Out-Null` 도 파이프다(S240 이 썼다).** 파일을 볼 때는 읽기 도구로
    본다. **조사 에이전트를 띄우면 이 금지를 글자 그대로 전한다(S240 에이전트가 sed | cut 을 썼다).**


0절 — 개시

0-1  어느 PC 인지(open_values) · git pull · HEAD 와 origin(S240 보고는 d3a6b58) · 워크트리.
0-2  미해결 건수(143) · 회귀값 여덟 · 덱 스냅 기준선(S240 뒤 스냅 — 없으면 이 절에서 HEAD 로 뜬다).

커밋: S241 0절: 개시


1절 — 고칠 자리를 못 박는다 (찾을 것을 먼저 적고 한 판에 뽑는다)

1-1  **기록** — CLAUDE.md 「화면 문구」 절 감사 규칙 · S207 · S240 결정 2 · collaboration.md 「배경 · 근거 ·
     제도 설명은 매뉴얼로」 를 글자 그대로 · 결정 주인. 결정 1 ~ 4 와 어긋나는 글자가 있는지.
1-2  **계약 하한 문장(결정 1)** — 29줄 전수 · 앞머리를 뗀 글자 · 사실 ID · 서는 조합 / 안 서는 조합 · 문장이
     수단에 대한 말인지 조합에 대한 말인지(글자 그대로 옮겨 가른다) · 접힌 뒤 글자(예정) · 미해결 「여지가
     없는 계약전력 판정 문장이 …」 의 몸과 이 29줄의 관계.
1-3  **세션 번호(결정 2)** — 산출물 네 갈래 · 부록 · 화면의 세션 번호 꼴 전수(S240 이 95줄로 셌다 — 다시
     센다) · 줄마다 만드는 코드 자리 · 개발 말이 아닌 쓰임은 따로 · 지운 뒤 글자(예정) · 문장째 빠지는 줄.
1-4  **내부 경로(결정 3)** — 고객 산출물 전수(S240 57줄) · 만드는 자리 · 줄마다 문장의 뜻이 경로뿐인가 ·
     빠지는 문장의 내용을 매뉴얼이 쥐는가 · 지운 뒤 글자(예정). 화면의 내부 경로는 수만 센다.
1-5  **고칠 자리 수를 값으로 못 박는다** — 코드 자리(계산 폴더 몫 따로) · 시험 자리. 울타리이고 하한이다.

커밋: S241 1절: 고칠 자리


2절 — 고친다

2-1  **1-5 가 센 자리를 고친다.** 결정 1 ~ 3 그대로. 자리마다 고친 줄 수와 전 · 후.
2-2  적게 닫았거나 번졌으면 자리마다 까닭을 값으로.

커밋: S241 2절: 겹침 29줄 · 세션 번호 · 내부 경로


3절 — 못 · 실물 · 회귀 (이 차례대로)

3-1  **좁은 확인 먼저** — 이 판이 고친 파일을 무는 시험 파일만 아래 꼴로 돈다(고른 파일과 까닭을
     적는다). 화면 시험 파일과 기온 기준선 시험 파일이 함께 들면 두 명령으로 가른다. 미해결 「화면
     시험과 한 프로세스에서 돌면 기온 기준선 못이 빨개지는 까닭」 의 두 건만 빨가면 그 까닭을 쫓지
     않고 3-2 로 간다 — 3-8 이 본다. 그 밖이 빨가면 고친 뒤 이 좁은 판을 다시. 초록일 때만 3-2 로.

         .venv\Scripts\python.exe -m pytest <고친 파일을 무는 시험 파일들> -rf --tb=no

3-2  **덱 몇 벌 먼저** — 결정 1 ~ 3 이 다 서는 벌을 가장 적게 골라(고른 벌과 까닭) 뜬다. **산출물
     넷(PPT · Excel · Word · 일괄)을 통째로 S240 뒤 스냅과 맞대** 갈린 줄이 전부 1-2 ~ 1-4 목록 안인지
     본다. 옛 코드 덱은 238세션 절의 길로 뜬다. 빨가면 고친 뒤 그 벌만 다시. 초록일 때만 3-3 으로.
3-3  못(결정 4) — 있는 못을 넓히거나 새 못으로 (1) 「산출물 실물 글자에 세션 번호 꼴이 0」 (2) 「고객
     산출물 실물 글자에 내부 경로 꼴이 0」 (3) 「Excel 요약 조합 묶음에서 수단에 대한 같은 사실은 한 번」
     을 문다. 화면 세션 번호는 있는 화면 시험에 넓힌다. 산출물을 실제로 만든 글자를 본다. 되돌려
     확인한다(스크래치 monkeypatch · plugin) — 되돌림이 섰는지 값으로 먼저 보고 빨간 것을 본다.
3-4  **덱 19벌 한 판** — S240 뒤 스냅과 맞댄다. 갈린 자리가 전부 1-2 ~ 1-4 목록 안인지 · 세션 번호 꼴과
     내부 경로 꼴이 19벌 다 0 인지. 금액 칸이 갈리면 멈춘다.
3-5  케이스 스터디 한 판 — 타당성 · 회귀값 여덟이 0-2 와 같은지. 갈린 칸은 이름으로(소요 칸과 1-5
     자리 밖이 갈리면 멈춘다).
3-6  CLAUDE.md 「화면 문구」 절이 걸리면 screen_audit 전 · 후.
3-7  `git diff --numstat` 으로 계산 폴더 여섯 — 1-5 자리 밖 0줄을 값으로 · 폴더마다 · tools\ 0줄.
3-8  **전체 pytest 한 판** — 2번 PC 면 PROCEED.md 「pytest 분할 실행」 네 명령 그대로 · 하나씩 차례로.
     1번 PC 면 아래 명령 그대로(회귀 동안 사람이 site-energy 를 멈춘다). 그 밖의 플래그를 더하지 않는다.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     pytest_counts 로 센다. 코드 탓으로 빨가면 고친 뒤 3-1 부터 다시 — 전체는 마지막에 한 판 더.
3-9  정적 검사 다섯 — 저장소 기준.

커밋: S241 3절: 못 · 실물 · 회귀


4절 — 기록 · 닫기

4-1  PROCEED.md 에 241세션 절 — 웹 대화창 판단(결정 1 ~ 4)과 사람 결정(가리키기만)을 갈라서 · 1 ~ 3절 값.
     세션 목록표에 241 행.
4-2  docs\OPEN_ITEMS.md — 이 판이 닫은 항목만 지운다(여지가 없는 계약전력 판정 문장 항목이 닫혔으면
     그것 · 고객 산출물의 세션 번호 · 내부 경로 · 반말 항목은 남는 몫(말투 · 「우리」 · 화면 경로 · 한글 없는
     칸)으로 이름을 고친다). 미해결 건수 대조를 이름으로. 이름에 「 · 」 를 넣지 않는다.
4-3  docs\HANDOVER.md — 1절 값(HEAD · 미해결 · pytest · 케이스 · 화면 감사 · 정적 검사)을 이 판 값으로 ·
     4절 ㅁ · ㅂ 행 · 5절에 웹 대화창 판단 행(결정 1 ~ 4 · S240 결정 2 행에는 곁 줄) · 머리 사슬에 S241
     단락 · 2절에는 pytest 건수 · 초록만.
4-4  PROCEED.md 「현재 상태」 「다음 작업」 칸 — 후보를 이름으로만 · 고르지 않는다(개발 말과 그물 가운데
     말투와 「우리」 — 사람 결정 대기 · 이 판이 고치지 않고 값으로 낸 자리 · 여지가 없는 수단이 조합의
     마지막 줄인 경우).
4-5  CLAUDE.md 는 고치지 않는다.
4-6  이 지시서 본문을 docs\directives\S241.md 로 그대로 앉힌다.
4-7  기록을 다 고친 뒤 기록 묶음 — 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-8  커밋 · git push. HEAD = origin · 워크트리 깨끗 · 이 판이 띄운 백그라운드 셸 0.

커밋: S241 4절: 기록 · 뿌리 진행표 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목, 그 밖의 서술은 덧붙이지 않는다. 표 칸은 30자 안쪽.
**대조표는 항마다 한 줄 — 여러 항을 묶지 않는다.** 대조표 줄 수가 지시서 항 수와 같은지 끝에 적는다.
소요 · 깨끗한 판 · 전원을 적지 않는다.

  1 대조표 — 울타리(하한)와 고친 수 · 멈춘 자리 / 안 멈춘 자리 · 마지막 줄에 HEAD = origin · 워크트리.
  2 바꾼 파일 — +/− · 계산 폴더마다 줄 수(1-5 자리 몫과 그 밖 0줄) · tools\ 0줄.
  3 수치가 움직인 곳 — 산출물 글자가 바뀐 자리 수 · 회귀값 여덟 · 총액 · 케이스 갈린 칸 · pytest 수.
  4 판단한 것
  5 돌린 것 / 안 돌린 것 — 좁은 pytest 판 수 · 덱 몇 벌 판 수 · 덱 19벌 판 수 · 전체 pytest 판 수를
    따로. 다시 돈 판은 까닭을 한 줄씩.
  6 예상과 달랐던 것 — 지시서 꼴 밖으로 나간 것이 있으면 여기 적는다(없으면 없다고).
  7 재업로드 — CLAUDE.md · collaboration.md · project-overview.md 셋만 본다.
  8 못 — 이름 · 되돌려 빨간 것.

  그 뒤에 「뿌리 진행표」 그대로.
  그 뒤에 「개발 말 고침 표」 — 자리(계약 하한 29줄 · 세션 번호 · 내부 경로)마다 전 → 후 줄 수 · 문장째
    뺀 줄 수 · 고치지 않은 줄과 까닭 · 화면 경로 수.
  그 뒤에 「사람이 정할 것」 — 없으면 없다고. 있으면 사람이 읽는 말로 · 코드 자리는 꼬리표로만.

마지막에 벽시계(절마다 · 시각 명령 출력대로) · 백그라운드 셸 확인 결과.

---

S241 보정 — 3-4 부터 이어서 돈다

· 다시 돌려도 된다. 차례는 아래 그대로 — 새 못 하나 → 3-4 → 3-5 → 3-6 → 3-7 → 3-8 → 3-9(남은 mypy ·
  scan_ctrl) → 4절 → 5절.
· 새 못 하나(3-3 보강) — 기상 폴백 문구에 경로가 없는지를 문다. 폴백을 세우는 기존 픽스처나 monkeypatch 가
  있으면 넓히고, 없으면 새 못 하나. 일괄 생성이 실제로 만든 글자를 본다. 되돌려 빨간 것을 값으로 본다.
  돌리는 꼴은 3-1 꼴 그대로(`-rf --tb=no` 밖 플래그 0).
· 3-4 덱 19벌은 처음부터 뜬다(끊긴 판은 스냅이 없다). 끊긴 판은 5절 5항 판 수에 넣고 까닭을 한 줄로.
· 3-4 가 또 메모리 부족으로 끊기면 곧바로 다시 돌지 않는다. 이미 스냅이 선 벌은 두고, 남은 벌만 같은
  도구 · 같은 옵션으로 나눠 뜬다(한 명령에 몇 벌인지 · 나눈 까닭을 적는다). 도구가 벌을 골라 뜰 수 없거나
  나눠 뜬 판도 끊기면 거기서 멈춰 보고한다(push 하지 않는다).
· 전체 pytest 갈래가 끊기면 지시서 그대로 — 다시 돌지 않고 멈춰 보고.
· 돌리기 전에 이 판이 띄운 python 이 남아 있으면 내린다. 남의 python 은 건드리지도 · 재지도 · 적지도
  않는다. 파이프 · `| Out-Null` 금지는 그대로다.
· 끝까지 초록이면 지시서대로 4절 기록 · push → 5절 보고. 이 보정문도 docs\directives\S241.md 에 본문
  뒤에 붙여 앉힌다.
