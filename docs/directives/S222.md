S222 — 전력삼각형을 가로·세로 같은 눈금으로 그린다 · 오늘 결정을 기록에 앉힌다

**이 판은 그림 판이다. 금액 0 이다.** 그리는 값을 안 바꾼다.

**사람이 정했다 — S221' 마감 뒤에 받은 셋. 저장소에 아직 없다.**
  ① **전력삼각형은 가로 · 세로를 같은 눈금(1:1)으로 고정한다.** 역률 100
     벌의 세로 범위 문제가 이것으로 함께 풀리는지 값으로 본다. **안 풀리면
     범위는 고치지 않고 값만 낸다.**
  ② **이 판(S222)은 전력삼각형 판이다.** 그 뒤 차례는 정하지 않았다.
  ③ **1번 PC 에서 kWise 회귀가 도는 동안 site-energy 를 사람이 멈춘다.**

**이 판이 닫을 미해결 — 수와 판정은 다시 잴 것이다.**
  「역률 100 벌에서 전력삼각형이 삼각형이 아니다」
  「전력삼각형의 x·y 축 비율이 안 맞아 각이 거짓말을 한다」

**앞에 정한 규칙은 그대로 선다** — 표시 규칙 (가)(나)(다) 와 HANDOVER 5절
전부. 세션 도중 사람에게 묻지 않는다.

**울타리에 하한이 있다.** 1절이 센 「고칠 자리 수」 다(코드 자리 · 실물
자리). 적게 닫으면 자리마다 왜 못 했는지 값으로 낸다.

이 판의 못 박힌 안전한 쪽 열넷.
  · **값을 만드는 줄을 안 고친다.** 유효 · 무효 · 피상전력과 역률 값, 그림이
    그리는 점 · 선 · 계열 · 그림 종류를 안 바꾼다. **바꾸는 것은 축의 눈금
    비율(같은 눈금)과 그에 따라 스스로 정해지는 축 범위뿐이다.**
  · **눈금 서식(자릿수 · 「-0」)은 ㄷ 자릿수 뿌리다 — 안 고친다.** 1:1 로
    바뀌면서 새로 서거나 사라지는 눈금은 세어 적기만 한다.
  · **범위를 손으로 박지 않는다.** 1:1 로도 역률 100 벌 세로 범위가 안 풀리면
    멈추지 말고 그 벌의 전후 범위 · png 를 값으로 내고 이름으로 둔다.
  · **새 안내 · 새 각주 · 새 문구를 세우지 않는다** — 역률 100 벌에서 삼각형이
    납작한 선이 되는 것을 글로 설명하지 않는다. 화면 예산은
    `tools\screen_budget.py` 가 쥔다. 넘으면 멈춘다.
  · **한 그림을 화면 · PPT · Word 가 함께 쓰면 한 자리에서 고친다.** 산출물마다
    따로 고치지 않는다. 화면이 다른 도구로 그리면 그 자리도 같은 규칙으로 —
    **같은 사실(1:1)을 정하는 자리를 산출물 수만큼 따로 두지 않는다.**
  · **금액이 한 원이라도 움직이면 멈춘다.** 회귀값 여덟 · 케이스 판정 174 ·
    덱 19벌 그림 밖 수치 조각 전수(앞 판 뒤 기준 14,183 — 다시 잴 것)가
    다 불변이어야 한다.
  · **그림이 잘리거나 글자가 겹치거나 장 밖으로 나가면 멈춘다** — 1:1 로
    그림 모양이 크게 바뀔 수 있다. png 로 본다.
  · **「멈춘다」 가 서면 반드시 멈춘다.** 멈춘 자리와 안 멈춘 자리를 대조표에
    각각 한 줄로. 멈출 일이 없었으면 없었다고 적는다.
  · **원인을 미리 적지 않았다.** 앞 판 보고와 브리핑의 수는 전부 다시 잴 것이다.
  · **실물을 본다 — 이 판은 png 가 몸통이다.** 전후 png 를 나란히 본다. 화면
    캡처 스팟이 못 찍는 자리는 「못 봤다」 로 적는다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. 되돌림이 실제로
    섰는지 값으로 먼저 본다. 출력이 잘려 확인이 안 섰으면 확인 안 한 못이다.
  · **지시서에 없는 pytest 플래그를 붙이지 않는다.** 전체 pytest 는 한 판이
    기본이고, 빨갛게 끝나면 고친 뒤 같은 명령으로 전체를 한 판 더.
  · **git stash 를 쓰지 않는다. S140 스태시를 지우지 않는다.**
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    파일은 편집 도구로만 고친다. 재는 것 · 세는 것 · 긴 출력 자르기는
    `tools\run_tool.py --tail` · `tools\pytest_counts.py`(인자 하나씩) ·
    `tools\deck_words.py`(온 경로 · `.py` 를 `run_tool` 에 직접 주지 않는다)로.
    **파이프를 썼으면 쓴 자리마다 「무엇을 보려고 · 왜 도구로 못 했나」 를
    적는다.** 미해결은 이름으로 떠 읽는다 — 칸을 통째로 흘리지 않는다.
    커밋 메시지는 편집 도구로 지은 `.txt` 로 `git commit -F` 로 단다.
    **덱 19벌 뜨기는 많아야 두 판.** 앱을 띄우면 포트를 적고 내린 뒤 닫힌
    것을 확인한다. **벽시계를 절마다 잰다.**


0절 — 개시

**브리핑 전문을 뜨지 않는다** — 머리만 본다. 아래를 원본에서 직접 뜬다.
**항목마다 「어느 파일 어디서 왔는지」 를 함께.**

0-1  **PC** — `os.cpu_count()` · RAM · `-n auto` 가 고른 일꾼 수(스크래치
     `.py` 로 뜬다). **1번 PC 면 4-2 는 통째 · 남의 python 은 셋(판 앞 ·
     회귀 앞 · 뒤). 2번 PC 면 4-2 는 갈래 넷(`PROCEED.md` 「pytest 분할
     실행」) · 남의 python 은 넷(갈래 사이 포함) · 소요는 그 PC 앞 판들과만.**
0-2  저장소 — `git pull` · HEAD 와 origin · 워크트리 · `git stash list`.
     **스태시는 PC 로컬이다** — 1번 PC 면 S140 스태시를 이름과 날짜로 적고
     안 지운다. 2번 PC 면 비어 보이는 것이 맞다.
0-3  남의 python 을 센다(`CLAUDE.md` 2항). 센 자리마다 값과 어느 프로젝트인지.
0-4  미해결 — 건수 · 갈래 다섯마다의 수 · 칸 글자 수 · 세 방법 검산.
0-5  **이 판이 닫을 두 항목**이 미해결에 어떤 글자로 · 어느 갈래에 서 있는지
     그대로 옮겨 적는다.
0-6  회귀 기준선 — 수집 · passed · xfail · skip · 화면 감사 네 수 · 케이스
     판정 수 · 정적 검사 다섯. **기록에서.** 그 PC 의 마지막 깨끗하고 플래그
     없는 판 값을 나란히(1번 PC 는 S218).
0-7  확인 안 한 못 — 수 · 못 총수 · 딱지가 어느 판 것인지.
0-8  기상 캐시에 용인 벌이 있는지. **캐시는 PC 로컬이다 — 없으면 케이스 절
     앞에 한 판 받아 채운다.**
0-9  **덱 스냅 기준선** — 그 PC 캐시에 앞 판이 넓힌 그물로 뜬 스냅이 있고
     그 뒤 코드가 안 바뀌었으면 쓴다(어느 시점 것인지 적는다). 없거나 낡았으면
     저장소 판을 한 판 뜬다. **이 판의 덱 판 수 · pytest 판 수를 미리 적는다.**
0-10 **브리핑 · HANDOVER 에서 재게 할 것 다섯을 값으로 본다 — 고치는 것은 6절이다.**
       ㄱ S221 요약의 「덱 19벌 갈린 줄 그림 밖 57 · 안 114 = 울타리」 와 S221
         보고의 「울타리 그림 안 38 · 밖 38 · 실물 76」 — 무엇을 센 수인지
         (줄 · 자리)와 어느 쪽이 기록에 서 있는지. 한 줄로 판정한다.
       ㄴ 미해결 「「다음 작업」 칸의 판치 둘이 상한 3,000자를 넘는다」 — 지금 칸
         글자 수와 여유. 조건이 떨어졌으면 닫을 수 있다고 적는다.
       ㄷ 미해결 「요금 청구서의 12개월 환산 BillingResult.annualize() 를
         src\ 에서 부른 적이 없다」 와 HANDOVER 5절 S216 행 · S219 판정 근거가
         「annualize 를 지나는지」 를 잣대로 쓰는 것 — 두 기록이 같은 사실을
         달리 말하는지. **코드에서 12개월 환산을 실제로 하는 자리를 값으로 짚는다.**
       ㄹ HANDOVER 1절 HEAD 행 · 제목 · 머리가 S221' 을 담는지.
       ㅁ HANDOVER 7절의 1번 PC 소요 예시가 지금 기준(S218)과 맞는지.
0-11 「다음 작업」 칸 — 글자 그대로 · 글자 수 · 여유. 사람이 정한 것과 다르면
     다르다고 적는다. 사람이 이긴다.
0-12 정적 값이 이 판 지시서를 앉히기 전 값인지 뒤 값인지. 통째로 잰 값으로.
0-13 **이 개시 절을 값으로 평가한다** — 못 뜬 것 · 안 쓰인 것 · 없어서 아쉬웠던
     것 · 소요와 읽은 글자 수. 앞 판과 PC 를 함께 적어 맞댄다.
     **저장소에 앉히지 않는다 — 열다섯 판째 시험이다.**

커밋: S222 0절: 개시


1절 — 전력삼각형을 센다

**이 절은 세고 판정만 한다. 고치지 않는다.**

1-1  **전력삼각형이 서는 자리를 네 산출물에서 전수로 센다** — 어느 산출물
     어디 · 어느 벌에서 뜨나 · **그리는 코드 자리와 도구** · 같은 그림을
     쓰는 다른 산출물. 꼴로 묶어 낸다.
1-2  **자리마다 지금 눈금 비율을 값으로 낸다** — 가로 1 단위와 세로 1 단위가
     그림 위에서 몇 대 몇으로 그려지나 · **그려진 빗변의 각과 그 벌의 역률각
     (arccos(역률))이 얼마나 다른가.** 역률 100 벌 · 역률이 낮은 벌 · 간주
     92% 벌을 각각 넣는다.
1-3  **역률 100 벌의 세로 범위를 값으로 낸다** — 지금 범위 · 눈금 글자 ·
     무효전력 값. **1:1 로 그렸을 때 범위가 어떻게 되는지 스크래치로 먼저
     재 본다 — 저장소는 안 고친다.**
1-4  **고칠 자리 수를 값으로 못 박는다 — 코드 자리와 실물 자리.** 그것이
     2절의 울타리이고 하한이다. **같은 사실(1:1)을 정하는 자리가 몇 곳이 되는지**
     함께 적는다.

커밋: S222 1절: 전력삼각형


2절 — 고친다

2-1  **기준선** — 0-9 가 정한 스냅 · 화면 감사 네 수 · **고칠 그림의 전 png**
     (역률 100 벌 · 낮은 역률 벌 · 간주 92% 벌 · 화면 · PPT · Word).
2-2  **1-4 가 센 자리만 고친다.** 자리마다 고친 줄 수와 전·후를 적는다.
     울타리를 넘는 자리가 보이면 멈추고 값을 낸다.
2-3  **1-2 를 다시 잰다** — 눈금 비율 1:1 · 그려진 각 = 역률각인지 벌마다.
2-4  **역률 100 벌의 세로 범위를 다시 잰다** — 풀렸나(무의미한 −0.04 ~ 0.04
     꼴이 사라졌나). **안 풀렸으면 범위를 손으로 박지 않고 값과 png 를 낸다.**
2-5  **수치 조각을 값으로 확인한다** — `deck_words --diff` 로 저장소 판과
     맞댄다. **그림 밖은 차 0.** 그림 안에서 눈금 글자가 서거나 사라진 것은
     전수로 적는다 — 값이 아니라 눈금 위치가 바뀐 것인지 가른다.
2-6  **후 png 를 전 png 와 나란히 본다** — 잘림 · 겹침 · 장 밖. 화면 예산을
     전·후로. 넘으면 멈춘다.
2-7  1-4 보다 적게 닫았으면 자리마다 까닭을 값으로.
2-8  **되돌려 확인한다** — 되돌리면 1:1 이 깨지고 각이 다시 어긋나는지.
     되돌림이 실제로 섰는지 값으로 먼저 본다.

커밋: S222 2절: 전력삼각형 같은 눈금


3절 — 못

3-1  **1:1 을 무는 못을 박는다** — **그려진 그림**(그림 객체의 눈금 비율이나
     그려진 빗변 각)을 보는 못. 소스 리터럴을 찾는 못은 안 박는다. 화면 ·
     PPT · Word 가 한 그림을 쓰면 한 못으로 문다. **이미 있는 못을 넓혀
     닫히면 새 못을 박지 않는다.**
3-2  **박은 못마다 되돌려 확인한다.** 확인 안 한 못은 확인 안 했다고 적는다.
3-3  새 못의 소요. 합이 20초를 넘으면 줄일 수 있는지 값으로.
3-4  0-7 이 센 확인 안 한 못 가운데 이 판 자리를 무는 것을 되돌려 확인한다.
     앞 판 딱지를 걷는다. **걷은 딱지 몫과 새 못 몫을 갈라 적는다.**
3-5  xfail 이 어떻게 됐는지. 새로 빨개지면 멈춘다 — xfail 을 새로 달지 않는다.
3-6  되돌림이 원복됐는지 `git status` · `git diff`.

커밋: S222 3절: 못


4절 — 회귀

4-1  `git diff` 로 **계산 폴더 여섯 가운데 갈린 것**을 폴더마다 한 줄로.
     값을 만드는 줄이 0 인 것을 값으로 보인다.
4-2  pytest — 0-1 이 정한 대로. **1번 PC 는 아래 명령 그대로 · 플래그를 더하지
     않는다.**

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no --durations=0

     **회귀 앞에 남의 python 을 센다 — 사람이 site-energy 를 멈추기로 했다.
     0 이 아니면 기다리거나 죽이지 말고 그 사실만 적고 돈다.**
     `run_tool` 로 받고 `pytest_counts.py` 로 센다 — `--base` 에 0-6 의 수집
     건수. skip 수를 함께. **빨갛게 끝나면 고친 뒤 전체를 한 판 더.**
4-3  소요 — PC · 일꾼 수 · 0-3 의 값 전부 · 깨끗한 판인지 · 상한 8분까지.
     **그 PC 의 앞 깨끗하고 플래그 없는 판들과만 맞댄다.**
4-4  화면 감사 — 네 수를 0-6 과 맞댄다. 갈렸으면 자리마다 글자까지.
4-5  케이스 스터디 — 판정 수와 회귀값 여덟 · 기상 취득 횟수. 같은 날 앞
     판 파일이 있으면 스크래치에 낸다. 칸 단위로 맞대고(fillna 뒤에 센다)
     소요 밖이 갈렸으면 멈춘다.
4-6  정적 검사 다섯 — 0-12 기준과 맞는지.

커밋: S222 4절: 회귀


5절 — 기록 · 닫기

**`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.**

5-1  `PROCEED.md` 에 222세션 절을 쓴다.
5-2  **미해결 건수 대조를 이름으로** — 늘어난 것 · 줄어든 것. 이름에 「 · 」
     를 넣지 않는다. 갈래별 수를 앞·뒤로. **이 판으로 닫힌 전력삼각형 두
     항목은 지운다.** 덜 닫혔으면 남은 몫을 이름으로 남기고 **사람이 이미
     정한 것이므로 결정을 기다리는 갈래에 두지 않는다.**
5-3  **0-10 의 다섯을 처리한다** — ㄴ 은 조건이 떨어졌으면 닫는다 · ㄷ 은
     두 기록이 달리 말하면 틀린 쪽 글자를 값에 맞게 고친다(코드는 안 고친다) ·
     ㄹ · ㅁ 은 HANDOVER 를 고친다 · ㄱ 은 판정만 적는다. **고친 자리와 안
     고친 자리를 갈라 적는다.**
5-4  **사람이 정한 셋(머리 ① ② ③)을 HANDOVER 5절(다시 열지 마라)에 앉힌다** —
     어느 판에서인지(S221' 마감 뒤 웹 대화창 · S222 가 앉힘)와 ① 의 조건을 함께.
     **4절 「S221 이 넘기는 것」 의 전력삼각형 줄에 뒤집혔다는 사실을 한 줄 단다.**
5-5  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
     「지금 · 앞 · 그전 · 그때」 표도 한 칸 민다.
5-6  `docs\HANDOVER.md` 를 이 판 값으로 간다 — 1절 「앞 값」 사슬은 두 판까지 ·
     **머리 사슬에 S221' 과 S222 를 붙인다** · 6절 앞 판 딱지를 걷는다 ·
     **2절은 2번 PC 절이다 — 2번 PC 판이 아니면 고쳐 쓰지 않는다.**
5-7  `CLAUDE.md` — 9항 **이 판을 돈 PC 칸.** 깨끗하고 플래그 없는 판이
     아니면 그 사실을 적고 깨끗한 판 칸은 안 고친다.
5-8  「다음 작업」 칸 — 이름으로만. 다음 판은 사람이 아직 안 정했다 — **후보를
     이름으로만 적고 고르지 않는다**(도구가 없어 셸이 새는 자리 · ㄷ 자릿수
     뿌리 · ㄱ 절사 뿌리). 글자 수와 여유.
5-9  이 지시서 본문을 `docs\directives\S222.md` 로 그대로 앉힌다.
5-10 **기록을 다 고친 뒤에** 기록 묶음을 돌린다. 아래 명령 그대로.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-11 커밋하고 `git push`. HEAD 와 origin 이 같은지 확인한다.

커밋: S222 5절: 기록 · 미해결 대조 · 결정 앉히기 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — 절·항마다 한 줄 · 하나도 빠뜨리지 않는다. 0-3 칸에 센 자리마다
    값과 프로젝트. **0-10 의 다섯을 한 줄씩 판정으로.** 1-2 의 「각 차이」 를
    벌마다 · 1-4 울타리와 2절 고친 수를 나란히 · 2-4 의 「풀렸나」 를 한 줄로.
    「멈춘다」 가 걸린 자리를 멈춘 자리 · 안 멈춘 자리로 갈라서.
  2 만든/바꾼 파일 — +/− 줄 수. 계산 폴더 여섯은 폴더마다. **png 는 전후 짝으로.**
  3 수치가 움직인 곳 — 금액은 없어야 한다. 그림 밖 조각 차 0 · 그림 안 눈금
    글자 증감 · **벌마다 그려진 각 전후와 역률각** · 역률 100 벌 세로 범위
    전후 · 케이스 판정 174 맞대기 · 화면 감사 · 예산.
  4 판단한 것 — 1-4 · 2-4 · 2-7 · 3-1 · 5-3. 권고는 적지 않는다.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패 · 0-9 에 적은 판 수와 실제 ·
    전체 pytest 마지막 판이 초록인지 · 끊겼으면 끊긴 자리와 이어 간 자리.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — 세 파일 중 고친 것과 9항을 고쳤는지.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로 · 소요 · xfail 전후 ·
    확인 안 한 못 전후와 딱지 몫 · 새 못 몫.

  **그 뒤에 「사람이 정할 것」 을 한 표로** — 2-4 가 안 풀렸으면 그 벌 ·
  멈춘 자리가 있으면 그 자리. 없으면 없다고. 권하지 않는다.
  **그 뒤에 「뿌리표」 · 「개시 절 평가」 · 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다 · 끊긴 시간) — **그 PC 의 앞 판들과만
나란히** · 덱과 pytest 가 몇 % 인지. ctx 셋(종전 → 지금 · 가장 크게 먹은 것 ·
줄일 수 있었던 것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
