S203 — 3단계 몫이 2단계 카드의 1.36~2.29배인 까닭을 값으로 가른다

재는 판이다. **계산도 화면도 시험도 0줄이다.**
S202 가 조합에서 켠 역률 수단이 목표를 쓰게 고쳐 16벌 몫이 섰는데,
그 몫이 2단계 역률 개선 카드의 **1.36~2.29배**다. 밑의 차는 S200 이
large-b 에서 452.8M 대 498.6M(1.10배)로 쟀다 — **밑만으로는 그 배수가
설명되지 않는다.** 이 판은 그 차를 분해해 값으로 낸다.
**어느 쪽이 옳은지 정하지 않는다** — 사람이 이 판의 값으로 정한다.
세션 도중 사람에게 묻지 않는다.

이 판의 못 박힌 안전한 쪽 열넷.
  · **src\ 와 tests\ 와 data\ 가 0줄이다.** 계산도 화면도 시험도 안 고친다.
    고쳐야 닫히는 것이 보여도 이름으로 올린다. 고치는 자리는 **기록 파일뿐**
    (PROCEED.md · docs\OPEN_ITEMS.md · docs\HANDOVER.md · CLAUDE.md ·
    docs\directives\S203.md)이고, 그 밖에 한 자리라도 고쳐야 하면 멈추고
    값만 낸다. **울타리는 폴더가 아니라 자리 수다.**
  · **「멈춘다」 가 걸린 자리를 보고에 낸다.** S202 가 4-4 에서 「빨가면
    멈춘다」 인데 안 멈추고 정본 못의 몸을 고쳤다 — 결과는 맞았으나
    울타리가 안 걸렸다(S198 과 같은 모양이다). **이 판은 멈추라는 자리가
    서면 반드시 멈추고, 멈춘 자리와 안 멈춘 자리를 대조표에 각각 한 줄로
    적는다.** 멈출 일이 없었으면 없었다고 적는다.
  · **원인을 미리 적지 않았다.** 「출발점이 갈린다」 도 「밑이 갈린다」 도
    전제가 아니다 — 2절이 값으로 분해한다. 앞 판 보고의 수(1.36~2.29 ·
    452.8M 대 498.6M · 28자리)는 전부 **다시 잴 것**이다. 줄 번호와 이름은
    지금 파일에서 다시 찾는다.
  · **어느 쪽이 옳은지 판정하지 않는다.** 2단계가 옳은지 3단계가 옳은지는
    사람이 정한다. 이 판은 **무엇이 갈리는지와 고치면 무엇이 드는지**만 낸다.
  · 금액이 한 원이라도 움직이면 그 자리에서 멈추고 되돌린 뒤 값을 낸다.
    회귀값 여덟 · 케이스 판정 174 · 감사 네 수도 같다. **이 판은 재기만
    하므로 움직일 까닭이 없다** — 움직이면 무언가 틀린 것이다.
  · 재려고 갈아 끼우는 것은 **스크래치 monkeypatch · plugin** 으로만 한다.
    **얕은 되돌림을 조심한다** — S200 의 ㄱ 흉내가 얕아 sensitivity.py 가
    쥔 감도 길에 안 닿았고 17자리와 28자리가 갈렸다. **갈아 끼움이 어느
    길에 닿고 어느 길에 안 닿는지 값으로 먼저 낸다.**
    src\ 를 돌렸으면 git checkout HEAD -- 경로 로 되돌리고 git status 를 본다.
  · **git stash 를 쓰지 않는다.** S140 스태시(2026-09-07)를 지우지 않는다.
  · **셸에 문자열을 태우지 않는다 — 네 판 연속 샜다**(S199 python -c ·
    S200 here-string 과 파이프 · S201 python -c 와 파이프 · S202
    Measure-Object 둘). **python -c 를 한 번도 쓰지 않는다. 수를 세는 일에
    PowerShell 파이프를 쓰지 않는다 — Measure-Object 도 센다.** 재는 것 ·
    세는 것 · 한 줄 확인 · 버전 찍기까지 전부 스크래치 .py 로 만들어
    .venv\Scripts\python.exe 로 돌린다(시스템 python 을 부르지 않는다).
    커밋 메시지는 스크래치 .txt 에 써서 git commit -F 로 단다.
    **남의 python 을 세는 Get-CimInstance 는 CLAUDE.md 2항이 정한 것이라
    규약 안이다** — 그것만 예외이고 세는 자리에 파이프를 덧붙이지 않는다.
  · 백그라운드로 보내기 전에 예상 소요를 말한다. 근거는 S202 값이다 —
    덱 76판 · 76판 · 38판 · 케이스 두 판. 상한을 걸고, 넘기면 스스로
    멈추고 보고한다. 대기 루프를 만들지 않는다.
  · 앱을 띄운 채 끝내지 않는다. **띄웠으면 포트를 적고 내린 뒤 닫힌 것을
    확인한다**(S202 가 네 판을 그렇게 했다). 케이스 결과 xlsx 는 저장소
    판을 먼저 뜬 뒤에 돌린다(같은 날 덮인다).
  · **권하지 않는다.** 갈래마다 값을 내고, 스스로 본 쪽이 있으면 근거와
    함께 따로 한 줄.
  · 미해결 이름에 「 · 」 를 넣지 않는다.
  · **화면 문구를 이 판에서 짓지 않는다.**


0절 — 판 열기

0-1  어느 PC 인지 확인한다. os.cpu_count() · RAM · -n auto 가 고른 일꾼 수.
     1번 PC 면 pytest 를 통째로, 2번 PC 면 갈래 넷으로.
0-2  git pull. HEAD 와 origin 이 같은지, 워크트리가 깨끗한지.
     git stash list 를 보고 수만 적는다 — 건드리지 않는다.
0-3  남의 python 을 센다. 세는 규칙은 CLAUDE.md 2항이 쥔다 — 센 자리마다
     값을 다 적는다. 0 이 아니면 소요에 「견주는 데 쓰지 않는다」 를 붙인다.
0-4  브리핑을 뜬다. 미해결 건수 · 미해결 칸 글자 수 · 「다음 작업」 칸 글자 수.
     **지금 값을 잰다 — 앞 판 보고의 수를 옮기지 않는다.**
0-5  기상 캐시에 용인 벌이 있는지 본다. PROJECT_CACHE 가 가리키는 자리를 본다.
0-6  이 판이 건드릴 미해결의 지금 이름을 docs\OPEN_ITEMS.md 에서 그대로
     옮겨 적는다. 이름이 다르면 문서의 이름을 따른다.
     **②-127 계열(케이스 스터디 수단 표의 역률 개선이 그 벌의 역률을 안 읽고
     늘 92→97 로 적고 계산한다)이 지금 어떤 이름인지 찾아 적는다** —
     번호가 밀렸을 수 있으므로 이름으로 찾는다.

커밋하지 않는다.


1절 — 두 수를 다시 잰다

**이 절은 재기만 한다. 고치지 않는다.**

1-1  **덱 19벌 전수로 두 수를 낸다** — 2단계 역률 개선 카드 절감액과
     3단계 조합 속 역률 몫. 벌마다 배수를 값으로.
     **S202 가 낸 1.36~2.29 와 맞댄다** — 같으면 같다고, 다르면 다르다고.
1-2  **벌마다 넷을 함께 적는다** — 그 벌의 역률 입력(%) · 2단계 카드가
     쓰는 출발점(%)과 도달점(%) · 3단계 조합이 쓰는 출발점(%)과 도달점(%).
     **각 값을 어디서 떴는지 자리를 적는다**(어느 함수의 무엇인지).
1-3  **밑을 벌마다 적는다** — 2단계 카드가 쓰는 기본요금과 3단계 조합이
     쓰는 기본요금. 두 밑의 비를 값으로.
1-4  **역률 100 세 벌도 같은 표에 넣는다** — 몫이 0 이므로 배수가 안 서는
     것이 예상이다. 안 서면 안 선다고 적는다.

커밋: S203 1절: 두 수를 다시 잰다


2절 — 배수를 분해한다

**이 절은 재기만 한다. 고치지 않는다.**

2-1  **배수를 곱으로 분해한다.** 벌마다
       배수 = (밑의 비) × (역률 폭의 비) × (남는 몫)
     셋을 값으로 내고, **남는 몫이 1.0 이 아니면 그것이 무엇인지 찾는다.**
     못 찾으면 못 찾는다고 적고 남는 몫의 값을 벌마다 적는다.
     **분해를 다른 꼴로 해야 맞으면 그 꼴을 적고 왜 그 꼴인지 값으로.**
2-2  **역률 폭이 갈리는지 값으로 가른다.** 2단계 카드가 92 에서 출발하는지
     그 벌의 실제 역률에서 출발하는지 **실행 경로로** 본다 — 코드를 읽고
     짐작하지 않는다. 실제로 그 줄을 지나는지, 지난 뒤 값이 무엇이 되는지.
     역률 85 인 벌(large-b-pf85)과 92 인 벌을 갈라 적는다.
2-3  **밑이 갈리는 까닭을 적는다.** 3단계 조합의 밑이 선택요금 전환과
     태양광 재선정을 지난 값이라면 그 단계마다 값을 적는다.
     **이것은 결함이 아닐 수 있다** — 조합은 다시 계산하는 자리이므로
     밑이 다른 것이 옳을 수 있다. 판정하지 말고 값만 낸다.
2-4  **셋 말고 다른 것이 더 있는지 본다** — 초과사용부가금 · 기간 환산 ·
     계절 구분 같은 것이 끼는지. 끼면 이름과 값으로.
2-5  **어느 자리가 「그 벌의 역률」 을 읽고 어느 자리가 92 를 쓰는지
     전수로 센다.** 2단계 카드 · 3단계 조합 · 케이스 스터디 수단 표 ·
     감도 · 산출물 네 곳을 다 본다. 그물을 적고 **그물이 못 보는 것**을
     함께 적는다.

커밋: S203 2절: 배수의 분해


3절 — 고치면 무엇이 드나

**이 절은 재기만 한다. 고치지 않는다.** 스크래치로 갈아 끼워 값만 낸다.

3-1  **갈래를 표로 낸다.** 2절이 찾은 갈리는 자리마다 갈래를 세운다.
     적어도 둘이다 — **2단계를 3단계에 맞춘다** · **3단계를 2단계에 맞춘다.**
     2절이 다른 갈래를 냈으면 함께 세운다.
     갈래마다 여섯 칸 — 무엇을 건드리나 · **고칠 자리 수(파일과 함수
     이름으로)** · 되돌리기 · **금액이 갈리는 폭(최소~최대 · 갈리는 벌 수)** ·
     드는 것 · 모르는 것. **권하지 않는다.**
3-2  **갈래마다 스크래치로 세워 값을 낸다.** **갈아 끼움이 어느 길에 닿고
     어느 길에 안 닿는지 먼저 값으로 낸다** — S200 이 sensitivity.py 를
     놓쳐 17과 28이 갈렸다. 닿는 길을 전수로 적고 시작한다.
3-3  **갈래마다 그 값이 뜨는 자리를 전수로 센다** — 화면 · PPT · Excel ·
     Word 각각. **S202 가 28자리를 셌다 — 지금 값으로 다시 센다.**
     그물을 적고 그물이 못 보는 것도 적는다(S202 가 다섯을 적었다 —
     화면 차트 · 반올림에 묻힌 차이 · 이 벌에 없는 ESS 단계 · 접힌 칸 ·
     같은 값을 두 번 세는 것).
3-4  **갈래마다 빨개지는 시험이 몇인지** 값으로 낸다. 이름으로.
     **S202 가 박은 못 둘과 몸을 고친 못 하나가 어떻게 되는지** 함께.
3-5  **갈래마다 요구사항서를 고쳐야 하는지** 본다. 고쳐야 하면 어느 절
     어느 글자인지 원문을 옮겨 적는다. **이 판에서 안 고친다.**

커밋: S203 3절: 갈래마다 드는 것


4절 — S202 가 안 멈춘 자리를 되짚는다

**이 절은 재기만 한다. 고치지 않는다.**

4-1  **S202 가 고친 정본 못의 몸을 값과 글자로 본다** —
     test_조합의_태양광_전_역률은_원_부하_역률이고_수단의_목표가_아니다.
     **git show 로 고치기 전 몸과 지금 몸을 나란히 뜬다.** 무엇을 물었고
     무엇을 무는지 각각 적는다. 뺐다는 옛 206행의 글자도 옮겨 적는다.
4-2  **지금 못이 정본(docs\REQUIREMENTS_kwise.md 8.1)을 값으로 무는지**
     확인한다. 출발점을 무는지 도달점을 무는지. **소스 글자나 식을 다시
     적는 못인지도 본다.**
4-3  **되돌려 확인한다** — 출발점을 목표로 틀면 빨개지는지. 되돌림이 실제로
     섰는지 값으로 먼저 본다. S202 가 212행이 빨갛다고 적었다 —
     지금 값으로 다시 잰다.
4-4  **S202 가 뺀 옛 206행이 무엇을 지키고 있었는지** 값으로 낸다.
     그 자리를 새 못이 정말 덮는지 확인한다 — 안 덮으면 **덮이지 않는
     것이 무엇인지** 이름으로 올린다. **이 판에서 못을 안 고친다.**
4-5  **경고 xfail 이 왜 안 닫혔는지** 값으로 낸다. S202 가 「조합 경고가
     여전히 원 부하 출발점의 역률을 본다」 고 적었다 — 그 자리가 어디이고
     무엇을 보는지. **3-1 의 갈래 가운데 어느 것이 그것을 닫는지** 함께.

커밋: S203 4절: S202 가 안 멈춘 자리


5절 — 회귀

5-1  git diff 로 **src\ · tests\ · data\ 가 0줄**인지 확인한다. 폴더마다 한 줄.
5-2  pytest. 1번 PC 면 앞단에서 통째로.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     2번 PC 면 갈래 넷으로. 종료 코드로 판정하지 않는다 — 실패 줄과 수집
     건수를 직접 읽고 앞 판(수집 1,783 · 1,779 passed · 4 xfailed · skip 0)과
     맞댄다. skip 수를 함께. **수집 건수를 세는 것도 .py 로 한다.**
5-3  소요를 적는다. PC · 일꾼 수 · 0-3 의 값 전부 · 깨끗한 판인지 ·
     상한 8분까지 얼마 남는지. 깨끗하면 --durations=0 을 붙여 도구 합과
     상위 스물다섯을 낸다. 앞 네 판과 맞댄다 — S199 4분 54초 · S200 5분 18초 ·
     S201 4분 58초 · S202 4분 55초.
5-4  화면 감사. 네 수를 앞 판(955 · 806 · 962 · 807)과 맞댄다.
     **ui\ 가 0줄이므로 불변이어야 한다** — 갈렸으면 멈추고 값을 낸다.
5-5  케이스 스터디. 174/174 과 회귀값 여덟. 기상 취득 횟수.
     **저장소 판을 먼저 뜬 뒤에 돌린다.** xlsx 를 저장소 판과 칸 단위로
     맞대고(fillna 뒤에 센다) 소요 칸만 갈렸으면 되살린다.
     **소요 밖이 갈렸으면 멈추고 값을 낸다.**
5-6  정적 검사 다섯.

커밋: S203 5절: 회귀


6절 — 기록 · 닫기

6-1  PROCEED.md 에 203세션 절을 쓴다. 1·2·3·4절 표는 이 절에 둔다.
6-2  미해결 건수 대조를 이름으로 적는다 — 늘어난 것과 줄어든 것을 각각
     이름으로. 이름에 「 · 」 를 넣지 않는다. 세션 목록표 마지막 행도 이름으로.
6-3  docs\OPEN_ITEMS.md 를 갈래별로 맞춘다. 세 방법 교차 검산 값을 다 적는다.
6-4  **0-6 이 찾은 ②-127 계열 항목의 본문을 이 판 값으로 간다** — 이 판이
     잰 배수와 갈리는 자리를 값으로. **그 항목이 이 판의 발견과 같은
     것인지 다른 것인지 판정하고, 다르면 새 이름으로 따로 올린다.**
6-5  docs\HANDOVER.md 를 이 판 값으로 간다 — 제목의 끝 판 이름 · 3절 표의
     S203 행 · 5절 「다시 열지 마라」 · 6절 못 표. 고친 자리와 안 고친
     자리를 갈라 적는다.
6-6  **S192 뿌리표에 이 뿌리의 칸을 세운다.** S201 6-5 가 「뿌리표는 있으나
     이 뿌리의 칸이 없다」 고 적었고 S202 도 안 세웠다 — 어느 뿌리 줄에
     붙일지 값으로 정하고 적는다. 못 정하면 못 정한다고 적고 안 만든다.
6-7  CLAUDE.md 9항 1번 PC 칸을 이 판 값으로 간다. 5-3 이 --durations=0 을
     붙인 깨끗한 판이 아니면 그 사실을 적고 안 고친다.
6-8  「다음 작업」 칸에 갈래 번호를 쓰지 않는다. 이름으로만. **상한 3,000자를
     넘지 않는지 값으로 확인하고 지금 글자 수를 적는다**(마감에 2,320자였다).
6-9  이 지시서 본문을 docs\directives\S203.md 로 그대로 앉힌다.
6-10 기록을 다 고친 뒤에 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

6-11 커밋하고 git push. HEAD 와 origin 이 같은지 확인한다.
     **커밋 메시지는 스크래치 .txt 에 써서 git commit -F 로 단다.**

커밋: S203 6절: 기록 · 미해결 대조 · 지시서 앉히기


7절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — **절·항마다 한 줄이고 하나도 빠뜨리지 않는다.**
    0-3 칸에 센 자리마다 값을 다 적는다. **2-1 의 분해는 벌마다 한 줄씩.**
    **「멈춘다」 가 걸린 자리를 각각 한 줄로 적는다** — 멈춘 자리와
    안 멈춘 자리를 갈라서. 멈출 일이 없었으면 없었다고.
  2 만든/바꾼 파일 — **src\ · tests\ · data\ 가 0줄인 것을 폴더마다
    명시한다.** 건드리지 않은 것도.
  3 수치가 움직인 곳 — **없어야 한다.** 움직였으면 멈춘 자리다.
  4 판단한 것 — 2-1 의 분해 · 2-2 의 실행 경로 · 3-1 의 갈래 표 ·
    4-2·4-4 의 판정. **권고는 적지 않는다.** 스스로 본 쪽이 있으면
    근거와 함께 따로 한 줄.
  5 돌린 것 / 안 돌린 것 — 안 돌린 것은 까닭. 백그라운드로 보낸 것이
    있으면 예상 소요를 미리 적었는지 함께. **앱을 띄웠으면 포트와 내린
    것을 함께.**
  6 예상과 달랐던 것 — 없으면 없음. **S202 가 낸 1.36~2.29 와 28자리가
    이 판 값과 다르면 그 차를 값으로.**
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 셋 중 이 세션에 고친 것. 셋 밖의 파일은 적지 않는다.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로. **이 판은 못을
    안 박는다 — 박았으면 왜인지 적는다.** xfail 이 몇에서 몇이 됐는지
    한 줄. 확인 안 한 못 수도.

마지막에 셸에 문자열을 태운 자리가 있었는지 한 줄로 적는다 — **python -c ·
커밋 메시지 · 수를 세는 파이프(Measure-Object 포함)를 모두 센다. 네 판
연속 샌 자리다.** 없으면 없다고 적는다. ctx 칸을 셋으로 적는다. 그리고
띄운 앱을 내렸는지와 포트, 백그라운드 셸 확인 결과를 적는다.
