S224 — 화면 전력삼각형을 같은 눈금으로 그리고 각도 글자를 선에서 뗀다

**이 판은 그림 판이다. 금액 0 이다.** 그리는 값을 안 바꾼다.
**고치는 것은 화면 하나다 — PPT · Word 는 0줄이다.**

**사람이 정한 것 — 그대로 선다** (HANDOVER 5절).
  전력삼각형은 가로 · 세로 같은 눈금(1:1)으로 고정한다. 역률 100 벌의 세로
  범위가 1:1 로 안 풀리면 범위는 고치지 않는다.

**웹 대화창이 S223 값으로 정한 것 — 이 판이 따른다.**
  ⑴ **PPT · Word 전력삼각형은 지금 판(S223 의 M1) 그대로 둔다.** S223 이
     18판에서 겹침 · 잘림 0 을 쟀고, 1:1 을 세우는 다른 방법은 역률 100 범위를
     못 풀거나 그림을 키운다.
  ⑵ **화면은 S223 의 S2-A3b 조합으로 고친다** — 벌 넷(역률 100 · 97 · 85 ·
     간주 92)에서 각도 글자 겹침 0 이 선 유일한 조합이다. **그 조합이 무엇을
     바꾸는지는 보고 요약이 아니라 `PROCEED.md` S223 절의 글자로 떠 온다.**
  ⑶ **잘림은 스크린샷에 그려진 글자 획으로 판정한다.** 글자 상자가 칸 밖으로
     넘쳐도 획이 안 잘렸으면 잘림으로 세지 않는다 — S223 이 잰 화면 축 이름
     2.4px 가 그것이고 지금 판에도 있다.
  ⑷ **역률 100 벌 화면** — 무효전력이 0 이라 세로 폭이 0 이 된다. **1:1 을
     세우되 세로 범위를 그리는 도구가 스스로 정해 주면 그렇게 한다. 세로
     범위를 우리 코드가 수로 적어야만 서면 그것은 손으로 박은 범위다 — 그
     벌만 눈금 비율을 지금 화면 꼴로 두고 값을 낸다.** 각도 글자 자리는 어느
     쪽이든 ⑵ 대로이고 그 벌도 겹침 0 이어야 한다.

**울타리에 하한이 있다** — S223 이 센 코드 2 · 실물 19 (다시 잴 것이다).
적게 닫으면 자리마다 왜 못 했는지 값으로 낸다.

이 판의 못 박힌 안전한 쪽.
  · **값을 만드는 줄을 안 고친다.** 유효 · 무효 · 피상전력 · 역률 값, 그림이
    그리는 점 · 선 · 계열 · 그림 종류, 두 도구가 함께 읽는 전력삼각형 표를
    안 바꾼다. **바꾸는 것은 화면 그림의 눈금 비율 · 크기 정하기 · 각도
    글자의 자리뿐이다.**
  · **`report\figures.py` 0줄.** PPT · Word 그림이 바이트로 같아야 한다.
  · **글자 내용을 바꾸거나 새 안내 · 새 각주 · 새 문구를 세우지 않는다** —
    역률 100 벌을 글로 설명하지 않는다. 화면 예산은 `tools\screen_budget.py`.
  · **눈금 서식(자릿수 · 「-0」)은 ㄷ 자릿수 뿌리다 — 안 고친다.** 새로 서거나
    사라지는 눈금은 세어 적기만 한다.
  · **멈춘다** — 금액이 한 원이라도 움직이면 · 그림 밖 수치 조각 차가 0 이
    아니면 · 벌 넷 가운데 하나라도 각도 글자가 선에 겹치면 · 글자 획이
    잘리면 · 예산을 넘으면 · PPT · Word png 가 한 바이트라도 갈리면.
    **「멈춘다」 가 서면 반드시 멈춘다.** 멈춘 자리와 안 멈춘 자리를
    대조표에 각각 한 줄로.
  · **겹침은 좌표로 낸다** — 각도 글자 경계 상자와 선의 거리. 스크린샷은 그
    값의 짝이다. **캡처 앵커가 서는지 먼저 보고 기다린다** — S223 이 역률 85
    벌에서 앵커를 5분 넘게 기다리다 죽었다. 캡처가 못 닿는 벌은 S223 이 그
    벌을 잰 방법으로 재고 「캡처 못 함」 을 적는다.
  · 앞 판 보고의 수는 전부 다시 잴 것이다. 세션 도중 사람에게 묻지 않는다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. 되돌림이 실제로
    섰는지 값으로 먼저 본다.
  · **지시서에 없는 pytest 플래그를 붙이지 않는다.** 전체가 빨갛게 끝나면
    고친 뒤 같은 명령으로 전체를 한 판 더.
  · **git stash 를 쓰지 않는다. S140 스태시를 지우지 않는다.**
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    파일은 편집 도구로만 고친다. 긴 출력은 `tools\run_tool.py --tail` ·
    `deck_words.py` 는 온 경로 · `run_tool` 에 `.py` 를 직접 주지 않는다.
    **`pytest_counts.py` 에는 받은 출력 파일을 인자로 준다** — 인자 없이
    부르면 `run_tool` 이 받은 판을 못 집는다(S223 4-1).
    **도구로 되는 자리에 파이프를 쓰지 않는다** — S223 파이프 여덟 가운데
    넷은 Glob · run_tool · 기본 출력으로 됐다. 썼으면 쓴 자리마다 「무엇을
    보려고 · 왜 도구로 못 했나」 를 적는다. 커밋 메시지는 편집 도구로 지은
    `.txt` 로 `git commit -F`.
  · **덱 19벌 뜨기는 많아야 두 판.** 앱을 띄우면 포트를 적고 내린 뒤 닫힌
    것을 확인한다. **벽시계를 절마다 잰다.**


0절 — 개시

0-1  PC · `os.cpu_count()` · RAM · 일꾼 수. **1번 PC 면 3-2 는 통째 · 남의
     python 은 셋(판 앞 · 회귀 앞 · 뒤). 2번 PC 면 3-2 는 갈래 넷 · 남의
     python 은 넷.**
0-2  `git pull` · HEAD 와 origin(S223 보고는 08dda42) · 워크트리 ·
     `git stash list`.
0-3  남의 python 을 센다 — 값과 어느 프로젝트인지.
0-4  미해결 — 건수(S223 보고 152) · 갈래별 수 · 세 방법 검산. **전력삼각형
     두 항목의 글자와 갈래를 그대로 옮긴다.**
0-5  회귀 기준선 — 수집 · passed · xfail · skip(1번 PC 는 S222 1,825 깨끗한
     판) · 화면 감사 네 수 · 케이스 판정 수 · 정적 검사 다섯. **기록에서.**
0-6  **⑵ 조합의 뜻을 `PROCEED.md` S223 절에서 글자 그대로 옮긴다** — 무엇을
     어느 파일 어디에서 바꾸나. 그리고 S223 이 그 조합을 저장소 밖에서 어떻게
     재었는지(스크래치 경로가 남아 있으면 경로).
0-7  덱 스냅 기준선 — S222 뒤 스냅이 이 PC 캐시에 있고 그 뒤 코드가 안
     바뀌었으면 쓴다(어느 시점 것인지). 없으면 저장소 판을 한 판 뜬다.
     **이 판의 덱 판 수 · pytest 판 수를 미리 적는다.**

커밋: S224 0절: 개시


1절 — 고친다

1-1  **⑷ 를 먼저 잰다 — 스크래치에서, 저장소는 안 고친다.** 역률 100 벌에서
     1:1 을 세웠을 때 세로 범위를 그리는 도구가 스스로 정해 주는지 · 정해
     주면 그 범위와 그림 칸 크기 · 우리 코드가 수를 적어야 하는지. **판정을
     한 줄로.** 이 판정이 1-3 에서 그 벌을 어느 쪽으로 둘지 정한다.
1-2  **전 기준선** — 벌 넷과 역률 100 벌 전부의 스크린샷 · 그려진 각과
     역률각의 차 · 각도 글자와 선의 거리 · 세로 범위 · 그림 칸 크기. PPT ·
     Word 전력삼각형 png 해시(역률 100 · 낮은 역률 · 간주 92 벌).
     화면 감사 네 수 · 예산.
1-3  **울타리 자리만 고친다.** 자리마다 고친 줄 수와 전 · 후. 울타리를 넘는
     자리가 보이면 멈추고 값을 낸다.
1-4  **1-2 를 다시 잰다** — 벌마다 그려진 각 = 역률각인지(⑷ 로 비율을 안
     바꾼 벌은 그렇다고) · 각도 글자와 선의 거리 · 글자 획 잘림 · 세로 범위.
1-5  **PPT · Word png 해시가 1-2 와 같은지.** `report\figures.py` 가 0줄인지
     `git diff --numstat` 으로.
1-6  **수치 조각** — `deck_words --diff` 로 기준선과 맞댄다. **그림 밖은 차 0.**
     그림 안에서 서거나 사라진 글자는 전수로 적는다.
1-7  화면 감사 네 수 · 예산 전후.
1-8  울타리보다 적게 닫았으면 자리마다 까닭을 값으로.
1-9  **되돌려 확인한다** — 되돌리면 1:1 이 깨지고 각도 글자가 다시 선에
     겹치는지. 되돌림이 실제로 섰는지 값으로 먼저 본다.

커밋: S224 1절: 화면 전력삼각형


2절 — 못

2-1  **화면 전력삼각형의 1:1 과 각도 글자가 선에서 떨어진 것을 무는 못** —
     **그려진 것**(그림 객체가 내는 크기 · 범위 · 글자 자리)을 본다. 소스
     리터럴을 찾는 못은 안 박는다. **S222 의 PPT · Word 각 못을 넓혀 닫히면
     새 못을 박지 않는다.**
2-2  박은 못마다 되돌려 확인한다. 확인 안 한 못은 확인 안 했다고 적는다.
2-3  새 못의 소요. 합이 20초를 넘으면 줄일 수 있는지 값으로.
2-4  앞 판 딱지를 걷는다. **걷은 딱지 몫과 새 못 몫을 갈라 적는다.**
2-5  xfail 이 어떻게 됐는지(앞 4). 새로 빨개지면 멈춘다 — xfail 을 새로 달지
     않는다.
2-6  되돌림이 원복됐는지 `git status` · `git diff`.

커밋: S224 2절: 못


3절 — 회귀

3-1  `git diff` 로 **계산 폴더 여섯 가운데 갈린 것**을 폴더마다 한 줄로.
     값을 만드는 줄이 0 인 것을 값으로 보인다.
3-2  pytest — 0-1 이 정한 대로. **1번 PC 는 아래 명령 그대로.**

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no --durations=0

     **회귀 앞에 남의 python 을 센다 — 사람이 site-energy 를 멈추기로 했다.
     0 이 아니면 기다리거나 죽이지 말고 그 사실만 적고 돈다.**
     `run_tool` 로 받고 **그 출력 파일을 `pytest_counts.py` 에 인자로 준다** —
     `--base` 에 0-5 의 수집 건수. skip 수를 함께.
3-3  소요 — PC · 일꾼 수 · 0-3 의 값 전부 · 깨끗한 판인지 · 상한 8분까지.
     **그 PC 의 앞 깨끗하고 플래그 없는 판들과만 맞댄다.**
3-4  화면 감사 — 네 수를 0-5 와 맞댄다. 갈렸으면 자리마다 글자까지.
3-5  케이스 스터디 — 판정 수와 회귀값 여덟 · 기상 취득 횟수. 소요 밖 칸이
     갈렸으면 멈춘다.
3-6  정적 검사 다섯 — 0-5 기준과 맞는지.

커밋: S224 3절: 회귀


4절 — 기록 · 닫기

`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.

4-1  `PROCEED.md` 에 224세션 절.
4-2  **미해결 건수 대조를 이름으로.** 이름에 「 · 」 를 넣지 않는다.
     **이 판으로 닫힌 전력삼각형 항목은 지운다.** ⑷ 에서 역률 100 벌을 지금
     꼴로 두었으면 그 몫만 이름으로 남기고 **사람이 정해야 열리는 갈래에
     둔다** — 정한 규칙 둘이 그 벌에서 함께 못 서는 자리다.
4-3  **HANDOVER 5절에 ⑴ ⑶ 을 앉힌다** — 웹 대화창이 S223 값으로 정했다는 것과
     뒤집힐 조건(⑴ 은 PPT · Word 그림에서 겹침 · 잘림이 서면 · ⑶ 은 획이
     잘린 자리가 나오면).
4-4  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
4-5  `docs\HANDOVER.md` 를 이 판 값으로 — 1절 「앞 값」 사슬은 두 판까지 ·
     머리 사슬에 S224 · 6절 앞 판 딱지를 걷는다 · 2절(2번 PC 절)은 2번 PC
     판이 아니면 안 고친다.
4-6  `CLAUDE.md` — 9항 **이 판을 돈 PC 칸.** 깨끗하고 플래그 없는 판이 아니면
     그 사실을 적고 깨끗한 판 칸은 안 고친다.
4-7  「다음 작업」 칸 — 다음 판은 사람이 아직 안 정했다. **후보를 이름으로만
     적고 고르지 않는다**(도구가 없어 셸이 새는 자리 · ㄷ 자릿수 뿌리 · ㄱ
     절사 뿌리). 글자 수와 여유.
4-8  이 지시서 본문을 `docs\directives\S224.md` 로 그대로 앉힌다.
4-9  **기록을 다 고친 뒤에** 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-10 커밋하고 `git push`. HEAD 와 origin 이 같은지 확인한다.

커밋: S224 4절: 기록 · 미해결 대조 · 판단 앉히기 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — 절·항마다 한 줄. 0-3 은 센 자리마다 값과 프로젝트. **1-1 판정을
    한 줄로.** 울타리(하한)와 고친 수를 나란히. 「멈춘다」 가 걸린 자리를
    멈춘 자리 · 안 멈춘 자리로 갈라서.
  2 만든/바꾼 파일 — +/− 줄 수. 계산 폴더 여섯은 폴더마다 ·
    `report\figures.py` 0줄을 값으로. 스크린샷은 전후 짝으로.
  3 수치가 움직인 곳 — 금액은 없어야 한다. 그림 밖 조각 차 · 그림 안 글자
    증감 · **벌마다 그려진 각 전후와 역률각 · 각도 글자와 선의 거리 전후** ·
    역률 100 벌 세로 범위 전후 · PPT · Word png 해시 · 케이스 판정 · 감사 · 예산.
  4 판단한 것 — 0-6 · 1-1 · 1-8 · 2-1.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패 · 0-7 에 적은 판 수와 실제 ·
    전체 pytest 마지막 판이 초록인지.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — 세 파일 중 고친 것과 9항을 고쳤는지.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로 · 소요 · xfail 전후 ·
    확인 안 한 못 전후와 딱지 몫 · 새 못 몫.

  **그 뒤에 「사람이 정할 것」 을 한 표로** — ⑷ 에서 역률 100 벌을 지금 꼴로
  두었으면 그 벌과 1-1 의 값 · 멈춘 자리가 있으면 그 자리. 없으면 없다고.
  **그 뒤에 「뿌리표」 · 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다) — **그 PC 의 앞 판들과만
나란히** · 덱과 pytest 가 몇 % 인지. ctx 셋(종전 → 지금 · 가장 크게 먹은 것 ·
줄일 수 있었던 것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
