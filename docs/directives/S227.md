S227 — 도구 판 1 · 수를 틀리게 내는 도구를 고치고 개시 도구를 세운다

**이 판은 도구 판이다. src\ 0줄 · 산출물 0벌 · 금액 0.** 고치는 것은
tools\ · tests\ · 도구 설정 · 도구 규약뿐이다.

**사람이 정했다 — S225 뒤 웹 대화창에서.** 다음 판들은 도구 판이다.
**웹 대화창이 S226 값으로 정했다 — 첫 묶음.** 사람 결정이 아니다.
  ㄱ `tools\pytest_counts.py` — 인자 없이 부르면 run_tool 이 받은 판(python_*
    이름)을 못 집고 자기 출력이나 앞 파일을 집는다 · **`--base` 가 걸러진
    시험(deselected)이 있는 판에서 −1695 로 잘못 맞댄다**(S226 6항). 한 파일.
  ㅋ1 브리핑이 「224'」 꼴 세션 번호를 못 잡아 직전 세션이 S224 로 뜬다 ·
    세션 목록표를 지키는 시험이 같은 잣대를 써서 함께 못 본다.
  ㅌ ruff format 통과 수에 .md 가 섞여 판마다 하나씩 는다.
  ㄹ **새 도구 하나 — 판 개시 값.** 남의 python(`CLAUDE.md` 2항 정의 그대로 ·
    값과 어느 프로젝트인지)과 **0-1 의 PC 값(PC 식별 · cpu 수 · RAM ·
    `-n auto` 일꾼 수)** 을 낸다. S224 · S226 이 0-1 에서 금지된 방법을 썼다.
  ㄴ `tools\run_tool.py` 에 `.py` 를 주면 WinError 193 — **규약만으로 세
    판에서 샜다. 도구가 받게 한다.**
  **빼는 것** — ㅈ(투명 글자) · ㅍ(덱 소요)은 둘째 묶음이다. ㄷ · ㅁ · ㅂ ·
  ㅅ · ㅇ · ㅊ · ㅋ2 · ㅋ3 · ㅋ4 · ㅎ 은 안 건드린다.

**S226 보고가 적은 것 — 전부 재게 할 것이다.** 증상을 이 판에서 먼저
재현하고 고친다. 재현이 안 되면 멈추지 말고 그 자리는 안 고치고 값을 낸다.

**울타리에 하한이 있다** — 1절이 센 「고칠 자리 수」. 적게 닫으면 자리마다
까닭을 값으로.

이 판의 못 박힌 안전한 쪽.
  · **src\ 0줄.** 계산 폴더 여섯 · report\ · ui\ 를 안 고친다. 하나라도
    갈리면 멈춘다.
  · **도구가 내는 수의 뜻을 바꾸지 않는다** — 고치는 것은 잘못 집는 파일 ·
    잘못 맞대는 기준 · 못 잡는 이름 꼴 · 섞인 파일 종류뿐이다. 같은 입력에서
    틀리지 않던 경우의 출력은 한 글자도 안 바뀌어야 한다(1-3 의 전 출력과
    맞댄다). 갈리면 멈춘다.
  · **ㅌ 은 .md 를 빼는 것만** — ruff 가 보는 .py 파일 목록 · format 어긋남
    가운데 .py 몫 · `ruff check` 결과가 전후 같아야 한다. 갈리면 멈춘다.
  · **ㄹ 은 남의 python 을 죽이거나 기다리지 않는다.** 세고 적기만 한다.
    「남의 python」 의 정의를 새로 짓지 않는다 — `CLAUDE.md` 2항 그대로.
  · **옛 기록의 수를 고쳐 쓰지 않는다** — S226 1-4 의 「틀린 수가 앉은 자리」
    셋은 HANDOVER 에 「이 수에 섞인 몫」 을 한 줄씩 단다.
  · 세션 도중 사람에게 묻지 않는다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. 되돌림이 실제로
    섰는지 값으로 먼저 본다.
  · **지시서에 없는 pytest 플래그를 붙이지 않는다. 전체가 빨갛게 끝나면
    고친 뒤 전체를 한 판 더 — 2번 PC 면 네 갈래 전부다(빨간 갈래만이
    아니다 · S225 가 ① 만 다시 돌렸다).**
  · **git stash 를 쓰지 않는다.** 1번 PC 면 S140 스태시를 안 지운다.
  · **heredoc · `python -c` · `Set-Content` · `Out-File` 을 쓰지 않는다.**
    **0-1 · 0-3 은 ㄹ 이 서기 전이므로 스크래치 `.py` 로 뜬다 — 셸 명령으로
    뜨지 않는다.** 긴 출력은 `tools\run_tool.py --tail` · `deck_words.py` 는
    온 경로 · ㄴ 을 고치기 전까지 `run_tool` 에 `.py` 를 주지 않는다.
    `pytest_counts.py` 는 ㄱ 을 고치기 전까지 받은 출력 파일을 인자로.
    **도구로 되는 자리에 파이프를 쓰지 않는다.** 썼으면 쓴 자리마다 「무엇을
    보려고 · 왜 도구로 못 했나」. 커밋 메시지는 편집 도구로 지은 `.txt` 로
    `git commit -F`.
  · **덱 · 케이스 스터디 · 화면 감사를 안 돌린다** — src\ 0줄을 값으로 보이면
    된다. 앱을 띄우면 포트를 적고 내린 뒤 닫힌 것을 확인한다. **벽시계를
    절마다 잰다.**


0절 — 개시

0-1  **PC** — 스크래치 `.py` 로 PC 식별 · cpu 수 · RAM · `-n auto` 일꾼 수.
     **2번 PC 면 3-2 는 갈래 넷(`PROCEED.md` 「pytest 분할 실행」) · 1번 PC 면
     통째.**
0-2  `git pull` · HEAD 와 origin(S226 보고는 60c4234) · 워크트리 ·
     `git stash list`.
0-3  남의 python — 스크래치 `.py` 로. 값과 어느 프로젝트인지. **1-3 의 ㄹ
     전 출력으로 쓴다.**
0-4  미해결 — 건수(S226 보고 159) · 갈래별 수 · 세 방법 검산. **이 판이 닫을
     항목을 이름으로 옮긴다**(ㄱ · ㅋ1 · ㅌ · ㄹ · ㄴ 에 묶인 「라」 항목 전부 ·
     S226 이 묶은 대로).
0-5  회귀 기준선 — 수집 · passed · xfail · skip · 정적 검사 다섯(ruff format 은
     통과 수 · 어긋남 수를 .md 몫과 .py 몫으로 갈라서). **기록에서 · 그 PC 의
     마지막 깨끗하고 플래그 없는 판.**

커밋: S227 0절: 개시


1절 — 재현하고 센다

**이 절은 재현하고 세고 판정만 한다. 고치지 않는다.**

1-1  **자리마다 증상을 지금 저장소에서 재현한다** — S226 이 재현한 조건 그대로.
       ㄱ 인자 없이 부르기(run_tool 이 받은 판이 있을 때 · 자기 출력이 있을 때) ·
          `--base` 를 deselected 가 있는 판(기록 묶음)에 줄 때
       ㅋ1 세션 번호에 「'」 가 붙은 판이 직전일 때 브리핑과 세션 목록표 시험
       ㅌ ruff format 이 세는 파일 목록 · .md 몫
       ㄴ run_tool 에 `.py`
     재현마다 명령 · 출력 · 기대값을 적는다.
1-2  **ㄹ 의 재료** — 0-1 · 0-3 을 뜬 스크래치 `.py` 와 `CLAUDE.md` 2항 정의 ·
     `PROCEED.md` 「pytest 분할 실행」 의 남의 python 세는 자리. 새 도구가 낼
     줄을 전수로 적는다 — 새로 짓는 값은 없어야 한다.
1-3  **전 출력을 떠 둔다** — 고칠 도구마다 틀리지 않던 입력 둘 이상에서의 출력.
     2절 뒤 한 글자도 안 갈려야 한다.
1-4  **고칠 자리 수를 값으로 못 박는다** — 코드 자리 · 설정 자리 · 규약 자리
     (`CLAUDE.md` · `PROCEED.md` 에서 이 도구들을 적은 글자). 그것이 2절의
     울타리이고 하한이다.

커밋: S227 1절: 재현


2절 — 고친다

2-1  **1-4 가 센 자리만 고친다.** 자리마다 고친 줄 수와 전 · 후. 울타리를
     넘는 자리가 보이면 멈추고 값을 낸다.
2-2  **자리마다 1-1 의 재현을 다시 돈다** — 기대값이 나오는지.
2-3  **1-3 의 전 출력과 맞댄다** — 틀리지 않던 입력에서 한 글자도 안 갈렸는지.
2-4  **ㅌ** — 전후 .py 파일 목록 · format 어긋남(.py 몫) · `ruff check` 결과가
     같은지. 통과 수 전 → 후와 그 차가 .md 몫과 같은지.
2-5  **ㄹ** — 새 도구 출력을 0-1 · 0-3 의 스크래치 값과 맞댄다. 남의 python 이
     0 이 아닐 때의 꼴을 스크래치로 하나 띄워 보고 내린다(띄운 것을 적고
     닫힌 것을 확인한다).
2-6  **도구 규약을 한 자리에 맞춘다** — `CLAUDE.md` 의 도구 규약 · 목록에서
     ㄱ(인자 없이 불러도 된다) · ㄴ(`.py` 를 받는다) · ㄹ(새 도구 이름과 쓰는
     절)을 이 판 값으로. 같은 글자가 다른 기록에 있으면 한 자리로 모으고
     나머지는 가리킨다.
2-7  1-4 보다 적게 닫았으면 자리마다 까닭을 값으로.
2-8  **되돌려 확인한다** — 되돌리면 1-1 의 증상이 다시 서는지. 되돌림이 실제로
     섰는지 값으로 먼저 본다.

커밋: S227 2절: 도구 고침


3절 — 못 · 회귀

3-1  **자리마다 증상을 무는 못** — 도구를 실제로 불러 낸 값을 본다(소스
     리터럴을 찾는 못은 안 박는다). ㄱ 은 run_tool 이 받은 판을 집는 것과
     deselected 가 있는 판의 `--base` · ㅋ1 은 「'」 붙은 세션 번호 · ㅌ 은 세는
     파일에 .md 가 없는 것 · ㄹ 은 출력 줄 꼴 · ㄴ 은 `.py` 를 받는 것.
     **이미 있는 못을 넓혀 닫히면 새 못을 박지 않는다.** 박은 못마다 되돌려
     확인 · 소요 · 합이 20초를 넘으면 줄일 수 있는지 값으로. 앞 판 딱지를
     걷고 **걷은 딱지 몫과 새 못 몫을 갈라 적는다.** xfail 이 새로 빨개지면
     멈춘다. 되돌림 원복을 `git status` · `git diff` 로.
3-2  **`git diff --numstat` 으로 src\ 0줄을 값으로 보인다** — 폴더마다.
3-3  pytest — 0-1 이 정한 대로. **2번 PC 면 `PROCEED.md` 「pytest 분할 실행」
     의 네 명령 그대로 · 1번 PC 면 아래 명령 그대로. 플래그를 더하지 않는다.**

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no --durations=0

     **남의 python 은 판 앞 · (2번 PC 면 갈래 사이) · 뒤에 ㄹ 새 도구로 센다.**
     0 이 아니면 기다리거나 죽이지 말고 적기만 한다. `run_tool` 로 받고
     **고친 `pytest_counts.py` 를 인자 없이 한 번 · 출력 파일을 인자로 한 번
     불러 두 값이 같은지 본다** — `--base` 에 0-5 의 수집 건수. skip 수를 함께.
3-4  소요 — PC · 일꾼 수 · 남의 python 값 전부 · 깨끗한 판인지 · 상한 8분.
     그 PC 의 앞 깨끗하고 플래그 없는 판들과만 맞댄다.
3-5  정적 검사 다섯 — 0-5 와 맞댄다. **ruff format 은 새 기준(.md 뺀 수)과 옛
     기준을 나란히.**

커밋: S227 3절: 못 · 회귀


4절 — 기록 · 닫기

`CLAUDE.md` 「세션 종료 시」 와 「같은 것을 두 자리에 적지 않는다」 를 따른다.

4-1  `PROCEED.md` 에 227세션 절 — 첫 묶음이 웹 대화창 판단이라는 것과 무엇을
     넣고 뺐는지 이름으로.
4-2  **미해결 건수 대조를 이름으로.** 이름에 「 · 」 를 넣지 않는다. **이 판이
     닫은 항목을 지운다.** 덜 닫았으면 남은 몫을 이름으로.
4-3  **HANDOVER 에 「틀린 수가 앉은 자리」 셋을 한 줄씩** — 어느 판 어느 수에
     무엇이 섞였는지(S225 그림 안 글자 수의 투명 눈금 18줄 · S224' 판 브리핑의
     직전 세션 · S205 ~ S224' ruff 통과 수의 .md 몫). 옛 판 절은 고치지 않는다.
     **정적 검사 기준이 이 판부터 .md 를 뺀 수라는 것을 한 줄로.**
4-4  `docs\OPEN_ITEMS.md` 를 갈래별로 맞춘다. 세 방법 교차 검산 값.
4-5  `docs\HANDOVER.md` 를 이 판 값으로 — 머리 사슬에 S227 · 1절 「앞 값」
     사슬은 두 판까지 · 6절 앞 판 딱지를 걷는다 · **2번 PC 판이면 2절을 이 판
     값으로.**
4-6  `CLAUDE.md` — 9항 **이 판을 돈 PC 칸.** 깨끗하고 플래그 없는 판이 아니면
     그 사실을 적고 깨끗한 판 칸은 안 고친다. 2-6 의 도구 규약.
4-7  「다음 작업」 칸 — 「도구 판 둘째 묶음 — 웹 대화창이 정한다」 와 남은 도구
     자리 이름(ㅈ 은 ㄷ 자릿수 뿌리 판보다 앞이어야 한다는 것 함께). 글자 수와
     여유.
4-8  이 지시서 본문을 `docs\directives\S227.md` 로 그대로 앉힌다.
4-9  **기록을 다 고친 뒤에** 기록 묶음을 돌린다 — **고친 `pytest_counts.py`
     를 인자 없이 불러 이 판의 수를 센다**(ㄱ 의 확인).

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

4-10 커밋하고 `git push`. **HEAD 와 origin 이 같은지 · 워크트리가 깨끗한지를
     보고 1항에 적는다.**

커밋: S227 4절: 기록 · 미해결 대조 · 지시서 앉히기


5절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — 절·항마다 한 줄. 울타리(하한)와 고친 수를 나란히. 「멈춘다」
    가 걸린 자리를 멈춘 자리 · 안 멈춘 자리로 갈라서. 마지막 줄에 HEAD =
    origin · 워크트리.
  2 만든/바꾼 파일 — +/− 줄 수. src\ 폴더마다 0줄을 값으로.
  3 수치가 움직인 곳 — ruff format 옛 기준 · 새 기준 · pytest 수 · 도구 출력이
    바뀐 입력(자리마다 전 → 후). 금액 0.
  4 판단한 것 — 1-2 · 1-4 · 2-6 · 2-7 · 3-1.
  5 돌린 것 / 안 돌린 것 — 도구마다 횟수와 실패 · 전체 pytest 마지막 판이
    초록인지 · 끊겼으면 끊긴 자리와 이어 간 자리.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 가운데 고친 것과 9항을 고쳤는지.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로 · 소요 · xfail 전후 ·
    확인 안 한 못 전후와 딱지 몫 · 새 못 몫.

  **그 뒤에 「재현 표」 — 자리마다 재현 명령 · 전 출력 · 후 출력 한 줄씩.**
  **그 뒤에 「사람이 정할 것」 을 한 표로** — 멈춘 자리가 있으면 그 자리.
  없으면 없다고.
  **그 뒤에 「셸 자리」 표를 앞 판과 같은 꼴로.**

마지막에 벽시계(연 시각 · 닫은 시각 · 절마다) — 그 PC 의 앞 판들과만 나란히 ·
pytest 가 몇 % 인지. ctx 셋(종전 → 지금 · 가장 크게 먹은 것 · 줄일 수 있었던
것). 띄운 앱과 포트 · 백그라운드 셸 확인 결과.
