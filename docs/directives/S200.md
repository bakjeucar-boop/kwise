S200 — 조합에서 역률 몫이 0 이 되는 것이 옳은지 값으로 가르고, 고칠 때 드는 것을 잰다

재는 판이다. **고치는 것은 기록뿐이고 계산도 시험도 0줄이다.**
S199 가 「기대값 하나만 고치면 닫히는 xfail 이 0개」 로 멈췄고, 남은 것이
전부 compare\combination.py 522행 한 자리에 매여 있다고 적었다. 이 판은
그 자리를 **고치지 않고 재기만 한다** — 고칠지는 사람이 이 판의 값으로 정한다.
세션 도중 사람에게 묻지 않는다.

이 판의 못 박힌 안전한 쪽 열둘.
  · **src\ 와 tests\ 가 0줄이다.** 고쳐야 닫히는 것이 보여도 이름으로 올린다.
  · **울타리는 폴더가 아니라 자리 수다** — 이 판이 고치는 자리는 **기록 파일의
    수 칸뿐**(PROCEED.md · docs\OPEN_ITEMS.md · docs\HANDOVER.md · CLAUDE.md)
    이고, 그 밖에 **한 자리라도** 고쳐야 하면 멈추고 값만 낸다.
  · **원인을 미리 적지 않았다.** 「덮어쓴다」 도 「옳다」 도 전제가 아니다 —
    1절이 값으로 가른다. 앞 판 보고의 수는 「값」 이 아니라 **다시 잴 것**이다.
    줄 번호와 이름은 지금 파일에서 다시 찾는다.
  · 금액이 한 원이라도 움직이면 그 자리에서 멈추고 되돌린 뒤 값을 낸다.
    회귀값 여덟 · 케이스 판정 174 · 감사 네 수도 같다. **이 판은 계산을 안
    고치므로 움직일 까닭이 없다** — 움직이면 무언가 틀린 것이다.
  · 재려고 갈아 끼우는 것은 **스크래치 monkeypatch · plugin** 으로만 한다.
    src\ 를 돌렸으면 git checkout HEAD -- 경로 로 되돌리고 git status 를 본다.
    **얕은 되돌림을 조심한다** — 갈아 끼움이 실제로 섰는지 값으로 먼저 본다.
  · **git stash 를 쓰지 않는다.** S140 스태시(2026-09-07)를 지우지 않는다.
  · **셸에 문자열을 태우지 않는다 — 버전을 찍는 한 줄도 같다.** S199 가 python -c
    한 줄로 샜다. 파일 수정도, 재는 스크립트도, 한 줄짜리 확인도 전부 스크래치
    .py 로 만들어 .venv\Scripts\python.exe 로 돌린다. 셸에는 파일 이름만 싣는다.
  · 백그라운드로 보내기 전에 예상 소요를 말한다. 근거는 S199 값이다 —
    덱 19벌 × 2 = 38판에 10분 40초였다. 상한을 걸고, 넘기면 스스로 멈추고
    보고한다. 대기 루프를 만들지 않는다.
  · 앱을 띄운 채 끝내지 않는다. 케이스 결과 xlsx 는 저장소 판을 먼저 뜬
    뒤에 돌린다(같은 날 덮인다).
  · **권하지 않는다.** 갈래마다 값을 내고, 스스로 본 쪽이 있으면 근거와
    함께 따로 한 줄로만 적는다.
  · 미해결 이름에 「 · 」 를 넣지 않는다.


0절 — 판 열기

0-1  어느 PC 인지 확인한다. os.cpu_count() · RAM · -n auto 가 고른 일꾼 수.
     1번 PC 면 pytest 를 통째로, 2번 PC 면 갈래 넷으로.
0-2  git pull. HEAD 와 origin 이 같은지, 워크트리가 깨끗한지.
     git stash list 를 보고 수만 적는다 — 건드리지 않는다.
0-3  남의 python 을 센다. 세는 규칙은 CLAUDE.md 2항이 쥔다 — 센 자리마다
     값을 다 적는다. 0 이 아니면 소요에 「견주는 데 쓰지 않는다」 를 붙인다.
0-4  브리핑을 뜬다. 미해결 건수 · 미해결 칸 글자 수 · 「다음 작업」 칸 글자 수.
0-5  기상 캐시에 용인 벌이 있는지 본다. PROJECT_CACHE 가 가리키는 자리를 본다.
0-6  이 판이 건드릴 미해결의 지금 이름을 docs\OPEN_ITEMS.md 에서 그대로
     옮겨 적는다. 이름이 다르면 문서의 이름을 따른다.
0-7  **앞 판 기록의 어긋남 둘을 값으로 낸다. 재기만 한다 — 고치는 것은 5절이다.**
       · **시험 건수 어긋남.** S198 기록이 「1,776 passed · 4 xfailed」 인데
         합이 1,780 이고 수집은 1,781 이다. **그 수가 적힌 자리를 저장소에서
         전수로 뜬다** — 파일과 줄을 이름으로. 지금 실제 값도 함께 낸다.
       · **--durations 를 붙였나.** S199 5-3 이 도구 합을 냈는지 기록에서
         확인한다. 안 냈으면 CLAUDE.md 9항 1번 PC 「무엇이 먹나」 칸이 두 판째
         비어 있는 것이고, 이 판 4절에서 --durations=0 을 붙여 채운다.

커밋하지 않는다.


1절 — 그 0 이 옳은가 그른가

**이 절은 가르기만 한다. 고치지 않는다.** 덱 19벌 전수.

1-1  벌마다 역률 수단을 **켠 조합과 끈 조합** 둘을 돌려 다섯을 값으로 낸다.
       · 원 부하 역률(%)
       · 태양광이 낀 뒤의 역률(%)
       · 그 수단의 목표(%)
       · **조합 청구서가 실제로 요금 계산에 쓴 역률(%)**
       · 역률요금(원)과 그 밑이 된 기본요금(원)
     **다섯을 어디서 떴는지 자리를 적는다** — 값이 어느 함수의 무엇인지.
1-2  벌마다 셋 중 하나로 판정하고 근거를 값으로 적는다.
       ㄱ **0 이 옳다** — 태양광 뒤 역률이 이미 목표 위여서 개선 여지가 없다.
         여지가 0 인 것을 값으로 보인다
       ㄴ **0 이 그르다** — 목표 아래인데 몫이 0 이다. 무엇이 여지를 지웠는지
         **실행 경로를 값으로** 낸다(코드를 읽고 짐작하지 않는다 — 실제로
         그 줄을 지나는지, 지난 뒤 값이 무엇이 되는지)
       ㄷ **못 가른다** — 무엇이 더 있어야 갈리는지
1-3  **2단계 카드 452만원과 3단계 몫 0 의 밑을 맞댄다.** 2단계 카드가 쓰는
     기본요금과 3단계 조합이 쓰는 기본요금이 각각 얼마인지, 같은 벌에서
     몇 대 몇인지 값으로. 두 수가 다르면 다른 까닭을 자리로 적는다.
1-4  **태양광이 없는 조합에서는 켠 쪽이 4,528,045.5625원을 더 낸다고 S199 가
     적었다. 지금 값으로 다시 잰다** — 그 수가 어느 벌들의 값인지 이름으로.

커밋: S200 1절: 조합 역률 몫 0 의 판정


2절 — 고치면 무엇이 얼마나 갈리나 (재기만)

**1-2 가 ㄴ 나 ㄷ 를 하나라도 냈을 때만 이 절을 연다.** 전부 ㄱ 면 이 절을
안 열고 그 사실을 적는다 — 대신 2-4 만 한다.

2-1  **갈래마다 스크래치로 갈아 끼우고 값을 낸다. src\ 는 안 고친다.**
     갈래는 S199 가 낸 넷을 그대로 쓴다 — 켠 수단이 목표를 쓴다 · 개선 폭을
     얹는다 · 설비 무효전력을 얹는다 · 문서에 적는다. **S199 가 그 넷을 어떻게
     적었는지 PROCEED.md 199세션 절에서 그대로 옮겨 적고 시작한다.**
     먼저 **대표 벌 셋**(large-b · large-ind-b · 태양광과 역률이 함께 서는
     용인 벌 하나 — 이름은 지금 저장소에서 찾는다)으로 잰다.
     **셋의 방향이나 크기가 서로 갈리면** 그때 19벌 전수로 넓힌다.
     넓혔는지 안 넓혔는지와 그 까닭을 적는다.
2-2  **갈래마다 표로 낸다.** 여섯 칸 —
     무엇을 건드리나 · **고칠 자리 수(파일과 함수 이름으로)** · 되돌리기 ·
     **금액이 갈리는 폭(최소~최대 · 갈리는 벌 수)** · 드는 것 · 모르는 것.
2-3  **갈래마다 xfail 넷 가운데 몇이 닫히는지** 값으로 낸다. 닫히는 못마다
     기대값이 무엇이 되는지 실물에서 뜬 값으로. **고치지 않는다.**
2-4  **그 값이 뜨는 자리를 전수로 센다** — 화면 · PPT · Excel · Word 각각
     몇 자리인지. 그물을 무엇으로 짰는지 적고, **그물이 못 보는 것**을 함께 적는다.

커밋: S200 2절: 갈래마다 드는 것


3절 — 글자 여덟이 지금도 서는가 (재기만)

3-1  S196 이 낸 여덟이 **지금도 그대로 서는지** 실물에서 확인한다.
     자리마다 지금 글자를 그대로 옮겨 적는다 — 소스 리터럴이 아니라 뜬 글자다.
     선 것과 이미 없어진 것을 갈라 센다.
3-2  **한 판에 잡으면 고칠 자리가 몇인지** 센다. 파일과 함수 이름으로.
     **화면 감사 네 수에 몇이 움직이는지는 짐작하지 않는다** — 모르면 모른다고 적는다.

커밋: S200 3절: 글자 여덟의 지금 상태


4절 — 회귀

4-1  git diff 로 **src\ 와 tests\ 가 0줄**인지 확인한다. 폴더마다 한 줄.
4-2  pytest. 1번 PC 면 앞단에서 통째로.

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     2번 PC 면 갈래 넷으로. 종료 코드로 판정하지 않는다 — 실패 줄과 수집
     건수를 직접 읽고 **0-7 이 잰 지금 값**과 맞댄다. skip 수를 함께.
     **앞 판 보고의 수를 옮겨 적지 않는다.**
4-3  소요를 적는다. PC · 일꾼 수 · 0-3 의 값 전부 · 깨끗한 판인지 ·
     상한 8분까지 얼마 남는지. **깨끗하면 --durations=0 을 붙여 도구 합과
     상위 스물다섯을 낸다** — 두 판째 비어 있는 칸이다.
4-4  화면 감사. 네 수를 앞 판(955 · 806 · 962 · 807)과 맞댄다.
4-5  케이스 스터디. 174/174 과 회귀값 여덟. 기상 취득 횟수.
     **저장소 판을 먼저 뜬 뒤에 돌린다.** xlsx 를 저장소 판과 칸 단위로
     맞대고(fillna 뒤에 센다) 소요 칸만 갈렸으면 되살린다.
4-6  정적 검사 다섯.

커밋: S200 4절: 회귀


5절 — 기록 · 닫기

5-1  PROCEED.md 에 200세션 절을 쓴다. 1·2·3절 표는 이 절에 둔다.
5-2  **0-7 이 뜬 시험 건수 어긋남을 자리마다 고친다** — 고친 자리와 안 고친
     자리를 갈라 적는다. 고칠 때 앞 판 값이 아니라 **그 판이 실제로 낸 수**로
     맞춘다. 무엇을 근거로 그 수가 옳다고 보았는지 한 줄.
5-3  **CLAUDE.md 9항 1번 PC 칸을 이 판 값으로 간다** — 4-3 이 --durations=0
     을 붙인 깨끗한 판이면 「무엇이 먹나」 칸도 채운다. 아니면 그 사실을
     적고 안 고친다.
5-4  미해결 건수 대조를 이름으로 적는다 — 늘어난 것과 줄어든 것을 각각
     이름으로. 이름에 「 · 」 를 넣지 않는다. 세션 목록표 마지막 행도 이름으로.
5-5  docs\OPEN_ITEMS.md 를 갈래별로 맞춘다. 세 방법 교차 검산 값을 다 적는다.
5-6  docs\HANDOVER.md 를 이 판 값으로 간다 — 제목의 끝 판 이름 · 3절 표의
     S200 행 · 6절 못 표. 고친 자리와 안 고친 자리를 갈라 적는다.
5-7  「다음 작업」 칸에 갈래 번호를 쓰지 않는다. 이름으로만. **상한 3,000자를
     넘지 않는지 값으로 확인한다**(S199 에 넘었다).
5-8  이 지시서 본문을 docs\directives\S200.md 로 그대로 앉힌다.
5-9  기록을 다 고친 뒤에 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

5-10 커밋하고 git push. HEAD 와 origin 이 같은지 확인한다.

커밋: S200 5절: 기록 · 미해결 대조 · 지시서 앉히기


6절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — **절·항마다 한 줄이고 하나도 빠뜨리지 않는다**(S199 는 열넷이
    빠졌다). 0-3 칸에 센 자리마다 값을 다 적는다. 0-7 둘은 각각 한 줄씩.
    **1-2 는 벌마다 ㄱ/ㄴ/ㄷ 로 한 줄씩 적는다.**
  2 만든/바꾼 파일 — **src\ 와 tests\ 가 0줄인 것을 폴더마다 명시한다.**
    건드리지 않은 것도.
  3 수치가 움직인 곳 — **없어야 한다.** 움직였으면 멈춘 자리다.
  4 판단한 것 — 1-2 의 판정 근거 · 1-3 의 두 밑 · 2-2 의 갈래 표 ·
    3-2 의 자리 수. **권고는 적지 않는다.** 스스로 본 쪽이 있으면 근거와
    함께 따로 한 줄.
  5 돌린 것 / 안 돌린 것 — 안 돌린 것은 까닭. 백그라운드로 보낸 것이
    있으면 예상 소요를 미리 적었는지 함께.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 셋 중 이 세션에 고친 것. 셋 밖의 파일은 적지 않는다.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로. **xfail 이 몇에서 몇이
    됐는지 한 줄.** 확인 안 한 못 수도.

마지막에 셸에 문자열을 태운 자리가 있었는지 한 줄로 적는다 — 버전을 찍는
한 줄도 센다. 없으면 없다고 적는다. ctx 칸을 셋으로 적는다. 그리고 띄운
앱을 내렸는지와 포트, 백그라운드 셸 확인 결과를 적는다.
