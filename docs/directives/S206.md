S206 — 역률 100 벌에서 「97%까지 올린다」 로 말하는 자리를 정본에 맞춘다

**고치는 판이고 고치는 것은 글자다. 계산은 0줄이다.**
S204 가 「표현이 어색하다」 로 가른 다섯 자리가 남았고 사람이 그것을
이 판 몫으로 정했다. **잣대는 이미 있다 — S154 정본이다**: 역률이 감액
상한(97%) 이상이면 「개선할 것이 없다」 로 처리한다. 값을 낮춰 잡거나
벌의 역률을 간주값으로 되돌리지 않는다 — 청구서 값이 정본이다.
**정본이 판정을 주는 자리는 이 판이 고치고, 표현만 남는 자리는 후보만
내고 고르지 않는다** — 표현·자리·길이는 사람이 화면을 열어 잡는다.
세션 도중 사람에게 묻지 않는다.

이 판의 못 박힌 안전한 쪽 열둘.
  · **계산이 0줄이다.** tariff\ · diagnose\ · measures\ · pv\ · compare\ ·
    io\ 의 계산 줄을 안 고친다. **금액이 한 원이라도 움직이면 그 자리에서
    멈추고 되돌린 뒤 값을 낸다.** 회귀값 여덟 · 케이스 판정 174 도 같다.
    **금액 칸은 전수로 맞댄다**(S205 가 1,216칸을 그렇게 봤다).
  · **울타리는 자리 수다.** 이 판이 고치는 자리는 **1-4 가 「정본이 판정을
    준다」 로 센 자리뿐**이고, 그 밖에 한 자리라도 고쳐야 하면 멈추고 값만
    낸다. **S205 가 멈춘 셋(조합 이름·수단 칸 · 역률 금액 칸 「0원」 ·
    화면 1단계 세 줄)을 건드리지 않는다.**
  · **화면 문구를 늘리지 않는다.** 하나를 더하면 무엇을 뺄지 함께 정한다 —
    예산은 `tools\screen_budget.py` 가 쥔다. **새 안내를 붙이기 전에 이름을
    고쳐 해결되는지 먼저 본다.** 배경·근거·제도 설명은 매뉴얼로 보낸다.
  · **화면 감사 네 수가 갈리면 자리마다 글자까지 적는다.** 수만 적지 않는다.
  · **표현을 고르지 않는다.** 정본이 판정을 안 주는 자리는 **후보를 둘씩
    내고 png 로 찍어 사람이 볼 수 있게만 한다.**
  · **「멈춘다」 가 서면 반드시 멈춘다.** 멈춘 자리와 안 멈춘 자리를
    대조표에 각각 한 줄로 적는다. 멈출 일이 없었으면 없었다고 적는다.
  · **원인을 미리 적지 않았다.** 앞 판 보고의 수(다섯 · 선 벌 셋 · 열아홉 ·
    감사 네 수)는 전부 **다시 잴 것**이다. 줄 번호와 이름은 지금 파일에서
    다시 찾는다.
  · **실물을 본다.** 소스 리터럴로 글자를 판정하지 않는다 — 화면은 png 를
    함께 보고(DOM 텍스트를 실물로 읽지 않는다), PPT·Excel·Word 는 떠서 본다.
  · 되돌려 확인은 스크래치 monkeypatch · plugin 으로 한다. **얕은 되돌림을
    조심한다** — 갈아 끼움이 어느 길에 닿는지 먼저 값으로 낸다.
    src\ 를 더 돌렸으면 git checkout HEAD -- 경로 로 되돌린다.
  · **git stash 를 쓰지 않는다.** S140 스태시를 지우지 않는다.
  · **셸에 파이프를 붙이지 않는다 — 일곱 판 연속 샜다.** 마지막 판에
    넷이 섞였다(`| head` · `2>&1` 둘 · `2>&1 | Select-Object`)고 적었고
    이음 판에도 `> $null` 하나가 있었다. **문자열 태우기만이 아니라
    파이프와 리다이렉트도 센다.** 재는 것 · 세는 것 · 한 줄 확인 · 긴 출력
    자르기까지 전부 **파일로 받아 스크래치 .py 로 읽는다.** python -c 를
    쓰지 않는다. 커밋 메시지는 스크래치 .txt 에 써서 `git commit -F` 로 단다.
    **남의 python 을 세는 `Get-CimInstance` 만 예외이고 거기에도 파이프를
    안 붙인다**(CLAUDE.md 2항).
  · **파일 수정은 편집 도구로 한다.**
  · 앱을 띄우면 포트를 적고 내린 뒤 닫힌 것을 확인한다. 케이스 결과 xlsx 는
    저장소 판을 먼저 뜬 뒤에 돌린다.


0절 — 판 열기

0-1  어느 PC 인지 확인한다. os.cpu_count() · RAM · -n auto 가 고른 일꾼 수.
0-2  git pull. HEAD 와 origin 이 같은지, 워크트리가 깨끗한지.
     git stash list 를 보고 수만 적는다 — 건드리지 않는다.
0-3  남의 python 을 센다. 세는 규칙은 CLAUDE.md 2항이 쥔다 — 센 자리마다
     값을 다 적는다. 0 이 아니면 소요에 「견주는 데 쓰지 않는다」 를 붙인다.
0-4  브리핑을 뜬다. 미해결 건수 · 미해결 칸 글자 수 · 「다음 작업」 칸 글자 수.
     **지금 값을 잰다 — 앞 판 보고의 수를 옮기지 않는다.**
0-5  **브리핑이 낡은 자리 넷을 값으로 낸다. 재기만 한다 — 고치는 것은 6절이다.**
       · **브리핑 「다시 열지 마라」 의 가장 새 판이 무엇인지 잰다.**
         사람이 S160 이라 보았다. 항목 수와 가장 새 판을 값으로 내고,
         **HANDOVER 5절이 쥔 것과 맞대 어느 판들이 브리핑에 없는지**
         이름으로 센다. **그 칸이 PROCEED.md 어디에서 오는지 자리를 적는다.**
       · **브리핑 「오늘 첫 작업」 칸이 무엇에서 오는지** 자리를 적고,
         지금 글자가 앞 판 기록인지 값으로 본다.
       · **PROCEED 「다음 작업」 칸이 브리핑에 뜨는지** 값으로 본다.
         안 뜨면 안 뜬다고 적는다. **까닭은 도구를 읽어 적는다.**
       · **「마」 갈래(사람이 정해야 열린다) 건수가 브리핑 「내가 밟아야
         할 것」 에 뜨는지** 값으로 본다.
     **네 자리 가운데 기록을 고쳐 해결되는 것과 도구를 고쳐야 하는 것을
     갈라 적는다.** 도구 쪽은 이 판에서 안 고치고 이름으로 올린다.
0-6  기상 캐시에 용인 벌이 있는지 본다. PROJECT_CACHE 가 가리키는 자리를 본다.
0-7  이 판이 건드릴 미해결의 지금 이름을 docs\OPEN_ITEMS.md 에서 **이름으로**
     찾아 그대로 옮겨 적는다(번호가 밀린다).
0-8  **S154 정본의 원문을 옮겨 적는다** — 어느 파일 어느 절에 있는지 찾아
     글자 그대로. **이 판의 잣대가 그 글자다.**
0-9  **docs\SCREEN_WORDING_S204.md 의 「표현이 어색하다」 표를 그대로 옮겨
     적는다.** 자리 수와 선 벌 수를 값으로.


1절 — 다섯 자리를 실물에서 다시 뜨고 정본에 맞댄다

**이 절은 가르기만 한다. 고치지 않는다.** 덱 19벌 전수.

1-1  **자리마다 지금 뜬 글자를 그대로 옮겨 적는다** — 소스 리터럴이 아니라
     그려진 글자다. 화면 · PPT · Excel · Word 각각 어디인지.
     **그물을 적고 그물이 못 보는 것을 함께 적는다.**
     **S204 표를 전제로 삼지 않는다** — 실물을 훑어 더 있으면 더 적는다.
1-2  **자리마다 조건이 실물에 서는 벌을 센다.** 역률 100 벌과 97~99 벌과
     92 미만 벌을 갈라 센다. 0 이면 0 이라고 적는다 — **뜨지 않는 갈래는
     없는 갈래다.**
1-3  **자리마다 그 글자가 무엇을 근거로 서는지 값으로 낸다** — 어느 값이
     그 문장을 만드는지. 목표 97 을 어디서 읽는지.
1-4  **자리마다 셋 중 하나로 판정한다.**
       ㄱ **정본이 판정을 준다** — S154 정본을 대면 그 글자가 무엇이어야
         하는지 값으로 갈린다(예: 여지가 0 인데 개선 안내가 선다).
         **이 판이 고칠 자리다**
       ㄴ **표현만 남는다** — 정본을 대도 어느 말이 나은지 값으로 안 갈린다.
         **이 판이 안 고친다. 3절이 후보를 낸다**
       ㄷ **울타리 밖이다** — 고치려면 계산 폴더를 건드려야 한다.
         **이 판이 안 고친다. 이름으로 올린다**
1-5  **ㄱ 로 판정한 자리 수를 값으로 못 박는다.** 그것이 2절의 울타리다.
     **고치는 방법이 「이름을 고치는 것」 인지 「글을 고치는 것」 인지
     자리마다 갈라 적는다** — 이름으로 되면 이름으로 한다.
1-6  **같은 벌에서 여러 자리가 다르게 말하는지 실물로 본다.** 역률 100 벌
     하나를 골라 다섯 자리 글자를 나란히 적는다. 어느 벌을 왜 골랐는지도.

커밋: S206 1절: 다섯 자리를 정본에 맞댄 판정


2절 — 정본이 판정을 주는 자리만 고친다

**1-5 가 센 자리가 0 이 아닐 때만 이 절을 연다.**

2-1  **저장소 판을 먼저 뜬다** — 화면 감사 네 수와 고칠 자리의 지금 글자.
2-2  **1-5 가 센 자리만 고친다.** 자리마다 고친 줄 수와 전·후 글자를 적는다.
     **울타리를 넘는 자리가 보이면 그 자리에서 멈추고 값을 낸다.**
     **고친 글자가 정본의 말과 같은지 자리마다 값으로 확인한다.**
2-3  **문구가 늘었는지 값으로 본다.** 늘었으면 자리마다 **무엇을 뺐는지**
     함께 적는다. `tools\screen_budget.py` 로 전·후를 재고 넘으면 멈춘다.
2-4  **금액이 한 원도 안 움직인 것을 값으로 확인한다** — 덱 19벌의 금액
     칸을 저장소 판과 전수로 맞댄다. 움직이면 멈춘다.
2-5  **역률 100 아닌 벌의 글자가 안 갈린 것을 값으로 확인한다** —
     92 벌과 85 벌에서 그 자리들이 그대로인지. 갈렸으면 멈춘다.
2-6  **되돌려 확인한다** — 옛 글자로 되돌리면 정본과 다시 어긋나는지
     자리마다 한 줄로. 되돌림이 실제로 섰는지 값으로 먼저 본다.
2-7  **고친 글자가 네 산출물에 다 따라오는지** 실물로 본다. 역률 100 벌
     하나를 골라 화면(png 포함) · PPT · Excel · Word 를 떠서 나란히 적는다.
     **안 따라온 자리가 있으면 이름으로 올린다** — 이 판에서 안 고친다.

커밋: S206 2절: 정본이 판정을 주는 자리를 고친다


3절 — 표현만 남는 자리는 후보만 낸다

**이 절은 후보를 내기만 한다. 고치지 않는다. 고르지 않는다.**

3-1  **1-4 가 ㄴ 로 판정한 자리마다 후보를 둘씩 낸다.** 후보마다 넷 —
     글자 · 글자 수 · 무엇이 달라지나 · 예산에 드는가.
     **지금 글자도 후보 하나로 함께 세운다**(그대로 두는 것도 갈래다).
3-2  **후보를 실물에 넣어 png 로 찍는다** — 화면 자리는 스크래치로 갈아
     끼워 찍고, 산출물 자리는 떠서 그 칸을 보인다. **src\ 를 안 고친다.**
     찍은 파일 이름을 적는다.
3-3  **docs\SCREEN_WORDING_S204.md 를 갱신한다** — 이 판이 닫은 행을 지우고
     (「진행표는 지우면서 간다」), 남은 행에 후보와 png 이름을 붙인다.
     **1-4 가 ㄷ 로 판정한 자리는 까닭을 붙여 남긴다.**
3-4  **파일 이름에 판 번호가 박힌 것을 어떻게 할지 값으로 판정한다** —
     S205 가 유지로 정했다. 뒤집을 근거가 있으면 적고, 없으면 그대로 둔다.

커밋: S206 3절: 표현 후보와 png


4절 — 못

4-1  **2절이 고친 자리를 무는 못을 박는다** — **실물에 뜬 글자**를 보는 못.
     소스 리터럴을 찾는 못은 안 박는다. **역률 100 벌과 92 벌 둘 다에서
     값으로 문다** — 한쪽만 보면 다른 쪽이 갈려도 초록이다.
4-2  **박은 못마다 되돌려 확인한다** — 어디를 어떻게 틀렸더니 빨개졌는지
     한 줄씩. 확인 안 한 못은 확인 안 했다고 적는다.
4-3  **새 못의 소요를 잰다.** 앞 두 판 못이 3.5초와 7.19초였다 —
     이 판 못이 몇 초인지 적고, **20초를 넘으면 줄일 수 있는지 값으로 본다.**
4-4  **앞 판들이 박은 못 다섯이 지금도 초록인지** 확인한다. 빨가면 멈춘다.
4-5  **xfail 넷이 어떻게 됐는지** 값으로 낸다. **새로 빨개지면 멈추고 값을
     낸다 — xfail 을 새로 달지 않는다.**
4-6  되돌림이 원복됐는지 git status · git diff 로 확인한다.

커밋: S206 4절: 못


5절 — 회귀

5-1  git diff 로 **계산 폴더 여섯이 0줄**인지 확인한다. 폴더마다 한 줄.
5-2  pytest. 1번 PC 면 앞단에서 통째로. **모든 수정 뒤에 돌린다.**

         .venv\Scripts\python.exe -m pytest tests -n auto --dist load -rf --tb=no

     2번 PC 면 갈래 넷으로. 종료 코드로 판정하지 않는다 — 실패 줄과 수집
     건수를 직접 읽고 앞 판(수집 1,785 · 1,781 passed · 4 xfailed · skip 0)과
     맞댄다. skip 수를 함께. **수집 건수를 세는 것도 .py 로 한다.**
5-3  소요를 적는다. PC · 일꾼 수 · 0-3 의 값 전부 · 깨끗한 판인지 ·
     상한 8분까지 얼마 남는지. 깨끗하면 --durations=0 을 붙여 도구 합과
     상위 스물다섯을 낸다. **앞 판들과 맞댄다** — S202 4분 55초 ·
     S203 5분 31초 · S204 5분 31초(깨끗한 판 아님) · S205 5분 27초
     (도구 합 1,822.94초). 방향만 적는다.
5-4  화면 감사. 네 수를 앞 판(955 · 806 · 962 · 807)과 맞댄다.
     **갈렸으면 어느 글자가 갈렸는지 자리마다 적는다.**
5-5  케이스 스터디. 174/174 과 회귀값 여덟. 기상 취득 횟수.
     **저장소 판을 먼저 뜬 뒤에 돌린다.** xlsx 를 저장소 판과 칸 단위로
     맞대고(fillna 뒤에 센다) **소요 밖이 갈렸으면 멈추고 값을 낸다.**
5-6  정적 검사 다섯. **앞 판이 「259 는 S205.md 앉히기 전 값이라 다음 판은
     260 이 맞다」 고 적었다** — 지금 값을 재서 맞는지 본다.

커밋: S206 5절: 회귀


6절 — 기록 · 닫기

6-1  **0-5 가 「기록을 고쳐 해결된다」 로 가른 자리를 고친다.**
     특히 **PROCEED.md 「다시 열지 마라」 칸이 S160 에서 멈춘 것**을 값으로
     확인하고, **HANDOVER 5절과 두 자리로 갈린 것인지 판정한다.**
       · **한 자리로 모을 수 있으면 어느 자리에 둘지 값으로 정하고 까닭을
         적는다.** 다른 자리는 그 자리를 가리킨다.
       · **한 판에 못 모으면 모으지 말고**, 그 사실과 「브리핑만 읽으면
         S161 이후 결정을 못 본다」 를 이름으로 올린다.
     **도구를 고쳐야 하는 자리는 이 판에서 안 고치고 이름으로 올린다.**
6-2  PROCEED.md 에 206세션 절을 쓴다. 1·3절 표는 이 절에 둔다.
6-3  미해결 건수 대조를 이름으로 적는다 — 늘어난 것과 줄어든 것을 각각
     이름으로. 이름에 「 · 」 를 넣지 않는다. 세션 목록표 마지막 행도 이름으로.
     **0-5 가 낸 갈래별 수를 함께 적는다.**
6-4  docs\OPEN_ITEMS.md 를 갈래별로 맞춘다. 세 방법 교차 검산 값을 다 적는다.
     **「지금 · 앞 · 그전 · 그때」 표도 한 칸 민다** — S205 가 이 표를
     빠뜨려 기록 묶음이 빨갰다.
6-5  docs\HANDOVER.md 를 이 판 값으로 간다 — 제목의 끝 판 이름 · 3절 표의
     S206 행 · 5절 「다시 열지 마라」 · 6절 못 표. 고친 자리와 안 고친
     자리를 갈라 적는다.
6-6  **S192 뿌리표의 이 뿌리 칸을 이 판 값으로 간다.**
6-7  CLAUDE.md 9항 1번 PC 칸을 이 판 값으로 간다. 5-3 이 --durations=0 을
     붙인 깨끗한 판이 아니면 그 사실을 적고 안 고친다.
6-8  「다음 작업」 칸에 갈래 번호를 쓰지 않는다. 이름으로만. **상한 3,000자를
     넘지 않는지 값으로 확인하고 지금 글자 수를 적는다.**
6-9  이 지시서 본문을 docs\directives\S206.md 로 그대로 앉힌다.
6-10 기록을 다 고친 뒤에 기록 묶음을 돌린다.

         .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

6-11 커밋하고 git push. HEAD 와 origin 이 같은지 확인한다.
     **커밋 메시지는 스크래치 .txt 에 써서 git commit -F 로 단다.**

커밋: S206 6절: 기록 · 미해결 대조 · 지시서 앉히기


7절 — 보고

마지막에 한 번만. 여덟 항목이고 그 밖의 서술을 덧붙이지 않는다.
표 칸은 30자 안쪽.

  1 대조표 — **절·항마다 한 줄이고 하나도 빠뜨리지 않는다.**
    0-3 칸에 센 자리마다 값을 다 적는다. **0-5 는 네 자리를 각각 한 줄씩.**
    **1-4 는 자리마다 ㄱ/ㄴ/ㄷ 로 한 줄씩.** 1-5 가 센 울타리 수와 2절이
    실제로 고친 자리 수를 나란히. **「멈춘다」 가 걸린 자리를 각각 한 줄로** —
    멈춘 자리와 안 멈춘 자리를 갈라서.
  2 만든/바꾼 파일 — **계산 폴더 여섯이 0줄인 것을 폴더마다 명시한다.**
    건드리지 않은 것도. **3-2 가 찍은 png 이름을 적는다.**
  3 수치가 움직인 곳 — **금액은 없어야 한다.** 움직였으면 멈춘 자리다.
    **화면 감사 네 수는 갈렸으면 자리마다 글자까지.** 고친 글자는 전·후로.
  4 판단한 것 — 1-4 의 판정 근거 · 1-5 의 「이름이냐 글이냐」 · 2-3 에서
    뺀 것 · 6-1 의 「한 자리로 모으나」. **권고는 적지 않는다.**
  5 돌린 것 / 안 돌린 것 — 안 돌린 것은 까닭. 백그라운드로 보낸 것이
    있으면 예상 소요를 미리 적었는지 함께. 앱을 띄웠으면 포트와 내린 것을 함께.
  6 예상과 달랐던 것 — 없으면 없음.
  7 프로젝트 지식 재업로드 — CLAUDE.md · collaboration.md ·
    project-overview.md 셋 중 이 세션에 고친 것. 없으면 없음.
  8 박은 못 · 걷은 못 · 되돌려 확인한 못 — 이름으로. **새 못의 소요를
    함께.** xfail 이 몇에서 몇이 됐는지 한 줄. 확인 안 한 못 수도.

  **그 뒤에 「사람이 볼 것」 을 한 표로 낸다** — 3절이 낸 후보를 자리마다
  한 줄로(자리 · 지금 글자 · 후보 둘 · png 이름). **권하지 않는다.**

마지막에 셸에 문자열을 태운 자리가 있었는지 한 줄로 적는다 — **파이프와
리다이렉트도 센다(`| head` · `2>&1` · `> $null` 포함). 일곱 판 연속 샌
자리다.** 없으면 없다고 적는다. ctx 칸을 셋으로 적는다 — 세션 예산 종전 →
지금(약 차이) · 가장 크게 먹은 것 하나 · 줄일 수 있었다고 보는 것 하나.
그리고 띄운 앱과 포트, 백그라운드 셸 확인 결과를 적는다.
