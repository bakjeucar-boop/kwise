# 인수인계 — 2026-09-09 마감 (S154)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153·S154 가 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S154 6절 |
| 덱 벌 | **17** | `render_deck.CASES` · S154 1-5 |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `PROCEED.md` · `docs\OPEN_ITEMS.md` · `docs\TEST_DATA.md` · `docs\HANDOVER.md` · `docs\directives\S154.md`(새 파일) **넷과 하나뿐이다** | S154 |
| `src`·`tests`·`tools`·`data` | **한 줄도 안 고쳤다** — `git diff --stat` 빈 출력 | S154 6절 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,701 passed · 0 failed · 1 xfailed · skip 0** (수집 **1,702**) · **3분 39초** | S154 0-2 · 1번 PC · **깨끗한 판** (다른 프로젝트 python 0개) |
| 마감 앞단 전체 | **같은 값** · **3분 39초** | S154 6절 · 1번 PC · **오염된 판** — `site-energy` streamlit 둘이 함께 돌았다. **견주는 데 쓰지 않는다** |
| 상한 8분까지 | **4분 21초 남는다** (1번 PC · 착수 판 기준) | S154 0-2 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상**이다 | S114 |
| 살아 있는 못 | **1건** — `tests\test_power_factor.py::test_역률_100_에서도_후_역률이_100_을_안_넘는다` (`xfail(strict=True)`) | S153 2절이 박았다 · S154 는 그대로 뒀다 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S154 6절 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S154 6절 |
| 소요 | 도구 **117.5초** / 벽시계 **125초** | S154 6절 · 1번 PC |
| 회귀값 여덟 | 아래 표 · **불변** | S154 6절 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

## 4. 화면·산출물 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `screen_audit` 조건별 문구 수 | **957** (을) · **807** (갑Ⅰ) · **961** (교육갑) · **808** (교육갑저압) | S154 0-3 · 6절 — **두 판 다 같다** |
| 규칙 위반 | **없음** | 같은 두 판 |
| 소요 | **35.2초** (1번 PC) | S154 6절 |

## 5. 역률 상한 갈래 — S154 가 값으로 잰 것

| 무엇 | 값 |
|---|---|
| 죽는 자리 | `src\kwise\tariff\power_factor.py:157` (`lagging_adjustment_ratio`) · 부른 곳 `src\kwise\measures\solar.py:685` (`_evaluate_point`) |
| 예외 | `ValueError: 역률은 0~100% 여야 합니다: 100.00000000000001` |
| 뿌리 | `solar.py:286` 의 `100.0 * after / math.hypot(after, reactive)` · 역률 100 이면 `reactive = 0.0`(`:283`) 이라 `100.0*after/after` 가 된다 |
| 넘기는 용량 | **스물하나 가운데 셋** — 24.000 · 52.000 · 60.000 kWp. 68·72 kWp 는 `99.99999999999999`. 0 과 80 kWp 는 정확히 `100.0` |
| 죽는 역률 | **100 하나** — 90·92·95·96·97·98·99 는 다 산다 |
| 역률요금 (기본요금 합 1,665,000원 · 4개월) | 90 **+6,660원** · 92 **0원** · 95 **−9,990원** · 96 **−13,320원** · 97~100 **−16,650원** |
| 카드 절감액 | 90 **23,310원** · 92 **16,650원** · 95 **6,660원** · 96 **3,330원** · 97~100 **0원** |
| 카드 목표 | 여덟 다 **97.0%** (`default_target_pct` = 감액 상한) |
| 「개선할 것이 없다」 가 서야 할 자리 | **19** — 카드 5(`measures\power_factor.py:61·133·149·151·165`) · 역률요금 4(`tariff\power_factor.py:156·162·180·199`) · 산출물 6 · 화면 4 |
| 이미 서 있는 자리 | `report\document.py:353` — 「지상역률 100% 에서 목표 97% 로 올릴 여지가 없습니다.」 **PPT·Word 가 이 한 자리를 함께 쓴다** |
| 안 서는 자리 | Excel(`excel.py:398` 「역률 개선 (100.0 → 97.0%)」) · 화면(`views\measures.py:699` 지표 「도입 후 97.0%」) |
| 97 초과 벌 | **덱 17 + 케이스 11 = 28 가운데 `small-ind-a1`(100.0) 하나.** 92 초과도 그 하나뿐이고 나머지는 92 간주 스물여섯 · 85 둘(`large-b-pf85`·`C8`) |
| 역률 100 의 출처 | **청구서 값** (`docs\TEST_DATA.md` 7절 「청구서 대조」 — 계약종별 · 계약전력 75 kW · 최대수요전력 32 · 역률 100) |
| 원문 쪽 | 합본 PDF **456쪽** 중 **105·107·109쪽** (약관 쪽수 91·93·95) · 기준 92 / 진상 95 · 감액 상한 97 · 미달 하한 60 · 매 1%당 0.2% · 미설치 간주 92 |

## 6. 「기본요금의 기준」 어긋남 — S154 2절

| 무엇 | 값 |
|---|---|
| PPT 장15 (ESS) | 「산업용전력(갑)Ⅰ 은 기본요금을 **계약전력**으로 매깁니다 (기본공급약관 제68조 제2항)…」 — `measures\ess.py:1462` |
| PPT 장07 (그림 캡션) | 「월별 요금 구성 — 기본요금은 **직전 12개월 최대수요**로 정해져 매달 같습니다.」 — `report\slides.py:1423` |
| 값이 갈리나 | **안 갈린다.** 기본요금 기준전력이 다섯 달 다 **75 kW = 계약전력** · 부록 A 「75.000 kW × 5,550 원/kW × 4.00개월 = **1,665,000원**」. 장7 캡션대로면 53 kW · 1,176,600원(**488,400원 차**)이지만 **뜨는 수는 하나이고 옳다** |
| 판정 | **「가」 가 아니다** — 한 이름이 두 뜻인 **결함 유형 ③** · 갈래는 **「나」** |
| 어느 벌에서 나나 | `base_fee_basis="contract"` 갈래에서만. `small-a2`(요금적용전력 기준)에는 장15 문장이 없고 장07 캡션은 두 벌에 똑같이 선다 |
| 어느 산출물이 어긋나나 | **PPT·Word 둘.** **Excel 은 계약전력 쪽 문장을 안 실어 안 어긋난다** — S153 이 「셋 다」 라 적은 것이 실은 둘이다 |
| 만드는 자리 | **여덟** — 사본 셋(`diagnose\contract.py:58`·`measures\contract.py:155`·`report\notices.py:73`) · `slides.py:1423` · 용어집 `narrative.py:204`·`:206` · `worksheet.py:258` · `ess.py:1462`. **뿌리가 하나가 아니다** |
| `fact_sites.json` | **이 사실은 없다.** 등재된 아홉 중 「기본요금 산식 문장」 이 가장 가까운데 그물이 `… × 단가` 꼴만 잡는다 |

## 7. 122일 경고 — S154 3절

| 무엇 | 값 |
|---|---|
| Excel | `요약!C55` 「기간이 122일로 12개월 미만입니다. 12개월 환산 시 경고를 붙이십시오.」 · `요약!C62` 「…연간 환산 결과에 경고를 붙여야 합니다.」 (**서로 다른 문장 둘**) |
| Word | 3.1 절에 앞 문장 **한 번** |
| PPT | **0건.** 하는 말은 장03 「…(122일 · 기본요금 4.00개월분)」 과 장08 「※ 금액은 12개월 환산 기준입니다」 둘뿐이다 |
| PPT 가 안 부르는 자리 | `report\slides.py:1952` — `_cautions` 가 **그림 굽기 실패 폴백에만** 있다. 그림 실패 0 이면 한 줄도 안 뜬다 |
| 문턱 | `period_days < 365` · 자리 넷 (`io\usage.py:705` · `quality\checks.py:252` · `tariff\engine.py:308`·`:886`) |
| 기간 | `small-ind-a1` **121.9895833…일** · `small-a2` **364.9895833…일** |

## 8. 자릿수 둘 — S154 4절

| 무엇 | 자리 |
|---|---|
| 축이 「0.0000~0.0200 억원」 | `report\figures.py:1061` 이 `/ 1e8` · `:1069` 가 「요금 (억원)」 |
| PPT 가 기준 둘을 라벨 없이 둔다 | 기간값 `report\document.py` 의 수단별 `_*_conclusion`(역률 `:339`) · 환산값 `document.py:509 _annual_saving`. 실물 장12 「16,000원」 대 「49,000원」 · 장08 「49만원」 대 「1,465,000원」 |
| Excel 은 열 이름으로 가른다 | `report\excel.py:401`·`:402` |
| Word 는 한 칸에 함께 | `report\document.py:501 _measure_saving` |

## 9. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **88건** (① 7 · ② 80 · ③ 1 · 줄 85) | S154 5-4 · `daily_brief` |
| 앞 판 | 86건 (① 7 · ② 78 · ③ 1 · 줄 83) | S153 |
| 대조 | **닫은 것 0 · 연 것 2 · 이름을 고친 것 1 · 갈래를 옮긴 것 1** | S154 5-4 |
| 연 것 | 역률이 감액 상한 이상일 때 Excel·화면이 「개선」 이라 적는다 · 「기간이 365일로 12개월 미만입니다」 가 제 말을 부정한다 | 둘 다 「나」 |
| 이름을 고친 것 | `small-ind-a1` 의 지역 좌표가 자료에 없다 → **받은 좌표(경상북도/구미시)를 벌에 넣고 덱으로 확인한다** | 「마」 → 「가」 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **5** · 나 **17** · 다 **6** · 라 **32** · 마 **28** | S154 5-4 |
| 미해결 칸 | **29,347자** | S154 |
| 「다음 작업」 칸 | **2,546자** (상한 3,000 · **남은 454자**) | S154 6절 — S152 판치를 걷고 S154 판치를 넣은 값이다. **다음 판은 S153 판치를 걷는다** |
| 「다시 열지 마라」 칸 | **6,218자** (상한 없음) | S154 5-1·5-3 |

## 10. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S154 6절 |
| `ruff format --check .` | **8 어긋남 · 205 통과** (합 **213**) | S154 6절. 착수 212 에서 `docs\directives\S154.md` 하나만큼 늘었다. **어긋남 여덟은 고치지 않는다** |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 168`) | S154 6절. **고치지 않는다** |
| `mypy src` | **통과** (110 파일) | S154 6절 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 약관 원문 건너뜀 1,272곳) | S154 6절 |

## 11. 이 판이 못 잰 것

- **역률 100 으로 뽑은 산출물** — 여전히 죽는다. 2·3·4절의 실물 확인은
  **역률을 비운 판**(간주 92)으로 했다. 그 판의 값 — `small-ind-a1` PPT
  **651,755바이트 20장** · Excel **310,296바이트 13시트** · Word
  **327,852바이트**(문단 218 · 표 22) · **8.9초**. 맞댈 벌 `small-a2` PPT
  **789,215바이트 19장** · Excel **864,235** · Word **366,315** · **58.7초**.
- **경상북도 구미시 좌표를 넣은 값** — 코드에 안 넣었으므로 발전량이 얼마나
  갈리는지 **안 쟀다.**
- 2번 PC 의 값 전부.
- **도구 합과 벽시계의 격차** — 이 판도 안 갈랐다.
