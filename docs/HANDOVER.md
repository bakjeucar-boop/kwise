# 인수인계 — 2026-09-10 마감 (S160)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S160 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S160 4-8 |
| 출발점 | `dfb8abd` (S159 마감) · 작업 트리 **깨끗** · `git pull` up to date | S160 0-1 |
| 덱 벌 | **18** — 정본은 `render_deck.CASES` 길이다. `key="` 는 **19** · `key=` 는 **21** | S160 4-5 · **세 방법으로 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `src\` 셋(`report\frames.py` · `report\worksheet.py` · `report\excel.py`) · `tests\` 둘(`test_dr.py` · `test_ui_screen.py`) · `PROCEED.md` · `docs\` 셋(`OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S160.md` **새 파일**) · `output\casestudy_20260910.xlsx` | S160 |
| **안 건드린 것** | `data\` · `tools\` · `reference\` — **한 줄도 안 고쳤다** | S160 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,723 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,723**) · **3분 52초** | S160 0-1 · 1번 PC · **깨끗한 판** |
| 마감 앞단 전체 | **1,726 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,726**) · **4분 17초** | S160 4-1 · 1번 PC · **오염된 판** |
| 마감을 두 판 돌았다 | 첫 마감 판은 **1,724 passed + 2 failed**(4분 51초) — 「다음 작업」 칸이 항목을 번호(`②-74`)로 부르고 세션 행의 미해결 대조에 이름이 없어 못 둘이 물었다. 고치고 다시 돌린 것이 위 값이다 | S160 4-1 |
| 판이 깨끗했나 | **착수는 깨끗하고 마감 두 판은 아니다** — 마감 때 `site-energy` 의 pytest 둘(PID 24272 · 23184)이 돌았다(남의 세션이라 안 죽였다). **착수와 마감의 소요를 견주지 않는다** | S160 0-1 · 4-1 |
| 왜 셋 늘었나 | **이 판이 세운 못 셋이 그대로다** — `test_dr.py` 둘(`test_저부하_기준선_줄은_접은_값으로_다시_곱하지_않는다` · `test_기준선_줄을_밖에서_다시_짓지_않는다`) · `test_ui_screen.py` 하나(`test_계시별_구성_라벨의_백분율_합이_100_이다`). **늘어난 수와 세운 못 수가 같으므로 죽은 시험이 없다** | S160 4-1 |
| 상한 8분까지 | **3분 43초 남는다** (1번 PC · 마감 판 · **오염된 판이다**) | S160 4-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| 못을 되돌려 확인했나 | **셋 다 했다.** 어느 자리를 어떻게 틀렸는지는 아래 5절 | S160 2-4 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S160 4-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S160 4-2 |
| 소요 | 도구 **135.9초** / 벽시계 **144초** | S160 4-2 · 1번 PC · **깨끗한 판**(앞뒤로 다른 프로젝트 python 0개 · streamlit 0개) |
| 회귀값 여덟 | 아래 표 · **불변** | S160 4-2 |
| 왜 안 움직여야 맞나 | 이 판이 고친 것은 `frames`·`worksheet`·`excel` 이고 **이 도구의 여덟은 `report\casestudy.py` 가 스스로 센다** — 그 계산에 안 닿는다 | S160 4-2 |
| 관측 최대는 어디서 읽나 | 화면 표에 안 나온다 — `output\casestudy_20260910.xlsx` 「케이스」 시트의 **「관측 최대수요(kW)」 열** | S158 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

**접는 문턱이 실제로 가르는 벌은 여기 하나뿐이다** (S160 3절) —
**C6 야간 피크형**: 관측 **10,920.64 kW** · 요금적용 **3,604 kW**.
덱 18벌에는 그런 벌이 없다.

## 4. 화면 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 고치기 **전** | **957** (을) · **807** (갑Ⅰ) · **964** (교육갑) · **808** (교육갑저압) | S160 0-2 — S159 마감값과 같다 |
| 고친 **뒤** | **957** · **807** · **964** · **808** | S160 2-5 · **하나도 안 늘렸다** |
| S159 의 +3 은 무엇이었나 | S158 마감(`8eb67b6`)과 HEAD 의 `--list` 차집합 — **는 여섯**(「역률 감액 변화」·「기본요금이 줄면 역률 감액도 준다」·「50,409,000원」·「자가소비 절감액」·「기본 + 전력량 + 역률」·「5,293.0 kW」) · **주는 셋**(「절감액」·「기본 + 전력량」·「5,293.4 kW」). 셋 다 `db26075` 에서 왔다 | S160 0-2 |
| 왜 교육갑만 움직였나 | 갑Ⅰ·교육갑저압은 **계약전력 기준**이라 태양광 기본요금 절감이 **0원** · 을은 기본 23,180,000 + 전력량 250,257,000 = 절감액 273,437,000 으로 **남는 몫이 없다** · 교육갑만 **50,409,000원**이 남는다 | S160 0-2 |
| 규칙 위반 | **없음** | S160 2-5 |
| 화면 예산 | **열여덟 자리 모두 한도 안** | S160 2-5 |

## 5. 이 판이 고친 둘 — S160 2절

| 무엇 | 고친 것 | 못 · 되돌려 확인한 것 |
|---|---|---|
| 화면 계시별 사용량 구성 겨울 99% | `report\frames.py` 에 **`whole_percents`** 를 세우고 「라벨」 이 그 자리를 지나게 했다. **저마다 반올림하고 잔차를 가장 큰 몫에 준다** — 겨울이 50+31+19 = 100. **계산값 「비중」 은 안 건드렸다**(조각 크기는 날값이 그린다). 몫이 통째로 0 인 표는 안 접는다 | `test_ui_screen.py::test_계시별_구성_라벨의_백분율_합이_100_이다` — **잔차 배분을 끄니** [49,31,19] · **라벨을 옛 꼴로 되돌리니** 표본 겨울이 **42+35+24 = 101** |
| Excel 진단 DR 기준선 | `report\worksheet.py` 에 **`low_load_threshold_line`**(과 `low_load_multiple_text`)을 세우고 `report\excel.py` 가 그 자리에서 받게 했다. **자리를 하나로 모았다** | `test_dr.py::test_저부하_기준선_줄은_접은_값으로_다시_곱하지_않는다` — **자릿수를 0 으로 되돌리니** 「36 kW × 1.2 = 44 kW」 / `test_dr.py::test_기준선_줄을_밖에서_다시_짓지_않는다` — **Excel 에 f-서식을 도로 넣으니** `report\excel.py:676` 을 짚는다 |

**두 자리는 한 뿌리가 아니다 — 둘이다** (S160 1-2)

| 자리 | 뿌리 | 만드는 자리 |
|---|---|---|
| 겨울 99% | 세 몫을 **저마다** `:.0f` 로 접어 **합이 100 이 아니다.** 날값 합은 100.000000 이므로 **틀린 것은 계산이 아니라 접는 법**이다 | `frames.band_frame` 의 「라벨」 **하나** |
| DR 기준선 | **접은 값을 다시 곱하는 자리가 없다.** 값은 내내 날값이고 갈리는 것은 **자릿수 하나** — Excel 만 `,.0f` 라 「36 kW × 1.2 = 44 kW」 로 **등호가 셈이 안 맞았다**(36 × 1.2 는 43.2). **S158 이 적은 「접힌 값으로 다시 곱한다」 는 틀렸다** | 적는 자리는 넷이 아니라 **셋** — 화면 카드 · `demand_response_worksheet`(화면 계산 근거·PPT·부록 A) · Excel 진단 |

**저장소에서 다시 잰 값** (S160 1-1 · 벌 `small-a2-pf100-offset`)

| 무엇 | 값 |
|---|---|
| 겨울 계시 날값 | 경부하 **49.3514** · 중간부하 **31.3891** · 최대부하 **19.2595** (합 **100.000000**) |
| 다른 계절 | 봄가을 52+29+19 · 여름 48+33+19 · 전 기간 50+31+19 — **다 100 이다** |
| DR 기준선 | **36.29270989408422** kW |
| DR 배수 | **1.2** |
| DR 문턱 | **43.55125187290106** kW — **날값 × 배수와 같다.** 접은 값 × 배수는 **43.2** 로 다르다 |
| 화면·PPT·부록 A 가 적던 글 | **36.3 kW × 1.2 = 43.6 kW** |
| Excel 이 적던 글 | **36 kW × 1.2 = 44 kW** |
| 지시서 값과 다르다 | 지시서의 34.6/41.6 · 35/42 는 **사용자 산출물의 값**이고 이 벌은 위 값이다. **꼴은 그대로 재현된다** |

## 6. 재기만 한 하나 — 접는 문턱 1% (S160 3절)

| 무엇 | 값 |
|---|---|
| 조건 | `billing_demand_kw < peak_kw * 0.99` |
| 자리 | **셋** — `ui\views\diagnose.py:805`(지표 칸 구성 · 캡션 한 줄 · `monthly_peak_chart(split=)` 의 계열 수) · `report\slides.py:1258`(PPT 칸 구성과 이름) · `report\document.py:1532`(Word 결론에 한 줄을 붙일지) |
| 무엇을 가르나 | **셋 다 표시다 — 계산도 판정도 아니다.** 값을 정하는 자리는 `tariff\demand.py::apply_contract_floor` 하나이고 셋은 읽기만 한다 |
| 대형 벌에서 1% | **52.93 kW** (관측 5,293.44 · 요금적용 5,293.00 · 차 0.44) |
| 약관 제7조 ① | 계산단위 **1 kW** (`tariff\demand.py::round_kw` 가 소수 첫째자리에서 반올림) — **1% 잣대가 53배 느슨하다** |
| 작은 벌 | `small-ind-a1`(52.88 kW)은 1% 가 **0.53 kW** 로 반올림 폭 0.5 와 거의 같다 — **관측 최대가 50 kW 미만이면 반올림 하나만으로 두 칸이 갈린다**(제7조 ① 로는 같은 수인데) |
| 덱 18벌 | **한 벌도 안 가른다.** 음수도 접힌다 — `small-b` −0.32 · `large-b-over` −706.56 · `small-a2-was` −77.72 (하한이나 계약이 이긴 자리) |
| 가르는 벌 | 케이스 **C6 야간 피크형** 하나 (관측 10,920.64 · 요금적용 3,604) |
| 고쳤나 | **안 고쳤다.** 계산에 닿는 변경이라 조사와 수정을 다른 판으로 갈랐다. 미해결에 이름으로 올렸다 |

## 7. 이 판이 새로 본 것 — 「역률 감액 변화」 의 이름 (S160 0-2)

| 무엇 | 값 |
|---|---|
| 어디 | `report\frames.py::solar_worksheet` — S159 3-1 이 세운 줄이다 |
| 어떻게 낸 값인가 | `총 절감액 − 기본 − 전력량 − 잉여` 를 **되짚어** 뺀다 |
| 왜 이름이 틀렸나 | `BillingResult.total_won` 이 **기본·전력량·역률·초과사용부가금** 넷의 합이다 — 남는 몫에 역률만 있는 것이 아니다 |
| 값 | 교육갑 **50,409,000원**. 같은 조건 기본요금 절감이 **15,462,000원**이라 역률 감액(기본요금 × 조정률 몇 %)일 수 없다 |
| 왜 교육갑만인가 | **초과사용부가금 1,173,161,000원을 총액에 넣는 유일한 조건**이다. 갑Ⅰ·교육갑저압은 「초과사용부가금 대상인데 이 종별은 산출하지 않았습니다」 · 을은 계약 6,000 kW 라 초과가 없다 |
| 고쳤나 | **안 고쳤다 — 범위 밖이라 이름만 올렸다.** 길 둘 — 줄을 「역률 + 초과사용부가금」 으로 가르거나, 이름을 「그 밖」 으로 넓히거나 |

## 8. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check src tests tools` | **All checks passed** | S160 4-4 |
| `ruff format --check .` | **9 어긋남 · 211 통과** | S160 4-4. **S159 가 적은 「8」 은 실물과 다르다** — HEAD 에서 이 판의 변경을 치우고 재도 9 다(`document.py` · `excel.py` · `slides.py` · `engine.py` · `test_casestudy.py` · `test_document.py` · `test_excess.py` · `test_tariff_engine.py` · `test_ui_screen.py`). **안 늘렸고 안 고쳤다** |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 169`) | S160 4-4. **안 고친다** |
| `mypy src` | **통과** (110 파일) | S160 4-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S160 4-4 |
| **mypy 뿌리 수** | **169** (src 110 · tests 39 · tools 20) — 도구가 찍는 `checked 169` 와 **같다**. **못이 문다**: `test_doc_counts.py` 의 「mypy 검사 파일 수」 | S159 0-2 · 이 판도 같다 |
| `tools\count_sites.py` | 그물 **아홉** — 이 판이 다루는 「표시용 반올림」 을 담는 그물은 **없고 새로 넣지 않았다**(범위 밖) | S160 1-2 |
| 덱 | `small-a2-pf100-offset` pptx **748 KB** · png **18장** · **그림 실패 0개** · **33.6초** | S160 4-5 |

## 9. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **98건** (① 7 · ② 90 · ③ 1) | S160 4-6 · `daily_brief.total_items` |
| 앞 판 | 98건 | S159 |
| 대조 | **닫은 것 2 · 연 것 2 — 수는 안 움직이고 갈래가 움직였다** | S160 4-6 |
| 닫은 둘 | 화면 계시별 사용량 구성에서 겨울이 49+31+19 = 99%다(가) · Excel 진단 DR 기준선이 「35 kW × 1.2 = 42 kW」 다(가) | S160 2절 |
| 연 둘 | 태양광 계산 근거의 「역률 감액 변화」 줄이 실은 초과사용부가금 몫이다(나) · 요금적용전력과 관측 최대가 같은지 보는 문턱이 1% 라 제7조 ①의 1 kW 와 다르다(나) | S160 0-2 · 3절 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **7** · 나 **26** · 다 **7** · 라 **28** · 마 **30** · 합 **98** — **절 제목·본문·갈래 표 셋을 다 갈았다** | S160 4-6 |
| **갈래 표를 갈았다** | **S147·S153·S157 로 세 판째 안 갈리던 자리다** — 그 셋은 절 제목의 수만 갈고 표의 「지금」 열을 한 판 뒤처진 채 두었다 | S160 4-6 |
| **셋째 방법** | 절 제목 합 **98** · `daily_brief.total_items` **98** · 브리핑이 편 **줄 95** — 앞 둘이 같으면 맞는다. **셋째는 원래 다른 수다** — ① 갈래가 머리말 「7건」 에 항목 넷이라 셋이 덜 선다 | S160 0-3 · 4-6 |
| 0-3 이 무엇이었나 | S159 의 101 → 98 을 이름으로 폈다 — **닫은 넷 · 연 하나 · 이름 간 하나 · 합쳐진 하나.** 항목이 99 → 95 로 넷 준 것은 「운영시간 외 부하」 가 띄운 가운뎃점에서 둘로 갈려 있다가 하나로 붙어서다 | S160 0-3 |
| 「다음 작업」 칸 | **2,791자** (상한 3,000 · **남은 209자**) | S160 4-7 — **S158 판치를 걷었다. 남긴 판치는 S160·S159. 다음 판은 S159 판치를 걷는다** |
| 판치를 걷기 전 확인 | S158 판치의 값 여섯 가운데 **다섯이 158세션 절에 있다**(열여덟 등재 · `use_key` 0자리 · 덱 벌 셈 17/18/20 · `output\` 28파일 · 상계거래 벌 없음). 남은 하나(「등재한 것은 이차 자료라 저장소에서 먼저 잰다」)는 **미해결 항목 본문들과 `docs\OPEN_ITEMS.md` 가 쥔다** | S160 4-7 |
| 용인 조건 넷 | **안 닫혔다 — 남은 몫은 DR 하나다.** S159 1절이 `small-a2-pf100-offset` 에 역률 100 · 태양광 32.0 kWp · 잉여 상계거래 셋을 세웠고, `Case` 에 **DR 정산 단가·SMP·외부 판매 단가·투자비 필드가 없으며** `CaseDefinition` 은 열하나 그대로다 | S160 0-3 |
| 월별 명세 2026-08 | **이름을 안 갈았다** — S159 가 이미 「사용자 DOM 에서 12행인데 저장소에서는 13행이다」 로 갈아 두었고 값 셋(13행 · 365일 · 476.0 MWh)과 「남은 자리는 `st.dataframe` 이고 안 쟀다」 가 본문에 다 있다 | S160 0-4 |

**S161 이 이어받을 것 — 미해결 이름 그대로**

- 태양광 계산 근거의 「역률 감액 변화」 줄이 실은 초과사용부가금 몫이다
- 요금적용전력과 관측 최대가 같은지 보는 문턱이 1% 라 제7조 ①의 1 kW 와 다르다
- Excel 「태양광 용량 곡선」 시트도 그 안에서 안 맞는다 — 역률 몫이 빠진다
- 「나」 갈래 스물여섯 — S158 이 등재한 것 가운데 아직 저장소에서 안 잰 것들

## 10. 이 판이 못 잰 것

- **`st.dataframe` 이 DOM 에 그리는 행 수** — S159 가 남긴 자리다. 브라우저가 있어야 한다.
- **마감 회귀를 깨끗한 판에서** — 두 판 다 `site-energy` 의 pytest 둘과 함께 돌았다.
  **착수 회귀와 케이스 스터디만 깨끗하다.**
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **「나」 갈래와 「태양광 용량 곡선」 시트** — 손대지 않았다. 지시서 밖이다.
- **`fact_sites.json` 에 표시용 반올림 그물을 넣는 것** — 범위 밖이라 안 넣었다.
- 2번 PC 의 값 전부.
