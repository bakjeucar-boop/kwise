# 인수인계 — 2026-09-11 마감 (S165)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S165 가 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

**S165 는 수정 판이다** — 「단순 합」 이 경제성DR 을 담게 했고 화면 요약표가 켠 ESS 를
「미산출」 로 세우게 했다. PPT 장04·장07 은 **재기만** 했고 「차이 0원」 은 **이름만**
갈았다. **이 문서는 5-6(미해결 대조)까지 끝난 뒤에 썼다** — S164 는 마감 확인보다
먼저 써서 갈래 표 어긋남과 커밋 수가 빠졌다.

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋이 마감 커밋이다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S165 5-8 |
| 출발점 | `752477b` (S164 마감) = `origin/master` · 작업 트리 **깨끗** · `git pull` up to date | S165 0-1 |
| 커밋 | **여섯** — 0·1·2·3·4·5절 하나씩 | S165 |
| 덱 벌 | **18** — `render_deck.CASES` 18 · `key="` 19(`build_ppt` 단추 하나 더) · `key=` 21(`region_key=` · `key=lambda` 더) | S165 5-5 · 벌을 **안 지었다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 | S153 0-3 |

**이 판이 만진 것** (출발점 대비 · 이 문서와 케이스 스터디 산출물 빼고)

| 파일 | +/− | 무엇 |
|---|---|---|
| `src\kwise\report\standalone.py` | +40 / −11 | `simple_sum_won` 의 `combinable_only` 를 걷었다 · ESS 결과가 없어도 줄을 세운다 · `payback_reason` 필드 |
| `src\kwise\report\notices.py` | +28 / −0 | `UNPRICED` · `ess_unpriced_reason` (판정과 사유 낱말 한 자리) |
| `src\kwise\report\document.py` | +7 / −6 | 「미산출」 글자와 ESS 절감액 사유를 `notices` 에서 받는다 |
| `src\kwise\report\slides.py` | +2 / −1 | 「미산출」 글자를 `notices` 에서 받는다 |
| `src\kwise\ui\views\compare.py` | +6 / −1 | 지표가 요약표와 같은 식을 부른다 · `ess_optimum`·`ess_curve` 를 넘긴다 |
| `tests\test_doc_counts.py` | +27 / −0 | 갈래 표 못 |
| `tests\test_ui_screen.py` | +14 / −16 | 「단순 합」 못을 걷어 보통 시험으로 |
| `tests\test_document.py` | +25 / −20 | 「같은 수단」 못을 걷어 매개변수 둘로 |
| `docs\MANUAL.md` · `MANUAL.html` | +4 · +1 | 5절 「단순 합은 경제성DR 을 담는다」 |
| `docs\OPEN_ITEMS.md` · `PROCEED.md` · `docs\directives\S165.md`(새 파일) | — | 기록 |
| `output\casestudy_20260911.xlsx` | 새 파일 | 케이스 스터디가 냈다 |

**안 건드린 것** — `data\` 전부 · `reference\` · `tools\` 전부 · `input\` ·
`src\` 의 나머지(`tariff\`·`measures\`·`diagnose\`·`compare\` 는 0줄).

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,740 passed · 0 failed · 3 xfailed · skip 0** (수집 **1,743**) · **3분 49초** | S165 0-1 · 1번 PC · **깨끗한 판** |
| 마감 앞단 전체 | **1,744 passed · 0 failed · 1 xfailed · skip 0** (수집 **1,745**) · **3분 37초** | S165 5-1 · 1번 PC · **깨끗한 판** — 앞뒤로 다른 프로젝트 python **0개** |
| 상한 8분까지 | **4분 23초 남는다** (1번 PC · 깨끗한 판 · 1,745건) | S165 5-1 |
| 수집 +2 | 세운 못 하나(`test_갈래_표의_지금_열이_갈래_절_제목과_같다`) + 걷은 못이 매개변수 둘로 선 몫 하나 | S165 5-1 |
| xfailed 3 → 1 | 걷은 둘이 passed 로 옮겼다 — passed +4 = 걷은 둘 + 새 못 + 매개변수 한 갈래 | S165 5-1 |
| 살아 있는 못 | **1건** — `tests\test_slides.py::test_두_장이_피크_여지를_반대로_말하지_않는다` | S165 3-4 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S165 5-2 |
| 기상 취득 | **0회** | S165 5-2 |
| 소요 | 도구 **110.0초** / 벽시계 **117.6초** | S165 5-2 · 1번 PC · **깨끗한 판** |
| 회귀값 여덟 | 아래 표 · **불변** | S165 5-2 |
| 왜 안 움직여야 맞나 | 이 판이 고친 것은 표시(「단순 합」 · ESS 줄 · 「미산출」 글자)뿐이다 | S165 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | 5,293.44 kW | 5,293 kW | 49.0% | 13.5% |
| R1 | 132.28 kW | 132 kW | 41.1% | 20.0% |
| R2 | 132.28 kW | 132 kW | 41.1% | 18.3% |
| R3 | 132.28 kW | 132 kW | 41.1% | 27.4% |

관측 최대는 화면 표에 안 뜬다 — `output\casestudy_20260911.xlsx` 「케이스」 시트에서 읽는다 (S158 5-2 가 정한 자리).

## 4. 화면 감사

| 무엇 | 고치기 전 | 고친 뒤 · 마감 | 언제 |
|---|---|---|---|
| 을 | 957 | **957** | S165 2-4 · 5-3 |
| 갑Ⅰ | 807 | **812** (+5) | 〃 |
| 교육갑 | 964 | **964** | 〃 |
| 교육갑저압 | 808 | **813** (+5) | 〃 |
| 규칙 위반 ①② | 없음 | **없음** | 〃 |
| 소요 | 39.2초 | 37.9초 · 32.2초 | 1번 PC · 셋 다 깨끗 |

**+5 는 3단계 요약표 ESS 줄의 칸 다섯이다** — 갑Ⅰ·교육갑저압은 ESS 가 성립 안 함
갈래라 이 판에 줄이 섰고, 을·교육갑은 ESS 결과가 서서 줄이 이미 있었다. **뺀 것** —
투자비 칸의 「— 사양 미정」(절감액 칸과 겹친다 · 칸 수는 그대로라 수는 안 준다).
**걷지 않은 후보** — 「미검토 — 없음」 아래 「미검토는 '효과가 없다' 가 아니라 '보지
않았다' 입니다.」 는 화면에서 `known-limits` 로 가는 **유일한 툴팁**이라 두었다.
예산(`tools\screen_budget.py`) 「모든 자리가 한도 안」.

## 5. 고친 것 — 화면 3단계

**「단순 합」** — 덱 벌 `small-a2-pf100-offset` 에 화면 위젯으로 DR 120 원/kWh 를 세워 쟀다.

| 자리 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 요약표 합계 행 | 588만원 | **588만원** |
| 합산효과 지표 「단순 합」 | 517만원 | **588만원** |
| 계산 근거 「단순 합」 | 5,170,000원 | **5,876,000원** |
| 지표 「차이」 | −3만원 · −0.5% | **−73만원 · −12.4%** (합산효과가 DR 을 안 담는다) |
| 조합에서 태양광 체크를 풀면 | 요약표 588 · 지표 0원 · 차이 0원 「—」 | 요약표 588 · 지표 **71만원** · 차이 **−71만원 · −100.0%** |
| Excel·PPT·Word | 글자 142,174 · 230 · 401줄 | **다른 줄 0** (stash 로 전후를 뽑아 맞댔다) |

**ESS 줄** — 판정과 사유 낱말을 `report\notices.py::ess_unpriced_reason` 하나로 두고
화면 `standalone_rows` 와 PPT·Word `measure_entries` 가 함께 부른다.

| 덱 벌 | 화면 요약표 ESS 줄 | PPT 장08 ESS 줄 |
|---|---|---|
| 일반용(을) 넷 · `large-edu-b` · `large-ind-b` (여섯) | 금액이 선다(이미 있었다) | 같다 |
| `small-b` · `small-b-sell` · `small-a2` · `small-a2-pf100-offset` · `small-edu-a` · `small-edu-a-over` · `small-ind-a2` (일곱) | **새로 섰다** — 「미산출 — 최소 규격에 못 미쳐 사양을 정하지 않았습니다.」 | 미산출 |
| `large-a` · `small-a` · `small-a-short` · `small-ind-a1` (넷) | **새로 섰다** — 「미산출 — 성립하지 않아 사양을 정하지 않았습니다.」 | 미산출 |
| `small-a2-was` (하나) | **없다** | **없다** — 곡선이 안 서는 셋째 갈래(`curve.best is None`) |

S164 는 「열둘에서 는다」 라 봤는데 실물은 **열하나**다 — `small-a2-was` 는 두 표 다
빠지는 셋째 갈래다. **Excel 「수단별 결과」 도 ESS 줄이 없다**(넷째 목록). 둘 다
이름으로만 올렸다. 열여덟 다 그림 실패 0.

**다 모으지 못한 까닭** — 화면 줄(`StandaloneRow`)은 단순 합을 더하려고 금액을
float 로 쥐고 PPT 항목(`MeasureEntry`)은 금액을 글자로만 쥔다. 한쪽에서 다른 쪽을
못 만든다. `measure_entries` 의 ESS `elif` 둘도 그대로 두었다 — 사유 함수가 같은
조건을 다시 적고 못이 두 갈래를 다 맞댄다.

## 6. 잰 것 — PPT 장04·장07 (고치지 않았다)

장04 `narrative.pattern_lead` 는 부하율만(`load_factor_flat` 0.70), 장07
`narrative.structure_lead` 는 기본요금 비중만(`base_fee_share_low` 0.20 · `_high` 0.35)
본다. 값은 두 함수에 들어간 것을 가로채 받았다.

| 갈래 | 벌 수 | 벌 | 두 장이 부딪치나 |
|---|---:|---|---|
| ① 부하율 아래 + 비중 아래 | **15** | `large-b` · `large-a` · `small-b` · `small-a` · `small-a-short` · `large-b-over` · `large-b-short` · `large-b-pf85` · `small-b-sell` · `small-a2-pf100-offset` · `small-edu-a` · `small-edu-a-over` · `large-edu-b` · `small-ind-a2` · `large-ind-b` | **부딪친다** — 「피크를 낮출 여지가 큽니다」 대 「피크를 낮춰도 줄어드는 몫이 작습니다」 |
| ② 부하율만 아래 | **3** | `small-a2`(20.030%) · `small-a2-was`(23.365%) · `small-ind-a1`(23.388%) | 안 부딪친다 — 장07 가운데 갈래 |
| ③ 비중만 아래 | **0** | 없음 | (코드상) 안 부딪친다 |
| ④ 둘 다 위 | **0** | 없음 | (코드상) 장07 높음이면 부딪친다 |

벌마다 두 값은 `PROCEED.md` 165세션 3-2 표에 있다. **부하율 최대 49.0%** 라 「평탄」
문장은 한 번도 안 서고 **비중 최대 23.4%** 라 「비중 높음」 문장도 안 선다.
**역률 하나가 장07 을 가른다** — `small-a2`(간주 92) 20.030% 대
`small-a2-pf100-offset`(역률 100) 19.869%.

## 7. 못

| 못 | 무엇 | 되돌려 확인 |
|---|---|---|
| `tests\test_doc_counts.py::test_갈래_표의_지금_열이_갈래_절_제목과_같다` | **세웠다** — 갈래 표 「지금」 열을 절 제목과 갈래마다 맞댄다 | 「다」 만 8 로 두니(합 98 그대로) 빨개졌다 |
| `tests\test_ui_screen.py::test_단순_합이라는_이름이_한_값만_가리킨다` | **걷었다** — DR 이 합에 담기는지 · 인자가 다시 생기지 않는지 | 인자를 되살리면 · 합이 DR 을 빼면 빨개졌다 |
| `tests\test_document.py::test_화면_요약표와_보고서_요약표가_같은_수단을_센다` | **걷었다** — 갈래 둘을 매개변수로 · 사유 낱말까지 맞댄다 | 보고서 ESS 갈래 둘을 끄면 · 화면 회수기간을 옛 말로 두면 빨개졌다. **하나만 끄면 안 문다** — 남은 갈래가 같은 사유 함수를 불러 표 글자가 안 바뀐다 |
| `tests\test_slides.py::test_두_장이_피크_여지를_반대로_말하지_않는다` | **남겼다** (xfail) | 이 판은 안 건드렸다 |

## 8. 도구가 낸 수

| 무엇 | 값 | 언제 |
|---|---|---|
| `ruff check .` | **All checks passed** | S165 5-4 |
| `ruff format --check .` | **9 어긋남** — 앞 판과 같다. 이 판이 건드린 넷(`document.py` · `slides.py` · `test_document.py` · `test_ui_screen.py`)이 그 안에 드는데 **이 판의 줄에 닿는 조각은 0** | S165 5-4 |
| 맨 `mypy` | **10건** (170파일) | S165 5-4 |
| `mypy src` | **통과** (111파일) | S165 5-4 |
| `mypy tests tools` | **36건** (59파일) | S165 5-4 |
| `tools\scan_ctrl.py` | 제어문자 0 · 탭 0 | S165 5-4 |

## 9. 미해결

| 무엇 | 값 | 언제 |
|---|---|---|
| 건수 | **100건** (① 7 · ② 92 · ③ 1) | S165 5-6 · `daily_brief.total_items` |
| 앞 판 | 98건 | S164 |
| 셋째 방법 | 절 제목 합 **100** · `total_items` **100** · 줄 **97** — 차 3 이 「청구서 4」 뭉침과 같다 | S165 5-6 |
| 갈래 | 가 **7** · 나 **24** · 다 **7** · 라 **27** · 마 **35** — 표 「지금 (S165)」 열도 같다(못이 문다) | S165 5-6 |
| 「다음 작업」 칸 | **2,073자** (상한 3,000) — S163 판치를 걷었다. 남긴 판치는 S165·S164 | S165 5-7 |

**닫은 둘** — 「「단순 합」 이 두 자리에서 577만원과 517만원이다」(화면 3단계) ·
「3단계 「개선안별 요약」 표에 ESS 행이 없다」(화면 3단계).

**연 넷** — 「조합에서 체크를 푼 수단을 요약표 합계는 담고 합산효과의 단순 합은
뺀다」(화면 3단계 · 사람이 정한다) · 「단순 합이 경제성DR 을 담자 합산효과의 「차이」 에
DR 몫이 함께 선다」(화면 3단계 · 사람이 정한다) · 「Excel 「수단별 결과」 시트는 ESS
결과가 없으면 줄을 안 세운다」(Excel) · 「초과 구간이 없어 ESS 곡선이 안 서는 벌은
검토함에 ESS 가 있는데 화면·PPT 요약표 어디에도 줄이 없다」(화면·PPT · `small-a2-was`).

**이름을 간 하나** — 「「차이 0원 / 0.0%」 인데 차이의 이유가 뜬다」 → 「조합 비교의
차이 표시가 반올림으로 0이 될 수 있는데 이유 문장은 차이 크기를 안 본다」(화면 3단계).
다시 잰 것 하나 — 지표 칸은 음수 차이면 「-0원」 이다.

## 10. 이 판이 못 잰 것

- **덱 벌 열여덟의 「차이」 값 전수** — 4절은 S164 값을 옮겼다. 이 판은 최소 벌 하나만 다시 봤다.
- **화면 png** — 값은 `AppTest` 로 떴다. `capture_screen.py` 로는 안 찍었다.
- **PPT png** — 덱 바이트를 글자로만 읽었다. PowerPoint COM 으로 안 구웠다.
- **교육갑저압 조건의 ESS 줄 사유 문구** — 감사 수(+5)만 봤고 `--list` 는 갑Ⅰ 만 떴다.
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- 2번 PC 의 값 전부.
