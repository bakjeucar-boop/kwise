# 인수인계 — 2026-09-09 마감 (S155)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S155 가 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S155 4절 |
| 출발점 | `31a08e7` (S154 마감) · 작업 트리 **깨끗** | S155 0절 |
| 덱 벌 | **17** — 정본은 `render_deck.CASES` 길이다. **`grep 'key="'` 는 이제 18 을 낸다** (`render_deck.py:519` 의 `app.button(key="build_ppt")` 가 섞인다 · `key=` 로는 20) | S155 0절 |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `src\` 넷(`measures\power_factor.py`·`measures\solar.py`·`measures\__init__.py` · `report\excel.py`·`report\standalone.py`·`report\worksheet.py` · `ui\views\measures.py`) · `tests\test_power_factor.py` · `tools\render_deck.py` · `docs\` 넷(`MANUAL.md`+`MANUAL.html` · `TEST_DATA.md` · `OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S155.md`) · `PROCEED.md` · `output\casestudy_20260909.xlsx` | S155 |
| `data\` | **한 줄도 안 고쳤다** — 상한 값은 `rules_kr.json` 에서 **읽기만** 한다 | S155 1-2 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,701 passed · 0 failed · 1 xfailed · skip 0** (수집 **1,702**) · **3분 37초** | S155 0-2 · 1번 PC · **깨끗한 판** (앞뒤로 다른 프로젝트 python 0개) |
| 마감 앞단 전체 | **1,709 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,709**) · **3분 38초** | S155 4-1 · 1번 PC · **깨끗한 판** |
| 늘어난 일곱 | 새 못 셋이 매개변수로 일곱이 된다 — 감액 상한 넷(97·98·99·100) · 상한 아래 둘(95·97) · 용량 훑기 하나 | S155 4-1 |
| passed 가 여덟 는 까닭 | 위 일곱 + **걷은 xfail 하나가 초록으로 옮겨왔다** | S155 4-1 |
| 문서를 고친 뒤 확인 판 | **같은 1,709 passed · 4분 10초** — 마감 판보다 **32초 길다.** 시작 전 다른 프로젝트 python 0개 · 끝난 직후 python 둘이 잡혔다 사라졌다(이 판 자신의 일꾼으로 보이나 **안 갈랐다**). **견주는 데 쓰지 않는다** | S155 4-1 |
| 상한 8분까지 | **4분 22초 남는다** (1번 PC · 마감 판 기준) | S155 4-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상**이다 | S114 |
| 살아 있는 못 | **0건** — S153 이 박은 것을 S155 1-4 가 걷었다. `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S155 4-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S155 4-2 |
| 소요 | 도구 **109.7초** / 벽시계 **117초** | S155 4-2 · 1번 PC |
| 회귀값 여덟 | 아래 표 · **불변** — 케이스 열하나가 역률 92(간주) 아니면 85(C8)라 이 판이 세운 갈래에 **한 벌도 안 걸린다** | S155 4-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

## 4. 화면·산출물 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `screen_audit` 조건별 문구 수 | **957** (을) · **807** (갑Ⅰ) · **961** (교육갑) · **808** (교육갑저압) | S155 4-3 — **S154 값과 같다** |
| 규칙 위반 | **없음** | 같은 판 |
| 소요 | **35.1초** (1번 PC) | S155 4-3 |
| 화면 예산 | 역률 자리 **본문 1 · 확인사항 1** (한도 3/3) · **모든 자리가 한도 안** | S155 1-3 |
| 안 늘어난 까닭 | 감사 조건 넷이 다 역률 92(간주)라 새 사유가 안 뜬다. 이 판은 `warn` 하나를 `basis` 하나로 **갈아 끼웠지 더하지 않았다** | S155 1-3 |
| 「고치기 전」 값 | **착수 판을 따로 안 돌렸다** — S154 6절 값을 「전」 으로 썼다(착수 작업 트리가 그 커밋과 한 글자도 다르지 않았다) | S155 4-3 |

## 5. 역률 상한 갈래 — S155 가 세운 것

| 무엇 | 값 |
|---|---|
| 판정 자리 | **하나다** — `src\kwise\measures\power_factor.py::has_no_headroom(current_pct, target_pct)`. 카드 속성은 `PowerFactorResult.no_headroom` |
| 판정 식 | `current_pct >= cap and target_pct >= cap` · `cap` 은 `rules_kr.json` 의 `power_factor.lagging_rebate_cap_pct` 에서 읽는다 — **코드에 97 이 없다** |
| **두 역률을 다 보는 까닭** | 현재만 보면 「현재 97 · 목표 92」 가 걸리는데 그 자리는 **정말로 악화**다(감액 1.0% 를 잃는다). `tests\test_ui_screen.py::test_역률_목표를_현재보다_낮춰도_화면이_살아_있다` 가 빨개져 잡았다 |
| 죽던 뿌리 | `measures\solar.py:286` — `min(100.0, 100.0 * after / hypot(after, reactive))`. 참값은 `hypot >= after` 라 100 을 못 넘는다 |
| 사유 문구 | 「현재 지상역률 100.0% 는 감액 상한 97% 이상이라 개선할 것이 없습니다. 절감액은 0원입니다.」 (`fact="power_factor.no_headroom"` · `basis`) |
| 낱말 한 자리 | `NO_HEADROOM_LABEL` = 「개선 여지 없음」 — Excel(`excel.py:398`) · 화면(`views\measures.py:699`) · 낱장 요약(`standalone.py:194`)이 함께 읽는다 |
| PPT·Word | **안 고쳤다** — `report\document.py:353` 이 이미 「지상역률 100% 에서 목표 97% 로 올릴 여지가 없습니다」 를 낸다 |
| 곁가지로 고친 값 | 계산 근거표 「조정률」 이 `(목표 − 현재) × 0.2` 라 약관 나목의 접기를 뺐다. `small-ind-a1` 에서 **−0.6%p → +0.0%p** · 상한 안쪽 벌은 불변(85% +2.4%p · 92% +1.0%p) |
| 자리 재기 | **19 — 두 방법이 같은 수를 냈다.** 산출물 6(`document.py:352`·`excel.py:398`·`frames.py:952`·`standalone.py:194`·`worksheet.py:371`·`:375`) · 화면 4(`views\measures.py:673`·`:699`·`:713`·`views\compare.py:253`) · 카드 5 · 역률요금 4 |
| 배경을 적은 곳 | `docs\MANUAL.md` 4.4 (매뉴얼 2,100 → **2,108줄** · `build_docs.py` 로 html 을 맞췄다) |

**1-1 스윕 — 후 역률 (벌 `small-ind-a1` 자료 · 기상 캐시 · 부하 11,712점)**

| 입력 역률 \ 용량 | 0 | 24 | 52 | 60 | 80 |
|---|---|---|---|---|---|
| 92 | 92.0 | 82.941928 | 66.033776 | 62.680599 | 56.341392 |
| 95 | 95.0 | 88.734065 | 75.166012 | 72.170449 | 66.225736 |
| 97 | 97.0 | 92.967619 | 83.112819 | 80.717356 | 75.717452 |
| 98 | 98.0 | 95.213586 | 87.913695 | 86.031234 | 81.962861 |
| 99 | 98.99999999999999 | 97.555031 | 93.465971 | 92.336042 | 89.781730 |
| 100 | 100.0 | **100.00000000000001** | **100.00000000000001** | **100.00000000000001** | 100.0 |

**넘긴 칸은 셋뿐이고 다 역률 100 줄이다** (S154 가 잰 셋과 같다).

## 6. 못 셋 — S155 1-4

| 못 | 무엇을 무나 | 되돌려 확인 |
|---|---|---|
| `test_감액_상한_이상이면_카드가_절감액_0_과_사유를_낸다` (97·98·99·100) | 절감액 0 · `power_factor.no_headroom` 이 선다 · **카드가 안 사라진다** · 「악화」 가 안 난다 | 판정의 목표 쪽 부등호를 `>=` → `>` 로 옮겼더니 **넷이 다 빨개졌다** |
| `test_역률_100_에서_어느_용량도_조정률_문을_안_두드린다` | 0.1 kWp 눈금 2,001점에서 후 역률 ≤ 100 이고 `lagging_adjustment_ratio` 가 안 죽는다 | `min(100.0, …)` 를 걷었더니 **「용량 2.2 kWp 에서 100.00000000000001」** 로 빨개졌다 |
| `test_역률_100_에서도_후_역률이_100_을_안_넘는다` (S153 의 xfail) | 같은 사실 한 점 | **먼저 `[XPASS(strict)]` 로 빨개지는 것을 보고** 걷었다 · 걷은 뒤 초록 |
| `test_목표가_상한_아래면_사유가_안_서고_악화_경고가_그대로다` (95·97) | 반대쪽 — 갈래가 안 넓어졌다 | (새 못 둘의 짝이라 따로 안 되돌렸다) |

**둘째 못은 눈금을 고쳐서야 물었다** — 1 kWp 정수 눈금 200점은 **한 점도 안
넘겨** 소스를 되돌려도 초록이었다. 0.1 kWp 2,001점으로 훑으면 **92점**이 문다.

## 7. 덱 벌 `small-ind-a1` — S155 2절

| 무엇 | 갈기 전 (경기도/용인시) | 갈고 나서 (경상북도/구미시) |
|---|---|---|
| 뽑히나 | 뽑힌다 (**S153 이후 처음**) | 뽑힌다 |
| 장 수 · 크기 | **19장** · 644,696바이트 | **19장** · **644,436바이트** |
| 그림 실패 · 소요 | 0개 · 14.0초 | 0개 · **14.3초** (1번 PC) |
| 연간 발전량 (80 kWp) | 39,530.23697257414 kWh | **39,564.781107719566 kWh** (+0.09%) |
| 자가소비 | 23,210.47419904523 kWh | **23,545.959711163247 kWh** |
| 자가소비율 | 0.5871574768233373 | **0.5951242254331377** (+0.80%p) |
| 잉여 | 16,319.762773528904 kWh | **16,018.821396556319 kWh** |
| 태양광 카드 절감액 | 2,583,382.0017641485원 | **2,620,572.7011194313원** (+1.44%) |
| 기상 | 캐시 | **network — 한 번 취득**(`openmeteo_36.2500_128.2500_20260508_20260909_Asia-Seoul.parquet`) |

**좌표** — 구미 36.21009 / 128.35442 · 용인 37.23825 / 127.17795.
**덱이 그 값을 그대로 보인다** — 장13 이 「연 39,565 kWh · 2,620,000원」 이다.

## 8. 다른 벌 불변 — S155 1-5 (실물에서 다시 쟀다)

| 벌 | 역률 | 카드 절감액 | 조정률 | `no_headroom` |
|---|---|---|---|---|
| `large-b-pf85` | 85 → 97 | **10,867,309.349999905원** | +2.4%p | 거짓 |
| `large-b` | 92(간주) → 97 | **4,528,045.5625원** | +1.0%p | 거짓 |
| `small-ind-a1` | 100 → 97 | **0.0원** | **+0.0%p** (앞 −0.6%p) | **참** |

앞 두 벌이 S133 이 적은 10,867,309.35 · 4,528,045.56 과 **같은 수다.**

## 9. 「12개월 미만」 경계 — S155 3절 (재기만 했다)

| 자료 (쓰는 벌) | `period_days` | 365 − 실제 | 판정 | `:.0f` |
|---|---|---|---|---|
| 용인 (`small-a2`·`small-a2-was`·회귀 R1·R2·R3) | **364.9895833333333** | **0.010416666666685614일 = 정확히 15분** | 참 | 365일 |
| 산업용 갑 저압 (`small-ind-a1`) | 121.98958333333333 | 243.01041666666669일 | 참 | 122일 |
| 대형 실측 (`large-*`) | 367.9895833333333 | **−2.9895833333333144일** | 거짓 | 368일 |

**경계에 걸린 것이다** — 모자란 폭이 **정확히 한 슬롯**이고, `io\usage.py:639`
가 기간을 **첫 라벨과 끝 라벨 사이**로 재는데 라벨이 구간 끝이라 첫 슬롯의 앞
15분을 잃는다. 대형 실측도 `367.9895833…` 로 **같은 몫**을 단다.
문턱 넷 — `io\usage.py:705` · `quality\checks.py:252` · `tariff\engine.py:308`·`:886`.
**지금 몫은 0** (회귀값 여덟이 다 기간값이라 안 닿는다).

## 10. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **88건** (① 7 · ② 80 · ③ 1 · 줄 85) | S155 4-6 · `daily_brief` |
| 앞 판 | 88건 (같은 수) | S154 |
| 대조 | **닫은 것 3 · 연 것 3 · 수는 그대로** | S155 4-6 |
| 닫은 셋 | 역률 100 에서 태양광 카드가 죽는다(가) · 역률이 감액 상한 이상일 때 Excel·화면이 「개선」 이라 적는다(나) · 받은 좌표를 벌에 넣고 덱으로 확인한다(가) | S155 |
| 연 셋 | `render_deck` 이 png 장 수를 폴더 파일 수로 센다(라) · 전력삼각형이 상한 위에서도 「개선 후」 라 적는다(나) · 꼭 365일치 자료가 「12개월 미만」 으로 판정된다(가) | S155 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **4** · 나 **17** · 다 **6** · 라 **33** · 마 **28** · 합 **88** — **절 제목과 「지금 수」 표가 같은 수인 것을 값으로 봤다** | S155 4-6 |
| 미해결 칸 | **29,222자** | S155 |
| 「다음 작업」 칸 | **2,525자** (상한 3,000 · **남은 475자**) | S155 4-7 — S153 판치를 걷고 S155 판치를 넣은 값이다. **다음 판은 S154 판치를 걷는다** |
| 「다시 열지 마라」 칸 | **6,218자** (상한 없음) | 안 갈았다 |

## 11. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S155 4-4 |
| `ruff format --check .` | **8 어긋남 · 205 통과** (합 **213**) | S155 4-4. **어긋남 여덟은 고치지 않는다.** 이 판이 아홉째를 냈다가 도로 없앴다 — `worksheet.py` 의 새 함수가 101자 한 줄이 되려 해 지역 이름 둘로 풀었다 |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 168`) | S155 4-4. **고치지 않는다** |
| `mypy src` | **통과** (110 파일) | S155 4-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 약관 원문 건너뜀 1,272곳) | S155 4-4 |
| `output\` | **`output\casestudy_*.xlsx` 스물여덟이 추적된다 — 실수가 아니다.** `.gitignore` 가 `output/*` 뒤에 `!output/casestudy_*.xlsx` 를 둔다. `output\decks\` 는 추적 밖 | S155 4-5 |

## 12. 이 판이 못 잰 것

- **화면 감사의 「고치기 전」 판** — 착수에 따로 안 돌렸고 S154 6절 값을 썼다.
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다(S137 이 남긴 자리다).
- **도구 합과 벽시계의 격차** — 이 판도 안 갈랐다.
- 2번 PC 의 값 전부.
- **전력삼각형·`render_deck` png 세기의 몫** — 새로 열었을 뿐 재지 않았다.
