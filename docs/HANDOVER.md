# 인수인계 — 2026-09-09 마감 (S153)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판이 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다 | S152 가 이 자리에 `52f96a0` 을 적었는데 그것이 HEAD 가 아니었다 (S153 0-1 이 값으로 잡았다). **수를 박지 않는다** |
| `origin/master` | `HEAD` 와 같다 | S153 5-8 |
| 덱 벌 | **17** | `render_deck.CASES` · S153 0-3 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,701 passed · 0 failed · 0 xfailed · skip 0** · **3분 49초** | S153 0-2 · 1번 PC · 깨끗한 판 (다른 프로젝트 python 0개) |
| 마감 앞단 전체 | **1,701 passed · 1 xfailed · 0 failed · skip 0** (수집 **1,702**) · **3분 50초** | S153 5-1 · 1번 PC · 깨끗한 판 |
| 늘어난 한 건 | S153 2절이 박은 xfail 못 하나 | 수집이 1,701 → 1,702 |
| 상한 8분까지 | **4분 10초 남는다** (1번 PC) | S153 5-1 |
| 2번 PC | **이 판이 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상**이다 | S114 |
| 살아 있는 못 | **1건** — `tests\test_power_factor.py::test_역률_100_에서도_후_역률이_100_을_안_넘는다` (`xfail(strict=True)`) | S153 2절이 박았다 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S153 5-2 |
| 벌 | **11벌** (C1~C8 · R1~R3) | S153 5-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S153 5-2 |
| 소요 | 도구 **121.5초** / 벽시계 **130초** | S153 5-2 · 1번 PC |
| 회귀값 여덟 | 아래 표 · **불변** | S153 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** (`test_casestudy` 가 문다) | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

## 4. 화면·산출물 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `screen_audit` 조건별 문구 수 | **957** (을 · `general_b`·`high_a`·II·6,000 kW) · **807** (갑Ⅰ · `general_a_1`·`low`·`single`·200 kW) · **961** (교육갑 · `education_a`·`high_a`·I·300 kW) · **808** (교육갑저압 · `education_a`·`low`·`single`·200 kW) | S153 0-3 · 2-6 · 5-3 — **세 판 다 같다** |
| 규칙 위반 | **없음** | 같은 세 판 |

## 5. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **86건** (① 7 · ② 78 · ③ 1 · 줄 83) | S153 5-5 · `daily_brief` |
| 앞 판 | 80건 (① 7 · ② 72 · ③ 1 · 줄 77) | S152 |
| 대조 | **닫은 것 1 · 연 것 7** | S153 4-1 |
| 닫은 것 | 덱 벌 `small-ind-a1` 을 산업용 실측으로 갈아 끼운다 | |
| 연 것 | 역률 100 에서 태양광 카드가 죽는다 · PPT 가 한 장에 기간값과 12개월 환산값을 라벨 없이 나란히 둔다 · 월별 요금 구성 그림의 세로축이 억원 고정이다 · PPT 에 「12개월 미만」 경고가 없다 · 「기본요금은 직전 12개월 최대수요로」 가 계약전력 기준 종별에서 어긋난다 · 덱 벌 `small-ind-a1` 과 회귀 벌 `R3` 의 짝이 깨졌다 · `small-ind-a1` 의 지역 좌표가 자료에 없다 | |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **4** · 나 **15** · 다 **6** · 라 **32** · 마 **29** | S153 5-5 |
| 미해결 칸 | **28,564자** (54,199바이트) | S153 5-5 |
| 「다음 작업」 칸 | 5-6 이 갈아 끼운 뒤에 잰다 — 이 줄은 5절이 채운다 | |

## 6. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S153 5-4 |
| `ruff format --check .` | **8 어긋남 · 203 통과** (합 **211**) | S153 5-4 — **`docs\directives\S153.md` 를 앉히기 전에 잰 값**이다. 5-7 이 그 파일을 앉히면 **212** 가 된다 (`ruff format` 은 `.md` 도 센다). S152 가 이 어긋남을 겪었다 — 그 판이 적은 「합 210」 이 실은 211 이었다 (S153 0-1). **어긋남 여덟은 고치지 않는다** |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 168`) | S153 5-4. **고치지 않는다** |
| `mypy src` | **통과** (110 파일) | S153 5-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 약관 원문에서 건너뛴 것 1,272곳) | S153 5-4 |

## 7. 이 판이 만진 자리

| 파일 | 무엇이 갈렸나 |
|---|---|
| `tools\render_deck.py` | 덱 벌 `small-ind-a1` — 자료가 `IND_LOW_XLSX`(`input\전력사용량_산업용(갑)저압.xlsx`) · 계약전력 **290 → 75 kW** · `power_factor_pct` **없음 → 100.0** · 제목과 건물명에서 「용인」·「290 kW」 를 걷었다 |
| `tests\test_power_factor.py` | xfail 못 하나 |
| `src\kwise\tariff\power_factor.py` | **주석만** — 제43조 ① 1호(저압 20kW 이상) · 제42조 ② 단서 · ② 2호 다목 · ② 1호 나목(지상 97% 초과는 97로 간주). **코드는 한 줄도 안 갈렸다** |
| `PROCEED.md` · `docs\OPEN_ITEMS.md` · `docs\TEST_DATA.md` · `docs\project\project-overview.md` · `docs\HANDOVER.md` | 기록 |

**`src\kwise\tariff\` · `measures\` · `diagnose\` 의 계산은 한 줄도 안 갈렸다.**

## 8. 이 판이 못 잰 것

- **역률 100 으로 뽑은 산출물** — 죽는다. 2절의 실물 확인은 **역률을 비운
  판**(간주 92)으로 했다. 그 판의 값은 덱 20장 · 그림 실패 0 · 636 KB ·
  Excel 303 KB · Word 320 KB 다.
- **`output\decks\small-ind-a1` 의 라벨과 저장소가 어긋난다** — 라벨에는
  역률 줄이 없고 저장소 벌은 100 이다. `output\` 은 `.gitignore:3` 밖이다.
- 2번 PC 의 값 전부.
