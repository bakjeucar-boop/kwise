# 인수인계 — 2026-09-09 마감 (S157)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S157 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S157 4절 |
| 출발점 | `a02c42b` (S156 마감) · 작업 트리 **깨끗** | S157 0절 |
| 덱 벌 | **17** — 정본은 `render_deck.CASES` 길이다(`BY_KEY` 도 17). `key="` 는 **18**(`app.button(key="build_ppt")` 가 섞인다) · `key=` 는 **20**(`region_key=`·`key=lambda` 가 더 섞인다) | S158 4-2 · **세 방법으로 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `tools\capture_screen.py` · `tests\test_deployment.py` · `docs\` 넷(`CAPTURES.md` · `OPEN_ITEMS.md` · `SCREEN_WORDING_S157.md` **새 파일** · `directives\S157.md`) · `HANDOVER.md` · `PROCEED.md` | S157 |
| **`src\` · `data\`** | **한 줄도 안 고쳤다** — `git diff --stat a02c42b HEAD -- src data` **빈 출력** | S157 4-6 · 준비 판의 멈춤 규칙 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,716 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,716**) · **4분 14초** | S157 0-2 · 1번 PC · **깨끗한 판** (앞뒤로 다른 프로젝트 python 0개) |
| 마감 앞단 전체 | **1,717 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,717**) · **3분 35초** | S157 4-1 · 1번 PC · **깨끗한 판** |
| 늘어난 하나 | 새 못 하나다 — `test_deployment.py::test_캡처_도구가_세_단계에_다_닿는다` | S157 4-1 |
| 수집을 두 방법으로 | `--collect-only` 의 「1716 tests collected」 와 파일별 건수 **35개의 합 1,716** 이 같다 | S157 0-2 |
| `-q` 함정 | 파일별 건수를 뽑을 때 `-q` 를 붙이면 `addopts` 의 것과 겹쳐 `-qq` 가 되어 **총계 줄이 사라진다** — 그 판에서는 파일별 줄만 남으므로 합으로 센다 | S157 0-2 · 67세션 함정을 실물로 다시 봤다 |
| 착수 4분 14초 대 마감 3분 35초 | **39초 갈렸다. 안 갈랐다** — 착수 판이 세션 첫 실행이라 캐시가 비어 있었을 수 있고, 짐작해 적지 않는다 | S157 |
| 상한 8분까지 | **4분 25초 남는다** (1번 PC · 마감 판) | S157 4-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| **낡아 있던 칸** | 「현재 상태」 테스트 상태가 **1,709**(S156 착수값)를 적고 있었는데 실물은 1,716 이었다. **그 칸의 수를 무는 못이 없다** — 배포 시험 수(20 대 21)는 `test_doc_counts.py` 가 잡았고 이쪽은 아무도 안 잡았다 | S157 0-2 · 4절 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S157 4-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S157 4-2 |
| 소요 | 도구 **114.5초** / 벽시계 **123초** | S157 4-2 · 1번 PC · 깨끗한 판 |
| 회귀값 여덟 | 아래 표 · **불변** | S157 4-2 |
| 왜 안 움직였나 | **`src`·`data` 를 한 줄도 안 고쳤다.** 준비 판이라 안 움직여야 맞다 | S157 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.014807% | 13.512160% |
| R1 | **132.28 kW** | 132 kW | 41.082293% | 20.029885% |
| R2 | **132.28 kW** | 132 kW | 41.082293% | 18.258111% |
| R3 | **132.28 kW** | 132 kW | 41.082293% | 27.395180% |

## 4. 화면·산출물 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `screen_audit` 조건별 문구 수 | **957** (을) · **807** (갑Ⅰ) · **961** (교육갑) · **808** (교육갑저압) | S157 3-1 — **문구를 안 고쳤으므로 그대로여야 맞고 그렇다** |
| 규칙 위반 | **없음** | 같은 판 |
| 중복 후보 | **4쌍** (문턱 0.45) | 같은 판 |
| 소요 | **33초** (1번 PC) | S157 3-1 |
| 화면 예산 | **열여덟 자리 모두 한도 안** (본문 3 · 확인 3) · **9초** | S157 3-1 |
| 예산에 가장 붙은 셋 | 「최단 회수기간 목표 5,180 kW 기준」 확인 **3/3** · 「3단계 · 개선안 조합」 본문 **3/3** · 「3. 경제성DR」 확인 **2** | S157 3-1 |

## 5. 캡처 도구 — S157 1절

| 무엇 | 값 |
|---|---|
| 막힌 것 | **도구다. 화면이 아니다.** `SPOTS` 다섯이 죄다 1단계이거나 첫 화면이었고 **탭을 누르는 코드가 없었다** |
| 왜 | 세 단계가 **탭**이라(`ui\nav.py`) 한 실행에 셋이 다 그려지는데 뷰포트에는 고른 하나만 보인다 |
| 더한 넷 | ① 탭 누르기(`Spot.tab`) ② 카드 켜기(`Spot.measures` — 접힌 채로 시작한다) ③ 3단계 「합산효과 계산」 누르기(안 누르면 합산 문구가 통째로 빠진다 · 33세션 5절) ④ 역률 넣기(`Contract.lagging_pct`) |
| 자리 수 | **5 → 16** (2·3단계가 다섯 · 1단계 다섯 조건 · 12개월 경고 자리 하나) |
| 앵커 함정 | **`visible=true` 로 좁혀야 한다.** 안 고른 탭의 글도 DOM 에 있어 「합산효과」 가 2단계의 숨은 글 「… 3단계 합산효과에서 별도로 산정합니다」 를 물고 **325초에 상한(300초)으로 죽었다.** `exact=True` 만으로는 모자라다 |
| 역률 칸 함정 | 「역률 (선택)」 접힌 칸을 **펴졌는지 보고 누른다**(최대 세 번). 확정 단추가 화면을 다시 그리는 사이에 눌러 도로 접히는 일이 났다 — 같은 명령에서 `역률-98` 은 살고 `역률-97` 이 30초에 죽었다 |
| 소요 | 한 장 **24~48초** · 열두 장 합 **약 7분** (1번 PC) |
| 못 | `tests\test_deployment.py::test_캡처_도구가_세_단계에_다_닿는다` — **브라우저를 안 띄운다.** 탭·카드 이름이 화면 것과 같은지만 문다 |
| 되돌려 확인 | ① 탭을 「3단계 · 조합 비교」 로 → 「도구가 누르는 탭은 … 화면의 탭은 …」 ② 카드를 「7.4 역률 개선」 으로 → 「화면에 없는 카드 이름을 누르려 합니다」. 되돌린 뒤 사본과 `diff` 해 **한 글자도 안 다름**을 봤다 |
| 닫힌 미해결 둘 | 「`capture_screen.py` 가 2·3단계에 못 간다」 · 「화면 캡처 도구가 역률을 못 준다」 |

## 6. 캡처 — S157 2절

**폴더** — `%LOCALAPPDATA%\kwise\cache\screens\` (`PROJECT_CACHE` 기본값).
**저장소 밖이다** (`cache\` 는 `.gitignore`). 목록은 `docs\CAPTURES.md` 의
「S157 판」 절이 파일마다 조건까지 적는다.

| 조건 | 종별 · 전압 · 선택 · 계약전력 · 역률 | 자료 |
|---|---|---|
| 을 | `general_b` · 고압A · 선택Ⅱ · 6,000 kW · 간주 92 | `전기사용량_소형건물.xlsx` |
| 갑Ⅰ | `general_a_1` · 저압 · 전체시간 · 200 kW · 간주 92 | 같음 |
| 교육갑 | `education_a` · 고압A · 선택Ⅰ · 300 kW · 간주 92 | 같음 |
| 교육갑저압 | `education_a` · 저압 · 전체시간 · 200 kW · 간주 92 | 같음 |
| **small-ind-a1** | `industrial_a_1` · 저압 · 전체시간 · 75 kW · **역률 100** | `전력사용량_산업용(갑)저압.xlsx` (**2026-05-09 ~ 09-08 다섯 달**) |

**앞의 넷은 화면 감사의 `CASES` 와 같다.** 을 6,000 kW 는 자료(290 kW)와
규모가 안 맞는다 — 감사가 그렇게 하는 것과 같은 방식이고, 여기서 보는 것은
**금액이 아니라 문구 틀**이다.

## 7. 지시서의 전제가 틀린 자리 둘 — S157 2절

| 무엇 | 지시서가 적은 것 | 실물 |
|---|---|---|
| 97% 정확히 | 「상한 위는 고쳤는데 **정확히 상한인 자리가 남았다**」 | **남지 않았다.** 「개선 여지 없음」 · 삼각형 하나 · 범례 「현재」. `has_no_headroom` 이 `current_pct >= cap and target_pct >= cap` 이라 **97 은 97 이상**이다. S156 이 적은 「97% 에서 두 삼각형이 완전히 겹쳤다」 는 **고치기 전** 이야기다 |
| 12개월 미만 경고 | 「데이터 품질」 에서 찍으라 | **거기 없다.** 그 위 안내 블록이 낸다 (`views\diagnose.py::_quality_block` 첫 줄이 「경고는 위쪽 `_notice_block` 이 이미 냈다」 고 적는다). 자리를 하나 더 세웠다 (`기간경고-산업갑1저압`) |

**원문** — 「⚠ 분석 기간이 12개월 미만입니다. 연간 환산 결과에 경고를 붙여야
합니다.」 (`quality\checks.py:259`)

## 8. 문구 판 목록 — S157 3절

**목록은 `docs\SCREEN_WORDING_S157.md` 다.** 자리 **열여섯**(ㄱ~ㄲ) ·
네 갈래 + 「다시 열지 마라」 둘.

| 갈래 | 수 | 무엇 |
|---|---|---|
| 이름이 걸리는 것 | **4** | 12개월 미만 경고 말투 · 역률 회수기간 「즉시」 · 계약전력 「없음」 · 선택요금 앵커 |
| 그림이 뜻을 잘못 말하는 것 | **5** | 역률 100 축 · **축 비율** · **축 제목 세로쓰기** · **지표 말줄임표** · 억원 고정 |
| 값 표기 | **3** | 잉여 반올림 · 총액 소수 · 절감액 0 |
| 없는 안내 | **3** | 요금표 판 걸침 · 태양광 하한 · 「나」 후보 셋 |

**144세션 전수와 맞댔다.**

| 자리 이름 | 144세션 | 지금 | 갈렸나 |
|---|---|---|---|
| 예산에 걸리는 것 | **6** | **6** | 그대로 (이름까지) |
| 예산 밖 (산출물·문서) | **13** | **14** | **하나가 둘로 갈렸다** |

**갈린 하나** — 「Excel 수단별 결과에 코드 식별자가 나온다」 를 S156 3절이
걷었고, 남긴 둘이 이름으로 섰다: 「Excel 진단 시트가 계절·시간대를 열쇠로
적는다」 · 「부록 B 가 기준 데이터 원문을 그대로 실어 열쇠가 뜬다」.
**줄어든 것이 하나 · 늘어난 것이 둘이라 수만 보면 +1 이다.**

**S157 이 새로 본 셋** (2절 곁가지 · **안 고쳤다**) —
전력삼각형 **x·y 축 비율**(y 0~0.25 대 x 0~1 이라 11.5°·14.1° 가 훨씬 가파르게
보인다 · **상한 안쪽 보통 갈래**라 역률 100 항목과 다르다) ·
**altair 축 제목의 한글 세로쓰기**(「무효전력」 이 「마 전 력 역」 처럼) ·
**3단계 지표 말줄임표**(「1억 9,769만…」 · 「1억 7,487만…」).

## 9. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S157 4-4 |
| `ruff format --check .` | **8 어긋남 · 209 통과** | S157 4-4. **어긋남 여덟은 안 늘렸고 안 고쳤다** |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 169`) | S157 4-4. **안 고친다** |
| `mypy src` | **통과** (110 파일) | S157 4-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S157 0-3 · 4-4 |
| S156 이 셸에 실은 자리 | **값으로 확인하고 닫았다** — 커밋 범위 `150d928~1..a02c42b` 의 diff 를 **문자 단위로** 훑어 제어문자·치환문자(U+FFFD) **0곳**. 그 범위가 건드린 파일 21개에 `PROCEED.md`(「다음 작업」 칸 포함)가 있다 | S157 0-3 |
| `output\` | `output\casestudy_*.xlsx` 가 추적된다 — `.gitignore` 가 정한 것이다 | S155 4-5 |

## 10. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **82건** (① 7 · ② 74 · ③ 1) | S157 4-7 · `daily_brief` |
| 앞 판 | 81건 | S156 |
| 대조 | **닫은 것 2 · 연 것 3** | S157 4-7 |
| 닫은 둘 | 「`capture_screen.py` 가 2·3단계에 못 간다」 · 「화면 캡처 도구가 역률을 못 준다」 (둘 다 **라**) | S157 1절 |
| 연 셋 | 「전력삼각형의 x·y 축 비율이 안 맞아 각이 거짓말을 한다」 · 「altair 축 제목의 한글이 세로로 한 글자씩 쌓여 안 읽힌다」 · 「3단계 합산효과 지표 둘이 말줄임표로 잘린다」 (셋 다 **나**) | S157 2절 |
| 연 셋이 계산에 닿나 | **안 닿는다.** 셋 다 **그리는 자리**이고 값이 아니다 | S157 4-7 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **4** · 나 **14** · 다 **6** · 라 **29** · 마 **29** · 합 **82** — **절 제목과 표가 같은 수인 것을 값으로 봤다** | S157 4-7 |
| 미해결 칸 | **26,798자** (앞 26,843) | S157 |
| 「다음 작업」 칸 | **2,829자** (상한 3,000 · **남은 171자**) | S157 4-8 — **S155 판치를 걷었다. 남긴 판치는 S157·S156. 다음 판은 S156 판치를 걷는다** |
| 판치를 걷기 전 확인 | S155 판치의 값 **열넷**이 155세션 절에 다 있는 것을 봤다 — `has_no_headroom` · `solar.py:286` · 조정률 · 경상북도 · 39,564.78 · 59.51%(절에는 `0.5951242254331377` 로 있다) · 2,620,572.70 · 19장 · XPASS · 2,001점 · png 장 수 · `frames.py:952` · `usage.py:639` · 14.3초 | S157 4-8 |
| 「다시 열지 마라」 칸 | **둘을 얹었다** (닫은 미해결 둘) | S157 4-7 |

## 11. 이 판이 못 잰 것

- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **도구 합과 벽시계의 격차** — 이 판도 안 갈랐다.
- **착수 4분 14초와 마감 3분 35초의 39초 차** — 안 갈랐다. 짐작해 적지 않는다.
- 2번 PC 의 값 전부.
- **역률 「즉시」·계약전력 「없음」·태양광 하한의 png** — 캡처를 안 찍었다.
  문구는 `screen_audit --case 을 --list` 로 값을 떴다(833행 · 643행).
- **`figures.py:488`·`613`·`646` 의 억원 축** — 이 판도 안 쟀다.
