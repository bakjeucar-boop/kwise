# 인수인계 — 2026-09-09 마감 (S158)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S158 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S158 5절 |
| 출발점 | `9cc1091` (S157 마감) · 작업 트리 **깨끗** · `git pull` up to date | S158 0-1 |
| 덱 벌 | **17** — 정본은 `render_deck.CASES` 길이다(`BY_KEY` 도 17). `key="` 는 **18** · `key=` 는 **20** | S158 4-2 · **세 방법으로 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `PROCEED.md` · `docs\` 넷(`OPEN_ITEMS.md` · `TEST_DATA.md` · `HANDOVER.md` · `directives\S158.md` **새 파일**) · `output\casestudy_20260909.xlsx`(케이스 한 판이 갈아 끼웠다) | S158 |
| **`src`·`tests`·`tools`·`data`** | **한 줄도 안 고쳤다** — `git diff --stat -- src tests tools data` **빈 출력** | S158 0절 멈춤 규칙 · 5-5 에 확인 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,717 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,717**) · **3분 51초** | S158 0-1 · 1번 PC |
| 마감 앞단 전체 | **1,717 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,717**) · **3분 50초** | S158 5-1 · 1번 PC |
| 판이 깨끗했나 | **아니다.** 다른 프로젝트 python 은 0개인데 **이 프로젝트의 streamlit 둘**이 착수부터 마감까지 돌았다 — **두 소요를 견주는 데 쓰지 않는다** | S158 0-1 |
| 상한 8분까지 | **4분 10초 남는다** (1번 PC · 마감 판 · **오염된 판이다**) | S158 5-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| 왜 안 움직였나 | **`tests\` 를 한 줄도 안 고쳤다.** 기록 판이라 안 움직여야 맞다 | S158 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S158 5-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S158 5-2 |
| 소요 | 도구 **135.3초** / 벽시계 **144초** | S158 5-2 · 1번 PC · **streamlit 둘이 함께 돌았다** |
| 회귀값 여덟 | 아래 표 · **불변** | S158 5-2 |
| 관측 최대는 어디서 읽나 | 화면 표에 안 나온다 — `output\casestudy_20260909.xlsx` 「케이스」 시트의 **「관측 최대수요(kW)」 열** | S158 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

## 4. 화면 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 조건별 문구 수 | **957** (을) · **807** (갑Ⅰ) · **961** (교육갑) · **808** (교육갑저압) | S158 5-3 — **문구를 안 고쳤으므로 그대로여야 맞고 그렇다** |
| 규칙 위반 | **없음** | 같은 판 |

## 5. 이 판이 등재한 열여덟 — S158 1~3절

**사용자가 용인 자료로 화면·Excel·PPT 를 실제로 뽑았고 웹 대화창이 셋을 맞대
어긋난 자리 열여덟을 찾았다.** 이 판은 그것을 이름으로 앉히기만 했다.

| 무엇 | 값 |
|---|---|
| 새로 세운 것 | **열다섯** |
| 겹쳐서 자국만 보탠 것 | **셋** — 상계거래 각주 · 12개월 미만 · Excel 진단 열쇠 |
| 갈래 | **가 여섯 · 나 열하나**(4절이 낸 둘을 더해 이 판의 순증은 19) |
| 자료의 등급 | **이차 자료다** — 저장소가 아니라 산출물 실물을 본 것이고, **항목마다 「고치기 전에 저장소에서 값을 다시 잰다」 를 본문에 적었다** |

**갈래를 가른 잣대** — 이미 「가」 에 서 있는 「화면 잉여 표시 반올림이
깨진다」(0.1 + 0.8 = 0.8 MWh)를 기준으로 삼았다. **한 산출물에 나란히 뜬 수끼리
안 맞으면 「가」**, 정의가 갈려 두 수가 되는 것이 값으로 확인되면(「단순 합」 은
DR 포함/제외 · 132.3 은 소수/접힘) **이름이 문제라 「나」**.

| 「가」 여섯 | 값 |
|---|---|
| 태양광 산식이 그 표 안에서 안 맞는다 | 529,000 + 4,571,000 = **5,100,000** 인데 표는 **5,170,000** · 용량 곡선은 **5,094,000** |
| 화면 월별 명세에 2026-08 행이 없다 | 표 **12행**(338일 · 437.2 MWh) 대 요약 **365일 · 476.0 MWh** · Excel 은 13행이고 **같은 화면의 차트는 2026-08 을 그린다** |
| 태양광 절감액이 517만원과 509만원 | 카드 **517만원 · 15.5년** 대 표 **509만원 · 15.7년** · **열 이름이 둘 다 「절감액」** |
| 운영시간 외 부하 70.1% 대 85.8% | 화면·PPT 는 「비중」, Excel 은 「비율」 — **한 글자 차이로 다른 수** |
| 겨울 계시 구성이 99% | 49 + 31 + 19 |
| Excel DR 기준선 「35 × 1.2 = 42」 | 화면·PPT·부록 A 는 **34.6 × 1.2 = 41.6** — **반올림한 값으로 식을 다시 곱했다** |

| 「나」 열하나 | 값 |
|---|---|
| 1단계가 요금적용전력을 132.3 kW 라 적는다 | PPT 장05 **132** · Excel 진단 **132.0** · 화면 3단계 **132.0** — **1단계만 다르다** |
| 「단순 합」 이 577만원과 517만원 | 앞은 DR 60만원 포함 · 뒤는 제외 · **이름이 같다** |
| 개선안별 요약 표에 ESS 행이 없다 | 위가 「검토함 — … 6. ESS」·「미검토 — 없음」 · **PPT 장08 에는 「미산출」 행이 있다** |
| 차이 0원 / 0.0% 인데 이유가 뜬다 | 「요금적용전력이 132.0 → 129.0 kW 로 내려갔고…」 |
| PPT 두 장이 반대말 | 장04 「피크를 낮출 여지가 큽니다」(부하율 41.1%) 대 장07 「줄어드는 몫이 작습니다」(기본요금 19.9%) |
| DR 시간대가 둘 | 안내문 **09–20시**(제도) 대 지표 **09~12 · 13~17시**(건물) |
| DR 이 「kWh만 참고」 라면서 정산금을 낸다 | 단가 120 원/kWh 가 들어와 **60만원**이 나온 판이다 |
| 역률 100 인데 개선 안내가 뜬다 | 화면 「97%까지」 + 「도입 후 지상역률 (%)」 칸 · PPT 장12 「100% 에서 97% 로 **올릴** 여지가 없습니다」 · Excel 비고 「시간대별 투입을 제어하십시오」 |
| 화면에 원시 부동소수·영어 불리언 | `118936.77419354838` · `0.0006944444444444445` · `true`/`false` — **Excel 은 같은 표를 천 원 절사** |
| Excel 조합 비교 회수기간이 날값 | `15.47137949227301` — **같은 시트의 다른 열은 다 절사돼 있다** |
| 마크다운 별표가 그대로 | Excel `**대한민국 전용 도구입니다.**` 등 · 화면 3단계 `**기본요금 기반이 달라집니다.**` |

**S155·S156 이 고친 것은 실물에서 잘 선다** — 역률 카드 「개선 여지 없음 ·
0원/년 · 회수기간 —」 · 전력삼각형 「현재」 하나 · 부록 A 조정률 **+0.0%p**.

## 6. 그 산출물의 재현 조건과 벌 — S158 0-2

**조건 전문은 `docs\TEST_DATA.md` 8절이다.**

| 무엇 | 값 |
|---|---|
| 자료 · 계약 | `input\전기사용량_소형건물.xlsx` · 일반용(갑)Ⅱ 고압A 선택Ⅱ · **290 kW** |
| 역률 | **주간 지상 100** (야간 진상 미입력) |
| 건물 | 연면적 3,000 m² · 운영 8~17시 · 경기도/용인시 |
| 태양광 | 400 m² · 밀도 보통 → **32 kWp** · 남 · 투자비 80,000,000원 |
| 잉여 · DR | **상계거래(한전)** · SMP 120 · 외부 판매 140 / DR 단가 **120 원/kWh** |
| 산출물 | 화면 3장 · Excel **13시트** · PPT **19장** |

**이 조건의 벌은 저장소에 하나도 없다.**

| 왜 | 값 |
|---|---|
| 회귀 벌 `R1` | 자료·계약까지 같은데 **역률이 간주 92** 이고, `CaseDefinition` 필드가 **열하나**라 태양광·잉여·DR 을 세울 자리가 없다 |
| 덱 벌 `Case` | `area_m2`·`surplus_use`·`power_factor_pct` 는 있으나 **DR 단가·SMP·외부 판매 단가·투자비 필드가 없다** |
| 셋이 한 벌에 안 모인다 | `power_factor_pct=100.0` 은 `small-ind-a1` 하나 · `surplus_use` 는 `small-b-sell` 하나이고 그것도 **「외부 판매」 라 상계거래 벌이 없다** |
| 이 판이 한 것 | **벌을 안 지었다 — 이름만 올렸다** |

## 7. 4절이 잰 것 셋

| 무엇 | 값 |
|---|---|
| **용도가 닿는 자리** | **하나** — `ui\views\diagnose.py:346` 의 `narrow_contract_types`. `report\` 아래에 `use_key` 가 **0자리**라 Excel·PPT 에 안 실린다 |
| **용도 선택지 원문** | 「선택 안 함」 + 일곱 — 사무실(`office`)·상업시설(`commerce`)·병원(`hospital`) → `general` / **공장**(`factory`)·**산업시설**(`industry`) → `industrial` / **학교**(`school`)·**교육시설**(`education`) → `education`. **뒤 넷은 둘씩 `contract_prefixes` 가 같아 고르는 뜻이 없다** |
| **덱 벌 셈** | `CASES` **17** · `key="` **18**(`app.button(key="build_ppt")`) · `key=` **20**(`region_key=`·`key=lambda` 가 더 섞인다) |
| 어느 문서가 틀렸나 | **S157 보고가 맞다.** S155·S156 **지시서**의 「`key=` 로 세면 18」 이 틀렸는데 **지시서는 기록이라 안 고쳤고** 이 문서 1절에 세 값을 다 적었다 |
| **이 판의 산출물** | **저장소에 없다** — `git ls-files output` 이 **28파일**이고 다 `casestudy_*.xlsx` 다. `kwise_20260909_1421.xlsx` · `kwise_report_20260909_1422.pptx` 는 추적에도 작업 트리에도 없다 |

## 8. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check` | **All checks passed** | S158 5-4 |
| `ruff format --check .` | **8 어긋남 · 211 통과** | S158 5-4. **안 늘렸고 안 고쳤다** |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 169`) | S158 5-4. **안 고친다** |
| `mypy src` | **통과** (110 파일) | S158 5-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S158 5-4 |
| `output\` | `output\casestudy_*.xlsx` **28파일**이 추적된다 | S158 4-3 |

## 9. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **101건** (① 7 · ② 93 · ③ 1) | S158 5-6 · `daily_brief.total_items` |
| 앞 판 | 82건 | S157 |
| 대조 | **닫은 것 0 · 연 것 19 · 겹쳐서 안 세운 것 3** | S158 5-6 |
| 왜 하나도 안 닫혔나 | **기록 판이라 코드를 한 줄도 안 고쳤다** — 닫힐 자리가 없다. S152 와 같은 모양 | S158 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **10** · 나 **25** · 다 **7** · 라 **29** · 마 **30** · 합 **101** — **절 제목 합과 `total_items` 가 같은 것을 두 방법으로 봤다** | S158 5-6 |
| 갈래 표가 낡아 있었다 | S157 이 절 제목과 본문은 갈면서 **표를 안 갈아** 「지금 (S156)」 인 채였다 — **S153·S147 에 이어 세 번째다.** 이 판이 채웠다 | S158 5-6 |
| 「다음 작업」 칸 | **2,593자** (상한 3,000 · **남은 407자**) | S158 5-7 — **S156 판치를 걷었다. 남긴 판치는 S158·S157. 다음 판은 S157 판치를 걷는다** |
| 판치를 걷기 전 확인 | S156 판치의 값 **열넷**이 156세션 절에 다 있는 것을 봤다 — `0.0° → 14.1°` · `no_headroom` · `test_artifact_words.py` · 2.9초 · 「선택single」 · `option_label` · `SCHEDULE_OWNERS` · 「별표3 단서 3호」 · `CODE_WORDS` · 122일 · 121일 · 「기준전력」 · `render_deck` · 96.9 | S158 5-7 |

**S159 가 고칠 넷 (「가」 앞 넷) — 미해결 이름 그대로**

- 태양광 「절감액 = 기본 + 전력량」 산식이 그 표 안에서 성립하지 않는다
- 화면 1단계가 요금적용전력을 132.3 kW 라 적는다
- 화면 월별 명세 표에 2026-08 행이 없다
- 태양광 절감액이 한 화면에서 517만원과 509만원이다

**고치기 전에 6절의 벌부터 세운다** — 지금은 저장소에서 값을 잴 자리가 없다.

## 10. 이 판이 못 잰 것

- **등재한 열여덟의 저장소 값** — 이 판은 이름만 앉혔다. 재려면 6절의 벌이 먼저다.
- **소요를 깨끗한 판에서** — 이 프로젝트 streamlit 둘이 함께 돌았다.
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **맨 `mypy` 의 `checked 169`** — 미해결 「`checked 168` 인데 문서는 169」 의
  전제가 지금은 맞는 쪽인데 **지시서 밖이라 안 건드렸다.**
- 2번 PC 의 값 전부.
