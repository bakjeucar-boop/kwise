# 인수인계 — 2026-09-10 마감 (S159)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S159 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S159 5절 |
| 출발점 | `8eb67b6` (S158 마감) · 작업 트리 **깨끗** · `git pull` up to date | S159 0-1 |
| 덱 벌 | **18** — 정본은 `render_deck.CASES` 길이다. `key="` 는 **19** · `key=` 는 **21** | S159 5-5 · **세 방법으로 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `src\` 다섯(`report\worksheet.py` · `report\frames.py` · `report\slides.py` · `ui\views\diagnose.py` · `ui\views\measures.py`) · `tools\render_deck.py` · `tests\` 넷(`test_doc_counts.py` · `test_measures.py` · `test_ui_screen.py` · `test_integration.py` · `test_slides.py`) · `PROCEED.md` · `docs\` 셋(`OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S159.md` **새 파일**) · `output\casestudy_20260910.xlsx` | S159 |
| **`data\`** | **한 줄도 안 고쳤다** | S159 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,717 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,717**) · **4분 02초** | S159 0-1 · 1번 PC · **깨끗한 판** |
| 마감 앞단 전체 | **1,723 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,723**) · **3분 45초** | S159 5-1 · 1번 PC · **오염된 판** |
| 판이 깨끗했나 | **착수는 깨끗하고 마감은 아니다** — 마감 때 `site-energy` 의 streamlit python 둘(PID 18520 · 1972)이 돌았다(사용자 것이라 안 죽였다). **두 소요를 견주지 않는다** | S159 0-1 · 5-1 |
| 왜 여섯 늘었나 | **이 판이 세운 못 여섯이 그대로다** — `test_doc_counts.py` 둘(`test_미해결_갈래의_머리말과_줄_수가_같다` · 「mypy 검사 파일 수」) · `test_measures.py` 둘(`test_태양광_계산_근거_표는_스스로_산수로_맞는다` · `test_용량_비교_표의_절감액은_자가소비만이라고_이름에_적는다`) · `test_ui_screen.py` 둘(`test_접은_피크_지표는_요금적용전력을_적는다` · `test_월별_명세_표가_달을_하나도_빼지_않는다`). **늘어난 수와 세운 못 수가 같으므로 죽은 시험이 없다** | S159 5-1 |
| 상한 8분까지 | **4분 15초 남는다** (1번 PC · 마감 판 · **오염된 판이다**) | S159 5-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| 못을 되돌려 확인했나 | **여섯 다 했다.** 소스를 일부러 틀리게 고쳐 빨개지는 것을 보고 되돌렸다 — 어느 자리를 어떻게 틀렸는지는 `PROCEED.md` 159세션 절 4절 표에 있다 | S159 0-2 · 0-3 · 4절 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S159 5-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S159 5-2 |
| 소요 | 도구 **106.9초** / 벽시계 **114초** | S159 5-2 · 1번 PC · **`site-energy` streamlit 둘이 함께 돌았다 — 견주는 데 쓰지 않는다** |
| 회귀값 여덟 | 아래 표 · **불변** | S159 5-2 |
| 왜 안 움직여야 맞나 | 이 판이 고친 것은 `worksheet`·`frames`·`slides`·화면 둘이고 **이 도구의 여덟은 `report\casestudy.py` 가 스스로 센다** — 그 계산에 안 닿는다 | S159 5-2 |
| 「현재 상태」 케이스 칸 | **안 갈았다** — 오염된 판으로 덮지 않는다(S158 과 같은 처리) | S159 5-2 |
| 관측 최대는 어디서 읽나 | 화면 표에 안 나온다 — `output\casestudy_20260910.xlsx` 「케이스」 시트의 **「관측 최대수요(kW)」 열** | S158 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

## 4. 화면 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 고치기 **전** | **957** (을) · **807** (갑Ⅰ) · **961** (교육갑) · **808** (교육갑저압) · 36.6초 | S159 2절 — S158 마감값과 같다 |
| 고친 **뒤** | **957** · **807** · **964** · **808** · 33.3초 | S159 5-3 |
| 늘어난 이유 | **교육갑만 +3** — 태양광 계산 근거 표의 **「역률 감액 변화」 한 줄**(구분·산식·값 세 셀). 을·갑Ⅰ·교육갑저압은 역률 조정률이 0 이라 그 줄이 안 선다. **없으면 「기본 + 전력량」 이 절감액에 못 미치므로 「없으면 결과를 오독하는 것」 이다** | S159 3-1 · 5-3 |
| 첫 판은 넷 다 늘었다 | 959 · 809 · 966 · 810 — 「잉여 <시나리오>」 줄이 **0원일 때도 서서**다. **0원 줄을 걸러 셋을 되돌렸다** | S159 3-1 |
| 규칙 위반 | **없음** · 중복 후보 **4쌍** | 같은 판 |
| 화면 예산 | **열여덟 자리 모두 한도 안** | S159 5-3 |

## 5. 이 판이 고친 넷 — S159 3절

| 무엇 | 고친 것 | 고른 근거 |
|---|---|---|
| 태양광 「절감액 = 기본 + 전력량」 | **값을 안 바꾸고 표에 두 줄을 세웠다** — 「역률 감액 변화」·「잉여 <시나리오>」 · 산식 「기본 + 전력량 + 역률 + 잉여」 | 빠진 것이 **둘**이라 문장만 고치면 독자가 맞댈 값이 표에 없다 |
| 화면 1단계 132.3 kW | **값을 `billing_demand_kw` 로 맞췄다** (`diagnose.py` · `slides.py` 둘) | 그 절의 결론이 요금적용전력이고, Excel 진단·화면 3단계와 같아지며, 관측 최대는 위 지표 카드가 제 이름으로 이미 적는다 |
| 화면 월별 명세 2026-08 행 | **안 고쳤다** | 저장소에서 재현이 안 된다 (아래 6절) |
| 카드 517 · 표 509 | **값을 안 바꾸고 열 이름을 「자가소비 절감액(원)」 으로 갈랐다** | 곡선의 점은 `with_surplus_revenue` 를 타기 전이라 둘 다 옳다(41세션 결정). **화면 문구를 안 늘렸다 — 열 이름 하나다** |

**태양광 절감액의 구성 — 날값이다** (S159 2절 · 새 벌 `small-a2-pf100-offset`)

| 무엇 | 값 |
|---|---|
| 기본요금 절감 | **529,109.35** |
| 전력량요금 절감 | **4,571,091.28** |
| 역률 몫 | **−5,291.09** — `power_factor_extra_won` 은 **0.0** 이다. 기본요금이 529,109원 줄면서 **역률 감액(기본요금 × −1.0%)도 그만큼 줄어든 몫**이다 |
| 자가소비 절감액 | **5,094,909.54** ← 「태양광 용량 곡선」 32 kWp 행 |
| 잉여 상계거래 | **+75,928.66** |
| 절감액 | **5,170,838.19** ← 카드 · 수단별 결과 · 부록 A · PPT |
| 절사 뒤 | 529,000 + 4,571,000 − 5,000 + 75,000 = **5,170,000** — **정확히 맞는다** |
| 뿌리 | `measures\solar.py:679` 의 `saving = base_bill.total_won - bill.total_won` 은 **요금 전체의 차**인데 `base_saving_won`·`energy_saving_won` 은 **기본과 전력량만** 담는다. 잉여는 `with_surplus_revenue` 가 `total_saving_won` 에만 더한다 |

## 6. 재현이 안 된 하나 — 화면 월별 명세

| 무엇 | 값 |
|---|---|
| 저장소 실물 | 화면 월별 명세 표가 **13행**이고 **2026-08 행이 있다** (부분 월 · 27.0일 · 38,849 kWh · 5,377,310원) |
| 합 | 계량일수 **365일** · 사용량 **476.0 MWh** — 바로 위 요약과 맞고 Excel 「월별 집계」·「요금 계산 명세」 13행과도 같다 |
| 사용자 DOM | **12행** · 338일 · 437.2 MWh (S158 이 적은 이차 자료) |
| 슬라이스가 있나 | **없다** — `ui\views\diagnose.py:975` 의 `_monthly_table` 이 `structure.monthly` 를 통째로 넘긴다(열만 고르고 행은 안 자른다) |
| 남은 자리 | 사용자가 센 것은 `<table>` 이므로 **`st.dataframe` 이 DOM 에 실제로 그리는 행**이다. **S159 는 그것을 안 쟀다** |
| 다음 판이 할 것 | 브라우저로 재야 갈린다. `tools\capture_screen.py` 에 「월별 명세」 자리가 **없고** 그 칸은 `expanded=False` 라 **펴야 한다** |

## 7. 새로 세운 덱 벌 — S159 1절

| 무엇 | 값 |
|---|---|
| 이름 | **`small-a2-pf100-offset`** |
| 조건 | 용인 실측(`input\전기사용량_소형건물.xlsx`) · 일반용(갑)Ⅱ 고압A 선택Ⅱ · **290 kW** · **역률 주간 지상 100** · 면적 **400 m²** → 밀도 보통 **32.0 kWp** · 잉여 **상계거래(한전)** · 경기도/용인시 |
| `small-a2` 에서 무엇을 갈았나 | **셋만** — 역률 · 면적 · 잉여. **기존 벌은 한 글자도 안 움직였다** |
| 안 만든 필드 | **DR 정산 단가 · SMP · 외부 판매 단가 · 투자비** — 이 판이 보는 넷 가운데 DR 을 쓰는 것이 없고 회수기간은 기존 필드로 「미산출 — 투자비 미입력」 이 그대로 뜬다 |
| `CaseDefinition` | **안 건드렸다** — API 가 바뀐다 |
| 덱이 사나 | pptx **748 KB** · png **18장** · **그림 실패 0개** · **35.7초** · **기상 취득 0회**(용인 격자 캐시가 이미 있었다). 슬라이드1 을 열어 표지 셋을 눈으로 봤다 |

## 8. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check src tests tools` | **All checks passed** | S159 5-4 |
| `ruff format --check .` | **8 어긋남 · 211 통과** | S159 5-4. **안 늘렸고 안 고쳤다** — `frames.py` 가 한 번 늘었는데 그 자리에서 되돌렸다 |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 169`) | S159 5-4. **안 고친다** |
| `mypy src` | **통과** (110 파일) | S159 5-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S159 5-4 |
| **mypy 뿌리 수** | **169** (src 110 · tests 39 · tools 20) — 도구가 찍는 `checked 169` 와 **같다**. **못이 문다**: `test_doc_counts.py` 의 「mypy 검사 파일 수」 가 `pyproject.toml` 에서 뿌리를 읽어 센다 | S159 0-2 |
| `streamlit_app.py` 를 따라가나 | **아니다** — `mypy --verbose` 로 보니 따라가는 `streamlit_app` 은 `.venv` 의 `streamlit.hello.streamlit_app` 하나다. **S145 가 적은 「그 하나」 는 지금 없다** | S159 0-2 |

## 9. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **98건** (① 7 · ② 90 · ③ 1) | S159 5-6 · `daily_brief.total_items` |
| 앞 판 | 101건 | S158 |
| 대조 | **닫은 것 4 · 연 것 1 · 이름이 바뀐 것 2** | S159 5-6 |
| 닫은 넷 | 태양광 「절감액 = 기본 + 전력량」 산식이 그 표 안에서 성립하지 않는다(가) · 태양광 절감액이 한 화면에서 517만원과 509만원이다(가) · 화면 1단계가 요금적용전력을 132.3 kW 라 적는다(나) · 맨 `mypy` 가 `checked 168` 인데 「현재 상태」 는 169 라 적는다(라) | S159 |
| 연 하나 | Excel 「태양광 용량 곡선」 시트도 그 안에서 안 맞는다 — 역률 몫이 빠진다(가). **첫 항목을 고치다 값으로 만났고 지시서가 그 시트를 손대지 말라 해 안 고쳤다** | S159 2절 |
| 이름이 바뀐 둘 | 「「운영시간 외 부하」 가 화면·PPT 70.1% · Excel 85.8% 다」 → 가운뎃점을 붙였다(0-3) · 「화면 월별 명세 표에 2026-08 행이 없다」 → 「… 사용자 DOM 에서 12행인데 저장소에서는 13행이다」 | S159 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **9** · 나 **24** · 다 **7** · 라 **28** · 마 **30** · 합 **98** — **절 제목·본문·갈래 표 셋을 다 갈았다** | S159 5-6 |
| **셋째 방법** | 머리말 합 **98** · 브리핑이 편 **줄 98**(뭉친 줄 넷을 제 수로 센다) · 갈래 표 합 **98** — 셋이 같다. **S158 은 두 방법이 다 머리말에서 나와 어긋남을 못 봤다** | S159 0-3 · 5-6 |
| 0-3 이 무엇이었나 | ② 머리말 93 · 줄 94. 쪼갠 글자는 **쉼표가 아니라 띄운 가운뎃점 ` · `** 이고 그것이 `daily_brief.split_top` 의 **항목 구분자**다. 붙여 쓰면 안 갈린다. **본문 없는 항목 여덟을 전수로 훑어 조각은 하나뿐임을 봤다** | S159 0-3 |
| 「다음 작업」 칸 | **2,964자** (상한 3,000 · **남은 36자**) | S159 5-7 — **S157 판치를 걷었다. 남긴 판치는 S159·S158. 다음 판은 S158 판치를 걷는다** |
| 판치를 걷기 전 확인 | S157 판치의 값 여섯이 157세션 절과 「다시 열지 마라」 칸에 있는 것을 봤다 — `visible=true` · 캡처 열두 장 · `has_no_headroom` · `SCREEN_WORDING_S157.md` · 예산 한도 초과 0 · **자리 16** | S159 5-7 |
| **`SPOTS` 실물** | **16** (`capture_screen.py` 의 `Spot(` 을 셌다). 걷은 판치와 「다시 열지 마라」 칸이 열여섯이라 적어 **그쪽이 맞다.** **157세션 절만 「열다섯」 이라 적는다** — 기록이라 안 고쳤고, 고치려면 기록을 고쳐야 해 닫을 수 없는 항목이 되므로 **미해결에도 안 올렸다** | S159 5-7 |

**S160 이 이어받을 「가」 나머지 둘 — 미해결 이름 그대로**

- 화면 계시별 사용량 구성에서 겨울이 49+31+19 = 99%다
- Excel 진단 DR 기준선이 「35 kW × 1.2 = 42 kW」 인데 화면·PPT·부록 A 는 34.6 × 1.2 = 41.6 이다

**셋째로 이어질 것 — 이 판이 연 하나**

- Excel 「태양광 용량 곡선」 시트도 그 안에서 안 맞는다 — 역률 몫이 빠진다

## 10. 이 판이 못 잰 것

- **`st.dataframe` 이 DOM 에 그리는 행 수** — 6절의 남은 자리다. 브라우저가 있어야 한다.
- **소요를 깨끗한 판에서** — 마감 회귀·케이스 스터디 두 판 다 `site-energy`
  streamlit 둘과 함께 돌았다. **착수 회귀만 깨끗하다.**
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **「나」 열하나와 「가」 나머지 둘** — 손대지 않았다. 지시서 밖이다.
- 2번 PC 의 값 전부.
