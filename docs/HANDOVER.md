# 인수인계 — 2026-09-09 마감 (S156)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S156 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S156 5절 |
| 출발점 | `e79c678` (S155 마감) · 작업 트리 **깨끗** | S156 0절 |
| 덱 벌 | **17** — 정본은 `render_deck.CASES` 길이다. `grep 'key="'` 는 **18**(`render_deck.py:522` 의 `app.button(key="build_ppt")` 가 섞인다) · `--help` 의 `--case` 선택지는 **17** | S156 5-5 · **세 방법으로 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `src\` 열(`report\frames.py`·`figures.py`·`slides.py`·`document.py`·`excel.py` · `ui\text.py`·`ui\views\measures.py` · `io\usage.py` · `quality\checks.py` · `tariff\engine.py`) · `tests\` 넷(`test_artifact_words.py` **새 파일** · `test_power_factor.py` · `test_report.py` · `test_tariff_engine.py`) · `tools\` 둘(`render_deck.py` · `screen_audit.py`) · `docs\` 셋(`OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S156.md`) · `PROCEED.md` | S156 |
| `data\` | **한 줄도 안 고쳤다** | S156 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,709 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,709**) · **3분 50초** | S156 0-2 · 1번 PC · **깨끗한 판** (앞뒤로 다른 프로젝트 python 0개) |
| 마감 앞단 전체 | **1,716 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,716**) · **3분 47초** | S156 5-1 · 1번 PC · **깨끗한 판** (시작 전 다른 프로젝트 python 0개) |
| 늘어난 일곱 | 새 못 일곱이다 — `test_power_factor.py` **둘**(전력삼각형 · 화면 삼각형) · `test_artifact_words.py` **다섯** | S156 5-1 |
| 소요 | 착수 3분 50초와 **사실상 같다**(−3초). 시험 일곱이 붙은 파일 전체가 **2.9초** | S156 5-1 |
| 오염된 판 (견주지 않는다) | **4분 25초** — 4절 직후, 실물 뽑기 바로 뒤라 **깨끗한지 안 봤다** | S156 4절 |
| 상한 8분까지 | **4분 13초 남는다** (1번 PC · 마감 판) | S156 5-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| S155 의 일곱 (참고) | 못 **셋**이 매개변수로 일곱이 됐다 — 감액 상한 넷(97·98·99·100) · 상한 아래 둘(95·97) · 용량 훑기 하나. **S155 보고의 「못 둘」 은 셋이 맞다** | S156 0-3 · `--collect-only` 와 `git diff` 두 방법 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S156 5-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S156 5-2 |
| 소요 | 도구 **109.5초** / 벽시계 **117초** | S156 5-2 · 1번 PC |
| 회귀값 여덟 | 아래 표 · **불변** | S156 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.014807% | 13.512160% |
| R1 | **132.28 kW** | 132 kW | 41.082293% | 20.029885% |
| R2 | **132.28 kW** | 132 kW | 41.082293% | 18.258111% |
| R3 | **132.28 kW** | 132 kW | 41.082293% | 27.395180% |

## 4. 화면·산출물 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `screen_audit` 조건별 문구 수 | **957** (을) · **807** (갑Ⅰ) · **961** (교육갑) · **808** (교육갑저압) | S156 5-3 — **착수·마감이 같다** |
| 규칙 위반 | **없음** (전·후 둘 다) | 같은 판 |
| 소요 | **33.4초** (1번 PC) | S156 5-3 |
| 「고치기 전」 판 | **따로 돌렸다** — 작업 트리를 `git stash` 로 되돌려 잰 값이다 (S155 가 못 한 자리다) | S156 1절 |
| 화면 예산 | **모든 자리가 한도 안** | S156 5-3 |

## 5. 전력삼각형 — S156 1절

| 무엇 | 값 |
|---|---|
| 그리던 것 (현재 100 / 목표 97) | 「개선 전 — 역률 100.0% · **0.0°**」 + 「개선 후 — 역률 97.0% · **14.1°**」 — **각이 넓어지는 것을 개선이라 불렀다** |
| 현재 97 / 목표 97 | 두 삼각형이 **완전히 겹친다** (둘 다 14.069868°) |
| 고친 뒤 | 상한 이상이면 **행 하나** — 구분 「현재」 · 각도는 현재 역률의 것 |
| 판정 | `PowerFactorResult.no_headroom` → `has_no_headroom` **한 자리**. 그림 쪽에 상한 값을 **한 번도 안 적었다** |
| 그림이 서는 산출물 | **셋** — png(`figures.power_triangle_png`)이 `document.py:916` 한 자리에서 `MeasureEntry.figure` 로 들어가 **PPT·Word** 가 함께 쓰고, 화면은 altair(`charts.power_triangle_chart`). **Excel 은 0 — 그림을 하나도 안 싣는다** |
| 글자를 짓는 자리 | 프레임 하나(`frames.py`) + 라벨 둘(`figures.py` f-string · `charts.py` 「설명」 열) |
| 곁들여 고친 글자 | 화면 캡션 「전력삼각형 — 개선 전후」 → 상한 위에서 「전력삼각형」 · 툴팁 「**개선 전후 두 삼각형의** 각도 차」 → 「**삼각형이 둘이면 그** 각도 차」 (28자 → 28자) |
| 상한 안쪽 불변 | 96.9 → 14.303649°/14.069868° · 92.0 → 23.073918°/14.069868° · 85.0 → 31.788331°/14.069868° — **고치기 전과 같다** |
| 못 | 95.0~100.0 을 **0.025 눈금 201점**으로 훑는다(96.9·97.0·97.1 이 다 점 위에 선다) + altair spec 에 「개선 후」 가 없다 |
| 되돌려 확인 | `if result.no_headroom` → `if False` 로 두니 **둘 다 빨개졌다** (프레임 못은 `labels == ["현재"]` · 화면 못은 spec 의 `'개선 후 — 역률 97.0% · 14.1°'`) |

## 6. 산출물 실물 — S156 2절

| 벌 | 덱 장 수 · png | pptx / xlsx / docx | 그림 실패 | 소요 (1번 PC) |
|---|---|---|---|---|
| `small-ind-a1` | 19 · 19 | 617 / 303 / 302 KB | 0 | **15.2초** |
| `large-b` | 21 · 21 | 766 / 868 / 412 KB | 0 | **26.7초** |
| `small-a2` | 19 · 19 | 771 / 844 / 358 KB | 0 | **56.8초** |
| `small-ind-a2` | 19 · 19 | 766 / 844 / 353 KB | 0 | **25.5초** |

**셋을 한 판에 뽑았다** — `render_deck.build_deck` 은 PPT 만 낸다. 화면을 한 번
띄운 자리에서 단추 둘을 누르고 Word 는 `compare.slides_bytes` 에서 잡은
`DocumentSections` 로 만들었다(**스크래치 한 벌 · 도구는 안 고쳤다**).
**기상 취득 0회.**

**png 장 수 세기** — 세는 코드가 아니라 **폴더가 거짓이었다.** PowerPoint 가
남는 파일을 안 지운다. `export_png` 가 뽑기 전에 `*.png` 를 지운다.
**되돌려 확인** — png 를 하나 심고 `if False and …` 로 두니 **「png 20장」**,
되돌리니 **「png 19장」**(폴더 실제도 19).

## 7. 코드 식별자 — S156 3절

| 무엇 | 앞 | 뒤 |
|---|---|---|
| 「선택single」 (`small-ind-a1`) | PPT **3** · Excel **1** · Word **3** | **0 · 0 · 0** |
| 「선택요금 전환 (`종별/전압/선택`)」 Excel | 벌마다 **1** | **0** |
| 규정 이름 없는 별표 | Excel **3** · Word **3** (네 벌 다) | **0 · 0** |
| Excel 코드 식별자 (한글 줄) | **5** | **4** (`spring_fall` 1 + 부록 B 3) |
| Word 코드 식별자 (한글 줄) | **3** | **3** (전부 부록 B) |
| PPT | `small-ind-a1` 만 **3**(낱말표) | **0** — 네 벌이 같아졌다 |

**두 방법으로 셌다** — ① `screen_audit.CODE_WORDS` 정규식 ② 코드에서 떠 온
**열쇠 스물둘**을 글자에서 찾는 낱말표. **「선택single」 은 ② 에만 잡힌다.**

**S151 의 「Excel 19~20 · Word 12~15 · PPT 0~3」 과 맞대면** — **PPT 는 값으로
맞았다**(`single` 벌만 3). **Excel·Word 는 안 맞는다** — 이 판은 **줄 수**를
셌고 S151 이 무엇을 셌는지는 기록에 없다. **짐작해 맞추지 않았다.**

**지시서가 적은 까닭 하나는 틀렸다** — `CODE_WORDS` 경계를 ASCII 로 해도
**`single` 은 안 걸린다**(밑줄도 `=` 도 없어 어느 갈래에도 안 맞는다).
실물에서도 `①지금` 과 `①ASCII` 가 **5 대 5** 로 같다. 그 갈래를 막는 것은
정규식이 아니라 `option_label` 이다.

**못** — `tests\test_artifact_words.py` **다섯 점 · 2.9초**. 합성 자료에
`industrial_a_1/low/single` 을 씌운다. **부록 B 표는 머리글
「구분·항목·값·근거·확인일」 로 가려 뺀다.**

| 되돌린 자리 | 빨개진 못 |
|---|---|
| `engine.py` → `f"선택{option}"` | 요금 근거 · 덱과 문서 **둘** |
| `excel.py` → `{switch.current.selection}` | 엑셀 수단 시트 **하나** |
| `document.py` 결론 → `f"선택{option}"` | 수단 카드 결론 · 덱과 문서 **둘** |
| `slides.py` 표지 → `f"선택{option}"` | 덱과 문서 **하나** |
| `CODE_WORDS` → `(?<![\w.\\/])` | 정규식 대조 **하나** |

**곁가지로 「선택I」 이 「선택Ⅰ」(로마자)로 갈렸다** — `test_report.py:280` ·
`test_tariff_engine.py:590`·`:1096` 의 **틀린 기대**를 고쳤다. **금액 불변.**

## 8. 글자 여섯 — S156 4절

| 무엇 | 값 |
|---|---|
| PPT 122일 경고 | 장03 각주에 **「※ 분석 기간이 12개월 미만입니다. …」** 가 선다 (앞 **0건**). `bill.notices` 의 `quality.short_period` 를 그대로 꺼낸다 |
| 장07 캡션 | 「직전 12개월 최대수요로」 → **「기준전력으로」** — 같은 장 각주가 이미 쓰는 이름이다 |
| 축 단위 | `small-ind-a1` 장07 이 **「요금 (만원)」 0~200** (앞 「0.0000~0.0200 억원」) · `large-b` 는 **억원 그대로** |
| 기준 라벨 | 조합 장이 **「기간 총 절감액」·「기간 절감액」·「조합별 기간 절감액과 투자비」** — `saving_won` 은 분석 기간 값이고 요약 장은 12개월 환산값이다 |
| 「365일로 12개월 미만」 | 경고 넷에서 **일수를 뺐다.** 내림으로 고쳤더니 한 장 안에서 표 「122일」 대 각주 「121일」 이 됐다 — **수를 두 번 적는 것이 병이었다.** 기간을 **적는** 자리 넷은 안 건드렸다 |
| 365일 판정 | **안 고쳤다** (계산에 닿는다). `period_days` **364.9895833333333** · 모자란 폭 **정확히 15분** · 대형 실측도 `367.9895833…` 로 같은 몫 |

## 9. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S156 5-4 |
| `ruff format --check .` | **8 어긋남 · 207 통과** | S156 5-4. **어긋남 여덟은 안 늘렸고 안 고쳤다** |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 169` — 앞 168) | S156 5-4. **안 고친다** |
| `mypy src` | **통과** (110 파일) | S156 5-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S156 5-4 |
| `output\` | `output\casestudy_*.xlsx` 가 추적된다 — `.gitignore` 가 정한 것이다. `output\decks\` 는 추적 밖 | S155 4-5 |

## 10. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **81건** (① 4 · ② 73 · ③ 1 · 줄 78) | S156 5-6 · `daily_brief` |
| 앞 판 | 88건 | S155 |
| 대조 | **닫은 것 11 · 연 것 4 · 이름을 고친 것 1** | S156 5-6 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **4** · 나 **11** · 다 **6** · 라 **31** · 마 **29** · 합 **81** — **절 제목과 표가 같은 수인 것을 값으로 봤다** | S156 5-6 |
| 미해결 칸 | **26,843자** (앞 29,222) | S156 |
| 「다음 작업」 칸 | **2,968자** (상한 3,000 · **남은 32자**) | S156 5-7 — **S154 판치를 걷었다. 다음 판은 S155 판치를 걷는다** |
| 「다시 열지 마라」 칸 | 안 갈았다 | — |

## 11. 이 판이 못 잰 것

- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **도구 합과 벽시계의 격차** — 이 판도 안 갈랐다.
- 2번 PC 의 값 전부.
- **S151 이 「Excel 19~20 · Word 12~15」 로 무엇을 셌는지** — 기록이 없어 못 맞췄다.
- **`figures.py:488`·`613`·`646` 의 억원 축** — 이름만 올렸고 안 쟀다(장16 가로축 「0.000~0.035 억원」 하나만 봤다).
