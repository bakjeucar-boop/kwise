# 인수인계 — 2026-09-10 마감 (S161)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S161 이 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S161 3-8 |
| 출발점 | `149ff49` (S160 마감) · 작업 트리 **깨끗** · `git pull` up to date | S161 0-1 |
| 덱 벌 | **18** — 정본은 `render_deck.CASES` 길이다. `key="` 는 **19** · `key=` 는 **21** | S161 3-5 · **세 방법으로 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `src\` 여섯(`report\columns.py` · `report\excel.py` · `report\worksheet.py` · `report\__init__.py` · `measures\ess.py` · `ui\views\` 넷) + **새 파일 `src\kwise\ui\tables.py`** · `tests\` 넷(`test_doc_counts.py` · `test_report.py` · `test_measures.py` · `test_ui.py`) · `PROCEED.md` · `docs\` 셋(`OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S161.md` **새 파일**) · `output\casestudy_20260910.xlsx` | S161 |
| **안 건드린 것** | `data\` · `tools\` · `reference\` — **한 줄도 안 고쳤다** | S161 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,726 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,726**) · **4분 17초** | S161 0-1 · 1번 PC · **오염된 판** |
| 마감 앞단 전체 | **1,733 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,733**) · **3분 47초** | S161 3-1 · 1번 PC · **깨끗한 판** |
| 마감을 세 판 돌았다 | 첫 판은 **1,732 passed + 1 failed**(3분 43초) — `test_doc_counts.py` 의 「mypy 검사 파일 수」 가 물었다(`ui\tables.py` 가 붙어 뿌리 169 → 170 인데 「현재 상태」 는 169). **못이 제 일을 했다** — 기록을 실측에 맞추고 다시 돌린 것이 위 값이다. **문서를 갈아 끼운 뒤 한 판 더**(1,733 · **3분 46초** · 깨끗) — 못 여럿이 그 글을 읽으므로 고친 뒤에 다시 본다 | S161 3-1 |
| 판이 깨끗했나 | **착수는 오염 · 마감은 깨끗**하다. 착수 때 `site-energy` 의 pytest 둘(PID 1492 · 3184)이 돌았고 **남의 세션이라 안 죽였다.** **두 판의 소요를 견주지 않는다** | S161 0-1 · 3-1 |
| 왜 일곱 늘었나 | **이 판이 세운 못 일곱이 그대로다** (아래 5절 표). **늘어난 수와 못 수가 같으므로 죽은 시험이 없다** | S161 3-1 |
| 상한 8분까지 | **4분 13초 남는다** (1번 PC · 마감 판 · **깨끗한 판**) | S161 3-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| 못을 되돌려 확인했나 | **일곱 다 했다.** 어느 자리를 어떻게 틀렸는지는 아래 5절 | S161 2-6 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S161 3-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S161 3-2 |
| 소요 | 도구 **126.0초** / 벽시계 **135.2초** | S161 3-2 · 1번 PC · **깨끗한 판** |
| 회귀값 여덟 | 아래 표 · **불변** | S161 3-2 |
| 왜 안 움직여야 맞나 | 이 판이 고친 것은 **표에 낼 때 접는 자리와 글자를 벗기는 자리**이고 **이 도구의 여덟은 `report\casestudy.py` 가 스스로 센다** — 그 계산에 안 닿는다 | S161 3-2 |
| 관측 최대는 어디서 읽나 | 화면 표에 안 나온다 — `output\casestudy_20260910.xlsx` 「케이스」 시트의 **「관측 최대수요(kW)」 열** | S158 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

**접는 문턱이 실제로 가르는 벌은 여기 하나뿐이다** (S160 3절) —
**C6 야간 피크형**: 관측 **10,920.64 kW** · 요금적용 **3,604 kW**.
덱 18벌에는 그런 벌이 없다.

## 4. 화면 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 고치기 **전** | **957** (을) · **807** (갑Ⅰ) · **964** (교육갑) · **808** (교육갑저압) | S161 0-5(교육갑) · S160 2-5(넷) |
| 고친 **뒤** | **957** · **807** · **964** · **808** | S161 3-3 · **하나도 안 늘렸다** |
| 왜 안 움직이는 것이 맞나 | 표 칸에서 **별표만 사라졌다** — 감사가 세는 것은 **문구 조각 수**이고 글자가 줄어든 것이다. **불리언을 「예/아니오」 로 안 바꾼 까닭 하나가 이것이기도 하다**(바꾸면 13행이 새 문구로 잡혀 수가 는다) | S161 2-5 |
| S159 의 +3 의 **원문** | 셋 다 자리가 `[표] 본문 › 2단계 · 개선 수단 › 5. 태양광 — 입력과 결과 › 계산 근거` 다 — 구분 `역률 감액 변화` · 산식 `기본요금이 줄면 역률 감액도 준다` · 값 `50,409,000원` | S161 0-5 · `--list` 로 떴다 |
| 왜 교육갑만 움직였나 | 갑Ⅰ·교육갑저압은 **계약전력 기준**이라 태양광 기본요금 절감이 **0원** · 을은 기본 23,180,000 + 전력량 250,257,000 = 절감액 273,437,000 으로 **남는 몫이 없다** · 교육갑만 **50,409,000원**이 남는다 | S160 0-2 |
| 규칙 위반 | **없음** | S161 3-3 |
| 화면 예산 | **모든 자리 한도 안** | S161 3-3 |

## 5. 이 판이 고친 것 — S161 2절

**뿌리는 하나다** — 표기 함수가 없는 것이 아니라 **그 문을 안 지나는 출구**가
넷이었다.

| 출구 | 몇 자리였나 | 무엇이 샜나 |
|---|---|---|
| 화면 `st.dataframe` | **열세 자리** (`compare` 2 · `diagnose` 3 · `measures` 5 · `rules_admin` 3) | 날값 · 마크다운 표식 |
| Excel 시트 조립 | 한 자리 — **천 원 절사만** 했다 | 회수기간 날값 · 마크다운 표식 |
| 진단 시트 비중 줄 | 두 줄 | `light`·`spring_fall` 열쇠 |
| ESS 근거 줄 | 한 줄 (`ess.py:1223`) | `daily` 열쇠 |

| 무엇 | 고친 것 |
|---|---|
| 문을 세웠다 | `report\columns.py` 에 **`round_columns`**(열 이름 꼬리가 자릿수를 정한다 · `DISPLAY_DECIMALS`)와 **`display_frame`**(접기 + `plain_text` 로 표식 벗김) |
| 자릿수 | `(원)` **0** · `(kW)`·`(kWp)`·`(kWh)`·`(MWh)` **2** · `(년)`·`(%)`·`(일)` **1** · `(h)` **2** · 「률」·「율」 **4**. **`(kW)`·`(kWh)` 를 두 자리로 둔 까닭** — 자료가 소수 두 자리라 한 자리면 15분 칸의 0.04 kWh 가 0.0 이 되어 **표기가 아니라 값을 잃는다** |
| Excel | `excel.py::_sheets` 의 반환 한 줄이 `display_frame(truncate_money_columns(...))` 를 지난다 — 시트마다 하지 않는다 |
| 화면 | **새 `src\kwise\ui\tables.py`** (`tables.show`). 열세 자리를 다 그리로 돌렸고 **`st.dataframe` 은 이제 그 모듈 하나에만 있다** |
| 이름이 `table` 이 아닌 까닭 | `measures.py`·`compare.py` 가 요금표를 `table` 이라는 지역 이름으로 들고 있어 **요금표를 가린다** — mypy 가 「TariffTable not callable」 로 네 자리를 짚었다 |
| 진단 시트 열쇠 | 이미 있던 번역표를 지나게 했다 — `value_label('band', …)` · `season_label(…)` |
| ESS 근거 | `SIZING_BASIS_LABELS` (`daily` → 「하루 최대」 · `event` → 「연속 초과 구간 최대」 · `total` → 「기간 합계」). **뜻은 `size_for_target` 의 `basis` 설명에 이미 있었고 이름만 없었다** |
| 계산 근거 표 | `Worksheet.frame()` **한 자리**에서 표식을 벗긴다 — 화면 둘과 Excel 부록 A 가 쓴다. **툴팁(`lines()`)은 안 건드렸다** — 그 자리는 마크다운을 그린다 |

**못 일곱 · 되돌려 확인한 것 일곱**

| 못 | 어디를 어떻게 틀렸더니 빨개졌나 |
|---|---|
| `test_doc_counts.py::test_갈래_절_제목의_합이_미해결_건수와_같다` | 「가 (7)」 → 「(6)」 · **「합이 97 인데 실물은 98」** |
| `test_report.py::test_엑셀_시트에_마크다운_표식이_남지_않는다` | Excel 조립에서 `display_frame` 을 뺐다 · **요약 다섯 칸을 이름으로 짚었다** |
| `test_report.py::test_엑셀_시트가_날값을_그대로_싣지_않는다` | 같은 되돌림 · **조합 비교 회수기간 `3.150123021206818` · 월별 집계 결측률·사용량** |
| `test_report.py::test_진단_시트가_계시와_계절을_사람이_읽는_이름으로_적는다` | `value_label` 을 걷었다 · **`light 사용량 비중`** |
| `test_report.py::test_계산_근거_표가_마크다운_표식을_안_싣는다` | `Worksheet.frame` 의 `plain_text` 를 걷었다 · **구분 = `**이유 1**`** |
| `test_measures.py::test_용량_산정_기준을_사람이_읽는_이름으로_적는다` | `SIZING_BASIS_LABELS` 를 걷었다 · **`용량 산정 기준: daily (하루 최대 34.7 kWh …)`** |
| `test_ui.py::test_화면_표는_한_자리를_지난다` | 월별 명세를 `st.dataframe` 으로 되돌렸다 · **`src\kwise\ui\views\diagnose.py:991`** |

**되돌린 것 — 불리언** (S161 2-3)

| 무엇 | 값 |
|---|---|
| 왜 되돌렸나 | Excel 은 **셀 자체가 불리언**이라 `TRUE`/`FALSE` 는 Excel 이 붙이는 표기이고, 화면은 `st.column_config.CheckboxColumn` 이 **체크로 그린다** |
| 무엇이 깨졌나 | 「예/아니오」 로 바꾸니 `test_report.py::test_timeseries_sheet_carries_every_slot` 가 `int('아니오아니오…')` 로 죽었다 — 자료가 글자가 되면 `COUNTIF`·필터도 죽는다 |
| 미해결은 어떻게 했나 | 항목의 **「영어 불리언」 절반은 전제 뒤집힘으로 닫았다** — S159 의 mypy 항목과 같은 모양이다. 까닭을 `columns.py` 주석에 남겼다 |

## 6. 재현한 다섯 — S161 1절 (벌 `small-a2-pf100-offset`)

| 무엇 | 재현 | 값 |
|---|---|---|
| 화면 월별 명세 날값 | **섰다** | 결측률 `0.0006944444444444445` · 역률 조정 전 기본요금(원) `118936.77419354838` · 역률 요금(원) `-1189.3677419354838` · 전력량요금(원) `3687139.3479999998` · 합계(원) `774036.1604516129` |
| 화면 월별 명세 영어 불리언 | **안 섰다** | bool dtype + `CheckboxColumn` 이라 글자가 아니다 |
| Excel 「조합 비교」 회수기간 | **이 벌에서 안 섰다** | 설치 단가가 없어 **투자비 미산출**이고 칸이 빈다(`None`). **단가 1,200,000 원/kWp 만 얹어 재니 섰다** — 고친 뒤 **7.5** |
| Excel 마크다운 별표 | **섰다** | 요약 **7칸** · 부록 C **9칸** → 고친 뒤 **0** |
| 화면 마크다운 별표 | **섰다** | 3단계 계산 근거 표 「값」 칸 **셋** → 고친 뒤 **0** |
| Excel 진단 열쇠 | **섰다** | `light`·`mid`·`peak`·`spring_fall`·`summer`·`winter` **여섯 줄** → 「경부하 사용량 비중」·「봄·가을 …」 |
| ESS 「용량 산정 기준: daily」 | **이 벌에서 안 섰다** | 필요 출력 25 kW < 상업용 최소 50 kW 라 **ESS 를 산출하지 않는 벌**이다. `sample_ess` 로 재니 섰다 |
| 곁가지로 본 둘 | — | 화면 DR 표 「감축 여력(kW)」 `30.929400469628767`(고쳤다) · Excel 「15분 시계열」 열 이름 `timestamp`·`kw`·`kwh`(**이름만 올렸다**) |

**표기를 만드는 자리가 셋뿐이라는 말은 참이 아니다** (S161 1-2) —
`count_sites` 의 표기 갈래 셋은 다 「한 자리 밖 0」 이거나 판정이 끝난 자리인데,
그물 밖에 `:,.Nf` 를 **그 자리에 직접 적는 곳이 `src\kwise` 에 43파일 500여**다.
다만 그것들은 **문장 안에 값을 넣는** 자리이고 이 판의 다섯은 **표기를 아예 안
지나는 출구**였다. `fact_sites.json` 에 그물을 넣지 않았다(범위 밖).

## 7. 셋째 방법 — S161 0-2

| 방법 | 어느 코드 | 어떻게 센다 | 착수 | 마감 |
|---|---|---|---|---|
| ① 절 제목 합 | 사람이 `OPEN_ITEMS.md` 의 `## 가 — … (7)` 다섯을 더한다 | **못이 없었다 → S161 이 세웠다** | 98 | **96** |
| ② `total_items` | `daily_brief.py::total_items` | 갈래 **머리말의 「N건」** 을 더한다 | 98 | **96** |
| ③ 줄 | `daily_brief.open_items` 가 편 항목 수 | 괄호 밖 ` · ` 로 자른 **줄** | 95 | **93** |
| ③′ 줄(뭉침 반영) | `test_미해결_갈래의_머리말과_줄_수가_같다` 의 `holds` | 「청구서 4」 를 **넷**으로 읽는다 | 98 | **96** |

**어긋남은 결함이 아니라 뭉침이다** — ①-1 「청구서 4」 한 줄이 넷을 담는다
(68세션 2절이 일부러 둔 모양). **빠진 셋은 이름이 없다** — 그 줄의 이름은
「청구서」 하나이고 나머지 셋은 등재된 적이 없다. `MAX_LINES` 절단도 · 본문 없는
항목도(일곱인데 어긋남은 셋) · 상쇄도 아니다.

## 8. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check src tests tools` | **All checks passed** | S161 3-4 |
| `ruff format --check .` | **9 어긋남 · 213 통과** | S161 3-4. 목록은 `document.py` · `excel.py` · `slides.py` · `engine.py` · `test_casestudy.py` · `test_document.py` · `test_excess.py` · `test_tariff_engine.py` · `test_ui_screen.py`. **안 늘렸고 안 고쳤다** |
| 어긋남 9 는 누가 늘렸나 | **S159 다** — S158 마감 `8eb67b6` **8** → S159 마감 `dfb8abd` **9**(`test_ui_screen.py` 가 붙었고 그 줄은 `db26075` 가 2026-08 행 확인용으로 넣은 `계량 일수` 한 줄이다) → S160 마감 **9**. **S159 의 「앞 판과 같다(8)」 가 틀렸고 S160 의 「내가 늘린 것이 아니다」 는 맞다** | S161 0-4 · `git worktree` 로 세 시점을 각각 재고 지웠다 |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 171`) | S161 3-4. **안 고친다** |
| `mypy src` | **통과** (**111 파일**) | S161 3-4 |
| **mypy 뿌리 수** | **170** (src 111 · tests 39 · tools 20) — `mypy src tests tools` 가 찍는 **170** 과 같고 맨 `mypy .` 의 **171** 보다 하나 적다. **그 하나는 저장소 뿌리의 `streamlit_app.py`** (`BuildSource(followed=False)` 로 **소스로 든다**). **못이 문다**: `test_doc_counts.py` 의 「mypy 검사 파일 수」 | S161 2절에 `ui\tables.py` 를 세워 169 → 170 · **S145 가 맞고 S159 0-2 의 「뿌리 수와 같다」 가 틀렸다** |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S161 3-4 |
| `tools\count_sites.py` | 무는 사실 열둘 · **표기용 반올림 그물은 없고 새로 넣지 않았다** | S161 1-2 |
| 덱 | `small-a2-pf100-offset` pptx **748 KB** · png **18장** · **그림 실패 0개** · **29.4초** | S161 3-5 |

## 9. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **96건** (① 7 · ② 88 · ③ 1) | S161 3-6 · `daily_brief.total_items` |
| 앞 판 | 98건 | S160 |
| 대조 | **닫은 것 4 · 연 것 2** | S161 3-6 |
| 닫은 넷 | 화면 월별 명세에 원시 부동소수와 영어 불리언이 그대로 나온다(나 · **불리언 절반은 전제 뒤집힘**) · Excel 「조합 비교」 회수기간이 `15.47137949227301` 이다(나) · Excel 요약 시트와 화면에 마크다운 별표가 그대로 뜬다(나) · Excel 진단 시트가 계절·시간대를 열쇠로 적는다(나) | S161 2절 |
| 연 둘 | 감도 시트와 요금 계산 명세의 날값이 안 접힌다(나 · 열 이름에 단위 꼬리가 없어 20칸) · Excel 「15분 시계열」 열 이름이 `timestamp`·`kw`·`kwh` 다(나) | S161 3-1 |
| 뺀 하나 | **「숫자 열의 절감액 0 은 「없음」 을 못 담는다」** — 열이 object 가 되어 정렬·서식·집계가 달라진다. 표기만으로 안 되므로 **「마」 에 그대로 두었다** | S161 2-4 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **7** · 나 **24** · 다 **7** · 라 **28** · 마 **30** · 합 **96** — **절 제목·본문·「지금 수」 절을 다 갈았다** | S161 3-6 |
| 「다음 작업」 칸 | **2,693자** (상한 3,000 · **남은 307자**) | S161 3-7 — **S159 판치를 걷었다. 남긴 판치는 S161·S160. 다음 판은 S160 판치를 걷는다** |
| 판치를 걷기 전 확인 | S159 판치의 값 **일곱**을 159세션 절에서 하나씩 찾아 다 있는 것을 봤다(두 줄 되짚기 +75,929/−5,291 · `peak_kw` → `billing_demand_kw` · 「자가소비 절감액(원)」 이름 갈림 · 2026-08 행 13행/365일/476.0 MWh 와 「DOM 은 안 쟀다」 · 용량 곡선 5,100,000 대 5,094,000 · 덱 벌 17 → 18 · `SPOTS` 16) | S161 3-7 |

**S162 가 이어받을 것 — 미해결 이름 그대로**

- 태양광 계산 근거의 「역률 감액 변화」 줄이 실은 초과사용부가금 몫이다
- 요금적용전력과 관측 최대가 같은지 보는 문턱이 1% 라 제7조 ①의 1 kW 와 다르다
- Excel 「태양광 용량 곡선」 시트도 그 안에서 안 맞는다 — 역률 몫이 빠진다
- 감도 시트와 요금 계산 명세의 날값이 안 접힌다
- Excel 「15분 시계열」 열 이름이 `timestamp`·`kw`·`kwh` 다
- 「나」 갈래 스물넷 가운데 표기 아닌 것 전부

## 10. 이 판이 못 잰 것

- **`st.dataframe` 이 DOM 에 그리는 행 수** — S159 가 남긴 자리다. 브라우저가 있어야 한다.
- **감도·요금 계산 명세의 꼴 없는 열을 어떻게 접을지** — 지표 이름이 열이 아니라
  줄에 있어 꼬리로 못 가른다. **이름만 올렸다.**
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **접는 문턱 1% · 「태양광 용량 곡선」 역률 몫 · 교육갑 부가금 문구** — 지시서 밖이다.
- **`fact_sites.json` 에 표시용 반올림 그물을 넣는 것** — 범위 밖이라 안 넣었다.
- 2번 PC 의 값 전부.
