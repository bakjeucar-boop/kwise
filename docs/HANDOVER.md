# 인수인계 — 2026-09-10 마감 (S164)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S164 가 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

**S164 는 조사 판이다** — `src\`·`data\` 를 **한 줄도 안 고쳤다**
(`git diff --stat -- src data` 빈 출력 · 4절의 되돌려 확인 셋을 다 되돌린 뒤에
값으로 봤다).

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S164 5-8 |
| 출발점 | `f8f93fb` (S163 마감) = `origin/master` · 작업 트리 **깨끗** · `git pull` up to date | S164 0-1 |
| 덱 벌 | **18** — 정본은 `render_deck.CASES` 길이다 | S163 3-5 · S164 는 벌을 **안 지었다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `tests\` 셋(`test_ui_screen.py` · `test_document.py` · `test_slides.py` — **못 셋만 더했다**) · `tools\fact_sites.json`(그물 하나) · `PROCEED.md` · `docs\` 셋(`OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S164.md` **새 파일**) · `output\casestudy_20260910.xlsx`(케이스 스터디를 돌려 다시 났다) | S164 |
| **안 건드린 것** | `src\` **전부** · `data\` **전부** · `reference\` · `tools\` 의 나머지 · `input\` — **한 줄도 안 고쳤다** | S164 5-5 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,740 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,740**) · **3분 54초** | S164 0-1 · 1번 PC · **오염된 판** |
| 마감 앞단 전체 | **1,740 passed · 0 failed · 3 xfailed · skip 0** (수집 **1,743**) · **3분 51초** | S164 5-1 · 1번 PC · **깨끗한 판** — 앞뒤로 다른 프로젝트 python **0개** |
| 상한 8분까지 | **4분 09초 남는다** (1번 PC · 깨끗한 판 · 1,743건). S163 은 1,740건 4분 03초였다 — **건수가 셋 늘고 소요는 12초 줄었다**(둘 다 깨끗한 판) | S164 5-1 |
| 문서를 갈아 끼우기 전 판에 하나 빨갰다 | 세션 목록표 164행이 「닫은 것 없음 · 연 것 없음」 이라 적어 `test_daily_brief.py::test_지금_마지막_세션_행에는_경고가_안_뜬다` 가 물었다 — `nameless_diff` 는 「늘/줄」 두 낱말을 찾는다. **「늘어난 것 없음 · 줄어든 것 없음」 으로 고쳤다.** S163 이 같은 못에 물린 자리다. 그 판도 **1,743건 3분 43초 · 깨끗** 이었다 | S164 5-1 |
| 판이 깨끗했나 | **착수는 오염됐다** — `site-energy` 의 streamlit 둘이 그 사이 CPU **118.7초**를 먹었다(178.2 → 296.9). 남의 세션이라 안 죽였고 **그 소요는 견주는 데 안 쓴다.** 마감 때는 그 프로세스가 스스로 끝나 **다른 프로젝트 python 0개**였다 | S164 0-1 · 5-1 |
| 왜 셋 늘었나 | **xfail 못 셋을 세웠다 — 걷은 시험 0 이라 순증이 그대로 셋이다** | S164 4절 |
| 살아 있는 못 | **3건** — S155 1-4 에 0 이 된 뒤 이 판이 처음 세웠다. 이름은 5절 | S164 4절 |
| 못을 되돌려 확인했나 | **셋 다 했다.** 어느 자리를 어떻게 틀렸는지는 5절 | S164 4-3 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S164 5-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S164 5-2 |
| 소요 | 도구 **113.7초** / 벽시계 **121초** | S164 5-2 · 1번 PC · **깨끗한 판**(앞뒤로 다른 프로젝트 python 0개) |
| 회귀값 여덟 | 아래 표 · **불변** | S164 5-2 |
| 왜 안 움직여야 맞나 | **이 판은 `src\`·`data\` 를 한 줄도 안 고쳤다** — 여덟이 움직일 자리가 없다 | S164 5-5 |
| 앞 판과 견줄 수 있나 | **S162(141.6초)는 오염된 판이라 안 견준다.** 깨끗한 판끼리는 S160 **135.9초** · S157 **114.5초** 다음이 이 판이다 | S164 5-2 |
| 관측 최대는 어디서 읽나 | 화면 표에 안 나온다 — `output\casestudy_20260910.xlsx` 「케이스」 시트의 **「관측 최대수요(kW)」 열** | S158 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

## 4. 화면 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 조건 넷 | **957** (을) · **807** (갑Ⅰ) · **964** (교육갑) · **808** (교육갑저압) | S164 5-3 · **앞 판 값과 같다** |
| 왜 안 움직여야 맞나 | **화면 문구를 한 자도 안 고쳤다** — 이 판이 만진 것은 `tests\` 셋과 문서뿐이다 | S164 5-3 |
| 규칙 위반 | **없음** (① · ②) | S164 5-3 |
| 소요 | **33.7초** (1번 PC · 한 판) | S164 5-3 |

## 5. 이 판이 잰 것 — S164 1·2·3절

**넷을 저장소에서 다시 재니 둘이 그대로 서고 하나는 조건이 모자라 못 서고 하나는
재현이 안 됐다.** 벌은 **`small-a2-pf100-offset`**(용인 실측 · 갑Ⅱ 고압A 선택Ⅱ ·
290 kW · 역률 100 · 태양광 32 kWp · 잉여 상계) 하나다. 화면과 PPT 를 **한 실행**에서
떴다 — `AppTest` 로 화면을 띄우고 「PPT 보고서 만들기」 를 눌러 나온 바이트를 그대로
읽는다(`render_deck.build_deck` 과 같은 길). **한 벌 25.6초.**

| 무엇 | 재현됐나 | 잰 값 |
|---|---|---|
| ㄱ 「단순 합」 두 값 | **안 섰다** | 요약표 **517만원** = 지표 **517만원**. DR 이 「미산출 — 정산 단가 미입력」 이라 같다 |
| ㄴ ESS 행이 없다 | **섰다** | 화면 요약표 **다섯 줄**(ESS 없음) · PPT 장08 **여섯 줄**(ESS 「미산출」) · 검토 범위 「검토함 — …6. ESS / 미검토 — 없음」 |
| ㄷ 차이 0원인데 이유 | **안 섰다** | 차이 **-3만원/년 · -0.5%**(날값 **-25,000원**). 이유 셋은 그대로 선다 |
| ㄹ 두 장이 반대 | **섰다** | 장04 「부하율 **41.1%**…피크를 낮출 여지가 큽니다」 · 장07 「기본요금은 **19.9%**입니다 — 피크를 낮춰도 줄어드는 몫이 작습니다」 |

**ㄱ 은 덱 벌 열여덟 어디서도 못 잰다** — `Case` 에 **DR 정산 단가 필드가 없어**
DR 이 늘 미산출이다(S159 가 그 벌 주석에 「DR 단가·SMP·외부 판매 단가·투자비 필드를
안 더했다」 고 적어 두었다). **새 벌을 짓지 않았다** — 스크래치가 화면 위젯
(「정산 단가를 안다」 체크 + 단가 칸)으로 S158 조건 **120 원/kWh** 를 세우니
**요약표 588만원 대 지표 517만원**(차 **71만원** = DR 절감액)으로 갈렸다.
S158 의 577/517 과 **같은 모양이고 수만 다르다** — 그 판은 운영 8~17시라 감축
가능량이 달랐다.

**덱 벌 열여덟을 화면만 띄워 훑었다 — 5분 24초.**

| 무엇 | 값 |
|---|---|
| ㄷ 이 서는 벌 | **0** — 차이 절댓값 최소가 이 벌의 **25,000원(-0.5%)** 이고 나머지는 34만~2,574만원 |
| ㄴ 이 서는 벌 | **12 / 18** — ESS 줄이 요약표에 서는 여섯은 `large-b` · `large-b-over` · `large-b-short` · `large-b-pf85` · `large-edu-b` · `large-ind-b` 다 |
| 검토 범위 | **열여덟 다 「검토함 — …6. ESS」** |
| ㄱ 이 서는 벌 | **0** — 요약표 합계와 지표가 열여덟 다 같다 |

**ㄱ — 「단순 합」 금액을 만드는 자리** (`tools\fact_sites.json` 에 그물
「「단순 합」 금액」 · `\bsimple_sum_won\(` · 못은 안 켰다). **자리 3 (파일 2) ·
한 자리 밖 1.**

| 자리 | 무엇 | DR 을 담나 |
|---|---|---|
| `report\standalone.py:235` | `def simple_sum_won(...)` 정의 | — |
| `report\standalone.py:288` | 화면 3단계 **요약표 합계 행** | **담는다** |
| `ui\views\compare.py:452` | 화면 3단계 **합산효과 지표** (`combinable_only=True`) | 안 담는다 |
| `report\worksheet.py:573` | 화면 **계산 근거 표** — 지표 값을 **넘겨받아 적는다** (그물 밖) | 안 담는다 |

**갈림은 인자 하나이고 그것을 주는 자리도 하나다.** 담기는 몫은
`_COMBINABLE = ("tariff_switch", "contract", "power_factor", "solar", "ess")` 이고
**경제성DR 과 잉여가 그 밖**이다. **금액 칸은 화면에만 있다** — Excel 13시트·
PPT 18장·Word 는 「단순 합이 아니다」 라는 **문장 한 칸**씩뿐이라 **어느 쪽으로
맞춰도 안 움직인다.** **회귀 기대값이 움직이는 자리 0** — `tests\` 의 「단순 합」
열아홉이 다 이름·문장·주석이고 금액을 무는 것이 하나도 없다.

**ㄴ — 목록을 만드는 자리가 셋이다. 화면과 PPT 는 같은 자리를 안 쓴다.**

| 낱말 | 만드는 자리 | 무엇을 세나 |
|---|---|---|
| 「검토함」·「미검토」 | `ui\spec.py::review_scope` (화면) · `report\document.py::DocumentSections.scope` (Word) | **켠 수단** — 결과가 있는지 안 본다 |
| 화면 요약표 줄 | `report\standalone.py::standalone_rows` | **결과 객체가 있는 것만** — `ess is None` 이면 줄이 없다 |
| PPT 장08 표 줄 | `report\document.py::measure_entries` | 결과가 없어도 **사유를 적는 갈래 둘** |
| 「미산출」 | `report\document.py:126` · `report\slides.py:153` | 같은 글자가 두 자리에 따로 있다 |

**PPT 쪽 갈래 둘** — `elif ess_optimum.below_minimum and ess_curve is not None`
(최소 규격 미달)와 `elif not ess_optimum.viable and ess_curve is not None`
(성립하는 목표 없음). 이 벌은 **앞엣것**이다 — 필요 출력 **25.3 kW** <
상업용 최소 **50 kW**. `standalone_rows` 에는 그런 갈래가 **하나도 없다**.

**ㄷ — 조건식이 차이를 안 보지만 그 문장은 참이다.**

    if "ess" in keys or "solar" in keys:
        reasons.append("**기본요금 기반이 달라집니다.** 요금적용전력이 …")

`ui\views\compare.py::_interaction_reasons` 이고 `keys` 에 **`gap` 이 안 든다.**
그러나 「132.0 → 129.0 kW」 는 이 벌에서도 **실제로 내려간다.**
**「차이 0원 / 0.0%」 는 반올림일 수 있다** — 「0원」 은 `money.truncate_won` 이
천 원 단위로 잘라 **차이 절댓값 1,000원 미만**에서 뜨고, 「0.0%」 는 **비율 절댓값
0.05% 미만**에서 뜬다.

**ㄹ — 둘 다 제 잣대로는 옳다. 계산 결함이 아니다.**

| 장 | 만드는 자리 | 보는 값 | 문턱 (`data\assumptions.json`) |
|---|---|---|---|
| 장04 | `narrative.pattern_lead` | 부하율 **41.1%** | `narrative.load_factor_flat` **0.70** |
| 장07 | `narrative.structure_lead` | 기본요금 비중 **19.9%** | `narrative.base_fee_share_low` **0.20** · `..._high` **0.35** |

**두 함수는 서로의 값을 인자로도 안 받는다.** 문턱이 아슬아슬하다 — 같은 자료의
`small-a2`(역률을 간주 92 로 두는 벌)는 비중이 **20.0%** 라 가운데 갈래로 넘어간다.
**역률 하나가 장07 의 결론을 가른다.**

**뿌리는 둘이다** (재현된 셋 기준) — **ㄱ·ㄴ 이 한 뿌리**(「한 사실을 만드는 자리가
여럿이고 저마다 다른 잣대를 쓴다」 · 결함 유형 ②. 고치는 자리만 갈린다 — ㄱ 은 한
화면 안 두 자리, ㄴ 은 화면과 보고서 두 모듈) · **ㄹ 은 따로다**(자리는 하나씩이고
둘 다 옳다 · 뿌리는 「두 잣대를 잇는 자리가 없다」). **ㄷ 은 뿌리를 못 적는다.**

**못 셋 · 되돌려 확인한 것 셋** (다 `xfail(strict=True)`)

| 못 | 무는 것 | 어디를 어떻게 고친 척했더니 빨개졌나 |
|---|---|---|
| `tests\test_ui_screen.py::test_단순_합이라는_이름이_한_값만_가리킨다` | 요약표 합계 행 글자와 지표가 쓰는 값이 같은가 | `standalone.py:288` 을 `combinable_only=True` 로 갈았다 · **`XPASS(strict)`** · 되돌렸다 |
| `tests\test_document.py::test_화면_요약표와_보고서_요약표가_같은_수단을_센다` | `standalone_rows` 와 `measure_entries` 가 같은 수단을 세나 | `document.py` 의 ESS 갈래 **둘**을 껐다 · **`XPASS(strict)`** · 되돌렸다. **하나만 꺼서는 안 물었다** — `not viable` 갈래가 대신 섰다 |
| `tests\test_slides.py::test_두_장이_피크_여지를_반대로_말하지_않는다` | 장04·장07 문장이 함께 서나 | `assumptions.json` 의 `base_fee_share_low` 를 0.20 → **0.15** 로 갈았다 · **`XPASS(strict)`** · 되돌렸다 |

**ㄷ 에는 못을 안 세웠다** — 지시서 4-2 가 「재현 안 된 항목에는 못을 세우지
않는다」 라 적었고 재현이 안 됐다.

## 6. 셋째 방법 — S162 0-4 이 정한 셈

| 방법 | 어느 코드 | 어떻게 센다 | S163 마감 | S164 마감 |
|---|---|---|---|---|
| ① 절 제목 합 | 사람이 `OPEN_ITEMS.md` 의 `## 가 — … (7)` 다섯을 더한다 | 못은 S161 이 세웠다 | 98 | **98** |
| ② `total_items` | `daily_brief.py::total_items` | 갈래 **머리말의 「N건」** 을 더한다 | 98 | **98** |
| ③ 줄 | `daily_brief.open_items` 가 편 항목 수 | 괄호 밖 ` · ` 로 자른 **줄** | 95 | **95** |

**어긋남은 「뭉침이라 옳다」 다.** ①-1 「청구서 4」 한 줄이 넷을 담으므로
① − ③ = **뭉쳐 더 담은 몫**(지금 **3**)이다. `test_doc_counts.py::
test_세_방법이_세는_수의_차가_뭉친_몫과_같다` 가 그 자리를 문다.

## 7. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S164 5-4 |
| `ruff format --check .` | **9 어긋남** — **S163 과 같은 아홉** (`document.py` · `excel.py` · `slides.py` · `engine.py` · `test_casestudy.py` · `test_document.py` · `test_excess.py` · `test_tariff_engine.py` · `test_ui_screen.py`). **안 고쳤다** | S164 5-4 |
| 이 판이 넣은 어긋남 | **0곳** — 못 하나의 인자를 손으로 접었던 자리를 한 줄로 펴서 값으로 확인했다(S163 이 밟은 자리다) | S164 5-4 |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 170`) | S164 5-4. **안 고친다** |
| `mypy src` | **통과** (**111 파일**) | S164 5-4 |
| `mypy tests tools` | **36건 · 파일 열** — 앞 판과 같다. **한 번 늘렸다가 되돌렸다**: 못 하나에 안 쓰이는 `# type: ignore[arg-type]` 를 달아 37 이 됐고 그 줄을 걷었다 | S164 5-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S164 5-4 |
| `tools\count_sites.py` | 사실 **하나 더했다** — 「「단순 합」 금액」 · **자리 3 (파일 2) · 한 자리 밖 1** · 못은 **안 켰다** | S164 2-1 |

## 8. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **98건** (① 7 · ② 90 · ③ 1) | S164 5-6 · `daily_brief.total_items` |
| 앞 판 | 98건 | S163 |
| 대조 | **닫은 것 0 · 연 것 0** — **조사 판이라 그것이 정상이다** | S164 5-6 |
| 대신 한 것 | **넷의 본문에 이 판이 잰 값을 앉혔다** — 「단순 합」 두 자리 · ESS 행 · 차이 0원 · 두 장이 반대 | S164 5-6 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **7** · 나 **24** · 다 **7** · 라 **27** · 마 **33** · 합 **98** — **그대로다** | S164 5-6 |
| 「다음 작업」 칸 | **2,536자** (상한 3,000 · **남은 464자**) | S164 5-7 — **S162 판치를 걷었다. 남긴 판치는 S164·S163. 다음 판은 S163 판치를 걷는다** |
| 판치를 걷기 전 확인 | **162세션 절은 있다**(`PROCEED.md` · 5,498자). 그 절에서 판치의 값 일곱을 값으로 찾아 다 있는 것을 봤다(`axis_unit`·`on_axis` · `bar_height` · `bar_label` · 걷은 `delta_label_place` · 세로 쌓임을 안 고친 까닭 · 「열일곱」 대 실물 18 · 뭉침 96·96·93 과 그 못) | S164 5-7 |

**S165 가 이어받을 것 — 미해결 이름 그대로**

- **넷 가운데 셋은 「어느 쪽이 옳은지」 를 사람이 정해야 열린다** — 「단순 합」 이
  DR 을 담을 것인가 · 화면 요약표가 「미산출」 줄을 세울 것인가 · 장04·장07 가운데
  어느 문장이 조건을 더 받을 것인가. **이 판은 값만 냈다.**
- 「차이 0원」 은 **재현이 안 됐다** — 다시 열려면 그 값이 서는 조건을 먼저 찾아야
  한다(덱 벌 열여덟에는 없다).
- altair 축 제목의 한글이 세로로 쌓인다 (남은 그림 **열다섯**)
- 감도 막대도 행마다 자리가 모자랄 수 있다 (**브라우저로는 안 봤다**)
- 태양광 계산 근거의 「역률 감액 변화」 줄이 실은 초과사용부가금 몫이다
- 요금적용전력과 관측 최대가 같은지 보는 문턱이 1% 라 제7조 ①의 1 kW 와 다르다
- 감도 시트와 요금 계산 명세의 날값이 안 접힌다 · Excel 「15분 시계열」 열 이름
- 「나」 갈래 스물넷 가운데 표기 아닌 것 전부

## 9. 이 판이 못 잰 것

- **넷 가운데 셋의 「어느 쪽이 옳은가」** — 사람이 정할 자리라 값만 남겼다.
- **ㄷ 이 서는 조건** — 덱 벌 열여덟에 없다. 차이 절댓값 1,000원 미만이 나오는
  자료를 못 찾았다.
- **화면 png·Excel 실물로 ㄱ 을 본 것** — 값은 `AppTest` 로 떴고 Excel 은
  「단순 합」 칸이 없는 것만 확인했다. `capture_screen.py` 로는 안 찍었다.
- **DR 단가를 벌에 세우는 일** — `Case` 에 필드를 안 더했다(조사 판이다).
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **깨끗한 착수 판의 소요** — 착수 때 `site-energy` 가 함께 돌았다.
- 2번 PC 의 값 전부.
