# 인수인계 — 2026-09-10 마감 (S162)

**새 대화창이 통째로 붙이는 자리다.** S152 가 처음 세웠고 S153~S162 가 갈아 끼웠다.

**이 문서는 값만 담는다** (S153 4-4). 판단·다음 판·지킬 것은 웹 대화창이 쓴다.
값마다 **언제 어느 판에서 잰 것인지** 붙였다. 앞 인수인계의 문장을 옮겨 적지
않았다. 정본은 언제나 저장소다.

**작업 PC — 1번 PC** (Intel Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB ·
`-n auto` = 8). **2번 PC 값은 이 판도 안 쟀다.**

---

## 1. 저장소

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `HEAD` | **`git rev-parse HEAD` 로 본다** — 이 문서를 담은 커밋보다 **뒤**에 마감 커밋이 온다. **수를 박지 않는다** | S153 0-1 이 그 병을 잡았다 |
| `origin/master` | `HEAD` 와 같다 | S162 4-8 |
| 출발점 | `875d14d` (S161 마감) · 작업 트리 **깨끗** · `git pull` up to date | S162 0-1 |
| 덱 벌 | **18** — 정본은 `render_deck.CASES` 길이다. `key="` 는 **19** · `key=` 는 **21** | S162 4-5 · **세 방법으로 다시 쟀다** |
| 케이스 벌 | **11** (C1~C8 · R1~R3) | `build_case_definitions` · S154 1-5 |
| 요금표 | `data\tariff_kr_20260601.json` 하나 · `schema_version` 은 그 파일에서 읽는다 | S153 0-3 |
| **이 판이 만진 것** | `src\` 셋(`money.py` · `report\figures.py` · `ui\charts.py`) · `tests\` 셋(`test_doc_counts.py` · `test_integration.py` · `test_slides.py`) · `tools\` 하나(`capture_screen.py` — 자리 하나를 세웠다) · `PROCEED.md` · `docs\` 셋(`OPEN_ITEMS.md` · `HANDOVER.md` · `directives\S162.md` **새 파일**) · `output\casestudy_20260910.xlsx`(케이스 스터디를 돌려 다시 났다) | S162 |
| **안 건드린 것** | `data\` · `reference\` · `src\kwise\report\excel.py`·`document.py`·`slides.py` · `tariff\` · `measures\` — **한 줄도 안 고쳤다** | S162 |

## 2. 시험

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 착수 앞단 전체 | **1,733 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,733**) · **3분 47초** | S162 0-1 · 1번 PC · **깨끗한 판** |
| 마감 앞단 전체 | **1,736 passed · 0 failed · 0 xfailed · skip 0** (수집 **1,736**) · **3분 55초** | S162 4-1 · 1번 PC · **오염된 판** |
| 문서를 갈아 끼운 뒤 한 판 더 | **1,736 passed · 0 failed · skip 0** · **3분 34초** | S162 4-7 · 1번 PC · **오염된 판** — 못 여럿이 그 글을 읽으므로 고친 뒤에 다시 본다 |
| 판이 깨끗했나 | **착수는 깨끗 · 마감은 오염**이다. 마감 때 `site-energy` 의 streamlit(PID 10360)과 그 자식 둘이 돌았고 **남의 세션이라 안 죽였다.** **두 판의 소요를 견주지 않는다** | S162 0-1 · 4-1 |
| 왜 셋 늘었나 | **못 넷을 세우고 시험 하나를 걷었다 — 순증 셋이다.** 걷은 것은 `test_slides.py::test_차액_라벨이_0선_반대쪽에_선다` 이고 그 자리를 `bar_label` 이 대신하면서 `delta_label_place` 자체가 없어졌다. **죽은 시험이 없다** | S162 4-1 |
| 상한 8분까지 | **4분 05초 남는다** (1번 PC · 마감 판 · **오염된 판이라 견주는 데 안 쓴다**) | S162 4-1 |
| 2번 PC | **이 판도 안 쟀다.** S114 에 1,631건 8분 06초로 상한을 6초 넘겨 **갈래별 분할 대상** | S114 |
| 살아 있는 못 | **0건** — `@pytest.mark.xfail` 이 `tests\` 전수에 0곳 | S155 1-4 · 이 판도 안 늘렸다 |
| 못을 되돌려 확인했나 | **넷 다 했다.** 어느 자리를 어떻게 틀렸는지는 아래 5절 | S162 0-4 · 3-4 |

## 3. 케이스 스터디

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 판정 | **160/160** | S162 4-2 |
| 기상 취득 | **0회** (캐시가 다 찼다) | S162 4-2 |
| 소요 | 도구 **141.6초** / 벽시계 **150초** | S162 4-2 · 1번 PC · **오염된 판** — `site-energy` streamlit 이 함께 돌았다. **앞 판(S161 도구 126.0초)과 견주지 않는다** |
| 회귀값 여덟 | 아래 표 · **불변** | S162 4-2 |
| 왜 안 움직여야 맞나 | 이 판이 고친 것은 **그림의 표기와 배치**뿐이고 **이 도구의 여덟은 `report\casestudy.py` 가 스스로 센다** — 그 계산에 안 닿는다 | S162 4-2 |
| 관측 최대는 어디서 읽나 | 화면 표에 안 나온다 — `output\casestudy_20260910.xlsx` 「케이스」 시트의 **「관측 최대수요(kW)」 열** | S158 5-2 |

| 벌 | 관측 최대수요 | 요금적용전력 | 부하율 | 기본요금 비중 |
|---|---|---|---|---|
| C1 | **5,293.44 kW** | 5,293 kW | 49.0% | 13.5% |
| R1 | **132.28 kW** | 132 kW | 41.1% | 20.0% |
| R2 | **132.28 kW** | 132 kW | 41.1% | 18.3% |
| R3 | **132.28 kW** | 132 kW | 41.1% | 27.4% |

**접는 문턱이 실제로 가르는 벌은 여기 하나뿐이다** (S160 3절) —
**C6 야간 피크형**: 관측 **10,920.64 kW** · 요금적용 **3,604 kW**.
덱 18벌에는 그런 벌이 없다.

## 4. 화면 감사

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 고친 **뒤** | **957** (을) · **807** (갑Ⅰ) · **964** (교육갑) · **808** (교육갑저압) | S162 3-5 · **한 자도 안 늘렸다** (앞 판 값과 같다) |
| 왜 안 움직이는 것이 맞나 | 이 판이 갈아 낀 것은 **그림의 축 제목과 막대 라벨**이다 — 감사가 세는 것은 화면 **문구 조각**이라 차트 안 글자는 그 대상이 아니다 | S162 3-5 |
| 규칙 위반 | **없음** (① · ②) | S162 4-3 |
| 화면 예산 | **모든 자리가 한도 안** (`tools\screen_budget.py`) | S162 4-3 |

## 5. 이 판이 고친 것 — S162 2·3절

**여섯을 저장소에서 다시 재니 여섯 다 재현됐다.** 벌은 **일반용(갑)Ⅱ 고압A
선택Ⅱ · 290 kW** — 요금제가 **넷**(선택Ⅰ~Ⅳ)인 유일한 갈래다(을 고압A 셋 ·
교육갑 둘). 화면은 `capture_screen.py --spot 선택요금`(이 판이 세운 자리),
PPT 는 `figures.py::tariff_option_png` 를 직접 구웠다.

| 무엇 | 재현된 값 | 어디 | 고쳤나 |
|---|---|---|---|
| ㄱ 축 이름이 빠진다 | 넷 가운데 **둘만**(선택Ⅰ·선택Ⅳ) | `charts.py::tariff_delta_chart` | **고쳤다** |
| ㄴ 억원 고정 | 위 칸 `억원` · 아래 칸 `현행 대비 (억원)` · 조합 `억원` | `figures.py` 셋 | **고쳤다** |
| ㄷ 값 라벨 없음 | 위 칸 막대 열둘에 라벨 **0** | `tariff_option_png` | **고쳤다** |
| ㄹ 단위가 둘 | x축 `1,000,000` · 라벨 `92만원` | `tariff_delta_chart` | **고쳤다** |
| ㅁ 라벨이 눈금과 겹친다 | `0.01억` 이 0 선·눈금에 얹힌다 | `tariff_option_png` 아래 칸 | **고쳤다** |
| ㅂ 축 제목이 세로로 쌓인다 | `억원` · `현행 대비 (억원)` | matplotlib 세로축 | **고쳤다 (PPT 만)** |

**뿌리는 셋이다** (S162 1-3).

| 뿌리 | 무엇 | 값 |
|---|---|---|
| ① 표기를 그림이 제 자리에서 짓는다 | ㄴ·ㄷ·ㄹ | `report\figures.py` 는 `money` 를 **들이지도 않았고** `1e8`·`1e4` 가 **다섯 자리** · `charts.py` 는 `won_short` **한 자리**만 지나 라벨만 만원이 됐다 |
| ② 자리가 모자라다 | ㄱ | 높이가 **140 으로 박혀** 있고 streamlit 이 `autosize: fit` 으로 태워 축·눈금이 65px 를 먹는다 — 막대 자리 **75px**, 한 행 **15px**. 겹치는 눈금 이름을 지우는 **vega 기본값**이 둘을 걸러 냈다 |
| ③ 라벨과 축 이름의 배치 | ㅁ·ㅂ | 53세션이 라벨을 0 선 건너편에 뒀는데 **거기가 x 눈금 자리** · matplotlib 은 세로축 이름을 90° 돌린다 |

**S161 이 세운 `report\columns.py`·`ui\tables.py` 는 표의 문이라 그림은 그 문
밖이었다 — 셋째 출구다.** `count_sites` 그물 아홉에는 **금액 축 단위를 보는 것이
없어** 리터럴을 `grep` 으로 셌다(그물이 못 보는 자리).

| 무엇 | 고친 것 |
|---|---|
| 문을 세웠다 | `money.axis_unit`(가장 큰 값이 1억 이상이면 억원, 아니면 만원 — S156 4-3 이 `monthly_charge_png` 에서 혼자 하던 셈을 옮겼다) · `money.on_axis`(그 단위로 접은 표기 · 접은 값이 10 미만이면 소수 두 자리) · `money.AXIS_UNITS` |
| 지나는 자리 | `figures.py` 넷(`combination_png` · `tariff_option_png` 두 칸 · `monthly_charge_png`) · `charts.py` 하나(`tariff_delta_chart`). **`1e8`·`1e4` 리터럴은 두 파일에 0곳** |
| 화면 차액 막대 | 열 이름이 `현행 대비(만원)` 이 되고 축 제목·라벨이 같은 자를 쓴다. **원값은 툴팁에 그대로** 남는다 |
| 자리 | `charts.bar_height(rows)` = `max(140, 65 + 30 × 행)`. `_AXIS_ROOM` 65 는 캡처로 잰 값이고 `_ROW_MIN` 30 은 눈금 이름이 안 걸러지는 폭이다 |
| PPT 값 라벨 | `Axes.bar_label` — 위 칸은 막대 위, 아래 칸은 **막대 끝 바깥**(0 선 건너편이 아니다). `delta_label_place` 와 그 시험을 걷었다 |
| PPT 축 이름 | `money_axis_title` — `set_ylabel` 을 비우고 **축 위 왼쪽에 가로로** 얹는다. 눈금에 천 단위를 넣어 라벨과 같은 꼴로 읽힌다 |
| 여백 | 아래 칸 `pad` 를 `reach * 0.10` → **0.16** — 첫 판에 「-3,492만원」 이 축 밖으로 밀려 x 눈금 이름에 닿았다(png 로 봤다) |
| 화면 세로 쌓임 | **안 고쳤다** — matplotlib(PPT)과 vega(화면)는 **다른 뿌리**라 미해결 이름을 그대로 뒀다 (S162 3-2 의 갈림길) |

**못 넷 · 되돌려 확인한 것 넷**

| 못 | 어디를 어떻게 틀렸더니 빨개졌나 |
|---|---|
| `test_doc_counts.py::test_세_방법이_세는_수의_차가_뭉친_몫과_같다` | 갈래 절 제목 「가 (7)」 → 「(8)」 · **「절 제목 합 97 · 건수 96」** / 「청구서 4」 → 「5」 · **「줄 93 + 뭉친 몫 4」** — **두 다리를 따로 봤다** |
| `test_integration.py::test_그림이_금액_단위를_밖에서_다시_짓지_않는다` | `fold = money.AXIS_UNITS[unit]` → `fold = 1e8` · **`figures.py:608` 을 이름으로 짚었다** |
| `test_integration.py::test_차액_막대가_갈래마다_축_이름_자리를_남긴다` | 높이를 `bar_height` → `140` · **「한 행이 25.0px 밖에 못 받습니다 (갈래 3)」** |
| `test_integration.py::test_차액_막대의_축과_라벨이_같은_단위다` | 축 제목을 「(원)」 으로 · **「축 제목이 접은 단위를 안 적습니다」** / 「(억원)」 으로 · **「단위가 둘입니다 — 만원 · 억원」** — **두 다리를 따로 봤다** |

**무는 못을 못 세운 자리 하나** — 라벨이 막대·눈금과 겹치는지는 `bar_label` 이
자리를 정하므로 값으로 물 수 없다. **png 로 눈으로 봤다**(S162 3-3).

## 6. 셋째 방법 — S162 0-4

| 방법 | 어느 코드 | 어떻게 센다 | S161 착수 | S162 |
|---|---|---|---|---|
| ① 절 제목 합 | 사람이 `OPEN_ITEMS.md` 의 `## 가 — … (7)` 다섯을 더한다 | 못은 S161 이 세웠다 | 98 | **96 → 97** |
| ② `total_items` | `daily_brief.py::total_items` | 갈래 **머리말의 「N건」** 을 더한다 | 98 | **96 → 97** |
| ③ 줄 | `daily_brief.open_items` 가 편 항목 수 | 괄호 밖 ` · ` 로 자른 **줄** | 95 | **93 → 94** |

**어긋남을 없애지 않고 「뭉침이라 옳다」 로 정했다** (S162 0-4). ①-1 「청구서 4」
한 줄이 넷을 담으므로 ① − ③ = **뭉쳐 더 담은 몫**(지금 3)이다. 펴려면 **이름
없는 항목 셋을 지어내야 한다** — 68세션 2절이 일부러 둔 모양이다.
**세 수가 한 자리에 서는 못을 세웠다** — 앞선 못 둘은 ①과 ②를(S161),
②와 갈래별 줄을(S159) 따로 물었다. **S161 이 적은 98 · 98 · 95 는 그 판 착수
시점 값이다** — 그 뒤 넷을 닫고 둘을 열어 96 이 됐다.

## 7. 도구가 낸 수

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| `ruff check .` | **All checks passed** | S162 4-4 |
| `ruff format --check .` | **9 어긋남 · 215 통과** (**지시서를 앉히기 전에는 214**) | S162 4-4. 목록은 `document.py` · `excel.py` · `slides.py` · `engine.py` · `test_casestudy.py` · `test_document.py` · `test_excess.py` · `test_tariff_engine.py` · `test_ui_screen.py`. **안 늘렸고 안 고쳤다** — 이 판이 만진 일곱 파일은 그 아홉과 겹치지 않아 형식을 맞췄다 |
| 곁수가 213 → 215 인 까닭 | **`ruff format` 이 `.md` 를 센다.** S161 은 제 지시서(`directives\S161.md`)를 앉히기 **전에** 쟀다(213) — 그 파일이 붙어 214 이고, 이 판이 `directives\S162.md` 를 앉혀 **215** 다. S146·S149 가 이미 가른 자리다. **어긋남 9 는 안 움직였다** | S162 0-3 · 4-4 |
| 맨 `mypy` | **10건 · 파일 둘** (`checked 170`) | S162 4-4. **안 고친다** |
| `mypy src` | **통과** (**111 파일**) | S162 4-4 |
| `tools\scan_ctrl.py` | **남은 자리 0곳** (제어문자 0 · 탭 0 · 건너뜀 1,272) | S162 0-2 · 4-4 |
| heredoc 자국 | **저장소로 들어온 것 0곳.** S161 커밋 범위의 파일 열아홉이 다 그 안에 있다. `%TEMP%` 스크래치는 S161 것이 안 남아 있어 못 훑었다 | S162 0-2 |
| 덱 | `small-a2` pptx **781 KB** · png **19장** · **그림 실패 0개** · **58.7초** | S162 4-5 |
| 캡처 | `capture_screen.py --spot 선택요금` **38초** · png 93~94 KB | S162 1-1 · 3-3 |

## 8. 미해결

| 무엇 | 값 | 언제 잰 값인가 |
|---|---|---|
| 건수 | **97건** (① 7 · ② 89 · ③ 1) | S162 4-6 · `daily_brief.total_items` |
| 앞 판 | 96건 | S161 |
| 대조 | **닫은 것 2 · 연 것 3** | S162 4-6 |
| 닫은 둘 | 그림 셋이 아직 억원 고정이다(나) · 미해결 「건」 이 갈래마다 다른 것을 센다(라 · **0-4 의 판정으로 닫았다**) | S162 |
| 연 셋 (다 「나」) | 화면 그룹 막대는 축이 원 고정이라 같은 카드의 차액 막대와 자가 다르다 · 「현행」 이 한 자리에 두 번 적힌다 · 감도 막대도 행마다 자리가 모자랄 수 있다 | S162 1-4 · 3절 |
| 갈래 (`docs\OPEN_ITEMS.md`) | 가 **7** · 나 **26** · 다 **7** · 라 **27** · 마 **30** · 합 **97** — **절 제목·본문·「지금 수」 절을 다 갈았다** | S162 4-6 |
| 「다음 작업」 칸 | **2,405자** (상한 3,000 · **남은 595자**) | S162 4-7 — **S160 판치를 걷었다. 남긴 판치는 S162·S161. 다음 판은 S161 판치를 걷는다** |
| 판치를 걷기 전 확인 | **160세션 절은 없다** — S160 은 세션 절을 안 쓰고 **목록표 행**(3,982자)에만 적었다. 그 행에서 판치의 값 여섯을 하나씩 찾아 다 있는 것을 봤다(자릿수 하나 43.2 · `whole_percents`·`low_load_threshold_line` · 「역률 감액 변화」 50,409,000 · 문턱 1% 세 자리와 52.93·C6 10,920.64/3,604 · 용인 조건에 남은 몫 DR 하나 · `ruff format` 9) | S162 4-7 |

**S163 이 이어받을 것 — 미해결 이름 그대로**

- 화면 그룹 막대는 축이 원 고정이라 같은 카드의 차액 막대와 자가 다르다
- altair 축 제목의 한글이 세로로 한 글자씩 쌓여 안 읽힌다 (**PPT 는 이 판이 고쳤고
  화면은 뿌리가 달라 남았다**)
- 「현행」 이 한 자리에 두 번 적힌다 (뜻을 사람이 정할 자리다)
- 감도 막대도 행마다 자리가 모자랄 수 있다 (**브라우저로는 안 봤다**)
- 태양광 계산 근거의 「역률 감액 변화」 줄이 실은 초과사용부가금 몫이다
- 요금적용전력과 관측 최대가 같은지 보는 문턱이 1% 라 제7조 ①의 1 kW 와 다르다
- 감도 시트와 요금 계산 명세의 날값이 안 접힌다 · Excel 「15분 시계열」 열 이름
- 「나」 갈래 스물여섯 가운데 표기 아닌 것 전부

## 9. 이 판이 못 잰 것

- **감도 막대가 실제로 축 이름을 잃는지** — 값으로는 한 행 25px 를 봤지만
  **브라우저로 안 봤다.**
- **화면 그룹 막대(`tariff_option_chart`)의 축 단위** — 원 고정 그대로다. 이름만 올렸다.
- **라벨 겹침을 무는 못** — `bar_label` 이 자리를 정하므로 값으로 물 자리가 없다. png 로 봤다.
- **`--durations=0` 파일별 건수** — 이 판도 안 쟀다 (S137 이 남긴 자리다).
- **`fact_sites.json` 에 금액 축 단위 그물을 넣는 것** — 범위 밖이라 안 넣었다.
- 2번 PC 의 값 전부.
