# 미해결 갈래 훑기

`PROCEED.md` 「현재 상태」 의 **미해결 칸을 갈래 다섯으로 갈라 이름으로 편 자리**다.
칸에는 항목이 등재된 차례로 서 있어 **무엇이 지금 가장 아픈지가 안 보인다** —
이 문서가 그 순서를 바꿔 놓는다.

**S144 에 세웠다.** 앞서 S140 이 같은 훑기를 했는데 **저장소에는 수만 남고
항목 이름 목록이 없었다** — 세션 절에도 「다음 작업」 칸에도 표가 없었고, 그래서
S143 이 「가」 갈래를 **다시 세야 했다.** 68세션 규약 「갈아 끼우는 칸에 일을
얹지 않는다」 와 같은 자리인데 이번에는 칸이 아니라 **보고**가 그 자리였다.

**미해결 칸 안에 두지 않았다.** 그 칸은 이미 2만 자대이고 S135 가 절반으로
줄인 자리다 — 표를 넣으면 그 일을 되돌린다. 표 칸 안에 표를 넣을 수도 없다.

---

## 어떻게 쓰나

- **정본은 `PROCEED.md` 의 미해결 칸이다.** 이 문서는 그 칸을 갈래로 갈라
  **이름만** 다시 세운 자리다. 항목의 몸(경위·근거·값)은 여기 옮기지 않는다 —
  칸에 있고, 600자를 넘긴 것은 세션 절에 있다.
- **세션을 마칠 때 함께 고친다.** 미해결이 늘거나 줄면 아래 수와 이름이 함께
  움직인다. **못이 문다** — `tests\test_doc_counts.py` 의
  「미해결 건수」 가 이 문서의 수를 `tools\daily_brief.py` 가 세는 실물과 맞댄다.
- **갈래 표식(가·나·다·라·마)을 `PROCEED.md` 의 「미해결」·「다음 작업」 두
  칸에 쓰지 않는다.** 그 둘은 이름으로만 부른다 (71세션 규약).

## 갈래 다섯

S140 3-1 이 세운 다섯이다. 뜻은 그대로 쓴다.

| 갈래 | 뜻 |
|---|---|
| **가** | 계산 결과 값이 틀린다 |
| **나** | 값은 맞는데 그것을 적는 문장·그림이 틀린다 |
| **다** | 갈래가 실물에 한 번도 안 섰다 (벌이 없다 · 조건이 없다) |
| **라** | 기록·도구·문서 안쪽 사정 |
| **마** | 사람이 정해야 열린다 (문구 · 어휘 · 자료 대기) |

**한 항목이 둘에 걸리면 「무엇이 막고 있나」 로 가른다** (S144 에 못 박았다).
증상이 「문장이 틀리다」(나)여도 **사람이 문구를 정해야 움직이면 마**다 —
「마」 의 뜻이 「사람이 정해야 **열린다**」 이기 때문이다. 결정 없이 고칠 수
있으면 증상 쪽 갈래에 둔다.

**이 잣대는 S144 것이다.** S140 이 무엇으로 갈랐는지는 저장소에 없다 — 그래서
**S140 의 수와 지금 수를 견주지 않는다.**

## 지금 수

미해결 70건 (2026-09-08 · S150 · `daily_brief` 가 센 값).

**S150 이 「다」 하나를 닫고 「다」·「나」 를 하나씩 열었다.** 닫은 것은
「덱 벌이 하나도 없는 종별 둘 — 산업용(갑)Ⅰ·(갑)Ⅱ」 다 — 두 벌을 다 세웠고
케이스 스터디에도 올렸다. 그 항목 안에 한 줄로 들어 있던 「고압C 는 아무 벌도
없는 전압이다」 를 **「벌이 하나도 없는 전압 행이 열이다」 로 꺼내 「다」 에
다시 세웠다** — 닫으면서 조용히 사라질 자리였다. 「나」 에 연 것은
「산출물이 선택요금 키를 날로 적는다 — 「선택single」」 이고, 새 벌이 처음
드러낸 것이다. **수는 69 → 70 이다.**

**S149 가 「라」 셋을 열었다** — 「그물 아홉 가운데 값을 무는 시험을 못 짚은 것
둘」 · 「산출물에 뜨는 값을 전수로 안 셌다」 · 「시험이 무는 수가 옳은지는 안
봤다」. 셋 다 S148 3절이 스스로 적은 것이고, 앞 둘을 한 건으로 묶은 까닭은
성격이 같아서다(회수기간 「즉시」·MWh 표기 — 이름으로만 훑고 전수로 안 갔다).
**줄어든 것은 없다.** 3절이 「라」 하나의 **이름을 좁혔다** —
「`data\source\` 와 `PROCEED.md` 의 줄끝을 어떻게 둘지」 →
「`data\source\` 의 줄끝을 어떻게 둘지」(`PROCEED.md` 쪽은 정할 것이 없다).

**S148 은 수를 안 움직였다** (66 → 66). **S147 도 안 움직였다** — 5절이
「라」 하나의 이름을 넓혔고(S149 가 도로 좁혔다) 3절이 「라」 하나의 **본문**을
1절 값으로 갈아 끼웠다(이름은 그대로).

**S146 3절이 「라」 둘을 열었다** — 「`fact_sites` 그물 다섯은 한 자리가 비어
못으로 켤 수 없다」 와 「`fact_sites` 못이 무는 것이 확인된 모양은 한 줄 인라인
하나다」 다. **줄어든 것은 없다.**

**S145 3절이 「라」 둘을 닫았다** — 「`docs\CAPTURES.md` 머리말이 「아홉 장」 인데
표에는 열하나다」(S144 가 등재한 지 한 판 만이다)와 「`test_measures.py`
독스트링이 걷힌 xfail 못을 「아래 xfail 못」 이라 부른다」 다. **늘어난 것은 없다.**

| 갈래 | 지금 (S147) | 그때 (S140) |
|---|---:|---:|
| 가 | **3** | 8 |
| 나 | **8** | 15 |
| 다 | **5** | 5 |
| 라 | **24** | 28 |
| 마 | **26** | 13 |
| 합 | **66** | 69 |

**두 열을 견주지 마라.** 잣대가 다르고(위 참조) 사이에 네 판이 닫은 것과 새로
등재한 것이 섞여 있다. 오른쪽 열은 **그때 그 판의 값**으로만 둔다.

---

## 가 — 계산 결과 값이 틀린다 (3)

이 갈래가 이 프로젝트가 지금 가장 크게 신경 쓰는 자리다.

| 항목 | 몫 | 재면 나오나 |
|---|---|---|
| 분석 기간이 요금표 판을 걸쳐도 갈아 끼우지 않고, 그 사실을 내는 안내가 없다 | **모름** — 판이 하나뿐이라 지금은 뜰 수 없다 | 새 판이 들어와야 잰다 |
| 교육용(을) 학교가 갑 고압을 희망하는 갈래가 없다 | **모름 — 안 쟀다** (약관 제58조 ④) | 재면 나온다 · 다만 「희망하는 경우」 를 자동으로 권할지는 사람이 정한다 |
| 화면 잉여 표시 반올림이 깨진다 | 0.1 + 0.8 = 0.8 MWh — **금액이 아니다** | 재면 나온다 |

### S140 이 센 여덟이 지금 어디 있나

S143 3절이 이름으로 다시 세운 표다. **판 번호로 무엇이 닫았는지 적는다.**

| 「가」 갈래 (S140) | 지금 | 무엇이 닫았나 · 몫 |
|---|---|---|
| 「0원」 과 「안 쟀다」 가 같은 모양이다 | **닫혔다** | 화면·Excel S142 · PPT·Word S143 1절 |
| 갑 갈래에서 계약전력 목표를 안 낸다 | **닫혔다** | S142 3절 (`small-a` 300 → 265 kW · 3,011,000원) |
| 계약전력 조정이 300 kW 종별 경계를 안 본다 | **닫혔다** | 계산은 98세션 · 안 뜨던 안내가 S143 2절 (을 700 → 갑Ⅱ 299 kW · 15,850,168원) |
| 초과 벌에서 목표도 부가금 금액도 안 낸다 | **닫혔다** | S143 2절 (`large-b-short` 4,000 → 5,294 kW · 122,451,200원) |
| 분석 기간이 요금표 판을 걸쳐도 안 갈아 끼운다 | **남았다** | 위 표 |
| 교육용(을) 학교가 갑 고압을 희망하는 갈래가 없다 | **남았다** | 위 표 |
| 화면 잉여 표시 반올림이 깨진다 | **남았다** | 위 표 |
| 덱 벌 `large-a`·`small-a` 가 종별 범위 밖이다 | **성격이 갈렸다** | 값이 아니라 **벌**이 틀린 자리라 「다」 로 옮겼다 (S143 3절) |

**S141~S144 에 새로 「가」 로 올라온 것은 없다.**

---

## 나 — 문장·그림이 틀린다 (9)

**결정 없이 고칠 수 있는 것만 여기 둔다.** 문구를 사람이 정해야 하면 「마」 다.

- Excel 요약 시트에 AMI 기준 안내 한 줄 (73세션에 자리와 글까지 정해 뒀다)
- 앵커가 약속한 전문을 매뉴얼이 담지 않는다
- Excel 조합 표에서 「+ ESS 목표 5,180 kW」 줄이 앞줄과 완전히 같다
- 상계거래 각주가 안 쓴 단가를 말한다
- Excel 수단별 결과에 코드 식별자가 나온다
- 계약전력을 먼저 낮추면 태양광 몫이 사는데 2단계가 「없음」 이라 적는다
- Excel 수단별 결과의 회수기간이 낮출 자리가 없을 때 「—」 다
- ⑦ 의 한계를 매뉴얼에 적는다 — 「우리 합계는 청구액이 아니다」
- 산출물이 선택요금 키를 날로 적는다 — 「선택single」

## 다 — 갈래가 실물에 한 번도 안 섰다 (5)

**뜨지 않는 갈래는 없는 갈래와 같다** (결함 유형 ②).

- 벌이 하나도 없는 전압 행이 열이다
- 덱 벌 `large-a`·`small-a` 가 제 종별 범위 밖에 서 있다
- 달 단위로만 걸리는 갈래가 서는 벌이 하나도 없다
- 배치에는 벌 목록이 없어 종별별로 서는지 아무도 안 본다
- 특례를 켠 벌이 저장소에 하나도 없어 Excel 특례 할인 열이 늘 빈다

## 라 — 기록·도구·문서 안쪽 사정 (27)

- `fact_sites` 그물 다섯은 한 자리가 비어 못으로 켤 수 없다
- `fact_sites` 못이 무는 것이 확인된 모양은 한 줄 인라인 하나다
- 그물 아홉 가운데 값을 무는 시험을 못 짚은 것 둘
- 산출물에 뜨는 값을 전수로 안 셌다
- 시험이 무는 수가 옳은지는 안 봤다
- 미해결 이름이 갈래 안에서 밀린다
- 미해결 「건」 이 갈래마다 다른 것을 센다
- 프로젝트 지식 재업로드 칸이 기록 어디에도 없다
- 지시서가 옮겨 적는 수를 지키는 못이 없다
- 문서의 수 가운데 못이 못 잡는 것이 남았다
- `ruff format --check .` 가 어긋난 파일을 남긴다
- 맨 mypy 가 파일 둘에서 10건 난다 — 문서는 「pass」 라 적고 있었다
- pytest 갈래 소요가 시험 수로 설명되지 않는다
- 2번 PC 의 pytest 소요가 어느 파일에 먹히는지 안 쟀다
- 한 판만 빨개진 시험이 `test_integration.py` 에 있는데 이름을 못 잡았다
- `capture_screen.py` 가 2·3단계에 못 간다
- `--spot 요금구조` 가 갑Ⅱ 를 세우지만 드롭다운이 화면에 안 남는다
- 화면 캡처 도구가 역률을 못 준다
- 같은 경고 문자열의 사본이 셋이다 — 걸름으로 막았고 뿌리는 남았다
- 부분 합을 만드는 자리가 리뷰가 센 것보다 많다
- xlsx 이름을 바꾸려면 두 상수를 같은 커밋에서 고친다
- 갑Ⅱ 선택Ⅲ·Ⅳ 를 갑Ⅰ 고압 행 참조로 세워 두었다
- 판정의 근거로 쓰는 정본 PDF 가 저장소에 없어 PC 마다 다를 수 있다
- `pv\archive.py` 주석이 시군구를 229 로 적는다
- 덱 자료 주석이 용인 실측을 「케이스 스터디 목록에 없다」 고 적는다
- S124 가 적은 317,220원이 그 벌에서 안 나온다
- Period 박싱

## 마 — 사람이 정해야 열린다 (26)

**자료·실물을 기다리는 것 여덟** (`PROCEED.md` 의 ① 과 ③ 갈래 전부)

- 청구서 넷 · 참고단가 하나 · ESS 실내형 사례 하나 · 실측 하나
- 1시간 간격 자료 하나

**문구·어휘·정책을 정해야 열리는 것 열여덟**

- 「나」 화면 후보 셋의 문구를 한 판에 잡는다
- 화면·PPT 는 태양광 절감이 하한에 닿은 것을 말하지 않는다
- 초과 벌의 상향 목표와 몫이 PPT·Excel 에 안 선다
- PPT 용어집이 요금적용전력을 「기본요금을 매기는 전력」 이라 적는다
- 선택요금 앵커가 Ⅳ 와 단일을 안 담는다
- 역률 개선 카드가 투자비를 안 받았는데 회수기간을 「즉시」 라 적는다
- 수단 장의 주의사항 표가 어느 산출물에도 서지 않는다
- 4장의 사용량 그림은 폴백이 없다 — 실패하면 덱이 통째로 죽는다
- 숫자 열의 절감액 0 은 「없음」 을 못 담는다
- 요금 총액에 소수가 남는다
- 「견주」 를 매뉴얼·기술서에 쓸 수 없다
- 제67조의3 제1항 갈래의 초과사용부가금을 산출하지 않는다
- 종별 문턱이 요금표와 `rules_kr.json` 두 자리에 있고 정본이 안 정해졌다
- 60세션 항목 B — `DocumentSections.diagnosis` 를 필수로 바꿀 것인가
- ESS 차익거래 운전 전략
- 교육용(을)·산업용(을) 벌을 케이스 스터디에 올릴지
- `data\source\` 의 줄끝을 어떻게 둘지
- `CLAUDE.md` 9항 1번 PC 의 `-n 4` 두 칸이 「모름」 인데 잴 것인지가 안 적혀 있다
