# kWise 프로젝트 규약

태양광 피크저감·전기요금 분석 도구. 15분 실측 사용량 데이터로 태양광 도입 시
전기요금 절감 효과를 산출한다.

## 작업 시작 전 필독

0. **`git pull` 을 먼저 한다.** PC 두 대를 GitHub 를 사이에 두고 번갈아 쓴다.
   당기지 않고 시작하면 다른 PC 의 작업 위에 덮어쓰게 된다.
1. `docs\ENVIRONMENT.md` — 실행 환경 제약 (Windows 11)
2. `docs\REQUIREMENTS_kwise.md` — 요구사항
3. `PROCEED.md` — 지금까지의 진행 이력과 미해결 항목

## 절대 규칙

- 가상환경은 프로젝트 내 `.venv` 를 쓴다. 패키지 설치는 `pip install`.
  새 패키지가 필요하면 **먼저 물어본다.**
- **python 실행은 `.venv\Scripts\python.exe` 로 한다.** 셸이 `.venv` 를 활성화하지 않은 채
  실행될 수 있으므로 시스템 python 을 쓰면 패키지를 못 찾는다. `pytest` 도
  `.venv\Scripts\python.exe -m pytest` 로 실행한다.
- **파일을 열 때 `encoding=` 을 반드시 명시한다.** Windows 기본이 cp949 라 생략하면 한글이 깨진다.
  읽기는 `encoding="utf-8"`, Excel 에서 열 CSV 는 `encoding="utf-8-sig"`.
- **경로는 `pathlib.Path` 만 쓴다.** 문자열 결합과 구분자 하드코딩 금지.
- 캐시·중간 산출물은 `PROJECT_CACHE` 환경변수 경로(기본값 `.\cache`), 최종 산출물만 `output\`.
- **Excel 저장 전 tz-aware 컬럼을 `dt.tz_localize(None)` 로 해제한다.** pvlib 결과는 항상 tz-aware다.
- 결과 파일에 날짜·시각 접미사를 붙인다. `result_20260806_1430.xlsx`
  Excel 이 파일을 열고 있으면 덮어쓰기가 실패하므로 필수다.
- matplotlib 사용 시 `font.family = "Malgun Gothic"`. Streamlit 차트는 altair/plotly 우선.
- 코드 주석과 응답은 한국어.

### 백그라운드 실행

1. **백그라운드가 필요한지부터 묻는다.** 앞단에서 돌려도 되는 것을 뒤로 보내면
   확인이 늦어진다. 진짜로 오래 걸리는 것만 뒤로 보낸다.
   **대기 루프(`until grep ... sleep`)는 만들지 마라. 그냥 기다려라.**
2. **돌리기 전에 예상 소요를 말한다.** 모르면 "모른다"고 말한다.
   **짐작한 숫자를 적지 마라.** `tools\run_benchmark.py` 가 단계별 소요를 재고
   있으니 그 값을 근거로 쓴다.

   **재기 전에 다른 프로젝트의 python 이 도는지 본다** (69세션). 이 PC 는
   한 대인데 세션은 여럿이다 — 68세션에 `cdu-simul` 의 pytest 가 **한 코어를
   물고** 있어 소요 둘이 오염됐다. 돌고 있으면 **소요를 적되 「견주는 데 쓰지
   않는다」 를 함께 적는다.** 남의 세션을 죽이지 마라.

       Get-CimInstance Win32_Process -Filter "Name='python.exe'"
3. **모든 명령에 타임아웃을 건다.** 네트워크가 끼면 반드시 —
   Open-Meteo 취득이 그렇다. 상한을 넘으면 죽이고 그 사실을 보고한다.
4. **출력은 파일로 리다이렉트하고 `python -u` 를 쓴다.** 0바이트로 끝나면
   판정 자체가 불가능하다. 진행을 중간중간 찍어 어디서 멈췄는지 알게 한다.
5. **같은 파일명에 두 실행을 쓰지 않는다. 실행마다 타임스탬프를 붙인다.**
   낡은 결과가 새 결과처럼 보인다. 감시 대상 파일을 지울 거면 루프를 먼저 멈춘다.
6. **예상 소요를 넘기면 스스로 멈추고 보고한다.** "아직 도는 중" 이 아니라
   "예상 O분을 넘겨 중단했다. 여기까지 나왔다" 로 보고한다.
   **사용자가 물어볼 때까지 기다리지 마라.**
7. **오래 걸릴 일은 시작 전에 나눌 수 있는지 본다. 나눠 돌리는 것이 기본이다.**
   pytest 는 갈래별로 나눈다 — `PROCEED.md` 의 "pytest 분할 실행" 을 따른다.
   절반이 먼저 나오고, 중단돼도 어디까지 됐는지 안다.
8. **종료 코드만 보고 통과를 판정하지 마라.**
   출력 파일에서 실패 줄(`FAILED`·`ERROR`·`passed`/`failed` 요약)을 직접 확인한다.
   배경 실행은 종료 코드가 실제 결과와 어긋날 수 있다 — 48세션에 `exit code 0`
   인데 `test_integration.py` 6건이 깨져 있었다.
9. **전체 시험은 앞단에서 상한을 걸고 돌린다** (49세션). 앞단이면 결과를 그
   자리에서 보므로 8번 항목에 걸릴 일 자체가 없다.

       .venv\Scripts\python.exe -m pytest tests -n auto --dist load

   `addopts = "-q"` 라 요약 줄이 안 나오므로 **건수를 확인할 때는 `-rN --tb=no`**
   를 붙인다. **`-q` 를 다시 붙이지 마라** (67세션) — `addopts` 의 것과 겹쳐
   `-qq` 가 되면 **「N passed」 줄이 통째로 사라져** 점만 세게 된다.

   **`-n` 에 수를 박지 않는다** (66세션). 65세션이 2번 PC(6코어)에서 재 보니
   `-n 4` 는 CPU 를 절반도 못 쓰고 `-n 6` 은 88.5% 를 쓴다 — **두 코어가 놀고
   있었다.** 그렇다고 `-n 6` 을 박으면 **1번 PC 에서 틀린 수가 된다.** 지금
   고치는 병이 「PC 를 빼고 적은 수」인데 같은 병을 다시 앓는다.
   `auto` 는 그 PC 의 `os.cpu_count()` 를 쓴다. `--dist load` 는 그대로다.

   **소요는 PC 를 빼고 적지 않는다.** 두 대의 사양이 다르다.

   | PC | `-n auto` 실측 | 메모리 (67세션 실측) |
   |---|---|---|
   | **2번** (AMD Ryzen 5 4500U · 6코어 6스레드 · RAM 7.5 GB) | `auto` → **6** · **1,513건 6분 50초 (77세션 — 깨끗한 판. 앞뒤로 다른 프로젝트 python 0개를 확인했다)** · 1,496건 **6분 37초·6분 47초** (67세션 두 판) · 1,493건 7분 45초 (66세션). **68세션의 7분 21초는 깨끗한 값이 아니다** — 다른 프로젝트(`cdu-simul`)의 pytest 가 한 코어를 물고 함께 돌고 있었다. 견주는 데 쓰지 않는다. **`-n 4` 는 1,496건 9분 41초** (67세션 · 65세션 9분 22초) | `auto` 최대 RSS **2,270 MB** · 가용 최저 **671 MB** (2,440 에서) · 하드폴트 **27,202쪽(≈106 MB)/402초**. `-n 4` 최대 RSS **1,785 MB**. **커밋 최고 12,383 / 13,429 MB** |
   | **1번** | **모름.** 재면 이 칸을 채운다 — `auto` 가 고른 수도 함께 적는다 | **모름** |

   49세션이 적어 둔 「1,379건 2분 53초」 는 **어느 PC 값인지 기록이 없어 지웠다.**
   짐작해 채우지 않는다.

   **`-n auto` 를 유지한다 — 메모리를 재고 정했다** (67세션). 43세션이 「여유
   메모리가 절반이 된다」 고 적었고 66세션이 **소요만 보고 뒤집었다.** 이제
   양쪽이 다 있다.

   **아래 넉 줄은 모두 2번 PC 값이다** (68세션에 PC 를 붙였다). 위 규칙이
   소요만 두고 한 말이 아니다 — **메모리도 PC 를 빼고 적지 않는다.**

   - **43세션 말이 맞다. 오히려 더 준다** — **2번 PC** 에서 가용이
     2,440 → **671 MB**(72%)까지 떨어진다. **2번 PC** 의 `-n 4` 보다
     최대 RSS 가 **485 MB(+27%)** 많다.
   - **그런데 그것이 기준이 아니다.** 기준은 **스왑이나 실패로 이어지느냐**다.
     **2번 PC** 에서 하드폴트가 402초에 **27,202쪽(초당 68쪽)** 뿐이라
     **스왑이 아니고**, 1,496건이 두 판 다 통과했다.
     **여유가 줄어도 남으면 문제가 아니다.**
   - **2번 PC** 가 얻는 것은 **179초(−31%)** 다. 그래서 유지한다.
   - **다만 2번 PC 는 커밋이 최고 12,383 / 13,429 MB (92%) 까지 간다.**
     그 PC 에서 시험이 도는 동안 **무거운 것을 함께 띄우지 않는다.**

   **1번 PC 는 이 판단의 근거에 들어 있지 않다.** 코어 수도 RAM 도 모르고,
   `auto` 는 그 PC 의 `os.cpu_count()` 를 쓰므로 일꾼이 6이 아닐 수 있다 —
   그러면 2,270 MB 도 그 PC 값이 아니다. **그 PC 에서 처음 전체 시험을 돌릴
   때 소요와 메모리를 함께 잰다** (`PROCEED.md` 미해결 ②에 올려 두었다).
   **짐작해 채우지 않는다.**

   **갈림길 — 앞단 전체 실행이 8분을 넘는 PC 에서는 갈래별로 나눠 돌린다**
   (`PROCEED.md` 「pytest 분할 실행」 의 갈래 넷). **2번 PC 는 안쪽이다 —
   77세션에 깨끗한 판에서 1,513건 6분 50초**(전후로 다른 프로젝트 python 0개를
   확인했다). 상한 8분까지 **70초** 남는다. `-n 4` 로는 **9분 41초**라 여유가
   19초뿐이었다.

   **S72~S76 의 7분 39초~8분 10초는 깨끗한 값이 아니다** — 다섯 판 다
   `cdu-simul` 이 함께 돌았다. **오염이 실제로 1분 가까이 먹는다.**
   넘긴 뒤에 판단하면 늦으므로 **잴 때마다 본다** — 그리고 **재는 판이
   깨끗한지 먼저 본다.**

   뒤로 보내는 것은 **갈래로 나눠도 10분을 넘길 일**뿐이다 — 기상 사전 취득 같은 것.
10. **세션을 마칠 때 돌고 있는 셸이 없는지 확인하고 보고한다.** 마무리 절차에 넣는다.

## 화면 문구

**화면 문구는 늘리지 않는다.**

- 새 안내를 붙이기 전에 **용어나 이름을 고쳐서 해결되는지** 먼저 본다.
  설명이 필요하다는 것은 대개 이름이 틀렸다는 뜻이다.
- 배경·근거·제도 설명은 **매뉴얼(`docs\MANUAL.md`)에 적는다.** 화면에 두지 않는다.
- 화면에 남기는 것은 둘뿐이다 — **없으면 입력을 못 하는 것, 없으면 결과를 오독하는 것.**
- 문구를 하나 더할 때는 **무엇을 뺄지 함께 정한다.**
  예산(본문 3줄·확인사항 3항목)이 그 장치다 — `tools\screen_budget.py`.

**고치기 전과 후에 `tools\screen_audit.py` 를 돌린다.** 화면에 실제로 그려진
문구를 전부 모아 규칙 위반(코드 식별자·내부 문서 번호·규정 이름 없는 조문·
맨 물결표)과 중복 후보를 센다. 수가 늘었으면 늘린 이유를 적는다.

## 코드 규약

- src 레이아웃. `pip install -e .`
- 타입힌트 사용. ruff, mypy 통과.
- **형이 없는 값은 `object` 가 아니라 `Any` 로 적는다.** `object` 는 「속성이
  하나도 없다」 는 뜻이라 쓰는 자리마다 오류가 나고 억제를 부른다 —
  70세션 mypy 142건의 뿌리가 이 한 낱말이었다. **남의 형(python-pptx·pandas
  같은 바깥 것)은 좁히지 않는다.** 좁히면 오히려 는다 — python-pptx `Slide` 로
  좁혔더니 `BaseShape` 때문에 90 → 110 으로 늘었다.
  **「모른다」 는 `Any`, 「없다」 가 `object`.**
- 모든 계산 함수에 단위테스트.
- **요금 계산은 순수 함수로 작성한다.** Streamlit 을 import 하지 않는다.
- 계산 로직과 UI 를 분리한다. UI 는 순수 함수를 호출만 한다.

- PowerShell 스크립트(`.ps1`)에 한글을 넣을 때는 **UTF-8 BOM** 으로 저장한다.
  BOM 없으면 Windows PowerShell 5.1 이 cp949 로 읽어 깨진다.

## 금지

- `pybuildingenergy` 사용 — 실측 부하를 쓰므로 불필요하다. 신축 부하 예측은 범위 밖이다.
- 결측값 자동 보간 — 기본은 미보간. 결측 구간은 계산에서 제외하고 명시한다.
- 요금 데이터 하드코딩 — 반드시 `data\tariff_*.json` 에서 읽는다.
  드롭다운 선택지도 이 파일에서 생성한다.
- 태양광을 기본 전제로 두는 설계 — 진단 단계는 설비 정보 없이 동작해야 한다.
  사용자가 파일만 올려도 결과가 나온다.
- 조합 절감액을 단순 합산 — 수단마다 요금을 다시 계산한다. 태양광이 사용량을 줄이면
  최적 선택요금이 바뀌고, ESS 가 피크를 낮추면 기본요금 기반이 달라진다.
- `reference\` 폴더 수정 — 읽기 전용 참조 코드다.
- 메모리 여유가 생겼다고 다중 시나리오를 동시 적재하지 않는다. 순차 처리를 유지한다.
- **파일 수정은 편집 도구로 한다. 셸에 문자열을 실어 파일을 고치지 않는다.**
  heredoc · `echo` · `printf` · `sed`/`awk` 치환이 **전부 같은 병**이다 —
  병은 heredoc 이 아니라 **문자열을 셸에 한 겹 더 태우는 것**이다.
  특히 **한글 · `\` · 백틱 · `$` · 정규식**이 든 문자열.
  뭉개진 자리는 **오류 없이 조용히** 지나가므로 돌려 보고도 모른다.

  - **33세션** — 같은 일로 **시험 둘이 죽었다.** 그 뒤 33세션 내내 몰랐다.
  - **65세션** — `\\` 가 하나로 뭉개져 `"tools\\run_casestudy.py"` 가
    `tools`+**CR**+`un_...` 이 되어 **치환이 안 먹었다**(오류도 안 났다).
    같은 날 `\\b` 는 **백스페이스로 박혀** `PROCEED.md` 를 오염시켰다.
  - **66세션** — **백틱이 명령 치환으로 먹혀** 문구 다섯 조각이 빈칸이 됐는데
    화면에는 `ok` 만 찍혔다. **heredoc 이 아니라서 옛 문구가 못 잡았다** —
    이름이 좁으면 규약 밖으로 샌다. 그래서 이름을 넓혔다.

  **글로만 있는 금지는 뜨지 않는 경고와 같다.** 검사는 `tools\scan_ctrl.py` 다 —
  제어문자가 박힌 자리를 **문자 단위로** 훑는다(`splitlines()` 로 세면 적게 센다).
  `tests\test_deployment.py` 가 매 시험에 돌리므로 새로 박히면 그 자리에서 걸린다.

## reference\ 폴더

기존 `pv_peak_cut` 코드다. **참조만 하고 수정하지 않는다.** 이식할 때는
`src\kwise\` 아래에 새로 작성하면서 타입힌트와 테스트를 붙인다.

| 파일 | 쓸 곳 |
|---|---|
| `mg_pv_core.py` | 4세션 — PV 계산 엔진 이식 |
| `mg_weather_openmeteo.py` | 4세션 — 기상 취득 이식 |
| `streamlit_app.py` | 1세션 — `parse_usage_datetime`, `read_uploaded_usage_file`, `match_usage_column` 참조<br>8세션 — `hourly_pattern_table` 참조 |

`parse_usage_datetime` 의 `24:00` 처리는 실측 데이터로 검증된 로직이다. 그대로 가져간다.
`mg_weather_openmeteo` 의 PowerShell 폴백은 프록시 우회용이다. Windows 에서는 동작하지만
`requests` 경로가 정상이면 쓰이지 않는다. 유지 여부는 4세션에서 판단한다.

## 세션 종료 시

작업을 마치면 `PROCEED.md` 에 아래를 추가하고 커밋한다.

- 완료한 항목
- 만든 파일 목록
- 미해결 항목과 그 이유
- **미해결 건수 대조 — 전 N건 → 후 M건, 늘어난 것과 줄어든 것을 각각 이름으로**
- 다음 세션이 알아야 할 결정 사항
- 돌고 있는 백그라운드 셸이 없는지 확인한 결과

이 기록이 다음 세션의 출발점이다. 빠뜨리지 않는다.

**건수 대조는 68세션에 붙었다.** 67세션이 「다음 작업」 칸을 갈아 끼우면서
60세션 항목 B 를 **소리 없이 지웠다** — 이 대조가 있었으면 그 자리에서
드러났다. 이미 쓰는 대조표와 같은 방식이라 새 장치가 아니다.
**수만 맞추지 말고 이름을 적는다** — 하나 늘고 하나 줄면 수는 그대로다.

**커밋 후 `git push` 로 원격에 올린다. 다른 PC 에서 이어받는다.**
올리지 않으면 그 PC 에만 남는다 — 다음 세션이 다른 PC 에서 열리면 없는 일이 된다.
