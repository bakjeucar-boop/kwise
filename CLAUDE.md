# kWise 프로젝트 규약

태양광 피크저감·전기요금 분석 도구. 15분 실측 사용량 데이터로 태양광 도입 시
전기요금 절감 효과를 산출한다.

## 작업 시작 전 필독

0. **`git pull` 을 먼저 한다.** PC 두 대를 GitHub 를 사이에 두고 번갈아 쓴다.
   당기지 않고 시작하면 다른 PC 의 작업 위에 덮어쓰게 된다.
1. `docs\ENVIRONMENT.md` — 실행 환경 제약 (Windows 11)
2. `docs\REQUIREMENTS_kwise.md` — 요구사항
3. `PROCEED.md` — 지금까지의 진행 이력과 미해결 항목.
   **「현재 상태」 는 통째로 읽지 마라 — 미해결 칸이 한 줄에 수만 자다**
   (몇 자인지는 여기 적지 않는다 — 지키는 못이 없는 수라 낡는다. 실측은
   `PROCEED.md` 「현재 상태」 의 그 칸을 재서 본다.
   S135 에 3만 6,948자에서 옮겼다. **항목 본문 상한은 600자**이고 넘으면
   몸을 세션 절로 보낸다 — `tests\test_doc_counts.py` 의
   `test_미해결_항목_본문은_상한을_넘지_않는다` 가 문다).
   필요한 항목만 이름으로 떠 온다 (`tools\daily_brief.py` 의 `open_items`).
4. **새 PC 에서 처음 케이스 스터디를 돌릴 때는 기상을 받는다.** 사전 취득분이
   2023~2025 라 용인 벌(2025-08~2026-08)은 그 밖이고, `PROJECT_CACHE` 캐시는
   저장소에 없다. 한 판 돌려 캐시를 채운 뒤에 작업에 들어간다.
   **`data\weather\`(사전 취득분 406파일)는 저장소에 있다** — 96세션 지시서가
   「없다」 고 적었는데 `git ls-files` 로 재 보니 추적되고 있었다. 없는 것은
   `cache\`(`.gitignore`) 쪽이다. `run_casestudy.py` 가 시작할 때 벌마다
   캐시를 보고 없으면 그 사실을 한 줄로 찍는다 (96세션 1절).

## 절대 규칙

- 가상환경은 프로젝트 내 `.venv` 를 쓴다. 패키지 설치는 `pip install`.
  새 패키지가 필요하면 **먼저 물어본다.**
- **python 실행은 `.venv\Scripts\python.exe` 로 한다.** 셸이 `.venv` 를 활성화하지 않은 채
  실행될 수 있으므로 시스템 python 을 쓰면 패키지를 못 찾는다. `pytest` 도
  `.venv\Scripts\python.exe -m pytest` 로 실행한다.
- **파일을 열 때 `encoding=` 을 반드시 명시한다.** Windows 기본이 cp949 라 생략하면 한글이 깨진다.
  읽기는 `encoding="utf-8"`, Excel 에서 열 CSV 는 `encoding="utf-8-sig"`.
- **경로는 `pathlib.Path` 만 쓴다.** 문자열 결합과 구분자 하드코딩 금지.
- 캐시·중간 산출물은 `PROJECT_CACHE` 환경변수 경로(기본값 `.\cache`), 최종 산출물만 `output\`.
- **Excel 저장 전 tz-aware 컬럼을 `dt.tz_localize(None)` 로 해제한다.** pvlib 결과는 항상 tz-aware다.
- 결과 파일에 날짜·시각 접미사를 붙인다. `result_20260806_1430.xlsx`
  Excel 이 파일을 열고 있으면 덮어쓰기가 실패하므로 필수다.
- matplotlib 사용 시 `font.family = "Malgun Gothic"`. Streamlit 차트는 altair/plotly 우선.
- 코드 주석과 응답은 한국어.

### 백그라운드 실행

1. **백그라운드가 필요한지부터 묻는다.** 앞단에서 돌려도 되는 것을 뒤로 보내면
   확인이 늦어진다. 진짜로 오래 걸리는 것만 뒤로 보낸다.
   **대기 루프(`until grep ... sleep`)는 만들지 마라. 그냥 기다려라.**
2. **돌리기 전에 예상 소요를 말한다.** 모르면 "모른다"고 말한다.
   **짐작한 숫자를 적지 마라.** `tools\run_benchmark.py` 가 단계별 소요를 재고
   있으니 그 값을 근거로 쓴다.

   **재기 전에 남의 python 을 센다** (69세션). **1번 PC 는 셋(판 앞 ·
   회귀 앞 · 뒤) · 2번 PC 는 넷(갈래 사이 포함)** — 1번은 통째 실행 중엔 잴 수 없고
   대기 루프가 금지다. **「깨끗한 판」 은 그 PC 의 세는 자리가 다 0 인 판**이고, 아니면
   소요에 「견주는 데 쓰지 않는다」 를 붙인다. 남의 세션을 죽이지 마라.

       Get-CimInstance Win32_Process -Filter "Name='python.exe'"
3. **모든 명령에 타임아웃을 건다.** 네트워크가 끼면 반드시 —
   Open-Meteo 취득이 그렇다. 상한을 넘으면 죽이고 그 사실을 보고한다.
4. **출력은 파일로 리다이렉트하고 `python -u` 를 쓴다.** 0바이트로 끝나면
   판정 자체가 불가능하다. 진행을 중간중간 찍어 어디서 멈췄는지 알게 한다.

   **셸 파이프·리다이렉트 대신 `tools\run_tool.py` 를 쓴다** (S207 4절).
   도구·`pytest`·아무 명령을 돌려 stdout 과 stderr 를 함께
   `PROJECT_CACHE\runs\<이름>_<타임스탬프>.txt` 로 받고 꼬리만 화면에 낸다 —
   `| head` · `| Select-String` · `2>&1` · `> $null` 이 여덟 판 연속 샌
   자리이고(S199~S206) 그것이 「금지」 절의 **문자열을 셸에 한 겹 더 태우는 것**과
   같은 병이다. `--tail` 은 도구 이름 **앞**에 둔다.

       .venv\Scripts\python.exe tools\run_tool.py --tail 20 screen_audit --list

   **도구가 있어도 안 부르면 샌다** (S209 1-5). S208 은 이 도구 셋을 손에 쥐고도
   아홉 자리를 셸에 태웠고 **그 가운데 하나가 실제로 뭉개졌다**(`\\` → `\`).
   그 판 앞 일곱(S200~S206)은 도구가 **없어서** 샌 것이라 갈래가 다르다 —
   **지금 남은 병은 「없다」 가 아니라 「안 부른다」 다.**

   **스물둘을 다 받는다** (S209 2절). `count_sites`(`main(argv)`)와
   `daily_brief`(`sys.stdout.reconfigure`)가 못 받던 자리를 닫았고
   `tests\test_deployment.py` 의 못 둘이 **새 도구가 붙어도 저절로 문다.**
   **다만 상대 경로 명령은 못 받는다** — Windows 가 `cwd` 에서 실행 파일을
   안 찾으므로 `.venv\Scripts\ruff.exe` 같은 것은 **온 경로로 적는다.**
5. **같은 파일명에 두 실행을 쓰지 않는다. 실행마다 타임스탬프를 붙인다.**
   낡은 결과가 새 결과처럼 보인다. 감시 대상 파일을 지울 거면 루프를 먼저 멈춘다.
6. **예상 소요를 넘기면 스스로 멈추고 보고한다.** "아직 도는 중" 이 아니라
   "예상 O분을 넘겨 중단했다. 여기까지 나왔다" 로 보고한다.
   **사용자가 물어볼 때까지 기다리지 마라.**
7. **오래 걸릴 일은 시작 전에 나눌 수 있는지 본다. 나눠 돌리는 것이 기본이다.**
   pytest 는 갈래별로 나눈다 — `PROCEED.md` 의 "pytest 분할 실행" 을 따른다.
   절반이 먼저 나오고, 중단돼도 어디까지 됐는지 안다.
8. **종료 코드만 보고 통과를 판정하지 마라.**
   출력 파일에서 실패 줄(`FAILED`·`ERROR`·`passed`/`failed` 요약)을 직접 확인한다.
   배경 실행은 종료 코드가 실제 결과와 어긋날 수 있다 — 48세션에 `exit code 0`
   인데 `test_integration.py` 6건이 깨져 있었다.

   **실패 줄뿐 아니라 수집 건수가 앞 판과 같은지 본다** (83세션). 이름을 바꾸는
   중에 전체 pytest 를 돌려 `test_ui_screen.py` **234건이 통째로 수집 실패**했는데
   남은 것은 다 통과라 **초록으로 보였다.** 48세션의 `exit code 0` 과 같은 모양이다
   — **안 돈 시험은 실패 줄을 내지 않는다.** 앞 판 건수는 `PROCEED.md` 「현재 상태」
   와 직전 세션 기록에 있다.

   **skip 도 안 돈 시험이다.** 요약에 skip 수가 안 뜨므로 화면에는 초록만
   보인다 — 자료 파일이 없어 걸러진 시험이 그렇다. 회귀 보고에 passed 만
   적지 말고 skip 수를 함께 적는다.

   **이 넷을 손으로 세지 않는다 — `tools\pytest_counts.py` 가 읽는다** (S207 4절).
   `run_tool.py` 가 받아 둔 출력에서 **수집 건수 · 결과 수(skip 포함) · `FAILED`
   이름 · `ERROR` 줄**을 내고, 수집과 결과 합이 어긋나면 그 사실을 적는다.
   `--base` 에 앞 판 수집 건수를 주면 차를 함께 낸다. **`FAILED` 줄의 한글
   인자 이름을 풀어 준다** — pytest 가 테스트 ID 를 이스케이프해 박으므로
   그대로 두면 사람이 못 읽는다.

       .venv\Scripts\python.exe tools\pytest_counts.py --base 1787
9. **전체 시험은 앞단에서 상한을 걸고 돌린다** (49세션). 앞단이면 결과를 그
   자리에서 보므로 8번 항목에 걸릴 일 자체가 없다.

       .venv\Scripts\python.exe -m pytest tests -n auto --dist load

   `addopts = "-q"` 라 요약 줄이 안 나오므로 **건수를 확인할 때는 `-rf --tb=no`**
   를 붙인다. **`-q` 를 다시 붙이지 마라** (67세션) — `addopts` 의 것과 겹쳐
   `-qq` 가 되면 **「N passed」 줄이 통째로 사라져** 점만 세게 된다.

   **`-rN` 이 아니라 `-rf` 다** (S132 0절). `-rf` 는 건수 줄을 그대로 내면서
   **실패한 시험 이름까지** 남기므로 `-rN` 이 하던 일을 다 하고 잃는 것이 없다.
   S131 ③ 갈래에서 한 건이 빨갰는데 `-rN` 이라 이름이 안 남았고 그 뒤 네 판을
   이름 찾느라 돌렸다. 규약은 **이 자리 하나다** — S131 은 그 사실을
   `PROCEED.md` 세션 목록표 행에만 적었고 그 칸은 기록이라 다음 판이 안 읽는다.

   **기록 묶음 — 코드를 안 고친 판은 이 한 줄을 돌린다** (S176 4절).

       .venv\Scripts\python.exe -m pytest tests -m records -rf --tb=no

   `PROCEED.md` · `CLAUDE.md` · `docs\` 를 읽는 시험에 `records` 마커가 붙어
   있다(파일 넷은 `pytestmark` · 그 밖은 함수에). **지시서는 파일 이름을 박지
   않고 이 한 줄을 부른다** — S173~S175 가 문서 못 두 파일(49건)을 박아 기록을
   무는 40함수 가운데 17만 돌았고, 세션 행 대조 시험이 빨간 채 두 판을 넘겼다.
   **기록을 다 고친 뒤에 돌린다** — 앞에 돌리면 옛 기록을 본다. 기록을 읽는
   시험을 그 파일 넷 밖에 새로 쓰면 마커를 붙인다 — 안 붙이면
   `test_doc_counts.py::test_기록을_읽는_시험은_records_묶음_안에_있다` 가 문다.

   **`-n` 에 수를 박지 않는다** (66세션). 65세션이 2번 PC(6코어)에서 재 보니
   `-n 4` 는 CPU 를 절반도 못 쓰고 `-n 6` 은 88.5% 를 쓴다 — **두 코어가 놀고
   있었다.** 그렇다고 `-n 6` 을 박으면 **1번 PC 에서 틀린 수가 된다.** 지금
   고치는 병이 「PC 를 빼고 적은 수」인데 같은 병을 다시 앓는다.
   `auto` 는 그 PC 의 `os.cpu_count()` 를 쓴다. `--dist load` 는 그대로다.

   **소요는 PC 를 빼고 적지 않는다.** 두 대의 사양이 다르다.

   **PC 가 열이고 항목이 행이다** (80세션에 뒤집었다). 앞서는 PC 가 행이라
   **한 칸 안에 건수 다른 판들이 섞여 있었고**(2번 칸에 1,513·1,496·1,493건이
   나란히 있었다) 상한까지 남은 시간은 두 PC 다 표 밖에 있었다. **꼴이 다르면
   「견주지 마라」 는 문장으로 안 막힌다** — 같은 항목이 같은 차례로 서야 한다.

   | 무엇 | **1번 PC** | **2번 PC** |
   |---|---|---|
   | CPU · RAM | Core Ultra 7 258V · 8코어 8스레드 · 31.5 GB | Ryzen 5 4500U · 6코어 6스레드 · 7.5 GB |
   | `auto` 가 고른 일꾼 | **8** | **6** |
   | 잰 건수 | **1,828건** (합 1,828 · passed 1,824 · xfail 4 · skip 0 · S224 — 첫 판 초록 · 깨끗한 판) | **1,832건** (갈래 넷 합 = 수집 1,832 · xfail 4 · skip 0 · S225 — ① 이 그 판 기록 탓으로 1 failed · 기록을 고친 뒤 ① 을 다시 돌았다) · 앞: 1,819건 (passed 1,815 · S214) |
   | 깨끗한 판 수 · 소요 | **1판 · 5분 01초** (S224 · 1,828건 · pytest 301.26초 · **깨끗하고 플래그 없는 마지막 판**) · 앞: 5분 06초 (S222 · 1,825건 · pytest 306.67초 · 사람이 `site-energy` 를 멈춘 첫 판) · S219 5분 12초(pytest 312.05초)는 깨끗했으나 **`-p no:logging` 이 붙어 명령이 달라** 견주지 않는다 · S220 은 플래그 없이 돌았으나 **오염된 판**이다(아래 칸) | **갈래 넷 한 판 · 합 20분 49초** (S225 · 1,832건 · **플래그 없다** · 남의 python 을 판 앞 · 갈래 사이 셋 · 뒤에서 세어 다 0 · **첫 판 초록이 아니다** — ① 이 그 판 기록 탓으로 한 건 빨갰다 · ① 5분 02초 · ② 7분 06초 · ③ 1분 42초 · ④ 6분 59초) · 앞: **갈래 넷 한 판 · 합 19분 02초** (S214 · 1,819건 · `--durations=0` · 남의 python 을 다섯 자리에서 세어 다 0 · **건수가 넷 많은데 앞 판보다 28초 짧다 — 한 판씩이라 까닭으로 단정하지 않는다** · **② 만 두 판 돌았다** — 첫 판 6분 37초에 `test_slides.py` 둘이 이 판 탓으로 빨갰고 재판 6분 44초가 264 passed 다) · 앞: **갈래 넷 한 판 · 합 19분 30초** (S213 · 1,815건 · 같은 잣대로 깨끗한 판) · 그 앞: **갈래 넷 한 판 · 합 19분 48초** (S212 · 1,811건 · 같은 잣대로 깨끗한 판) · 통째 실행은 **1판 · 8분 06초** (114세션 · 1,631건) |
   | 오염된 판 (견주지 않는다) | **6분 05초** — 1,823건 · 지시서 명령 그대로인데 회귀 앞 2 가 `site-energy` pytest 였다 (S221 4-3 · pytest 365.59초 · 도구 합 2,041.83초 · 가장 긴 단계 232.74초) · 앞: **6분 02초** — 1,821건 · 지시서 명령 그대로인데 회귀 앞 2 · 뒤 2 가 `site-energy` pytest 였다 (S220 4-3 · pytest 362.71초 · 도구 합 2,064.17초 · 가장 긴 단계 235.06초) · 앞: **5분 31초** — 수집 1,784건인데 회귀 뒤 `site-energy` pytest 가 떴다 (S204 5-3 · 도구 합 1,659.5초) · 앞: **5분 26초** — 수집 1,762건(xfail 1)인데 `site-energy` pytest 가 뒤 2분 36초에 붙었다 (S179 6-2 · 도구 합 1,697.5초 · 가장 긴 단계 203.7초) · 앞: 4분 00초 — 같은 1,752건인데 `cdu-simul` pytest 가 앞뒤로 돌았다 (S169 4-1) | **9분 22초** — 같은 1,631건인데 `cdu-simul` pytest 가 함께 돌았다 (114세션). 앞: 1,513건 7분 21초 (77세션) |
   | **상한 8분까지** | **2분 59초 남는다** (S224 · 1,828건 · 깨끗하고 플래그 없는 마지막 판) · 1,825건 판 2분 54초 (S222) · 1,820건 판 2분 20초 (S218) · 1,823건 오염된 판 1분 55초 (S221 · 견주지 않는다) · 1,821건 오염된 판 1분 58초 (S220 · 견주지 않는다) · 1,820건 플래그 판 2분 48초 (S219 · 견주지 않는다) · 1,819건 판 2분 46초 (S217) · 1,805건 판 2분 21초 (S210) · 1,797건 판 2분 36초 (S208) · 1,794건 판 2분 35초 (S207) · 1,785건 판 2분 33초 (S205) · 1,783건 판 2분 29초 · 3분 05초 (S203 · S202) | **갈래마다 본다** (S225 · 1,832건 · 한 판 · 플래그 없다) — ① 엔진 2분 58초 · **② 산출물 54초 남는다** · ③ 통합 6분 18초 · ④ 화면 1분 01초 — **② 가 가장 적게 남기고 앞 판보다 22초 줄었다(까닭은 안 갈랐다)**. 앞: (S214 · 1,819건 · 한 판) — ① 엔진 **3분 46초 남는다** · ② 산출물 **1분 16초 남는다** · ③ 통합 6분 32초 남는다 · ④ 화면 **1분 24초 남는다**. **넷 다 안쪽이고 ② 가 가장 적게 남긴다** — 앞 판(S213 · 1,815건)은 **②④ 가 나란히 1분 15초**였다. 그 앞(S212 · 1,811건)은 ① 3분 26초 · ② 1분 18초 · ③ 6분 20초 · ④ **1분 08초** 남았다. 통째 실행은 **6초 넘겼다**(114세션) |
   | 무엇이 먹나 (도구 합 / 벽시계) | **S214(1,819건 · 갈래 넷 · 깨끗한 판 · `--durations=0`) — 도구 합 4,016.11초 / 벽시계 합 1,141.62초(19분 02초) · 일꾼 6.** 갈래마다 — **① 엔진** 708.55초 / 254.20초 · 가장 긴 61.93초(`test_ess_cost.py` call · **24.4%**) · **② 산출물** 1,820.27초 / 403.59초 · **가장 긴 397.40초**(`test_casestudy.py` 모듈 픽스처 setup) · **그것이 ② 벽시계의 98.5% — 일꾼을 늘려도 안 준다** · **③ 통합** 371.38초 / 88.11초 · 가장 긴 21.43초(24.3%) · **④ 화면** 1,115.91초 / 395.72초 · 가장 긴 167.66초(`test_ui_screen.py` call · **42.4%** 라 일꾼을 늘리면 준다). 파일별로는 `test_ui_screen` 1,100.6초(④ 의 98.6%) · `test_casestudy` 795.7초(② 의 43.7%) · `test_base_fee_basis_words` 715.1초(39.3%) · `test_integration` 371.4초(③ 의 100%) · `test_ess_cost` 338.6초(① 의 47.8%). **숨은 단계 3,896개**(많아야 19.5초). 전표는 `PROCEED.md` S214 절 5-2·5-3. **앞 판과 건수가 넷 다르다 — 방향만 본다: 도구 합 −84.9초에 벽시계 −28초다.** **S213(1,815건 · 갈래 넷 · 한 판 · 깨끗한 판 · `--durations=0`) — 도구 합 4,100.98초 / 벽시계 합 1,170.37초(19분 30초) · 일꾼 6.** 갈래마다 — ① 713.56초 / 272.68초 · ② 1,792.39초 / 405.36초(**가장 긴 398.67초 setup · 98.3%**) · ③ 436.54초 / 86.49초 · ④ 1,158.49초 / 405.84초(가장 긴 170.04초 · 41.9%). **숨은 단계 3,859개.** 전표는 `PROCEED.md` S213 절 5-3. **S212(1,811건 · 갈래 넷 · 한 판 · 깨끗한 판) — 도구 합 3,963.51초 / 벽시계 합 1,187.57초(19분 48초) · 일꾼 6** (① 723.17 / 273.95 · ② 1,605.17 / 402.33 · ③ 441.45 / 99.63 · ④ 1,193.72 / 411.66 · 숨은 단계 3,844개 · 전표는 `PROCEED.md` S212 절 5-3) |
   | `-n 4` 소요 | **모름 — 안 쟀다** | 1,496건 9분 41초 |
   | `auto` 최대 RSS | **2,944 MB** (정점 3,115 · 전용 6,989) | **2,270 MB** |
   | `auto` 가용 최저 (시작값) | **17,611 MB** (20,120 에서 · −12%) | **671 MB** (2,440 에서 · −72%) |
   | `auto` 하드폴트 | **859쪽 / 176초** (초당 4.9) | **27,202쪽 / 402초** (초당 68) |
   | `auto` 커밋 최고 / 한도 | **22,052 / 34,315 MB (64%)** | **12,383 / 13,429 MB (92%)** |
   | `-n 4` 최대 RSS | **모름** | **1,785 MB** |
   | 잰 세션 | **소요 S224** (1,828건 · 깨끗한 판 · `--durations=0` · 가장 긴 단계 194.63초 · 도구 합은 안 냈다) · 앞: **S222** (1,825건 · 깨끗한 판 · 가장 긴 단계 195.37초) · 그 앞 **S221** (1,823건 · **오염된 판** · 도구 합 2,041.83초 · 가장 긴 단계 232.74초) (그 앞 S220 은 1,821건 오염된 판 · 도구 합 2,064.17초 · S219 는 1,820건 플래그 판 · 도구 합 1,831.10초 · S218 은 1,820건 · S217 은 1,819건 · S210 은 1,805건 · S208 은 1,797건 · S207 은 1,794건 · S205 는 1,785건 · S203 · S202 는 1,783건) · 메모리 79세션 (1,515건 판) | **소요 · 갈래별 S225** (1,832건 · 갈래 넷 한 판 · 플래그 없다 · 파일별 · 메모리는 안 쟀다) · 앞: **소요 · 갈래별 · 파일별 소요 S214** (1,819건 · 갈래 넷 한 판 · `--durations=0` · 메모리는 안 쟀다) · 앞: S213 (1,815건 · 갈래 넷 한 판) · S212 (1,811건 · 갈래 넷 한 판) · 소요 114세션 · 67세션 (메모리 · 1,496건 판) · 파일별 소요 S171 (1,758건 · 갈래 넷) · 갈래별 소요 S173 (S172 판을 떴다 · 1,758건 17분 16초) |

   **건수가 다른 옛 판은 표에서 뺐다** — 견줄 수 없는 것을 나란히 두지 않는다.
   1번 PC 기록으로만 남긴다: **1,825건 5분 06초 (S222 · 깨끗한 판 · pytest 306.67초)** · **1,820건 5분 40초 (S218 · 깨끗한 판 · pytest 340.38초)** · **1,819건 5분 14초 (S217 · 깨끗한 판 · pytest 313.12초)** · **1,805건 5분 39초 (S210 · 깨끗한 판 · pytest 337.8초)** ·
   **1,797건 5분 24초 (S208 · 깨끗한 판 · pytest 322.2초)** ·
   **1,794건 5분 25초 (S207 · 깨끗한 판 · pytest 323.3초)** ·
   **1,785건 5분 27초 (S205 · 깨끗한 판 · pytest 325.8초)** ·
   **1,783건 5분 31초 · 4분 55초 (S203 · S202 · 깨끗한 두 판 ·
   pytest 329.2 · 293.3초)** · **1,781건 4분 58초 · 5분 18초 · 4분 54초 · 5분 18초
   (S201 · S200 · S199 · S198 · 깨끗한 네 판 · pytest 296.7 · 316.8 · 292.4 · 318.1초)** ·
   **1,752건 3분 43초 (S170 · 깨끗한 판 · 상한까지
   4분 17초)** · 1,757건 4분 58초 (S170 4-1 · 깨끗한 판 · 수집 1,758 · xfail 1) ·
   **1,748건 3분 39초 · 3분 41초 (S168·S169 · 깨끗한
   두 판 · 수집 1,749)** · 1,748건 4분 00초 (S168 · 오염 · 그 앞 판 3분 29초도 오염) ·
   **1,681건 3분 58초 · 3분 56초 (136·137세션 ·
   깨끗한 두 판)** · **1,565건 2분 55초 (93세션)** ·
   1,515건 2분 45초~3분 00초 (79세션 세 판).
   2번 PC 기록으로만 남긴다: **1,513건 6분 50초 (77세션 · 깨끗한 판)** ·
   1,496건 6분 37초·6분 47초 (67세션 두 판) ·
   1,493건 7분 45초 (66세션) · `-n 4` 1,496건 9분 22초 (65세션).

   **메모리 넉 줄은 79세션 1,515건 판의 값이다.** 93·136·S168 이 소요만 다시 쟀다 —
   그 판들에서 메모리를 안 쟀으므로 옮겨 적지 않는다.

   49세션이 적어 둔 「1,379건 2분 53초」 는 **어느 PC 값인지 기록이 없어 지웠다.**
   짐작해 채우지 않는다.

   **1번 PC 를 어떻게 쟀나** (79세션). 함정이 셋이라 적어 둔다 — 다음에 2번 PC
   를 다시 잴 때도 같은 자리에 빠진다.

   - **`.venv\Scripts\python.exe` 는 4.1 MB 짜리 런처 스텁이다.** 실제 작업은
     **자식 프로세스**가 하고 그 `ExecutablePath` 는 base python
     (`AppData\Local\Programs\Python\Python312\python.exe`) 이다. venv 경로로만
     거르면 **일꾼 아홉이 통틀어 37 MB** 로 나온다. 스텁과 **그 자식**을 함께 센다.
   - **`Win32_Process.WorkingSetSize` 는 프로세스가 뜬 무렵 값에 얼어 있다.**
     `Get-Process` 의 `WorkingSet64` 가 살아 있는 값이다.
   - **`Win32_OperatingSystem.FreePhysicalMemory` 를 쓰지 않는다.** 대기 목록을
     빼고 세어 여유를 과소평가한다 — `Win32_PerfRawData_PerfOS_Memory` 의
     `AvailableMBytes`(= `\Memory\Available MBytes`) 가 정직한 수다.

   `psutil` 은 넣지 않았다. 위 셋을 피하면 PowerShell 만으로 충분하다.

   **`-n auto` 를 유지한다 — 메모리를 재고 정했다** (67세션). 43세션이 「여유
   메모리가 절반이 된다」 고 적었고 66세션이 **소요만 보고 뒤집었다.** 이제
   양쪽이 다 있다.

   **아래 넉 줄은 모두 2번 PC 값이다** (68세션에 PC 를 붙였다). 위 규칙이
   소요만 두고 한 말이 아니다 — **메모리도 PC 를 빼고 적지 않는다.**

   - **43세션 말이 맞다. 오히려 더 준다** — **2번 PC** 에서 가용이
     2,440 → **671 MB**(72%)까지 떨어진다. **2번 PC** 의 `-n 4` 보다
     최대 RSS 가 **485 MB(+27%)** 많다.
   - **그런데 그것이 기준이 아니다.** 기준은 **스왑이나 실패로 이어지느냐**다.
     **2번 PC** 에서 하드폴트가 402초에 **27,202쪽(초당 68쪽)** 뿐이라
     **스왑이 아니고**, 1,496건이 두 판 다 통과했다.
     **여유가 줄어도 남으면 문제가 아니다.**
   - **2번 PC** 가 얻는 것은 **179초(−31%)** 다. 그래서 유지한다.
   - **다만 2번 PC 는 커밋이 최고 12,383 / 13,429 MB (92%) 까지 간다.**
     그 PC 에서 시험이 도는 동안 **무거운 것을 함께 띄우지 않는다.**

   **위 넉 줄을 덮어쓰지 않는다 — S171 값은 짝이 다르다.** 위는 67세션의
   **앞단 `-n auto` 1,496건** 값이고, 아래는 S171 이 **갈래 넷으로 나눠 돌린
   1,758건** 값이다. 같은 2번 PC 지만 **견주는 자리가 아니다.**

   - **커밋이 한도에 닿는다** — 마감 판 전체가 **13,414 / 13,429 MB (99.9%)**
     로 **15 MB** 남았다. 67세션의 92% 보다 높다.
   - **하드폴트가 창마다 여덟 배 갈린다** — 깨끗한 ④ 화면이 **초당 95쪽**인데
     깨끗한 ① 엔진 창은 **초당 778쪽**이다.
   - **가른 것은 시작 가용이다** — ① 은 **1,056 MB** 에서 시작해 241 MB 까지
     갔고, ④ 는 **1,937 MB** 에서 시작해 586 MB 까지 갔다.
   - **「스왑이나 실패로 이어지느냐」 로는 아직 안 넘었다 — 1,758건이
     통과했다.** 실패로 오독하지 마라. 다만 「시험이 도는 동안 다른 것을
     띄우지 않는다」 는 규약은 더 굳었다.

   **1번 PC 도 `-n auto` 를 유지한다 — 79세션에 재고 정했다.** 다만 **근거가
   2번 PC 와 다르다.** 그 PC 는 「여유가 줄어도 남는다」 로 버틴 자리인데,
   1번 PC 는 애초에 **줄지를 않는다** — 가용이 20,120 → **17,611 MB**(−12%)
   로 그칠 뿐이고 하드폴트가 176초에 **859쪽(초당 4.9쪽)** 이라 스왑 근처도
   아니다. 커밋도 최고 **22,052 / 34,315 MB (64%)** 라 2번 PC 의 92% 와 다르다
   — **이 PC 에서는 시험이 도는 동안 다른 것을 띄워도 된다.**

   **두 PC 의 값을 견주지 않는다.** 사양이 다르다 — 2번은 Ryzen 5 4500U
   6코어·RAM 7.5 GB, 1번은 Core Ultra 7 258V 8코어·RAM 31.5 GB 다. 일꾼 수도
   6과 8로 다르므로 **RSS 를 나란히 놓는 것 자체가 뜻이 없다.** 각자의 값을
   각자의 칸에 적는다.

   **갈림길 — 앞단 전체 실행이 8분을 넘는 PC 에서는 갈래별로 나눠 돌린다**
   (`PROCEED.md` 「pytest 분할 실행」 의 갈래 넷). **2번 PC 는 114세션에 바깥으로
   나갔다 — 깨끗한 판에서 1,631건 8분 06초로 상한을 6초 넘겼다**(전후로 다른
   프로젝트 python 0개를 확인했다). **다음 세션부터 이 PC 에서는 갈래별로 나눠
   돌린다.** 77세션의 1,513건 6분 50초는 **건수가 다른 판**이다 — 그때는 70초
   남았고, 시험이 118건 늘면서 76초를 더 먹었다. `-n 4` 로는 1,496건에서도
   **9분 41초**였다.

   **그 PC 에서 다음에 밀리는 것은 ② 산출물 갈래다** (S171). 1,758건에서
   갈래 넷이 **18분 54초 / 19분 32초**(두 판 · 넷을 따로 돌린 합)인데
   **가장 긴 ② 가 6분 32초 · 6분 34초로 상한 8분까지 1분 26초밖에 안 남는다.**
   그리고 **그 벽시계의 96.5%가 단일 setup 한 단계**(`test_casestudy.py` 의
   모듈 픽스처 `study`)라 **일꾼을 늘려도 안 줄어든다** — 케이스가 몇 벌 더
   붙으면 그 갈래를 다시 나눠야 한다. **여덟 갈래 판 가운데 깨끗한 것은 둘뿐이다** —
   그 PC 는 다른 프로젝트 python 이 수시로 붙는다.

   **같은 1,758건을 S172 가 다시 돌렸다 — 합이 17분 16초다** (S173 2-1 이
   S172 6-1 에서 떴다 · 일꾼 6). **위 S171 값을 덮어쓰지 않는다 — 같은 건수
   두 판을 나란히 둔다.**

   | 갈래 | 건수 | 소요 | 상한 8분까지 |
   |---|---:|---:|---:|
   | ① 엔진 | 1,098 | **5분 00초** | 3분 00초 남는다 |
   | ② 산출물 | 247 | **6분 47초** | **1분 13초 남는다** |
   | ③ 통합 | 65 | **1분 51초** | 6분 09초 남는다 |
   | ④ 화면 | 348 | **3분 38초** | 4분 22초 남는다 |
   | **합** | **1,758** | **17분 16초** | — |

   - **② 가 S171 마감(6분 34초)보다 13초 길다.** 그 갈래에
     `test_base_fee_basis_words.py` 가 있고 S172 가 그 파일을 고쳤다. **까닭은
     안 갈랐다** — `--durations` 를 안 붙였다. 그래서 **S171 이 본
     「② 벽시계의 96.5%가 단일 setup」 이 S172 판에서도 그대로인지 못 본다.**
   - **오염 딱지 — 「깨끗한 판」 이라 단정하지 않는다.** `Get-CimInstance
     Win32_Process -Filter "Name='python.exe'"` 를 **① 첫 판 앞과 ① 재판 뒤에
     각각 0** 으로 봤을 뿐 **갈래 사이는 안 쟀다** (S172 6-1). python 밖
     (`claude`·`msedge` 따위)도 안 쟀다.
   - **S171 19분 32초와 S172 17분 16초의 차 2분 16초가 오염 몫인지는 안 갈린다.**
     S171 판은 여덟 갈래 가운데 둘만 깨끗했고 S172 판은 갈래 사이를 안 쟀다 —
     **어느 쪽도 깨끗한 판이 아니므로 그 차를 오염에 돌릴 근거가 없다.**
     그 사이에 S172 가 xfail 하나를 보통 시험으로 갈았으나(`1,757 passed ·
     1 xfailed` → `1,758 passed · 0 xfailed`) **수집 건수가 같다.**
   - **메모리는 S172 가 안 쟀다.** 위 넉 줄의 S171 값(커밋 최고
     **13,414 / 13,429 MB**)이 여전히 이 PC 의 갈래 분할 판 유일한 값이다.
     **덮어쓸 것이 없다.**

   **1번 PC 는 안쪽이지만 여유가 줄었다** — 136세션 3분 58초 · 137세션
   **3분 56초**(둘 다 1,681건 · 깨끗한 판)다. 상한 8분까지 **4분 04초** 남는다.
   **S168 에 1,748건 3분 39초(깨끗한 판)로 4분 21초 남는다** — 건수가 67건
   느는데 17초 줄었다. S168 은 까닭을 안 갈랐다(`--durations` 를 안 붙였다).
   **S169 가 갈랐다 — 준 것은 파일 쪽이 아니다.** 같은 1,748건 3분 41초 판에서
   S137 과 맞대니 도구 합이 **+8.3초** 늘었다. 준 것은 격차 쪽이다(아래).
   **S170 에 1,752건 3분 43초(깨끗한 판)로 4분 17초 남는다.** 같은 판 마감(4-1)에는
   **1,757건(수집 1,758 · xfail 1) 4분 58초(깨끗한 판)** 로 **3분 02초** 남는다 — 시험이 여섯
   늘면서 **75초**가 늘었다. 그 여섯 가운데 둘이 앱을 띄워 네 산출물을 뜨는 시험
   (`test_base_fee_basis_words.py` · 도구 합 약 96초 — 오염된 둘째 판 값)이다. 표는 0절 판
   (1,752건 · `--durations` 를 붙인 깨끗한 판)으로 두었다 — 4-1 판은 `--durations` 가 없어
   「무엇이 먹나」 를 못 채운다. **75초가 어느 일꾼 꼬리에서 났는지는 안 갈랐다.**
   **S198 에 1,781건 5분 18초(깨끗한 판)로 2분 42초 남는다** — S170 0절(1,752건
   3분 43초)에서 **건수가 29 느는 동안 95초**가 늘었다. **까닭은 안 갈랐다** —
   `--durations=25` 만 붙여 도구 합을 못 냈다. 다만 **가장 긴 단계가
   205.1 → 196.52초로 줄었는데 벽시계가 늘었으므로 꼬리 쪽이다.**
   S170 4-1 판(1,757건 4분 58초)과 맞대면 24건에 20초다.
   **S199 에 같은 1,781건 4분 54초(깨끗한 판)로 3분 06초 남는다** — S198 보다
   24초 짧다. 까닭은 안 갈랐다(S198 판에 도구 합이 없다). S170 0절 판과 맞대면
   도구 합이 +51.2초인데 격차가 33.9 → 97.8초로 커졌다 — **늘어난 몫이 파일보다
   격차 쪽이다.** 건수가 달라 견주는 값이 아니라 방향만 적는다.
   **S200 에 같은 1,781건 5분 18초(깨끗한 판)로 2분 42초 남는다** — S199 보다
   24초 길다. 도구 합은 +0.33초로 거의 같은데 가장 긴 단계가 182.60 →
   204.83초(+22.23초)로 늘었다 — **까닭으로 단정하지 않는다**(한 판씩이다).
   **S201 에 같은 1,781건 4분 58초(깨끗한 판)로 3분 02초 남는다** — S200 보다
   20초 짧고 S199 보다 4초 길다. 가장 긴 단계가 185.71초로 다시 짧고 도구 합은
   1,600.59초로 두 판보다 33초쯤 크다 — 역시 한 판이라 방향만 적는다.
   **S202 에 1,783건 4분 55초(깨끗한 판)로 3분 05초 남는다** — 새 못 둘(표본 벌
   조합 둘씩 · 한 판 3초대)이 붙은 판이다. 도구 합 1,565.29초 · 가장 긴 단계
   181.53초 · 격차 98.9초로 S199 · S201 쪽에 가깝다 — 건수가 달라 방향만 적는다.
   **S203 에 같은 1,783건 5분 31초(깨끗한 판)로 2분 29초 남는다** — 시험도 코드도 안 고친
   판인데 S202 보다 36초 길다. 도구 합 1,636.27초 · 가장 긴 단계 212.64초 · 격차 126.1초로
   S200 쪽에 가깝다 — 한 판이라 방향만 적는다. **이 절에 적힌 1번 PC 깨끗한 판 가운데 가장 길다.**
   **S205 에 1,785건 5분 27초(깨끗한 판)로 2분 33초 남는다** — 새 못 둘(S204 87초대 · S205
   7초대)이 붙은 판이다. 도구 합 1,822.94초로 가장 크고 가장 긴 단계 206.50초 · 격차 99.4초다 —
   건수가 달라 방향만 적는다.
   **S207 에 1,794건 5분 25초(깨끗한 판)로 2분 35초 남는다** — 새 못 둘(인자 넷 · 합 5.74초)이
   붙은 판이다. 도구 합 1,730.69초 · 가장 긴 단계 207.12초 · 격차 108.5초 —
   **건수가 아홉 많은데 벽시계는 S205 보다 2초 짧다.** 한 판씩이라 **까닭으로 단정하지 않는다.**
   **S208 에 1,797건 5분 24초(깨끗한 판)로 2분 36초 남는다** — 새 못 둘(인자 셋 · 합 2.51초)이
   붙은 판이다. 도구 합 1,788.12초 · 가장 긴 단계 211.26초 · 격차 100.1초 —
   **건수가 셋 많은데 벽시계는 S207 보다 1초 짧고 도구 합은 57.4초 크다.** 한 판씩이라
   **까닭으로 단정하지 않는다.**
   **S210 에 1,805건 5분 39초(깨끗한 판)로 2분 21초 남는다** — 새 못 넷(인자 넷 · 합 1.34초)이
   붙은 판이다. 도구 합 1,767.56초 · 가장 긴 단계 220.25초 · 격차 118.1초 —
   **건수가 여덟 많은데 벽시계는 S208 보다 15초 길고 도구 합은 20.6초 작다**(격차가 18.0초 커졌다).
   한 판씩이라 **까닭으로 단정하지 않는다.** **이 절에 적힌 1번 PC 깨끗한 판 가운데 상한까지
   가장 적게 남긴 판이다.**
   **S217 에 1,819건 5분 14초(깨끗한 판)로 2분 46초 남는다** — 새 못 0(있는 못 하나를 넓혔다)인 판이다.
   도구 합 1,818.44초 · 가장 긴 단계 202.03초 · 격차 85.8초(pytest 313.12 − 도구 합 ÷ 8) — **건수가 열넷 많은데 벽시계는 S210 보다
   25초 짧다.** 그 사이 2번 PC 다섯 판과 오염된 S216(6분 18초)이 끼었다 — 한 판이라 **까닭으로 단정하지 않는다.**
   **S218 에 1,820건 5분 40초(깨끗한 판)로 2분 20초 남는다** — 새 못 하나(앱을 안 띄운다 · 0.67초)가 붙은 판이다.
   도구 합 1,986.45초 · 가장 긴 단계 221.25초 · 격차 92.1초(pytest 340.38 − 도구 합 ÷ 8) — **건수가 하나 많은데 벽시계는 S217 보다
   26초 길고 도구 합은 168.0초 크다.** 한 판이라 **까닭으로 단정하지 않는다.**
   **S219 에 같은 1,820건 5분 12초(깨끗한 판)로 2분 48초 남는다** — 새 못 0(있는 못 둘을 넓혔다)인 판이다.
   도구 합 1,831.10초 · 가장 긴 단계 208.42초 · 격차 83.1초(pytest 312.05 − 도구 합 ÷ 8) — **S218 보다 28초 짧다.**
   **그 판은 `-p no:logging` 이 붙어 명령이 달랐고 failed 1 · errors 2 가 섰다**(error 둘은 그 플래그가 `caplog` 를 끈 탓) — 견주지 마라.
   **S220 에 1,821건 6분 02초 — 오염된 판이다**(지시서 명령 그대로 · 첫 판 초록 · 회귀 앞 2 · 뒤 2 가 `site-energy` pytest) — 새 못 하나(0.03초)가
   붙은 판이다. 도구 합 2,064.17초 · 가장 긴 단계 235.06초 — **견주지 않는다.** 깨끗한 판 칸은 S218 그대로 둔다.
   **S221 에 1,823건 6분 05초 — 또 오염된 판이다**(지시서 명령 그대로 · 첫 판 초록 · 회귀 앞 2 가 `site-energy` pytest · 뒤 0) — 새 못 하나 두 인자가
   붙은 판이다. 도구 합 2,041.83초 · 가장 긴 단계 232.74초 — **견주지 않는다.** 깨끗한 판 칸은 S218 그대로 둔다.
   **S222 에 1,825건 5분 06초(깨끗한 판 · 플래그 없음)로 2분 54초 남는다** — 새 못 하나 두 인자(합 1.6초)가 붙은 판이고
   사람이 `site-energy` 를 멈춰 판 앞 · 회귀 앞 · 뒤가 다 0 이었다. 가장 긴 단계 195.37초 — **건수가 다섯 많은데 S218 보다 34초 짧다.**
   도구 합을 안 내 격차를 못 가른다 — 한 판이라 **까닭으로 단정하지 않는다.** 깨끗한 판 칸을 이 판으로 갈았다.
   **S224 에 1,828건 5분 01초(깨끗한 판 · 플래그 없음)로 2분 59초 남는다** — 새 못 하나 세 인자(합 약 1초 · 앱을 안 띄운다)가 붙은 판이고
   판 앞 · 회귀 앞 · 뒤가 다 0 이었다. 가장 긴 단계 194.63초 — **건수가 셋 많은데 S222 보다 5초 짧다.** 도구 합을 안 냈다 — 한 판이라
   **까닭으로 단정하지 않는다.** 깨끗한 판 칸을 이 판으로 갈았다.
   **93세션(1,565건 2분 55초)에서 건수가 7.4% 느는 동안 소요는 36% 늘었다** —
   79세션(1,515건 2분 45초~3분 00초) 범위 밖으로 나갔고, 건수만으로는 설명이
   안 된다.

   **S137 이 그 까닭의 절반을 값으로 봤다 — 소요를 정하는 것은 건수가 아니라
   어느 파일에 시험이 붙느냐다.** 위 표 「무엇이 먹나」 칸이 그 값이다: 건당이
   파일마다 **1,500배** 갈리고 상위 넷이 건수 25.9%로 도구 합의 73.8%를 먹는다
   (S169 판은 25.7% · 74.0%).
   **나머지 절반은 여전히 안 보인다** — 93세션에서 늘어난 116건이 어느 파일에
   붙었는지는 **기록에 파일별 건수가 없어** 못 가른다. **S137 도 상위 넷만
   남겼다** — S169 가 파일 35개 전표를 `PROCEED.md` S169 절에 남겼으니 다음
   판은 그것과 맞대면 파일마다 갈린다.

   **도구 합과 벽시계의 격차 51.1초(21.6%, S137) → 35.5초(16.0%, S169) 는 안
   갈랐다** — 일꾼 여덟이 저마다
   하는 수집·import 를 `--durations` 가 안 재고(빈 파일로 재 보니 여덟 기동에
   5.7초, 단일 2.9초), 나눔 꼬리와 섞여 있다. 짐작해 적지 않는다.

   이 PC 는 아직 갈래별 분할 밖이지만 **다음에 잴 때 남은 4분을 본다.**

   **S72~S76 의 7분 39초~8분 10초는 깨끗한 값이 아니다** — 다섯 판 다
   `cdu-simul` 이 함께 돌았다. **오염이 실제로 1분 가까이 먹는다.**
   넘긴 뒤에 판단하면 늦으므로 **잴 때마다 본다** (잣대는 2항).

   뒤로 보내는 것은 **갈래로 나눠도 10분을 넘길 일**뿐이다 — 기상 사전 취득 같은 것.
10. **세션을 마칠 때 돌고 있는 셸이 없는지 확인하고 보고한다.** 마무리 절차에 넣는다.

## 화면 문구

**화면 문구는 늘리지 않는다.**

- 새 안내를 붙이기 전에 **용어나 이름을 고쳐서 해결되는지** 먼저 본다.
  설명이 필요하다는 것은 대개 이름이 틀렸다는 뜻이다.
- 배경·근거·제도 설명은 **매뉴얼(`docs\MANUAL.md`)에 적는다.** 화면에 두지 않는다.
- 화면에 남기는 것은 둘뿐이다 — **없으면 입력을 못 하는 것, 없으면 결과를 오독하는 것.**
- 문구를 하나 더할 때는 **무엇을 뺄지 함께 정한다.**
  예산(본문 3줄·확인사항 3항목)이 그 장치다 — `tools\screen_budget.py`.

**고치기 전과 후에 `tools\screen_audit.py` 를 돌린다.** 화면에 실제로 그려진
문구를 전부 모아 규칙 위반(코드 식별자·내부 문서 번호·규정 이름 없는 조문·
맨 물결표)과 중복 후보를 센다. 수가 늘었으면 늘린 이유를 적는다.

**그 감사는 조건 넷이다. 덱 벌 전수는 `tools\deck_words.py` 가 본다** (S207 4절).
벌마다 앱을 띄워 **그려진 글자**를 뜨고(`--snap`), 낱말이 어느 벌 몇 자리에
서는지 세고(`--count`), 두 스냅을 줄 단위로 맞대(`--diff`) **갈린 줄**과
**수치 조각의 차**를 낸다 — 고치는 판마다 「금액이 한 원도 안 움직였다」 를
이것으로 센다. 판마다 새로 짓던 스크래치다(S192·S196·S200·S204·S205·S206).
**S212 에 화면 하나에서 네 산출물로 넓혔다** — 19벌을 다 뜨는 데 **2번 PC 에서
16분 남짓**이다(S212 982.0초 · S213 983.0초 · **그림 안 글자까지 담는 S225 두 판 1,080.7초 · 1,054.7초**) · **1번 PC 에서 8분 남짓**이다(S216 447.6초 · 486.3초 · **그림 안 글자까지 담는 S217 판 493.8초** · S218 판 555.1초 · S219 판 439.5초 · S220 판 508.8초 · S221 판 527.2초 · S222 판 466.9초 · S224 판 448.8초). 좁히려면
`--case` 를 쓰고, **담아 둔 스냅이 있으면 `--read` 로 읽어 안 뜬다.**

    .venv\Scripts\python.exe tools\deck_words.py --snap 앞.json
    .venv\Scripts\python.exe tools\deck_words.py --read 앞.json --count 도입 후
    .venv\Scripts\python.exe tools\deck_words.py --diff <온 경로 앞.json> <온 경로 뒤.json>

**세는 판에는 `--read` 를 쓴다** (S209 2절). `--count` 만 주면 **19벌을 다시 떠
5분을 버린다** — 담아 둔 스냅을 읽으면 **316.7초가 1.8초**다(같은 값 12벌 · 자리 12).
**`--diff` 는 아직 상대 경로를 스냅 자리에 안 댄다 — 온 경로를 적는다.**

## 코드 규약

- src 레이아웃. `pip install -e .`
- 타입힌트 사용. ruff, mypy 통과.
- **형이 없는 값은 `object` 가 아니라 `Any` 로 적는다.** `object` 는 「속성이
  하나도 없다」 는 뜻이라 쓰는 자리마다 오류가 나고 억제를 부른다 —
  70세션 mypy 142건의 뿌리가 이 한 낱말이었다. **남의 형(python-pptx·pandas
  같은 바깥 것)은 좁히지 않는다.** 좁히면 오히려 는다 — python-pptx `Slide` 로
  좁혔더니 `BaseShape` 때문에 90 → 110 으로 늘었다.
  **「모른다」 는 `Any`, 「없다」 가 `object`.**
- 모든 계산 함수에 단위테스트.
- **요금 계산은 순수 함수로 작성한다.** Streamlit 을 import 하지 않는다.
- 계산 로직과 UI 를 분리한다. UI 는 순수 함수를 호출만 한다.

- PowerShell 스크립트(`.ps1`)에 한글을 넣을 때는 **UTF-8 BOM** 으로 저장한다.
  BOM 없으면 Windows PowerShell 5.1 이 cp949 로 읽어 깨진다.

## 금지

- `pybuildingenergy` 사용 — 실측 부하를 쓰므로 불필요하다. 신축 부하 예측은 범위 밖이다.
- 결측값 자동 보간 — 기본은 미보간. 결측 구간은 계산에서 제외하고 명시한다.
- 요금 데이터 하드코딩 — 반드시 `data\tariff_*.json` 에서 읽는다.
  드롭다운 선택지도 이 파일에서 생성한다.
- **시점 없는 이름으로 `data\source\` 에 자료를 두는 것.** 이름 앞에
  시점(시행일·판 날짜)을 붙인다 — `2026-08-01_전기요금표(종합).pdf`.
  **시점이 없는 자료는 정본으로 쓰지 않는다.** 시점을 안 갈라 두면 **개정을
  결함으로 읽는다** (85세션 3절). 무엇이 있고 무엇이 정본인지는
  `data\source\SOURCES.md` 에 있다.

  **「본문 첫 장에서 읽는다」 는 규칙이 아니다** (88세션 1절에 고쳤다).
  85~87세션이 그렇게 읽어 세 번 같은 자리를 돌았다. 실제 모양은 이렇다 —
  **한전은 시행일이 여럿인 단가를 한 장에 담고 표지에는 하나만 적는다.**
  4-16 시행 요금표가 표지는 「4월 16일 시행」 인데 각주가 「그 외 종별
  ’26.6.1 시행」 이라 **6월 1일 시행 단가를 함께 싣고 있다.**
  **그러니 판은 표지가 아니라 표 안의 종별·선택요금 구성으로 가른다.**
  86세션이 「2026년도 8월 1일 시행」 이라는 이름을 「본문이 6월이니 이름이
  틀렸다」 고 읽었는데 **틀린 것은 그 읽기였다.**

  **약관·세칙은 또 다른 자리를 본다** (86세션 3절). **합본은 개정이 여러
  개 겹친다 — 시점은 표지가 아니라 「가장 나중 부칙의 시행일」 이다.** 85세션이
  세칙 합본의 표지 날짜 「2022.09.01」 을 읽어 이름을 그렇게 붙였는데, 그 문서의
  마지막 부칙은 약관 (2026. 5. 22) · 세칙 (2026. 5. 28) 이고 **둘 다 2026년 6월
  1일 시행**이다 — **네 해를 낡게 적었고, 그 바람에 「합본은 정본이 아니다」 라는
  틀린 결론이 세 문서로 번졌다.**

  **이름을 바꾸기 전에 실물을 끝까지 읽는다.** 표지 · 첫 장 · **부칙의 맨 끝**
  셋을 다 본 뒤에 정한다. 읽기 전에 옮기지 마라.
- 태양광을 기본 전제로 두는 설계 — 진단 단계는 설비 정보 없이 동작해야 한다.
  사용자가 파일만 올려도 결과가 나온다.
- 조합 절감액을 단순 합산 — 수단마다 요금을 다시 계산한다. 태양광이 사용량을 줄이면
  최적 선택요금이 바뀌고, ESS 가 피크를 낮추면 기본요금 기반이 달라진다.
- `reference\` 폴더 수정 — 읽기 전용 참조 코드다.
- 메모리 여유가 생겼다고 다중 시나리오를 동시 적재하지 않는다. 순차 처리를 유지한다.
- **파일 수정은 편집 도구로 한다. 셸에 문자열을 실어 파일을 고치지 않는다.**
  heredoc · `echo` · `printf` · `sed`/`awk` 치환이 **전부 같은 병**이다 —
  병은 heredoc 이 아니라 **문자열을 셸에 한 겹 더 태우는 것**이다.
  특히 **한글 · `\` · 백틱 · `$` · 정규식**이 든 문자열.
  뭉개진 자리는 **오류 없이 조용히** 지나가므로 돌려 보고도 모른다.

  - **33세션** — 같은 일로 **시험 둘이 죽었다.** 그 뒤 33세션 내내 몰랐다.
  - **65세션** — `\\` 가 하나로 뭉개져 `"tools\\run_casestudy.py"` 가
    `tools`+**CR**+`un_...` 이 되어 **치환이 안 먹었다**(오류도 안 났다).
    같은 날 `\\b` 는 **백스페이스로 박혀** `PROCEED.md` 를 오염시켰다.
  - **66세션** — **백틱이 명령 치환으로 먹혀** 문구 다섯 조각이 빈칸이 됐는데
    화면에는 `ok` 만 찍혔다. **heredoc 이 아니라서 옛 문구가 못 잡았다** —
    이름이 좁으면 규약 밖으로 샌다. 그래서 이름을 넓혔다.
  - **115·116세션** — `sed`(115) · heredoc(116) 으로 **두 판 연속 샜다.**
    **자국은 안 남았다** — 117세션이 두 커밋 범위와 그 스크래치를 값으로 훑어
    제어문자 0곳 · 탭 0 을 봤다. **안 걸린 것이지 안 한 것이 아니다.**
  - **S167** — 파일을 고친 것이 아니라 **재는 파이썬**을 heredoc 으로 태웠다가
    정규식이 틀린 값(**0줄**)을 조용히 냈다(S168 지시서가 적은 자리).
    **그래서 재는 스크립트·측정용 파이썬도 셸에 태우지 않는다** — 스크래치
    `.py` 파일로 만들어 `.venv\Scripts\python.exe` 로 돌린다.

  **스크래치 파일도 같다.** 검사는 **저장소 폴더 안만** 훑는데 스크래치는
  `%TEMP%` 아래라 그 대상 밖이다 — 거기 박힌 것은 **안 걸린다.**
  **걸리지 않는 것이지 안 박히는 것이 아니다.** 뭉개진 스크래치로 저장소를
  고치면 그때 들어온다.

  **글로만 있는 금지는 뜨지 않는 경고와 같다.** 검사는 `tools\scan_ctrl.py` 다 —
  제어문자가 박힌 자리를 **문자 단위로** 훑는다(`splitlines()` 로 세면 적게 센다).
  `tests\test_deployment.py` 가 매 시험에 돌리므로 새로 박히면 그 자리에서 걸린다.

## reference\ 폴더

기존 `pv_peak_cut` 코드다. **참조만 하고 수정하지 않는다.** 이식할 때는
`src\kwise\` 아래에 새로 작성하면서 타입힌트와 테스트를 붙인다.

| 파일 | 쓸 곳 |
|---|---|
| `mg_pv_core.py` | 4세션 — PV 계산 엔진 이식 |
| `mg_weather_openmeteo.py` | 4세션 — 기상 취득 이식 |
| `streamlit_app.py` | 1세션 — `parse_usage_datetime`, `read_uploaded_usage_file`, `match_usage_column` 참조<br>8세션 — `hourly_pattern_table` 참조 |

`parse_usage_datetime` 의 `24:00` 처리는 실측 데이터로 검증된 로직이다. 그대로 가져간다.
`mg_weather_openmeteo` 의 PowerShell 폴백은 프록시 우회용이다. Windows 에서는 동작하지만
`requests` 경로가 정상이면 쓰이지 않는다. 유지 여부는 4세션에서 판단한다.

## 세션 종료 시

작업을 마치면 `PROCEED.md` 에 아래를 추가하고 커밋한다.

- 완료한 항목
- 만든 파일 목록
- 미해결 항목과 그 이유
- **미해결 건수 대조 — 전 N건 → 후 M건, 늘어난 것과 줄어든 것을 각각 이름으로**
- 다음 세션이 알아야 할 결정 사항
- 돌고 있는 백그라운드 셸이 없는지 확인한 결과

이 기록이 다음 세션의 출발점이다. 빠뜨리지 않는다.

**건수 대조는 68세션에 붙었다.** 67세션이 「다음 작업」 칸을 갈아 끼우면서
60세션 항목 B 를 **소리 없이 지웠다** — 이 대조가 있었으면 그 자리에서
드러났다. 이미 쓰는 대조표와 같은 방식이라 새 장치가 아니다.
**수만 맞추지 말고 이름을 적는다** — 하나 늘고 하나 줄면 수는 그대로다.

**커밋 후 `git push` 로 원격에 올린다. 다른 PC 에서 이어받는다.**
올리지 않으면 그 PC 에만 남는다 — 다음 세션이 다른 PC 에서 열리면 없는 일이 된다.

### 같은 것을 두 자리에 적지 않는다 (S209 4절)

기록 절차 열둘을 항마다 「**못이 무나 · 다음 판이 읽나 · 사람이 읽나**」 로
가르니 **뺄 항이 하나도 없었다** — 열하나가 둘 이상에 걸린다. 깎을 것은
항이 아니라 **항 안에서 두 번 적는 자리**였다. 셋을 못 박는다.

- **「다음 작업」 칸에 「판치를 걷었다」 는 기록을 두지 않는다.** 그 칸은
  상한 3,000자를 못이 무는 자리인데(`tests\test_doc_counts.py` 의
  `test_다음_작업_칸은_상한을_넘지_않는다`) 거기에 **기록을 걷었다는 기록**이
  들어가 있었다. **걷은 이력은 세션 절과 세션 목록표가 쥔다.** 다음 판이
  그 칸에서 읽는 것은 **판치 둘**뿐이다. S208 이 그 메모 378자를 남겨
  여유가 12자였다 — 빼면 390자다.
- **`docs\HANDOVER.md` 1절의 「앞 값 —」 사슬은 두 판까지만 남긴다.**
  미해결·pytest·케이스·정적 네 칸이 S204 까지 거슬러 붙어 **19,472자**였다.
  **판마다의 값은 `PROCEED.md` 세션 절이 쥐고**(수집 건수만 26곳) PC 별
  소요는 9항 표가 쥔다 — 다음 판이 그 문서에서 읽는 것은 **지금 값과 앞
  값 하나**다.
- **HANDOVER 6절 못 표의 「이번 판에 되돌려 확인」 딱지는 다음 판이 걷는다.**
  새 딱지를 다는 일만 적혀 있고 **걷는 일이 어디에도 없었다** — S206 이 단
  딱지가 세 판을 살아남아 「확인 안 한 못」 이 **기계로 41 · 뜻으로 42** 로
  갈렸다. 딱지를 달 때 **앞 판 딱지를 함께 걷는다.**

**이 셋을 `docs\project\collaboration.md` 에 다시 적지 않는다.** 그 문서는
「왜 그렇게 하나」 를 쥐고(「갈아 끼우는 칸에 일을 얹지 않는다」 가 그 자리다)
**무엇을 어디에 적나는 이 절 하나**다. 지시서에도 안 적는다 — 지시서는
판마다 새로 쓰이므로 **다음 판이 읽는 자리가 아니다**(S207 4-4 가 같은
까닭으로 도구 부르는 자리를 이 파일에 두었다).
