# 텍스트 파일은 저장소에서 LF 로 고정한다 (ENVIRONMENT.md 2.5).
* text=auto eol=lf

# PowerShell 5.1 은 CRLF 를 기대한다.
*.ps1 text eol=crlf
*.bat text eol=crlf

# 실측 입력 데이터는 바이트를 그대로 보존한다. 줄바꿈 변환이 인코딩 판정을 흔들면 안 된다.
*.csv -text
*.xlsx binary
*.xls binary
